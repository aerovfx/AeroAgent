# 3-EXPL~2

---

So, now the download process is

complete and we have the archive file.

this zip. First, let's unzip that file

and then we can explore further.

\u003cbr />So, now once the extraction is complete, let's go ahead and open

this extracted folder and you'll find the same three folders over here.

\u003cbr />So, these are datasets which are

used for testing, training, and validation.

Since our training data is in this

folder called training, let's open that up.

and you will be able to find the name

of the classes this dataset belongs to.

Therefore, this contains photos

of apple, banana, radish, etc.

So, if over here we open up a specific folder like this apple

folder, you'll be able to find an image of an apple over here.

\u003cbr />And similarly with

this banana folder, let's open it up.

\u003cbr />And you should be able

to find an image of a banana over here.

So now that we have downloaded and unzipped this dataset, we need

to choose the classes that we're going to use to train our model.

Although we can to train our

model on all these classes.

But to train our first custom image classification

model, we're going to pick a couple classes here.

I'm going to pick this apple here, and then a

banana , and then we'll pick this watermelon.

Then we'll pick some vegetables, so

here we have an onion, so let's pick that.

Then let's select lemon and

finally let's select this potato here.

And once you have selected these classes, all we have

to do is to copy them and put them into a new folder.

So let's go back to our directory and here let's

create a new directory and let's call it dataset.

And now inside this dataset

directory, I'm going to copy the classes.

And that's it. So you can see we have copied

six directories from our training directory.

And these are the classes that we will train our model

to recognize, which are these fruits and vegetables.

So now that we have separated these directories, if you want to add some more images into each directory,

you can just copy the images from validation directory or the test directory and put them in here.

And if you want, you can also download some more images of apples, bananas, lemons, onions, potatoes, and watermelons

and put them into their respective folders to increase the number of images in your dataset.

I am saying this because the more images you have in

your dataset, the more accurate your model will be.

But for now, after getting these 6 directories, all we

need to do is create a zip file of the dataset directory.

So, what I am going to do here is right-click and

then add to archive. And then create a zip file.

And if you are on a Mac, you can just

compress this folder to get this zip file.

So, once we have this dataset.zip file, we

are ready to train our model on this dataset.