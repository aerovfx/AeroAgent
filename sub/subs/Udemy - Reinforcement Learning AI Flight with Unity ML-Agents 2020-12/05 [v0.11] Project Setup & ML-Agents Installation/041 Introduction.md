# Introduction

This section is a pretty big one.

What we're gonna do is create an entirely new Unity project.

We're gonna import M.L. agents to that project.

Then we're gonna import all our 3D assets and start building out a desert environment.

Once we've built our desert an environment we can place a race path in it and that race path will be

used to automatically generate a course with checkpoints in it.

Once we have a course we can build an agent an aircraft agent in particular and by the end of this section

you'll be able to fly an airplane around in your desert scene.