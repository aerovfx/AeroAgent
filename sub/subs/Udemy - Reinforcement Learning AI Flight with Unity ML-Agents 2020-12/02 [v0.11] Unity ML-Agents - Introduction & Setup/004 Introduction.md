# Introduction

In this section I'm going to show you how to get up and running with Unity MLA agents.

So we'll start with what to download and how to install it.

Then we will import it into a fresh Unity project.

Then we're gonna look at the examples and in particular the basic example we'll look at how the scene

is structured and how MLA agents works at a very high level.

Then we will train the basic agent and once it gets to a high performance state then we will save off

a neural network and then we can run inference in the scene.