# Project Clean-up & Install ML-Agents

In this video, we're going to clean up our project and install M.L. agents.

So once this is done, loading, it may take some time, especially if it's generating lighting, you'll

see some progress bars down here.

Make sure you wait until that's done before you start trying to modify things.

But this is a sort of template scene.

And we don't really need it.

So let's first create a new folder.

And we'll call this aircraft and this is where we're going to put all of our aircraft related stuff.

And inside here, we'll create a new folder called Scenes.

And then inside of the scenes folder, we will create a new scene.

Let's see scene and we'll call this desert.

And then you can open this up and then we'll go out to the assets folder and we'll clean up what we

don't need.

So the first thing you don't need is this read me file.

This is just for the template.

So we'll delete that.

And then why did that not do we go?

Delete that.

And then the example assets is basically all of those prefabs that were in that scene.

So we can also select the example assets and hit the delete key.

And then same goes with.

Well, let's leave the materials, that skybox we can use presets.

Probably not doing any harm.

The scenes, this sample scene, we don't don't need this.

So we can just remove this and we won't need the scripts either.

And then also tutorial info, we don't need tutorial info, so we'll delete that, too.

Now, the folder's a little cleaner, settings are these are some default settings for the different

quality levels for the universal render pipeline, so it's best to just leave those in there.

And now we're just left with our aircraft folder and a few minimal other things.

So that should make our project smaller.

And then we want to install M.L. agents.

This has gotten a lot easier than it used to be.

So go up into window package manager.

And you want to find M.L. agents and at the time of this recording.

It's under advanced and show preview packages to make sure you see all these previews.

Go about halfway down until you find M.L. agents and then make sure that you install version one point

zero point two.

You might have to expand it just to see which version.

Just to that, you're as close as possible to this project.

And honestly, if if it's on like one point zero point three or point four something, some very small

update.

Those were probably just bug fixes.

So those are safe.

But I would be cautious with anything that's like one point one or one point five or something like

that, because I don't know what will change between now and then.

So just install that version by clicking the install button.

And then this will take a moment to install and once it's ready, then we can take a look inside of

the packages folder and we'll be able to see what got imported.

OK, this appears to have installed correctly, but we just want to double check and make sure everything

came in correctly.

Make sure there's no errors in the console at this point.

There should be no reason you have any errors sometimes if there is yellow warning messages.

That's OK.

But just be wary of definitely wary of anything with a red error message.

And then we'll scroll down and we'll make sure that we have M.L. agents in this folder.

And as long as that's there, everything should have worked properly and we should be able to proceed

with the course.