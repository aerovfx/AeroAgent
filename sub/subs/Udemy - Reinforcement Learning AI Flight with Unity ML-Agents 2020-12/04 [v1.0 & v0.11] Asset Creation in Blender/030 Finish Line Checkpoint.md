# Finish Line Checkpoint

Now for our finish line I want to do something a little bit different.

Make it a little bit more obvious that the finish line is different from the rest of the checkpoints.

So I'm going to do the same idea but we're gonna make it so that it has 10 points instead of just five.

So first of all let me make sure I save this and we'll call this checkpoint and I will create a new

file.

Actually let's just do it in here.

Doesn't matter.

The finish line can go inside of here.

So I'm going to create a I'll just hide these two and I'm going to create a circle.

So we go to add mesh circle.

And this one's going to be 10 and I think we wanted to be quite a bit larger than this.

So let's double the size of it.

We'll do 40 and you can see here that the there's a point you know around each one quite obviously double

what we had before.

So that's good.

And then we go to the top view and I'm going to do the exact same thing to sort of cut out a piece that

I want to use.

So I'm gonna hit tab to go into edit mode.

I'm going to switch into vertex mode control are scroll up once and click control our scroll up once

and click OK.

So now I'm going to let's just select go into edge mode select these two edges and I'm gonna go to this

select menu and select or go to invert and that's going to select all the other ones X and delete edges.

Now I'm going to select both of these with shift select to go e and we're going to extrude it.

We want to hit y to lock it to the y axis.

And if you remember before we did about three meters if we hold down control that should be good again.

So we're just going to do three meters.

Now I need to extrude these two upwards so I'm going to switch into face mode and extrude with the E

key and extrude it by one and then I'll hit the A key to select everything.

And then we will set the z value to zero OK.

So now we have this here.

Now we can go into the let's do it a little differently this time rather than doing the the copy and

paste the duplicate version of it.

We're gonna use the array modifier that I intended to do originally.

So what we want to do basically is create a new object so hit tab to go back into object mode.

We're gonna do add and we're gonna add an empty.

And we'll do this plane axes here.

So this creates an empty object and all we want to do.

I'll go into the top view with the number pad seven we want to rotate this not around the Y or the x

axis but around the z axis the number of degrees that this point should be rotated so the way that we

do that is we go into here and we can actually do math in here so we'll do 360 degrees divided by 10.

Thirty six degrees probably could've done that in my head.

So now we're gonna use that as a rotator.

And the way we do that is we create this we go to this wrench modifier tab add modifier array.

OK now what this will do is it'll create a bunch of them in a row.

And what you can then do is if you click and drag this empty down into object offset uncheck relative

offset and then check object offset.

Then it will actually create them around this central point and it'll rotate them by that same amount

as that empty was.

So now we have the shapes that we're that we want and we can click apply and we've got almost exactly

what we need now.

So I'm going to rename this to finish finish line enter and then we want to view it from the top again

and shift.

Let's see.

Well to add mash and we'll add a cylinder and we'll put this at 10 and then let's see what's this gonna

be about 20 now.

Probably like wow let's try like 30 six yeah.

Good guess.

All right.

So thirty six will work.

That's good enough for me.

So we can click away and that is going to be our finish line collider

OK.

The last thing to do is hit a.

And we can rotate this hit are to rotate hit X to block it to the x axis and then type in 9 0 and then

we just need to do object apply rotation and now we've got the finish line and the finish line collider.

So I'll hide this empty and I will export I'll export FBI X we'll call this finish line and we still

got selected objects and experimental apply transform and then this one file export FBI X and we'll

do finish line collider.

All right.

So I'm going to save this as is.

And then we are done with our checkpoints and finish lines.

Now.