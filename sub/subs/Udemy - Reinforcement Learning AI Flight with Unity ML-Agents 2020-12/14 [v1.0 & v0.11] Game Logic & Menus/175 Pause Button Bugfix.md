# Pause Button Bugfix

So I found a fix for that error message and the solution was just to disable the inputs in aircraft

played out C yes I inserted this into the course earlier on so that you won't hit this issue but this

is an issue that I had while I was doing it.

So apparently if you just if you don't disable the pause input then it can be listening to an older

version of the pause input and then some weird things happen.

So good good practice to disable your inputs on destroy when you are working with the input system.