# RaceManager.cs_ GetAgentCheckpoint()

Let's define this function here so we can right click quick actions generate this method.

And this is going to return.

It's gonna be a private transform and the agent will just call it agent and what this does is get the

agents next checkpoints transform.

So we get the checkpoint or the transform of the next checkpoint of the agent and it returns the transform

of the next checkpoint.

The agent should go to

so we're going to return aircraft area checkpoints

aircraft statuses

agent.

So we're looking up the status for the agent and then we have to get the checkpoint index and then we

return the transform of that.