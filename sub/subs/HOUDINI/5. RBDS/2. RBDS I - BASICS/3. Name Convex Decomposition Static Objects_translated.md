# 3. Tên Dịch Convex Decomposition Static Objects

---

Được rồi, vậy là bây giờ chúng ta đã thấy rằng chúng ta có thể giải quyết vấn đề lõm lồi bằng cách sử dụng nút phân tách lồi.

Và chức năng của nó là biến một vật thể lõm thành nhiều vật thể lồi.

Một cách khác để hiển thị điều này là với nút chế độ xem đã được mở rộng.

Và ở đây bạn có thể thấy chiếc nơ bây giờ đã biến thành nhiều mảnh.

Bây giờ chúng tôi gặp phải một vấn đề.

Nếu chúng ta thực sự mô phỏng điều này, tất nhiên mô phỏng RBD sẽ không hiểu rằng hiện tại đây được coi là một đối tượng.

Vì vậy, khi bạn chạy cái này tất nhiên nó sẽ vỡ ra và vỡ thành nhiều mảnh một cách tự nhiên.

Và để giải quyết vấn đề này, chúng tôi giới thiệu một khái niệm mới gọi là thuộc tính tên.

Và đây thực sự là lý do tại sao phân tách lồi theo mặc định theo cách chúng ta sử dụng ở đây có một dấu chấm than nhỏ

bởi vì nó thực sự cho bạn biết các thuộc tính sau không được tìm thấy trong tên tệp.

Vì vậy, nút này thực sự mong đợi một thuộc tính tên sẽ được sử dụng.

Vì vậy, điều chúng tôi có thể làm là đặt cho mỗi phần một thuộc tính tên khác nhau.

Và điều này thậm chí còn có một nút gọi là tên và mục đích của nó là như vậy.

Và thường làm điều đó trước khi phân tách lồi để toàn bộ cung có một tên nhất định và sau khi chia nhỏ mỗi phần có cùng tên.

Và nút này tạo ra một thuộc tính mới và đây là thuộc tính nguyên thủy.

Vì vậy, khi nhìn vào các nguyên thủy, chúng ta có thể thấy ở đây có một thuộc tính mới tên là name, mặc định trống.

Vì vậy, chúng tôi phải đảm bảo rằng bạn nhập nội dung nào đó vào đây, chẳng hạn như bát.

Và bây giờ bạn có thể thấy mọi khuôn mặt nguyên thủy trong trường hợp này sẽ có thuộc tính tên này.

Và sau khi phân tách lồi điều đó vẫn đúng, mọi mặt của đối tượng của tôi ở đây đều có tên thuộc tính được đặt thành bát.

Và điều đó có nghĩa là người giải dấu đầu dòng bây giờ đã biết, ồ mọi thứ có cùng tên đều được coi là cùng một đối tượng.

Và nếu tôi chạy cái này bây giờ thì bạn có thể thấy nó tính toán nó như một.

Và đây là một khái niệm rất quan trọng đối với RBD và thuộc tính tên này thực sự quan trọng mà bạn có thể biết vì có một nút cho nó.

Nhưng chúng ta cũng có thể tự làm điều đó. Chúng ta chỉ cần nhớ rằng khi bạn thực hiện nó trực tiếp trên hình học thì bạn phải thực hiện nó trên các hình gốc.

Và như chúng ta đã thấy, chúng ta đặt tên cho nó chứ không phải một con số hay bất cứ thứ gì. Và tất nhiên điều đó có nghĩa là chúng ta đang nói về thuộc tính chuỗi.

Vì vậy, nếu bạn muốn tự thiết lập nó, điều mà bạn luôn có thể thực hiện với vex chẳng hạn như trên thuộc tính create, chỉ cần đảm bảo rằng đó là một chuỗi.

Và lần này chúng tôi đặt tên cho nó. Tôi không biết món ăn. Tôi không biết. Nó không thực sự quan trọng miễn là nó độc đáo.

Và hãy đảm bảo rằng chúng ta gọi tên cuộc tranh cãi của mình ngay bây giờ và tất nhiên bạn sẽ thấy cách này cũng hiệu quả.

Vì vậy, việc bạn tạo thuộc tính tên như thế nào không quan trọng miễn là nó ở đó và nó là một chuỗi có tên duy nhất trong đó.

Vì vậy, chúng tôi có thể sớm thực hiện việc này với thuộc tính sắp xếp của riêng mình khi bạn tạo, chúng tôi có thể thực hiện việc này với thuộc tính tên.

Nhưng thực ra chúng ta cũng có thể làm điều này với tổ hợp mà chúng ta đã thấy. Và trên thực tế, tính năng này đã được thực hiện theo mặc định vì có hộp kiểm thuộc tính tạo tên được bật theo mặc định.

Và nếu bạn nhìn vào thuộc tính tên ở đây, bạn có thể thấy nó thực sự đã tạo ra một thuộc tính tên với phần tên bằng 0 trong đó.

Và điều này là do ở trên đây có một tiền tố đầu ra cho mọi đối tượng mà nó tìm thấy về cơ bản nó sẽ đưa nó vào.

Vì vậy chúng ta có thể in đậm chẳng hạn và bây giờ tất nhiên tên thuộc tính sẽ khác nhau.

Và chúng ta thực sự sẽ sử dụng hợp ngữ khá nhiều vì một lý do khác là nó có thể giải quyết một số vấn đề nhất định cho chúng ta.

Và sau đó giả sử chúng ta hãy loại bỏ điều này, chúng ta muốn tạo nhiều phiên bản in đậm như trong ví dụ trước.

Vì vậy, chúng tôi lấy một bản sao, biến đổi và di chuyển một bản lên trên một chút và tạo một vài bản sao.

Thế đấy. Và nếu bạn chạy cái này ngay bây giờ thì tất nhiên điều gì sẽ xảy ra do sự phân tách lồi, tất cả những phần không có thuộc tính tên sẽ bị tách rời.

Không có gì đáng ngạc nhiên ở đây để giải quyết vấn đề này, chúng ta có thể sử dụng lại thuộc tính tên và chỉ đặt cho chúng cái bát tên.

Nhưng điều xảy ra bây giờ là vì tất cả chúng đều có cùng thuộc tính tên nên chúng không được coi là cùng một đối tượng.

Vì vậy, nó không hiểu rằng đó được cho là bốn bu lông vì chúng đều có thuộc tính tên giống nhau.

Vì vậy, những gì chúng ta có thể làm ở đây với sự trợ giúp của việc lắp ráp giống như chúng ta đã thấy trước đây khi chúng ta đóng gói một thứ gì đó đủ thông minh để tìm các đồ vật riêng biệt và đặt cho chúng một tên riêng.

Vì vậy, nếu chúng ta nhìn vào đây, chúng ta có số 0 đậm, sau đó chúng ta có số 0 đậm, đậm 2 và đậm 3 và điều này giải thích tại sao chúng ta có số 0 này trước đó cho một cái bát.

Vì nó tự động có thể gán tên cho nhiều đối tượng dựa trên tiền tố.

Và điều này bây giờ cho phép chúng ta sử dụng phân tách lồi sau đó và khi chúng ta chạy nó, mỗi đối tượng vẫn được coi là một đối tượng riêng biệt và sẽ va chạm một cách chính xác.

Bởi vì mỗi chiếc bát bây giờ đều có thuộc tính tên riêng để gắn liền với nhau. Và tất nhiên đây là cách tôi xây dựng các ví dụ trước đó bằng cách kết hợp phân tách lồi và tập hợp.

Vì vậy, như bạn có thể thấy, phân tách lồi cho phép bạn biến bất kỳ đối tượng lõm nào thành nhiều đối tượng lồi, điều này giúp bộ giải dấu đầu dòng RBD có thể tính toán chúng một cách chính xác và nhanh chóng.

Nhưng nếu bạn làm điều này, bạn sẽ gặp phải vấn đề là bây giờ bạn có nhiều mảnh và mẹo là sử dụng thuộc tính tên cho bộ giải dấu đầu dòng RBD để nhận ra chúng là cùng một đối tượng, ví dụ như chỉ sử dụng ghi chú tên.

Nhưng vấn đề là khi bạn có nhiều đối tượng thì kỹ thuật này không còn tác dụng nữa. Vì vậy, đây là nơi tập hợp thực hiện điều tương tự.

Nó gán một thuộc tính tên dựa trên đối tượng mà nó tìm thấy, sau đó được phân tách lồi và sau đó toàn bộ mọi thứ hoạt động chính xác và mỗi chiếc bát có tên riêng trong khi vẫn được chia thành các phần lồi.

Vì vậy, như bạn có thể thấy, toàn bộ mọi thứ bắt đầu trở nên phức tạp hơn một chút so với lúc ban đầu và tất nhiên chúng ta sẽ sử dụng kỹ thuật này trong một số video tiếp theo.

Vì vậy, bây giờ tôi hy vọng điều này hữu ích. Cảm ơn rất nhiều và hẹn gặp lại sau.