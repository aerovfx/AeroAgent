# 04 bộ dữ liệu trong python

---

Bây giờ chúng ta hãy nói về bộ dữ liệu.

Vì vậy, khi chúng ta nói về bộ dữ liệu, nó tương tự như danh sách của bạn trong python, được chứ.

Vì vậy, hãy tạo một dự án khác, một tệp khác và đặt tên nó là ví dụ về bộ dữ liệu.

Vì vậy, bất cứ khi nào chúng ta viết các bộ dữ liệu, các bộ dữ liệu cũng tương tự như danh sách nhưng chúng là loại bất biến

của danh sách.

Và bất cứ khi nào chúng ta nói về bất biến, điều đó đơn giản có nghĩa là bạn không thể thay đổi một phần của bộ dữ liệu.

Bây giờ để tạo một bộ dữ liệu, chẳng hạn như cách chúng tôi đã tạo danh sách.

Vì vậy, số bộ dữ liệu bằng nhau.

Bây giờ, bất cứ khi nào chúng ta biểu thị một danh sách, chúng ta biểu thị bằng dấu ngoặc vuông bên phải.

Nhưng bất cứ khi nào chúng ta biểu thị một bộ dữ liệu, chúng ta sẽ sử dụng dấu ngoặc tròn.

Ví dụ: giả sử tôi đang lấy các số như 10, 20, 30, 40, 50 và 60.

Và giả sử nếu tôi lấy các bộ dữ liệu tên bằng, giả sử, John, Tracy, Victor, Max và

Jason, được rồi.

Vì vậy, đây là danh sách.

Bây giờ, giả sử nếu tôi muốn in một dữ liệu, tôi sẽ nói in chỉ số num tuples là 2.

Và khi tôi thử thực thi mã này, bạn có thể thấy kết quả là 30.

Và giả sử nếu tôi nói tên in chỉ số bộ dữ liệu là 4, thì chúng tôi nhận được kết quả là Jason.

Đây là cách chúng ta truy cập các bộ dữ liệu ở đây.

Bây giờ có một cách nữa để truy cập một bộ dữ liệu và một danh sách, chẳng hạn, nếu tôi muốn

tên từ Tracy đến Max, tức là 1, 2, 3 dữ liệu gộp lại nên tôi có thể viết tên in

các bộ từ 1, 2, 3.

Và bây giờ nếu tôi thực hiện điều này, bạn có thể thấy, được rồi, bây giờ tôi đã có hai cái tên này.

Tôi sẽ sử dụng ở đây là 4.

Chúng ta có cả ba cái tên phải không?

Vì vậy, đây cũng là cách chúng ta có thể lập chỉ mục, truy cập các bộ dữ liệu của mình.

Bây giờ nếu tôi thử, chẳng hạn, nếu tôi thử, giả sử chỉ số bộ dữ liệu là 1 bằng 90.

Và nếu tôi thực hiện điều này, bây giờ bạn có thể thấy chúng tôi đã gặp lỗi, đối tượng tuple không hỗ trợ

phân công mục.

Điều đó có nghĩa là bất cứ khi nào chúng tôi cố gắng cập nhật một bộ dữ liệu, chúng tôi không thể, điều đó có nghĩa là chúng tôi không thể cập nhật chúng

vì các bộ dữ liệu là bất biến, có nghĩa là bạn không thể cập nhật hoặc thay đổi giá trị của các bộ dữ liệu.

So that's the reason this will be not happening.

Bây giờ chúng ta có thể làm một điều, chúng ta có thể nói như tup, giả sử trộn tuple bằng số tuple

cộng với tên tuple.

Và bây giờ nếu tôi nói bộ in trộn, bạn có thấy không, cả hai bộ đều được hợp nhất với nhau.

Vì vậy, chúng ta có thể hợp nhất chúng, nhưng chúng ta không thể sửa đổi chúng, được chứ?

Ví dụ: nếu tôi muốn xóa bất kỳ dữ liệu nào khỏi bộ dữ liệu của mình, giả sử từ bộ dữ liệu số của tôi,

Tôi muốn xóa 30.

Vì vậy tôi nói del delete num tuple chỉ số là 2.

Vì vậy, bây giờ nếu bạn thấy khi tôi đang sử dụng phần tử để bị xóa, nó sẽ báo lỗi cho tôi

nói đối tượng tuple không hỗ trợ xóa mục.

Điều đó có nghĩa là bạn thậm chí không thể xóa, như trong phân đoạn danh sách, nếu bạn đến với danh sách,

chúng tôi đã có tùy chọn cập nhật danh sách của mình, đó là bằng cách sử dụng phần bổ sung.

Chúng tôi cũng nhận được tùy chọn xóa danh sách, nhưng ở đây chúng tôi không thể làm như vậy.

Nhưng chúng ta có thể làm một việc đó là xóa num tuple.

Bây giờ nó cũng nói rằng tôi không thể xác định được số bộ dữ liệu, được chứ?

Vì vậy, chúng tôi thậm chí không thể sửa đổi hoặc thậm chí chúng tôi không thể cố gắng thay đổi dữ liệu hoặc xóa dữ liệu

trong các bộ dữ liệu.

Nhưng nếu bạn muốn thực hiện một số hành động chẳng hạn, giả sử tôi muốn thực hiện một

hành động để tìm độ dài của tuple.

Vì vậy, tôi có thể nói độ dài in của bộ số.

Bây giờ nó in tôi và cho tôi giá trị 6.

Điều đó có nghĩa là có 6 dữ liệu có sẵn trong bộ số.

Bây giờ nếu mình muốn nối 2 dữ liệu thì mình đã đưa ra 1 ví dụ về nối rồi phải không?

Vì vậy, điều này chúng ta có thể làm để nối dữ liệu.

Ví dụ: nếu tôi muốn lặp lại một số dữ liệu, hãy xem xét tôi phải lặp lại một số dữ liệu, sau đó

điều tôi có thể làm là tôi có thể nói in số bộ dữ liệu nhân với 4 lần hoặc bất cứ lúc nào

bạn kể nó đi.

Vậy nếu bạn nói ngay bây giờ, xem 10, 20, 30, 40, 50, 60, 1, đây là 2, đây là 3, và đây

là 4.

Vậy chúng ta có thể in cái này 4 lần liên tiếp nhé?

Ngoài ra, nếu bạn muốn lặp lại dữ liệu, bạn có thể lặp lại dữ liệu đó, được chứ?

Để tìm một dữ liệu đơn lẻ, bạn có thể nói for x trong các bộ số và sau đó bạn có thể nói print

x.

Vì vậy, bạn sẽ nhận được dữ liệu cá nhân như chúng tôi biết.

Sau đó, bạn thậm chí có thể thử tìm tư cách thành viên.

Bây giờ nếu bạn nói nếu 3 trong số bộ dữ liệu, nếu 3 có sẵn trong số bộ dữ liệu, hãy in nó là thành viên

của các bộ dữ liệu, nếu không thì in ra nó không phải là thành viên của các bộ dữ liệu.

Vì vậy, nếu bạn thấy ngay bây giờ, nếu tôi thực thi phần mã cụ thể này, thì tôi sẽ nhận được kết quả

rằng nó không phải là thành viên của bộ dữ liệu, được chứ?

Vì vậy, đây là tất cả về bộ dữ liệu của bạn.

Được rồi, bây giờ chúng ta hãy hiểu về bản đồ Python.

Bây giờ khi chúng ta nói về bản đồ Python, bản đồ Python là một hàm, được chứ?

Hàm bản đồ trả về đối tượng, đó là đối tượng bản đồ là một trình vòng lặp lặp lại

trên dữ liệu bản đồ.

Để hiểu chính xác chức năng bản đồ này là gì, hãy lấy một ví dụ đơn giản:

được không?

Vì vậy, trước tiên tôi sẽ tạo một tệp, tệp Python và giả sử là ví dụ về bản đồ, được chứ?

Bây giờ hãy xem xét một ví dụ trong đó tôi có một loại danh sách nhất định.

Ví dụ: namelist nó bằng, mình muốn lấy danh sách những cái tên như Tracy,

Victor, vậy hãy nói Jason, hãy nói John, được chứ?

Đây là những dữ liệu nhất định mà tôi có trong một danh sách.

Bây giờ điều tôi có thể làm là nếu tôi muốn lặp lại dữ liệu, được chứ?

Nếu tôi muốn lặp lại dữ liệu, tôi chỉ cần viết bản in, đầu tiên là danh sách tên.

Vì vậy, nếu bạn thấy, tôi đang nhận được kết quả đầu ra như bất kỳ kết quả nào.

Nếu tôi muốn in từng cái một thì tôi sẽ sử dụng vòng lặp for phải không?

Đối với tên trong danh sách tên và in tên.

Đây có thể là một ví dụ, phải không?

Nhưng nếu tôi nói với bạn rằng tôi muốn tất cả các chữ cái được phân tách thành một danh sách thì tôi có thể làm gì

làm gì?

Vì vậy, điều tôi sắp làm ở đây là, tôi đang sử dụng, giả sử ví dụ, scp, đó là cho

phần riêng biệt, bằng với danh sách tên và bên trong danh sách tên này, tôi đang sử dụng chức năng bản đồ

giả sử tôi có ở đâu, tôi đang lấy bản đồ danh sách và tôi đang lấy danh sách nào?

Danh sách tên.

Được rồi, tôi cũng sẽ lập nó thành một danh sách vì tôi đang tạo một danh sách, được chứ?

Bây giờ chúng ta sẽ in scp.

Vậy bây giờ, đây là một danh sách, phải không?

Bên trong này, chúng tôi đã tạo một danh sách có tên là Tracy, một danh sách được tạo

là Victor, một danh sách được tạo là Jason và một danh sách được tạo là John.

Đây là cách hoạt động lặp lại bản đồ.

Về cơ bản xem, phép lặp bản đồ dù chúng ta viết gì thì hàm bản đồ đều có một trình vòng lặp

bên trong nó.

Vì vậy, nó đang lặp lại trên một dữ liệu duy nhất và tạo một danh sách tương ứng, được chứ?

Đó là việc sử dụng chức năng bản đồ trong danh sách hoặc bộ dữ liệu.

Bạn thậm chí có thể làm điều đó theo bộ dữ liệu, được chứ?

Không bắt buộc nó phải được đưa vào danh sách, bạn cũng có thể sử dụng nó trong các bộ dữ liệu,

được không?

Đây là tất cả về bản đồ của bạn bằng Python.