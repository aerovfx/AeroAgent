# 01 chức năng

---

Chúng ta hãy nói về chức năng trong Python là gì.

Vì vậy, khi chúng ta nói về hàm, điều cơ bản hiện lên trong đầu chúng ta là hàm

là một khối câu lệnh để thực hiện một tác vụ duy nhất, đúng không.

A function is a block of code to get it to perform a single or a particular task.

Vì vậy, bất cứ khi nào chúng ta tạo hàm, chúng ta cố gắng tạo một hàm có thể được thực thi nhiều hơn.

hơn một lần.

Điều đó có nghĩa là chúng ta có thể sử dụng lại mã và thực hiện mã nhiều lần mà không cần viết chúng,

đúng.

Python cũng có hai loại hàm khác nhau được gọi là hàm tích hợp như print

là một chức năng tích hợp trong đó bạn in dữ liệu trên bảng điều khiển và cả do người dùng xác định

chức năng.

Vì vậy, bây giờ chúng ta hãy nói về các hàm do người dùng định nghĩa trước.

Vì vậy, bất cứ khi nào chúng ta nói về các hàm do người dùng xác định và nếu bạn muốn tạo các hàm do người dùng xác định,

vì vậy hãy để chúng tôi nói, tôi sẽ tạo một tệp Python, ví dụ về hàm.

Bây giờ, bất cứ khi nào chúng ta muốn tạo một hàm do người dùng xác định, bạn luôn phải nhớ rằng

bất cứ khi nào chúng ta muốn tạo một hàm do người dùng định nghĩa bằng def, def có nghĩa là định nghĩa, thì chúng ta

cần đặt tên hàm.

Vì vậy, giả sử tên hàm của tôi sẽ là show và dấu ngoặc của show.

Bây giờ bên trong bất cứ điều gì bạn muốn viết, hãy để chúng tôi nói rằng tôi muốn viết bản in, chào mừng bạn hoạt động

bằng Python, được thôi.

Và hãy để chúng tôi nói nếu tôi muốn gọi cái này, vì vậy bạn có thể viết trực tiếp show và bạn gọi

chức năng.

Vì vậy, giả sử nếu tôi chạy mã này, bạn có thể thấy hoan nghênh hoạt động trong Python và một lần nữa

và một lần nữa bất cứ khi nào tôi gọi hàm này, chúng tôi sẽ nhận được kết quả tương tự.

Vì vậy, đây là ví dụ đơn giản về một hàm.

Bây giờ chúng ta cũng có thể tạo các hàm với một số tham số.

Đây là một hàm không chứa bất kỳ tham số nào, đúng không.

Hàm không có tham số, nhưng nếu bạn muốn tạo hàm có tham số,

vì vậy bạn có thể nói định nghĩa, cộng, giả sử add là tên của hàm, trong đó tôi có dấu phẩy

b và chúng ta sẽ nói in, tổng là a cộng b.

Bây giờ bất cứ khi nào bạn muốn gọi hàm này, hãy cộng 10 dấu phẩy 20, thêm 45 dấu phẩy 65

và bất cứ khi nào tôi thực thi mã này, tôi sẽ nhận được kết quả đầu ra.

Như vậy các bạn cũng có thể tạo hàm có tham số nhé.

Các hàm bây giờ khi chúng ta tạo không có tham số và không có tham số.

Ngoài ra, các hàm còn có loại viết và loại không viết, đúng không.

Vì vậy, chúng tôi nói rằng các hàm được tham số hóa và không được tham số hóa.

Vì vậy, chúng ta đã thấy một hàm không được tham số hóa và đây là một ví dụ đơn giản về hàm được tham số hóa

chức năng.

Hàm có tham số, vì vậy đây là hai ví dụ khác nhau, đúng không.

Bây giờ nếu bạn muốn tạo hàm trả về một cái gì đó, được thôi, nếu bạn muốn tạo hàm

trả về một cái gì đó, vì vậy bạn có thể nói định nghĩa, chúng ta hãy nói căn bậc hai của hàm và

chúng ta sẽ lấy một số và chúng ta sẽ nói ở đây là sqrt bằng số thành số

và sau đó trả về sqrt.

Vì vậy, đây là một hàm có tham số và cũng trả về dữ liệu.

Bây giờ bất cứ khi nào bạn gọi hàm này, hãy nói căn bậc hai của 5.

Nếu tôi thực thi mã này, ngay bây giờ tôi sẽ không nhận được bất kỳ kết quả nào vì tôi chưa sử dụng bất kỳ

câu lệnh in nhưng nếu tôi đến đây và viết một câu lệnh in và bây giờ nếu tôi thực hiện câu lệnh này

mã, vậy sqrt của 5 là 25.

Một lần nữa nếu bạn muốn gọi hàm này, giả sử, tôi sẽ nói sqrt là 25, chúng ta có

đầu ra.

Vì vậy, đây là một ví dụ đơn giản về hàm có kiểu trả về.

Như vậy, chúng ta đã thấy một số ví dụ về hàm không có tham số, có tham số, hàm trả về

loại và loại không trả lại, phải.

Bây giờ, hãy luôn nhớ bất cứ khi nào bạn gọi một đối số hàm, có bốn loại đối số,

nó có thể được yêu cầu, nó có thể là một từ khóa cơ bản, nó có thể là đối số mặc định hoặc nó

có thể là đối số có độ dài thay đổi, được thôi.

Vì vậy, đây là bốn điều mà bạn có thể đạt được ở đây.

Bây giờ hãy xem, nếu bạn đang sử dụng một đối số bắt buộc, điều đó có nghĩa là bạn chỉ đang viết một kiểu đơn giản

của dữ liệu.

Ví dụ: giả sử chúng ta sẽ lấy lại hàm cộng, hàm trước đó.

Được rồi, chúng ta sẽ lấy mã này để hiểu.

Bây giờ ở đây, chúng ta hãy nói, được rồi ở đây những gì tôi đã làm là, tôi có a và b, đúng không.

Vì vậy, tôi đã gọi trực tiếp a và tôi đã gọi trực tiếp b.

Vì vậy, đây là một đối số loại bắt buộc.

Bây giờ, bất cứ khi nào bạn sử dụng đối số từ khóa, nó giống như một từ khóa, bạn viết một từ khóa, hãy

nói ở đây, nếu chúng ta đang làm việc với một đối số loại từ khóa, giả sử, tôi sẽ để một giá trị bằng

đến 10, b bằng 20, a bằng 10, b bằng 20, được.

Vì vậy, đây là loại đối số từ khóa, nơi bạn chỉ cần viết từ khóa, cung cấp từ khóa.

Sau đó, bạn có một đối số mặc định, được thôi.

Vì vậy, giả sử, nếu tôi nói về đối số mặc định, tôi có thể đưa ra một đối số ở đây giống như

10.

Vì vậy, hiện tại kết quả đầu ra của tôi sẽ là 30 và 110.

Nhưng tình cờ nếu mình quên đưa data này thì nó sẽ lấy đối số mặc định nhé.

Vì vậy, đây là một số loại đối số hàm nhất định.

Bây giờ, điều tiếp theo.

Bây giờ, chúng ta luôn thấy rằng bất cứ khi nào chúng ta tạo hàm, chúng ta xác định hàm, chúng ta đưa ra

tên của hàm và chúng ta đưa ra sự tổng hợp của hàm, đúng không.

Vì vậy, trong Python, bạn có thêm một ví dụ nữa, được gọi là hàm ẩn danh.

Bây giờ, bạn có ý gì khi dùng từ ẩn danh?

Chúng tôi biết ẩn danh có nghĩa là không xác định, phải không.

Và về mặt kỹ thuật, chúng tôi nói, và bất cứ khi nào chúng tôi gọi là ẩn danh, chúng tôi có nghĩa là không có tên

cho chức năng, phải.

Vì vậy, bất cứ khi nào chúng ta tạo một hàm không có từ khóa def, loại hàm này được gọi

như các chức năng ẩn danh.

Điều đó có nghĩa là, nếu tôi tạo một hàm không có def này, bây giờ hãy xem khi nào tôi đang sử dụng, không có

dùng def thì nó báo lỗi phải không.

Nhưng bất cứ khi nào chúng ta tạo một hàm mà không sử dụng def, hãy luôn nhớ rằng loại hàm này

là chức năng ẩn danh.

Vì vậy, làm thế nào bạn có thể tạo chức năng ẩn danh này?

Vì vậy, hãy luôn nhớ để tạo một hàm ẩn danh trong Python, chúng ta phải sử dụng lambda

biểu thức.

Nếu không có biểu thức lambda, chúng ta không thể sử dụng chuỗi cụ thể này.

Vì vậy, hãy lấy một ví dụ, tổng bằng biểu thức lambda, được thôi, tôi nói đối số

1, đối số 2, sẽ là đối số 1 cộng với đối số 2.

Vì vậy, đây là những gì tôi muốn làm trong hàm.

Bây giờ bất cứ khi nào tôi gọi hàm này, giả sử tên hàm là tổng, có hai tham số

đó là đối số 1 và đối số 2, sẽ thực hiện hành động cộng.

Vì vậy, bất cứ khi nào tôi gọi hàm này, ví dụ như, nếu tôi gọi ở đây tổng, được, và

xem khi tôi viết tổng, nó hiển thị cho tôi giống như một biến, nó không hiển thị cho tôi

chức năng.

Vì vậy, tôi sẽ chỉ đề cập đến 10, tôi sẽ chỉ đề cập đến 20 và nếu tôi thực thi mã này, chúng tôi sẽ nhận được kết quả

của 30.

Vì vậy, chúng tôi đã tạo cái này mà không sử dụng từ khóa def.

Vì vậy, loại hàm này có thể là hàm ẩn danh, được chứ.

Vì vậy, đây là tất cả về chức năng của bạn, được chứ.

Đây là tất cả các hàm mà chúng ta biết trong Python, được chứ.

Vậy ta có các hàm không có tham số, hàm có tham số, hàm không có kiểu trả về,

có kiểu trả về thì ta có các hàm có kiểu trả về thì ta có các hàm, khác nhau

các loại đối số và hàm và chúng tôi có một hàm ẩn danh.