# 01 người dùng nhập vào python

---

Bây giờ, hãy xem dữ liệu đầu vào của người dùng bằng Python nhé.

Vì vậy, nếu tôi muốn lấy bất kỳ dữ liệu nào từ người dùng, hãy xem xét một ví dụ, chúng tôi sẽ tạo người dùng

đầu vào, được rồi.

Giả sử nếu tôi muốn lấy thông tin đầu vào từ người dùng thì đó là tên.

Vì vậy, giả sử nếu tôi lấy biến tên bằng một hộp nhập liệu và bạn sẽ gửi bất kỳ thông báo nào

muốn viết, hãy nhập tên của bạn.

Và sau đó, chúng ta chỉ có thể đề cập như thế này, biến in, chào mừng và tên,

được.

Vì vậy, bây giờ nếu tôi chỉ thực thi mã này, nó sẽ cung cấp cho tôi thông tin đầu vào của người dùng ở đây.

Được rồi, xin lỗi, nó đang vào file cơ bản nên mình chạy thôi, nhập tên bạn vào.

Giả sử mình nhập tên là Tracy thì đúng rồi, chào mừng Tracy.

Vì vậy, nếu tôi đặt tên và nhập tên, tên là John, nó sẽ cho tôi thông báo về John, đúng vậy.

Điều đó có nghĩa là bất cứ điều gì tôi nhập dữ liệu, nó sẽ được hiển thị cho tôi.

Vì vậy, đây là một giá trị đơn giản của hộp đầu vào.

Bây giờ, hãy xem xét một ví dụ nếu tôi muốn cộng hai số bằng cách lấy dữ liệu đầu vào của người dùng.

Nếu tôi muốn cộng hai số bằng cách lấy dữ liệu đầu vào của người dùng, thì tôi nói số một bằng dữ liệu đầu vào,

nhập số đầu tiên, tôi nói số thứ hai bằng đầu vào, nhập số thứ hai rồi tôi nói

phép cộng in là num1 cộng num2, được rồi.

Bây giờ nếu tôi thực thi đoạn mã này, tôi nói số 10 là 10.

Tôi nói hãy nhập số hai, đó là 20 và nó hiển thị cho tôi phép cộng là 1020.

Vì vậy, đó chỉ là vì bất cứ khi nào chúng tôi lấy bất kỳ thông tin đầu vào nào của người dùng, nó đều có kiểu dữ liệu cơ bản là

gõ chuỗi.

Nó có kiểu dữ liệu cơ bản là kiểu chuỗi, được chứ.

Vì vậy, làm cách nào chúng ta có thể thực hiện việc này hoặc làm cách nào tôi có thể nhập số vào, đúng không.

Bởi vì bây giờ nếu tôi lấy một dữ liệu, chẳng hạn như khi tôi lấy một cái tên hay thứ gì đó, tôi sẽ

lấy dữ liệu, nó hiển thị cho tôi giá trị như chính giá trị đó, đúng không.

Vì vậy, bây giờ nếu tôi lấy đầu vào, được rồi, bây giờ nếu tôi lấy đầu vào và bây giờ nếu tôi muốn thực hiện

nó giống như một định dạng số nguyên, làm cách nào tôi có thể biến nó thành định dạng số nguyên, đúng không.

Vì vậy, tôi phải nói rằng tôi đang lấy một số, vì vậy tôi sẽ viết số nguyên ở đây và cả một số

số nguyên ở đây và bây giờ nếu tôi chạy cùng một mã, giả sử là 1020, thì phép cộng bây giờ là 30.

Vì vậy, nếu tôi lấy, giả sử nếu tôi lấy ngẫu nhiên, nếu tôi lấy 12,45 và bây giờ nó đang cho tôi thấy điều đó

bạn không thể lấy vì bạn phải ở đây là số nguyên đúng không.

Nếu mình lấy float, bây giờ mình lấy 12,46 thì mình chấp nhận nhé.

Vì vậy, bạn muốn lấy loại dữ liệu nào thì phải đề cập ở đó nhé.

You want to mention over there.

Bây giờ, hãy xem xét một ví dụ nữa, giả sử nếu tôi nhập 10 và nếu tôi nhập 20, nó sẽ cho

cho tôi câu trả lời trong float vì chúng ta đã lấy kiểu dữ liệu ở đây dưới dạng float, được chứ.

Vì vậy, hãy luôn nhớ một điều rằng bất cứ khi nào bạn tiếp nhận bất kỳ loại thông tin đầu vào nào của người dùng, bạn

phải nhận cái đó.

Kiểu nhập mặc định là chuỗi, nhưng nếu bạn muốn thay đổi kiểu dữ liệu, bạn

có thể thay đổi loại dữ liệu bằng cách chỉ đề cập đến loại bất kỳ loại nào bạn muốn nhập vào

đây, được rồi.

Đó là một ví dụ về dữ liệu đầu vào của người dùng trong tập lệnh Python.