# 02 cài đặt-python

---

Xin chào, chào mừng trở lại.

Trong chương này, chúng ta sẽ cài đặt Python và một loạt công cụ khác, chủ yếu là

liên quan đến Python, nhưng không liên quan đến Django hoặc thậm chí không phải Visual Studio Code.

Chương này sẽ chỉ dành riêng cho việc cài đặt Python, tải xuống và cài đặt

Python từ trang web của nó và thử một vài lệnh và tất cả những thứ đó.

Vì vậy, hãy xem mọi thứ chúng ta sẽ làm.

Đầu tiên là chúng ta sẽ tải xuống gói Python dành cho Windows và nếu bạn đang sử dụng một số

của hệ điều hành, sau đó bạn có thể tiếp tục và chọn hệ điều hành của mình và tải xuống

Python cho điều đó.

Chúng tôi cũng sẽ cài đặt nó và sau đó chúng tôi sẽ xác minh rằng phiên bản mà chúng tôi đã cài đặt là

đúng và chúng tôi sẽ sử dụng các lệnh thông qua dấu nhắc lệnh và sau đó chúng tôi sẽ

install a list and uninstall packages through PIP.

Bây giờ PIP là trình quản lý gói được tích hợp trong Python.

Nếu bạn đang sử dụng Python lớn hơn 3.4, phiên bản 3.4 thì PIP sẽ ở dạng một gói

trình quản lý được cài đặt sẵn với bản cài đặt Python.

Và sau đó chúng ta xem xét IDE có tên IDLE cũng đi kèm với cài đặt Python chứ không phải

một IDE rất thuận lợi hoặc phổ biến, nhưng nó chỉ đi kèm nếu bạn muốn thử thứ gì đó

ra ngoài nhanh chóng.

Nhưng nếu bạn không sử dụng nó, tôi chỉ muốn cho bạn thấy rằng nó cũng có IDE nếu

bạn muốn sử dụng nó.

Được rồi.

Và sau đó chúng ta cũng sẽ xem xét thứ gọi là repl.it.

Đó là một trang web nơi bạn có thể viết mã mà không cần phải cài đặt bất cứ thứ gì trên PC.

Vì vậy, nó hoàn toàn dựa trên trang web URL.

Vì vậy, nó có hỗ trợ cho nhiều ngôn ngữ.

Bây giờ chúng ta sẽ xem xét mọi thứ từng bước một.

Vì vậy, trước tiên hãy tiếp tục và tải xuống Python từ trang web của nó.

Vì vậy, bạn chỉ cần mở trình duyệt yêu thích của mình và truy cập python.org hoặc chỉ Google để tìm

Tải xuống Python hoặc bất cứ thứ gì Python và nó, liên kết đầu tiên sẽ là python.org.

Vì vậy, chỉ cần nhập cùng một URL, HTTPS, python.org.

Và đây là trang chủ của Python.

Và tất cả những gì bạn phải làm là nhấp vào tải xuống rồi tìm phiên bản HĐH của bạn.

Bây giờ, ngay cả khi bạn đang sử dụng Windows, chúng tôi cũng có hai phiên bản khác nhau, phải không?

Chúng tôi có phiên bản 32 bit và phiên bản 64 bit tùy theo phiên bản bạn có,

hãy tiếp tục và tải về nó.

Nó rất đơn giản.

Nó khá đẹp, khá tự giải thích.

Và đối với các HĐH khác, bạn có các liên kết ở đây, Linux, Unix và Mac OS X và các HĐH khác

ở đó.

Chỉ cần tiếp tục và tải về nó.

Và nếu bạn đang tìm kiếm một bản phát hành rất cụ thể thì đây là bản mới nhất, 3.8.2

cái mới nhất.

Và nếu bạn đang tìm kiếm thứ gì đó cũ hơn, bạn sẽ có tất cả những thứ đó quay trở lại

đến năm 2001, tức là 2.0.1.

Vì vậy, nếu bạn đang tìm kiếm một bản phát hành cụ thể thì bạn cũng có thể tiếp tục và tải xuống bản phát hành đó.

Vì vậy, nó được liệt kê gọn gàng trong trang web này.

Vậy là tôi đã tải nó xuống rồi.

Tôi sẽ không tải lại nó.

Vậy là tôi đã tải nó xuống rồi.

Vì vậy, đây là nơi tôi có nó.

Và Python 3.8.1 AMD64.exe.

Vì vậy, hãy nhấp đúp chuột vào đó.

Và nó nói cài đặt launcher cho tất cả người dùng, đó là điều nên làm.

Và điều quan trọng là bạn thêm Python 3.8 vào đường dẫn.

Vì vậy, các biến môi trường cũng được thêm vào máy của bạn.

Vì vậy, bạn không cần phải làm điều đó bằng tay.

Và nếu bạn muốn tùy chỉnh cài đặt, nếu bạn muốn chọn vị trí và tính năng,

bạn có thể làm điều đó

Vì vậy, PIP, như tôi đã nói, khi bạn có phiên bản 3.4 trở lên, khi bạn đang cố gắng cài đặt Python 3.4 hoặc

lớn hơn thì PIP được cài đặt sẵn.

Như trong, đó là tùy chọn nếu bạn muốn, nhưng tất cả đều được chọn theo mặc định.

Và nó cũng bao gồm IDLE, đó là môi trường phát triển và Tkinter và những thứ mà chúng tôi

không thực sự cần

Và nó cũng đi kèm với tài liệu, cũng rất tiện dụng.

Vì vậy, chúng tôi sẽ không thay đổi bất cứ điều gì ở đây.

Chúng tôi sẽ giữ mọi thứ như mặc định.

Chúng tôi sẽ không gây rối với bất kỳ hộp kiểm nào ở đây.

Vì vậy, hãy quay lại và chỉ cần nhấp vào cài đặt.

Trừ khi bạn muốn chọn một địa điểm khác và tôi khuyên chúng ta không nên làm vậy.

Và đây là một vị trí đủ tốt cho chúng tôi.

Tôi chỉ định nói hãy cài đặt ngay bây giờ và hãy xác minh nó ở đó.

Và chúng ta hãy tiếp tục và cài đặt nó.

Nó sẽ mất khoảng một phút hoặc lâu hơn.

Nó gần như đã hoàn tất.

Đảm bảo rằng bạn có phiên bản phù hợp nếu bạn đang sử dụng Windows.

Nếu bạn đang sử dụng 32-bit, hãy lấy phiên bản 32-bit.

Nếu bạn không sử dụng phiên bản 32-bit, nếu bạn đang sử dụng phiên bản 64-bit, hãy đảm bảo rằng bạn có phiên bản 32-bit.

Phiên bản 64-bit.

Nếu không, bạn sẽ phải đối mặt với các vấn đề.

Và cũng đi kèm với PIP bootstrap, nhưng có thể chúng tôi sẽ tải xuống thứ gì đó bổ sung.

Hãy xem.

Và bây giờ nó đang chạy trên con đường đó.

Và nếu muốn, bạn có thể tiếp tục và đọc tài liệu hoặc bạn có thể đóng lại.

Và tôi sẽ đóng nó lại.

Và hãy lấy dấu nhắc lệnh và kiểm tra phiên bản.

Hãy xác minh rằng chúng tôi có phiên bản phù hợp.

Actually, I should have showed you the command before we installed it and what it looks like

và nó trông như thế nào khi cài đặt Python.

Vì vậy, lệnh là gõ Python, được rồi, dấu cách, dấu gạch ngang, dấu gạch ngang, phiên bản.

Đó là mệnh lệnh.

Và nó nói 3.8.1.

Bây giờ, nếu bạn không có Python, nó sẽ không hiển thị gì ở đó.

Được rồi.

Nó sẽ chỉ đi đến dòng tiếp theo.

Nó sẽ không cung cấp cho bạn một lỗi hoặc bất cứ điều gì.

Nó sẽ chỉ chuyển sang dòng tiếp theo và không in bất cứ thứ gì.

Vì vậy, bây giờ chúng tôi đã xác minh rằng chúng tôi có phiên bản Python mới nhất, là 3.8.1.

Vì vậy bây giờ chúng ta hãy nhìn vào PIP.

Bây giờ, như tôi đã nói, PIP là trình quản lý gói cho các gói hoặc mô-đun Python, nếu bạn thích,

nếu bạn muốn gọi chúng là mô-đun.

Vì vậy, đây là trình quản lý tải xuống, không phải trình quản lý tải xuống, mà là trình quản lý gói.

Và nó nhanh chóng cho phép bạn cài đặt, gỡ cài đặt và liệt kê tất cả các gói

bạn có Python trong máy của mình.

Và chính xác thì một gói là gì?

Gói là thứ chứa tất cả các tệp mà bạn sẽ cần cho một mục đích cụ thể

mô-đun.

Và các mô-đun là thư viện mã Python mà bạn có thể đưa vào dự án của mình.

Giống như nếu bạn đến từ một số nền tảng lập trình khác, như C hoặc C++, C++

có rất nhiều thư viện.

Bạn có thể sử dụng SDL cho đồ họa và rất nhiều thư viện khác mà bạn có, tất cả những gì bạn phải làm

là lấy các thư viện đó vào đó và đưa các thư viện đó vào dự án của bạn.

Và bạn có thể bắt đầu sử dụng các chức năng được tích hợp trong thư viện, chính xác là những gì

nó cũng xảy ra trong Python.

Bạn nhận được gói thông qua PIP và nó chứa tất cả các tệp bạn cần cho một mục đích cụ thể.

mô-đun.

Và những mô-đun đó chứa tất cả các thư viện mã mà bạn có thể đưa vào cụ thể của mình

dự án.

Đầu tiên, chúng ta phải kiểm tra xem PIP đã được cài đặt chưa.

Giờ thì không cần phải đắn đo nữa vì chúng ta đã biết điều đó xảy ra nếu phiên bản Python của bạn tốt hơn

hơn 3.4 thì bạn sẽ cài đặt PIP.

Nhưng dù sao đi nữa, hãy tiếp tục và kiểm tra xem chúng ta có phiên bản PIP nào.

Vì vậy, lệnh cho điều đó sẽ là PIP và một lần nữa, phiên bản dấu gạch ngang kép.

Và thế là xong.

Vì vậy, nó nói 19.2.3 là phiên bản.

Vậy là PIP đã được cài đặt và chúng ta không thực sự phải lo lắng về điều đó.

Bây giờ hãy tìm hiểu cách tải xuống một gói và cài đặt nó vào máy.

Việc này khá dễ dàng và tất cả những gì chúng ta phải làm là sử dụng lệnh PIP và chúng ta sẽ chỉ nói cài đặt.

Đó sẽ là lệnh và chúng ta muốn tải xuống gói nào?

Và hãy tải xuống một gói có tên là Camel Case, một gói rất đơn giản mà hầu hết mọi người

được tải xuống lần đầu tiên để kiểm tra xem mọi thứ có hoạt động không, PIP có hoạt động không.

Vì vậy, đây là một gói rất đơn giản, nó chỉ chứa một số chức năng nếu bạn

chuyển chuỗi dưới dạng tất cả chữ thường hoặc chữ hoa và nó sẽ chuyển đổi chuỗi đó thành lạc đà

trường hợp đơn giản như vậy.

Đây chỉ là để kiểm tra xem hệ thống đóng gói có hoạt động hay không, PIP có hoạt động trong máy của bạn hay không.

Và khi tôi nhấn enter, điều đó phụ thuộc vào tốc độ Internet.

Vì vậy hãy chắc chắn rằng bạn có internet.

Nó rất quan trọng.

Tất cả các gói này sẽ được tải xuống từ dấu nhắc lệnh và tốc độ càng nhanh thì càng tốt.

internet thì việc đó sẽ được thực hiện càng nhanh.

Vì vậy, bây giờ nó báo là trường hợp lạc đà được cài đặt thành công và nó cho biết bạn đang sử dụng phiên bản PIP

phiên bản 19.2.3.

Tuy nhiên, phiên bản 20.2.2 đã có sẵn.

Vì vậy, hãy thực sự nâng cấp nó ngay bây giờ.

Vì vậy, chúng ta sẽ chỉ nói Python và sử dụng lệnh tương tự dash M PIP và cài đặt

và chúng tôi sẽ chỉ cài đặt và nâng cấp PIP, được thôi.

Và hãy nâng cấp PIP lên 20.0.2.

Vì vậy, đó là loại độc lập.

Bản thân PIP hoạt động độc lập với gói Python mà bạn mang vào vì

đây chỉ là một trình quản lý gói.

Vì vậy, họ có thể cập nhật thường xuyên hơn rất nhiều so với chính Python.

Vì vậy bây giờ nếu bạn kiểm tra phiên bản PIP và phiên bản dash dash thì đó là 20.0.2.

Tuyệt vời.

Vì vậy, tôi sẽ chỉ xóa màn hình ở đó.

Bây giờ chúng ta đã có cái này, bây giờ chúng ta sẽ làm gì với gói mà chúng ta vừa

cài đặt?

Nhưng bây giờ, trước khi chúng ta xem chúng ta sẽ làm gì với gói hàng, chúng ta hãy cùng xem xét

hãy xem cách chúng tôi có thể liệt kê tất cả các gói mà chúng tôi đã cài đặt trong máy.

Vì vậy chúng ta sẽ chỉ nói PIP và chúng ta sẽ chỉ liệt kê.

Đó là một lệnh rất đơn giản.

Và đây là tất cả các gói mà bạn có hộp đựng lạc đà tiểu hành tinh colorama và tất cả những thứ này

các gói khác nhau đã đi kèm với nó.

Và pylint cũng là thứ có sẵn, họ bắt đầu kết hợp cùng với chính Python.

Vì vậy, một loạt các gói và hộp đựng lạc đà là thứ mà chúng tôi vừa tải xuống và phiên bản là

0,2.