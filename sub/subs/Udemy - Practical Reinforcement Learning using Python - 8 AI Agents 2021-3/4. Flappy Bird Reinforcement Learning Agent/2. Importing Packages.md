# 2. Nhập gói

---

Được rồi, vậy hãy tạo tập tin cho đại lý Flappy Bird RL của chúng ta, vì vậy tôi sẽ nhấp chuột phải vào đây

khoảng trống và tôi sẽ tạo một tệp mới và chúng ta sẽ gọi đây là chiếc bánh mềm d'Yquem để chúng ta có thể tạo một con trăn

tập tin.

Và do đó, tệp này khác với các tệp cuối cùng mà chúng tôi đang tạo nơi chúng tôi đang tạo sổ ghi chép Python

tập tin.

Được rồi, hãy nhập các phần phụ thuộc sẽ sử dụng trong gói này.

Vì vậy tôi sẽ đưa lên các phụ thuộc DPN nhập khẩu như thế nào cho đẹp.

Và vì vậy cái đầu tiên sẽ là ngẫu nhiên.

Được rồi, cái thứ hai, Nikpai và chúng ta sẽ làm nó như một enpi, cái thứ ba, Flappy Bird Jim, là

môi trường tập thể dục mà chúng tôi đã chọn cài đặt trong bài giảng trước.

Phần sau đây là phần nhập từ bộ sưu tập hoặc dequeue.

Và chúng ta sẽ tìm hiểu xem đây là gì.

Nhưng đây là bộ nhớ mà chúng tôi đã tạo trong tác nhân R-AL đầu tiên mà chúng tôi tạo.

Và bộ nhớ đó được lưu trữ trong một thứ gọi là dequeue.

Và do đó, nó rất giống với cấu trúc dữ liệu Q.

Vì vậy, nếu bạn quen thuộc với các thuật toán dữ liệu khoa học máy tính, bạn sẽ biết rằng thuật toán này tương tự như một

khối lập phương nơi có cái vào trước, cái ra trước.

Tiếp theo sẽ là các gói mạng thần kinh của chúng tôi.

Vì vậy, từ CARUS, các lớp, nhập đầu vào và phân bổ.

OK, đó là hai loại lớp sẽ được sử dụng trong mô hình của chúng ta.

Phần tiếp theo từ các mô hình CARUS nhập, tải, mô hình, lưu mô hình và tuần tự.

ĐƯỢC RỒI.

Và cuối cùng, gói cuối cùng là từ Carus Optimizers Import RMX prop và đó sẽ là trình tối ưu hóa

mà chúng tôi sử dụng trong mạng lưới thần kinh của mình để có thể tạo một thiết bị đầu cuối mới tại đây và chúng tôi có thể thử chạy nó để

đảm bảo rằng chúng tôi không có lỗi và chúng tôi cần đảm bảo rằng chúng tôi đang ở trong môi trường của mình, đó là

thiết thực.

Vì vậy, hãy chạy nó.

Và chúng tôi không có mô-đun tên là Carus, điều mà tôi nghĩ là chúng tôi đã có.

Tôi đoán đó là từ Tesla mà chúng ta phải làm từ Tenzer flow dot carus.

Nhưng hãy cứ cài đặt thôi.

US để chúng ta có thể sử dụng trực tiếp.

Được rồi, xin lỗi, điện thoại của tôi đang tắt.

OK, vậy bây giờ chúng ta đã có nó, vậy hãy thử chạy mã của chúng ta.

Tuyệt vời.

Và ở đây chúng ta có mã thiết lập luồng Tenzer và điều đó chỉ cho thấy rằng chúng ta đang thiết lập luồng tenso.

Thật tuyệt vời.

Và chúng tôi không gặp phải bất kỳ lỗi nào và luồng Tensas đang chạy thành công.

Vì vậy, trong video tiếp theo, chúng ta sẽ bắt đầu xây dựng lớp học cho mạng Q sâu của mình và chúng ta sẽ bắt đầu

áp dụng nhiều cách tiếp cận lập trình hướng đối tượng hơn cho tác nhân học tăng cường này, bởi vì thông thường

việc có các lớp cho phép bạn lưu trữ tất cả dữ liệu trong một đối tượng và đối tượng là tác nhân.

Và chúng tôi có thể theo dõi các biến số đó, nhưng bạn sẽ hiểu nó khi chúng tôi trình bày.

Và đó là những gì chúng ta sẽ làm trong video tiếp theo.

Vậy tôi sẽ gặp bạn ở đó và.

Vâng.