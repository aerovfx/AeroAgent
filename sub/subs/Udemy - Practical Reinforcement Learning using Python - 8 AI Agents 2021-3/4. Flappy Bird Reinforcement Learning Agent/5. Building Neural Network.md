# 5. Xây dựng mạng lưới thần kinh

---

Được rồi, bây giờ, trong video trước đây nơi mã hóa lại các siêu thông số này, chúng ta đã bắt đầu xây dựng

biến mạng lưới thần kinh của chúng tôi.

Và vì vậy, để xây dựng biến mạng thần kinh, chúng ta sẽ gọi hàm sẽ tạo ra bên ngoài

của lớp này và chúng ta sẽ đặt nó bằng với mô hình này bên trong dequeue và tác nhân của chúng ta.

Được rồi, để bắt đầu, hãy tạo một hàm bên ngoài lớp của chúng ta và chúng ta sẽ đưa ra nhận xét ở đây, một nơron

mạng dành cho tác nhân và ở đây sẽ có một chức năng mới và chúng ta sẽ gọi nó là mạng thần kinh và nó sẽ lấy

hình dạng đầu vào và hình dạng đầu ra tương tự như tất cả các thuật toán học tăng cường trước đây của chúng tôi.

Và ở đây chúng ta sẽ có một mạng lưới thần kinh tuần tự và chúng ta sẽ thêm một lớp dày đặc.

Và chúng tôi không sử dụng mạng nơ ron tích chập vì như tôi đã đề cập, khi chúng tôi tải xuống mạng này

phòng tập thể dục flypaper, chúng tôi có lựa chọn giữa việc sử dụng môi trường pixel hoặc môi trường ram.

Và trong trường hợp này, chúng tôi đang sử dụng môi trường ram.

Vì vậy, điều đầu tiên sẽ là một lớp dày đặc và chúng ta sẽ tạo ra 512 đơn vị.

Chúng ta sẽ chuyển vào một hình dạng đầu vào bằng với hình dạng đầu vào.

Chúng tôi sẽ có kích hoạt bằng RELU.

Và điều này giữ tính phi tuyến tính bên trong mô hình của chúng tôi để chúng tôi có thể tìm hiểu các mẫu phức tạp hơn.

Phần tiếp theo, chúng ta sẽ sử dụng chủ nghĩa khởi tạo kernel và chúng ta sẽ đặt nó bằng một bộ đồng phục.

Bây giờ, tôi sẽ không đi sâu vào đó là gì, nhưng nếu bạn muốn tìm hiểu thêm về các chi tiết cơ bản trong máy

học tập, hãy thoải mái nhìn vào đó.

Tiếp theo, chúng ta sẽ thêm một lớp khác và đây cũng sẽ là một lớp dày đặc.

Và ở đây chúng ta sẽ có một kích hoạt tương đương với Relu, và vì vậy các lớp này là tùy ý.

Tôi chỉ chọn chúng vì cách tôi nhìn nhận là chúng tôi đang tạo ra một chiếc hộp đen.

Và tôi chỉ muốn.

Tôi chỉ muốn có thêm thông số trong hộp đen để chúng tôi có thể tìm hiểu thêm các hành vi trong môi trường.

Vì vậy, bạn chỉ có thể thêm số lượng lớp tùy ý, nhưng chỉ cần biết rằng nó sẽ thêm nhiều tham số hơn

và điều đó có thể cần được đào tạo thêm.

Vì vậy, tôi sẽ đặt kích hoạt bằng RELU và chúng ta cũng sẽ khởi tạo kernel là

bằng H-E đồng phục và vì vậy chúng ta có thể sao chép dòng mã này, dán nó vì chúng ta sẽ có giống nhau.

Nhưng chúng tôi muốn thay đổi đơn vị đầu ra thành 64.

Theo đó, chúng ta sẽ có đầu ra, vì vậy chúng ta sẽ lập mô hình đầu ra của quảng cáo và sẽ đặt

kích hoạt bằng tuyến tính để chúng ta có thể biến tính toán mà chúng ta đã thực hiện ở đây thành một trong những

các biến đầu ra có thể có.

Và do đó, biến đầu ra sẽ hoạt động.

Vì vậy, số 0 là không có gì, một cho bước nhảy tiếp theo.

Chúng ta cũng sẽ khởi tạo một kernel và nó sẽ giống như một bộ đồng phục.

Được rồi, sau đó chúng ta sẽ biên dịch mô hình của mình bên trong hàm này và đặt tổn thất bằng bình phương trung bình

lỗi, trình tối ưu hóa bằng R và S prop.

Và đây là một trình tối ưu hóa và mạng CU sâu rất phổ biến.

Vì vậy, nếu bạn muốn tìm hiểu thêm về phần lý thuyết, vui lòng nghiên cứu về phần đó tại đây.

Chúng ta sẽ thêm tốc độ học tập vào 0 điểm 0 0 một R r r o đến 0 điểm

chín năm, Epsilon bằng 0 điểm 0 một.

Và đây là những thông số cần thiết cho cánh tay chống.

Và điều cuối cùng là các số liệu.

Và số liệu mà chúng ta sắp xem xét là độ chính xác.

Và cuối cùng, chúng ta có thể in ra mô hình của mình bằng cách thực hiện tóm tắt mô hình và chúng ta có thể trả về mô hình.

Vậy là đã kết thúc việc xây dựng mạng lưới thần kinh của chúng ta.

Vì vậy, bây giờ chúng ta có thể đi xuống đây bên trong lớp của mình và có thể tạo mô hình biến, bằng

tới mạng lưới thần kinh.

Và chúng ta sẽ chuyển tham số hình dạng đầu vào và chúng ta sẽ đặt nó bằng trạng thái tự dot

không gian.

Và sẽ thiết lập hình dạng đầu ra bằng không gian hành động tự học.

Được rồi, bây giờ chúng ta có thể thử chạy cái này để đảm bảo mọi thứ đều ổn.

Tôi không nghĩ nó sẽ chạy.

Vì vậy, chúng tôi thực sự cần phải tạo ra chức năng chính của mình.

Ối.

OK, vậy chúng ta hãy tạo hàm main để xem tác nhân này có chạy chính xác hay không để chúng ta có thể thực hiện

nếu tên gạch dưới dấu gạch dưới dấu gạch dưới bằng dấu gạch dưới chính dấu gạch dưới chính.

Sau đó, chúng tôi muốn tạo tác nhân biến của mình và chúng tôi sẽ đặt nó bằng với tác nhân loại bỏ.

Vì vậy, điều này sẽ tạo ra tác nhân của chúng ta để chúng ta có thể lưu nó và chúng ta có thể chạy Python, Flappy Bird, d'Yquem.

Và nếu mọi thứ đều ổn, chúng ta nên in bản tóm tắt mô hình của mình để nó không hoạt động.

Và thế là tôi đang truy tìm.

Vì vậy, có vẻ như chúng ta đang nhận được không khí trong mạng lưới thần kinh.

Hãy xem.

Vì vậy, chúng tôi có tuần tự nó không thể lặp lại được.

Ồ, tôi nghĩ chúng ta có thể quên điều này, đó là mô hình thêm đầu vào và chúng ta sẽ chuyển đầu vào

kiểm tra.

Chúng tôi không cần điều đó.

Vì vậy, nó có vẻ như.

Nó có ở đây không?

Vì vậy, chúng tôi đang chuyển sang 512, mô hình hóa một kích hoạt dày đặc, có hình dạng đầu vào 512, hình dạng đầu vào bằng nhau

col, hãy khởi tạo đồng phục của bạn.

Được rồi, hãy thực hiện một số lỗi, vì vậy tôi sẽ nhận xét chúng, xem bây giờ nó có hoạt động không, cái nào nên làm và

nó không.

Ừm, hãy chắc chắn rằng đây là không khí.

Được rồi, mạng lưới thần kinh là không khí.

Hình dạng đầu vào, hình dạng đầu ra, hành động, không gian.

Ừm.

Hấp dẫn.

Được rồi, có lẽ chúng ta cần một dấu phẩy ở đây và dấu phẩy chỉ có nghĩa là chúng ta có thể lấy bất kỳ số nào sau tham số này.

Được rồi, thật đẹp, vậy là nó đã hoạt động và chúng ta đang thiếu dấu phẩy này và điều đó chỉ cho thấy rằng có thể có

là một giá trị sau này.

Và điều này đã thành công và chúng tôi đã in ra bản tóm tắt mô hình của mình.

Và bạn có thể thấy rằng chúng ta có tổng cộng một nghìn năm trăm ba mươi sáu tham số.

Vì vậy, đó là một số tiền rất nhỏ.

Được rồi, nhưng chúng ta phải thiết lập cái này.

Xin lỗi về điều đó.

Và hãy loại bỏ điều này, chúng ta không cần phải làm vậy.

Tôi nghĩ đó là một vấn đề, nhưng không sao, bây giờ chúng tôi đã thêm các lớp của mình và bây giờ chúng tôi có nhiều thứ dễ huấn luyện hơn

và tổng các tham số.

Và bây giờ chúng ta đã có một nền tảng tốt cho mạng lưới thần kinh của mình.

Và vì vậy chúng ta có thể giả định rằng nó sẽ học theo các mẫu mà chúng ta mong đợi nó học.

Vì vậy, trong video tiếp theo, chúng ta sẽ bắt đầu xây dựng các chức năng cho tác nhân dequeue nơi nó thực hiện

một hành động và nó học hỏi cũng như thực hiện tất cả những điều thú vị đó.

Vậy tôi sẽ gặp bạn ở đó.

Và nếu bạn có bất kỳ câu hỏi, nhận xét hoặc quan tâm nào, vui lòng cho tôi biết trong phần bình luận.