# 3. Xây dựng mạng lưới thần kinh

---

Được rồi, bây giờ hãy bắt đầu xây dựng mạng lưới thần kinh của chúng ta để xây dựng mạng lưới thần kinh của chúng ta, chúng ta

cần phải nhập một vài gói.

Vì vậy, tôi sẽ đặt một khóa mã ở đây và tôi sẽ nói rằng hãy nhập các gói mạng thần kinh.

Và vì vậy gói đầu tiên chúng ta cần là không.

Vì vậy chúng ta có thể làm vampi quan trọng như ENPI.

Sau đó, chúng ta sẽ cần luồng Tenzer để có thể thực hiện tuần tự nhập mô hình karez luồng Tenzer

theo sau đó.

Nếu mô hình tuần tự về cơ bản cho chúng ta biết rằng chúng ta sẽ tạo một chuỗi các lớp trong

mạng lưới thần kinh của chúng tôi theo sau mà chúng tôi có từ dòng tensor, các lớp CARUS nhập từ lũ và độ dẫn điện.

Và các gói này là các loại lớp khác nhau mà bạn có thể có trong mạng nơ-ron.

Dence là một mạng lưới thần kinh được kết nối đầy đủ.

Bánh flan là một lớp chỉ làm phẳng đầu ra trước đó và do đó nó tạo ra một mảng một chiều.

Và theo sau đó chúng ta có lớp đối lưu 2d và lớp tích chập về cơ bản là cách máy tính

hiểu hình ảnh hoặc cách máy tính nhìn thấy tầm nhìn hoặc tầm nhìn máy tính.

Vì vậy, theo đó, chúng tôi muốn thực hiện từ luồng Tenzer, tối ưu hóa sự sợ hãi, nhập nguyên tử.

Và do đó, trình tối ưu hóa nguyên tử về cơ bản là thứ sẽ cho phép chúng ta tìm ra tập hợp trọng số tối ưu

trong mạng lưới thần kinh của chúng tôi.

Và một khi bạn tìm thấy tập trọng số tối ưu trong mạng lưới thần kinh của mình, bạn có thể nói rằng bạn có một giá trị tối ưu

tác nhân có thể tương tác trong môi trường của nó mà không gặp vấn đề gì.

OK, vậy bây giờ chúng ta đã nhập các gói của mình, chúng ta có thể bắt đầu xây dựng mô hình của mình.

Và vì vậy chúng ta sẽ xây dựng mô hình của mình trong một hàm gọi là mô hình xây dựng.

Và vì vậy hàm này sẽ có bốn tham số.

Cái đầu tiên bị đánh, cái thứ hai là chiều rộng oops uzu, cái thứ ba là kênh và cuối cùng

một là hành động.

Và vì vậy bạn có thể nhận thấy rằng ba tham số ở đây là bốn pixel cho màn hình của chúng ta.

Vì vậy, điều tôi có thể làm với điều này là chúng ta có thể xem mô hình của chúng ta sẽ hiểu được điều gì bằng cách thực hiện không gian quan sát cuối cùng

và chúng ta sẽ đưa nó ra ngoài.

Và ở đây bạn có thể thấy rằng chúng ta có mảng này ở đây.

Và đây là chiều cao, đây là chiều rộng và đây là các kênh.

Được rồi, đây là cửa sổ mà mô hình của chúng ta sẽ học từ đó.

Điều đó thật tuyệt vời.

Vì vậy, chúng ta cũng có thể tạo hình để trích xuất các biến ở đây.

Vì vậy, bên trong hàm này, chúng ta muốn tạo một lớp tuần tự để có thể thực hiện mô hình tương đương với tuần tự.

Và điều này chỉ tạo ra một lớp tuần tự trong đó chúng ta có một chuỗi các lớp.

Theo đó, chúng ta sẽ có lớp chập của chúng ta.

2D và như tôi đã đề cập trước đây, đây là cách mô hình của chúng tôi hiểu các pixel và chúng tôi sẽ có

32 đơn vị đầu ra theo sau đó.

Chúng ta sẽ có tám giờ tám.

Sau đây chúng ta sẽ có bước tiến bằng bốn.

Và đây chỉ là các biến dùng để cô đọng hình ảnh để người mẫu có thể hiểu được hình ảnh

và một cách tính toán thân thiện hơn.

Và tiếp theo, chúng ta có kích hoạt bằng RELU.

Bây giờ, việc kích hoạt cho phép chúng tôi có một số điểm phi tuyến tính bên trong mô hình của mình.

Và khi bạn có sự phi tuyến tính bên trong mô hình của mình, bạn có thể nói rằng mô hình đó đang học hỏi nhiều hơn

các mẫu phức tạp.

Và cuối cùng, chúng ta muốn hình dạng đầu vào và như tôi đã đề cập trước đó, mô hình này sẽ làm gì và

đây là tham số quan trọng nhất trong mô hình của chúng ta, nó sẽ lấy dữ liệu đầu vào của màn hình

và chúng ta sẽ thực hiện một số tính toán và sau đó chúng ta sẽ đưa ra một hành động dựa trên màn hình.

Được rồi, vậy theo nghĩa rộng hơn, chúng ta có một màn hình.

Chúng ta chuyển nó vào một hộp đen, và hộp đen đó sẽ thực hiện một số tính toán và nó sẽ

để phun ra một hành động.

Và do đó, khi giao dịch quá mức mạng lưới thần kinh của chúng ta, nó sẽ học cách liên kết một màn hình cụ thể với

hành động cụ thể đó.

Và hành động đó sẽ là hành động tốt nhất mà mô hình của chúng ta có thể thực hiện.

Và do đó, hình dạng đầu vào sẽ có ba chiều cao, chiều rộng, chiều rộng và sau đó là các kênh.

OK, đó sẽ là thông tin đầu vào của chúng tôi.

Theo đó, chúng ta có thể thực hiện mô hình thêm quảng cáo chuyển đổi 2D và chúng ta sẽ có 64 đơn vị.

Và đơn vị này hay đơn vị đầu ra này là.

Một con số mà bạn có thể điều chỉnh để tăng kết quả, vì vậy khi bạn tăng con số này, bạn đang thêm nhiều nơ-ron hơn

về cơ bản đối với bộ não của chúng ta.

Và do đó, với nhiều nơ-ron hơn, mạng nơ-ron của chúng ta sẽ có khả năng học hỏi nhiều hơn và nó sẽ hoạt động nhiều hơn

để có sự hiểu biết về những gì nó phải học hoặc môi trường mà nó đang học.

Được rồi, vậy thì chúng ta sẽ có 4 x 4 bước tiếp theo và các bước sẽ bằng 2 x 2.

Và nếu bạn muốn tìm hiểu thêm về các biến này, các bộ lọc và các bước tiến, vui lòng thực hiện

một số nghiên cứu về điều đó.

Nhưng hiện tại, nó nằm ngoài phạm vi và nó chỉ làm phức tạp hình ảnh để người mẫu có thể hiểu được

thêm chút nữa là ổn.

Và sau đó chúng ta cũng sẽ có lớp kích hoạt RELU.

Và điều này chỉ giữ tính phi tuyến tính trong trạng thái của chúng ta để chúng ta có thể tìm hiểu các mẫu phức tạp theo đó

chúng ta sẽ có một quảng cáo mô hình được làm phẳng và vì vậy chúng ta sẽ làm phẳng kết quả đầu ra của mình thành một mảng một chiều.

Và cuối cùng, chúng ta sẽ có một vài lớp dày đặc.

Vì vậy, chúng tôi sẽ thực hiện mô hình quảng cáo dày đặc 512 và kích hoạt sẽ là RELU vì chúng tôi muốn duy trì tính phi tuyến tính đó

và chúng ta có 512 nơ-ron đầu ra.

Vì vậy, mô hình của chúng ta đang học từ rất nhiều tham số hoặc nó sẽ có rất nhiều tham số bên trong

của bộ não nó.

Và khi tôi nói thông số, ý tôi là tế bào thần kinh.

OK, tiếp theo chúng ta sẽ thêm một lớp dày đặc khác.

Bây giờ, bạn có thể thoải mái sử dụng các lớp dày đặc hơn và thêm nhiều lớp hơn vào mạng lưới thần kinh này, bởi vì như

Tôi đã đề cập, càng nhiều lớp, kết quả càng tốt bên cạnh 56.

Và chúng tôi cũng sẽ kích hoạt RELU.

OK, và cuối cùng, chúng ta muốn có một lớp cuối cùng, dày đặc và sẽ có đầu ra của một hành động.

Được rồi, và việc kích hoạt ở đây sẽ tuyến tính để chúng ta có thể biến đầu ra của mình thành một trong sáu khả năng có thể

hành động.

OK, và cuối cùng chúng ta có thể trả lại mô hình của mình như thế.

Vì vậy, chúng tôi chạy cái này và bạn sẽ nhận thấy rằng chúng tôi đã thiết lập kiến ​​trúc mạng thần kinh đầy đủ ngay bây giờ.

Và như tôi đã đề cập trước đó, hai biến quan trọng trong mô hình này là hình dạng đầu vào và

đầu ra hành động.

Được rồi, vậy chúng ta sẽ chuyển qua cái này.

Chúng tôi thực hiện một số tính toán bên trong hộp đen và đưa ra một hành động dựa trên trạng thái.

Mát mẻ.

Vì vậy, bây giờ chúng ta có thể học từ bất kỳ màn hình nào mà chúng ta chuyển vào mô hình của mình.

Rất tuyệt.

Vì vậy, hãy xây dựng các mô hình của chúng ta để chúng ta có thể xây dựng mô hình tương đương với mô hình xây dựng.

Và ở đây chúng tôi muốn chuyển về chiều cao, chiều rộng, kênh và hành động.

Bây giờ chúng ta không có các biến này ở đây, vì vậy hãy tạo nó ngay ở trên.

Vì vậy, ở đây tôi sẽ nhấn khoảng trắng này ở đây và tôi sẽ nhấn a và để tạo ra một --

phía trên khối này hoặc bạn có thể vào phần chèn và chèn ô bên trên.

Và vì vậy tôi sẽ tạo các biến này ở đây để chúng ta có thể tạo các kênh chiều cao, chiều rộng.

Bằng phần cuối của hình dạng không gian đặt trước và đó là biến mà tôi đã chỉ cho bạn trước đó, trong đó

chúng tôi có chiều cao, chiều rộng và kênh nhưng chúng tôi đang trích xuất nó thành các biến riêng biệt.

Tiếp theo, chúng tôi muốn hành động của mình có thể thay đổi.

Và vì vậy cái này sẽ bằng và không gian hành động.

Và điều đó sẽ tạo ra các biến cho mô hình của chúng ta.

Và bây giờ chúng tôi đã xây dựng thành công mô hình tìm hiểu môi trường.

Điều đó thật tuyệt vời.

Bây giờ, trong video tiếp theo, chúng ta sẽ bổ sung thêm mô hình này bằng cách kết hợp học tăng cường

một phần của mô hình này.

Vậy tôi sẽ gặp bạn ở đó.

Bây giờ, nếu bạn có bất kỳ câu hỏi, nhận xét hoặc quan tâm nào, vui lòng cho tôi biết trong phần nhận xét và tôi sẽ

hẹn gặp lại bạn trong bài giảng tiếp theo.