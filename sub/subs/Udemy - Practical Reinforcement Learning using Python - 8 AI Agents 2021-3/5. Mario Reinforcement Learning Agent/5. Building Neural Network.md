# 5. Xây dựng mạng lưới thần kinh

---

Được rồi, vậy hãy xây dựng hàm để tạo mạng lưới thần kinh của chúng ta.

Được rồi, vậy chúng ta sẽ tạo một hàm có tên Build Network.

Và vì vậy chúng ta sẽ gọi cái này và chúng ta sẽ chuyển vào self để có thể sử dụng các biến này ở đây.

Nhưng thực ra chúng ta không cần phải làm vậy trong trường hợp này.

Vâng, chúng tôi sẽ sử dụng nó cho hình dạng đầu vào.

Được rồi, vậy ở đây chúng ta có thể làm mô hình bằng tuần tự.

Bây giờ có nhiều cách khác nhau để bạn có thể xây dựng mạng lưới thần kinh của mình.

Nhưng như tôi đã nói, tôi muốn giới thiệu cho bạn các chiến lược khác nhau để bạn hiểu rõ hơn

về những gì đang xảy ra.

Chúng ta sẽ thêm một lớp chập và một lớp chập được sử dụng để xem các pixel trên

màn hình.

Đó chỉ là cách máy móc hiểu được hình ảnh.

Và chúng ta sẽ có 4 x 4 sải chân bằng 4 khoảng đệm, bằng nhau.

Và hình dạng đầu vào sẽ bằng nhau để cho tôi một chút thời gian.

Tôi chỉ muốn chắc chắn.

Uh, vâng, hình dạng đầu vào sẽ bằng không gian trạng thái, vì vậy chúng ta có thể tự áp dụng không gian trạng thái.

Tôi chỉ muốn đảm bảo rằng nếu có cách nào đó, chúng tôi cần phải cấu trúc nó.

Nhưng chúng ta chỉ có thể vượt qua nó như thế.

Chúng tôi sẽ thêm một lớp kích hoạt và đây sẽ là Relu.

Và như tôi đã đề cập trước đây, điều này là để giữ tính phi tuyến tính bên trong mô hình của chúng ta để chúng ta có thể tìm hiểu

các mẫu phức tạp hơn.

Tiếp theo, chúng ta sẽ thêm một lớp chập khác và vì vậy chúng ta sẽ thực hiện conf tuti và ở đây sẽ có kết quả là

64 đơn vị, sẽ vượt qua 4/4, và chúng ta sẽ có bước tiến bằng hai.

Và sau đó chúng ta sẽ có một phần đệm tương tự và sau đó chúng ta sẽ kích hoạt RELU.

Vì vậy, hãy kích hoạt Relu, OK, và sau đó chúng ta sẽ tạo một lớp chập khác để chúng ta có thể

chỉ cần sao chép cái này

Và ở đây chúng tôi muốn có ba bộ lọc và một bộ lọc.

Và sau lớp chập này, chúng ta sẽ thêm một mô hình và làm phẳng để chúng ta có thể làm phẳng đầu ra của mình

của lớp tích chập này thành một chiều.

Được rồi, bây giờ chúng ta có một chiều và vì vậy chúng ta có thể chuyển lớp một chiều này thành một lớp dày đặc

và việc này là để thực hiện thêm một số tính toán trên các lớp chập.

Và chúng ta có thể kích hoạt RELU.

Và hãy sao chép cái này, vì chúng ta sẽ sử dụng một lớp dày đặc khác đến 56 và sau đó chúng ta sẽ có

lớp đầu ra của chúng tôi, lớp này sẽ được mô hình hóa với mật độ quảng cáo dày đặc và ở đây chúng tôi sẽ chuyển sang không gian tự hành động

và điều đó sẽ tạo ra các hành động hoặc các hành động khả thi mà chúng ta có thể thực hiện trong môi trường của mình.

Và vì vậy các lớp này là tùy ý.

Bạn có thể thêm nhiều lớp hơn nếu muốn, nhưng hãy nhớ rằng nó sẽ thêm nhiều tham số hơn vào hộp của bạn.

Nhưng đây là điều đã mang lại cho tôi kết quả tốt nhất.

Vì vậy, hãy thoải mái thử nghiệm nó, có thể thay đổi các lớp đầu ra hoặc tăng số nơ-ron đầu ra

đó.

Bạn có thể làm điều đó, có thể thêm một số lớp dày đặc hơn.

Nhưng hãy nhớ rằng nó bổ sung thêm nhiều thông số hơn, điều này đòi hỏi phải đào tạo nhiều hơn.

Và ở lớp cuối cùng, chúng tôi muốn kích hoạt tuyến tính để chúng tôi có thể ép nó vào

không gian hành động.

Và cuối cùng, hãy biên dịch mô hình của chúng ta và chúng ta sẽ sử dụng hàm mất sai số bình phương trung bình

và chúng tôi sẽ sử dụng trình tối ưu hóa tương đương với Adam.

Và cuối cùng chúng ta có thể trả lại mô hình.

Và thế là kết thúc việc xây dựng mạng lưới thần kinh của chúng ta.

Nhưng hãy lên đây và giả sử việc xây dựng mạng lưới thần kinh cho tác nhân.

Và như tôi đã đề cập trước đó, chúng ta sẽ sử dụng hai mạng lưới thần kinh ở đây và chúng sẽ có

có cùng trọng số, mặc dù chúng tôi sẽ cập nhật mạng mục tiêu, mạng mục tiêu của mình theo định kỳ.

Nó sẽ có ý nghĩa hơn khi chúng ta đi.

Nhưng hiện tại, hãy tạo một mạng chính và chúng ta sẽ đặt mạng này ngang bằng với mạng xây dựng tự học.

Sau đó, chúng tôi cũng sẽ tạo một mạng mục tiêu và sẽ đặt nó theo cùng một kiến trúc cho

mạng chính của chúng tôi, vì những mạng này yêu cầu chúng phải có cùng kiến trúc để chúng tôi có thể chuyển

trọng số với nhau.

Được rồi, vậy là chúng ta có thể xây dựng một mạng lưới như thế.

Và cuối cùng, chúng ta sẽ gọi một hàm có tên là Cập nhật mạng mục tiêu.

Và điều này sẽ chỉ đơn giản là đặt trọng số của mạng chính cho mạng mục tiêu để chúng tôi có thể thực hiện điều đó.

Bây giờ chúng ta có thể đi xuống đây, chúng ta có thể cập nhật Mạng mục tiêu.

Chúng ta sẽ vượt qua chính mình và sau đó ở đây chúng ta có thể tự mình làm mục tiêu mạng.

Đặt trọng số và như tôi đã đề cập trước đó, điều này sẽ đặt trọng số của mạng mục tiêu của chúng tôi thành

mạng chính của chúng tôi để chúng tôi có thể tự thực hiện các trọng số doget mạng chính, được chứ?

Vì vậy, chúng tôi chỉ đặt mạng mục tiêu có cùng trọng số với mạng chính của mình.

Và chúng ta sẽ gọi hàm này theo định kỳ.

Và đây là chiến lược được sử dụng bằng cách mở mắt để tránh dao động và để đạt được kết quả tốt hơn trong

mạng Q sâu của chúng tôi.

Được rồi, vậy là bây giờ chúng ta đã xây dựng xong mạng lưới thần kinh và trong video tiếp theo chúng ta sẽ tạo hàm

điều đó sẽ cho phép chúng tôi hành động dựa trên nhà nước.

Và đó là những gì chúng ta sẽ làm với video tiếp theo.

Và nếu bạn có bất kỳ câu hỏi, nhận xét hoặc quan tâm nào, vui lòng cho tôi biết và tôi sẽ gặp bạn trong lần tiếp theo

bài giảng.