# 16. PEFT - Parameter Efficient Fine Tuning

---

WEBVTT

Now, another important customization technique that we have while working with large language models

is parameter efficient fine tuning technique.

Now, this parameter efficient fine tuning technique is a technique which is designed to adapt the pre-trained

large language models to a specific downstream task, but with minimal modification to the model's parameter.

So whatever the concepts that you have learned so far, like we were not dealing much with the model

parameter, we were introducing some new elements and we were trying to fine tune it for our given data

set, isn't it?

Now, this parameter efficient fine tuning technique.

Here we will perform the minimal modification to the model's parameter.

Now with this approach, we'll be able to address the challenges of fine tuning our large language model

like the high computational cost, memory usage, and the risk of overfitting.

So the best part is, this technique achieves this by only tuning a small subset of parameters.

So we are doing the fine tuning of the model, but we are not performing the fine tuning of the entire

model.

We are just performing the fine tuning on a small set of parameters.

Now we'll do this by leveraging the extensive knowledge which have been already embedded in the pre-trained

model.

Some of the key concepts and the techniques that are being followed in this PhD technique is here.

This PhD technique focuses on updating only a small number of parameters.

So as a result of this, this will reduce the computational resources.

And because we are updating the small number of parameters, we are retaining our general knowledge.

Okay.

And with this we'll have a better generalization on the new task.

Two.

And this methods are particularly useful, especially when we are dealing with the large models like

GPT three, GPT four, or any such models, or even T5, where the full fine tuning would be infeasible

due to the resource constraint.

Now, there are some key concepts and techniques that are part of this pbft technique.

So one is the adapters.

So in this BFT technique we'll have something called as the adapters.

We will have a small neural network module and that will be inserted into each layer of the pre-trained

model.

Now during the fine tuning activity okay, only these adapters are trained while the rest of the model

remained fixed.

And this technique is called as the adapter technique.

And there is another technique that is used that's called as Laura which is known as low rank adaptation

technique.

Now this Laura introduces the low rank decomposition matrices into the models layer.

Now with this, only the low rank matrices are updated during the fine tuning activity, and the prefix

tuning technique is also used in the PhD, where we are going to prepend the learnable prefix vectors

to the input at each layer.

Now these vectors are the only components that is updated during the fine tuning, and we will also

make use of bit fit technique where bias term fine tuning where we are going to update only the bias

terms of the pre-trained model while keeping the weight matrices unchanged, and that is layer wise

freezing technique used.

Now this is going to freeze the majority of models layers and only fine tunes the top few layers or

specific components of the model.

Now let me illustrate it with a high level example.

Let's say you want to use this BFT technique to perform a task.

Let's assume a text classification.

And now you have chosen to make use of a large language model like the Bert as a large language model.

Now what you will do is you will make use of the adapters.

So you will make use of this small adapter modules, and you will be adding it to the overall architecture

that we have for this bird.

Okay.

You will make use of small adapter modules.

And this will be added to the architecture of the bird.

And each adapter will consist of some kind of a down projection.

We call it as a reduced dimensionality.

And it will also have some kind of a nonlinear activation.

And it will have an up projection which will restore the dimensionality.

So that is what it would have.

So it will have something called as a down projection nonlinear activation plus up projection.

Okay.

This is what your adapters would have.

Now during the fine tuning activity, only the weights of these adapter modules are updated based on

the task specific data.

While the original parameters of your Bert model would remain unchanged.

Okay, you will perform the forward pass, and once you perform the forward pass, you will go ahead

and update the same.

Now when I talk about this Laura technique right here.

So this is another efficient approach under this parameter efficient tuning.

Now inside this Laura.

So what you will do is you will introduce the low rank matrices.

Now you can make use of a pre-trained model.

And in each layer the weight matrices is decomposed into some form of vector and matrix product, which

is basically the low rank rule, low rank matrices.

So that's the another technique that we have got that is low rank adaptation.

And similarly we have the prefix tuning which you are already aware of at a very high level.

And same thing the bias terms fine tuning technique.

So these are all the techniques that can be used under this PhD.

I can make use of all these techniques while performing the fine tuning.

Or I can selectively use some of them depending on our requirement.

So I can say this is one of the powerful paradigm for adapting the large language models to a specific

task in an efficient manner by updating a small subset of parameters so the techniques like adapters

Laura and prefix tuning, bit fit and others, they are actually enabling the resource efficient fine

tuning while preserving the model's general knowledge, which is a practical way to deploy and customize

our large language models for a variety of applications.

So with this, we come to the end of this video guys, and I'll see you next time.