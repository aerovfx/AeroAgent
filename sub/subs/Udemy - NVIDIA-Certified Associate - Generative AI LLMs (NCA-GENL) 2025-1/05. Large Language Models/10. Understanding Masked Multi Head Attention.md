# 10. Understanding Masked Multi Head Attention

---

WEBVTT

Now, apart from this multi-head attention, you also have another component while working with this

transformer architecture that's called as masked Multi-head attention.

So let us understand what exactly is this masked multi-head attention?

Now the masked multi-head attentions primary purpose is to maintain the autoregressive property in a

sequence generation task, especially in the tasks such as machine translation or text generation.

That means here the idea of this masked multi-head attention is to ensure that whenever it is predicting

the new word okay, whenever it is predicting a new word or a new token, the model should only consider

the words that have come before it and not the ones that follow.

So that's the overall idea about this masked multi-head attention.

So the main idea is to ensure that this will enforce the causal relationship, where the output at each

timestamp depends solely on the previous input.

Now, now this is especially essential for generating the coherent and the meaningful sentences.

So this is to ensure that whenever it is performing the what we call it as the the next state, That

is basically the next word in such scenarios.

Okay.

We would go ahead and generate we we have to look into the only the previous step.

That means we just have to look into the previous step and previous words, not on the future words.

So that's the core idea about this masked multi-head attention.

It will become clear in the next video when we explain in detail.

For now, just understand that the idea of this masked multi-head attention is actually part of my decoder.

When it is generating the new text at that time, it has to simply look into the future words.

Okay?

It has to look into the past words when it is generating the future words.

So that's the idea over here.

And the mask helps us in stabilizing the training by preventing the model from cheating.

That is, by looking ahead in the target sequence, and especially during the inference, this mask

skill still applied to ensure that the model generates the text one word at a time.

So that's the idea.

So that's the idea about this masked multi-head attention.

Now that you have the high level idea about this masked multi-head attention, let's deep dive in the

next video.