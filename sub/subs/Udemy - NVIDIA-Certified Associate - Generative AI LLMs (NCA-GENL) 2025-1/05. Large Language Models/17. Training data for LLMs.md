# 17. Training data for LLMs

---

WEBVTT

In this video, let's understand as what kind of data that we would use it for training for large language

models.

We have seen already about the large language model and how it works.

Now, it's important that you have a general idea about how do we select the training data for large

language model.

So one of the common resource that we generally use it for training data preparation for this large

language model is web text corpora, which means we are going to make use of the abundant data which

is available over the internet, and we will do it with the help of.

We will collect the data by making use of various web crawlers, and we will crawl the entire internet

and get this web text corpora.

So by crawling the web data.

So we'll be able to get the data set on variety of topics, and we'll be able to get the data in various

writing style vocabulary.

And this will help us to mirror the real world use of the language.

But there are some challenges when we are using this web text corpora.

So one key challenge is the web text the information that we would collect from internet.

So it might have some noise as well.

It might contain misinformation.

There will obviously be biases because each and every person have their own opinion.

And it might contain the some some of the potential harmful content to.

So it's very important that we apply careful filtering and cleaning whenever we are trying to use such

web text corpora for training our large language model.

Some of the common examples for crawling is.

Maybe we can make use of common crawl or web text.

So these are the ones that are used to train the GPT based models.

Okay.

Now the other resource which makes up good candidate for training data is the books.

The books represent the curated collection of high quality text, and this is often with a more complex

sentence structure, and it also contain the domain specific vocabulary.

If you pick up any textbook or any book so it will have the condensed knowledge, it will definitely

contain the domain specific vocabulary which are suited for that certain audience.

So by making use of these books.

So we'll be able to provide a well structured, coherent narratives about a given subject.

And we'll also be able to enable our large language model to have an in-depth, exploited exploration

of these various topics.

But one of the challenges that we would get when we are using this books as a resources, so this would

lack the conversational tone, which is what we would expect when we are working or interacting with

the large language model, and when we want the human touch that is actually missing in the books.

And it will be making more of a formal language instead of the informal language.

Okay, some of the examples are Project Gutenberg book corpus.

So these are all the resources that are available in the books category for us.

The other resource that that can be good candidates for training are large language models are basically

the code repositories.

The code repositories, like GitHub, host millions of lines of code in various programming language,

which makes it an apt candidate for preparation of the training data.

So with this repositories like GitHub.

So it will be advantage for us.

It is essential for training the models to understand the overall programming concept.

This would help our large language model to generate code snippets and even debug it.

Debug the existing code.

But some of the key challenge would include.

It requires specialized preprocessing in order to handle such structured nature of code and commands.

So some of the common resources that we can use it for code repositories can be GitHub and StackOverflow.

The other candidate for preparing the training data is the scientific literature.

The research papers scientific journal textbook.

They contain highly specialized language and the domain specific language.

So these are having the domain specific knowledge.

And with this we get to have an advantage because here the scientific literature are crucial for models

which are aimed at scientific tasks like summarizing, summarizing the research, answering the technical

question, or even generating the scientific hypothesis.

But there are some challenges over here with this approach, because here the scientific literature

are often dense and too much technical in nature, and in some cases it requires the deeper understanding

of the subject matter for the effective processing of the text.

So some of the common resources that would that that is available for the scientific literature is arXiv

and PubMed.

Now, news articles are another good resource for training data for this large language model.

The new sources actually offer the factual information on current events, and this is often with a

focus on clear and concise writing.

So some of the best advantages that we would get when we are using these news articles.

Because like this.

This would help the model to stay up to date with latest information and understand how language is

used to report on real world events.

And one of the key challenges.

So the news articles have got a potential for bias depending on the news source.

So fact checking and the source verification are little important when we are using this news articles.

Some of the examples for news resources can be New York Times, Reuters, BBC news and so on.

Then we have conversational data.

Conversational data are good because dialogue from chat logs, customers interaction and social media

conversation will provide a good resource.

It provides a rich resource, we can say, as how humans naturally interact with each other and how

humans exchange information through the language.

But here, just like we have seen with other resources, so it contains slag typos, fragmented sentences,

it requires careful cleaning or normalization.

It might have the potential bias terms as well.

So because of all these reasons.

So it's very important that we perform careful cleaning and the normalization.

Some of the good resources for compositional data would be Reddit comments, Twitter conversations and

so on.

The other data which are which can be used on the large language model, which has been which is being

used in most of the large language models.

Training would include the structured summary of various topics like reports, legal documents for which

is useful for model that needs to understand the legal language and reasoning, historical text to help

the models and understand the language evolution and the cultural context and so on.

So this was about the training data of large language models.

So I hope this gave you an insight about what kind of languages that we can, what kind of data sources,

language sources that we can use it to in order to perform the training of our large language model,

and what are the challenges that we could have in each of these sources?

The examination will test you the basic understanding of these challenges that you would get in variety

of resources.

I'll see you next time.