# 19. Data Cleaning for LLMs

---

WEBVTT

In this video, we are going to understand about the data cleaning strategies whenever we are preparing

the data for large language models.

So when we think about data cleaning strategy, the first thing that we'll be focusing on is removing

of the irrelevant content.

This involves identifying and removing the content, which is not useful for relevant training data

set.

So whenever we think about training the large language model, there will always be some data set which

will not be useful for us, especially when we are preparing the training data.

Now this could include the HTML tag scripts advertisement.

This can also include the boilerplate code or the content in the languages other than the language that

we are interested in.

In such scenarios, the commonly used techniques are.

One is regular expression we use it to match and remove the specific patterns in the text.

The second one is language identification, where we use it to detect and remove the content in the

irrelevant language.

The third one is heuristics, where we can make use of rule based approach to identify the irrelevant

content based on certain characteristics.

It can be length and formatting.

By using this technique, we will be preparing the data set by removing the irrelevant content.

But this doesn't end here.

We'll also have to take care of other activities as well whenever we are actually cleaning the data

set.

So this involves okay this involves deduplication.

Now when we say deduplication this involves identifying and removing the duplicate text entries, which

can skew the model's understanding and it can lead to overfitting.

And another like the techniques that we'll be using in this deduplication are exact matching comparing

text strings, which are character by character.

The other techniques that we want to use it are fuzzy matching, which is identifying the similar or

near duplicate text using the algorithms like Levenshtein distance hashing, which means creating the

unique hashes for text strings to quickly identify the duplicates.

So this is how we take care of duplicate entries, but there can be a scenario where we are missing

some values itself.

Now, whenever we are missing the value in such scenario, addressing such missing or incomplete data

points is crucial, so this can involve removing the entries with missing data, which can be imputing

the missing values based on other data point, or replacing them with a special token.

Here, some of the commonly used techniques are deletion, which means removing the entries with missing

values, or imputation that is, filling in the missing values using the statistical methods such as

mean, median, or regression.

Tokenization, which means replacing the missing values with unique tokens, or a special token that

is called as unk for unknown.

So this is how we handle the missing values.

We have to take care of it, else we are going to run into errors from the language standpoint.

We'll also go ahead and check the spelling and the grammar as well.

So we'll be interested in correcting the spelling errors, grammatical mistake as well as the typos,

so that we can improve the overall quality of the text here.

Some of the commonly used techniques would include rule based spellcheckers.

So using the predefined rules, we can use it to identify and correct the common spelling errors.

We will be interested in statistical language models as well.

We can leverage the probabilities to suggest the likely correction in the given text data.

And the other technique is neural spellcheckers, where we can employ the deep learning models, which

has been trained on large text corpus for more accurate correction.

Then we have something known as normalization.

So normalization involves standardizing the text formatting.

Text formatting capitalization punctuation and other inconsistencies that we have on this data set.

Some of the commonly used techniques under this normalization which are relevant for examination are

lowercasing converting all the text to the lowercase.

Removing the punctuation.

Handling the special characters, and tokenization.

So let me tell you guys, out of all these techniques.

Tokenization is one of the very important technique from the perspective of examination.

We have two types of tokenization.

One is word tokenization and the other one is known as subword tokenization.

When we think about word tokenization, we'll be splitting our document that is our sentence into individual

words.

That's called as word tokenization.

And we have subword tokenization here.

In this case, we will split the text or document that we have got in terms of the vocabulary which

my large language model is aware of, and that's called as subword tokenization.

So out of these two methods, the subword tokenization is the one that is widely used whenever we are

training these large language models.

Then we have bias mitigation.

Bias mitigation involves identifying and mitigating the biases in the training data.

It can be gender, racial, or other social biases too.

And this is especially critical for ensuring the fair and equitable LLM output.

Some of the commonly used techniques under this bias mitigation are.

We can make use of statistical methods or machine learning models to identify the potential biases.

Data augmentation.

This will involve introducing more diverse data in order to counter the existing biases.

The third method is Debiasing technique.

We can actually make use of algorithms or post-processing techniques to reduce the impact of biases.

Then we have something for fact checking and verification.

Now, this is especially crucial whenever we are building the large language model to inform people,

or creating something which will be used by the people which is trustworthy.

So in such scenario, it's very important that we ensure the accuracy and the truthfulness of the information

in the training data that we have got, especially when we are working for the task like question answering

and summarization.

To do that, some of the some of the commonly used techniques are external knowledge base.

So we can compare the information in the training data with the external sources.

For verification.

Fact checking models, we can employ the machine learning models which has been trained to identify

false or misleading information.

Human review.

So manually review of the training data by experts in order to verify the accuracy.

Some of the other approaches that has been recommended are unsupervised learning.

So you can make use of clustering or topic modeling.

So you can actually use it to identify the patterns in the data and filter out the irrelevant or noisy

content.

The other techniques would include active learning, so we can make use of human annotators so they

can selectively label a subset of the data.

Now we can actually use it to train models to perform automated cleaning tasks.

Then we have reinforcement learning.

So this approach we can use it to train the model that can learn to identify and correct the errors

in the data, which is based on the feedback.

Okay, so this is about the other approaches that we can use it to clean the data for large language

models.

Now once we do that what are the benefits.

Are we going to enjoy.

So some of the data cleaning benefits for LMS one improved model performance.

So clean and high quality data will lead to better trained model with higher accuracy and reliability.

Second, reduced bias and fairness.

We can mitigate the bias in the training data.

This will ensure that the Llms are fair and equitable in their output.

Third, one enhanced generalization to diverse and representative data helps the model generalize better

to unseen data, and it will also help us in avoiding the overfitting, then ethical considerations

should responsible data cleaning practices will ensure that the llms do not perpetuate the harmful stereotypes

or the misinformation.

So these are the key benefits of data cleaning for large language models.

So folks, with this we come to the end of this video and I'll see you next time.