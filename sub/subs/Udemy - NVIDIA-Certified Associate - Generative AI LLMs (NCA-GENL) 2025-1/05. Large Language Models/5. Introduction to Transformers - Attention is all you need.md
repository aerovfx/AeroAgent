# 5. Introduction to Transformers - Attention is all you need

---

WEBVTT

Hey everyone, welcome back to your learning journey.

Now in this video let's have an introduction to the Attention is All you Need research paper which introduces

the transformer model architecture.

We have already learned about the importance of this transformer architecture.

The large language models are based on this transformer architecture, where the encoder and the decoder,

which are the key components which are powering the large language model that is enabling the large

language models to understand the context.

So it is important that you understand the transformer architecture in detail.

In order for you to understand and work with these large language models.

So let's understand why did this research paper came into picture, and what exactly are the main components

of this transformer architecture?

That would help you to understand as how the text is being processed using this transformer architecture.

Now, before this transformer architecture and the large language models, in order to understand the

context from a long, long sequences, we were actually making use of the sequential architecture,

which we call it as the recurrent neural network, and the LSTM networks, which is nothing but the

long short term memory neural networks.

Now, when we were dealing with such neural network architecture, one key challenge that we faced was

there was a limited parallelization during the training.

Now what happened was if you have got a text, okay, if you have got a text which contains ten words,

then you have to process one word after the other.

To give you an example, suppose I have got a text document which says as machine learning.

I'll just mention a random text.

Suppose if I've got a document that says has.

Machine learning is a branch of AI.

The way that we were processing the data with the help of this RNNs, first we'll perform the tokenization

using our text pre-processing techniques.

Then I'll generate some form of mathematical representation for each of the words.

So one of the best way that we were that we can convert this words into the vector format is basically

making use of word embeddings.

So we were making use of the word embeddings technique and using this word embedding technique, we

were converting each of the words into the vector format, assuming that this is one vector, another

vector, another vector.

So each of the vector represents the numerical representation of each words, which considers that similar

sounding words will have a similar vector structure.

Okay, that's the basic idea that we are aware of the pre-processing.

Now, once this pre-processing was done, the way that we were feeding these data to our RNN was we

were feeding it in a sequential way.

First I was sending the word machine, and this is going to send it to my RNN, one that is my neural

network at the timestamp one, and I can have series of such RNN neurons to make it deep RNN network.

Okay.

The idea is I'll send that to the network and I'll generate some output.

Now I'll be sending the second word.

And while processing the second word for this RNN, I'm going to consider the second word as the input.

And I'll also consider the output from the first RNN as an input.

I'll say oh one okay, maybe I'll call it as oh one.

And this is maybe T2, which is timestamp two.

The second word and I'll generate some output over here.

This hidden representation I'll call it as O2.

So like this we were processing the words in a sequence.

Even though I may, I may be sitting on top of let's say powerful GPU and powerful CPU.

Because of this architecture limitation, I'm still able to process the data in sequences.

You can clearly see the challenge.

I have a limited parallelization, especially while performing the training, which makes it slower

for my neural network to train.

And with this architecture itself, you can clearly observe it is having a difficulty in capturing the

long range dependencies.

So if I have the data timestamp ten, that means 10th word.

If I'm processing it, I'll be getting the hidden representation, let's say oh nine okay.

When it is processing this timestamp ten word.

Now imagine that you have to find the dependencies with the word, which is a timestamp one.

So that's one of the challenge that we had with recurrent neural network because it cannot remember

the data over longer sequences, because it is limited by this bottleneck of this single hidden state

representation.

And this leads to inefficiencies when it is dealing with longer sequences.

And these longer sequences will also lead to slower computation.

So these were the key challenges that we had with the recurrent neural networks and the similar challenges

that we had with the LSTMs as well.

And that's when the transformer model architecture was actually introduced.

Now, this transformer architecture was basically introduced as part of the landmark paper in the year

of 2020, 2017 by Vaswani et al.

And they have introduced this transformer architecture, which is a revolutionary approach, especially

for the sequential processing task that could include translation as well as the language modeling.

Now this is the architecture diagram of this transformer architecture.

Now if you are seeing this for the first time so don't worry.

So it will become familiar as you progress in your learning journey.

Just be with me.

I'll explain each and every component with a relevant example, so that you can understand what is happening

inside this transformer architecture.

Okay, so if I want to name some of the key features of this transformer architecture, one thing,

as you can clearly see, we have got something called as positional encoding.

This is one of the key component of this transformer architecture.

We have multi-head attention.

Okay.

This is one key component.

And overall the left part we call it as the encoder.

The right part we call it as the decoder.

Okay.

The encoder is going to encode the given input.

And decoder is going to decode the processed encoders.

And we also have something called as self-attention in the given data.

And we also have the masked multi-head attention as well.

So these are all the key components of this transformer architecture.

The self-attention over here.

It's the core of this transformer architecture.

And here with this self-attention mechanism it would allow the given words in a given sequence okay,

the words in a given sequence to weigh its relationship with all the other words.

So as a result of this, this would help the model to determine which are the most relevant words to

its meaning.

That's the core idea about this self-attention.

And we also have multi-head attention like when we talk about this multi-head attention the multiple

self-attention layers operate in parallel, so each will focus on different aspects of the input sequence.

It's basically the combination of self-attention, and we will have multiple such attention and we call

it as multi-head attention.

This would enable the words to understand its context in a much deeper sense.

We have positional encoding.

So here in the case of this transformer architecture it's going to process the words in parallel.

Now to ensure that the words are considered the ordering of the words are considered.

We will also encode it with the help of this positional encoding.

Okay.

So it will add the positional encoding for the given input embeddings so that we can convey the word

order and the overall encoder decoder architecture to process our input sequence.

And the decoder will generate the output sequence.

So both of these encoder and the decoder will consist of self-attention and the feed forward layers,

which will be discussing as we progress in our learning journey.

So to summarize guys, this transformer architecture is one of the landmark research paper in the domain

of natural language processing, which is powering most of the large language models that you see these

days.

So we'll have a deep dive as we progress in our learning journey in this section.

So we'll end the video over here and I'll see you next time.