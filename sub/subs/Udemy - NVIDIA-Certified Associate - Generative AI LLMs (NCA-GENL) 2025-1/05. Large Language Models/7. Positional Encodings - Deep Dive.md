# 7. Positional Encodings - Deep Dive

---

WEBVTT

So we have learned about this positional encodings.

Now in this video let's build a deeper understanding about these positional encodings.

Now the idea or the purpose of this positional encodings is we can inject the position in which a particular

word is present in a given sequence.

So that is the idea about this positional encodings.

And in order to generate the same, we are going to make use of the sine and cosine function.

And whatever the output that we would get, we would then go ahead and add it to our word embeddings.

Now let us try to understand the high level concept as how it actually works.

And then I'll take you to the mathematics part.

And we'll also generate some positional encodings for some dummy data.

So let us consider the sequence which says as dog is crossing the road.

Now in this scenario the sequence is dog is crossing the road.

And if I do the tokenization, dog is crossing road.

Okay.

That's the overall tokens that we have got.

Now once the tokenization is performed, obviously we'll have to perform the feature engineering or

feature generation.

So let's assume that I'll make use of pre-trained model.

And I'll go ahead and generate the vector representation for each of the tokens.

So the values that you see right here 1215 minus two eight.

Let's assume that these are all the word embeddings for the given word.

I'm just showing you for the intuition purpose.

These are all just the random values.

So the idea is we will convert the word into a numerical format.

So that's the first step.

Then whatever the positional encoding that we generate we are going to add it to the word embeddings.

In this way we are going to inject the position details to the word embeddings.

So that's the overall idea about this positional encoding.

Now in order to generate this positional encoding.

So this is the formula that we would use.

So that is what we have learned so far in our learning journey.

Now let's take up a dummy example.

And let's try to understand how these values would be generated whenever we are working on the data

set.

So let us consider the example and we will follow some values.

We will follow the same formula, but we will consider the different values.

So for the demonstration purpose what I'm going to generate is I'm going to generate the positional

encoding with the number of dimension as four.

So I'm going to specify some values like this.

I'll say d is equal to four.

This corresponds to the number of dimension.

Now in the research paper they had mentioned as 10,000 as the value over here, the value that is over

here, as you can clearly see it is 10,000.

Now what we will be doing is instead of 10,000 we are going to consider this value of n is equal to

100.

Okay.

That is what I would consider for the example purpose.

Now with this n is equal to 100 and d is equal to four.

Let us go ahead and generate the positional encoding for the text.

So what is the text that we are talking about?

We are talking about the text for dog is crossing Road.

So let's go ahead and do that.

I'm going to mention some columns right here so that it is easier for us to get started.

So the first column I'm going to call it as sequence.

And the second column I'll say index of token.

That's my second column.

And for the third column it's basically I'll be mentioning the values of I for the positional encoding,

I'm going to call it as positional encoding matrix.

This is what I would call this will be my positional encoding matrix with let's say D is equal to four

and n is equal to 100.

Now what I would have is obviously as a result of this will be requiring multiple columns.

Since my d is equal to four that means I'll be having four dimension.

This is dimension one, dimension two, dimension three, and this one will be the dimension four.

Now I would go ahead and mention the values of my sequence that is ease crossing road.

So this is what I have right here.

Okay, now with this thing in place, let's go ahead and generate the positional encoding using the

formula that I have described above.

So in this case I'll start with for the word dog.

So for the word dog, the way that I'll be representing is something like this.

This is at the index position zero okay.

So I'm going to mention the position as P00.

So what would be my P00.

Let's go ahead and let's look into the formula right here.

So here if you try to observe right here it says sign.

So the position is zero okay.

The position is zero sine of zero divided by here instead of like instead of 10,000.

We just have it as 100.

Let me just show it to you okay I'll show the calculation right here.

I'll say P00 is equal to sine of zero divided by my position is zero.

Right sine of zero divided by 100.

Power two into I.

That is nothing but the zero itself in this case.

Okay.

See here the values are.

This is I is equal to zero and this is I is equal to zero.

I is equal to one and I is equal to one.

These are the values of I.

So since the I is zero 100 power two into I, that is two into zero divided by a D model.

So in this case the D model is nothing but four.

This is what I would have.

So sine of zero 100 power zero is basically one anything power zero is one.

So sine of zero by one which is nothing but zero.

So p of zero zero will be equal to zero in this case.

On the other hand.

So I'm going to say P01 here instead of sine.

This time I'll have cos.

So since the cos will be the formula is same okay P01 is equal to cos of zero by one.

This is what we would get.

So cos of zero will be equal to one.

So the values would be zero one.

And this is also zero because I'll have sine and this will be cosine.

So B11 is equal to zero and P12 is equal to one okay.

So this is what the values are okay.

The better way to represent it is it's not it's it's actually I'll call this as P02 is equal to sine

zero by uh ten which is nothing but zero.

And this is actually

P03 that is equal to cos of zero by one, zero by ten actually.

Okay.

So this will be equal to one.

So why are we getting it as ten over here okay.

It's very simple guys.

See what we are seeing right here.

So here in this case okay.

If you look into the P03 it's an interesting one.

This is equal to cause of what will be a top.

So obviously the position is zero right here.

So this will be zero.

That is the index of my token okay.

So this is my index of my token which is nothing but pos pos okay.

That is my position.

So that is zero divided by I have got 100 power two into I.

This is equal to one divided by what is the dimension of my model.

It is equal to four okay.

Because c I have d is equal to four, which is nothing but cos of zero divided by 100 power two into

one by four.

It is nothing but one by two.

So 100 power one by two it is nothing but square root of 100.

What is the square root of 100?

That is equal to ten?

And that is what I have mentioned right here when we when we mentioned as cos of zero by ten, which

is equal to one.

So in this way I'll be generating the positional encoding for the word here.

In this case that is dog in the same way for the second token that is token of one.

So this will be P10 in such scenario.

So right now I'm going to show you for P10.

So P10 sign of my position is equal to one.

This time the index is 100 power two power two times I.

It is nothing but zero divided by four.

So sign of this would basically uh one divided by one.

So sign of one.

So if we simplified, I think it will come closer to 0.84.

Now in this way we can do the simplification.

And I'll get the values as 0.840.54, 0.10, 1.0.

And if I fill in the other values, since I have my personal nodes.

So the values will be something like this 0.20.98 and 0.14.

-0.990 .30.96.

So this is how the positional encoding that we would get with the positional encoding of my model with

dimension.

As for where the value of n is equal to 100.

So these are the values of my positional encoding matrix.

So that's the core intuition behind this positional encoding matrix.

Now to get a better understanding of this for you to get more confidence of these values, let's go

ahead and implement it in a hands on manner.

In order to implement it in a hands on manner, I would go ahead and launch my Jupyter notebook.

And over here I'll start by importing my numpy library and we will implement the same formula guys.

Okay.

And to have a better visualization I'll also go ahead and we'll go ahead and import the matplotlib library

as well.

So I'll create a function I'll say get positional encoding okay.

And I'm going to get the sequence length.

And I'll get the value of D.

And I'll set the value of n is equal to 10,000 as per my research paper.

Then I can go ahead and initialize the value of p.

So I'll say p is equal to NP dot zeros.

And this will be of the dimension sequence length and the number of dimension.

This is the dimension of my positional encoding matrix.

Then I will say for k in range sequence length.

Okay, so for I have to generate each and every value.

And I'm going to mention the logic.

As for I in NP dot arange and I'll say int of d by two, because this is what we'll be using.

And I have described it in our explanation as well.

So we'll be considering number of dimensions by two.

And I'll say denominator.

See the denominator is common I'll say denominator is equal to NP dot power n power two times I divided

by d.

So this is what my denominator term is.

Now I'm going to say p k comma two times I okay is equal to np dot sign of k divided by denominator

and p k comma two times I plus one okay.

Which is very much similar to what we have seen in the formula.

So NP dot cos of so sign sin and cos k divided by denominator.

Now once that is done finally I'm going to return the positional encoding matrix.

Now for the demonstration I'll say get positional encoding.

We'll consider the same example.

I have a sequence of let's say four okay.

Let us do a quick check.

One.

Two.

Three four.

Yes, my sequence is having four.

That is four words.

Then I'm going to generate the positional encoding where the matrix is having four columns.

So d is equal to four and n is equal to 100, just like we have considered earlier.

Now if I display the value of p see I've got the values as 01010.841, 0.54, 0.998, which is what

I had described earlier in the table.

So that is the overall idea of generating the positional encoding.

So we'll be generating the positional encodings in this manner.

And once we generate the positional encoding we will add this positional encoding to our word embedding.

So if I want to explain you in a better way with some more illustration so you can think of it in this

way.

So suppose I've got my input sequence.

Let us assume this is my input sequence.

Okay.

Now in my input sequence I have four words in this example.

So dog is crossing road.

So these are the four words that I have got in my input sequence.

So what we will do.

So for this input sequence we will go ahead and perform the activity of word embedding.

So I would get the embedding vector.

So I would call it in this manner.

So I'll say like this is my word embedding vector v v0.

This is my word embedding vector v1, word embedding vector v2 and v3.

So this is basically my word embedding.

Now what I'm going to do is once I generate these word embedding next I would go ahead and generate

the positional encoding.

So I'm going to call it as this is my positional vector p zero.

Positional vector p1, positional vector p2, and positional vector p3.

So we have the individual positional vector.

Now what we are going to do is we are going to add the respective positional vectors or positional encodings

with our word embedding.

So as a result of this we are going to have the output from this positional encoding is basically saying

as v zero plus P0V1 plus P1V2 plus P2V3 plus p three.

So this is the output of the positional encoding.

So this is my positional encoding.

And this is basically my output.

And the above one is my input sequence.

So this is the intuition behind working of this positional encoding.

Now if you take a look into our slide as well.

And if you look into the architecture of this transformer it says the same thing.

See I've got the input data.

I'm going to have a word embedding.

This is going to generate the positional encoding matrix.

The place over here describes the addition of word embeddings with the positional encoding.

So that's the output from this step.

So this is the overall idea behind this positional encoding.

I hope this gave you an in-depth idea about how does this positional encoding will be created and used.

And I'll see you in the next video.