# 14. Customization of LLMs - Prompt Learning - Prompt Tuning & p-tuning

---

WEBVTT

Moving on in the techniques of customization of the large language model.

The next technique that we have got is called as the prompt learning.

Now, in the scenario of this prompt learning, we make use of prompt and the completion pair with the

help of virtual tokens.

Now, in order to perform this prompt learning, we require the moderate data and the moderate compute

resources.

Now coming to the effect while this has got better performance compared to the prompt engineering technique,

now coming to this prompt learning, there are two types of prompt learning.

One, it's called as prompt tuning and the other type that's called as p tuning.

In the case of prompt tuning, we are going to have the soft embeddings for specific task.

And in the scenario of p tuning we will make use of prompt encoder, which will preserve the past tokens.

So let's understand in detail about this prompt learning technique.

So under this prompt learning the first thing that I mentioned is the prompt tuning technique.

So this prompt tuning is basically a technique.

And we use it to adapt the large pre-trained large language models like it can be Bert or Bert or GPT

three to a specific task without modifying the models parameter.

That means we are adapting the already trained large language model to a specific task, without modifying

the model's parameters.

This is an important thing that you need to remember.

You are not modifying the model's parameter, so what you will do is instead of fine tuning the entire

model which will result in updation of model parameters, what you would be doing is you will take up

an optimized approach where you will optimize a small set of additional input embeddings, which we

call it as soft prompts, and we are going to append it to the original text.

So by doing this, we'll be able to reduce the requirement of computationally expensive and the time

consuming task of the tuning itself.

That is, the fine tuning of large language model.

So with the help of these soft prompts, this would help us to take the models prediction towards the

desired task.

Now, when we talk about this prompt tuning, the way this is going to work is first we'll start with

the pre-trained large language model.

Then we are going to introduce soft prompts, which is nothing but the learnable embeddings which are

appended to our input text.

Then, as part of the step three, what we are going to do is we are going to optimize these soft prompts

on a small task specific data set.

Now, once that is done, we are going to use it for the inference.

So we'll use this optimized soft prompts along with our original input in order to get the task specific

predictions from the given model.

So that's the overall idea about this prompt tuning technique.

So you are going to make use of these soft embeddings.

That is what you need to remember from this perspective.

Okay.

Now the coming to the benefits of prompt tuning one, because you are adding the soft embedding and

you are not fine tuning the entire model, it requires fewer computational resources compared to the

full model.

Fine tuning and coming to the implementation itself.

It's relatively simple because only a small number of parameters are being tuned and coming to the flexibility,

it can quickly adapt to the same model it.

It can quickly adapt our same existing model to the multiple task by using the various sets of soft

prompts.

To give you an example, let us assume that we've got an original input that says as the movie was fantastic,

now the first thing that you're going to do is you will perform the soft prompt initialization.

So at this step you're going to introduce a set of learnable embeddings, which we also call it as soft

prompts.

And this is called as a soft prompt initialization.

And we'll let's say we'll call it as p is equal to P1P2P3 up to p n, which are nothing but the learnable

embeddings.

Then what we will do is we are then going to modify the input with this soft prompt.

So instead of we directly feeding the sentence to the model, we are going to concatenate the soft prompts

with the sentence.

So let's assume my sentence is the movie was fantastic.

Okay, I'm going to concatenate the soft prompts before this original sentences.

So my original sentence will be changed as p one, p two up to p and comma the original sentence.

So this is the modified input with the soft prompt.

Then what we are going to do.

We are going to train these soft prompts on a sentiment analysis dataset.

Like for example, let's say you have got a data set on the movie reviews which has been labeled as

positive or negative.

You would go ahead and perform the training on this given data set.

Now during the training, only the soft prompts are actually updated while the rest of the model remains

unchanged.

That means you're just going to update these embeddings, that is the soft embeddings.

The rest of the original model will stay as it is.

Now here, once the training is completed, we would go ahead and use this optimized soft prompts for

generation of the prediction.

Now the model will receive the optimized p one, optimize p two up to p n, and we are going to send

in the new text, whatever the original text that we want to generate the output, we will send that

as an input.

And the model is going to generate the output that says as the sentiment prediction is positive.

So what you have done in this overall technique is first you went ahead and created the soft embeddings.

You have performed the training.

Okay.

Now during this training you'll only be updating your soft prompts.

And once your overall training has been completed, you would then go ahead and generate the prediction

using this optimized soft embeddings and you will be appending this optimized soft embeddings even for

the new sentences for which you want to generate the inference.

So that's the idea behind this technique that we are talking about.

That is this prompt learning technique.

Okay.

Now apart from this technique we also have another technique that's called as p tuning technique.

So the technique that we have just seen is called as prompt tuning.

Now this is called as p tuning.

Now what happens inside this p tuning technique.

While in the scenario of this p tuning technique we are going to make use of the prompt tuning with

continuous prompt embeddings.

So this is a technique where we are efficiently adapting the present large language model whatever we

have got into a specific task.

Okay, we are going to efficiently adapt our large, pre-trained large language model to a specific

task.

Now, you know, already when we talk about the fine tuning, this is going to adjust the entire model

parameter.

Now this p tuning.

This will optimize only a small set of task specific parameters, and we call this as prompt embeddings.

Now what you will do is here you are going to retain the pre-trained model knowledge, and you will

reduce the computational cost of tuning.

That means we are doing some kind of tuning.

But here in the case of this tuning, you are going to optimize a small set of task specific parameters.

That is what you are doing right now.

So the way this is going to work is we'll start with large language model, which is a pre-trained large

language model.

It can be GPT, Bert or any version of the pre-trained large language model.

And you will introduce a small set of learnable embeddings.

Now this time we call it as prompt embeddings.

And we are going to add it to the input text.

And we will optimize these prompt embeddings using the task specific data set.

And you would then go ahead and perform the inference.

So we will make use of the optimize prompt embeddings along with our original input, in order to get

the task specific predictions from the model.

Some of the key benefits that we are going to get is one is the efficiency, because only a small number

of parameters.

That is, the prompt embeddings are actually updated and this makes it computationally efficient.

And since we are not modifying the existing large language model, we are preserving the knowledge.

And with this technique we can actually use it for various tasks.

Okay, that's the key advantages of this p tuning technique.

Now let's get a complete understanding as how this p tuning can be used for a specific NLP task.

Let's assume that I've got an original input that says, As John Doe is a software engineer at Google.

Now the first thing that you will do is you will perform the prompt embeddings initialization.

So you will introduce a set of learnable embeddings.

That is P1P2 up to p n and you will have the modified input with this prompt embeddings and you will

train the embeddings on this Nar data set, which is a task specific data set.

Then during the training you will only modify these prompt embeddings and rest.

Everything will remain unchanged and when it comes to inference, So you will make use of these optimized

values that is optimized p one, optimized p two up to optimize p n along with the new data.

And you will generate the inference.

So that's the idea about this p tuning technique.

Now after you have seen this you might be confused as hey, what's the difference between our p tuning

technique and prompt tuning technique?

Well, let me explain that in the next video.