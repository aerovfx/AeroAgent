# 12. Encoder Decoder Architecture

---

WEBVTT

That you have learned about this transformer models and its important components.

Lets understand the overall encoder decoder architecture that we have got in this transformers.

Lets start with encoders.

So as we have discussed multiple times, this is the order in which the data would flow inside our encoders.

So inside our encoders first we get the input.

So we will receive the input sequence which is nothing but a sentence in a source language for, let's

say, the task.

Like the classic task, like the translation.

That is a machine translation.

I'll be receiving the input text.

Then what we are going to do is next step is we are going to generate the embedding.

Now in this embedding step, we will convert each token in a given input to a continuous vector representation,

which is basically our embedding step.

Then we are going to perform the positional encoding.

That means we add the information about the position of each token in a given sequence to the embeddings

post that we have the multi-head self-attention.

This is going to process the sequence of embeddings in parallel, so that.

This will allow the model to weigh the importance of importance of different words in relation to each

other.

Then we have a feed forward network.

This applies a series of transformations for each embedding, so that it will also enable us to capture

the complex relationships and the pattern.

And we have multiple such encoder blocks.

So what you see right here this is one encoder block.

And we will have multiple such encoder block which performs these steps.

And finally we get the output from this encoder.

This output is nothing, but it's a sequence of final embedding which capture the meaning and the context

of the input sequence.

So that's the important details about our encoder part.

Then we have got the decoder.

Now in the case of this decoder the decoder receives the output sequence okay which is nothing but our

target data along with it is shifted by one position to the right.

Now, why is it.

Well, it think about what I have explained in the masked multi-head attention.

So in the masked multi-head attention, I have mentioned that the model should not cheat by looking

at the next words, isn't it?

That means when it is processing the output, it should process the output from each side itself, from

its uh, a learning it has to generate the output.

It should not be looking at the answer itself and generate.

So that's the reason we'll be shifting one position to the right.

So if I have the target that says as Aik lib cafe, then inside scenario there are three steps, I'll

move one step to the right.

So that is the reason we'll add a start as a dummy token to the left of it, so that we'll be shifting

once position to the right, and we'll have the embedding and the positional encoding.

So we'll have the embedding for the given output target.

And we'll also perform the positional encoding.

And we have the masked multi-head self-attention.

So this will also process the sequence of embeddings in parallel.

But because of this masking.

So this will ensure that the model will be able to understand its context only from the previous word,

not on the future words.

And here it says that masked multi.

This is actually called as the Multi-head Cross-attention.

So this is basically this one the Cross-attention.

Now why are we calling this cross attention?

Because it is attending to both the output embedding of the encoder as well as the current decoder embedding.

So it is looking at the output from the encoder as well as the output or data encoding itself.

That is our output embedding.

So that's the reason we call it as the cross attention.

So this will allow the model to focus on the relevant parts of my input sequence whenever it is generating

the output.

And we have a feed forward neural network layer, which is very much similar to the encoder.

This will ensure that we are going to learn the complex relationship in the data set, and we'll have

multiple such decoders to.

So this is the example of one decoder like this.

We will have multiple such decoders connected in a sequence, so that the overall output will be generated

in an efficient manner.

So that's the idea about this multiple decoders that we would get.

And finally we'll have an output layer.

So this is basically a probability distribution over the vocabulary because I'll have multiple words

in the vocabulary to predict what word should be there.

So we'll have a softmax output.

So that's the idea about this decoders.

So some of the key things that you need to remember whenever you think about this transformer architecture

is one parallelism.

Both encoder and the decoder processes the sequences in parallel.

So as a result of it this will be an efficient one.

And the another ability is the self-attention With the self-attention.

This will allow the model to focus on different parts of the input and output sequences, depending

on the given text.

We have got Cross-attention.

Now this Cross-attention is going to enable the decoder to utilize the information from the encoder

to generate the output sequence.

The same basic architecture can be used for various tasks, so which includes translation, summarization,

question answering and so on.

So that's another flexibility that we get to enjoy while working with this transformer architecture.

And one of the main reason this transformer architecture is powering the large language models is because

the transformer based encoder decoder models have achieved the state of the art results on a wide range

of NLP benchmarks, and that's the reason the transformer architecture is like is, I would say, a

foundation for the large language models.

I hope this series of videos gave you a clear perspective as the why this transformer architecture is

important from the perspective of large language models.

So I'll see you next time.