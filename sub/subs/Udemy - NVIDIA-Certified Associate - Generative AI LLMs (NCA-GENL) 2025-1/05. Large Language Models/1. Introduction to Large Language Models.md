# 1. Introduction to Large Language Models

---

WEBVTT

Welcome back to your learning journey.

So folks, in this section we'll be looking into the one of the important concepts that's called as

the large language models.

So as we proceed along in this section, you'll be understanding what are these large language models,

the importance of it, the applications of large language model.

And you'll also understand the technicality behind these large language models, which is nothing but

the transformer architecture.

So let's get started guys.

See when we talk about this large language models, the LMS also known as the large language models,

they are the sophisticated algorithms.

And these LMS are based on a special type of neural network we call it as the transformers.

So the transformer is a special type of neural network.

And it has got a lot of aspects, and it actually changes the way we process the text data.

So the Llms are basically a special type of this neural network that is a transformer.

Now, the reason this llms are special compared to the other deep learning models, because these LLM

models are designed to understand the context of words in a given sentence, just like we humans do.

The common example that I give in every class is.

Suppose if I've got a sentence that says, as the dog is very tired, it's crossing the street.

Now when I have a sentence or a text in this manner, if I ask you what exactly the verb word it is

referring to, is it referring to the word tired, or is it referring to the dog?

So if I ask this question as a human, it is easier for us to say that, okay, the word it the context

of the word it, it is referring to the dog and not on anything else.

But understanding this type of context on the natural language data was a challenging task.

Now to solve this, we had made use of RNN and LSTM in our neural network, but even the RNN and the

LSTM models suffer from the non ability to remember the context over longer sequences.

Now that is where the large language models comes into picture.

Now these large language models are able to remember the context over the long sequences.

And it is also having the ability to perform the parallel processing which was missing in those sequential

model.

All these were possible because of this transformer architecture.

So these large language models, since it is based on the transformer architecture, it is now able

to understand the context of words in a given sentence in a much better way.

Now this is possible with the help of two important components, which we call it as self-attention

and the positional encoding.

The positional encoding, as the name says that it refers to the position in which the word is being

used in a given sentence, and self-attention will enable the network to understand the importance of

the given word with the other words of the sequence.

These two components of the large language models makes it powerful when it comes to understanding the

context for a given sentence.

Now, when we talk about the LMS and large language models, I'm sure you might have come across multiple

such models, to name a few.

We've got GPT, we have got Gemini, Megatron, Turing, meta, llama, Bert, Claudy.

So these are all some of the examples of these large language models.

So all these models that you see right here are developed by individual developers.

And all these are using the transformer model architecture.

Now, because of the ability of the large language models to remember the context of the given words,

it is finding its application in various use cases, to name a few.

It is used in the applications such as text generation, where it is used for creative writing.

Story writing.

Market content creation.

It is used in summarization for legal paraphrasing, meeting note summarization and so on, and it is

used in translation task where we want to translate a given text sequences from one language to the

another language, or conversion of, uh, let's say my text into a code format.

It is used in the applications such as classification, where I want to classify a given text to one

of the labeled classes.

Maybe an example can be sentiment classification of a review.

It is used in the applications such as chatbot.

Now these chatbot can be an open domain, or it can be a virtual assistant which has been trained on

a specific data set on a specific domain.

These are all the common use cases of the large language models, and as you can clearly see, the use

cases of large language models, you can clearly observe that here.

In this case of this use case okay, it is able to perform all the tasks that we had described.

When we talk about the natural language processing, and not just these applications have found its

way in retail, but it has also found its way in enterprise level as well.

To give you some of the use cases at the enterprise level in healthcare domain, the medical researchers

use the large language models for performing the task like protein structure prediction, uncovering

the disease patterns, and even predicting the outcomes too.

In retail sector, the large language models are used to enhance the customer experience through dynamic

chat bot.

In software development sector, it is used to write the software and teach robots the physical task

in the finance sector.

The financial advisors use these large language models to summarize the earnings call, and even create

the transcript of the important meetings.

It's used in marketing.

Team two the marketers use the large language models to organize the customer feedback.

Segment the products into the different categories.

So that's the importance of large language models in the current days.

So with this we come to the end of this video and I'll see you next time where we are going to learn

in a deeper way about these large language models.