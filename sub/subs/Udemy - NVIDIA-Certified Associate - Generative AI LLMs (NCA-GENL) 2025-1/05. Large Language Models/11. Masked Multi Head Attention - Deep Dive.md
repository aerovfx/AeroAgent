# 11. Masked Multi Head Attention - Deep Dive

---

WEBVTT

Now in this video, let's build an intuition behind this masked multi-head attention.

Now, when we think about this masked multi-head attention, so we have understood that it is part of

the decoder.

Now let me clarify a bit.

So in the transformer architecture we have already aware that there are two components.

One is the encoder component.

The another one is the decoder component.

So the left hand side is basically my encoder.

And the right hand side is basically my decoder, and I'll have series of encoders.

The data gets processed through the input layer word embedding, positional encoding.

It will pass through multi-head attention.

And we have some deep learning neural network where we have got the add and normalization layer, as

well as the feed forward neural network layer.

Once the data is processed from the individual encoder.

So I'll have multiple such encoder and the data would move to the decoder.

Now inside this decoder we have this masked multi-head attention present.

Now to help you understand the concept of this masked multi-head attention, let's take up a very simple

example.

Let's say we have got something called as.

Let's.

To make it simple, let's consider the machine translation.

Let's say I've got a text okay.

For the demonstration purpose, we'll consider the text in this manner.

I'll have a text as translate I love coffee, okay, let us assume that I want to translate.

I love coffee to a new language.

Let's say French.

Okay, let's say this is the French translation or let's consider the German.

Okay.

So this is my English.

Okay.

And this is my German.

So this is my source language.

And this is the target language.

Let us assume that we are building for this machine translation task.

Now when I'm building it for my machine translation task, let us assume that we are going to make use

of this transformer architecture.

So this is our data and we want to use this transformer architecture.

Now when we are using this transformer architecture let me repeat a bit because that would help you

to reinforce the concepts that you have learned.

So your source will be your input will be I love coffee so I will I love coffee.

These are the three tokens that you would have.

So on this three token you're going to generate the embedding positional encoding.

And you will learn the relationship between the words with the help of multi-head attention from the

encoders, and then it will move on to the decoders.

Now when it is generating the output.

So if I'm performing the training from the knowledge of machine learning, you know that this is your

target Y and this is your input x.

Now whenever you are performing the training, you will provide both x and y.

Isn't it for generation of the model that is for training the model?

Now, in such scenarios here I have got the encoded data.

Let us assume that I'm going to call it as Z.

This is basically my encoded data of my X through my encoders.

Now this Z contains the information about my source data.

And when I want to generate the output, which is basically a club coffee.

So in such scenario, I'll have to use this and I'll have to say okay, start the decoding.

Maybe I'll just mention as let's say start.

So what we are trying to visualize is we are trying to visualize this part.

That is my decoder part.

So if you try to visualize the decoder part.

So here we are saying that let's say we are saying that we will mention the values in this manner.

I'll say start B okay, this is what I would mention now when it is starting.

Okay.

I would expect that here when it is starting.

So this start should go to my decoder.

Now decoder should also receive my Z which is the output from my encoder.

And I would expect that the first word it has to generate me the output is h.

Then I'll have to send.

The h will have the decoder, which is decoder part of our network, and it has to generate the next

word that is lib.

And I'll have to use the lib as an input.

It has to go to my decoder and it has to go ahead and generate the output that is Cafe.

So as you can clearly see, I would expect the model to generate the output based on my current input.

Now when it is generating the word lib, it should not be looking at my existing label data and simply

come up with a next word that is lib.

I do not expect that that would be a cheating.

So that's where the masked multi-head attention comes into picture.

What it says is even though my target.

Why that means in my data set I might have the next word.

My model should be generating the next word based on the context it has learned from the encoder, as

well as the only previous input.

So that is the idea behind this masked multi-head attention.

The masked multi-head attention will help us to avoid cheating.

That means it will help.

The more it will help the model to mask the future words so that whatever the output that we would get,

it will be based on the existing process data.

So that is the overall idea about this masked multi-head attention guys, I hope this gave you a clarity

about this masked multi-head attention.

So I'll see you in the next video.