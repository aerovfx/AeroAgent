# 2. How Large Language Models (LLMs) are trained

---

WEBVTT

Now that you have a high level understanding about this large language models, let's understand how

are these large language models are actually trained?

The LMS that is the large language models are trained on massive amounts of text data from the internet.

Now in order to perform the training.

So one challenge that we had earlier before the LMS and the transformer models was we had a challenge

of requirement of the labeled data set, which means we had a requirement of huge corpus which are labeled

in nature.

Now, that was a challenge that we had when we were training the model for natural language processing.

And another challenge that we had when we were training the models for natural language processing was

we were unable to use the already trained model, because if you look into the trained models from the

NLP before the transformer model architecture, we were training the model on a specific data set where

we have generated the numerical representation for a given text data.

After some pre-processing.

And then we were performing the training on that labeled data set.

Now anything that was true, it was specific to the data set that we had trained, which means we were

not having a model which generalizes the overall natural language understanding from the given data,

just like we had in the computer vision domain at that time.

So these two were some of the core challenges that we had to progress in that NLP domain.

Now, what researchers proved is that they were able to train the model in an unsupervised learning

manner, and that is what has been followed in the large language models.

Technique two here they have applied the unsupervised learning technique, which means here in this

case, the model learns by itself by looking at the text corpus.

Now the task was pretty simple.

So what they mentioned is suppose if I've got a sentence which says that movie is good.

Suppose if I've got a sentence, I'm just simplifying it so that you can understand it.

So suppose if I've got a sentence as movie is good.

So basically we will split this sentence into the individual tokens.

And the idea is simple.

Given a word, I want to predict what is the next word.

So this is the task that we have trained our model, which we call it as the language modeling task.

The model learns by itself to predict the next word given the current word.

And we do this on a huge volume of data set.

To give you an example.

So we have trained on, let's say Google News data set or Wikipedia text, which is the entire corpus

from various domain, and we are going to train it on this simple task.

Now, as and when the model comes across the lot of different words, it is it is able to understand

the overall understanding of the language.

And once it has got the overall understanding of the language, how one word can be used to generate

the next word.

Okay, so as a result of this, the model slowly learns to predict the next word in a given sentence

or by looking at the context of the existing words.

Now, as a result of this extensive training, these llms, they have actually emerged, what we call

it as the foundation models.

Now these foundation models have got the broader understanding of the language which can be applied

to multitude of tasks.

Now, the reason that we call it as the foundation models, because think of it in this way.

Let's say this is a model that I've got.

I'll say foundation model.

Now, this foundation model that I have trained on a Wikipedia data set.

So when I talk about this Wikipedia data set, you know how large the data set will be, isn't it?

I've got a huge volume of data now on this huge volume of data.

I have learned the overall context in which the words can be used.

Now, in such scenario, this model has got the broader understanding of the language as well by looking

at the text from various domains.

Now once I have this model using the knowledge it has gained, I can now go ahead and perform a variety

of tasks.

It can be starting from write the stories like create the stories or summarize the complex sentence,

or perform the classification or answer a scientific question.

So these are all the various abilities that we get to see once the model has been trained on this huge

corpus of this text data.

Now, this incredible versatility has actually come from their ability to perform what we call it as

the zero shot learning.

Which means once I've got the foundation model, they can generate the text for a wide range of purpose

without requiring the specific training of individual tasks.

So you can think of the foundation models that we see as a multi-talented writer who can effortlessly

switch between various genres, various styles, various language depending on how the model has been

trained on.

So obviously there are some variations of this capability, like we have something called as one shot

or few shot learning, which we'll be discussing as we progress.

So that's the overall idea guys.

So in the case of large language models, we train it on huge corpus so that the model learns the pattern

in which the words can be used.

So once it understands the context, it can go ahead and generate the text for the new task.

So with this approach, so obviously a single model with zero shot learning can solve a wide range of

problems by understanding and generating the human like thoughts in an instantaneous manner.

Now when I say zero shot learning, it's a scenario where you take a foundation model which you have

trained on huge corpus, and you will ask that model to perform a task which it was not actually trained

upon.

And that's one of the main capability of these large language models.

And as a result of this, we see the use cases that would span across the industries and the business

transaction.

And this has created a potential for immense value as well as the innovation.

Now, when we are talking about the large language models.

So as I mentioned, it is based upon the transformer architecture.

Now here in the case of this transformer architecture, it is made up of two components encoder and

decoder.

So if I want to describe the types of large language models I can classify those types of the large

language models are encoder only models.

Decoder only models and encoder decoder models, so they are all the distinct variations in the transformer

architecture, and each of them is suited for the different task.

If I take a look into this encoder only model, these are the large language models where they are suited

for understanding the text in a much better way.

You can think of them as the one who who is actually better in comprehending the given text.

So they excel at the tasks such as classification and the sentiment analysis.

One of the prominent example of this encoder only model is the Bert architecture, and we have a decoder

only model.

Now these are all the models which we call it as the creative models.

They are the one who is best in generating the text data, whether it is writing the text, composing

the email, or even generating the code.

The decoder only models are suited for certain tasks.

Some of the common examples under this decoder only model is the famous GPT model.

So that's an example of decoder only models.

And we have another kind of model in the large language models that's called as encoder decoder models.

Now in the case of this encoder decoder models here it's a combination of both encoders and decoders.

They can comprehend the given input.

And they can also go ahead and generate the data like generate the text like the decoders.

So this makes it ideal for the tasks such as translation.

And it is also used in summarization and even the question answering task.

To give you an example, the T5 model, which is one of the famous model of uh LM so which is from the

Google.

So that's an example of encoder decoder model.

All right guys, so what you have seen is as part of your learning journey in this video, you have

understood how does these large language models are trained.

And the key capability of this large language model is that is zero shot learning and the types of the

large language models.

So we'll stop the video over here and I'll see you next time.