# 9. Self Attention & Multi Head Attention - Deep Dive

---

WEBVTT

Now in this video, let's understand about this self-attention component when we are working with this

transformer architecture.

Now we have understood that while working with the self-attention we'll be dealing with three important

components.

They are query key and value vector.

Okay, these are the three vectors that will be creating when we are dealing with this self-attention

layer.

Now the question is what exactly are these query key and values and what is the overall importance around

it.

So let's understand it in detail.

So for the simplicity of the example, let's say that I've got the input data.

I'm going to call it as an input.

And I'll consider the two sequences as the input.

One is dog and maybe the other one is crossing.

Okay, so these are the two words that I have got in my given sequence.

I'm just considering two words over here.

So obviously the first step is basically the generation of embedding.

So when I say embedding, it's basically the representation of the vector in a uh, like representation

of the word in a vector format, assuming that this is one vector that I've got.

Okay.

And this is another vector that I've got.

So both of these are two different vectors that I have got.

So which is basically the matrix uh, or the numerical representation of my individual words.

So this is the output of my embedding I'll call it as x one and x two.

Now I have mentioned that we'll be generating three vectors query key and value vectors.

The question is how would I get the query vector or how would I get the value or a key vectors.

So we are going to generate those query key and value vectors with the help of the weights.

Like we will be multiplying our input embeddings that we have got to the weight matrix.

So this is the weight matrix one for query I'm going to call it as wq.

Similarly I'll have w k and I'll also have w v.

I am going to take the multiplication between my embedding with the WQ, and this is going to generate

the query matrix that is the queries vector.

So I'm going to say this is my query vector I'll call it as q one.

And this is my another query vector which is nothing but my q two.

So x two times w q is Q2X1 times w q is q one.

So that's what we would say right here okay.

To ensure that we are going to perform the matrix multiplication.

Since I've got four columns I'll have 1234 rows.

And maybe I'll have three columns over here.

So that is how the structure of these matrices would look like Okay, we'll multiply x one with w q

to get Q1X2 with w q with q two.

In the same way I'm going to multiply the same thing.

Like I'll multiply my embeddings with my w k to get k1 and k2.

So this will be my k1 and this will be my k2.

Then I'll also get this are nothing but my keys.

And then I'll also multiply x one with w v and x two with w v.

And I'm going to get the v1 and v2.

So this is what we would get when we are working over here okay.

This is what we would get when we are working over here.

We'll multiply the embeddings with these three matrices to get query key and values.

Now once we get this query key and values, the next thing that we are going to do is we would go ahead

and calculate the score.

We get the score calculation.

This would help us to understand how much importance that I want to give for the other words.

So let's suppose that I am processing for the word dog.

If I'm processing for the word dog, then the way that I'll be calculating the score is, I'm going

to say score is given by.

I'll multiply my respective q with the k values.

So I'll multiply q one and q one and q one times k two.

This is important.

And I'm going to get some values.

So let's say I'll get the values as 112.

And this one I'll get the value as 96.

So I would get the values of score right here which is multiplication of q one times k one.

That is keys of my current word.

I'll also multiply the q one query vector of my current word with the key vector of the other words

in my sequence.

In this case, I've got two words.

Now, if I've got multiple words, it will be Q1 times Q3, Q1 times K4, and so on.

That is how we'll be calculating the score.

Now, the research paper also says one more thing.

I'm going to divide by eight, which is nothing but the square root of dimension of K.

Okay, so that's what they describe.

So if I divide by eight, which is basically the output will be 14 and 12.

This is how the scores will be calculated.

Lastly what we will do is okay after this step.

After this step we'll take the softmax.

Once I apply the softmax for 14 and 12, I'll get the value which is in terms of probability score,

I'll say 0.88 and 0.12.

So this is what we would get at the end after the application of the softmax.

Now once I get this softmax okay.

Once I get this softmax then I would go ahead and multiply the softmax output with the respective v.

So I'll multiply 0.88 times v one, and I'll multiply 0.12 times v two.

Okay.

Once I do this multiplication that is softmax multiplied with value.

Once I do this activity okay.

Then I would go ahead and perform the summation.

That means I'll perform the summation of all the v value and I'll get the value of z.

So let's say this is the value of z one.

And similarly I'll be doing some parallel activity and I'll be getting the z two.

So this is how the overall attention will be calculated.

This is the overall calculation of my self-attention.

So if you look at it I'm I'm actually considering my current words query with the other words keys and

finally getting the score.

And once I get the score, I'll take the multiplication with the vectors values.

So this would help me to give the importance for the other words as well.

So that's the idea about this self-attention.

And when we have multiple such self-attention we call it as multi-head attention.

A multi-head attention is basically self-attention used multiple times.

So that's the idea about this multi-head attention, guys.

Okay, so that's the core idea when we are working with this given scenario T, and we'll be using multiple

self-attention and using that multi self multi-head self-attention will be able to learn the patterns

in a much better way for the given data set.

So that's the overall idea of applying this self-attention on the data that we have received after we

have applied the positional encoding.

So once we do that, okay, if we take a look into this architecture that we have got, if you observe

right here okay.

So here in this case.

So we have got the input encoding positional encoding.

And we have the multi-head attention.

Now after that we have some connections right here which is nothing but the feedforward layer and the

add or normalized layer, which are nothing but the residual connections.

And we'll have multiple such layers which will be stacked one after the another.

Now that is the output from the encoder section.

So this is the idea about working with this multi-head self-attention.

So with this we come to the end of this video guys.

Let's continue further on this Transformers journey in the next video.