# 21. Loss Functions for LLMs

---

WEBVTT

In this video, we are going to learn about the loss functions for the large language model.

First of all, let's understand what our loss functions from the perspective of large language model.

So when we say loss function, a loss function is also known as a cost function here.

In this case, it's a mathematical function which quantifies the difference between the expected output

and the prediction from the model.

So, so from the context of LMS this discrepancy often represents the difference between the predicted

text sequence and the actual target sequence.

So the intuition is same.

The intuition is very much similar to what you have learned so far in the case of machine learning and

deep learning, but here we'll be interested in measuring this discrepancy on this text data.

Now, these loss functions provide a measurable metric when it comes to evaluating how well a particular

LLM is performing during training.

By minimizing the loss, the model actually learns to adjust its internal parameters, so thereby improving

its ability to understand and generate the text which aligns with the human language pattern.

So we want to ensure that the text that we are generating from the large language model, it aligns

with the language pattern that we have got in our natural language.

Now let's take a look into the common loss functions that we will be using for the large language model.

We have the cross entropy loss.

The cross entropy loss is a most widely used loss function for the large language model, because this

actually predicts or this actually measure the dissimilarity between the predicted probability distribution

over the vocabulary and the true probability distribution of the next word in a sequence.

So this model, or this loss function actually encourages the model to assign the higher probabilities

to the correct words, and lower probabilities for the incorrect words.

Some of the variants of this cross function loss would include negative log likelihood loss.

Now this is a simplified form of cross entropy loss, and this is commonly used in practice.

And we have perplexity.

Now perplexity measures how well the probability distribution predicts a sample, which is often used

as an alternative metric to the cross entropy loss.

Then, under this common loss function we have the contrastive loss.

Now, this contrastive loss encourages the model to learn the representation that brings similar sentences

or phrases together in the embedding space.

Now this will also push the dissimilar ones further apart.

Now this is commonly used in tasks such as sentence similarity or paraphrase identification.

To give you an example, in a sentence pair classification task, the contrastive loss will aim to minimize

the distance between pairs labeled as similar.

It could be paraphrases and maximize the distance between the pairs, which are labeled as dissimilar.

Next, we have reinforcement learning loss.

Now, reinforcement learning loss is generally used in combination with other loss function, and we

use this to fine tune the LMS based on the human feedback.

Now, this involves rewarding the model for generating the output that align with the human preferences

and penalizing the output which deviates from it.

Now, to give you an example, in the text summarization task, the reinforcement learning can be used

to reward the models to that produce concise and informative summaries, while penalizing those which

are verbose or inaccurate.

Then the question is how we choose such loss function.

Now the question is how do we choose this loss function?

First of all, depending on the task.

So different task it could be text generation.

Translation or classification might require different loss functions so that we can optimize the specific

aspects of the model's performance.

Now we have the data.

The characteristics of the data can also influence the choice of loss function.

For example, if the data is noisy or imbalanced, then in such scenario, we have to remember that

certain loss function might be more robust in such scenarios.

Then another key aspect is the model architecture.

The specific architecture of LM might favor certain loss function over the others.

So these are the key things that we need to remember in mind when we are deciding such loss functions.

Now, coming to the challenges and the complexities while performing this activity, one is an overfitting.

The models can actually overfit to the training data set, which means it will perform well on the examples

that it has already seen, but it poorly performs on the unseen examples inside scenario regularization

techniques and the early stopping can help mitigate this next bias amplification.

The loss function can invert inadvertently amplify the biases that are present in the training data

set.

Now this can lead to unfair or discriminatory outputs.

So in such scenario, careful data curation and bias mitigation techniques are very essential.

And the third thing or the third challenge is the computational cost.

So some loss functions, especially those involves reinforcement learning can be computationally expensive

when it comes to optimization.

So these are some of the challenges and the consideration of choosing such loss function.

So with this we come to the end of this video and I'll see you next time.