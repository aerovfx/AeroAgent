# 8. Self Attention & Multi Head Attention

---

WEBVTT

Previously, you have looked into the core component of this transformer architecture.

That is the positional encoding.

Now, once the data moves from the positional encoding, it reaches what we call it as the multi-head

attention.

Now this multi-head attention is also called as the self-attention layer.

So let us understand what exactly is this self-attention layer?

Now in this self-attention layer, what happens is each word that we have got, it's going to create

a query key and a value vector.

So that means a three vectors will be created for each word.

Now the query and the key vectors they are compared to compute the attention score.

So we make use of this query and key in order to compute the attention score.

Now these attention score are used for where the value vectors so that this would produce the weighted

representation of the overall word's context.

It would make more sense when you look into the mathematical part of it.

For now, just be with me.

So for now, understand that in this self-attention layer.

So it would help us to compute the query and key values so that we will be able to identify how much.

Attention that I have to give for the other words.

And at the end, I'll be having the weighted representation of the words context.

That means for the current word, I'll be able to get the weighted context from the other words as well.

So that's the idea behind the self-attention.

So the self-attention layer will help us to understand the context of my current world with the surrounding

words, whatever the words that I'll have.

So I'll be looking into it from the surrounding words.

Okay, so that is the idea about this self-attention.

Now there is one more term that you would see right here that's called as the Multi-head attention.

So what is this multi-head attention?

So here in the case of this multi-head attention, we will have multiple heads.

We call it as the multiple heads, where we are going to perform the parallel processing of the self-attention.

Now each head in the multi-head attention, it's going to operate independently, which will focus on

the different parts or different aspects of the given input sequence.

As a result of this, we'll be able to simultaneously capture the diverse relationship between the words

or the words within the given sequence.

So think of it as multiple self-attention, which works in parallel, is nothing but the multi-head

attention.

Now, since I'll be using multiple, head over here for the attention purpose, the intuition behind

this is different.

Heads can learn to specialize in different types of relationship.

One can focus on syntactic, another will focus on semantic.

Another would focus on positional.

So that's the idea of using this multi-head attention.

So as a result of this we'll be having a more comprehensive understanding of the given input, because

we'll be considering the multiple perspectives.

And here in this multi-head attention.

With this, we'll be able to enjoy some of the key benefits like see, by having the multiple sets of

attention weights, that is one per head, the model can now represent the wider range of interactions

and the dependencies.

Now this makes the transformers a more effective, especially in the task like language understanding

and the language generation.

This has much more robustness to the ambiguity.

So let's say if one of the head fails in the multi-head attention.

So we'll be able to capture the existing relationship from the other heads at least.

So this makes the model more resilient to noise and the variations in the input data.

And by analyzing the attention patterns of the different heads, this will also provide us with the

insights as how the model is making the decision.

So this is useful whenever we are performing the activities such as debugging.

understanding the model behavior, and improving the overall model design.

So the key role of this multi-head attention is to empowering the model to process the information in

parallel, capture the rich variety of relationships, and overall achieve the impressive performance

in wide range of natural language processing tasks.

So that's the role of this Multi-head attention, guys.

So we'll end the video over here.

Now what we will do is in the next video we will first understand what is the self-attention.

What are these q, k and v.

That is query key and value vectors.

And from there we will build a deeper understanding on this multi-head attention.

So I'll see you in the next video.