# 3. Capabilities of LLMs

---

WEBVTT

Now with the capabilities, we'll also get to face the challenges when we are working with these large

language models.

So let's look into the challenges of having to work with these large language models.

So one of the major challenges of working with this large language model is the compute cost and time

intensive workload.

If we think about training our own large language model, it requires substantial investment, especially

from the compute infrastructure.

Now, to give you an example, training the GPT three with $175 billion, it might cost over $12 million

in compute.

So think about the amount of cost that it would involve.

When we think about training such large language models, and it requires thousands of GPUs and extensive

periods of training.

And another challenge that we get to face while working with this large language model is it requires

significant data.

Now, training these large language models requires the large data set, and many enterprises struggle

to access sufficiently large data sets.

Now, these issues are more pronounced for the use cases, which requires the private data, which could

be financial data, or the health data, which is even challenging to get if you want to build a best

model.

And sometimes the necessary data may not even exist to.

So this is another challenge that we get to face whenever we are thinking of building the large language

model for a specific domain.

Another key challenge is around the complex training and deployment.

So as you have clearly seen, training the large language models or even just working on it, it requires

strong understanding of deep learning.

Understanding of transformer architecture, distributed system and even.

It is required for you to have the ability to manage the thousands of GPUs in a simultaneous manner.

And another thing is, the technical complexity is another barrier for many companies in order to train

their own models.

So in order to overcome the challenges.

So obviously the high cost data requirement and the need for technical expertise, these are the ones

that would pose a significant barriers.

So if you want to have the wider adoption in the enterprise, so we have to come up with a strategic

way to reduce all these things.

And the enterprises obviously must invest in the resources and the expertise in order to leverage these

large language models in an effective manner.

And addressing these challenges is crucial for the successful implementation of these large language

models.

So this was about the high level details about the challenges of working with these large language models.

So I'll see you next time.