# 4. Challenges of LLMs

---

WEBVTT

Now when we have talked about this large language models, we have mentioned that we can get a foundation

model.

And on that foundation model we can go ahead and perform the various tasks.

And we have mentioned that as a zero shot learning.

Now that foundation model has learned from the general data set, maybe a Wikipedia data set or a Google

News data set.

Now, in such scenarios, you cannot use that foundation model itself for your own company's application,

because that model has not seen your company data or your company information.

So that is where the customization would come into picture.

Now this customization can be for the specific data set, or it can be for the specific task itself.

Now by performing this customization this will enhance the performance and the relevance of LMS for

this specific application.

This would include prompt tuning, fine tuning and the adapters.

So these are the three common techniques that we refer to whenever we think about customization of these

large language models.

Now these methods involves providing the model with additional training data or adjusting its parameters

so that it can align with the desired behavior.

Now, as a result of this, this could lead to significant improvements in the accuracy and the relevance

for the specific use case.

So let's understand each of them at a very high level.

So when we talk about this prompt tuning.

So prompt tuning involves adjusting the input prompts in order to guide the model's responses.

So when I say prompt, prompt is basically the input to illustrate it with an example.

Suppose if I've got a large language model, let us consider the example of Gemini.

So this is an example of large language model.

Gemini is another large language model which is capable of generating the text.

Now if you look at it here, it says as enter a prompt here.

So what it means is a prompt is basically an input that you would provide to the large language model.

There's a separate section called Prompt Engineering which we discuss in the future section.

But for now to proceed ahead.

Think of the prompt as an input that you would convey to your large language model.

Now let's suppose I'll provide an information as what is the length of 30 centimeter in inches?

Here I am mentioning the text okay, which is my input, and this text that I'm providing as an input

for my large language model is called as prompt.

And if I press the enter key, the large language model is accepting my input and it is able to generate

the output from that trained model.

So this capability is called as the generation of the text.

Now, by modifying the input that I would provide to my model, I'll be able to generate the output

as per my expectation.

So this kind of modification of the text that we provide to the model, so that we can guide the model's

responses to get the better outcome on a required application or the use case.

That's what we call it as the prompt tuning.

An example can be framing a question or providing an instruction in a specific way so that we can get

an accurate answer.

So this technique is called as the prompt tuning technique.

Now in this case of prompt tuning technique, one key thing that you need to remember is you are just

providing the input in different format.

There is no changes on the model parameters.

The model parameter stays as it is.

Okay, you are just going ahead and asking the model in a different, different manner.

Okay, that's the idea about this prompt tuning technique.

And we have another technique that's called as a fine tuning technique.

Now in the case of this fine tuning we'll be training the model further on a smaller data set or a task

specific data set.

Now, by doing like this, we'll be able to improve the model performance on this specific data because

we have focused on that relevant data.

Now in this scenario, when you perform the fine tuning, there will be a change in the model parameters

because you are providing your own data set and you are changing the values of the parameters, which

in turn will be adjusted for the data that you would provide.

So as a result of this, the model will perform better on the data that you have trained or the domain

that you have trained, which we call it as the fine tuning technique.

Okay.

So here the example for this fine tuning would include the general model fine tuning a general model

on medical text so that you can use it for the health care application.

Okay.

So this is about the fine tuning technique.

And we have another most commonly used technique that's called as the adapters technique.

So here we use this adapters technique.

It involves adding the lightweight layers to the model for the specific task.

So as a result of this what you will be doing is by adding this lightweight layers on the existing models

you are going to change the model parameters, but not on the original model, but only on these lightweight

layers.

Now by doing like this, you'll be able to get the specialization benefit without retraining of the

entire model.

So you're already aware how big the model will be like.

As I mentioned earlier, the GPT three model will have more than 1 billion parameters.

So instead of retraining that entire model, you're just adding the lightweight layers so that it can

work on that specific task.

To give you an example, so you can add the adapters to a large language model so that it can be specialized

in a legal document analysis.

So guys, this was about the customization of the large language models for a specific task.

So with this we come to the end of this lecture and I'll see you next time.