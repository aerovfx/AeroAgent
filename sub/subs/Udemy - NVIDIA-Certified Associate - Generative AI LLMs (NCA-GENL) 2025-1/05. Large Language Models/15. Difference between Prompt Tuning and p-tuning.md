# 15. Difference between Prompt Tuning and p-tuning

---

WEBVTT

Now that we have learned the two techniques, that is prompt tuning and the P tuning technique, let's

look into the difference between them.

In order to understand the difference, let's understand some technical aspects of this prompt tuning

and p tuning technique.

Now when we start working with this prompt tuning technique, what we are going to do is this technique

is going to focus on optimizing the soft embeddings okay.

This will focus on the optimizing the soft embeddings.

And these are prepended to the input text.

Now this is essentially acting as a task specific prefix or a prompt for us.

Now these soft embeddings are actually learned through back propagation while the overall model's weight

will remain frozen.

That's one of the main advantages of this prompt tuning technique.

So the advantage is it's simple and it is effective for adapting to the new task.

But when it comes to the disadvantage, it may not perform well on the task, which requires the deeper

understanding of the model's parameters, because this can be prone to overfitting to certain tasks.

Over here.

If you are making use of this prompt tuning technique.

On the other hand, if you make use of this p tuning technique, which is also known as the prefix tuning

technique, what we are going to do it here, here is we will make use of a separate prompt encoder.

Now this can be an LSTM or multi-layer perceptron.

We use this to generate the continuous prompt which are inserted throughout the input text, which means

we are not limiting to the beginning, but it can be inserted throughout the input text.

Now the prompt encoder is going to learn to generate the optimal prompt and the model's weight will

remain frozen.

The advantages.

With this tuning technique we will have more flexible than the prompt unique technique because it can

inject the prompts at multiple locations within the given input, and this can lead to better performance

on the task, which require more nuanced understanding.

And it also preserves the knowledge from the past task coming to the disadvantage.

It's a little bit more complex than the prompt tuning, because here you are training the additional

prompt encoder itself.

So that's the core idea about this p tuning technique.

So with this we come to the end of this video on the difference between the prompt tuning and P tuning.

I highly recommend you to just have a look into this document before the examination.

Your examination tests about the various kinds of prompt tuning and the various kinds of in which you

can customize your large language model for your business data set.

Okay, I'll see you next time.