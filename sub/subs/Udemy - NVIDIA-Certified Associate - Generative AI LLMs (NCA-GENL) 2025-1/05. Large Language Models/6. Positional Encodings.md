# 6. Positional Encodings

---

WEBVTT

Now, one of the main components of the transformer architecture is the positional encoding.

Now this is the initial part of our transformer architecture.

So let us understand what exactly is this positional encoding and how it works and its importance.

First of all, let's begin by understanding this positional encoding.

Now when we talk about this Transformers model.

So one thing is majorly an advantage for us is it doesn't follow any sequence.

It will go ahead and process my entire text, whatever the tokens that I have got in a parallel manner.

Okay, suppose if I've got a text that says has machine learning is a branch of AI, so it is going

to process all these words together in parallel.

That is what the transformer model is going to do.

Which means it doesn't consider or it doesn't have any understanding of the word order.

And that is where the positional encoding would come into picture.

Now, the reason the positional encoding is important because if we consider our human language.

Suppose I have got two sentences.

Okay.

Dog eats.

Man.

Man eats dog.

If I've got two sentences.

Okay.

You can clearly observe that both of these sentences convey two different meanings, just by the change

in the position of dog and man.

Both of the sentences are conveying the different meaning, so you understand the importance of position

of the words in a given language.

But I'm just saying that the transformer model do not consider the ordering itself.

Now, how do we ensure that the transformer processes the text in the required order?

That's where the positional encoding would come into picture.

So here in the transformer, as I mentioned, it is processed in parallel without the knowledge of their

order.

So this is very difficult when they are actually capturing the meaning.

And as as we have clearly described in this example, the the order is everything when we are dealing

with the text data, and that's where the positional encoding will come into picture.

These positional encoding, what it does is it is going to describe the location or the position of

an entity for a given sequence, so that each position that I would have, it will have a unique representation.

Okay.

Each position will have a unique representation.

So each word position in a sequence is assigned with a unique vector.

And that's called as a positional embedding.

Now these positional encodings are then added to the word embeddings so that this would infuse them

with the information about their position in the given sentence.

So that's the idea about this positional encoding technique.

Now the question is how can we go ahead and inject the positional value?

Or how can we go ahead and add the positional details for the given text data?

Well, the way this positional encoding is going to work is we will make use of sinusoidal function,

which is which is the combination of sine and the cosine function.

So the positional encodings are calculated using this sine and cosine function.

So as a result of this this will produce a pattern that would convey the positional information.

Now here this is the overall formula that we will be using it in order to go ahead and generate the

positional vectors.

Now here we've got various terms that you see right here.

Now here the first term that is pos.

So pos is nothing but the position of an object in a given input sentence.

Then we have something called as two.

This is a D model.

So here the d is nothing but the dimension of the output embedding space.

That is what we call it as the D model.

The dimension of the output embedding space that I want to generate.

Then over here we have mentioned as uh p of position two I and p of position comma two I plus one.

So like position comma two I.

Okay.

Position comma two I.

It's basically the position function for mapping a position here.

In this case pause in a input sequence to the indexer to the given index.

That's what we generally call it okay.

And we also have something called as n whenever we are working.

And here in this case it is mentioned as 10,000.

Okay.

That's what the authors have described in the overall attention.

Attention is all you need.

Research paper.

This is actually called as the n term.

So since we are referring to the attention is all you need.

So I have directly mentioned as 10,000.

And there is something called as I over here.

Now I is used for mapping the column indices.

Okay I is the mapping indices which is in the range of zero to d by two.

That is what we would have.

Oh okay.

So that's the overall idea about the positional encoding calculation.

And you can clearly observe that overall in this expression the even position correspond to the sine

function and the odd position are corresponding to the cosine function.

So that's the overall understanding of this expression of this positional encoding.

Now by performing it in this way okay.

So we will be able to get the better representation of the text.

Now one question that you might have is why are we using the sine and cosine function.

Why can't we simply go ahead and make use of a single value, maybe an index value?

Well, think of it in this way.

Let's say if I have got the one sequence that is one sentence with three words, okay.

And I've got another sentence with five words so I can come up with let's say index position 012, maybe

01234.

And something like this.

I can come up with a single value.

Now, obviously when I go ahead and normalize it, if you normalize both for the three word sentence,

I'll have the different values, let's say zero, 0.33, 0.66 or 0.33, 0.66 or 0.9 and something like

that.

And in the same way, if I have got five words, I'll have 0.2, 0.4, 0.6 and so on.

So with the change in the length of the sequence, the values is going to vary.

And this will cause the inconsistencies, especially when we are dealing with lengthier sequences.

So since we require a smart positional encoding scheme, that's the reason we prefer to use this sine

and cosine function.

So by making use of this sine and cosine function this will allow the model to easily learn the relative

position of the words.

The distance uh between two positional encoding can be directly related to the distance between the

corresponding positions in a given sequence.

So that's the main advantage of using this sine and cosine function when we are performing this positional

encoding.

So this would allow the model to generalize the sequences which are longer than those which it has seen

during the training as well.

So that's the another benefit that we would get by making use of this sine and cosine function.

So this is the visualization of the positional encoding that we would get.

So the overall positional encoding layer that we have got.

So once we get this positional encoding for each and every vector, we are going to perform the summation

with the word encoding that we would get, which will be sent to the subsequent layers of our transformer

architecture.

All right guys.

So this was about the positional encoding.

We'll discuss in detail.

I'll provide you some more technical details in the next video.

So stay with me and I'll see you in the next video where we deep dive into these positional encodings.