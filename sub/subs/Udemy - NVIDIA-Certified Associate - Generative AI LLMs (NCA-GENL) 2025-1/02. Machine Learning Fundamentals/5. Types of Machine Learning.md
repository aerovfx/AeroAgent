# 5. Types of Machine Learning

---

WEBVTT

In the last video, you have learned about the machine learning and the core idea of this machine learning.

Now in this video, let's have a look into the types of machine learning.

And then that would give us the foundation for starting our learning journey with machine learning models,

which is the core aspect of this section.

So when we talk about the machine learning, depending on types of data that we are dealing with and

the type of tasks that we are working.

So we basically have three types of machine learning.

One is supervised machine learning, unsupervised machine learning and reinforcement learning.

Now supervised machine learning is a scenario where we are dealing with a labeled data set.

Now when I talk about this labeled data set here, I've got the x as well as y.

That means I've got the feature along with the target outcome in case of unsupervised learning.

So we basically have only the features present in the given data set.

And in case of reinforcement learning here, this is a kind of a system where the system tries to improve

itself through the experience.

Now in the case of reinforcement learning, it actually learns in a trial and error manner.

So that is the overall idea about this reinforcement learning system.

Now this is a currently an highly research field where we are trying to build a system which can improve

itself from the experience.

So we still have a long way to go in this research area.

But most of the applications that we see in the real time is around the supervised machine learning

and the unsupervised machine learning.

So in case of supervised machine learning, we basically have the X and y that is the features and the

target.

Now under the supervised machine learning, we've got various machine learning models like linear regression,

logistic regression, decision tree, random forest algorithm, boosting algorithm, bagging algorithm.

So like this.

We've got various statistical models which will help us to find the patterns and establish the relationship

between X and y.

Now, under the unsupervised machine learning we basically have only features.

That means we just have X and there is no information about the target Y, which means at this point

of time, we do not have any information as what is the expected output in the scenario of this unsupervised

learning.

So we try to find the patterns in the data set.

Now this pattern can be as simple as grouping the data points depending on the how it actually look

like which comes under the task of clustering.

And like this, we will try to find the various patterns in a given data set, which will help me to

group the data and help me to further understand the data in an efficient manner.

So this is about the unsupervised learning.

And under the reinforcement learning we make use of the systems which are based on the hidden Markov

model or Markov chain based models, where we try to learn from our previous experience.

So these are the types of machine learning that we have got.

In general, depending on the type of data and the task that we are performing.

Now if you refer to the official documentation page, you are expected to know about the machine learning

models in a deep level so that you'll be able to understand and answer the questions which are related

to the application level.

So going further in the next video, we'll start our first learning journey.

That is with supervised machine learning journey with one of the most commonly used model that is linear

regression algorithm from the perspective of the certificate examination.

So we'll stop the video over here and let's continue in the next video with our machine learning model.

That is linear regression.