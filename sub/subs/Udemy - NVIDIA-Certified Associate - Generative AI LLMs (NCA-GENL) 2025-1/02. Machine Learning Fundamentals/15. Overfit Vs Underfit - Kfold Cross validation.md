# 15. Overfit Vs Underfit - Kfold Cross validation

---

WEBVTT

In this video, we are going to learn about the concept of overfitting and the underfitting in machine

learning.

And also we will look into the one of the most commonly used machine learning overfitting technique,

that is the cross-validation technique.

So cross-validation is one of the technique that is used to prevent the overfitting.

So we learn about the same as we progress in this video.

So let's get started.

Now this overfitting and underfitting are the two crucial concepts in machine learning and are generally

prevalent, and they actually causes the poor performance of a machine learning model.

So here let's start by understanding this.

Overfit.

Now overfit is a scenario where a model performs very well, extremely well on a training data, but

has a poor performance with the test data.

That is the new data.

Now in that case, we call it as an overfit case.

Now in this case, the machine learning model actually learns the details and including the noise that

we have got in a training data set.

So as a result of this, it negatively affects the overall performance of the model on the new data.

That is the test data.

Now one of the like one of the common example to give you okay how does an overfit would look like.

Suppose I've got a data set that would look like this okay.

This is how my data set would look like okay.

So this is how my data set would look like.

Now what I'm going to tell a model as a overfit model where this model when it draws the decision boundary

it is going to draw a decision boundary in this way.

As you can clearly note down, I am drawing the decision tree is is being drawn based on each and every

point that I have got in this data set.

Now imagine what happens if I had one more point over here.

Now what?

That time it might have actually joined the line.

Like in this way that means whatever the decision boundary that I have drawn earlier will change, even

if there is a small change in my data.

So this scenario is called as an overfit model.

Now when we have an overfit model, we say that an overfit model is going to have high variance in my

model and it will actually have a low bias.

So these are the two features that we will have inside this overfit model.

Some of the common reasons of this overfitting is so the data that we generally used it, especially

during the training.

So at that time the data is unclean.

That is the data is not cleaned and it contains noise.

Okay.

That's one of the major reason, I would say when it comes to the overfitting.

Okay.

And another reason that I would say is in case of this overfit model, one of the another reason is

model is having high variance model is too complex.

Because of these three reasons I would say we would observe the overfit.

Now how do we tackle this overfit model?

Well, we can actually tackle the overfit model by using k fold cross validation.

Okay.

And the another way that we can reduce the overfit, which you have learned in the previous section

is the regularization technique.

So by applying the regularization technique we are going to reduce the overfit on this given data set

and also training with more data will also help us in generalizing the model instead of performing the

overfit on the given data set.

And lastly, making use of the ensemble techniques.

That means instead of using single model, we will have to use the multiple models so that we can learn

the patterns in a given data set in a better way.

Okay, these are the important concepts related to the overfitting.

Okay.

Now coming to underfitting.

See, I'm going to say that a model is actually underfitting on this given data, when the model has

actually not learned the patterns in the given data set.

To illustrate the same, consider a scenario.

Let's say I have got the data points in this way.

So I've got the data points in this way.

When I have a data points in this way, let's say I have applied a very simple model.

So the algorithm that I have chosen is linear regression algorithm.

So as a result of this, this is how my linear regression line would look like.

You can clearly observe that this linear regression line that I have clearly drawn is not able to explain

the overall patterns that is present in this given data set.

Now, if I use this algorithm and fit on this given data set, and if I go ahead and generate the prediction

from this trained model that is logistic regression model, then we are going to observe poor performance

from this model.

So this kind of model is called as an Underfit model.

In case of underfit we will have high bias and low variance.

So we will have high bias and low variance in my given data set.

Now when we talk about this underfit.

So one of the main reason that we can observe there is an under underfit that is happening in this given

data set.

Well, one because that data that is used for training is not cleaned and it might contain the noises.

So that could be one more value, one more reason.

That is the same reason that I have mentioned earlier.

And the model has high bias.

Okay.

And model is too simple in order to learn the pattern in this given data set.

Now some of the techniques that we can use to avoid this underfitting is increase the number of features,

because one of the main problem that we get whenever we are performing the overfit is we have more number

of features.

So this is actually the opposite.

So we actually increase the number of features because there is only less number of features when we

have an underfit model and increasing the model complexity.

that's one more thing that we have to do.

And another thing is increase the duration, increase the duration of training.

So these are the commonly used techniques in order to avoid the underfitting whenever we are performing

the fit on our given data set.

Now the question is what exactly is a good fit in machine learning?

So in order to find a good fit model, we have to look at the performance of a machine learning model

over the period of time with the training data.

Now, as the algorithm learns over the period of time, the error of the model on the training data

reduces.

Along with that, it is also going to reduce the error on the test data as well.

Now, if you train the machine learning model for too long, then at that time the model may actually

learn the unnecessary details and the noises that are present in the training data, which would lead

to overfitting.

So in order to achieve a good fit, you have to stop the training at a point where the error starts

to increase.

Okay, this is one of the key thing things that you have to remember whenever we are training the machine

learning algorithm.

Now one of the common way that we reduce the overfit because in the problem data that we are working,

we are actually working with the scenario where my model is actually performing the overfit.

So in order to reduce the overfit, we make use of a technique that is k fold cross validation technique.

So let's understand this k-fold cross validation technique that we can apply it on the given data set.

See when we talk about the cross validation.

So you may see this cross validation is also being referred as rotation estimation.

Or sometimes it is also known as out-of-sample testing.

Now the overall goal of this cross validation technique is to help us to evaluate the machine learning

models.

So here in the scenario.

Okay.

So we are going to train the number of models on different different subsets of my input data.

Based on that we are going to validate the machine learning model to understand as whether it is overfitting

or whether it is underfitting on the given data set.

So basically we check the whether my model is actually performing the overfit or not.

Okay.

Now by limiting the data set.

So what I'll be doing is I'll be forcing the models to generalize the given data with the limited data

that I'll be sharing with it.

Now when we talk about the cross validation.

So how do we actually perform?

Well, the way that we perform the cross validation is we are going to split the data set into training

data and the test data.

Now the parameters that I'll have, it will undergo the cross validation test to see which parameters

are actually the best ones to select.

And these parameters will then be implemented into the model for retraining, and the final evaluation

will occur.

And this will depend on if the cycle has to go again, depending on the accuracy and the level of generalization

that has been achieved by my model.

Now under this cross validation technique there are various techniques.

So some of the commonly used cross-validation techniques are.

One is, as I mentioned, k fold cross-validation technique, the other one is hold out cross-validation

technique, and the next one is leave one out cross-validation technique, and the last one is leave

p out cross-validation technique.

So these are the various types of cross-validation technique.

So when we talk about this k fold cross-validation technique, the k fold cross-validation technique

is when the data set is actually split into k number of folds, and it is used to evaluate the model's

ability whenever we have the new data.

Now, the value of k over here refers to an integer value.

To give you an example, let's say I have the value of k is equal to y, k is equal to five.

Now whenever I've got the value k is equal to five, it would mean that this is actually the five fold

cross validation okay.

This is actually known as five fold cross validation.

Here each fold that I'll have, each fold is actually used as a test data at one point in a given process.

So what we generally do is whenever we have the data set, we are going to define the value of k.

And we will divide the data set into k parts.

In this example I have mentioned as k is equal to five.

So I'm going to perform the split of my data set into k parts 1234, five.

Okay, so in this way we will split the data set into k fold or k parts.

Then what I'm going to do is I'm going to consider k minus one parts for training and one part for testing.

Okay I'm going to keep one part for testing.

Now I'm going to train the model on this k minus one parts.

And I'm going to evaluate the model on the one part.

To give you an example, in the first iteration I'm going to treat part one as my training data and

the part one as the test data okay.

And remaining 2345 as my training data.

In the next iteration, I am going to treat part two as my test data and part 1345 as a training data.

And in the third iteration, part three as my test data and part one, two, four, five as my training

data and so on.

So we'll repeat this same step by changing the value of k test data set okay.

So we are going to choose k minus one parts as our as our actually the training data.

And we are going to generate perform the testing on the remaining part.

So we'll repeat this activity until all the parts have been treated as the test data at some point of

the time.

Now if I want to implement this k fold cross validation for a given data set, let me demonstrate the

same.

See if I want to implement this K-fold scenario.

I'll start by importing this K-fold from sklearn dot model underscore selection I'm going to import

K-fold here.

I'm also going to import the numpy library as well.

Next I'm going to say x is equal to NP dot random dot randint.

Okay.

So zero comma ten comma four comma two okay I'll have four rows and two columns.

And I'll mention my y as NP dot array okay 123 and four.

So what I'm going to do in this scenario is I'm going to initialize the object of k fold.

And I'll say that k fold.

And inside the parentheses number of splits is equal to two.

That means I'm choosing the value of k is equal to two.

Next I'm going to say for train underscore index comma test underscore index in CF dot split.

And I'll mention it as x.

Okay.

So I'm going to print my train as well as my test.

So I'm going to mention it like this I'll say this as I'll say this as X train.

And I'm going to mention my train index.

These are the index.

And I'll say test.

And I'm going to mention my test index.

Okay.

I'll represent my training index and my test index.

Now along with that I'm going to display my training data and the test data as well.

I'll say X train comma X test is equal to X train index okay.

And I'll say X test index.

And I'm going to print.

Okay.

And then I'm going to print my X test.

You can repeat the same activity for Y as well.

I'm going to say y train comma y test is equal to y train index test index y train and y test.

So if I execute this see I've got my given data set okay.

My let me just print my X so that you'll have a better picture print x okay.

So my x has 79263704.

So I'm choosing the row id two and three for the first iteration as my training data and row id zero

comma one as my test data.

Okay, so here you can clearly see that I've considered k minus one parts okay.

So that like I've considered over here in this scenario I have performed the number of splits as two.

So as a result of this this is going to split over here okay.

And for the training it has considered two and three and four test.

It has considered zero and one.

Okay.

So what it has done is it has created the splits in this way.

Number of splits I have mentioned as two.

Okay.

To index it to one three is my training data.

And zero and one are the test data.

In the other case zero and one are my training data and two and three are my test data.

So in this way we can perform the k fold cross validation.

And in this scenario the value of k is equal to two.

That means I have splitted the data into two parts.

Okay, this is how we can go ahead and implement this k fold cross validation technique on the given

data set.

Now the way that we implement this k-fold cross validation technique is during the hyperparameter tuning.

So during the hyperparameter tuning we will perform this k fold validation technique with the help of

the hyperparameter tuning algorithm, which will help us to evaluate the model in a better way.

So team, with this, we come to the end of this video as part of your learning journey.

In this video, you have understood about the concept of overfit and underfit and the way that we can

reduce the overfit in our given machine learning algorithm with the help of the k-fold cross validation.

So I'll stop the video over here.

So let's continue in the next video where we are going to learn about the hyperparameter tuning.

So I'll see you next time.