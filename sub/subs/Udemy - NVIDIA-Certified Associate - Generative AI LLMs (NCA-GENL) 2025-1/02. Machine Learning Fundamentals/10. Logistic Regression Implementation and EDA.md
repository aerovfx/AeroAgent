# 10. Logistic Regression Implementation and EDA

---

WEBVTT

Okay.

In this video let's go ahead and implement the logistic regression algorithm on a given data set.

So the data set that I'll be using is a bank data set.

So before I take you to the hands on journey, let's have a quick recap and some of the key considerations

that we have to make whenever we are implementing the logistic regression algorithm.

So here when we talk about this loss regression it's a machine learning classification algorithm, which

we use it to predict the probability of a categorical dependent variable.

And in the scenario of this logistic regression, the dependent variable will be binary in nature.

That means the target y will be either 0 or 1.

An example okay.

So in other words, we are going to use the logistic regression model where it would predict whether

the given data belongs to one of the classes.

That is one of the two classes.

So here when we talk about this logistic regression model.

So this also makes some assumptions.

Some of the important assumptions of this logistic regression model includes whenever we are performing

the fit on a given data set that is on my x and y, we always should consider the meaningful variables,

and there should not be any multicollinearity in a given data set.

We have to take care of multicollinearity of this data set.

And another thing is the independent variables okay.

The independent variables are linearly related to log odds.

Okay.

So these are some of the key assumptions that this logistic regression algorithm is going to make.

Now the data that I'm going to use this data is from UCI Machine Learning Repository.

And this data set is based on direct marketing campaign that is phone calls of a Portuguese banking

institution.

Now the classification goal is to predict whether the client will subscribe to a term deposit or not.

Here subscription to the term deposit.

I am going to call it as a target variable.

So I've already downloaded the necessary data set and it is present in my working directory.

The file is banking dot csv.

So let us start by loading the necessary libraries.

And let's load this given data.

And let's see how does the overall data would look like.

So I'll start by importing the necessary libraries I'll import pandas.

So I've imported the numpy, matplotlib, seaborn and traintestsplit function from the scikit learn

library, and I've also imported the logistic regression algorithm from sklearn dot linear underscore

model.

The labelencoder from sklearn dot pre-processing.

And I'm also going to set the style of the seaborn.

I'll say style equal to white.

Okay I'm going to set the seaborn plotting style.

Now I'll just go ahead and load this data as a data frame.

So data is equal to PD.

Read underscore csv.

The file is banking dot CSV.

And let's load the first few entries of this data frame.

Okay, if I load the first few entries of this data frame, this is how the first few entries of this

data frame would look like.

I've got the columns of age, job, marital status, the education, whether they have defaulted or

not.

Okay.

And then I have the information about housing loan.

So these are the various columns in the data.

See if I want to give complete information about each of the column.

I'll do one thing.

I'm going to display the column names of this data frame.

Okay.

So and then I'll display the information about this data frame.

Now this will be a better way to describe my data set.

So age as you can clearly see it's an input variable.

This is numeric in nature.

And job is string in nature which is the type of job.

So the values are admin, blue collar entrepreneur, housemaid management.

So these are the various values that we currently have in this column of job.

So let's go ahead and take a look into the same okay.

So these are the various jobs that are available.

And considering the other column let's say marital for so for the column of marital.

So this is also a categorical variable.

And we have the values as married, single divorced and unknown.

And then we have the education column.

So under this education column this is also a categorical variable.

We have got the values like basic for y basic six y basic nine years high school illiterate professional

course, university degree and unknown.

Okay.

And we have the information about the column of default which says that whether they have defaulted

in their credit.

So this is a categorical variable.

We basically have no yes and unknown values.

And then we have one more column over here in this data set that is housing.

If you consider this housing column.

It says whether they actually have a housing loan or not, which is also a categorical variable.

And we have got the loan column over here okay.

Whether they actually have a personal loan or not.

So this is our other information.

And we have the contact.

Contact describes the type of communication type that has been set by the customer.

Okay.

And we have the month which describes the last contact.

Month of the year, days of the week.

Duration campaign p days.

So these are the various columns that we have got on this data set.

Now out of all these column the last column, that is why this is the target column.

That means here it says whether the client has subscribed to a term deposit or not.

So let's go ahead and check how does the distribution of this target y would look like.

So if I perform the value counts on this column Y see I've got three, six, five, four, eight rows

where the value equal to zero and 4640 rows where value equal to one.

Okay, so that is how we can get to know the class distribution.

And we can clearly observe that there is an imbalance of the class C.

There is an imbalance of the class okay.

So there like now the next thing that we can do is we can go ahead and try analyzing the data.

So here what we can do is we can just go ahead and perform the grouping on any of the required column

and then generate the summary.

For example, I can go ahead and I'll say that as data dot group by and I'll perform the grouping on

the basis of, let's say, job dot mean.

Okay, so we have the mean age of each and every job that we have got.

Okay.

So this is what we can observe from this given data the student is having.

The average age is 25.

Self-employed are having the average age as 39 retired is having the average age are 62.

So we can just go ahead and perform these kind of various analysis on this given data.

Now let's do one thing.

Let's have a look into a scenario where what kind of customers have actually purchased the term deposit.

So I'll say data dot group by I'll perform on the target column Y and I'll apply the mean function.

So here if I take a look into this data set.

So the average age of customers who have actually bought the term deposit is higher than that of the

average age of customers who have actually didn't purchase it.

Isn't it?

And the P days.

Okay, the P days is nothing, but the days since the customer was last contacted is understandably

lower for the customers who have actually bought it.

The lower the price, the better the memory of the last call and hence there are better chances of a

sale.

So another thing that we can observe is the campaign.

So when I say campaigns, that is number of contacts or calls made by the current campaign are actually

lower for the customers who actually bought the term deposit.

Okay, so that is how we can interpret from this analysis, that is from this summary that we are getting.

Now let's go ahead and understand the relationship between the purchase frequency for a specific job

title.

So in order to do this comparison I'll make use of PD dot cross tab.

And then on top of that we'll create a bar chart.

So let me show you how what I would actually mean.

So I'm going to say mat plot lib inline okay.

And PD dot cross tab I'll perform the cross tabulation between the column of job and column of I.

And here I want to plot the bar plot.

Okay.

And I can add the title.

So if I now execute this cell.

Okay.

So we should be able to see the visualization.

See we have got the bar plot and we can see like who are the customers who have actually bought more

compared to the others.

So clearly admin has more number compared to the other people.

And we also see that more number of people who is working in the admin, they are actually they are

actually the one who is actually purchasing more bank deposits compared to the others.

That is what I could observe.

Okay, now let's go ahead and check whether the person marrying has any impact on whether or not they

are going to purchase the purchase this product or not.

So this time, instead of job, I will mention as marital.

So now if I take a look into this so we can observe that the people who are actually married, they

are most likely they are most likely to buy.

But it's not actually a strong predictor for this outcome.

Variable like that is what I could see from here.

But we also have the more number of rows for the married.

So even this does not seem to be a strong predictor for the outcome variable.

Okay.

Now let's go ahead and take up the further visualization as well.

Now let's proceed ahead.

And this time I'll consider the education.

Okay.

So this is marital.

So now if I visualize this see this is for my education.

So we can see how the education and their respective number would look like.

That is the number of rows number of people actually purchasing.

Okay, so this is looking good for us folks okay.

Now what we can do is in order to get a better idea, we can actually normalize the data.

So when I say normalizing the data, I can just go ahead and bring all the values in a specific manner.

Let me show you what I would mean.

I'm going to assign this to a variable called table okay.

And I'm going to add this information.

Okay.

So we now have the information about the marital status and whether they have bought or not.

Now I have actually scaled the data.

So from this scaling, so we can see that the the person's marital status is not much of an information

when it comes to checking whether they are going to buy this product or not.

So moving further, I'll consider the education.

Now, if we consider the education, we can see that people who are illiterate, actually they are the

one who are actually purchasing the deposit compared to the others.

That is what we can observe.

And we can also go ahead and check out the various combination as well by comparing it with month and

all those things.

Okay, so in this way we can perform the analysis.

Now moving on.

We can also perform the analysis on the numerical column.

So let us consider the column of H.

So let's see how does the distribution of this H column would look like.

I'll say data h and I'll calculate the I'll call the hist function.

So this is how the histogram chart would look like.

Okay.

So all these are working correctly folks.

Now what I'm going to do is so we'll just create the dummy variable for the given data set.

The reason that I'll take up this get dummy is because most of the data that have got is nominal in

nature, since most of the data is in nominal nature, as we have studied earlier in the exploratory

data analysis, the technique that we have to apply is the one hot encoding.

So the easier way to implement it is with the help of Getdummies.

So I'm going to say data final is equal to PD dot get dummies okay.

And here I can mention my data.

And then I can also specify other parameters like drop first is equal to true.

So this will help me to drop the first column and help me to reduce the number of columns in the data

set.

Okay.

Now I'll keep the remaining settings as it is.

Now, once this is done, I'm going to display the first few entries of this data underscore final.

So we now have 54 columns in this given data set.

Okay.

Now we have all the data which is in numerical format.

So whichever is the one which was in string format, we have converted into a numerical format with

the help of one hot encoding, since we have got the imbalance data.

So we'll be balancing with the help of spot.

So I'm going to say x is equal to data.

Dot drop columns is equal to y.

Y is equal to final data y.

So we have separated the x and y.

Now once we have separated the x and y we'll just go ahead and split the data into two parts and then

apply the fit resample on a given data set.

So I'll just go ahead and import the Smote and initialize the value of Smote.

Perform the split into two parts the.

Once we have split into two parts, I'll just go ahead and perform the fit resample and then we'll construct

it as a data frame.

Okay, so I'm extracting the column names from the xtrain.

Then we are performing the fit resample.

And once we have done that we are regenerating back our input and output.

Okay.

So fit underscore.

Resample.

Okay.

So this has completed the activity of balancing the data set.

Okay.

So we can validate it.

So if I just go ahead and show you.

Okay.

Value counts.

See, we have got the class balanced in this given data set.

Okay, now what we are going to do is we are going to implement.

And one of the common technique in machine learning that's called as recursive feature elimination technique.

Now that's the technique that we will be employing for this logistic regression algorithm here.

When we talk about this recursive feature elimination, it is based on the idea to repeatedly construct

a model and choose either the best or the worst performing feature.

And then we will set the feature aside, and then we'll repeat all the process with the rest of the

feature.

Now this process is applied until all the features in the data set are actually exhausted.

Now, the overall goal of this recursive feature elimination technique is to select the features by

recursively considering smaller and smaller set of features.

Okay, so that is the core aspect.

And the idea of this RFE that is recursive feature elimination technique.

Now in this case I'll just go ahead and implement the same.

So what I'll do is I'll start by importing the RFE.

And we have already imported our logistic regression.

And I'm going to initialize my logistic regression model.

And then I'm also creating an RFE will perform the fit on the data that we have created earlier.

Then we are going to return the support and ranking.

So here as a result of this execution.

So I have actually mentioned my logistic regression model and number of features to select as 20.

So as a result of this it has come up with the 20 features which it is actually shortlisting.

Now if I want to show you the list of features that it has actually selected, see, these are the columns

that have been selected as the important columns in order to perform the like.

Learning the representation between the X and Y.

Okay, that is what we can see from this given data set.

So what I'll do is I'll just go into I'm just going to create another variable called columns where

I'm going to store all these values.

Next I'm going to select only these features for my x and y.

Now once I've selected only these features for the x and y.

Next we can go ahead and perform the fit on this given data set.

Now here I have again split this given data set into two parts.

And then I'm actually performing the fit on this logistic regression model.

And at the end I'm generating the prediction.

And finally I'm trying to access the accuracy.

Okay.

So the accuracy score for the test data is given by 0.88.

So this is what we see from this given data set.

Okay.

So what we have done in this scenario is we have actually prepared the data set, perform the EDA,

understood the given data set, and we have applied the RFA technique and found what are the features

actually important for me now based on those, we have again selected those features and again perform

the fit on the logistic regression model.

And finally on the accuracy of the test data.

We are getting the we are seeing the accuracy of 88%.

Okay, so with this we come to the end of this video on logistic regression.

In the next video, we will proceed ahead and see and learn about the various evaluation metric for

logistic regression algorithm.

In other words various evaluation metric for the classification task.

So we'll end up we learn about it in the next video.

So I'll see you next time.