# 17. KNN Algorithm

---

WEBVTT

Welcome back to your learning journey.

Now in this video we'll talk about the KNN algorithm.

The k KNN algorithm is also known as k nearest neighbor algorithm.

And it's very easy to understand algorithm which comes under supervised machine learning.

So let's get started and let's understand what exactly is this k KNN algorithm and how it actually finds

the pattern in a given data set.

Now this k KNN algorithm, as I mentioned it comes under the supervised machine learning.

Now we generally use it for classification.

And this can also be extended for regression as well.

I can say this as one of the versatile algorithm, which we generally use it for imputing the missing

values as well as resampling the given data sets.

Now here as the value of k such as k is nothing but the integer okay.

K nearest neighbor is nothing, but it is going to consider the nearest data points in order to predict

the class of the continuous value or of a new point.

Now, to help you understand the same, let's consider a scenario.

Okay?

Let's say we've got a school and there are actually five students that we have got.

Okay.

So consider this example.

So I've got like people in a college okay.

So I can clearly see in a playground there are two groups of people.

So this is one group and this is another group okay.

This is actually another group.

Now out of this group I think there is one person over here who has forgot to bring in the sports jersey.

Now, if I ask you as to which group does this person who has not worn the uniform would belong to,

then you would say that this person would actually belong to the blue group, isn't it?

It's very easy and intuitive for us to guess that this person is actually belonging to the blue group.

Now, the reason that I would say that this person belongs to blue group compared to the pink group,

because here, in this case, this person is actually standing near the near the group of blue people.

So from this I'm going to guess that this person belongs to the blue group.

Suppose if this person was actually nearest to the pink group at that time, I might have said that

it belongs to the pink group, so the core intuition is in order to classify the data points, the new

data points that I've got.

In order to do that classification, I'm going to look into the nearest data points and perform the

activity of classification.

That's the core idea of this KNN algorithm.

So what happens in this scenario is it is called as an instance based learning.

So here as you can clearly see in this given scenario.

So I'll only consider with the help of my data points I would say that there are no parameters to learn

during the training, because I'll just look into my data set and I'm going to store it in my memory.

And this technique, because of this technique we call it as lazy learning because the model is not

doing any learning.

What it just does is it is going to save the entire training data in the memory.

And there is no parameters to learn during the training.

Okay, now the way this KNN algorithm is going to work is here.

It is going to save the entire data points in a parameter plane.

Okay?

And depending on the number of k that I have mentioned, it will choose those nearest data points.

And then it looks into the number of classes or majority of the classes to the new data point.

Based on that, it will perform the classification.

This is the overall idea about this KNN classifier.

In case of regression at that time, we generally implement it on the.

We generally implement it where instead of taking the voting, I'll be considering the value of average

value.

So that is the overall idea of this k k n and that is the k nearest neighbor classifier or the k nearest

neighbor regressor.

Now, in our example purpose, I'm going to make use of the same data set that we had used earlier.

So since this is based on the distance metric, it is a good practice that we perform the scaling.

So I'll split the data set and I'll apply the scaling.

Now this KNN algorithm can be imported from sklearn dot neighbors.

So from sklearn dot neighbors I'm going to import k neighbors classifier okay.

Now once I import this k neighbors classifier we can go ahead and generate the prediction.

So I'm going to mention it in this way.

So here I'll write the code as here I have mentioned as train error as a blank Python list, and for

test error I have mentioned as blank Python list.

Now for the k neighbors classifier algorithm, the number of neighbors that we have got, which is actually

a hyperparameter which we have to set during the training.

So I'm going to try out the values between 1 to 15.

And based on the performance of the model I'm going to decide on the value of k.

So I'm going to initialize the value of k neighbors classifier and neighbors is equal to k, and I'll

perform the fit on my training data.

And then I'm just trying to understand the error that we have got on this given data set.

And I'm plotting the error as well.

So this is the overall performance that we observe on the model.

Okay.

Now from this plot, okay, I'm going to choose the value of k.

So here from this case okay.

So the value of k where the where we have reached the minimum value of losses.

Maybe when value of k is equal to 11.

So when the value of k is equal to 11 that is when I am seeing most better performance on the test data.

So what I'm going to consider is I'm going to consider for this example k is equal to 11 okay.

You can make use of the decision uh that is with the help of plot.

Or you can also make use of the, the various other hyperparameter tuning technique that we have discussed

earlier that is gridsearchcv and perform the grid search as well.

Now here I'm just considering the visualization method and considering the value of k as 11.

So k neighbors classifier and underscore neighbors is equal to 11 and k and n dot fit x train y train.

Okay, before we do that let's just say okay.

So we have to actually use the X train transform and the X test transform.

That's something that I have to use.

Okay.

I'll just modify it.

And now let's see okay.

We can observe that when the value of k is equal to three okay.

That is when we are seeing the better performance on the test data.

Okay I'm going to use the value of k is equal to three.

So that's what I'll be using right now.

Let's just validate it.

Yeah.

So now everything is looking correct.

Now with k is equal to uh, number of neighbors S three.

Now I'll say X train transform.

I'll perform the fit on my training data.

Now I can go ahead and calculate the accuracy score.

So to calculate the accuracy score I'm going to import it from sklearn dot metrics I'm going to import

accuracy score.

And here for the accuracy score method I'm going to send in my test data and my prediction.

So how can I find the prediction?

I'll say y pred is equal to k and n dot predict x test transform.

Okay, then I'm going to mention my y pred.

So the overall accuracy that I'm seeing on the test data is 95 percentage of accuracy Okay, so this

is how we can make use of the KNN classifier, which is a very simple algorithm which actually stores

the given data set when it performs a fit.

And then when it is generating the prediction, it finds a distance from the data point to each and

every data data point in my data set, and then sorts it in a descending order and then it in an ascending

order, and then it's going to choose the top data points or the classes of the data points, which

are nearest the most.

Okay, that is the overall idea.

Alright.

So with this we come to the end of this video.

So as we progress in our next learning journey, we will look into the another machine learning algorithm

that is support vector machines.

So I'll see you next time.