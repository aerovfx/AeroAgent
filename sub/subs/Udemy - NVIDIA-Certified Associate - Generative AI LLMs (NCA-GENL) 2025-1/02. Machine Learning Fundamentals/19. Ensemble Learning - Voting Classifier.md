# 19. Ensemble Learning - Voting Classifier

---

WEBVTT

Welcome back to your learning journey in this AWS Machine Learning.

Now let's say we are dealing with a complex problem, and when we have got a complex problem, and when

we try to take the answer from an individual expert for that complex problem at that time, there is

a very high chance that we are going to make a mistake.

But if you pose the same complex question or the complex problem to the thousands of random people,

then the overall aggregation of their answer.

I would say that it's always better than an individual expert's answer, and this is called as wisdom

of crowd.

So the ensemble learning is based on the core idea of wisdom of crowd.

So in compare to the individual expert it's always better to get the aggregated answers from all the

random people.

That's the overall idea of this wisdom of crowd in the case of the complex problem.

Similarly, if I go ahead and aggregate the predictions of the group of predictors, it could be classifiers

or it could be regressors.

Then at that time, there is a very high chance that I'm going to get a better prediction than the best

individual predictor.

Now this group of predictors is actually called as a ensemble.

So ensemble is a group of predictors.

So we call this overall learning technique as called as a ensemble learning technique.

And an ensemble learning algorithm is actually called an ensemble method.

To give you an example, let's say I'm making use of this ensemble method.

Now, in that case, what I can do is I can train multiple decision tree classifiers.

And each of this decision classifier that I'm going to train it on my random subset of the training

data set, to give you an example, let's say this is my given data.

Okay.

Now I'm going to split it into three subsets.

This I'll call it a subset one.

This is my subset two.

And below one I'll call it as subset three.

So these are the three splits that I've created on my data.

Then I'm going to pick up each subset from my data set.

And I'm going to train individual decision trees.

Decision tree 1D2.

And the decision tree three.

So I'm going to train three individual decision trees.

Then once I have trained the three individual decision trees whenever I am sending a new data.

So this is a new data.

So whenever I've got the new data I'll be sending it for all the three decision trees.

And this decision tree is going to give the respective outcome.

Let's assume I'm dealing with the case of classification.

Then my decision tree one will say that this belongs to class one.

Decision tree three says class zero.

Then at the end, once I have generated the prediction for each of the decision tree classifiers that

have got at the end, I'll go ahead and check how many number of votes that I have got for class one.

I've got one vote, and for class zero I've got two votes.

So since I've got two votes for class is equal to zero, I would say that the overall outcome for this

new data, that is prediction for this new data is class zero.

And this technique of the ensemble of the decision tree is what we call it as random forest classifier.

Okay, so the core idea is ensemble is a group of predictors.

Now when we talk about this ensemble learning methods okay.

In the topic of this ensemble learning methods, we basically have various methodologies that are present

in this ensemble learning.

So we have a technique that is called as a voting classifier.

And we have something called as a bagging classifier.

And the other type of classifier is called as uh random forest and boosting.

So these are the various types of ensemble learning method that we have got in the ensemble learning

techniques.

So as you proceed we are going to look into the some of the popular ensemble methods, which are very

important from a learning perspective and the certificate examination perspective.

Now let's talk about the voting classifier.

Now let's say you have trained the few classifiers and let's assume that each classifier that you have

trained has actually achieved more than 80% of accuracy on the given data.

Now, in that case, let's say you have the logistic regression classifier, and let's assume that you

have got the SVM classifier.

Along with that, you have the random forest classifier and a k nearest neighbor classifier.

So let's assume that you have the.

You have these various classifiers that are present with you.

Now when you have trained these individual classifiers okay, you are getting an average accuracy of

80% on each of the individual models.

Now, a simple way to create an even better model or a better classifier is to aggregate the predictions

of each classifier.

Then, at that time, the class that gets the most number of votes is the ensemble's prediction, and

this majority vote classifier is actually called as hard voting.

To give you an illustration about it, let us say that I've got four models model one, model two,

model three, and model four.

The first model is decision tree, second model is logistic regression, third model is SVM and the

fourth model is KNN.

Okay, so I have performed the fit on my data set and each model is giving me the overall accuracy of

let's say 70%, 72%, 70%, 69% respectively.

Now, what I'm going to do is in the scenario of voting classifier.

So what it says is instead of training my individual models and then generating the prediction from

the individual model, we are going to consider the voting.

So once the training is complete here during the prediction, I'm going to send the new data to each

of the classifiers that have got.

Then I'm going to generate the prediction for each of the classifiers.

This will say class is equal to one.

Class is equal to one.

This will be class is equal to two, and class is equal to one.

So as you can see I'm using the various different classifiers in this example.

And then I'm generating the prediction on the new data.

Now once you generate the prediction on the new data, I am getting the output.

Now at the end, whichever is the one that is receiving the most number of votes.

In this case class is equal to one.

I am getting three votes and here class is equal to two.

I am getting one vote.

So depending on the number of votes that I am getting.

So I am going to decide okay to which class will be the overall output will be for the new data.

So I would say that this voting classifier can actually achieve the higher accuracy than the best classifier

that we might have got in this individual model, let's say compared to the logistic regression model

that I have got, I would say that this is going to give us the better accuracy compared to the individual

classifier that have got.

Okay, now just to illustrate the same, even if I have got a very worse classifier or a very worse

learner with me, even at that time, yes, even at that time, if I have got more number of weak learners

in the ensemble, then at that time we are going to get the better accuracy as well.

Okay, that is the overall idea about this voting classifier.

Now, to illustrate the working of this voting classifier, let's go ahead and implement it on a very

simple data set.

Here in this case I have actually written the code as from sklearn dot dataset.

I'm importing the make moons and I'm actually importing the decision tree classifier.

And along with this decision tree classifier, I'll also have to import the voting classifier.

This voting classifier is available under this from sklearn dot ensemble.

Now this voting classifier is what I can use it where I can perform the activity of ensemble learning.

So once that is done, I'm going to generate some random data and I'll split the data set into two parts.

Now here for the creation of my voting classifier, I'm going to instantiate my voting classifier with

my logistic regression algorithm, decision tree classifier algorithm and the SVC.

So these are the three models that I'm currently using right now.

Okay.

Now once I've specified the estimators, now what I'm going to do is I'll just go ahead and perform

the fit on this given data.

Now, once I perform the fit on this given data, if I want to find the overall accuracy of the individual

estimator, well, I can get the same by calling like this.

I'll say for name comma clf in quoting underscore clf dot named underscore estimator's underscore dot

items.

Okay, now for each and every model I'm going to mention it like this.

I'll say print and I'm going to say name is equal to I'm going to say self dot name.

Sorry self dot score.

Okay I'll send in my x comma y test.

Okay.

So if I execute this.

So this is going to tell me as what is the performance of my individual model.

Okay.

What is the performance of my individual model on this given data set.

It says logistic regression is 86.4 percentage of accuracy.

Decision tree is 85.6.

Percentage of accuracy.

And the support vector machine that is support vector classifier is 89.6.

Percentage of accuracy.

Okay, now when we call this predict method okay.

Whenever we call the predict method at that time it would actually generate and perform the hard voting.

Now if I want to go ahead and perform the soft voting at that time, I can also go ahead and make use

of the soft voting methodology as well.

Okay.

So by just specifying the soft voting that is voting as a soft, we can actually go ahead and perform

the soft voting as well.

So these are the individual model performance.

Now let me just go ahead and look into the performance of the voting classifier.

So x test comma y test.

Now on the overall performance of the classifier it is 90.4 C.

As you can clearly observe in this output, the classifier clearly outperforms the individual models

that we have got.

Okay.

So in this way we can go ahead and implement the voting classifiers by using the variety of classifiers,

that is the diverse classifiers which could be the weak classifiers as well.

Okay, this is the core idea of this voting classifier.

Now if I want to perform the soft voting, then what I have to say is apart from this, apart from mentioning

the estimators, I'll have to say voting is equal to soft.

So in this way I'll have to mention the soft voting.

Now along with that we also have to specify the estimators as with the with the probability as true.

So what I have to say is especially for the support vector classifier if you know, so it it actually

expects a parameter that is probability to be set as true.

So I'll say dot probability is equal to true.

Okay.

Now I can go ahead and perform the fit.

I can perform the fit on my data set.

That is with the help of my x and y, and I'll generate the scoring on this given data.

See now the overall accuracy on the model by making use of soft voting is 91.2 percentage of accuracy.

Okay.

Which is a slight improvement from the hard voting.

So this methodology of using the diverse models to overall increase the accuracy of my machine learning

models compared to the individual models, is called as the voting classifier.

All right, team, I'll stop the video over here.

In the next video, let's learn about the bagging technique, which is one of the most widely used technique.