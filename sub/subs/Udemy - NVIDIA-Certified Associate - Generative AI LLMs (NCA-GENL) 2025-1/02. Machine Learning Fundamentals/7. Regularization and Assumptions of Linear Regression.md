# 7. Regularization and Assumptions of Linear Regression

---

WEBVTT

In this video, we are going to talk about the regularization of the linear regression algorithm.

See, when we talk about the loss function of this linear regression algorithm, one of the common loss

function that is being used.

That means whenever the data that we have got is being fit on an algorithm, this x and y is being fit

to my linear regression algorithm.

It is being fit to the equation of y is equal to theta naught plus theta 1X1 plus up to theta and x

n.

Now the goal is to find the values of theta naught, theta one, theta two, and such that when I input

the value of x, I will be getting the output of y.

Now, in order to reach the best values of the theta naught, theta one, theta two up to theta n in

order to get the better values of the same.

Here, the system is going to make use of what we call it as a loss function.

Now one of the commonly used loss function is the squared error loss.

It's the squared error loss.

So for this squared error loss value okay.

By referring to this value, the system is going to decide as okay how it has to update the values of

theta.

So it will find the values of theta naught theta n such that yes, this squared error loss is minimum.

Now what happens is in some scenarios, even though we use the squared error loss in some scenarios,

the performance of the linear regression algorithm might not be as effective as we would expect.

In such scenarios.

We would see that even though the even though like we have tried to find the best parameters, but the

value of loss is very high in such cases, what we do is we are going to apply the regularization for

this linear regression algorithm.

Now here when we talk about this regularization, there are three types of regularization that are actually

applicable in the scenario of this linear regression algorithm.

The first regularization that we have is the Lasso regularization.

And here in this Lasso regularization which is also known as L1 regularization, we are going to add

the constant values.

Yes, we are going to add the parameters itself okay.

Which will be the absolute magnitude of the coefficient as the penalty term to my loss function.

So I am adding an additional penalty term for my loss function.

So in case of this lasso regularization, the equation or the overall idea is something like this.

Okay, the overall idea is for the Lasso regression loss function.

The goal is to find the minimum value such that the squared error loss term.

I'll say squared error loss plus lambda into absolute value of this theta.

So this is the overall representation of this lasso loss function.

See the idea is this squared error loss is what we have that finding the minimum of squared error loss.

It is the general loss criteria that we use it whenever we are performing the fit with the linear regression

algorithm.

Now, on top of that, I'm going to add the absolute value of the magnitude of coefficient.

Now here what I'm trying to do is I'm going to multiply it with the lambda.

So this lambda is another hyperparameter where I can tune this value to decide as how much effect that

I need to have from this L1 term.

Okay, if my lambda is equal to one, then that.

That would mean that I'm adding the absolute value.

And if I say lambda is equal to zero, that means I am actually removing this term itself, which is

equivalent to linear regression algorithm.

If I say lambda is equal to 0.5, then I'll multiply the absolute sum of value, whatever I'm getting

by 0.5.

Now by applying this lasso regularization.

So what this would tend to do is this is going to tend to make the coefficients to absolute zero.

Now you might be wondering as how does it make it absolute zero.

So just think about it.

Okay?

So here in this case you have the value of loss function that is squared error loss.

And along with that you also have the term which says as absolute value of theta okay this is what we

have got.

Now what is squared error loss.

Squared error loss is y minus y hat whole squared.

What is y hat is made up of y minus I would say theta naught plus theta 1X1 up to theta n x n.

So this is the squared error loss that we have got.

Maybe I'll have it as squared something like this okay.

So here if you consider the squared error loss I've got the parameters theta naught theta one theta

two up to theta n okay.

And along with that I have the additional term.

Now since I have the additional term let's illustrate it with a very simple example.

Let's say the general value of loss that I am getting is 5000.

Now when I perform the addition, that is the when I perform the summation of the absolute values of

coefficients, I am getting the value as another 1000.

So now in case of L1 regularization, I am getting the total loss as 6000 because 5000 plus 1000.

Now 6000 is the outcome of sum of squared error loss and the absolute.

Yes, and the absolute sum of the absolute magnitude of these parameters.

Now, in such scenarios, yes, in such scenarios, if I want to reduce the value of loss function,

I will be forced to reduce the value of coefficients.

But the reason because c y is given from the data x is also given from the data only the parameters,

that is, theta naught, theta one, theta two is the one that is in under my control to reduce the

value of loss, which in turn forces my algorithm to reduce the values of these coefficients.

Furthermore.

So when we are applying this L1 regularization, it actually tends to make the coefficients to absolute

zero.

The reason because we are going to add the absolute value of magnitude.

So here in case if I want to apply this lasso regularization so I can just import the model from sklearn

dot linear underscore model, I'm going to import Lasso.

And I can initialize my Lasso regularization algorithm.

That is the a regression algorithm.

And here for this lasso, I can also control the value of lambda that I had shown in the equation.

So there are various parameters in this lasso.

So we can actually go ahead and check out the same.

So here the value of alpha is something.

So using this value of alpha we can decide as how much importance that I have to give for the summation

of the parameters.

Okay.

By default it is set as one.

Okay.

Now I can go ahead and perform the fit on my training data X train comma y train.

And after I perform the fit, we can generate the prediction on the test data.

Once we generate the prediction.

So this time I'll just calculate the R2 score.

So if I calculate the R2 score the R2 score is 0.9122.

Which means with the application of Lasso regression.

So the overall R2 score, that means the explainability of the features about the target is all is still

the same.

Maybe I can fine tune it by changing the values of alpha, so we can either fine tune it and see whether

we would get a better performance on this given data or not.

Okay, now this is about the L1 regularization.

And in the same way we also have L2 regularization.

Now this L2 regularization is also known as ridge regression.

Now, in the scenario of this ridge regression, what we do is we are going to add squared magnitude

C, we are going to add the squared magnitude of coefficients as penalty term.

So here when we are adding this squared magnitude of coefficients as the penalty term for the loss function,

what this is going to result is this is going to result in getting the value of coefficients closer

to zero.

But yes, it is going to never okay, never set the value of coefficients to absolute zero.

So this is something that you need to remember.

Now when it comes to this regression, you can think of it as instead of absolute value, you'll just

take the square of it.

That's all D.

So when it comes to the implementation, coming to the implementation is very simple.

We'll just import the ridge regression okay.

And we'll initialize the same.

And if I calculate the R2 score see with this ridge regularization, I'm getting the same output like

before.

Now, apart from this lasso and ridge, we also have another regularization technique that is called

as elastic net regularization.

In this elastic net regularization, it is the combination of L1 and L2.

Okay.

It's a combination of L1 and L2 where in this case we are going to specify how much how much L1 regularization

that I have to apply and then how much L2 regularization that I have to apply.

And this we can control it with the help of the parameter.

And this regularization method is called as the elastic net regularization method.

So I'm going to apply it on this given data set.

So I'll initialize my elastic net.

And if I perform the fit.

So we are getting the similar performance as we have got earlier.

So for the elastic net regularization we have the L1 ratio and the alpha.

So with the combination of L1 ratio and the alpha we can actually apply how much L1 I should apply.

That means how much L1 regularization to be applied on my given data set, and how much L2 regularization

to be applied on the data set.

So this is the importance of regularization techniques in case of the regression task.

Now we will revisit the topic of regularization even when we are talking about deep learning.

So during the deep learning we also can add the regularization methodology over there in our deep learning

neural network, which will also help us in preventing the overfit in the deep learning neural network.

Okay, so this was about the regularization methods of linear regression.

Moving on.

The next topic that we have got is assumptions of the linear regression algorithm.

See, when we are applying the linear regression algorithm at that time, we have to ensure that the

data set that we are using.

It actually follows the assumptions of this linear regression algorithm.

It is our responsibility to ensure that the data that we have follows the assumption made by this algorithm.

If the data does not follow the assumption, then at that time there is a very high chance that my model

would not work as per our expectation.

Hence, it is our responsibility to ensure that it satisfies the assumptions.

So the first assumption that is being made in case of linear regression algorithm is there is a linear

relationship between features and the target.

So here in my given data set.

So let me just bring back our data set in our given data set.

The features are TV radio and newspaper.

And the target is sales.

So the first assumption is there is a linear relationship between them.

So how do we check for linear relationship?

Well, we can check for linear relationship by creating a pair plot.

Okay.

So I can creating a pair I can create a pair plot which will be a scatter plot for my features, and

the target which will help us to visualize whether we have the linear relationship between the feature

and the target or not.

So import seaborn sns dot pair pairplot.

Here df and x variables X variables are TV, radio, newspaper.

So I have mentioned my y as sales.

Now if I create a plot, see this is how the plot would look like.

As you can clearly see, there is a kind of linear relationship between my TV and sales.

Okay.

And there is a loosely linear relationship between the radio and sales.

And I would say that the between the newspaper and the sales.

So there is not much of high of a positive relationship or positive linear relationship between newspaper

and sales.

Okay.

That is what that we can clearly observe in the scenario of this case here.

In this case that is the pair plot.

Now this is a visual method.

Now apart from that, if I want to go ahead and uh, look into the numerical method.

So we also have a Pearson correlation coefficient.

So I can create a matrix like this I'll say df dot q r, which will help me to get the correlation coefficient

correlation correlation coefficient between the variables.

So here I am interested in the sales column.

So TV and sales are having the correlation coefficient of 0.78 radio it is 0.57.

And newspaper you can clearly see it is very low.

That is 0.22.

Okay.

So in this way we can visualize the data.

Now another another important assumption that my linear regression algorithm is going to make is there

is actually a mean residuals.

The mean residuals of my given data set is actually equal to zero.

Okay.

The mean residual is actually equal to zero.

So this is another assumption of this linear regression algorithm.

So let's do one thing.

Let's go ahead and generate the prediction for this given data.

So here we have the y pred test.

See I have the Viper test.

Now if I calculate the mean of this Viper test, that is the difference between y and y pred test for.

So let me just calculate the residual.

See residual is given by okay y test minus y pred test okay.

This will be the residual if I now display the residual this is the value of residual.

Now if I calculate the mean of this residual.

see I've got the value of mean of this residual is -0.075 which means it is closer to zero.

So the mean residual in this case is closer to zero.

And I can say that for this given data set this clearly satisfies the assumption of this linear regression

algorithm.

Okay.

And the other technique.

Other assumption that it makes is there is a normal distribution of error term.

So we would assume that there is a normal distribution of error terms.

So when whenever we want to look into the distribution of of any given data we can easily make use of

this dist plot to look into the overall distribution of this data set.

So if I say residuals and plt dot show.

So this is how the plot would look like.

Okay.

So this is how the overall plot would look like.

We can clearly observe that there is a normal distribution of the error terms.

And another, another condition that we have got, or another condition or another assumption that we

have got when we talk about this linear regression is multicollinearity.

So here in the scenario of this multicollinearity, what happens is we will make use of the statistical

term that is called as VIF score or the variance inflation factor.

Now using this variance inflation factor, we would actually find whether there is any correlation between

the features or not.

See we are okay to have a high positive correlation or high negative correlation between my feature

and the target, but we do not expect the features to have the correlation between each other, and

that is called as a multicollinearity.

So whenever the data that we have got is having the multicollinearity at that time, the ability to

generate the prediction is going to reduce the reason.

Because just think about this with a very simple example.

Let's say in your data set you have got height, weight and BMI.

Now the expectation is using this height and weight you have to predict the BMI score.

Now one thing that you would find is okay, if the height increases BMI is going to increase, weight

increases, BMI is also going to increase.

But there is a very high chance that if I increase the height, then the weight is also going to increase.

In other words, if I'm analyzing the individual column, let's say height.

And if I increase this column even though I want it or not, this is having an effect on weight, which

actually creates a confusion for the model as whether this change in BMI is due to height or whether

it is due to height.

So the predictive power of my linear regression algorithm is going to decrease whenever we have got

the multicollinearity issues in the given data set.

Now to check for the multicollinearity we will make use of variance inflation factor.

So in order to find this variance inflation factor we make use of the library that is statsmodels.

So I'm going to say from Statsmodels dot stats dot outlier underscore influence I'm going to import

variance inflation factor.

Okay.

It says that there is no module found.

So here to do the installation.

So the command that I have to run is pip install Statsmodels.

So let me just include this command in my notebook so that during the execution you can do the installation

first okay.

So this will take care of the necessary installation.

Now once it's done we'll just come down and we'll try to import the same variance inflation factor.

Okay.

So here once we have done the import, in order to calculate the variance inflation factor we have to

send in the data.

And also we have to send in the column index for this variance inflation factor function.

So in order to find the same I'll start by defining my own custom function I'll say def VI of score

and I'll get the data that is x.

And here, whatever the data that I'm going to get, let's say I'll be sending the data which is in

the pandas data frame format.

So I'll extract it in the format of an array by getting the values attribute.

Next I'll calculate the VIF score as variance inflation factor.

Here.

For this function I'm going to specify x and y where x and y are nothing, but x is my array that we

have got earlier.

I'll say x array okay.

And here for I in range I'm going to say x dot shape number of columns in my data set.

Okay.

Now once this is done I'm going to create a pandas data frame.

PD dot data frame.

Okay I'll just mention it like this.

Next, for this data frame I'm going to add a column called score, which are nothing but my VIF underscore

score.

Then I'm going to add the column for my data set.

So I'll say VIF score and I'll mention a new column called features.

This is is equal to x dot columns.

Okay.

Then I'm going to.

return.

So here we are F is equal to v I f okay I'm going to return v f underscore score.

So in this way I'm going to create a function.

Now for this function I'm going to send in my x train I can send in my x train.

This will help me to calculate the v I f score on my given data set Okay, I have in the given example

I've actually sent in my xtrain or instead of Xtrain.

You can also send the x as well.

So if I send in my data set this gives me the value of variance inflation factor.

So the way that we interpret this, if I have a var of score okay.

The general thumb rule, if I have the VIF score greater than four, then that means there is a multicollinearity

in a data set.

So if the VIF score is less than four, there is no multicollinearity in this given data set.

Okay.

So it's not so.

So it is not zero multicollinearity.

But if it is less than four I would say it is negligible or more moderate multicollinearity in this

given data set.

So when when we have multicollinearity in a data set, what we do is we are going to drop the column

which has the highest multicollinearity value.

That is VIF score.

And we repeat the process of evaluation of this VIF score.

Okay.

So this was about the assumptions of linear regression algorithm and the regularization technique that

will be applied on the linear regression algorithm.

So from the examination perspective you need to be remember about what is the overall assumption of

this linear regression algorithm.

The way in which the data will be fit for the given data set that is between X and Y, and some of the

assumptions and the way in which that we evaluate the linear regression algorithm.

So team, with this we come to the end of this video.

We will continue further in the next video where we will take a look into the another algorithm which

comes under the classification that's called as the logistic regression algorithm.

So I'll see you next time.