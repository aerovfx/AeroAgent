# 24. Clustering - Hierarchial Clustering

---

WEBVTT

In this video we are going to learn about the hierarchical clustering.

Now this hierarchical clustering is an alternative approach.

Here it is going to build a hierarchy from the bottom up approach.

And the best part is we don't have to specify the number of clusters beforehand.

So in the scenario of this hierarchical clustering we are going to build a structure what we call it

as a dendrogram.

So this is how a typical dendrogram would look like.

So suppose if I've got five data points in my data set, what I'll do is I'm going to put each data

point as an individual cluster, let's say a, b, C, D and e.

So these are the five data points that I have got in my data set.

I'm going to put each data point as an own cluster.

Then I'm going to identify the similarities between the data points.

Let us assume that A and B are similar, so I'll make it as one cluster.

I'll say a and B are one cluster, then A, b, C, D, and d I'll go ahead and find the two closest

clusters.

Let us assume c and d are actually closer.

So I'm going this is going to result in kd.

So a, b, C, d and e out of these three clusters.

Then I'll check what are the two clusters that are similar?

Let us assume that I'm going to find that C, D and e all these are actually together.

That is c d e.

So they are similar.

So I'll combine c d and e and I'll make it as one cluster.

Finally I'm going to combine a, b and c d and create an individual cluster that I'm going to call it

as a, b, c, d, e.

That means I'll identify the closest two clusters, and I'll combine them into one cluster.

And I'll repeat the same step until I have all the data points in a single cluster.

So this is the overall idea about this hierarchical structure.

Hierarchical clustering and this diagram that you see right here.

This is called as dendrogram.

This is called as the dendrogram.

So dendrogram is a tree diagram which is frequently used to illustrate the arrangement of clusters produced

by this hierarchical clustering.

So that is the role of this dendrogram here.

When we talk about this hierarchical clustering we basically have two types of hierarchical clustering.

So the first type is actually called as the agglomerative clustering.

So in the case of agglomerative clustering what we do is we are going to build the dendrogram from bottom

level.

So we'll start from the bottom level.

So we'll start the creation of dendrogram from the bottom level.

And we will merge the similar points of clusters.

And we will stop when all the data points are actually merged into a single cluster.

So this is the case of agglomerative clustering.

And we have something called as divisive clustering.

So in the case of divisive clustering or division clustering.

So what we do is in the case of division clustering we are going to split the data set okay.

We are going to split the root set of all the child clusters.

So what we will do is we'll start with all the data points in one cluster that is the root cluster.

And we will split from root, which is nothing, but we will start from top level.

Okay.

This is the overall idea of this.

The hierarchical clustering especially the division clustering.

Okay.

Now when it comes to finding the similarities between the clusters, there are various metrics that

are available for us in finding the similarities between the clusters, to name a few, and the most

commonly used ones include.

We have got complete linkage clustering, and we have single linkage clustering.

And we have mean linkage clustering.

And we also have centroid linkage clustering.

So these are the various types of distance metric that are available for us to determine how close the

two clusters are.

When we talk about the single linkage clustering so what we do is we will consider the maximum distance

possible between two clusters okay.

That's the idea of this complete linkage clustering in the case of single linkage clustering this is

the minimum possible distance between two clusters.

So this is the case of the single linkage clustering okay.

And in the case of mean linkage clustering in this case what we do is we will find the mean of pairwise

distance.

We will find the mean of pairwise distance for points of two clusters.

So this is called as the mean linkage clustering.

And we also have the centroid linkage clustering.

So what we will do is this is actually the distance between two centroids okay two circle or two cluster

centroids.

So this is about the case clustering.

So these are the four possible distance metric that we have got in order to find the similarities between

the clusters in the case of hierarchical clustering.

Now that you have an overall idea let's go ahead and implement it on the given data set.

So what we will do is we'll start by creating the clusters using the agglomerative hierarchical clustering

technique.

So we have we already have the data set.

So in order to create the dendrogram and perform the agglomerative clustering first I'll start by importing

the necessary functions.

So from SciPy dot cluster dot hierarchy I'm going to import dendrogram and I'm also going to import

linkage.

Now we will try out all the linkage methods that I have mentioned earlier.

I'll say methods is equal to.

We will use the single.

We will also use the average.

And we'll also use the complete for the demonstration purpose.

Now for each of the possible methods, I'm going to create a dendrogram.

So in order to do the same I'm going to say fig comma axis is equal to plt dot subplots.

So I'm going to have Len of methods.

And here I'll have one column.

And fixed size is equal to 20 comma 15.

2015.

Okay.

Next I'm going to say for I comma methods in enumerate.

Okay.

So I'm going to calculate the linkage I'll say linkage z is equal to linkage I'm going to send in my

data scaled.

Okay.

Now here we'll make use of the metric as the Euclidean metric.

And then I'm going to use the method as the method from this Python list.

Then I'm going to send it inside this dendrogram function z and a x is equal to axis I.

Then we can set the title and I'm going to say axis I.

Dot set underscore title.

okay.

And I'm going to say dendrogram.

And here the method whatever we have I'll say method dot upper okay.

I'll just convert it to an uppercase linkage okay.

That's the linkage method that I'll be using.

And I'll say access.

And we will set the Y label.

Set Y label okay.

The Y label that I'm going to mention it as the distance.

Okay.

We'll just execute the same.

So this is going to create a dendrogram for us.

So this has created the dendrogram for us.

So this is the overall dendrogram for single linkage.

This is the dendrogram for average linkage.

And this is the dendrogram for complete linkage.

Now we can see that out of all the three different linkage methods that we have got, the complete linkage

is the one.

Yes.

Complete linkage is the one.

It would give me the better separated cluster.

So I'm going to say that a cluster is better separated from this dendrogram when the vertical distance

connecting those clusters is actually higher.

And if you compare see for the complete linkage clustering I have the distance greater than 20 compared

to the average linkage and the single linkage.

Okay.

Now let us assume that if I want to create any four clusters or three clusters, then at that time I

can simply draw a horizontal line.

For example, if I want to create four clusters, maybe I can draw a horizontal line over here, which

is going to result in four different clusters.

Okay.

So in this way we can actually create the clusters for a given data.

So this is a dendrogram structure.

Now with this dendrogram structure what I'm going to do right now is I'll go ahead and perform the agglomerative

clustering.

So in order to perform the same I'll say hierarchical is equal to agglomerative clustering.

Now this agglomerative clustering is available in clusters okay.

I'll say agglomerative clustering.

Now hierarchical is equal to agglomerative clustering.

And inside the parentheses I'm going to choose the number of clusters as four.

As an example I'm going to use the affinity as Euclidean.

And then we will use the linkage as.

So what's the linkage that we would use.

We will use the complete linkage method okay.

So once we initialize this hierarchical object the next thing that I'll do is I'll go ahead and I'll

perform the fit on my data set.

Now, once the fit is complete.

So here I have performed the fit.

Now once the fit is complete, we can actually get the hierarchical cluster labels.

So I can say data hierarchy is equal to hierarchical dot labels.

Okay, now if I display the first few entries.

So I have got the hierarchical clustering over here.

Now I can also look into the distribution.

Okay.

The data hierarchy.

And I can also call the value underscore counts.

Okay.

This will help me to understand as how the overall clustering would look like.

So it says in the cluster zero I have got 432 rows.

In cluster two I have got five rows, cluster one two rows and cluster three one row.

Okay.

This is how we have performed the hierarchical clustering.

Now we can even further, uh, take a deeper look and understanding as what are the rows that are coming

in each cluster and try to interpret whether it is actually working as per the expectation or not,

or whether we have to adjust it for the data that we are working.

Okay, so this is the overall idea when it comes to the hierarchical clustering.

Now as we progress in the next video we will talk about the other clustering methodology.

The important one is DBscan methodology.

So let's talk about the DBscan clustering in the next video.

I'll see you next time.