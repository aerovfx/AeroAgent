# 11. Evaluation Metrics for Classification

---

WEBVTT

In this video, let's talk about the various ways in which we can evaluate the classification task model

performance.

See, when we talk about machine learning, the reason that we apply the machine learning, because

we want to find the patterns between the input and the output.

And once we find the patterns between the input and the output, we have to go ahead and generate the

prediction on the test data.

Now we cannot simply go ahead and generate the prediction on the test data, because we will not know

as whether the model is working as per the expectation or not.

So to evaluate a model, we make use of the evaluation functions.

Okay.

The evaluation functions helps us to evaluate how my machine learning model is actually performing on

a new data.

And this would actually help us in generalizing my given data set for the task that we are working.

It will help us as a guiding way to say that, okay, whether my model is working as per the expectation

or not.

So one of the common way that we perform the evaluation is with the help of the accuracy.

Now accuracy is one of the common classification metric.

It's very easy to understand, and it is generally suited whenever we are dealing with multi-class classification

and a binary classification problem.

And we'll make use of this accuracy whenever we want to describe the proportion of true results among

the total number of cases that has been examined.

In other words, here in case of accuracy, it is number of correct predictions divided by total number

of predictions.

That is how we'll be calculating the accuracy.

Now in the last video, we have calculated the accuracy score for the logistic regression model that

we have trained.

And the accuracy that we have got was 88%.

Now we'll be using this accuracy whenever we are dealing with the classification problem.

And when we are dealing with the data set, which is a balanced data set, that means whenever the data

set is not skewed or when there is no imbalance at that time classification, that is the accuracy score

is a most suitable one for the classification task.

Now, what happens when we have got a scenario where there is an imbalance of the class?

Now can it still be used?

So let's consider a scenario.

Let's say I've got a data set which talks about fraudulent transaction or not.

So I want to determine whether a particular transaction is a fraudulent or not.

So in my data set that I have got, let's say 10,000 rows is the total number of size of my data set

out of this 9900 rows.

It's for the it's not a fraudulent and 100 rows is for fraudulent.

Now in this scenario, let us say that I'm not going to train my machine learning model.

So I'm going to create a dummy function which will always give me the output as false.

That means which tells me that it's not a fraudulent transaction.

So if I just train, if I just create a function which is a dummy function which gives me the output

of zero every time, even that model can actually give me an accuracy of 9900 divided by 10,000.

So if I just calculate it.

So this is going to give me the accuracy of 99%.

As you can see in this example.

See the model which is not trained which is a dummy model.

Even that model is giving me 99 percentage of accuracy if it simply predicts false for every input data.

So that means when we are dealing with that data, which is an imbalanced in nature in such scenarios,

accuracy score might not be a suitable evaluation metric.

So what we do in such scenarios is we will make use of confusion matrix.

The confusion matrix will actually help us to identify as how model is confused whenever it is generating

the prediction for multiclass classification or a binary classification.

Okay, so that's the general idea of this confusion matrix.

Now if I show you a confusion matrix.

So this is an example of a confusion matrix.

In a confusion matrix we will draw a rectangle okay.

And in the rectangle we basically have four region.

Four regions are true positives true negative true negatives, false positives and false negatives.

So these are the four regions that we will we are going to see in the scenario of this confusion matrix.

Now this overall tells us, while classifying the examples for which class my model is actually doing

more mistakes.

Okay, so here if you observe this confusion matrix.

So we are going to say it as a true positive case when my predicted value is one and the actual value

is equal to one.

Now that is a true positive detection.

A true negative detection is when the actual value is also zero.

My predicted value is equal to zero.

That is a case of true negative.

Now false positive is a scenario where the actual is actually zero, but it is giving the output as

positive.

Now that's the case of false positive where the patient is not pregnant.

But the doctor is saying that you are actually a pregnant.

So that is a scenario of a false positive.

And false negative is a scenario where the patient is actually a pregnant, but the doctor is saying

that you are not a pregnant.

That means predicted value is zero, even though the actual value is one.

So by creating this confusion matrix, I can come to know for determining which class my model is doing

a lot of error, and whether I'm having false false positives more, or whether I'm having false negatives

as more number of rows.

So this is one of the most commonly asked question in the AWS machine learning examination.

That is the interpretation of the confusion matrix.

Okay.

Now in which scenarios that we would prefer the kind of data.

Now let me just give you an example.

Let's say if I am building a system for the healthcare where I want to predict a disease or not, okay,

I want to predict whether the patient is suffering from a disease or not.

To give you an example, let's say I have built a system to predict the Covid 19.

Now, in that case, I'll be okay to have a model to have more number of false positives compared to

the false negative.

See, false positive is a scenario where patient is actually healthy.

Okay, but I'm predicting that patient is having a disease now when when a model is giving that kind

of result, at least if the patient if the model says that the patient is having a disease, the doctor

or the patient can consult the another doctor or the or the doctor can actually look into the reports

again to decide whether the patient is actually healthy or whether the patient is actually suffering

from a disease, isn't it?

So at that time, it gives us to ensure that the patient's life at the priority.

So the patient care can always come first.

So at that time I'll be okay to have a model which is false positive compared to the false negative.

False negative model.

See in case of false negative where the patient is having a disease.

But I'll say that okay you are actually unhealthy which is very dangerous.

And this person can affect the other person as well.

Hence when we are training so those kind of model.

So I'll, I want the model I'll be okay to have a model which is false positive one but not the false

negative one.

So this confusion matrix will help us to evaluate from the given data and the model to understand as

how exactly my model is performing for classifying the various classes.

Okay, now from this confusion matrix we are going to find the other metrics as well.

So the other metrics that we have is given by precision recall and F1 score.

Okay.

So these are the other three metrics that we have got when we talk about evaluating our the classification

model.

So let's start one by one.

So let's start with the first metric that we have got.

That is the recall.

Now let's say that you've got a best friend okay.

You've got a best friend.

And your best friend is giving you a birthday surprise every year.

So every year he is ensuring that he's giving you a birthday surprise.

And he has been giving you from the last ten years.

Now, one day your best friend comes and tells you as, hey, do you remember all the birthday surprises

that you have actually taken from me?

Now this is a time that you have to prove that, okay?

You remember all the birthday surprises that have been given by your best friend.

So what you will do, you will try to recall all the birthday events and all the surprises that you

have got in the last ten years, and then you are going to share all the ten birthday surprises that

was given by your best friend.

So, for example, if you are able to recall all the ten events, okay, if you're able to recall all

the ten events out of ten birthday surprises, then I would say that you have the recall ratio as one.

That means 100%.

Now let's say if you are able to recall only seven events out of ten events, then the recall ratio

is 0.7, which is nothing but the 70%.

So recall is nothing but number of events you can correctly recall.

So this is called as a recall ratio.

Okay.

So I hope you are now clear with the definition of recall.

Now when we talk about precision let's say whenever you are predicting this okay.

Whenever you are trying to recollect the last ten birthdays surprises, you have actually taken 15 chances.

Okay, you have taken 15 chance.

You might have got confused with the best friend number two birthday surprises as well.

And you actually took 15 chances to recall ten events.

Which means in this scenario you have the recall ability of 100%, but it is not so precise.

It is not so precise.

So precision is given by number of events.

Correct recall divided by total number of chances that you have taken.

So in this scenario, I can say that ten divided by 15 is equal to 66.66 percentage.

So in this case the precision score is 66.66 percentage.

So this is the overall, uh, like overall way in which that we calculate the precision and recall.

Now when we talk about this precision and recall.

So we can actually find these values of precision and recall with the help of the formula.

So the formula for calculating the precision and recall is this.

So the precision is given by true positives divided by true positives plus false positives.

And recall is given by true positives divided by true positives plus false negative.

So this is the formula for precision and recall.

Now, apart from this precision and recall, we also have something called as an F1 score.

Now F1 score is actually an harmonic mean of precision and recall.

It's a harmonic mean of precision and recall, which is nothing but two into precision, into recall

divided by precision plus recall.

Okay.

These are the three commonly used evaluation metrics for the classification.

And we can get all these individual values by constructing the confusion matrix.

Are you clear, folks?

Excellent.

Now with this basic understanding, let's come back to our notebook.

And let's go ahead and, uh, generate the confusion matrix for the given data.

And along with that, we will also go ahead and calculate the values of precision, recall and other

stuff.

So let's proceed.

So first I'll start by generating the confusion matrix okay I'll first start by generating my confusion

matrix.

So in order to generate the confusion matrix I'm going to say from sklearn dot matrix I'm going to import

confusion matrix.

Okay.

Common confusion matrix.

And along with that I'm also importing the function confusion matrix display.

Then to calculate the R2 to calculate the F1 score I'll import my F1 score.

To calculate the precision score I'll be importing my precision score as well as the recall score.

Okay, I'll do the necessary imports.

Now then let's go ahead and generate the confusion matrix.

See, in order to generate the confusion matrix.

So what I'll do is I'm just going to call the function confusion matrix.

And inside the parentheses I just have to send in my actual values as well as the prediction values.

That is, my actual values is y test okay.

And my prediction value is this.

That is why pred okay, I just have to send in these values.

So this is going to generate the confusion matrix for me.

Now we'll create a visualization I'm going to say CM display is equal to confusion matrix display.

And inside the parentheses I have to send in my confusion matrix.

Then I can mention my display labels.

I'll say display labels is equal to.

So I can mention the display labels as per my requirement.

So in this case the display labels are zero and one.

Okay.

Then I'm going to say cm display dot plot plt dot show.

So this is how my confusion matrix would look like.

See.

In total we have got 6908 and 6595.

These are the correct predictions or the true positives and the true negatives in my given data.

And we also have the false positives and the false negatives from this given data set.

Okay, so that is how we are going to create a confusion matrix okay.

So here in this case the true negative is the left top quadrant that we have got over here.

That is actually my true negatives.

Then let me do one thing.

Let me write down that information.

Okay.

And false positives is top right quadrant and false negative is bottom left quadrant.

And true positive is bottom right quadrant.

So these are the various parts of this confusion matrix.

Okay.

Now from this confusion matrix we can directly go ahead and call the precision score.

Okay I'm going to print the precision score like this okay.

So I'm calculating the F1 score precision score and the recall score.

So the precision score for this given data is 0.8966.

Recall score is 0.8566 and F1 score is 0.8761.

Okay, so this is what we have got on our given data set.

Okay.

So with the help of our confusion matrix so we are able to find the values of precision score recall

score and the F1 score.

Now apart from these metrics there's one more metric which is commonly used during the classification.

And that metric is called as ROC AUC score.

So let's understand how do we make use of this ROC AUC score and what is the overall idea?

See, when we talk about this classification, especially the models which are based on probability,

the output that I get in the in this logistic regression model will be in the range of 0 to 1.

So this will be zero and this will be one.

It will be in the range of 0 to 1.

And based on the value of threshold I'm going to decide whether it belongs to class zero and class one.

And by default the value of threshold is set as 0.5.

Now that's the default value of a threshold.

Now let's consider a hypothetical scenario like this.

Now here, in case of actual values, let's say my y true values are like this 01100, one zero, and

one.

So these are all my actual values.

Now assume that my prediction probability is.

In this way my prediction probability is nothing but the output from the sigmoid or the logistic regression

function.

So in the scenario of the output of this last regression function, let's say the prediction probability

is something as 0.80.65 okay.

Let's say 0.2 okay.

And then I'll say 0.70.4 okay 0.45.

And the value over here is let's say 0.850.1 okay.

And the value is 0.82.

So these are the values that have got for the prediction probability.

Now let me consider the threshold as threshold as 0.5.

If I consider the threshold as 0.5 this would belong to class zero.

class one, class 1001 and I'll say zero and one.

Now if I change the value of threshold okay.

If I change the value of threshold, let's say for the threshold value of, let's say 0.75, then this

will be equal to zero.

This is actually equal to zero because here 0.0.65 is less than 0.75.

And this is also equal to 00010 and one.

So you can clearly observe that with every change that I'm doing on the threshold.

Yes.

For every change my value of my confusion matrix is getting changed.

See, for each and every value of the threshold, the value of my confusion matrix is getting changed,

so if I have n number of threshold, then this is going to result in n number of confusion matrices.

So that is where the ROC comes into picture.

So the ROC is known as receiver operating characteristic curve.

It is an evaluation metric for binary classification problem.

So the ROC actually helps us okay.

It actually helps us in creating the plot where the probability curve which plots my TPR versus the

FPR.

That is true positive rate versus false positive rate.

And this is going to plot at various threshold values.

And then there is another curve that's called as AUC.

That is area under the curve.

Here this is the measure of ability of a binary classifier to distinguish between the classes, and

is used as a summary for the ROC curve.

AUC is nothing but the area under the curve.

Okay, so the idea is the higher the value of AUC that is area under the curve, the better will be

the model's performance whenever we are distinguishing between the positive and the negative classes.

So whenever I have got the AUC is equal to one at that time, my classifier can correctly distinguish

between two classes okay.

And if I have the AUC zero at that time, my model is not working correctly.

Okay, that is the overall idea of this ROC and AUC.

Now to generate the summary in a in a mathematical way.

So we generally make use of a score that's called as an ROC AUC score.

Now we can calculate a ROC score from this ROC AUC score function The way that we can calculate is from

a sklearn dot matrix.

I'm going to import roc AUC score.

Okay.

Now in order to calculate the roc AUC score, what I have to do is I have to generate the probability.

So I have to call the function that is predict underscore prob a on my test data.

And then on these probability scores we can go ahead and calculate the racc score.

So I'll say y pred prob a.

And if I generate my y pred prob a it's going to tell me the confidence between one class to the another

class.

That is, it is 2.9% confident that it belongs to class zero, and it is 97% confident that it belongs

to class one.

So that is how we interpret this prediction.

That is the probability score.

Now if I want to calculate the ROC AUC score, I have to send in my Y test.

Along with that I have to send in the column okay.

And that is the column ID one.

So if I execute this here I've got the ROC AUC score as 0.9262.

So with this score of 0.9262, we can clearly observe that the model that we have got can classify between

two classes in a much more efficient way.

Okay.

That is the overall idea behind this ROC AUC score.

Now if I want I can also go ahead and actually create a plot for ROC AUC curve as well.

So if I want to create a plot for this ROC AUC curve.

So I have to import a function.

So instead of ROC score I have to import the ROC AUC curve okay.

Which is nothing but my ROC curve.

So once I import this, this is going to give me the FPR, TPR and threshold values.

And I'll send it inside my ROC curve function.

And I'm going to send my y test and y probability.

That is the y test.

Okay.

Now once this is done, I'll just go ahead and perform the activity.

So here I've actually got gone ahead and created the random probability, which is nothing but the random

prediction.

Okay.

So for that random prediction I'm getting the FPR and TPR and I'm creating a plot with the FPR and TPR

from this ROC curve of the logistic regression model, and I'm generating the plot, and this is how

my ROC plot can be created.

Okay, so as you can clearly see from this plot, the logistic regression model that is logistic regression

curve that we have got is actually higher than that of the random prediction.

Therefore, this logistic regression model actually does a better job than random classifier that I

would use.

Random classifier is nothing but the function which is not trained on the data set.

Okay, so these are the ways in which that we can evaluate the classification task on any classification

models.

So only thing that you have to keep in mind is whenever I'm performing the activity of finding the ROC

AUC curve.

So at that point ensure that.

So here at that point ensure that in the like you have, you are actually dealing with the model which

can generate the probability score.

Okay.

So with this we come to the end of this video.

So let's continue further in our learning journey of machine learning from the next video.