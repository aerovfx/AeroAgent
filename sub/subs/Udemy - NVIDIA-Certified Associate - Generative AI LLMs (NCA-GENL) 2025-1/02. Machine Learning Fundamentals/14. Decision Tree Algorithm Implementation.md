# 14. Decision Tree Algorithm Implementation

---

WEBVTT

So in this video, let us go ahead and implement the decision tree algorithm on a classification task.

So the data set that we are going to use is this social network social network ads dot CSV.

So this is a CSV file which I'll be using as part of our implementation.

So I'll start loading the necessary libraries.

I'll start by importing the pandas library Py library And along with that we'll import the matplotlib

library.

And I've also imported the seaborn library as well.

Okay, so in this way I'll just go ahead and import everything.

Okay.

So once I have done.

Once I have loaded the necessary libraries, we'll load this data as a data frame.

So PD, dot, read, underscore, CSV and I'll say social network ads dot CSV and I'll display it first

few entries of this CSV file.

So this is how the first few entries of the CSV file would look like.

So I've got the age column and I have got the estimated salary column.

And I have the information as whether the person has actually purchased the ad or not, or purchased

that particular product or not.

Okay, so this is what we have got on this data set.

Now what I'm going to do is first I'm going to separate my features and the target.

So data dot drop columns is equal to purchased.

Okay.

Next my why is equal to data.

And the column is purchased.

Okay I'll separate the x and y.

Now once I separate the x and y, we'll just go ahead and split the data set into two parts.

Sklearn dot model underscore selection I'm going to import train test split.

Then we'll split the data set into two parts.

So once I split the data into two parts.

So I'll start by scaling this given data set in order to scale the data I'm going to mention as from

sklearn dot pre-processing I'm going to import standard scaler okay.

Now I'll start by initializing my standard scalar.

Now once I initialize the standard scalar I'll perform the fit and transform on this training data set.

So I'm going to say x transform is equal to x dot fit underscore transform.

And I'll apply it on top of my x train.

And then on my X test I'll just go ahead and perform the transformation on this X test.

So I'll say X test transform.

And the above one I'm going to mention as X Train transform okay.

In this way we will go ahead and perform the scaling on this given data set.

Now once we have performed the scaling this time I'll start by implementing my decision tree algorithm

for this.

Given data from sklearn dot tree I'm going to import decision tree classifier.

Now apart from this decision tree classifier in the family of decision tree, we also have another algorithm

which is there for regression.

That is the decision tree regression.

So decision tree classifier okay.

And the other one is decision tree regressor.

So let's have a look into both.

So if I open the decision tree classifier.

So these are the various parameters of this decision tree classifier.

So we've got criterion.

We've got splitter maximum depth minimum sample split and so on.

So here criterion if you look into that default value default value is currently mentioned as Gini.

That is Gini impurity index.

Now apart from Gini we also have the entropy log loss and so on.

So this is the error function that we can use whenever we are trying to measure the randomness that

we have got in the given data set.

So in each tree that means for each sub node that we will have in the decision tree, we will check

the criteria.

And we are going to understand whether the values are getting reduced or not.

So that is the core perspective.

So the idea is reduce the value of loss as much as possible.

Now apart from that there is a parameter called splitter.

Now this is the strategy that we should use to split at each node.

Okay.

By default it is best.

And the next one it says maximum depth.

So maximum depth refers to the depth of the decision tree that when we construct it.

Okay.

So here if I specify the maximum depth as two then that would mean that I'll only have two nodes in

this given data.

That is, I'll only have two levels of node from this given decision tree.

Okay.

If I don't specify anything for maximum depth it is going to take the maximum depth possible to ensure

that each and every row in my data set has been correctly classified.

And there is a parameter called minimum sample split.

So this refers to the minimum number of samples which are actually required in order to split an internal

node.

And we have got the minimum sample leaf.

So this is nothing but the minimum number of samples that are actually required in order to become a

leaf node.

Okay.

And we have got the minimum weight fraction lift.

This is the weighted fraction of the sum total of weights required to be at leaf node.

And more importantly we've got the maximum features.

That means whenever we are trying to look for the best split.

So what is the number of features that I have to actually take?

Okay.

So that is the working of this maximum features.

So we have got three different possibilities auto square root and log two.

Okay.

If I do not specify anything then it will consider all the features in the given data set.

Then we have got the random state and the maximum leaf node and the minimum impurity index impurity

decrease and so on.

So these are the various parameters that we have got on this decision tree classifier.

Remember these are the parameters of this decision tree which we as a data scientist have to find so

that we can get the optimized results for a given data set.

It is our responsibility to find these values.

The system does not automatically determines these values.

Hence we actually call it as the hyper parameters.

These are the parameters which as a data scientist, which we have to find it.

And finding these hyper parameters for a given model is actually called as the hyper parameter search

or hyper parameter tuning.

Now to get started.

As of now, I'll go ahead and I'll start by initializing my decision tree classifier.

Now, in case if I was using any random this regression task, then at that time I would have made use

of this decision tree regressor.

So since it is classifier, I'll start by initializing the object for decision tree classifier.

Now to ensure the repeatability, I'm going to specify the random state as zero.

Next I'll just perform the fit on my given data set.

So I'll say X train transform okay then y train.

So in this way I'll perform the fit with the default parameter values on this given data set.

Now once the fit has been performed, we can go ahead and generate the prediction on the test data.

So I'm going to say y pred test is equal to tree dot predict x test.

Now once I have generated the prediction.

So here one thing that we have to do is it's actually x test transform.

Okay.

I'll perform the predict on this model.

Okay.

Now what we can do is we can go ahead and evaluate this classification algorithm.

Since it's a classification, you have already learned about the various evaluation metrics that we

can use for the classification task.

For the demonstration purpose, I'll make use of two metrics.

One is accuracy score and the other one that I'll be using is the F1 score.

So accuracy score.

So I'll say y test comma y test.

So the accuracy score for this given data is 0.9.

That's 90% accuracy.

And if we calculate the F1 score for this given data the F1 score is 0.8260.

Okay, so this is the overall matrix that we see with the help of this decision tree classifier that

we have trained on this given data set.

Now let's go ahead and visualize the decision tree that we have actually constructed right now.

So in order to construct the decision tree we will make use of the function that is called as plot underscore

tree.

So here if we look into this documentation of this plot underscore tree.

So the first parameter it gets is the decision tree itself.

So in my case that is a tree which is my trained object.

Then we can specify the feature names.

So in my scenario the feature names are nothing but the x dot columns.

Okay.

And then we can also make use of the value of class names if I want.

So I'm going to say class names is equal to true.

Okay.

We can specify the class names as per our requirement.

And we can also mention any other thing as per our requirement.

For now, I'll keep the remaining settings as it is.

Okay.

And finally I'll just say it as plt dot show.

So this is how my decision tree would look like.

Okay, so as you can clearly see, we've got so many levels that are actually present in this decision

tree that has been constructed.

Now to increase the visibility I'm going to modify the figure size.

So I'll just mention as plt dot figure fixed size.

So once I modify this figure size this is how the figure size would actually look like.

Okay.

Now let's go ahead and let's try to visualize it now.

Yeah now it is much better.

So here.

So even let's let me try to modify it again.

I'll just say 30 by 30 for now since it's a very big one.

So let's try to visualize it in the 30 by 30.

And yeah now it's much better.

See observe on top.

We have got the column okay.

Then we also have the splitting criteria.

The above node is called as a root node.

Now in this root node.

So I'm checking the age whether it is less than 0.611 okay.

And based on that I am actually splitting the given data set into two parts.

Now one is for class one and the other one is for class zero.

Then I've actually performed the further split and in a point just observe.

Okay I'm going to highlight a specific node.

So if you look at this node here I've got the class distribution as zero comma 28.

That means all the rows that are reaching this node are belonging to the class one, since it belongs

to the class one, and all the rows are belonging to the same class, I'm going to call this as a leaf

node.

Okay.

I'm going to call this as a leaf node.

And after it has reached this node that is leaf node state, there is no further split.

So that is the overall idea behind this leaf node.

So leaf node is a point where there is no further split is going to happen on this data set.

Now here let's do one thing.

Let's go back to our notebook and let's try to get the evaluation on the training data.

See if I perform the evaluation on the training data I'll just copy this code and I'll paste it in this

cell where I'll perform the evaluation.

And I'll find the F1 score as well on the training data.

So I'll say Y pred train.

And I'm going to send in my Y train transform.

Okay.

Next I'm going to validate with the help of my Y train and y train.

So now if you look into the F1 score on the training data, I've got the F1 score as 0.9958.

That means here we can clearly observe there is an overfit that is actually happening on this given

data set.

That means here my model is actually performing extremely well on the training data.

And if I look into the metrics on my test data, it is not performing well on the test data.

So this kind of situation is called as an overfit situation.

Okay, so let's park this overfit situation aside, and let's come back to the decision tree algorithm.

We'll talk about the overfit in the upcoming videos.

Okay.

Coming back over here.

So this is the performance that we see on this decision tree algorithm.

Now another thing that you remember is when I have constructed the decision tree algorithm here in this

case, the, the whatever the parameters that we have got, those are all the initialized value of the

parameters.

Now let's suppose if I modify any of the parameters of my decision tree, then the way my decision tree

is going to look is also going to change.

Okay.

And also if any small changes happens in my training data, then even the shape of my decision tree

is going to change.

So what we clearly observe the problems in this decision tree algorithm is clearly it is performing

the overfit on the data set.

And then we do not know the correct value of the parameter for the decision tree.

So these are the two key challenges that we currently observe from this practical implementation of

decision tree classifier.

So I'll stop the video over here.

In the next video, we'll talk about the overfit and the underfit of what exactly does it mean from

the machine learning algorithm perspective, and how do we go ahead and approach this overfit and the

underfit problems that we have got?

Okay, so I'll see you in the next video.