# 26. Time Series Analysis

---

WEBVTT

In this video, we are going to perform the time series analysis, and we are also going to learn some

of the key terminologies that are involved inside this time series analysis, and learn the techniques

to analyze the time series data.

So let's start by understanding what exactly is this time series analysis.

So first of all you need to understand a time series.

A time series is defined as the set of data that is dependent on the time Here what happens is time

actually acts as an independent variable.

So you can think of x as the time.

And we are going to estimate the dependent variable okay.

This y will be the dependent variable.

So when we talk about from a mathematical perspective time series is a set of observations which are

taken at specific times, usually at an equal interval.

Suppose I can say that y is equal to f of t, where y is a function of t.

So that is the time.

So I can say that a time series is actually made up of y one, Y2Y3 values up to y n at that time.

Instance of T1T2T3 up to t n.

So this is the overall idea about this time series.

Now understanding and analyzing this data is little different.

The reason because here the independent variable is actually the time itself.

Now this time series analysis is used in various applications.

So it is used in business forecasting.

It is helpful in understanding the past behavior.

It will also help us in planning for future operations and evaluate the current accomplishment.

So that is the reason in business and in real time, we make use of this time series analysis.

Now, in order to analyze the time series in an effective way, you need to understand four important

components of a time series.

They are trend, seasonality, cyclical patterns, and the irregularity.

So let's understand each of these in detail so that you get a better idea about these four components.

Let's start with trend.

So when I say trend so trend is a gradual shift or a movement to relatively higher or lower values over

a long period of time.

Okay.

Suppose if I've got a data and this is the initial value of sales, and let's say as the time proceeds,

this is the final value of sales that have got.

This is day one and this is a date 30.

Now you can clearly observe that compared to the day one in the day three and if I observe the overall

pattern in the data set, I can say that there is an increasing in trend.

There is a increasing trend in this given time series data.

Okay.

So when I have a time series data where it shows the upward movement in the values.

That's where we call it as an uptrend.

Now, when the trend that I have got, when it exhibits a general pattern that is downward, then we

call it as a downtrend.

Now if there is no trend then we call it as a horizontal trend.

Okay.

That is how we are going to describe the trend in a time series data.

It generally describes how the shift or the movement of values from my initial point to the final point,

whether it is increasing or the decreasing, coming to the another component that we have got right

here, this is called as seasonality.

Now seasonality refers to upward and downward movement that we observe in a repeated fashion within

the specific period of time.

Now let us suppose that if I create a plot which describes the sale of mobile phones.

So let's say this is how the sale of mobile phones would look like.

So this one describes for 2020 and this sale describes the 2021.

If you observe both the year data in this month okay, we see there is an increase in purchase.

We see an increase in purchase for the month of, let's say, June compared to the other months, let's

say January or December.

So what it means is here we observe this increase in sale of mobile at the same point of the year,

that is in the month of June.

So this repeating pattern that we observe in a fixed time period in this time period is actually one

year.

So this repeating pattern that we observe in a fixed time of one year is actually called as a seasonality,

because we observe it in the June season.

That's what we call it.

In the same way the sale of ice cream would actually go higher when it is in a summer month.

So that's the overall idea in which we actually interpret the seasonality component.

And then we have a cyclic component.

Now cyclic component is very similar to seasonality.

But in the case of cyclic component there is no fixed time period.

In the case of seasonality the time period is fixed.

I mean it can be for in one year or it can be for two year, but it is fixed.

But in case of cyclicality, this is a repeating up and down movement.

But it will go more than one year and there is no fixed time period.

So in those cases it is very difficult to predict the pattern in a given data set.

Okay, that is the overall idea about cyclical components.

Lastly, there is an irregularity.

Now irregular time period is nothing but the erratic, unsystematic the short term and the non-repeating

fluctuation that we observe in the data is what we call it as an irregularity in a time series.

Now, this could be due to the random variation or unforeseen events, or it could be because of the

presence of the white noise in a given data.

So because of all these reasons, we would observe the irregular pattern in a time series data.

Okay, now that you have understood the important components of time series.

So let us go ahead and look into the limitation of this time series.

See if I want to perform the time series analysis in an effective manner.

So some of the key thing that we have to keep in mind is so one is the data points must be linear in

the relationship.

The data transformations that we are going to make are the mandatory ones.

And the models mostly work on the univariate data.

So these are the key things that you have to keep in mind whenever we are making use of statistical

methodologies in order to perform this time series analysis.

Now, when we are working with the time series data so we can classify our time series data as either

a stationary time series data or we can classify it as a non stationary data.

When I say stationary data, stationary data is a data set where it does not have any trend.

There is no seasonality, there is no cyclic component, and even there is no irregularity component

of the time series.

Now, in this case of time series, the mean value will be a constant one, and even if you check the

variance in the time series data, it will be a constant with respect to the time frame.

So that kind of plot is a stationary plot, and the time series data is called as a stationary time

series data.

So this is a special kind of time series data where there is no cyclic component, there is no irregularity

component, and there is no trend.

So this kind of data is very important for us for the effective analysis and even the algorithm that

we use it to analyze the time series expects that the time series data that we are having is actually

stationary in nature.

On the other hand, we have a time series data as a non stationary time series data.

So if in case the mean we mean variance or a covariance is changing with respect to the time, that

means if the mean value is not constant or the variance is not constant, then we call the data set

as a non-stationary data set.

That is the non-stationary time series data.

Here, in this non-stationary time series data, if I have either a trend seasonality or a cyclic component,

or even if I have an irregular component, then at that time we are going to say that the time series

data as a non-stationary time series data.

Now why do we actually care?

Well, the reason that we care about this, whether it is a stationary or a non-stationary, because

most of the statistical algorithm expects that the time series that we are dealing with is actually

stationary in nature.

Okay, now that you have the idea about this, now let's proceed ahead and see how we can check for

stationarity for a given time series data.

In order to check for stationarity in a time series data, we can make use of a statistical test like

the augmented Dickey-fuller test, or we can also make use of Skoki, Kwiatkowski, Phillips, Schmidt,

Shin test.

So this is the test that we'll be using to check for stationarity in a given time series data.

Okay, so what we do is in case of this augmented Dickey-fuller test, we assume that okay, we assume

that the given time series is a non-stationary time series.

We assume that it's a non-stationary time series.

Now, with that assumption, yes, we will go ahead and check for the hypothesis by applying this augmented

Dickey-fuller test.

Based on that, we will describe whether the time series is a stationary time series data, or whether

it is a non-stationary time series data.

Okay, that is how we would check whether a given time series is a stationary or not.

Now we know that to apply the algorithm, I have to get the time series data into a stationary format.

Well, the way that we can get the time series data into a stationary format is by applying the various

methods.

Here in the time series analysis, we've got various methods which will help us to convert a non stationary

time series data into a stationary time series data.

The methods are detrending differencing and the transformation.

So these are the various strategies that are available for us to convert the stationary time series.

That is the converting my non stationary time series into a stationary time series.

Now out of these I would say differencing is the one which is most suitable on each and every data that

we that we generally work with.

Okay.

And it is much more flexible compared to the other methodologies okay.

we apply all these techniques to convert a non-stationary time series data into a stationary time series

data.

Now that you know the basics of time series data and key things that you need to remember whenever you

are analyzing this time series data, let's go ahead and take up an example time series data.

We will do all these analysis.

We'll check for trend seasonality and we check for the stationarity in a given time series data.

And as we progress, we'll also look into the analysis as how we can apply the algorithm, such as Autoregression

moving average models to understand how to predict the future values.

So we'll discuss everything in the next video.

So I'll see you next time.