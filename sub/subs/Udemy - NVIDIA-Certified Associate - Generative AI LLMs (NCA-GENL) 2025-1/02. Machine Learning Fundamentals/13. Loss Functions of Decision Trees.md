# 13. Loss Functions of Decision Trees

---

WEBVTT

In the last video you have had an introduction to Decision Tree algorithm.

Now, before we go to the hands on implementation, let's have a quick understanding about the technicalities

of decision tree algorithm.

When we talk about this decision tree algorithm, this decision tree algorithm has actually uses multiple

algorithms in order to decide how we can split a node into two or more sub node.

Okay.

Now this choosing of algorithm will be based on the target variable.

Now when we talk about this scenario that is the decision tree algorithm.

We have got various algorithms that will be used in decision tree.

So to name a few I have got the ID3 algorithm okay.

We have got C 4.5 algorithm which is nothing but the successor of ID3.

And we have got CART algorithm which is classification and regression tree.

We have got Chaid algorithm chi square automatic interaction detection.

Apart from that we also have the Mars algorithm.

So these are the commonly used algorithm that we have got in decision tree.

Now you don't need to remember the names I'm just giving you so that you have the better understanding

as how the patterns will be learned.

So let me just explain one by one at a very high level, so that you get a clear picture as how it actually

works.

Now when we talk about this ID3 algorithm, it actually begins with the original set.

Let's say yes at my root node.

Now on this root node with this existing data set, what it does is it is going to calculate the entropy

and the information gain.

Yes, it will calculate entropy and the information gain for this attribute.

So what is that attribute.

It iterates through each and every attribute that we have got in.

Yes.

And then it calculates the entropy and information gain for the each and every attribute that we have

got on the data set.

And then it is going to select the attribute where the entropy is lower, or we have the very largest

information gain okay.

So if I get the maximum information gain that is the attribute that I'll be using it for performing

the split.

Okay.

This is the core idea of this ID3 algorithm.

Now here when we talk about this entropy.

So entropy is nothing.

But it's a measure of randomness in the data set.

So this is very important for you to remember.

The higher the entropy the harder it is to draw the any conclusion from that particular information.

To give you an example, let's say I'm flipping a coin.

So if I'm am flipping the coin at that time, suppose it is a fair coin, then the chances of getting

a head and the chances of getting a tail is actually equal to 0.5.

That is one by two because it's completely random.

We do not have any control.

Now, if I go ahead and create a plot for my entropy as well as the probability score that I have got.

So if I create a plot, we can clearly observe that the value of entropy is maximum.

That is, it is equal to one when the value of probability is equal to 0.5.

Now the entropy will be minimum when the probability is either 0 or 1, which means that if I am 100%

confident that a particular event would occur, then at that time the probability that is the randomness

is actually zero.

Okay, so this is the overall idea of this entropy.

Entropy measures the randomness in the given data set.

So here the formula for this entropy is given by sigma of I is equal to one to c minus p I log to the

base two of p I.

Now let's go ahead and calculate the entropy.

When value of p is equal to one by two c when the value of p is equal to one by two.

So let's say probability of head is equal to probability of tail.

So suppose if I have in my sample space or in my in my space, if I have got two kinds of event that

is head and tail, then at that time, okay.

So probability of head and probability of tail is one by two.

So this is given by minus p of head log to the base two of p of head minus p of tail log two to the

base two of p of tail, which is given by.

If I substitute it minus one by two, I'll just take the minus one by two.

Common.

This will be nothing but log to the log of base two of minus one by two.

Sorry.

One by two okay.

Minus plus this will be log to the base two of one by two.

So this is what we have for this formula.

Now if I go ahead and calculate this value.

So let's go ahead and calculate the values.

So we'll just open up a calculator.

Okay.

Suppose if I say it as 0.5 c log to the base two of 0.5 is given by minus one.

So here in both the cases this is nothing but minus one by two.

This is minus one minus one which is nothing but minus one by two multiplied with minus two.

So two into two cancel.

This will be minus one.

So minus into minus is plus.

So the value of entropy that I'm getting when the value of p is equal to one by two is actually equal

to one.

So which means the entropy is maximum when the probability is 0.5 okay.

Now imagine in the same way what happens when the probability of zero, when the probability is zero

at that time, one of the term will definitely be zero and the other case, the probability of, uh,

the probability of other event will be equal to one, which is nothing but minus one into log log of

one to the base two.

So if I say x is equal to one of, log to the base two, c log of one to the base two is equal to zero

okay.

So in this way we can go ahead and perform the activity of finding the entropy.

Okay, so my entropy will be maximum when the when there is a high randomness in the data, entropy

will be minimum when we have no randomness in the given data set.

Okay.

Now another term that we have actually talked about is actually my information gain.

Okay.

When we talk about this information gain.

So whenever we split the decision tree into two different node okay.

So what we will do is we will calculate the entropy at the root node okay I'll calculate the entropy

at the root node.

And I'll also calculate the entropy at the child node.

So I'll go ahead and find the difference between my previous entropy and the overall entropy that I've

got in my child node.

This difference is actually known as the information gain.

So information gain is a statistical property which measures how well a given attribute that we have

got separates our training examples according to the target classification okay.

So this is the core idea of this information gain.

So this is this is nothing but entropy of the parent minus entropy of the children.

So that's the overall idea of this information gain.

And apart from this there is one more loss function that we would specify while implementing the decision

tree algorithm is Gini impurity index.

Now when we talk about this Gini impurity index here in this case you can consider this Gini impurity

index as the cost function, which we use it to evaluate the splits in a data set.

Now we calculate this Gini impurity index by subtracting the sum of squared probabilities of each class

from one.

So what it does is it generally favors the larger partition and it is very easy to implement.

Whereas when we talk about the information gain it favors the smaller partition with distinct values.

Okay.

And apart from that we also have a chi square and the reduction of variance technique, which will help

us to fit the given data set on the training examples.

So this is the core idea behind the loss functions that are being used in the scenario of decision tree

algorithm from the certification examination perspective.

So you need to remember about the entropy, which is the measure of randomness in the data set.

And the information gain is the difference between the parent one and the child one.

Okay.

That is what we call it as an information gain and Gini impurity index is another way of finding the

error during the classification.

Okay.

So this was about the working of the loss function in case of decision tree algorithm.

So with this we come to the end of this video.

Let's continue in our next video where we will go ahead and continue our learning journey in this decision

tree algorithm.