# 27. ARIMA Hands On

---

WEBVTT

So in this video let's go ahead and let's see how we can analyze this time series data.

And an algorithm that we apply in order to perform this time series analysis.

So I'll begin by installing the necessary libraries.

So here I've imported the libraries of pandas numpy seaborn.

So I had already installed.

So I just imported these libraries.

I'll begin by reading this data as a data frame.

So the data frame, like the data that I'm going to use right now is a seaplane travel Travel dot csv.

So if I display the first few entries of my dataframe, this is how the first few entries of this dataframe

would look like.

I've got the month column and I have the number of passengers that are traveling.

Okay, let's do a quick analysis on the data that whether we have any missing values in this given data

set.

So we have 144 entries that we have got on this data set.

And we can clearly see that we do not have any missing values in this given data.

Okay.

Now, one thing that we have to ensure is that while reading the data for time series analysis, we

always prefer to get the column that is the date column in the format of date time format.

Currently, the month column is reeded or being treated as the object data type.

So let's do one thing while reading this file itself, I'm going to make use of the parameter pass underscore

dates and I'll specify the column of month.

Okay.

So I'm going to process that column in the format of the date time.

Okay.

Now we can validate it df.info.

So now the column month has been read in the format of date time.

Now what I'll do is I'll just set this column as, uh, the index that is the month column as the index

by saying it as DF dot set index in place is equal to true.

Now along with that, I'm also going to rename the column of hash passengers into just passengers.

Okay.

So let's just validate this fixes.

So now we have fixed the data.

We have the month column that has been set as index and passengers I have renamed this column.

So this is looking good.

Now the first thing that we want to check is we want to understand whether this given data set is like

is this a stationary plot or a non-stationary time series data?

So in order to create a visualization I'll also import the matplotlib library.

So import matplot lib dot pyplot as plt and matplotlib inline.

Okay, now I'll just select this column of passengers And on top of this I'm going to call the plot

function.

So this is going to create a line plot for me.

So if we look into the visualization, this is how the overall time series data would look like.

So clearly from the visual analysis we can see that this time series data.

Yes, this time series data, we can clearly observe that this is non-stationary in nature.

Now, since this is non-stationary in nature, we have to find a way to convert this non-stationary

time series data into a stationary time series data.

Now before we do that.

So let's also validate it from the statistical perspective as well by performing the hypothesis test

of a fuller test.

Now the ad fuller test.

is available inside the Statsmodels library.

So from statsmodels dot tsa dot stat tools, I'm going to import Eddie Fuller.

Okay.

Now once I import this Eddie Fuller, if I want to get the p value, I'll just mention it in this way.

I'll say print.

Okay.

The p value of the given data is okay.

I'll send my A data to this add folder function.

And for this add folder function I'm going to specify the column of the data set okay.

That is passengers.

Okay.

From here, I'll get the value at the index position one.

Okay.

This is going to return the value of p.

So the p value that we are getting on this given data set, once we apply this Eddie Fuller hypothesis

test is 0.99.

Since the value of p is greater than 0.05, it means null hypothesis follows.

That means given data is a non-stationary data.

Okay, this is what we can decide by performing this hypothesis testing.

So now the expectation is we have to find a way to convert this non-stationary data into a stationary

data.

Now as I mentioned during our discussion one of the easier way that we use it to convert a non-stationary

data into a stationary data is by taking the difference.

So I'm going to mention it like this.

So if I want to take a first order difference okay I'll copy this.

And I'll mention like this the p value of the data after first order difference.

See, first order difference is nothing.

But for this DF passengers column, I'll call the diff method.

So this will take the difference.

That is the consecutive difference.

Now when I take the difference this way is going to result in an any values.

So that's the reason I'm going to apply this drop in a.

Now if I take the first order difference and calculate the value of p, If you look into the value of

p after the first order of difference, this is very much closer to 0.05.

Okay, so what I can say is, if I take a first order of difference on this given data set, I can get

a data which is closer to a stationary data.

So I can use this level of differencing as one.

Or if I want, I can also proceed ahead by taking the second order of difference as well.

So second order difference is due of passengers dot diff.

Dot diff okay.

So here if I take the second order difference.

Now this second order difference is much more less than 0.05.

So we can either choose first order of difference or the second order of difference to convert the non

stationary time series into a stationary time series.

So we just take the difference between one value and its previous value.

And then we will analyze those values those difference values okay.

That is the overall idea about this differencing.

Now we can go ahead and find the value of T.

And now once we have actually converted the given data into a stationary format, the next thing that

we can do is we can apply the statistical models.

Now from the certification examination perspective you need to be aware of the basics of Arima model.

So here the Arima model is known as auto regression integrated moving average model.

So auto regression is nothing but one value in my time series is is is expressed as a linear regression

equation of its previous terms.

So that is called as an auto regression.

And when we talk about the moving average here we are considering the error terms.

So auto regression integrated moving average integrated is nothing but the value of D.

That is the level of differencing that I have to apply it on a given data set to convert our non-stationary

time series data into a stationary time series data.

Now, in order for us to find the value of p, D, and q, in order to apply this Arima model, we can

make use of a library that's called as PMD Arima.

I'm going to say pip install PMD Arima.

So this is going to do the installation of this PMD Arima.

Now once this is done I'll just go ahead and find the values of p, d and q for the given data set.

So in order to do the same I'll apply this function auto arima.

And from this I will get to know the best model that is suitable for this given data.

Okay, in this case, the best model that is suitable for this given data is P is four.

That is, the auto regression term is four, level of differencing is one, and the level of the q that

is the moving average terms is three.

Okay.

So these are the values of p, d and q.

Now the best values are p and q.

Now with this best values of p and q we can now go ahead and perform the implementation.

So we have the values of p and q.

And I have extracted this column value of this data frame.

Now once I've done the extraction I can now go ahead and apply this Arima model.

So I can apply this Arima model by writing as statsmodels dot tsa dot arima dot arima.

The time series values order is equal to 413, which is the order that we have got from this PMD arima

function.

Okay, so I'll perform the fit on the given data.

Now, once I perform the fit on this given data, we can now go ahead and perform the forecast.

So if I say result dot forecast.

So this is going to generate the forecast for the next day.

Okay.

This will generate the forecast for the next day.

So now if I want to generate the forecast for let's say next ten days I can say result dot predict 144

comma 155.

So in this way I can generate the prediction for the future days.

So the key takeaway is when we when we when we talk about the time series data, it's a data where it's

a collection of the where the collection of values, where the values are collected at a specific interval.

And when we are working with this time series data.

So we will first check whether it is a stationary or non stationary once we bring the model that is

the data into a stationary format, then only we will be applying any algorithm.

One of the commonly used algorithm is the autoregressive integrated moving average algorithm.

That's just a name that you need to remember from the examination perspective.

And then we can find those parameters and generate the forecast.

Okay.

So I did not go in depth about the implementation of Arima because that is not generally asked in the

examination.

In the examination, it's more about how do you find the trends in a given data set, or how do you

find the seasonality pattern in a given data set.

So it's more of a conceptual idea.

Okay.

So with this we come to the end of this video on time series analysis.

So let's continue further in the next video.