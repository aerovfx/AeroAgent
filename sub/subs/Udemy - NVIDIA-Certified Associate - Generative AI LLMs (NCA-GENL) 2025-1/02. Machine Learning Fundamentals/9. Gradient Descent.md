# 9. Gradient Descent

---

WEBVTT

In this video we are going to learn about the gradient descent algorithm.

See, when we talk about this gradient descent.

Gradient descent is by far the most popular optimization strategy that is being used in machine learning

as well as deep learning.

So we'll take a look into this gradient descent from the machine learning perspective in today's session.

And then as we proceed in the future learning session, we'll also talk about the gradient descent in

the deep learning as well.

So coming back to our topic, where do we use this gradient descent algorithm?

So we use this gradient descent algorithm whenever we are training the machine learning algorithm like

the algorithms like the logistic regression algorithm.

And it's not just related to only logistic regression algorithm, we can actually implement it on top

of any algorithm as well.

So here let's understand what is the core idea about this gradient descent algorithm.

So it's an optimization algorithm.

Now what happens is whenever we are performing the fit on our machine learning algorithm, the idea

is to learn the relationship between the input and output.

Now when I say learning the relationship the way that we would measure that my relationships is actually

strong or weak is by with the help of the loss function.

So what we will do is this gradient descent algorithm helps us to move towards from the higher loss

value to a lower loss value in an iterative manner.

So that is the core idea of this gradient descent algorithm.

So when I say gradient a gradient is nothing but it's a measure of small change.

So if I change a value by a very small quantity, how does it impact the other value?

So how much the output of a function changes if you change the inputs by a very little bit.

And that is what we call it as a gradient.

And in the scenario of this gradient descent algorithm, you can think of it in this way.

Imagine we've got a blindfolded man over here, and this blindfolded man is actually present in a mountain.

So this.

We've got a blindfolded man, and he's on top of the hill as of now.

Now what?

What this person has to do is he has to reach.

He has to climb down the hill as much as possible.

That means he has to move.

Towards the steepest direction possible.

Now, since this guy is blindfolded, what he can do is he can make use of his foot and tap towards

him, and then move in a direction where there is a steep.

So as and when he founds that, okay, there is a steep in that particular direction, he can take baby

steps at a time in order to avoid, uh, missing out on the steepest point of the hill.

So here, in the same way you can think of the problem solution as a loss function.

Now, whenever we are training the machine learning algorithm at that time, let's say this is a point

where we have the initial loss.

Now the goal is to go ahead and update the parameters such that the value of loss will be minimized.

Now these on the x axis I have the values of parameters.

So at my initial value of my parameter, let's say theta initial with my initial value of parameter,

this is the initial value of loss.

Now what is the goal?

The goal is to reach a point of the parameter such that the value of loss is minimum.

So this is a point where the loss is minimum.

So I want to reach from this initial position to my minimum value of loss such that I'll be taking as.

I'll be taking up an iterative step.

Iterative step, just like the blindfolded man takes in order to reach down the hill.

So this is the overall intuition behind this gradient descent algorithm.

From the theta initial, I'll move towards the theta best in an iterative fashion.

Now how do we actually realize it?

Well, the way that we can realize it is with the help of the gradient.

So what is a gradient?

Gradient is derivative of a function.

So in this scenario of this gradient descent algorithm I'll take a derivative of a loss function.

And once I take the derivative of this loss function, I'll then go ahead and update the parameters

with the help of the gradient.

So in case of gradient descent algorithm, I'll take the gradient of loss function with reference to

each of the parameters that we have got in the machine learning algorithm.

Now, once I take the gradients, I'll be updating the parameters such that the my value of loss will

be minimized.

So the update rule is as follows.

So I'm going to write it like this I'll say w new is equal to w initial minus learning rate multiplied

with grad w.

So what it means is I'm updating the value of this parameter, assuming that w is a parameter.

So I'll be updating the parameter as w new is equal to w initial minus learning rate times del by del

w.

So this is the mathematical representation of this gradient descent approach.

Here del F by del W is actually the grad, which is the gradient component that is derivative of my

loss function with reference to a parameter which we are tuning it.

So this is the overall idea and the intuition behind this gradient descent algorithm.

Now in this case we have something called as a learning rate.

So learning rate describes the step size.

It actually describes the step size.

And it tells us as how big or how small that we are taking the step whenever we are updating the values

of the parameter.

If my learning rate is too high, I might overshoot.

If my learning rate is too low, then at that time there is a very high chance that I will take a lot

of time in order to converge.

So we'll take a look into this learning rate in detail when we talk about the deep learning.

We'll take a look into it in an intuitive way.

So for now, remember that learning rate is something which will decide the step size.

Okay, so what we do in the scenario of this logistic regression algorithm is we will make use of a

log loss function, and on that log loss function, we are going to implement this gradient descent

algorithm such that we move towards the right value of the parameters.

Now that is the core understanding of this logistic regression algorithm.

Okay team.

So now that you have the core and core perspective and the understanding of this gradient descent algorithm,

I'll stop the video over here.

In the next video, let's implement the case of classification on the logistic regression algorithm.

And then let's have a look into the various evaluation functions that we have got for performing the

classification, which is very important for us whenever we are evaluating any classification algorithm.

So I'll see you next time.