# 22. Emsemble Learning XGBoost

---

WEBVTT

In this video, we are going to look into the implementation of the gradient boosting algorithm.

Now there are various good implementations of the gradient boosting algorithm.

So out of this we have got the XGBoost which is one of the commonly used gradient boosting classifier

implementation.

So apart from this we also have a gradient boosting classifier from the scikit learn library.

We have already explored it in the previous session.

See.

Now as we proceed, we will look into the gradient boosting implementation from the XGBoost library,

which is known as Extreme Gradient Boosting, which is the most popular implementation of the gradient

boosting example.

That is the gradient boosting model.

So this XGBoost does not come with Anaconda.

So in order to use it, first we have to do the installation.

The easiest way to do it is by running the command pip install XGBoost.

So this is going to do the necessary installation.

Okay, now during the implementation in the project we will just import it as import XGBoost as Xgb.

Now in order to show you this implementation here, I'm using the same data preprocessing which we have

done for Support Vector Machine.

I'm going to skip the part where we have done the pre-processing on the given data set, and then applied

the label encoder min max scalar on the given data set.

And we have done the pre-processing, and we have also done the resampling of the data.

I'm going to skip that part.

So here once we have prepared the data into two parts.

So I've started by importing the library that is XGBoost as Xgb.

Okay.

Now once we have done the import okay, we would then go ahead and get started.

So this usage of alias as HTTP whenever we are XGBoost is just a convention, just like with any other

popular machine learning packages in Python.

Now, before we can train any XGBoost model we have, we have to wrap our data into the D matrix, which

is a special data structure for finding the splits in an effective manner.

So in order to do the same here, I have mentioned the code as D train is equal to xgb dot d matrix,

and I have mentioned my X train label as my Y train and feature names as x train dot columns.

So for the X train I have mentioned my feature matrix for Y train, which is my target variable and

the feature names.

These are nothing but the names of the features in my X train.

Okay, now once that is done.

So we have done the same thing on our test data as well.

Now once we create this team matrix, the next step is specifying the parameters for training.

Now in this case we are going to use only a small subset of default parameters of XGBoost.

Now we can actually check out the official documentation page of XGBoost and look out the various parameters

that are available for us to fine tune.

Now, in our case, I'm going to set that Xxb params is equal to eta as 0.3.

And since this is based on gradient boost, I'm mentioning the maximum depth as six and minimum child

weight as one.

Objective binary logistic and thread as eight and seed as seven.

silent as one.

So here the most important parameter is the objective.

So this actually specifies the learning task.

Now in this case the task that we are actually doing is a binary classification okay.

We are performing the task of the binary classification.

Since we are performing the task of this binary classification, I am going to mention it as binary

logistic.

Okay, next for training my XGBoost model.

So we will make use of train function.

So I've started with specifying the number of trees as ten.

So xgb dot train, xgb params d train and number of trees as ten.

Okay, So once this is done okay.

So we can go ahead and continue with the see see here I have mentioned my xkb params.

That is the parameters for training d train.

That is the data set for training, which is an instance of the d matrix number of boost round, which

is the number of trees that I want to train.

Okay, now we have got the model.

Now using that model we can also evaluate this.

So in order to evaluate it we have to generate the prediction on the test data.

So Model.predict t test which is the instance of the D matrix.

So once I have generated the prediction if I display the first few entries, this is how the first few

entries of my prediction would look like.

Okay, now with this values of predictions, I can now go ahead and calculate the ROC AUC score.

So in order to calculate the ROC AUC score, racc score, specify the Y test and as well as the white

bread.

So here in this case I'm getting the ROC AUC score as 0.9399, which is 93.99% closer to 94%, which

is quite a good result.

So as you can clearly see when we talk about training the XGBoost model, it's very simple when we see

how its performance changes, when the number of decision tree grows.

Okay, now if I want I can also monitor the performance as well.

So here I've just shown you as how we can perform the performance tuning and the performance monitoring

as well.

So to get the idea as how the AUC changes as the number of trees grows, so we can use the watch list,

which is actually a built in feature in the XGBoost for monitoring the model performance.

So a watch list is a Python list with tuples, and each tuple will contain a d matrix and its name.

So this is how we typically do it.

So watch.

This is equal to d train.

This belongs to the training data D test belongs to my test data.

Now additionally we modify the list of parameters for training.

Here we need to specify the metric that we use it for evaluation.

And in our case I'm going to use the evaluation metric of AUC.

Okay.

Now in order to use this watch list during the training what we have to do is we have to specify two

extra arguments for train function.

So I can directly say model is equal to xb dot train.

Train evals is equal to watch list.

Okay, now if I execute this the XGBoost prints the scores to the output.

So this is how the overall output would look like.

See, as the number of tree grows, the scoring on the training also goes up.

Okay, so obviously this behavior is expected in boosting.

Every next model tries to fix the mistakes from the previous step.

So the scoring is always improving.

Now when it comes to the validation score.

So you also have to check out on the validation score for the validation score.

This may not be the case in every scenario.

Now in this case it would actually ease growing in this case, but not every time this would be the

case.

It may go up initially, but then it starts to decrease.

So this is the effect of overfitting.

So our model becomes more and more complex at that time.

It just when it becomes more complex it just memorizes my training data set, which is not helpful for

generalizing the outcome for the new data.

Okay, so by observing the training score as well as the test score, we will decide the number of parameters

or the best hyperparameters for the given model.

Okay.

That is how we would go ahead and perform the hyperparameter tuning.

So the core idea is when it comes to the XGBoost.

So this has some of the amazing benefits compared to the scikit learn implementations of gradient boosting

classifier or the regressor.

One is the performance metric and the another metric on the.

Another advantage is the ability to fine tune it in an efficient way and the speed boost.

So we'll get a better speed and the performance when it comes to training compared to the scikit learn

implementation.

So these are the key takeaways that you need to remember from this XGBoost algorithm for your machine

learning certification examination from AWS.

So with this we'll come to the end of this video.

So let's continue further in our learning journey in the next video.