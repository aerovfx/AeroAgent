# 21. Ensemble Learning - Boosting Adabost and Gradient Boost

---

WEBVTT

In this video.

Let's continue our learning journey in this ensemble learning.

See in the last video you have learned about the bagging classifier or the bagging technique.

Now, the next type of ensemble learning that we are going to see is the boosting.

So here in the case of boosting, what we do is we train the models in a sequence.

Okay.

That means even in this case I'll be using multiple machine learning algorithms, but I'll be training

my individual machine learning models in a sequence.

So first, on a given data set, let's say this is my data set.

On this data set I now I'll apply a model one.

Now depending on the performance of this model one, wherever this model one is doing the mistake I'm

going to select only those data.

Okay.

Once I select only those data, only that data I'll be selecting right now.

Maybe I'll do some technique of data selection.

Okay I'll perform the data selection.

Once I perform the data selection on this new data I'm going to train a new model.

And then I'll look into the performance of this model to wherever that it has done the error on the

previous data.

it will resample the data set.

And once you perform the read sample, then I'm going to apply the next machine learning model here.

What I'm doing is I'm training my multiple models in a sequence, and all the models should be of the

same family.

Now, one such technique that we'll be doing and we'll be learning right now is AdaBoost technique.

So AdaBoost is also known as the adaptive boosting.

It's a technique under the boosting which comes under the ensemble learning here.

It's called as the adaptive boosting because whenever we are dealing we are going to adjust the weights

we will adjust the weights for the given data set.

Okay, so at the end of each iteration, I'm going to reassign the weights to each instances where I'll

be assigning higher weights to the incorrectly classified instances.

Now this boosting technique is used to reduce the underfit as well as it will also help us in reducing

the overfit of the supervised machine learning.

Here, this adaptive boosting technique works on the principle of learners growing in a sequential manner.

Okay, where I'll be growing in a sequential manner.

So whatever the learnings that I do it on the first model, I'll use that learning and wherever the

mistakes that I'll be doing, I'll assign those roles where the mistakes have happened.

The more weightage though.

So that is the overall idea about this AdaBoost algorithm which comes under the boosting technique.

So the way this AdaBoost technique is going to work is see what it does?

Is this AdaBoost, it is going to make n number of decision trees during the data training period.

Okay, it is going to train or it is going to make n decision trees.

Okay.

Now here as the first decision tree or the model is made, the incorrectly classified records in the

first model is actually given the priority to illustrate the same.

Let me take up a dummy data set.

So here I'm going to open up a new sheet on my Excel.

So let us say I've got two features in this given data set that is x one and x two.

These are the two features that we have got on this data.

Now on this two features what I have got I also have the white room now at the beginning.

Okay, so what I'm going to do is I'm going to assign each row the equal weightage.

So how many number of rows do we have.

We have five rows.

So I'm going to assign one divided by five.

So what is one divided by five.

That is 0.2.

I'll assign the same weightage for each and every row in my data set.

And then based on this importance or the weightage.

Okay, I'm going to perform the sampling.

In this scenario, each and every row is having the same weightage.

So the entire data set will be used for training my model one.

So I'll use my entire data on training my model one okay.

now once I have trained my first model, then what I'm going to do is I'll go ahead and I'll generate

the predictions from the first model.

So let us consider that I've got the predictions from the first model.

So what I'll say is I'm going to call it as y pred m one.

That is my model one.

So let's say this is how the predictions would look like.

So in this case this is an incorrect classification.

I'll highlight in a different color okay.

This is also an incorrect classification.

And this is also an incorrect classification.

Now once I find that these are the three instances where I'm doing the misclassification on this data

set.

What I'll do is I'm going to change the weightage okay.

I'm going to change the weightage.

So here there is a formula that we have got in order to assign the new sample weightage.

The the formula is given by okay.

Old weight okay.

Old weight multiplied with e power performance.

Okay.

E power performance is the formula that we use it.

So what is what is this performance.

So the calculation of the performance okay.

The calculation of the performance is actually given by one by two into where this performance given

by one by two into natural logarithm of one minus total error divided by total error.

Okay.

So this is the formula of calculating the like.

This is a formula for calculating the performance score.

Okay.

Now once I go ahead and find this uh value okay I'm going to update the new weights in this manner.

So old weight into E power performance okay.

That's the formula that I'll be using it in order to update the parameter values.

That is the weight values okay.

So here what we do is for correctly classifying okay.

this is for the formula for the correctly classified.

This is for for correct classified records.

That is, for correct rows.

This is the formula in case of incorrect rows okay.

At that time the formula is given by old weight multiplied with E power minus of performance Okay,

this is the this is how the score would look like.

Okay.

So we'll calculate the value of performance.

And once I calculate the value of performance I'll just go ahead and I'll update the values in this

way.

Now let us suppose that once I do this calculation the new weights that I'm going to get is something

like this.

I'll say new underscore weight is given by.

This is going to result in reduction of the values, that is for the correctly classified rows.

And for the other case I'm going to increase the value of weights okay.

So maybe this is how the new weights would look like.

Now once I've got the new weights.

Now what I'll be doing is for the next model, for the next sequence, the for the next model that I'll

be training, I'll ensure that I'll be using only these rows.

Okay.

I'll be using only these rows for training my next machine learning model M2.

So here what I'm doing is I'm updating the weights after each iteration.

And based on the weightage assignment, I'm deciding as which rows of data should flow to the next model.

So this kind of learning is called as boosting learning.

Or we are training the machine learning model in a sequence.

Okay, that's the core idea about this ad boosting algorithm.

Now when it comes to working.

So this AdaBoost algorithm is available inside the inside the sklearn dot ensemble itself.

So here I've actually create I'm actually creating the plot.

Okay.

This is just the implementation of the same I create.

So if you come below okay so let me just run everything I'll say run all above.

So this is just a function that we have got.

Now here what it says is this is just the implementation of the various support vector machines that

have got.

If you see over here I've, I've implemented the support vector classifier and I'm calculating the weights.

Okay.

Once I've calculated the weights I'm updating the values of weights.

I'll update the values of weights with the same technique that I have mentioned earlier and as and when

that I update it.

So the each and every model, like the iteration that you see right here, each model is improving upon

the previous one.

Okay.

So this is just a visual representation of the implementation.

So to get the better perspective when it comes to implementation.

So we'll generally import this adaboostclassifier.

And we will mention the weak learner.

So in this case decision tree with the depth as one.

And I'm using 30 models which will be trained in sequence.

We'll perform the fit.

And we are trying to visualize the decision boundary.

So this is how the overall decision boundary would look like when we train the.

Uh AdaBoost classifier with Decision tree as the base model.

Okay, this is the overall intuition behind this AdaBoost technique.

Now, apart from this AdaBoost technique, there is one more technique that's called as Gradient Boosting

technique.

Now when we talk about this gradient boosting.

So gradient boosting is going to I would say that it would actually refer to the value of loss of my

previous model.

And it will try to perform the fit on the value of loss to help you with the intuition on the same.

So here I'm importing that.

I'm generating the data set okay.

This is how my x and y would look like.

And I'll perform the fit on this x and y Okay, on the random data that is x and y.

So if I try to show you as how does this data would look like, I'm going to say plt dot plot.

So here this is actually a regression problem.

Hence the plot is not applicable.

See I've just created the pair of x and y.

Okay.

So what I'm trying to do is I'm trying to find the relationship between x and Y.

Okay.

So let's do one thing.

Let's create a scatter plot.

I think that would actually make more sense.

So this is how my data points would look like.

Clearly this is not a linear pattern in the given data set.

So what I'll do is I'll make use of the technique that is the in this case the gradient boosting technique.

And we will try to perform the fit on this given data set.

So here when we talk about this gradient boosting algorithm, what we do is we are going to train multiple

models in a sequence.

In the case of Adagrad, in the case of AdaBoost you have seen that there we will update the values

of weights.

In case of the gradient boosting classifier, we are going to implement on top of the loss function

to illustrate the same.

Here I am training my decision tree classifier and I'll perform the fit on this given data set.

Now, once I perform the fit on the given data set, I'll take the residual.

The residual is nothing but the prediction from the required value.

And then once I take the residual, the next model, I'm going to fit on that residual.

So if you observe the next decision tree regression that I'm training, I'm performing the fit on my

input data and the target value that is here.

The target value is actually the residual.

Now once I do this activity then for the next decision tree I'll again go ahead and find the residual.

Okay.

And again I'll perform the fit on the residual value that is x comma y three.

And we can repeat the same activity okay.

Now if I have any new data what I'll be doing is whatever the predictions that I'm getting it from each

and every model, I'll perform the summation of each and every prediction that I'm getting.

So that summation is going to result in the output of this overall ensemble model.

So here if I have a new data as x new okay.

So if I want to generate the prediction, it's the sum of the predictions that I'm generating from all

the three models.

So this is the overall idea about the generation of the prediction on the new data.

Okay.

Now if you try to visually understand it.

So what happens is see on the left hand side we have the residuals and tree predictions.

So on the right hand side we have the ensemble predictions.

So on the first iteration we just have the data set.

And we have as how the model is actually performing the fit on the next iteration.

What I'll do is I'll take the residual that is the prediction value and the given data.

Once I take the residual, I'll perform the fit on the residual.

See, this is the fit that I performed on the residual.

And if you combine both the models you can see that this pattern is much more better than the previous

algorithm model that I have used to learn this pattern.

Then I'll again take the residuals and perform the fit, and we will include it on the model prediction.

As you can clearly see, by increasing the number of models, I am able to learn the patterns which

are much closer to the existing existing data set.

So this is the importance of the ensemble learning technique, especially the gradient boost classifier

technique.

It builds upon the previous models coming to the practical implementation.

We don't have to do it in this way.

We can directly call the gradient boosting classifier regressor.

In this case, we also have the classifier for your information.

So this is the demonstration of regressor.

So gradient boosting regressor maximum depth as two number of estimators as three will perform the fit

on the data, and then we can go ahead and get the best estimators for the given data set.

Okay, that's how we can actually go ahead and visualize the overall working.

So this is the visualization of like or like if I make use of three estimators how it actually look

like.

And if I make use of 92 estimators, how does the overall estimation would look like?

So for from this video, the key takeaway is when it comes to boosting algorithm, the models are trained

in a sequential manner where one model would improve upon the another model.

In case of ada-boosting, I'll make use of weightage as a criteria, and based on the weightage of the

rows, I'm going to perform the training on the rows where I have done the misclassification in case

of, uh, the gradient boosting.

So what we do is we refer to the value of residual, and then we perform the fit on the residual.

And at the end we take the summation of prediction of all the previous model to generate the required

output.

So that's the core difference between gradient boosting technique and the AdaBoost technique.

Now we'll stop the video over here.

In the next video, we will talk about one of the important technique, specifically from the AWS certification

examination that is XGBoost, which is known as Extreme Gradient Boosting.

So we'll talk about its implementation in the next video.