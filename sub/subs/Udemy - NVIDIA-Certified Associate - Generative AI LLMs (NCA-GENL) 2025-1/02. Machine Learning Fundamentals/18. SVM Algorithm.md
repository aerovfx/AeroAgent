# 18. SVM Algorithm

---

WEBVTT

The next algorithm that we'll be learning today is the Support vector machine.

Now, this support vector machine is one of the powerful supervised machine learning algorithm.

And it actually works really well whenever we are working on a very small data set.

And along with that on on the complex data set as well.

So SVM is also abbreviated as Support Vector Machine.

And this model we can use it for both classification as well.

As we can use this for regression.

So that means we can use this algorithm for both classification and the regression task.

But generally they actually work best for the classification problem.

And they were actually very famous around the time they were actually created, especially during the

year of 1990s.

So this support vector machine was one of the go to method for performing the classification in an efficient

way.

And this was one of the high performing algorithm with little tuning at that time.

Okay.

This is the overall background of this support vector machines.

Now going further.

So let's understand what exactly is this technique.

See here when we talk about the support vector machine.

It's a supervised machine learning where we are going to learn, okay, where we are going to learn

and where we will find the hyperplane.

I'll say find hyperplane that best separates the two classes okay.

That is the overall idea of the support vector machine.

It will help us to find the hyperplane that best separates the two classes.

Now here when we talk about this hyperplane is like you might be confused about the logistic regression

and the support vector machine.

So please don't get confused here.

The algorithm tries to find the best hyperplane in case of SVM, but the main difference is the logistic

regression algorithm that we have got.

It is based on the probabilistic approach.

But here when we talk about the support vector machine, the approach is statistical in nature.

Now the question is how does it finds the hyperplane.

Because whenever we take up any given data set okay.

So if I take any given data set let's say this belongs to one class X belongs to one class okay and

circle belongs to another class.

If I take the these two classes in this given data, I can draw any type of hyperplane.

I can have infinite amount of number planes.

Now how can this algorithm finds okay, this is the hyperplane which is suitable for this given data.

Well what this SM is going to do is the SM actually finds the best hyperplane by finding the maximum

margin between the hyperplanes.

That means the maximum distance between the two classes.

So it will find the hyperplane that best separates the two classes and it will consider the distance

metric.

Okay.

Now when we talk about the support vector machine, this support vector machine, they are basically

classified into two types of support vector machines.

One is a linear support vector machine which is also known as linear SVM.

The other one is called as non linear SVM.

So here when we talk about this linear SVM, whenever we are dealing with the data which is perfectly

linearly separable, then we can use the linear SVM.

So the perfectly linearly separable means that data points can be classified into two classes by using

a straight line, in case if it is a 2D, or it could be a 3D hyperplane, and so on, but it should

be linear in nature.

That means it has to represent the straight format.

So that's when we'll be using this linear SVM.

And we have the nonlinear SVM.

Now when my data points is not linearly separable then we can actually use the nonlinear SVM, which

means when the data points cannot be separated into two classes using a straight line.

To give you an example, if I have a data points like this okay, I can separate these two classes with

the help of a straight line At that time.

The linear SVM is actually suitable for this kind of data points.

Now, if you take the another case, let's say this is one set of data points.

And surrounding this set data points I have the data points from the other classes.

Now whenever I have got this kind of pattern, the decision boundary is like in the format of a circle.

So in that case we will make use of non-linear SVM algorithm okay.

Here we mean that when we talk about this non-linear SVM algorithm we basically make use of the kernels.

So these are called as the kernel tricks which will help us to classify the non-linearly separable data.

In most real world application we do not find linearly separable data points.

Hence, we generally make use of this kernel trick.

In order to solve this non-linear data points and find the patterns between the input and the output.

Okay, so this is the overall idea about this types of the support vector machines.

Now moving further, now that you have understood about the types of the support vector machine, now

let's understand the important terms that are generally involved whenever we talk about the support

vector machine algorithm.

So the first terminology which you need to be aware when we talk about the support vector machines is

support vectors.

So here when I say support vectors support vectors are the data points okay.

These are all the data points that are closest to the hyperplane.

That is what a support vectors are.

These are nothing but the data points that are closest to the hyperplane.

Now a separating line will be defined with the help of these data points okay.

And then there's another common terminology that you would generally come across when we talk about

the support vectors is the margin.

So what exactly is this margin.

So margin is nothing but the distance between the hyperplane and the support vectors.

This is the distance between a hyperplane and support vectors.

Now here in case of SVM the large margin is actually considered as a good margin.

And there are actually two types of margin.

So we actually refer it as hard margin and soft margin okay.

These are the two types of margin that we have got.

Okay.

We'll talk about it as we progress in this video.

And then the another terminology that you would come across is the like the like the let me just the

other terminology that you would generally come across is the positive hyperplane and the negative hyperplane.

Okay.

So let's do one thing.

Let me show you a visual representation about these terms that you would generally come across.

See suppose if I have got the data points which look like this.

This is one class and this is another class of data points Okay.

And now I'll assume that this is the hyperplane okay.

This is the hyperplane that have created.

Then the nearest data points to the hyperplane is actually the support vectors.

So these data points okay.

These data points are actually my support vectors.

That is the nearest points to my hyperplane and this one is actually my hyperplane.

Okay, so here the margin.

Okay, the margin is nothing but the distance between the hyperplane and the observations closest to

the hyperplane.

So that is this one and this one.

Both are the margin.

Okay.

This is the margin.

And these two data points are support vectors.

So in this way we are going to refer to the terms that we have got whenever we are making use of the

support vector machine algorithm okay.

Now the question is how does this algorithm actually going to work.

See this SVM is defined such that it is defined in terms of support vectors only.

We don't have to worry about the other observations, because the margin is made using the points which

are closest to the hyperplane.

That is my support vectors.

Whereas if you consider the other algorithms like the logistic regression at that time, this classifier

is defined for all the data points.

So as a result of this, compared to the other algorithms, the support vector machine is computationally

faster compared to the algorithms like the logistic regression.

Now let's understand this.

Overall working with the help of a simple example.

Let us consider.

We've got our data points that would look like this.

Okay, this is how the data points would look like.

I've got the set of data points over here okay.

Now on this set of data points okay.

So I want to classify this.

Data points are whether it belongs to the blue class or whether it belongs to the green class.

So I've got the data points belonging to the two classes now to classify these data points in an efficient

way, I can actually draw the hyperplane as per my requirement.

So I can draw a hyperplane like this.

This is one hyperplane or I can draw another hyperplane like this, or I can draw a different hyperplane.

So see as you can clearly see I can draw the hyperplane as per my preference and as per my requirement.

I mean there is no restriction as such because all these hyperplanes are actually differentiating these

data points of these two classes in an efficient way.

Now the question is which hyperplane is best?

The question is which one is best.

That is the question.

Well, the way we decide on the hyperplane, which is an optimal for this given data set is.

So what we will do is for the given set of data points we are going to draw a hyperplane okay.

We are going to we are going to select the hyperplane for the given set of data points such that it

separates the data points in an efficient way.

Okay, it will separate the data points in an efficient way.

So to illustrate the same, this is how I'm going to represent.

So in this.

So in this example this is the best hyperplane.

Now if this is my hyperplane or the optimal hyperplane I'll be constructing this.

Or I'll be seeing this as an hyperplane or an optimal hyperplane such that it maximizes the distance

between the support vectors and the hyperplane.

Distance between the support vectors and my hyperplane that have got.

Okay, that is the core principle over here.

So in this scenario, this line that you see right here, let me do one thing.

Let me color it in a red color.

So the red color line is our hyperplane okay.

And these two lines are the ones that we have got.

And the core idea is I have to maximize the margin okay.

I have to maximize the margin between the between the support vectors that we have got.

So this is the overall idea in which we would select the best hyperplane for the given set of data points

Okay, so here in the case of support vector machine.

So I have mentioned that there are two types of classifier.

One is hard classifier and a soft classifier.

So the example that I've just shown you right now this is called as a hard classifier.

So in case of hard classifier we do not allow any data points to lie in this margin region.

Okay, so that is something that we always have to remember in case of hard classifier, we do not allow

the data points in the margin region.

In case of soft classifier.

So we will allow the data points or I would say misclassification or misclassified data points in the

margin region.

So these are the two types of classifiers that we have got in case of support vector machine.

And there is also a mathematical intuition behind it where it makes use of dot product and finds the

best hyperparameters, which is out of scope of our learning journey.

I would recommend you to check out it as per your preference.

If you prefer to learn more about the Support Vector machine for now, you need to remember that in

case of Support Vector machines, it will try to find a hyperplane to classify the data points.

And this is the core idea of this linear SVM.

Now what happens in the scenario of the non-linear SVM.

So what we do is in the scenario of this non-linear SVM we will transform the data okay.

We will transform the data with the help of the various polynomial kernels or the kernel equation.

Some of the way some of the other kernel equations or the functions that we will be using it in this,

in the case of the support vector machines are one is a polynomial kernel that is one of the most commonly

used one.

Polynomial kernel.

Okay.

And apart from that we have something called as sigmoid kernel.

And there is another kernel that's called as Gaussian RBF kernel.

Okay.

These are and if I want to name few more okay.

There is something called as Bessel function kernel and Anova kernel.

So these are the various uh, kernels that are available for us, where what we do is for the given

set of data set, we will pass the data to this polynomial functions.

Okay.

That is for the polynomial kernel, depending on the kernel that we will be using, we will also have

to fine tune the hyperparameters as well.

Then by applying the by applying the data to with the help of this polynomial kernels, we will be transforming

the data.

And on top of this we will try to draw a hyperplane.

That is the core principle of using this various kernel functions.

Okay, so this was about the core intuition behind the support vector machines model.

Now that you have a core intuition behind it, let's go ahead and implement it on a given data set.

See, as part of our working okay, we'll be making use of a data set that is income evaluation data

set.

And I've already shared the CSV file over here.

That is income evaluation dot csv file.

So I'll start by importing the necessary libraries.

So import pandas as pd, import numpy as np and I'll also import matplotlib.

And then we'll also import the seaborn library.

Okay.

Now once I've done the import I'll start by reading the data as a dataframe pd dot read underscore csv

and the file is income evaluation dot csv.

And if I display the first few entries of this data frame, this is how the first few entries of this

data frame would look like.

We've got various column age, working class, and education, and these are the various columns that

we have got on this data set.

And we also have an income.

Now this income column, if you look into it it is less than 50 K.

And let's do one thing.

Let's look into the unique values that have got in this income column.

DF income and I'll say value underscore counts.

There's an additional space in the beginning.

Okay.

So here less than or equal to 50 K there is 24,720 rows.

So these are the two unique values that we have got on this income column.

And we can also go ahead and check out the unique values that we have got on marital status.

Okay.

Yeah, we have got marital status.

If I look into those unique values.

So these are the unique values that we have got.

Okay.

So what we'll do is we'll just go ahead and remove the white spaces that we have got in this data.

Okay I'll say call underscore names is equal to DF dot columns.

Then I'm going to write it like this.

Okay I'll say for v in call underscore names.

What I'll do is I'll just say v dot strip.

This will help me to remove the white spaces.

Okay, I removed the white spaces in this given data set.

Okay.

If I want, I can also go ahead and adjust anything as per my requirement.

For now, I'll keep it as it is and we'll just proceed further, folks.

So one thing that I can observe from this given data set is this column that is flagged.

So this is the column which is not required because it is just representing, I would say an ID over

here.

So we can drop that column.

So first thing is I'll just rename the column.

So DF dot columns is equal to column names okay.

Then I'll say df dot drop columns is equal to the column name that I want to remove.

Is this in place is equal to.

Then we'll display first few entries of this data frame.

Okay.

This is how the first few entries of this data frame would look like.

And we have fixed the column names as well.

Now let's check for any missing values in this data set df dot Isnull dot sum.

So we can see that there are no missing values in this given data set.

So we'll start by performing the pre-processing on this given data set.

So what I'm going to do is so for the column that we have got let's say the age column okay.

For this age column we have the values.

That is the that is the integer values that we have got.

Now what we will do is we will convert these integer values into a categorical format.

Now, the way that I'll be doing is I'll perform the binning.

So I'll create the binning something like this I'll say 16, 24, 64 and 90.

Okay.

These are the bins that I'm going to create.

And for each of these bins I'm going to mention as young adult Okay, then I'll say old.

Okay.

Then I'm going to say age types.

That's the new column that I'm going to create.

And here I'll perform the activity of using the function PD dot cut.

And I'll apply it on the column of age.

And here we'll specify the bins as bins that we have created earlier.

And labels is equal to labels.

And then I'm going to mention DF income okay I'll say df income num is equal to NP dot where okay.

So I'll say df income equal to.

the values is greater than 50 k.

Okay.

If yes, one else zero.

Okay.

And I'll mention it as as type 60.

Okay.

So what I'm trying to do is I'm just trying to add a new column called Income Numbers where I have converted

these values, that is, string values into a categorical format.

That means we have just performed the pre-processing on the target column.

And we have also added a new column called Age Type, where we have converted the numerical values to

the string format.

Now, apart from dropping the uh columns which are not required, I have done further pre-processing

on this given dataset and implemented the code.

So I've checked for missing values, and I've also performed the binning and variable.

We had the question mark.

I have replaced it with the respective values right here okay.

Now once that is done.

So I have applied some of the min max scaler on the numerical columns.

And apart from that we have like perform the activity of imbalance that is balancing the data set.

So oversampling for handle handling the imbalanced data.

And once that is done we have split the data into two parts.

Then we have implemented our SVC algorithm.

And during the evaluation this time I have made use of this cross validation method.

So we have calculated the cross validation score for this given model with cross validation that is,

the value of k as five.

And at the end.

If you look into this scenario, the overall accuracy that I have got on the test data is 0.83.

That is, 83% is the accuracy that I have got from this support vector machines.

Okay.

The model that I have used is the SVC.

That is the support vector classifier.

Now under this SVC here we can actually specify the various kernel.

So under the documentation you can refer that by default the kernel applied is RBF.

If you want we can specify the polynomial sigmoid.

And we can also specify the value of c which is a regularization parameter gamma.

So these are the various hyper parameters which we can tune it by specifying the range.

Okay.

So this was about the implementation of support vector machine algorithm for a given data set.

The key takeaway from this algorithm is we are going to generate a hyperplane which separates the data

points in a maximum manner.

Okay.

So I'll see you in the next video.