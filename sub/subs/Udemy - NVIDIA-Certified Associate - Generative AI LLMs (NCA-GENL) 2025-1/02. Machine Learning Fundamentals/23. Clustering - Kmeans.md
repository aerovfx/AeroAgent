# 23. Clustering - Kmeans

---

WEBVTT

Welcome back to your learning journey in this AWS Machine Learning Certification Examination preparation.

Now in this video we will talk about the unsupervised learning and more specifically the tasks that

we would perform today is the clustering task.

Now first thing is what exactly is this clustering.

So clustering means grouping the objects or the data points based on the information that is found in

the data which describes the information or the relationship about them.

So the goal is that the objects in one group will be similar to each other, and the objects between

the different groups will be as much dissimilar as possible.

Technically speaking, we would say that the objects in the same cluster will have the same properties

we call it as internal cohesion.

Okay.

We call it as internal cohesion.

And not only that, not only just that, we also expect that the data points from different clusters

should be as much separated as possible, which means we are looking at external separation.

Okay.

Internal cohesion and the external separation is what we would expect whenever we create the cluster

or whenever we group the data points.

Now that you have the basic idea about this clustering, why do you think that we would require a clustering?

Well, think about it.

Let's say you are working in a marketing department of a company, and let's say you have the data points

that you have collected.

And this is how the your data points would look like.

Okay, so on the x axis you have the salary.

And on the y axis you have on the x axis you have age.

On the y axis you have the salary.

Now let us assume that you are working in a credit card department and in your bank.

You are launching a new credit card which is a premium credit card.

Now since it's a premium credit card, when you are planning to send out any marketing communication

about this new card, the apt people who will be suitable for this credit card will be somewhere.

The people in this group, the people in this group will be the one who is suitable for the new credit

card that you are planning to launch, which is a premium credit card.

Where age is less, salary is more.

Now those are the right set of people who are actually suitable for the card that you are offering.

So what we will do is we can create the groups in the given data points.

Once we create the groups, I can say that this is the group which is most suitable for the marketing

activity.

So that is the usage of clustering in real time.

Okay.

From the given set of data points itself, we will find the similarities between the data points.

Now, in order to find the similarity between the data points, what we do is we will make use of the

distance metric.

Using the distance metric, we will find the similarity between one data point to the another data point.

And then we will decide as to which group it would belong to.

So basically we will look into my existing data points and find the similarities between them.

One of the common distance metric that will be used in the case of clustering is Euclidean distance.

Okay.

Euclidean distance.

So here in this case of clustering, we are planning to find the groups in the data set by looking into

the like, by looking into my data points itself.

Now, when it comes to clustering we basically have three types of clustering.

So the first type we call it as exclusive clustering.

Okay.

We call it as an exclusive clustering.

Now in this case of exclusive clustering here an item or an object or a data point belongs to exclusively

to only one cluster.

Okay.

So here the example for this exclusive clustering is k means clustering algorithm okay.

K means clustering algorithm is an example for exclusive clustering.

And when we talk about the other type so the other type that we have got actually known as the overlapping

clustering.

So in the case of overlapping clustering here I'll be interested in establishing the relationship to

multiple clusters for an individual data point.

So here the example for this overlapping clustering is fuzzy or C means clustering Okay, this is an

example algorithm for overlapping clustering.

And apart from that we also have another kind of clustering which is called as hierarchical clustering.

Here we are going to establish a parent child relationship between the data points the data set okay.

So these are the three types of clustering that we have when we talk about the clustering task.

So let's start by working with the most commonly used clustering algorithm that is k means clustering

algorithm.

So here when we talk about this k means clustering algorithm.

This comes under the exclusive clustering.

So in the case of k means clustering algorithm these are the steps that we follow in the case of k means

K-means clustering algorithm.

So what we do is we'll start by defining the number of clusters.

And then what we do is once we define the number of clusters that is k clusters, the k is nothing but

an integer value.

Once we decide on the number of clusters that I want to create on the data set, next I'm going to create

k points at a random.

That means I'll random and I'll generate five clusters or k clusters like if k is equal to five.

Now once I generate the clusters, I'll compute new centroid of each cluster by taking the mean of the

data points.

And this is followed by assessing the quality of each cluster.

And we'll repeat this activity multiple times to help you with understanding of this K-means clustering

algorithm, I want you to observe this example.

See, I have the value over here.

Now in this given set of data points.

Okay, there are three clusters.

And as you can clearly see the green circle here and the purple that you see over there.

These are the individual values, okay.

These are the individual values that we have got.

So what it means is let me just take an example.

So here let me stop the animation to help you get a better picture better picture on this.

So here this is my initialization.

So for the given set of data points in this case I have initialized the values of cluster.

So this is one cluster okay.

The one that I have highlighted that is one cluster.

Now apart from that.

So there is a red data point that you see over there okay.

So in that case.

So this was the initial diagram that we were seeing earlier.

Okay.

So in the initial diagram.

So you have practically seen how does the first cluster would look like.

This is the first cluster green.

And the another cluster that I have considered is red one.

And this is another cluster.

So in this case I have began by saying that I would require three clusters.

So it started by initializing three different clusters or the three different cluster centroid.

These are the cluster centroid.

Now once I have initialized this three different cluster centroid I'll find the distance between each

and every data point in my data set to each of the cluster centers.

That is the centroid.

Whichever is the one that is nearest to that cluster color has been assigned for the data points.

So that is what has happened in this case.

So as a result of this, all these data points belongs to green cluster because it is nearest to my

green cluster centroid.

And all this data points belongs to the red cluster.

And all these data points belongs to the blue cluster.

Now one thing that you can observe right now is I basically have an single cluster assignment for each

of the data point, Okay, for each data point, there is only a single cluster assignment.

Hence we call it as exclusive clustering.

Okay, now that this is clear.

Now what happens once I perform this activity?

See, once I perform this activity, the next thing that I'll do is I'll calculate mean of the data

points belonging to that cluster.

I'll calculate the mean of this data points.

Let's assume that once I calculated the average, that is the mean of the data points.

This is where my cluster center will be.

So I'll move this cluster to this position okay.

To get a better understanding on this, let us go ahead and observe in the diagram that you see right

here.

So now on the left hand side we have the second iteration.

See, what has happened was my green cluster was over here.

After it has found the mean it has shifted the mean or it has shifted my.

And once it shifts, it has also done the similar activity for the red cluster as well as the blue cluster.

And then it will again find the distance between each and every data point.

So it will again find the distance between each and every data point to the all the three clusters,

whichever is the cluster that is nearest to it.

That's the cluster color that I have actually assigned.

And this process repeats in each and every iteration.

And we repeat this process until there is no further changes in the data point assignment.

So this is the overall idea of this K-means clustering algorithm.

Here k refers to the integer value, that is, the number of clusters that we want to create for a given

data set.

Okay.

The term means is used because at each iteration I'm calculating the mean of the data points.

And this mean of the data points is the new cluster centroid for the given data set Okay.

So at the initial at the initial part it is our responsibility always to decide on the value of k.

Okay.

It is our responsibility to decide on the value of k based on our decision.

So the clustering will happen on a given data set.

Okay.

So this is the overall idea about this K-means clustering algorithm.

Now here, in order to perform this algorithm and get the value of k, what we do is we will make use

of uh, uh, of an attribute that's actually called as an inertia.

Okay.

We make use of what we call it as an inertia.

Now inertia talks about the internal cohesion or this is also known as sum of squared error.

So what it says is it is going to calculate the distance between each and every data point to its cluster

center.

It is going to square it.

And if we perform the sum of all those distances we are going to get the sum of squared error.

So this is the overall idea about this inertia.

So in order to decide on the value of k, we are going to create a plot where on the y axis we will

have the inertia.

On the x axis we will have the values of k, and from there we are going to identify whether we find

any elbow like structure.

Based on that, we will decide what should be the value of k for the given data set.

As I increase the value of k, it is going to decrease the value of inertia.

So we have to balance out by looking into the business problem and also the given data set to decide

on the values of k.

Okay.

That is the overall idea about this inertia.

Now apart from this inertia okay.

The another metric that we use it in order to decide on the number of clusters for the given data set

is actually seal Haulouts.

Seal out score.

So seal out score.

It actually talks about the external separation.

So higher the value of out score, the better is the external separation that we have got for the clusters.

By considering inertia and seal out score, we will decide the number of k which is suitable for a given

data set, and then once we perform the clustering, we will then go ahead and interpret those clusters

as per the business problem.

So this is how we are going to implement this K-means clustering algorithm.

Now that you have a high level idea as the core idea about this K-means clustering algorithm, let us

go ahead and implement it in a hands on manner.

So I'm now in my Jupyter notebook.

So let's start by importing the necessary libraries here.

I'll start by importing my pandas library and numpy library as well as the Seaborn library.

So here the data set that I'll be using is a data set from the UCI Machine Learning.

So the CSV file is wholesale customers data dot CSV.

So that's the CSV file which will be referring right now.

So I'll start by reading this data as a data frame.

And let's have a look into the data set.

So data is equal to PD dot wholesale customers data dot CSV and this frame.

So this is how the first few entries of this data frame would look like.

So the overall aim of this problem is to segment the clients of wholesale distributor based on their

annual spending on various product categories like milk, grocery, region, and so on.

Okay, that is the overall expectation.

Now let's go ahead and let's look into the statistics of this given data.

I'll just begin by describing this given data frame.

So this is how the overall output would look like.

The count mean standard deviation, minimum maximum value 25% l50 percentile, and so on.

So one thing is for sure, by looking into the various columns, there's a lot of variation in the magnitude

of the data.

So if you look into the column like channel it has got the minimum as one and the maximum as two.

And if you look into the column of region, minimum is one, maximum is three.

But if you look into the other column let's say detergents paper minimum is three and maximum is 40827.

So which means we have got different different magnitudes for each of the column.

Now since this K-means clustering algorithm is a distance based algorithm, this difference in magnitude

can actually create a problem.

So let's first bring all the variables to the same magnitude.

So in order to do that, one of the common way of pre-processing this numerical variables is by the

application of the standard scaler.

So let's start by importing it from sklearn dot pre-processing I'm going to import Standardscaler.

Then I'm going to initialize my Standardscaler.

Okay.

Once I initialize my standardscaler I'll perform the scaling of the data set.

So data scale is equal to ss dot fit underscore transform okay.

And I'll apply the transformation on my data set okay.

Now once that is done, let us go ahead and perform this activity of describing this scale data.

So I'll say data scale is equal to PD dot data frame okay.

Data scale and columns is equal to data dot columns.

Data scale dot describe.

So once we perform this activity of scaling, we can now see that all the columns in my data set now

has the mean equal to zero and standard deviation of one.

So we have actually bought down all the data set having the same number of same value of mean and the

same standard deviation okay.

That's how we generally do the scaling.

Now that we have scaled the data we can now go ahead and apply this k means clustering algorithm.

Now in order to apply this k means clustering algorithm.

First we'll have to import the same from sklearn dot cluster I'm going to import k means okay I will

start by initializing my k K-means algo.

And here we have to specify the hyperparameter the number of cluster from our side.

I'll say number of clusters is equal to two.

Then we'll perform the fit on our given data set.

Once we perform the fit okay we can then go ahead and generate the prediction.

But before I do that okay.

Before I do that, let's see how the system has actually performed the clustering.

And in order to evaluate the clustering, we can actually make use of inertia.

Now inertia talks about the internal cohesion.

Now in this case the inertia value is 2599.385 okay.

Which is closer to 2600.

Now in order to determine the number of clusters so there is no automated method, they we have to make

use of manual method.

And the manual method that we use it is the elbow plot.

So to decide on the value of k we'll create this plot by considering the various values of cluster.

And from there we are going to decide on the value of k by looking into the elbow plot.

So let me just go ahead and do that.

I'll say Z as a blank Python list.

Okay.

For cluster in range of two comma 20 okay.

From two comma 20 I'm going to initialize my object of k means and number of clusters to cluster.

Okay.

then we'll perform the fit on the given data set that once we perform the fit, we will go ahead and

append the value of inertia on each on each iteration.

Now, once this is done, we will go ahead and create it as a data frame.

Okay I'll say LB is equal to PD dot data frame.

And for this data frame I'm going to mention it in this way.

Cluster range 2 to 20.

Okay.

Then I'll create it as SSC.

And this is equal to SS.

Okay.

So in this way I'll do the assignment.

Next I'll define my figure region region.

So fig size is equal to.

Let's consider uh ten comma five.

Then plt dot plot along the x axis I'll have the out cluster okay.

And along the y axis I'll have out ss e okay.

Now we can also define the marker.

I'll use the marker as oh okay then plt dot x label.

So along the x axis it is actually the cluster.

So this is the number of clusters and plt dot y label.

So here it is the inertia field dot show if I execute this okay.

Yeah.

This is how my elbow plot would look like.

Now let's do one thing.

Let me just go ahead and add the x ticks as well.

So plt dot x ticks okay.

We'll consider range of two two comma 20 okay.

So this is how the overall plot would look like.

So in this case okay one thing is pretty clear.

There is no clear elbow like structure.

Like after five till eight.

Okay, we see a kind of a sudden drop in sudden dip in this given scenario, but it's not pretty much

clear for us.

It's not clearly visible to us as which one is actually suitable.

I can choose any value from 5 to 10.

So what we will do is in in the cases where the elbow is not clear, we will take the second opinion.

The second opinion is with the help of the sellout score.

So let's go ahead and calculate the sellout score for each of the cluster number okay.

In order to calculate the sellout score, I'll copy this code and I'll paste it below.

And we'll modify a few things here from sklearn dot metrics.

I'm going to import sell out score.

Okay.

Now this time I'm going to say.

Out score okay.

So we'll replace this with sell out score.

Now what we will do is this time in order to calculate the sell out score.

Yeah.

Let's say I'll just mention it as score.

Okay.

That will be an easier one.

Score dot append here.

Inside.

Once we have performed the fit in order to calculate the sell out score, what we need to do is if you

observe the documentation.

So the documentation says the x and the label.

That means I have to mention my data that is data scaled.

And the label.

So what is the labels.

So column dot labels.

Underscore.

So that is that is going to return me the labels that has been selected okay.

Next we'll create a plot okay.

I'll just call it a score.

And will create a plot.

So this time on the Y label, it is actually a sellout score.

So let's execute it.

So we now have the plot of the sellout score.

So here, if we observe when the value of K is three at that time, the sellout score is high.

But if you observe when the value of k is three, the inertia is pretty much high.

Let's do one thing.

Let's see if we have any better value.

Greater than five okay.

Greater than five.

So what do we have the value of k where this allowed score is also high.

So if it is greater than five I can choose the value of k as seven okay.

So when the value of k is seven this will outscore also seems to be high.

So I can now say that by the combination of silhouette score and the inertia, we are going to decide

that in this case.

Okay.

So I'll choose the number of clusters on this given data set as seven.

Because by doing like this, we will have a better external separation as well as the better internal

cohesion.

So I'll initialize this.

Then we will perform the fit on the data set.

Okay.

Then what I'm going to do is on my data set that I have got.

So I'll say data that we have got I'm going to call the I'm going to create a new column called cluster

and I'll say column dot labels underscore okay.

Now if I go ahead and display the value counts.

So this is how the distribution of the clusters would look like for the cluster zero ten rows are present

for cluster 239 rows.

So in this way we can look into the distribution of the clusters.

Okay, so to decide on the number of clusters on the given data, we basically consider the inertia

as well as the silhouette score.

Along with that we also have to consider the business case.

So by combination of all these things we are going to decide as what is the number of clusters that

is suitable for the given data.

Okay.

So this was about the k means clustering algorithm.

So team with this we come to the end of this session.

Now we'll continue further in our learning journey in the next video where we are going to learn about

the hierarchical clustering.

So I'll see you next time.