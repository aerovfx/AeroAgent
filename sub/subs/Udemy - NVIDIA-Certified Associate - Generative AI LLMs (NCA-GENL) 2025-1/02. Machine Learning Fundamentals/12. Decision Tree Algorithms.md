# 12. Decision Tree Algorithms

---

WEBVTT

Welcome back to your learning journey in this AWS Machine Learning Certification.

Now in this video we are going to talk about this decision tree algorithm.

Now here when we talk about a decision tree, it's a algorithm which is going to ask several questions

on the given data set.

And based on the answer to that question, it's going to split the data into subparts.

So that is the core idea of this decision tree algorithm.

Now in order to illustrate the working of this decision tree algorithm, let's take up a scenario.

Let's say that you are going to decide whether you want to go to a restaurant, or whether you want

to actually go for home.

So here in that scenario, so what you will do is first you will check whether you are hungry or not.

Okay.

And then once you have decided that or once you are sure that you are actually hungry, then you will

check whether you have enough cash with you to go there to the restaurant.

Now let's suppose that if you have more than ₹1,000, then you will decide that you will go to the Novotel.

Now, if you do not have ₹1,000, you'll go to the south kitchen.

Now let's say if you are not hungry anymore, maybe you'll just simply go home and sleep.

So, as you can clearly see in the case of this decision tree algorithm, what you are trying to do

is at each and every step for a given situation, for a given training data, you're going to ask the

question, okay, now by asking the question, you are going to depending on that answer to that question,

you will take the you will split the data set into the multiple parts.

So this is the core principle behind this decision tree algorithm.

Now this decision tree algorithm practically speaking it comes under the topic of supervised learning.

That means in this case of decision tree algorithm we actually make use of the x as well as Y while

finding the pattern in my given data set.

So it comes under supervised learning.

And here this decision tree algorithm can be applied for both regression as well as the classification

problem as well.

Now the goal of this decision tree algorithm is to create a training model that can predict the class

or a value of a target variable by learning the simple decision rules.

By learning these simple decision rules that you see right here, which has been inferred from the training

data set.

Now, in case of decision tree, in order to predict the class label for a record, what we do is we

generally start with the root node okay.

We generally start with the root node of the tree, and we compare the values of the root attribute

with the records attribute.

On the basis of this comparison, I'm going to follow the branch which corresponds to that value and

jump to the next node okay.

And we repeat the process until we reach the end of the decision tree.

So this is the overall way in which that we are going to work with this decision tree algorithm.

So it's a simple representation for classifying the examples.

It comes under supervised machine learning where the data is continuously split according to a certain

parameter.

Now when we talk about a decision tree, each and every decision tree that we are going to construct,

it will have the node.

So node is nothing, but it's a test for the specific value of a certain attribute.

And then we will have edges or branches.

Now edges or branches correspond to the outcome of a test which connects to the next leaf or a node.

And we also have a leaf node.

Now leaf node is nothing but the end node or the terminal node which predicts the outcome.

That means where it represents the class label or the class distribution.

Okay, this is the parts of a decision tree algorithm.

Now.

Now to give you another example, let's say you want to decide whether a person is fit or not.

So in that case you will check his age, whether his age is less than 30.

If his age is less than 30, you will ask one more question that says whether he eats lots of pizzas

or not.

If yes, he's unfit, if no, he is fit.

If the person's age is more than 30, whether he exercises in the morning or not.

Yes.

He's fit.

No, he is unfit.

So, as you can clearly see, in order to classify any person, you are asking the questions based on

the answer to the question, you are deciding whether that person is actually fit or an unfit.

Now this decision that is classifying the fit or an unfit is called as a leaf node.

And on the top we have the root node which receives my entire data set.

Now these nodes that you see right here, these are called as nodes or a sub node okay.

And the lines that you see these are called as the edges or the branches okay.

So these are the parts of a decision tree algorithm.

So when we construct a decision tree we'll have a root node.

We'll have leaf nodes.

And we also have the branches that are connecting each nodes or the leaf node.

So when we talk about this decision tree the depending on the type of target variable that we have got

it can be of two types.

It can be the classification trees where we have got the categorical target variable, then at that

time the target and the and the overall tree that we call it as a classifier decision tree.

Now if the target variable is actually continuous, then we call it as a continuous variable decision

trees where we have got the continuous target variable okay.

Now to give you an example, let's say we've got a problem to predict whether the customer is going

to pay his renewal premium or not.

So that means it is like a yes or no problem.

Then in such cases we call this as a classification tree.

Or in other case, if I want to predict the income of a person based on the other characteristics like

occupation, product and other variables, then at that time I'll be trying to determine a continuous

value.

Then we call this as a regression decision tree okay.

So these are the two types of decision tree that we generally come across.

Now when we talk about this decision tree see on top we have got the root node.

Now this root node represents the entire population or the sample and this root node or the entire population

of the sample is going to further gets divided into two or more homogeneous sets.

Now this acting of dividing the data into the sub parts is actually called as splitting.

And then you also see a decision node.

Now decision node is when a sub node splits into further sub nodes.

Then we call it as a decision node, and at the end we have something called as leaf node.

So leaf node or a terminal node where these are the nodes which do not further split.

That means where the split is going to end.

That is actually called as a the leaf or a terminal node.

Okay.

And apart from that you might also hear something called as a parent and child node and so on, which

actually means the top one is actually called as a parent node.

And the bottom one we call it as a the child node.

Okay.

This is the overall way in which that we construct a decision tree model.

And we try to generate the pattern by looking at our training data set.

Now whenever we are implementing this decision tree algorithm, the technique that we apply is a divide

and conquer technique.

So what happens in this scenario is we are going to consider the entire training data set for the root

node.

And then we are going to ensure that we are going to create the branch for each possible outcome of

the test.

Then we will split the instances into subsets, one for each branch extending from that particular node,

and we repeat this process recursively for each branch until only instances that reach that particular

branch, using only the instances that reach that particular branch.

Until we, we have all the instances have the same class.

So this is the overall idea about this decision tree algorithm.

So here we'll start with complete training data in the root node.

And whenever we are generating the split.

So just like we are going to use the loss function while performing the fit on the linear regression

and the logistic regression algorithm.

In the same way, in case of decision tree, we also have to specify the loss function.

It can be Gini impurity index or the entropy.

So we can use either of them when we are dealing with the task of classification.

And then we can search for the variable which minimizes the impurity.

That means we will select the column which will help me to reduce the value of error.

And we'll repeat this.

Step two for each and every subset of the data set until all the dependent variables are exhausted and

until the stopping criteria are met.

And finally, we are going to generate the business rules for the leaf.

So these are these are the steps that we generally follow whenever we are generating a decision tree.

Okay.

So now that you have the clear understanding of this decision tree.

So as I mentioned, there are two types of loss function that we can specify while performing the fit

on the decision tree algorithm one is a Gini impurity index and the other one is the entropy.

So this is the formula for Gini impurity index.

And this is the formula for entropy.

So we can go ahead and find the value of Gini impurity index and the entropy at each node.

and check how much impurity that we are trying to reduce compared to the previous node.

And then we will choose the column.

That means we will choose the variable which helps us to reduce those impurity to the maximum extent.

Okay.

Now while working.

So if I are trying to construct a decision tree, it is recommended that you have the pi dot plus and

Graphviz.

Now if you do not have the same, you can also make use of the scikit learn plot tree function, which

I'll be showing you as we progress in the hands on session.

Okay, now what we'll be doing is we'll take up the hands on implementation of this decision tree algorithm

so that you get a clear picture as how do we actually perform the fit on this given dataset.

Okay.

So I'll see you in the next video.