# 16. Hyperparameter Optimization Techniques

---

WEBVTT

Back to your learning journey in this AWS Machine Learning Certification Examination.

Now, in the previous video, we have actually implemented the decision tree algorithm for a given data

set.

And here during the initialization of this dictionary, we have made use of the various like various

techniques, like when I say various techniques.

So we have actually used the default parameters.

And as a result of this, this has constructed a decision tree model which would look like this.

And in this decision tree model, we have found that this is actually performing an overfit on this

given data.

Now the solution is to avoid the overfit so we can make use of the cross-validation technique.

So using this cross-validation technique I can reduce the overfit.

But that's just not all apart from using this k fold cross validation technique, we also have to perform

the hyperparameter tuning.

So that is what we'll be learning as we progress in today's session.

So let's start with a basic question.

What are these hyperparameters.

Well when we talk about this hyperparameters.

So in machine learning we have to differentiate between the parameters and the hyperparameters.

parameters.

Now, when we start working, the learning algorithm that we use, it actually learns or estimates the

model parameters for a given data set.

To tell you what I mean, if I consider the algorithm of linear regression algorithm.

So if I consider the linear regression algorithm, this is given by a plus b x.

So here a and b are actually the model parameters.

Okay.

These are my model parameters.

And this model parameters will be actually learned from the given data set.

So when I share x and y as an input for my algorithm okay.

When I share x and y as the input for my algorithm, it is going to find patterns between x and y.

Okay, it will find the patterns between x and y, and after it finds the patterns between x and y,

we can go ahead and generate the prediction.

Now, while finding the pattern, these values of parameters A and B will actually be learned.

Okay, now the way this parameters will be updated is by taking the reference of loss function.

In case of linear regression algorithm, I can make use of the loss function like squared error loss,

and using this squared error loss technique, I can go ahead and find the best values of the parameters

of the machine learning algorithm.

Now when we talk about the hyper parameters, on the other hand these hyper parameters are specific

to the algorithm itself, and these hyperparameters cannot be learned from the given dataset.

So here we actually use these hyperparameters to calculate the model parameters.

Now if I change the values of my hyperparameters, these values of the parameters is going to change

for a given data set.

Now to give you an example, if I consider the model that we are currently working, that is the decision

tree algorithm.

So if I say decision tree classifier.

So these are the various inputs that is expected by the decision classifier class.

So these are actually called as the hyperparameter.

So these hyper parameters that you see right here this is specific to the decision tree classifier.

On the other hand if I may open the linear regression or let's say logistic regression.

So these hyper parameters that you see.

So these are specific to this logistic regression algorithm.

So we have got penalty dual fit intercept.

See all these things are specific to this algorithm.

Now when we talk about the hyper parameter tuning, the hyper parameter tuning it is actually consist

of finding the set of optimal hyper parameter values for a given learning algorithm whenever we are

applying the optimized algorithm to any data set.

So here the idea is to find the values of these hyper parameters okay.

Finding the best values of the hyper parameter such that the relationship between x and y can be expressed

and can be found in an efficient way.

That is the overall idea of this hyper parameter tuning technique.

So the combination of these values of the hyper parameters will actually help us to get the optimized

performance of my machine learning model, and as a result, it will minimize the predefined loss function

based on the given input data and tries to find an optimal solution within the given setting.

Now, when we talk about this hyperparameters, as I mentioned, this is specific to the algorithm.

So these are all the hyperparameters for the decision tree classifier.

Now by varying these values of these hyperparameters the result is going to change.

Now the question is how can I find the best values of these hyperparameters.

See there are various methods of performing the tuning of this hyperparameters.

So three common methods if I want to name it first, is manual method, and the second method is called

as grid search CV method, and the third method is called as random search CV method.

So let's understand each of these technique.

So let us start with the manual method.

See when we talk about the manual method in the case of this manual method here manually, we are going

to adjust the values of the hyperparameters, and we will look into the model's performance.

And by adjusting it manually from our knowledge on the trial and error, we are going to optimize the

values of this parameters.

Now to illustrate the same, let's go ahead and go back to our notebook.

See if I go back to my notebook.

This is how the overall output would look like.

And when we have constructed the decision tree, if I want to get the default parameters that have been

considered for this decision tree algorithm are CCP alpha zero, maximum depth has none, maximum features

has none, and so on.

So these are the default values of the hyper parameters that have been considered to learn the patterns

between x and y.

Okay.

Now when I say manual technique, in the case of this manual technique, I'll be interested in finding

the patterns between x and y.

Okay, I'll be interested in finding the patterns between x and y such that.

Yes, such that the such that I'll be getting the better performance to illustrate the same this time.

During the initialization of this decision tree algorithm.

So I'm going to specify some of the values of this hyper parameters.

I'm going to call it as tree two.

Okay.

And here we'll perform the training on my X train transformer and Y train.

And along with the random state I'm going to mention one of the hyper parameter that we have got that

is maxdepth.

And I'll mention as three.

Now, with the Maxdepth mentioned as three next, what I'm going to do is I'll just go ahead and perform

the prediction on my training data as well as my test data.

So I'll copy the same over here.

I'll find the whitest white pred test, and I'll add the code, which will help me to generate the prediction

as well as create me the visualization of the decision tree.

So let me do that.

Okay.

In this code I'm generating the prediction on my training and the test data.

I'm calculating the F1 score on my training and test data for this decision tree classifier with the

depth as three, and I'm creating the visualization for this decision tree.

Okay, so here it says number of labels is two.

Let me just go ahead and fix that.

This is X train transform and y train X test transform okay.

So I think I'll have to fix the same.

So currently the x and y this is actually considering I'll just fix the code over here.

I'll say x underscore a.

Okay, so this is going to fix the seam okay.

So once I've fixed the errors over there.

And now if we visualize the given data set see the F1 score when I make use of maximum depth as three

with the Gini criterion, which is a default criterion that will be used.

The F1 score on my training data is 0.8897, and on my test data it is 0.91 with by limiting the maximum

depth as three.

So this is how the decision tree actually would look like with the criterion of maximum depth as three.

Now I'll do one thing.

I'll use the same number of parameters.

And I'm going to say that this time I'll say criterion is equal to entropy.

Okay.

Now I'll execute the code.

So with the criterion as entropy, the F1 score on my training data is 0.8862, and the test data is

0.9130.

That means there is a changes that are happening.

If I modify the value from entropy, that is, from Gini to entropy.

Now if I modify the value of depth.

So, I would still see the further changes on the overall outcome on my given dataset.

So from this, we can say that by modifying the values of this hyper parameters of this decision tree

algorithm, I am actually getting different different outcome as you would see.

So this clearly means okay, this clearly means that the tuning is is we can actually do it.

And in the case of manual tuning, it is very difficult to find the best values of this hyper parameter

so that I can get the better performing model.

So this manual approach will not be suitable for every case.

Now imagine that you have got you have to try out let's say 120 different combinations.

So at that time you will have to create 120 models manually.

And you have to manually compare us.

Which model is giving you the best performance.

So we need a way, an automated way to perform this hyperparameter tuning.

So an automated way to perform this hyperparameter tuning is the grid search method.

Now you can think of this grid search method that we have got is some sort of a brute force hyperparameter

tuning method.

Now what we do is we create a grid of possible discrete hyperparameter values, and then we fit the

value.

Or this we fit the model with every possible combination of the data.

Okay, we fit the model with every possible combination of the data set.

And then we are going to record the model performance of each set.

And then select the combination which has actually produced the best performance.

Now this can be easily illustrated with an example.

So let's take up a scenario where you have a date.

Now when you have a date, you have decided to go for a shopping.

Now when you went out for shopping, you have shortlisted the items that you want to wear it on your

date.

So you have shortlisted three t shirts.

So I'll say t shirt one, t shirt two and t shirt three.

And you have also shortlisted two jeans.

Jeans one.

And I'm going to call this as a jeans two and you have actually shortlisted three shoes shoes one,

shoe two and shoe three.

So since it's a special date, you you don't want to take any chances.

So what you will do, you will go ahead and you will pick all these items into a trial room, and you

will start trying out each and every possible combination of these items that you have selected.

So you will select t shirt one and you will wear the jeans one and you will try out each of the shoes,

that is shoes one, shoes two, and shoes three.

Now, while trying out each and every possible combination, you will rate yourself as how are you actually

looking?

So to document what I am saying.

So let me just write it in this way.

Okay.

First you will try out t shirt one jeans one shoes one, then t shirt one jeans, one shoes two, t

shirt one, jeans one, and shoes three.

And for each of the combination, you will rate yourself out of ten.

Okay, maybe you will rate yourself as seven out of ten, six out of ten, five out of ten.

Okay, now once you have done these different combination, you will check out the next combination.

Now this time you will wear the same t shirt one.

But you will pick up the jeans two okay.

And try out with shoes one so t shirt one jeans two shoes two t shirt one jeans two shoes three okay,

so you will rate yourself in this case as well.

Let's say six, five, eight and so on.

And then once you have tried out all the different combination, now you will change the t shirt as

well.

So you'll pick up the t shirt number two.

Okay.

And you will wear the jeans one and choose one, jeans one and choose two jeans one and choose three.

And you will try out each and every possible combinations that you have got.

So in total you are going to try out three into two into three.

That means you will try out 18 different combinations of the dresses that you have got.

and for each of the combination, you will personally rate yourself as how do you actually look now

since it's a special date?

After trying out all the 18 combinations, then you will select the combination assuming that okay,

you will select the t shirt two, jeans one and the shoe three, you will select these combination are

the one which is actually looking good on you.

Okay.

T shirt two, jeans one and shoes three.

Based on the rating, maybe you have rated yourself as ten out of ten.

So you are going to select this particular combination for your date.

Now in the same way the grid search technique is going to work in the case of grid search technique,

instead of having these values okay, instead of having these t shirt, jeans and shoes, think of this

as the various hyper parameters.

Let's say I have got one hyper parameter, which is called as the criterion.

And for the criterion you have got Gini, you have got entropy, and you also have log.

Okay.

And then maybe you have one more different hyper parameter that is the maximum depth.

You can use two, you can use four, you can use six.

These are the values that you want to try which you are currently shortlisting.

Then you will make use of one more hyper parameter.

Let's say minimum sample split from the hyper parameter list of decision tree.

You will say that this is four, this is six and maybe this is one.

Okay.

In this way you will specify the various values of hyperparameters.

Now what you will do on on the given data set that is your x and y.

So you will apply the various different model configuration.

The total number of possible combinations are three into three into three which is equal to 27 combination

okay.

That means you are going to create 27 models.

You are going to create a 27 models, each model with each and every possible model combination that

have got between the hyper parameter space that have chosen.

Then on top of these, each and every model I am going to perform the cross validation technique and

at the end, whichever is the combination, which is giving me the best performance, that's the model

that I'm going to use it for the final model.

So best performance model is equal to my final model.

So this is the overall idea of this decision trees hyperparameter tuning technique.

Now I have given an example of the decision tree algorithm.

Now this can be extended for any algorithm that we have got.

See that's the best part about this decision trees algorithms hyperparameter tuning technique.

So the the technique that we have just seen is the grid search technique.

As you can clearly see I'm taking the brute search approach okay.

Where I'm checking out the each and every possible combination of the hyperparameters, and I'm going

to find the best combination of these hyperparameters.

So if I'm applying this technique, one thing that you have to remember is this is very slow.

So this will take a lot of time to converge because it has to try out each and every possible combination.

So this is about the grid search CV method.

Okay.

Now in order to implement this Gridsearchcv method in Python programming language, we've got the Gridsearchcv

from this sklearn dot model selection.

So let's have a look into the same.

I'll start by importing.

So from sklearn dot model underscore selection I'm going to import Gridsearchcv okay.

That is Gridsearch cross-validation technique.

Now if you look into the grid search CV documentation from the scikit learn library.

See this is the official documentation of this gridsearchcv.

So it's a exhaustive search over specified parameter values of an estimator.

So this will take the estimator the parameter grid, and the scoring that we want to use.

And then we can also go ahead and mention the cross validation that is the value of k.

So value of k for this k fold cross validation okay.

So this is the one.

And along with that so we can go ahead and get the other parameters as well.

Now let me show you as how we can actually implement it.

See, the way that we can implement it is.

So I'll be implementing the same for the decision tree algorithm that we are currently working.

So I'm going to say grid is equal to here.

I'm going to say that Gridsearchcv.

And for the model I'll be mentioning the tree that is the decision tree.

Now what is decision tree?

Let me go ahead and initialize it so that you have the clarity.

This is the decision tree classifier.

And I'll initialize it with the rand with the default values with random state as 42.

Okay.

This is my decision tree.

That is my estimator.

Next we have to specify the parameter grid.

Okay.

We have to define the parameter grid, which is actually my required number of parameters.

That is my hyper parameter space.

So this we have to create it as a Python dictionary.

So here I'm going to mention the parameter name that I want to tune.

So I'll say criterion okay.

For the criterion the values that I want to try out are these values okay.

Entropy Gini and log loss okay.

Then along with the criterion, the next thing that I'm going to specify is which I want to fine tune

is maximum depth.

I'm going to say max underscore depth.

The various values that I want to try out are two four, five, and six.

Okay.

These are the values that I want to try out under the maximum depth.

And then let me choose one more.

So one more is will be choosing the minimum sample split.

So here I'll choose as minimum sample split.

And the value combination that I want to try out are four, two and one.

These okay Let's say four, two and six.

These are the values that I want to try out.

So if you look into the total number of hyperparameters there are three for the criterion, four for

maximum depth and minimum sample split is three.

So the total model combinations okay or the total number of models that will be tuned is three times

four times three, which is nothing but three fourths are 1212 threes are 36.

Okay, now on top of this we will have the cross validation.

Let's say in case of k-fold cross validation, if my k fold is equal to phi then total iterations will

be nothing but 36 into phi which is nothing but five, 6 or 33 carries.

So 180 so 180 number of fits will be performed on the given data.

Now let me show you it will be very visually clear for you.

I'll say CV is equal to five okay.

That is the number of value of k for the k fold cross validation.

And then I'm going to mention my param grid param underscore grid is equal to param.

underscore grid.

Okay.

This is how I would specify.

And by default the metric will be used is the accuracy.

So I'll mention the same okay.

And I'll mention as verbose is equal to one so that we can get the information once the system has performed

the fit.

Now in order to perform the fit on the given data set, I'll say grid dot fit x train Transform and

I'll mention y y train.

Okay, so here we are seeing an error.

Let's have a look into the scenario.

So it's actually the scoring not the metric.

See this has performed the grid search CV fit on the given data set.

Okay so here if you look into this output, it has total of 180 fits for 36 candidates.

Okay.

So this is what the hyper parameter tuning technique that is gridsearchcv can be implemented.

Now once the fit has been complete, we can access the best performing model I'll say grid dot best

estimator.

If I return this, this is going to return me the decision classifier object where the parameters are

maximum depth s2, minimum split, minimum sample split as four, and the random state as 42.

And here on the best estimator, the best accuracy score that I was able to reach on the given data

is 90%.

So this is how we can perform the hyperparameter tuning on the decision tree classifier.

In the US, we actually have the automated way of performing the hyperparameter tuning.

So we'll take a look into it as we progress in our learning journey in the further parts of this section.

For now, I hope you are now clear as how we can perform this grid search CV technique and how to implement

it in a hands on manner.

Now, apart from this gridsearchcv technique, there is another technique under the hyper parameter

tuning that's called as random search technique.

Now, in the scenario of this random search technique, what we do is we'll take up the same example

that we have got.

Let's say you are going for shopping in a very last minute.

So since you are going for a shopping in a last minute, you do not have time to try out each of the

combination.

So what you will do is you will select out of three shortlisted.

You will just try out some random combination from this.

From this shortlisted items.

Maybe you will pick up t shirt one, jeans one and you will try out shoes two.

Okay you will pick up t shirt two, maybe jeans one and shoes three and t shirt three, jeans two and

shoes one.

So let's assume these are the three random combinations that you would try out.

And when you have tried out these three random combinations, you have selected that T three jeans,

two that is t shirt three, jeans two, and shoes one.

So this is what you have picked up as the final one, because out of three combinations that you have

tried out, this is the combination that is actually looking good on you.

Even though the best possible was actually t2 j1.

Okay, let's say t2 j2 say j1 okay.

T1 j s1.

So out of these combinations, even though the best one was t2 j1, since you did not try out that combination,

that is t2, j1, and S3, you out of these possible combination.

The best one that you have found is t3, j2 s1.

So this kind of technique is called as random search cv technique.

So in case of random search CV technique.

So this also comes under the hyperparameter tuning.

This will actually help us in randomly selecting the various values of the hyperparameters which will

overall help us in finding the best possible ones.

Okay, so let's go ahead and look into the official documentation page.

So this is the official documentation page for Randomizedsearchcv.

So for this Randomizedsearchcv this expects the estimator the parameter distributions and the number

of iterations.

That is the number of combinations that I want to choose.

And this is followed by the CV.

So let's go ahead and implement the C from sklearn dot model underscore selection I'm going to import

randomizedsearchcv and most of it will be the same okay I'll just copy and I'll paste it below.

Okay I'll initialize my decision tree classifier.

And here instead of gridsearchcv at cv cv as phi and instead of param underscore grid in case of randomizedsearchcv.

This is actually param distributions.

Okay.

So coding is equal to accuracy.

Verbose as one.

And we'll perform the fit on the given data.

Now once I perform the fit on the given data it is performing five folds for ten candidates.

See the number of candidates it has chosen is ten, and it is totaling to 50/5.

Now, if I get the best parameters okay, if I say random dot best params.

So the best params that has been found is minimum sample split as for maximum depth is two and criterion

is entropy.

Okay.

So that is what it says.

Now in the other case of gridsearchcv technique, the best params was actually okay, let me just show

you grid dot best params.

It was actually criterion was good and maximum depth as two in case.

In this case the criterion is entropy.

So this is the best parameters.

And if I say best score on the given data, it is 0.9.

Okay, so in this way we can actually perform the hyperparameter tuning for any machine learning algorithm.

So as part of our learning journey, we have seen how we can perform the hyperparameter tuning on one

of the most commonly used algorithm that is the decision tree classifier.

And you can apply the same algorithm on any other algorithms where you have more than one hyperparameters,

and when you want to automatically find the best values of the hyperparameters.

Okay, with this, we come to the end of this video about the hyperparameter tuning technique, and

we have learned about all the three methods.

That is the manual method, Gridsearchcv method, and the Randomizedsearchcv method.

So I'll see you in the next video.