# 20. Ensemble Learning - Bagging Classifier & Random Forest

---

WEBVTT

So continuing in the learning journey of ensemble learning techniques, in this video we are going to

talk about the bagging classifier.

Now when we talk about this bagging.

So bagging is a full form of two terms.

One is bootstrap and the other one is called as aggregation.

So it's the bootstrap aggregation classifier.

So let's understand these two terms bootstrap and aggregation.

So bootstrap is a statistical technique where we will perform the sample selection with replacement

okay I'll say sample selection with replacement.

That kind of sample selection with replacement is actually called as bootstrap sampling.

Okay.

In case of aggregation I'll take the overall aggregation value, or I'll perform the overall aggregation.

In case of numerical value, I'll take care of, let's say, mean value.

That's the mean aggregation.

In case of categorical, I'll make use of the counts okay.

So in this way we will perform the aggregation.

So bagging is consisting of two things bootstrap and aggregation.

To give you a better understanding of these terms, let's consider an example.

Let's say you've got a bowel.

Okay.

In this bowel there are actually eight marbles are present inside this bowel.

Now let's say if I have got eight marbles okay.

If you ask your friend one to come over here and pick one of the marble from this bowel at that time,

if you think about it, as a result of picking one marble from the bowl, then this would result in

you are having only seven marbles in the bowl.

So if you now ask your friend to to pick another marble one from the bowl, then at that time your friend

two has to pick up one marble out of seven marbles.

Now, if the friend two picks up the one marble, then when you are asking your friend three to pick

up the next marble, he will be picking up one marble out of the remaining six marbles.

Okay, this is called as a sample selection.

So each and every person that is, each and every person that is each friend of yours will come over

here and he'll pick up one marble in a random way.

Okay.

Once you pick up each marble.

Okay, so the in the total number of marbles, one will be reduced.

That is, one marble will be reduced.

Okay.

So this is called as a sample selection.

Now when I say sample selection with replacement what happens in this scenario is here in the case of

sample selection with replacement I'll take the same example.

I'll have a ball.

Okay.

And I have got eight marbles over here.

Now what I'll do is I'll ask my friend one to pick up any one random marble.

So we'll come over here and he'll pick up one random marble.

Now once he picks up the one random marble and show it to us next, what this friend is going to do

is he's going to place this marble back into the jar.

Okay.

He's going to place this marble back into the jar.

Now you're going to call your friend to your friend who will come, and he's going to pick one of the

marble from this jar.

And once he selects and shows it to us, he's going to again place that marble back into the jar.

So what is happening in this scenario is I am performing this sample selection.

Okay.

I'm performing the sample selection.

Once I've done this sample selection I am replacing this sample back into my population.

Okay.

That means I am replacing the sample.

In other words, the overall activity that I'm doing is sample selection with replacement.

This is the overall idea about this bootstrapping.

So sample selection plus replacement is called as bootstrap samplings okay.

And the other term that I mentioned is an aggregation.

Now to illustrate the same let's say you are planning to go for a trip.

Now to go for a trip you will go ahead and you are going to ask your friends, okay?

You're going to ask your friend one friend two, friend three, friend four.

Like this.

You're going to ask various friends of yours.

Now, after you have asked each and every friend of yours, at the end of the day, what you would do

is you are going to choose the destination, okay?

You would choose the destination which has been recommended by majority of your friends.

So this is called as aggregation.

So bootstrapping plus aggregation is called as bagging.

So bagging is the combination of bootstrap technique and the aggregation technique.

Now with this basic idea if I now have to explain this bagging classifier, what we do is from the given

set of data set, let's say in my data set, I've got 100 rows.

And maybe I'm going to have, let's say 80 columns in this given data set.

So what I'll do is this step one is I'm going to generate bootstrap samples.

I'm going to generate bootstrap sample data.

For example I'm going to perform the selection along the column.

So I'll select my subset one okay where I'll have 100 rows by 30 columns.

And like this I'm going to perform the sample selection with replacement where I'll be generating multiple

subsets.

To give you an example, let's say I'm going to generate five sets.

That is five samples that I am going to make where each sample is having 100 rows and 30 columns.

Here the 30 is the number of features that is nothing but the number of columns in the data set.

So while selecting the 30, I am performing the bootstrapping.

That means I am performing the sample selection with replacement.

Now, once I have generated this bootstrap samples from this given data, I am I'm going to train each

and every subset on the individual machine learning model.

Okay, I'll perform the training on the individual machine learning model.

Remember all the machine learning model should be of the same family.

That means should be of same algorithm.

So I'm going to train let's say m1 m2 up to M5 okay, up to M5.

That means I'm actually training five machine learning models.

Now, once I've trained five machine learning models, then the next thing that I'm going to do is whenever

I've got the new data for this new data, I'm going to generate the prediction from each and every model

that I have got.

So let's assume my model one gives the output as one.

Model two gives the output as one.

Model 3010 okay 12345 okay, so this is how the overall prediction from each of the model would look

like.

Now at the end what I'm going to do is I'll perform the aggregation.

So in this step of aggregation I'm going to count the number of Odes that I'm having for each of the

class.

For class one, there are three odes.

For class zero, there are actually two votes, since class one is having more number of votes at the

end.

As a result of this aggregation, the total outcome or the overall outcome is class one.

So this is the core idea of this bagging classifier where we are going to perform the activity of bootstrapping

and the aggregation.

Okay.

This is the idea of this bagging classifier.

Now with this basic understanding of this bagging classifier, let's go ahead and implement it on a

given data set C in order to implement it on this given data set from the scikit learn library, we've

got something called as the the the bagging classifier.

So let me just go ahead and import the same.

So for the demonstration purpose I have imported this bagging classifier.

And along with that I have also imported the decision tree classifier.

Okay, now to instantiate this bagging classifier I'm going to say this as bagging classifier.

And inside the parentheses, the first parameter that we have to send in is the estimator.

That means the model family for which that I want to use.

Now in this example, I'm going to make use of this decision tree classifier.

And apart from that I'm seeing the number of samples as 500.

That means I want to train 500 models in parallel.

Remember, in case of bagging classifier or this overall bagging technique, the training of my algorithms

will be done in parallel.

So I'll be training 500 models in parallel.

Okay.

Then I am going to mention few more things.

So I have I have I can specify the maximum number of samples.

So here the maximum number of samples is nothing but the number of rows that I want to consider for

each of the model.

Okay.

Apart from that I can also perform the sample selection for maximum features as well.

Okay.

So in this way we can specify the other parameters.

That is my hyperparameters and in this case number of estimators is 500 maximum samples as 100.

So each model that I'll be training will have 100 rows.

And I'll be using the same data which we had used earlier.

So here in the last case.

So we have actually made use of some example data.

So just like the same even in this case as well I'm using the same data which we have randomly generated

with the help of make moons function.

Now, on the same data, I'm going to apply the fit on the Xtrain and Ytrain.

Now, once that is done, in order for us to visualize the overall decision boundary for the bagging

classifier and also compare it with the individual decision tree.

Okay, I have created a custom function.

Okay?

This is a custom function which will help us to create the plot.

And if I execute this cell, see, this is how our decision tree classifier would look like.

On the left hand side you can clearly observe on the left hand side there is lot of variations and there's

lot of I would say overfit is happening in the case of the individual decision tree.

Now, if we look into the bagging classifier, see with the help of bagging classifier, I'm actually

reducing the overfit by giving the prediction power to multiple decision trees.

Okay.

That gives you a visual perspective perspective as to why bagging classifier is more suited compared

to an individual.

Decision tree.

And here while working.

So I have mentioned that I'm going to create multiple data sets by using the random sampling with replacement.

Now if I want to evaluate my individual classifiers that have got what I can do is I can make use of

a data set that is called as out of back data set.

Now, out of back data is a data which is not consumed by the individual model during the training purpose.

And if I want to enable the scoring of those out of bag data set during the initialization, we have

to mention as OB underscore score is equal to true.

Okay.

Now this time I'll perform the fit on my given data set.

And I'm trying to access the OB underscore score.

And if I execute this, see the out of bag score on this bagging classifier that we have just trained

is 89.6 percentage.

Okay.

This is the performance of this bagging classifier on the out of bag data.

And if I go ahead and generate the prediction on the test data, the accuracy score that I'm getting

on the test data is 92% accuracy.

Wow.

That's great.

So this was about the bagging classifier.

Now in case of the bagging classifier this is a generalized algorithm.

I can use algorithm of my choice.

In the case of decision tree I have made use of it.

Now if I want, I can replace it with the logistic regression model, or even I can also make use of

the support vector machines.

Now, if I make use of the decision tree algorithm and apply this bagging technique, this is actually

called as the random forest technique.

In case of random forest, it is actually made up of multiple decision trees.

So let's go ahead and look into the documentation page of this random forest classifier.

See this is the documentation page of this random forest classifier.

And if you observe this is a random forest classifier, which is a meta estimator that fits number of

decision tree classifiers on various subsamples of the data set, and then it uses averaging.

To improve the predictive accuracy and it will control the overfitting.

And apart from the number of estimators, if you go through it, since this random forest classifier

is actually made up of decision tree, we also have the parameters which are related.

These are the hyperparameters which are related to the decision tree algorithm.

We can specify all these values and use it and perform the hyperparameter tuning as part of our requirement.

Now coming back to our code here, in order to implement this random forest classifier, I have imported

the random forest classifier and we can specify the number of estimators.

Now these are nothing but the number of decision trees that we want to have, and the maximum leaf node,

which is one of the hyperparameters of the decision tree algorithm and perform the fit, generate the

prediction.

And here I'm also going to perform the fit on the bagging classifier as well.

And what I'm trying to do is I'm going to fit on both the models bagging classifier as well as the random

forest classifier.

And I'm going to check whether the prediction from both the models is actually same or not.

So it looks like both the models are giving me the same output.

So this confirms that the technique that is used by bagging classifier with decision tree classifier

with these hyperparameter settings gives us the same outcome as what we would get in the case of random

forest classifier.

Okay, now this bagging classifier, especially the random forest classifier that we have just studied.

It also tells us the importance about the individual features that we have got on the dataset.

And we can access those feature importances with the help of the attribute feature underscore importances

attribute.

Now in this case I'm just loading the iris data set and I'm applying this random forest classifier.

And then I'm just displaying the feature importance of the individual columns.

So if you look into it it says that petal length has 44% of importance, petal width has 42% of importance

and rest is divided between sepal length and sepal width.

Okay, so in this way we can actually interpret the importance of the individual features for learning

the patterns in a given data set.

Okay.

So with this we come to the end of this video where we have learned about the bagging classifier and

the random forest classifier.

We will continue further in our learning journey, where we will look into the another ensembling technique

that is called as boosting.