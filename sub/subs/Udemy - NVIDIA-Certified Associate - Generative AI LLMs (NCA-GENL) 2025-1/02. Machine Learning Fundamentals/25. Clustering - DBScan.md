# 25. Clustering - DBScan

---

WEBVTT

Now, the other type of clustering that we are going to learn is the DBscan clustering.

So DBscan the full form is density based spatial clustering of applications with noise.

So this is what a DB scan the full form is.

So here what we are going to do is we are going to find the clusters in the data that is groups in the

data set by looking at the local density of the data points.

So we look into the density of the data points, that is, how many number of points are present.

And based on that we will create the cluster.

So the best part is when we talk about this DBscan clustering.

This DBscan clustering is not sensitive to outlier.

So if I have any outlier I can still be able to get a better clusters for the given data set.

And the best part is we don't have to no requirement to specify the number of clusters.

Okay.

So if you observe the diagram over here, see since this DB scan clustering algorithm, this can actually

predict the clusters in any shape and form.

But when you look into the K-means clustering algorithm, it can only predict the clusters which are

actually spherical in nature.

So if I have the clusters which are in spherical in nature, only those kind of clusters, I can find

it in the case of K-means.

But in the case of DBscan, since we are looking into the density of data points.

So this can even find the clusters which are non spherical in shape.

Okay so here when we are working with this DBscan algorithm we basically have two important parameters.

One is epsilon.

The other one is minimum points.

Here epsilon is nothing but it is the neighborhood around the data point.

If the distance between two points is lower than or equal to this epsilon value, then they are actually

considered as neighbors.

Now, if I choose the value of epsilon as very small, then I can expect the large part of data will

be considered as the outliers.

And if I choose the epsilon as very large, then the clusters will merge and the majority of data points

will be in the same clusters.

So one way to find the epsilon value is based on the k distance graph.

So you can make use of the k distance graph and find the better value of epsilon.

And the.

Another parameter that I've got is minimum points.

So this is nothing but the minimum number of data points within the epsilon radius.

So the larger the value of the data set that is larger the data set.

The larger value of minimum points must be chosen As a general rule, the minimum points can be derived

from the number of dimensions in the data set.

So we will generally say as d plus one.

Okay, if in my data set I've got four dimensions, that is four columns in the given data set.

Then the thumb rule is we will say minimum point should be greater than or equal to d plus one okay.

This d plus one should be chosen at the least.

So this is the quick idea about this DBscan algorithm.

So let us see how we can implement it in a hands on manner.

So this DBscan algorithm is also available inside this sklearn dot cluster from sklearn dot cluster

I'm going to import DBscan okay I'm going to initialize my DB scan.

And I'll mention my value of epsilon as one.

Okay.

Now here I'll go ahead and perform the fit on the given data set.

So the way that I'll do is I'll say data for this data.

I'll say db scan output.

So DB scan is equal to DBS dot fit underscore predict data underscore scaled.

Okay.

In this way we can generate the cluster labels.

Okay.

Now once this is done if I go ahead and print the value counts.

So this is going to display the cluster identification for the given data.

So here we have actually got in total 01237 clusters in this given data.

Okay.

And there are some clusters which have got as minus one.

So for the minus one cluster I have total of 81 rows.

So what it means is for these 81 rows the system is actually uncertain.

The algorithm is uncertain as to which class it would belong to because it does not fall under the criteria

that I have mentioned.

That is, within the epsilon radius of one, it is not able to find the minimum number of data points.

Hence it is classifying 81 data points as the minus one, which it is unable to identify the cluster.

So in this way we can perform the DBscan clustering algorithm, and this DBscan algorithm is also used

to find the outliers in the given data set, where whenever we have got the clusters is equal to minus

one at that time, we can use this DBscan clustering and say that okay, these are the data points can

be considered as the outliers for the given data okay.

This is how we can implement this technique.

All right team.

So with this we come to the end of this video.

So let's continue further in the next video.