# 2. EDA for LLMs

---

WEBVTT

In this video, let's learn about the EDA that is exploratory data analysis for the large language models.

Let's start with the basic question why do we require EDA for the large language model?

See, EDA will help us to understand the model behavior.

It helps us to understand how the LM is generating the text, what patterns it is actually learning

from the given training data, and what biases that it might be exhibiting for us.

By identifying these in the early stages, will we be able to build the LLM application in a much effective

manner?

And another key reason this EDA is essential is identifying the strengths and the weaknesses.

The EDA can reveal the LLM strengths and the weaknesses in various domains or the task.

And another key part is improving the performance.

By identifying the areas of improvement through EDA, we can actually refine prompts, training data,

or the model architecture so that we can enhance the model performance.

And another key reason is, I can say, ensuring the fairness and ethics.

So EDA can help us to detect as well as mitigate the biases that we have in the LLM generated text.

Okay.

Now another common aspect of analyzing the large language model is frequency analysis.

So when it comes to frequency analysis, the common techniques that we would use is word frequency analysis.

So we'll generally analyze the frequency of words in the LLM generated text, which can reveal the different

themes, topics, or the biasness that is exhibited by the model.

Now under this frequency analysis technique, I'm sure we have talked about it already.

We have the N-gram analysis.

So here we are interested in examining the sequences of two or more words, that is, bigrams, trigrams,

so that we can uncover the patterns about the common phrases and the patterns in the text.

Then we also have POS tagging.

POS tagging, which is nothing but the part of speech tagging.

This involves analyzing the distribution of different part of speech.

It can be nouns, verb adjective that can provide the insights into grammatical structure and the style

of the generated text.

Then we have something known as sentiment analysis.

Now under this sentiment analysis we generally analyze the polarity.

So this will help us to determine the overall sentiment of the text, whether it is the positive, negative

or neutral.

And we'll also measure the intensity.

We'll measure the strength of the sentiment which is expressed in the text and emotion classification.

This involves classifying the text into specific emotions like joy, anger, and sadness.

So these are the various aspects in which we are going to check the output, the output of these large

language models.

And then we'll also perform the topic modeling when it comes to evaluation and analyzing the large language

model.

So topic modeling involves the various techniques such as latent Dirichlet allocation.

This involves discovering the latent topics within the text and identifying the most relevant words

which are associated with that topic.

And we have something called as NMF that is non-negative matrix factorization.

This is another technique for identifying the latent topics in the given text of the last language model.

And we'll also use the method of diversity and novelty analysis.

Now this involves making use of the techniques such as type token ratio, where we are interested in

measuring the ratio of unique words to the total number of words in order to assess the vocabulary diversity

of the generated text.

And novelty metric.

So this will involve evaluating the originality and uniqueness of the generated text compared to the

existing data.

So overall, when we talk about the EDA workflow and analyzing the text that is generated by large language

model, it involves something like this.

It involves generation of the text, application of the pre-processing on the text data, Then applying

the EDA techniques, this can be performing the frequency analysis.

Sentiment analysis.

Topic modeling or diversity analysis to uncover the patterns and insight.

Evaluate performance where you will make use of appropriate metrics, both specialized.

LM metrics and general analysis metric in order to assess the quality and relevance of the generated

text.

Iterate and refine.

So this involves based on the insights gained from EDA and evaluation, it is recommended that you refine

and refine the overall LMS prompt or training data to improve its performance.

So these are the steps that is followed in the EDA workflow.

So with this we come to the end of this video.

So I hope you have enjoyed.

So I'll see you in the next video.