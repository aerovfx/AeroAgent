# 1. LLM Integration and Deployment

---

WEBVTT

Hey guys, welcome back to your learning journey.

Now in this section we are going to learn about the LM integration and the LM deployment.

Now when we think about deployment and integration, there are various ways in which the large language

models can be integrated and deployed.

So let's get a high level understanding as what are those levels, what are the benefits of the same.

And then as we progress in this section, we'll also understand as what are the services that we have

got from Nvidia to help us achieve that.

So to start with, we have something known as the API integration.

Now many llms like the OpenAI go here even to one labs that we have got.

They offer their models as a service through well-defined APIs.

Now these APIs are known as the application Programming interface.

Now by by using these APIs, this actually allow the developers to interact with LMS in a programmatic

manner.

So this will send the prompts and receive the generated text responses.

So that is I can say a very simple way to build an LM application.

So what are the benefits of it?

See some of the key benefits of this API integration are one.

It is easy to use because these APIs, they abstract away the complexities when it comes to hosting

and managing the LM infrastructure, which because that itself is a huge challenge.

So by using these APIs, this will allow the developers to focus on their application logic.

That's all.

And another best part is scalability.

The cloud based APIs, they can handle large volume of requests.

Now this will ensure the smooth performance even under heavy load.

And lastly, it is cost effective because you only have to pay for the resources that you would use,

which makes it super cost effective, especially for many applications.

Now what is the process of performing this API integration?

Well, the first step is you would generally obtain an API key.

You will sign up for an account, and you will go ahead and obtain an API key for authentication with

the LM provider.

Then from your application, you are going to send a prompt to the API endpoint and you are going to

receive the responses.

So you will make use of the SDK that is provided by your provider.

That is LM provider.

It can be SDK or it can be the libraries.

So using that, you will send in the inputs to that endpoint and you will receive the response.

So if you have worked with OpenAI platform or Google Gemini then I'm sure you can actually understand

what I'm actually conveying.

Now the best part is you can integrate the response.

You can actually incorporate the LM generate response into your application logic as per your requirements,

so that you would have a complete flexibility.

Now, other way of integration and deployment is by making use of the model serving framework we have.

We have various model serving frameworks.

Like.

To give you an example we have TensorFlow serving.

We have Torchserve.

They actually provide a streamlined way in order to deploy and manage these large language models in

a production environment.

Now, these scenarios or these frameworks, they actually handle the task like loading the model, processing

the input, and even batching the request and managing the resources.

So all these things will be managed by these Torchserve or these TensorFlow serving framework.

And it will be easier for us to deploy it.

Okay.

And some of the key benefits is these frameworks are optimized for efficient inference.

So this will always ensure that we get to experience fast and responsive LLM powered application.

And they can scale horizontally to handle the increased traffic and demand and more importantly flexibility.

So they support model formats which are in various formats.

And it also supports various deployment scenarios as per the application requirement.

Now what's the process when it comes to model serving framework.

So let's understand about the same.

So first step is exporting the model.

Second step is setting up the server where you will go ahead and configure the deploy the model serving

framework.

Specify the model location, runtime environment and other parameters.

And finally you will make the inference request.

You will send in the request to the server endpoint so that you can get the predictions from that LLM.

Now when it comes to exporting the model, you will generally save your model in the compatible format.

It can be saved model for TensorFlow serving or Torchscript for the torchserve.

So this is the general steps that you would follow whenever you are making use of this model serving

framework.

And we have the microservices architecture.

Now you can also integrate the LMS as an independent microservices within the large application.

So each microservice can handle a specific LM LLM powered functionality like for example, text summarization,

translation, sentiment analysis, and so on.

Now, by doing like this, we get to enjoy some of the key benefits, such as modularity, so you'll

be able to break down the complex application into smaller, manageable services.

And another key benefit is scalability.

You can independently scale each LLM microservice based on the demand.

Flexibility.

You can choose different LMS or the models for different tasks within the same application.

And lastly, file fault isolation.

If none of the microservice fails, it doesn't necessarily affect the entire application.

These are the key things about the microservices architecture.

Now the question that comes to our mind is what strategy should we use?

So we can use this API strategy that is API integration strategy.

Whenever we are performing this prototyping, or when we are developing a small scale application,

or the scenarios where we just want to have a simplicity and the rapid development, that's when the

API integration method is actually chosen and we use the model serving framework, especially for production

grade application, which requires high performance, scalability and the control over the LM infrastructure.

Because you are taking care of the LM infrastructure in the side of model serving framework.

And you will make make use of this microservices architecture.

This is actually ideal for large, complex application with LM powered features where modularity, scalability

and flexibility are key things.

Now, to give you a better perspective, let's consider a use case a use case where you have got the

microservices architecture for LLM powered chat bot.

So what you can have is you can have an NLU service, which is natural language understanding service

that can analyze the user input to understand the intent and extract the entities.

Then you can also have a dialogue management microservice that can manage the conversation flow and

the context.

And the third one is the LM microservice which can have the which can have the ability to generate the

responses based on the user input as well as the conversation history.

And you can also have the other microservices which will handle the task like data storage, authentication,

integration with external system and so on.

So this is an example as how you can have the microservices architecture for an LLM powered chatbot.

So I hope this gave you a key insights as how you can deploy and integrate your large language models.

So with this we come to the end of this video and I'll see you next time.