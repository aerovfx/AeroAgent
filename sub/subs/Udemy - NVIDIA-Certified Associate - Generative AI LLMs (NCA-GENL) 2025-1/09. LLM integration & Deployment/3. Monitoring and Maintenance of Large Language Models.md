# 3. Monitoring and Maintenance of Large Language Models

---

WEBVTT

In this video, let's learn about the monitoring and maintenance of the large language models in a production

environment.

So when it comes to monitoring and maintenance, first important aspect is the performance monitoring.

Now there are various metrics that we have got when it comes to assessing the LM model's performance.

So from the infrastructure point as well as from the working standpoint, these are all the key metrics

that are generally used.

First one is accuracy.

Now when I say accuracy, accuracy is a measure of proportion of correct responses or the prediction

and latency, it's the time it takes for the LM in order to generate the responses.

We have something known as throughput.

The throughput measures the number of requests which are processed per unit of time.

And we have something called as perplexity.

Perplexity.

Evaluate how well the model predicts the next word in a given sequence.

So if I have a lower perplexity, it means there is a better performance of this LM model.

And we can also define the custom metrics as well.

We can define the task specific metrics which are relevant for your application.

Like for example, it can be relevance of response or adherence to the safety guidelines.

So these can be the task specific metric that is always recommended to implement.

Because not every metric is a one size fits all solution.

Okay.

From the examination standpoint, you should be aware one of the key term that's called as the perplexity.

Okay.

Then coming to the monitoring tools.

Now there are various ways in which we can monitor the large language models.

So one is Nvidia Triton inference server.

So it offers the built in monitoring capabilities for tracking the metrics like the latency and the

throughput.

So that is sorted.

You can also make use of Prometheus and Grafana which are the open source tools, and you can use it

to collect, store and even visualize the metrics for your LLM performance.

Then you can also go ahead and make use of the custom dashboard.

So it's always recommended that you create custom dashboard in order to create and visualize the key

metrics and tracking the trends over the period of time.

Okay, next we have something known as the alerting.

It's always recommended to set up the alerts so that it can notify you when the specific metrics exceed

or fall below the predefined thresholds.

So this will allow you to proactively address the potential issues before they impact the users.

And lastly, the model maintenance.

Because the deployment doesn't end there.

Just when you let's say you deploy your machine learning or a deep learning model.

So you always have to maintain the model, keep on monitoring it.

So one of the key aspects of monitoring under the model maintenance is data drift monitoring.

So you have to regularly monitor for the changes in the distribution of input data.

If the data your LM encounters in production, if it is different than your training data, And this

can lead to degraded performance.

So in such scenarios, it's very important that you take the steps, such as considering the retraining

or fine tuning the model on the newer data set.

Then model updates.

So keep your LM up to date with latest model versions or improvements that have been released by the

provider.

Then a common practice is retraining and fine tuning.

So if the performance of the LM degrades due to data drift or other factors, retraining or fine tuning

is a way in which you keep your model updated and adversarial robustness.

Now, it's very important that you test your model, and especially its resilience against the adversarial

attack, which will attempt to trick into producing the harmful or incorrect outputs.

In such scenarios, maybe you can make use of these services, such as Nemo, guardrails or your custom

metric, to ensure that you're testing the model's resilience against such adversarial attacks, then

bias monitoring.

So it's always recommended that you continuously monitor for potential biases in the LM output.

Now if biases are detected.

So it's recommended that you consider applying the debiasing techniques or retraining the model on more

diverse data.

Now let's take a look into some of the Nvidia tools for LM monitoring and maintenance.

One is Nvidia Triton inference server.

As I've clearly mentioned earlier, you can use this Triton monitoring features to track the key performance

metric.

Nvidia TensorBoard.

So this tool can be used to visualize the LM training progress and evaluate the performance as well.

You can make use of Nvidia Nemo, so this will provide the utilities for evaluating and monitoring the

LM model performance, which will include the perplexity, calculation and visualization of the model

output.

All right.

So with this we come to the end of this video.

I'll see you next time.