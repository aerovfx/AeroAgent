# 2. Techniques for Large Language Models Experimentation

---

WEBVTT

In this video, let's look at the techniques for large language model experimentation.

So this experimentation as you know, it's a crucial for optimizing the LM performance.

And it will help us to uncover the best approaches for this specific task.

So when we talk about the techniques.

So these are the key things that we generally consider starting with the hyperparameter tuning.

Now when we say hyperparameter tuning hyperparameters parameters are basically the settings that control

the learning process of an LLM.

It can be learning rate, batch size, number of layers, and tuning such hyper parameters is essential

to achieve the optimal performance from the large language model.

Now, under this hyper parameter tuning, some of the commonly used techniques are.

Grid search, where we will systematically explore all the possible combinations of the hyperparameter

values within a defined grid.

But remember, guys, if we make use of this grid search approach, this can be computationally expensive

because you are going to try out each and every possible combination in the hyperparameter space.

Then random search.

So this is a sample hyperparameter values from the defined range.

This approach is less exhaustive than the grid search but can be more efficient.

So that's the idea about this randomized search.

Then we have one more technique that's called as Bayesian optimization technique.

Now as part of this technique, you will make use of probabilistic models to guide the search for optimal

hyper parameter values based on the previous evaluation.

Now, this approach can be more efficient compared to the randomized search, because when you're working

with the hyperparameters, when the number of hyperparameters is large.

So Bayesian optimization is the one that is found to be a better hyperparameter optimization technique.

Then we'll also make use of something known as a B testing.

So a b testing is basically a method for comparing two or more variants of LM it can be different model

architecture.

It can be prompt or training data set in order to determine which one performs better for a specific

task.

Now the process that we follow is we'll start by defining a metric.

We'll choose a relevant metric in order to measure the performance.

It can be accuracy, F1 score, or perplexity.

Then we'll divide our data set into two or more groups, let's say A and B.

Then we are going to run the experiments.

We will train and evaluate each variant of these LM onto its corresponding group.

Now finally we are going to compare the performance of the variants using the statistical test to determine

if the differences between those two tests are significant or not.

Now, once we find it, we will select the variant that performs the best based on the evaluation metric.

So this is called as the a b testing.

Then we have something known as ablation studies.

Now the ablation studies involves systematically removing or modifying the components of LM to understand

their individual contributions to the performance.

The key idea of this technique is to identify which components are most important for a specific task.

So as a result of this, this will allow us for the targeted improvements.

Like to give you an example in a transformer based large language model, you can actually perform an

ablation study by removing or modifying different attention heads to see how it affects the model performance

on the language understanding task that is, on the language understanding task.

How well this like how does this each attention head is going to affect the model performance and the

other technique is called as prompt engineering experiment.

In the case of this prompt engineering experiment, you will experiment with different prompt formats,

instructions, and the examples to see how they impact the LMS output.

So this will involve usage of techniques such as few shot learning which you are aware of, or prompt

template zero shot learning.

So using these techniques we will try out with different prompt format and we'll see how they impact

the LLM output.

Okay.

Then the another technique in experimentation is the data set augmentation.

Here you will expand your existing data set by creating additional examples through techniques such

as paraphrasing back translation or synthetic data generation technique.

Now, the key idea of performing this data set augmentation is to improve the model's generalization

ability and robustness to variations in the input.

And finally we have something called as error analysis.

So in this error analysis, we carefully analyze the errors which are made by the LM to identify the

pattern and understand the root cause, and the overall purpose is to provide the valuable insights

into the areas where model needs improvement and guide further on the experimentation aspect.

So these are the various techniques of performing the experimentation of large language models.

So with this we come to the end of this video and I'll see you next time.