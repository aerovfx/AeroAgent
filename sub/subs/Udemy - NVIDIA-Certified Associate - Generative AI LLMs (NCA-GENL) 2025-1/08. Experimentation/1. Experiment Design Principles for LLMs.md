# 1. Experiment Design Principles for LLMs

---

WEBVTT

Hey guys, welcome back to your learning journey.

Now in this section we'll talk about the experiment design for large language models.

Now this topic is a kind of an open ended topic.

So there's a lot of things that can fit in inside this experimentation design aspects of large language

models.

So we have tried our best to ensure that you get a general understanding of what exactly is this experimentation

when we talk about large language models and then from the examination standpoint, what are the key

things that you have to focus on?

Okay, so once you have done, once you have completed the activity, I highly recommend you to check

out the Nvidia page for some of the other services that would actually help us in this aspect of experimentation.

So with that said, so let's get started.

And let's start by understanding what exactly is this experimentation in this context of large language

models?

So experimentation is a fundamental part of developing and fine tuning the large language model.

By doing this experimentation, it will allow us to systematically evaluate various approaches, identify

what works best, and even gain the valuable insight into how these llms learn and behave.

And that is what the experimentation is all about.

Now, when we think about this experimentation, the first step is always defining the given problem.

So it's always starts by clearly defining the task that we want the LLM to perform, and the specific

outcome that we want to achieve.

This could be improving the accuracy on a specific task like summarization or translation, or it can

be generating more creative text or reducing the bias in the model's output.

Then we'll also start with a research question.

We'll formulate research questions that address these specific aspects of the problem that we want to

investigate.

Now these questions are the ones that will guide the design of the experiments and the data that we

collect.

Now let's consider an example for the problem definition and the research questions.

So let's say we've got a problem that states as how can we improve the accuracy of an LLM for a medical

text summarization.

Now the research question can be something as what are the most effective prompt engineering techniques

for medical summarization?

How does the choice of fine tuning the data set affect the model's performance on the medicine medical

text?

And can we reduce the bias in medical summaries by using more diverse data set for fine tuning?

So these are the research questions that we generally formulate when we think about a problem definition.

Now, another key aspect when we are performing the experimentation is formulating the hypothesis.

It's very important that we are going to formulate this hypothesis.

We have to develop the specific hypothesis that predict the relationship between the various factors

and the LMS performance and these hypotheses should be testable through our experimentation.

So whenever we are defining the hypothesis, we generally frame our null and alternate hypothesis.

So null means no effect, and an alternate hypothesis means an effect is present.

So this will allow us for statistical testing to determine if the results that we are actually getting

it from the LM model are actually significant or not.

To give you an example, a null hypothesis can be saying, as there is no significant difference in

summarization accuracy between the models which are fine tuned on different medical data set, and the

alternate hypothesis says that models which are fine tuned on larger and more diverse medical data set

will achieve higher summarization accuracy.

So this is how you would go ahead and frame your hypothesis.

And another key aspect during the experimentation of the large language models are the control variable.

It's important that you identify all the variables that could potentially influence the outcome of the

experiment.

Now these can include the model architecture, training data, hyperparameters, evaluation metrics,

and even the computational resources that are being used.

And it's also important that we control for confounding variables too.

So design your experiment in such a way that it isolates the effects of the specific variable that you're

interested in while keeping other variables constant.

So this ensures that any observed changes in performance can be attributed to the manipulated variable.

Then randomization.

If possible, randomize the assignment of data points to different experimental groups so that you can

reduce the impact of any unknown or uncontrolled variables.

So these are the key aspects when it comes to control variables.

Identifying the factors controlling for the confounding variables and the randomization.

Now let's take a look into the examples of this control variable.

See some of the key examples of this control variables are model architecture.

So keep the LM architecture constant.

Whenever you are comparing the different fine tuned data set and training data.

So it's always recommended that you use the same training data when you're evaluating different prompt

engineering techniques evaluation metrics.

So use the consistent evaluation metrics across all of your experiment to ensure that there is a fair

comparison between the experiments.

All right, guys, so this was about the high level idea about experiment design principles for large

language models.

So these are the key design principles that we generally apply whenever we are performing the experimentation

on these large language models.

So with this we come to the end of this video and I'll see you next time.