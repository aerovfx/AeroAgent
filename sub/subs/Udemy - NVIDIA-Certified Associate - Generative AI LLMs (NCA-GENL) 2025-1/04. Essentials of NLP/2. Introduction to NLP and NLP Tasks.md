# 2. Introduction to NLP and NLP Tasks

---

WEBVTT

So in this section let's continue further and we'll understand about the natural language processing.

First of all let's first define it.

What is this NLP which is also known as the natural language processing.

Now this NLP it's an area of artificial intelligence here.

The special thing is it takes the raw written text, which is nothing but the language that we humans

speak.

It takes in the text as an input and interprets and transforms it into a form which the computer can

understand.

So this deals with the input which is in the format of text.

Let's assume I've got a movie review which says that movie is awesome.

Now if you look at this text, it is basically in a unstructured format.

The written text.

So this NLP is a system which will take these input, which is in the format of text, and will generate

an output which will consist of the vector of numbers to simply put.

So it deals with the effective way to represent such text data into a numerical format.

Now, when we are working with natural language processing, we generally define the various things

that we can do with the help of this technique as the task inside this natural language processing.

If I want to name some of the high level tasks that we generally perform while working with this natural

language processing domain is we have language modeling, we have text classification, information

extraction, information retrieval, conversational agent, text summarization, question answering,

machine translation, as well as the topic modeling.

So these are the various tasks that we generally perform while working with this NLP that is natural

language processing domain.

So let's understand in a brief about what are these tasks.

It's very important that you understand what each task would mean.

Let's start with language modeling.

Now the language modeling is a basic task.

And here the the main, uh, task of language modeling is it is to predict what is the next word for

a given sentence based on the sequence of words that is appearing in a given language.

Now, when you start writing a sequence of words, a sequence of words is basically a sentence.

Now, once you write a particular sentence, determining what is the next word for the given sequence,

that's what the task of language modeling is.

Now, considering the nature of the task.

So this is used in various tasks like various applications such as speech recognition, optical character

recognition, machine translation, and so on.

So this was about the language modeling.

Then we have got the text classification task.

Now the task of classify text classification.

It's about assigning the text into a known set of categories based on the contents of a text.

Like for example, if I have a book review that says that Harry Potter book is awesome.

Now this is basically my input, which is my text data.

Now assigning the sentiment, I'm just giving an example as a sentiment, assigning the sentiment for

this given sentence, whether it is conveying positive sentiment or whether it's conveying the negative

sentiment, that is, whether it is positive review or negative review.

Assigning this kind of labels for a given text data is what we call it as the text classification.

This is used in applications such as email spam identification, sentiment analysis, and so on.

So if you want to assign a label that is a naming for a given text, that's when we will make use of

this classification task.

Then we have something called as the information extraction.

Now this information extraction is a task where we are interested in extracting the relevant information

from the given text data.

That's what we call it as the information extraction.

So basically we'll be extracting the important information which is relevant for us here.

One of the common application can be extracting the user's intent from the given input text, or identifying

the calendar calendar events from the email which you have seen whenever you are working with Microsoft

Outlook, or identifying the details of what exactly the person is conveying from that email.

So these are all some of the examples and the applications of this information extraction.

And we have something known as the information retrieval.

Now in the case of this information retrieval here this involves finding the documents which is nothing

but the text, finding the text required text or data based on the user's query.

Now this is generally used in the search engine.

Then we have something called as the conversational agent.

Now in the case of this conversational agent, it involves building the dialogue system that can converse

in the human language.

To give you some of the examples for the applications of this conversational agent, we have Apple Siri.

we have Google Assistant Alexa.

So these are some of the common applications for the conversational agent because these are the dialogue

system.

And it can converse in the human language.

And we have text summarization.

Text summarization is a task where we are interested in creating the short summary of a very large document

by retaining the important information, That's what the text summarization is all about.

Like to give you more information when we talk about this text summarization.

So like think about it about generation of the summary report from our social media information.

That's an common example for the text summarization.

Then we have something called as the question answering the question answering refers to the ability

to answer the question, which are posed in the natural language in an automatic manner.

Here, the question answering is a kind of task where we are going to expect a system to take the input

in the format of text, like for example, we might provide a complete database about the product information.

Then we would go ahead and ask the question, tell me, what is the storage size of this mobile phone,

assuming that I've got the database of the mobile phone.

Then the question answering task is generating the answer for the question that I have placed.

And the applications for this question answering is the chatbot.

And then we have something called as the machine translation.

As the name clearly says it's about converting the text from one language to the another language.

So an application and an example can be Google Translate.

Then we have something called as topic modeling.

Topic modeling is another task that we have got while working with the natural language data.

So it involves uncovering the important topics.

Okay, important information from the large collection of text.

Now this topic modeling is used in the text mining.

So what you have seen is from the NLP standpoint, from the natural language processing standpoint,

we can perform various tasks.

So the outcome of what kind of tasks that we want to perform, it is all dependent on the type of data

and as well as the type of model that we have built.

All right guys.

So it's very important that you understand these tasks at a very high level.

That is more than enough for you to progress along in your learning journey.

So with this we come to the end of this video guys, and I'll see you next time.