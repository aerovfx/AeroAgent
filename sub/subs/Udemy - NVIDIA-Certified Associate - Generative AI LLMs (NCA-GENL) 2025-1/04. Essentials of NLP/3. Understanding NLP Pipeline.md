# 3. Understanding NLP Pipeline

---

WEBVTT

In this video, let's understand one of the key terminologies that you would generally come across while

working with natural language processing based projects, that is NLP pipeline.

So let us understand what exactly is this NLP pipeline and how is it relevant for us now?

A step by step processing of a text is what we call it as a pipeline.

Now when we talk about NLP pipeline, it consists of various components, The important ones are data

collection and data acquisition, text cleaning, preprocessing, feature engineering, modeling, evaluation,

deployment and monitoring.

So these are all the key components of this NLP pipeline.

Now let me remind you guys.

So these are some of the main techniques that we are going to generally follow while working with this

NLP pipeline.

Now, when I'm working with the LM model, the way that we would work is entirely different, which

you will be learning more as we progress in the transformer architecture.

But the what we are trying to understand right now is at a very high level.

What are the key things that would make up our NLP pipeline?

First and foremost, we have got the data acquisition.

Now, when we say data acquisition, it's a process of gathering, filtering and cleaning the data before

we put into a storage solution like an S3 bucket or an Azure blob.

So the data acquisition is basically a process of retrieving the relevant business information, transforming

it, and then loading it into a designated system.

And that is what the data acquisition part of this NLP pipeline is all about.

Post that we have got the text cleaning.

Now the text cleaning involves it's a process of preparing the raw text for the NLP system so that the

system can understand the human language.

The human language is completely complex in nature.

It's very difficult to explain to a computer.

Now just think about it hypothetically.

If you want the if you want to teach the computer as how to spell your name, and how do you ensure

that you will be able to converse with the computer?

It's a very challenging task.

It's very difficult to train the system to understand our human language, and that's where the text

cleaning would come in.

It's a process of preparing the raw text that we have got into a format which my system can understand.

So that's the key idea about this text cleaning here.

What we do as part of this text cleaning is we are going to remove some of the non-textual information,

like the metadata, markup tags and so on.

All these will be removed as part of this text cleaning.

Now, once the text has been cleaned, this moves on to the pre-processing step in the NLP pipeline.

In the case of this pre-processing step, here, we'll bring our text into a form so that we can make

the prediction and we can ensure that the text is in the form which is ideal for performing the analysis.

Some of the common preprocessing techniques that we apply under this preprocessing step would include

lowercasing.

That means converting all our text into a lowercase format.

Tokenization, which is about splitting my sequence of words into the individual units.

That's called as the tokens, and we have sentence segmentation.

Now, in the case of the sentence segmentation, what we have caught is we are interested in segmenting

these sentences.

Okay.

For a given long list of paragraph, we want to segment or separate the sentence.

We have the stemming.

So stemming is basically converting our text okay.

Converting our text words that we have got into the root form and we have the lemmatization Lemmatization

involves the same thing.

It is converting the tokens into the root form.

But here the root form word will have its presence in the dictionary.

And we have the POS tagging, which is nothing but the part of speech tagging.

So these are all the common steps that we would perform as part of the text pre-processing.

Okay.

So these are the steps that we will be performing as part of this text pre-processing.

Now once we have done the pre-processing then we move on to the feature engineering.

Now this is a step where we will use the domain knowledge of the data and we will create the features.

So this feature engineering, it is going to capture the characteristics of the text into a numerical

format.

Now the reason because as we have learned so far in our machine learning and deep learning, okay,

from the basics of machine learning and deep learning, the important thing that you need to remember

is while working with machine learning and deep learning system, it expects the data in a structured

format and in a numerical format, and the feature engineering is all about understanding the importance

of various tokens that we have got in a sentence, and capture the numerical characteristics which explain

such language relationship.

Now, in order to do that, we have various techniques.

We have one of the most used technique that is the word embedding technique.

We have count vectorizer technique, Tfidfvectorizer technique and so on.

So these are some of the methods that we have got to convert the text data into a numerical format.

Now this is then followed by the modeling.

In this modeling step it involves the application of machine learning algorithms so that we can learn

the pattern in the data set.

Now once the modeling is completed, we move on to the final steps, which is about the evaluation,

deployment and monitoring.

So these are the key steps that we perform as part of our NLP pipeline.

Now that you have got a high level idea about the overall NLP pipeline, the important thing that we

need to understand is the pre-processing techniques.

So I'll end the video over here.

We'll discuss further in the next video.