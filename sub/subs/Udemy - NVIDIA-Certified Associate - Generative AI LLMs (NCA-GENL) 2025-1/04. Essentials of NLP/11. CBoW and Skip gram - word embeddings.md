# 11. CBoW and Skip gram - word embeddings

---

WEBVTT

In the last video, you have understood about the importance of word embedding and how it works at a

very high level.

Now let's understand in detail as part of a learning journey in this video.

Now, this information is very important for you to learn more about this representation of this text

data.

Now this word embedding is also known as the word two vec technique.

It is developed by Google in the year of 2013.

Now this word two vec is one of the technique that is based on the distributional hypothesis.

And when we talk about training such word to work model, there are two variants of this word two vec

model.

They are c, b, o, w that is continuous bag of words variant and the skipgram variant.

So these are all the two techniques that we have got when we think about generation of the word embeddings

for the given text token.

So let's understand it.

See the idea of this word two vec representation is basically given a word.

I want to have a vector representation of it.

Now that's the word two vec technique is all about.

Suppose if I have a word like king, I want to have a vector representation of the king.

If I have a word like man, I want to have a vector representation of the man.

And the important thing here is I want to have the vector representation of each of the words in a common

vector size, so that I can use it in a structured format.

Apply my machine learning or deep learning on top of it, so that I can find the patterns in a given

data set.

So that's the idea about this word two vec technique.

Now this word embedding or the word two vec technique is actually trained with the help of a task that's

called as the language modeling task.

Now, based on the language modeling task, we have trained these word embedding model so that it can

generate the efficient word representation.

Now when I say language modeling, it's basically a model which is which is a probability distribution

over the words or word sequences.

It's a language model.

That's what it is okay.

Like the idea is given a word, what is the probability that my next word would be based on that?

We have went ahead and generated this language model.

Now this slide over here describes the difference between the continuous bag of words technique and

the skip gram technique.

Now if I consider a skip gram technique.

Okay, now in the case of this skip gram technique, we want to predict the context word for the given

target word.

Okay.

So what it says is for the given target word I want to predict the context words.

So that is the core idea about this skip gram technique.

Now, to help you understand this, let's consider an example.

Let's say I've got a text that says as the quick brown fox over lazy dog.

So suppose this is my given text document.

Now the first thing that we will do is we'll choose a context size.

Okay, just for illustration purpose, I'll choose the context size or the window size as one.

Okay.

Now here what we are seeing is we want to predict the context word from the given target word.

So for example I'm going to say that the first word is my target word.

And the context word with the window size of two is two words to the right of it and two words to the

left of it.

If I'm considering the target word as the the the context word will be quick and brown.

So I'll be generating the training samples as the quick the brown.

Okay, the idea is given the target word, I want to predict the words that is surrounding my target

word.

On the other hand, if my target word is brown, I think this would give you much better clarity.

If my target word is brown, I want to predict its surrounding words.

Maybe for and jumped quick and dumb.

So my training samples will be in that scenarios will be brown box.

Brown jumped Quick, Brown.

The in this way I'm going to generate the training samples.

So the idea is I'll be training this word embedding models by generating the training samples such that

given the target word, I want to predict the context word that is its surrounding words.

And that's the technique of this skipgram technique.

On the other hand, we have another technique that's called as the continuous bag of words technique.

Now here, in the case of this continuous bag of words, what we do is it's the opposite.

So given the context word I want to predict the target word.

For example, if I've got the similar words like a quick the quick brown fox jumped.

over the lazy dog.

Okay, I'll consider the same example with the window size as two.

And just for the sake of simplicity, I'll consider the target word as brown.

The context word that is surrounding words are fox and jump.

Quick.

And the these are all the context words.

So the idea of this continuous bag of words technique is given by context word.

I want to predict my the target.

That means I'll be generating the training data such that the words like the quick brown, the quick

fox jumped.

If I have these words as an input, okay, I'll be I'll be expecting the output as brown.

This is called as the continuous bag of words technique and it is clearly represented over here in this

slide as well.

So it is not relevant for our discussion.

You don't need to know the mathematical part of it for your learning journey in this course.

Okay.

So the idea is there are two models.

There are two ways of generating this word embeddings.

One is continuous bag of words technique and the other one is skipgram technique in the case of this

skipgram technique.

So we want to predict the context word from the target word.

That means given the target word I want to predict the surrounding words.

So that's the idea about this skip gram technique.

And in the case of this continuous bag of words technique it's a little opposite.

So given the context word we want to predict the target word.

So these are the two techniques that we have got if we want to generate the word embeddings.

So in this way we can train the word embeddings model.

And once we train such word embeddings model, we'll be able to get the vector representation of the

individual words.

So here this uh screen over here describes the vector representation of the individual word.

And you can see that see the words like niece daughter.

They are having the similar vector representation and they are similar together.

Mother Husband.

They are having some similar father uncle they are having similar vector representation.

And you can see that as a result of this vector representation, we have each and every word having

the vector representation in the n dimensional space.

So that's the idea about this word embeddings.

Guys.

Now that you have learned about two techniques, there are two techniques that we have got that is continuous

bag of words and skip gram technique.

Now what are the difference?

So let's have a look at it.

At a very high level, when we look into the difference between the skip gram technique and the continuous

bag of words technique here in the case of the skip gram technique.

So here what we are doing is given the target word we want to predict the the context word.

So as a result of this when we are performing the training, the number of training samples generated

by the Skipgram is more than the number of samples that is generated by this continuous bag of words

technique, and the number of samples in the Skipgram is dependent on various factors like the window

size.

And another thing when we talk about the continuous bag of words technique.

In the case of this continuous bag of words, it is one sample per word.

In the case of this continuous bag of words and skip gram technique, because of the number of samples,

it takes longer period to train, and the larger the window size, the more sample it is going to result

in, so hence it will take much more longer in order to perform the training.

Now when we are looking at the working so this skip gram may perform better when the training data set

is very small because it generated more data for training and skip gram creates better representation

of the real words.

Now, some of the things that you need to note down about the continuous bag of words technique is the

continuous bag of words.

Technique performs slightly better than skip gram in capturing the syntactic relationship because we

are looking at predicting the target word from the context words, and this continuous bag of words

technique performs slightly better at representing the more frequent words.

So these were some of the key difference between the two approaches.

That is, Skipgram approach and the continuous bag of words approach.

So with this we come to the end of this section on the NLP fundamentals.

So I hope this section gave you some clarity about important pre-processing things that you need to

remember when you are working with the text data, especially from the perspective of natural language

processing.

In the learning journey that you had so far, you have seen the importance of machine learning and deep

learning, and how it can go ahead and help us with the representation of the of the given data set.

Now, while working with the NLP data, it's a little different now to represent that text into a numerical

format.

That's a whole different game.

And we have seen the various techniques that we can use it to convert such the text data into a numerical

format.

Now, out of all the techniques that we have discussed, I would say the word embeddings is one of the

powerful technique that we have got to represent our text data into a numerical format, because it

is capturing the relationship between the surrounding words.

So as a result of this, we will have the better representation for the given text data when we are

representing it in a vector format.

So with this we come to the end of this video guys, and I'll see you next time.