# 10. Word Embeddings

---

WEBVTT

In this video, let us go ahead and learn about the word embedding technique.

When we are working with the text data.

Now, we use this word embedding technique to represent the documents and the words in a much more effective

way.

And the best part is here when we are using this word embedding, this would help us to generate the

words in a numerical format, such that the words with similar meanings will have the same representation.

So that's the idea about this word embedding technique.

Okay.

So it's an approach where we are representing our words and the overall documents itself.

Now when we are using this word embedding technique.

So using this word embedding we can approximately uh check what would be the meaning and represent that

word in a lower dimensional space.

Now, in practice, what we do is we make use of the pre-trained word embedding models like flair,

Fasttext and Spacy and other libraries whenever we want to go ahead and represent our text into this

numerical format.

So that means when we are training this word embedding models, that means model is nothing, but it's

a model which is capable of getting the input, which is nothing but the word.

So your word will be the input.

And this is basically a model.

And this is going to generate the vector representation of that individual word.

Now the idea is the words that has got the similar meaning that has got a similar vector representation.

That means the words like good, nice, and maybe excellent.

So these are all the positive sentiment and it are used.

It are having the it is having the similar meaning.

Right.

So these words will have a similar vector representation.

That means if you represent that vector in that n dimensional space.

Okay, so all those vector representation like this could be one vector for good.

This is another vector for let's say nice.

And this is another vector for excellent.

So as you can clearly see these vectors are similar together.

So the words that has got the similar meanings will have a similar representation.

That means in the vector space they are closer together.

That is the idea behind this word embedding technique.

Now the question is how do we get that model?

Well, we get that model by making use of the process of deep learning.

So using the deep learning technique on a very large corpus, we tend to understand how words are used

in a given corpus, and based on that, this model will be trained.

And once the model has been trained with that word embedding matrix, we are just going to get the vector

representation of that given word.

Now you might think as like where will I use this word embedding.

What is the main usage of it?

Now to help you understand where we can make use of this word embedding.

So let's say you've got a problem.

Statement of tweet classification.

You are being given a supervised learning task.

Now you want to predict as which tweets are about the real disasters and which ones are not.

So in such scenarios, obviously the task is like a classification.

You want to classify the given samples to one of the labels.

So that's the idea about this text classification, isn't it?

Now, in such scenarios, the independent variables, which is nothing but your input is basically the

tweets, which is your text data.

And the target in this scenario is obviously the binary values, which says that whether the, uh,

whether, uh, the target variable is a real disaster or a not real disaster.

Now, in such scenario, okay, now in such scenario, in order to understand the best relationship

between your input text and the output value, that is the disaster.

Okay?

In order to understand such relationship between the text and the label, obviously we will be using

the machine learning and deep learning.

Now this machine learning and deep learning will only take the numerical input.

So the question is how do we convert such text into numerical values.

Well, we can make use of where we can make use of the bag of words technique term frequency IDF.

That is term frequency technique that is countvectorizer technique.

Or we can make use of tf IDF technique.

Or the best part we can actually make use of the word embeddings, where we can represent the individual

words as real valued vectors in lower dimensional space.

The best part is, if we make use of this word embeddings, we'll be able to capture the inter word

semantics.

That means we'll be able to look into the relationship between the other words.

So as I mentioned, this word embedding will capture the relationship such that the words which have

got the similar meaning when when we are using in a real world context, those would be having the similar

vector representation.

So with this approach, we'll be able to capture the inter word semantics.

And all these text data will be now represented with fixed length vectors.

So that's the best part about this word embedding technique.

Now to give you the demonstration, let's have a look into some demonstration about this word two vec

technique.

Okay so this is called as the word embedding okay.

So there are various models that are available.

I'll use uh let's say Google News or -300.

That's one example that I would consider now using this word embeddings.

Okay.

So there is a lot of applications guys.

So I can mention the word and I can look into the nearest words in the vector space.

Suppose if I mention the word as good and if I select the option as show nearest and show top ten nearest,

the words which are having the similar context good.

Great.

Bad.

Terrific.

Decent.

Nice.

Excellent.

Fantastic.

See, these are the words that are nearest to the words of good.

So the words, uh, that are used in the similar context.

So that can be displayed over here with the help of the word embedding model.

We can also look into the similarity between the words I can say good and maybe nice.

And if I say show similarity, similarity 0.68, which is closer to one.

And suppose if I say good and maybe worst, if I say show similarity, it is lesser, it is 0.28.

In the same way we can also look into the word analogy.

Okay, we can use as word analogy.

This is another example.

Or this is another application of this word embedding guys Okay.

You can also check out the other links as well.

This is another link which would uh where we can mention the text.

We can mention the model to generate the data set.

And we can just go ahead and generate the data set itself.

So these are all the various links that I have included over here.

You can have a look at it and learn more about this word embeddings.

All right guys so what you have learned so far is word embeddings is basically a pre-trained model.

Now using this pre-trained model we will have a vector representation okay.

We will have a vector representation.

And with that vector representation we will be able to learn the patterns which because here this word

embedding vector representation will be able to capture the Inter-word semantics.

So that's the idea about this word embeddings model.

So with this we come to the end of this video guys.

In the next video I'll give you a brief idea about how does this word two vec.

That is word vector representation of these words would work while working with this word embeddings.

So I'll see you next time.