# 6. Feature Extraction - NLP

---

WEBVTT

Now, after performing the preprocessing on the text data, the next thing that we generally do is we

will perform the feature extraction.

Now, in the step of feature extraction, we are going to convert our raw text into a numerical form.

And we generally call this as a representation of the text data.

So that's the idea about this feature extraction.

Now while talking about this feature extraction, one common term that you would generally come across

is the text vectorization.

Now text vectorization is basically same as the text representation where we are converting our raw

text into a numerical format.

Now the question that you might think is why should we perform this text vectorization.

Now when we consider the machine learning model as well as even the deep learning model, it requires

the data, whatever the input data that we have got in the form of vectors.

Now in that vectors, obviously it expects my individual values are in numerical format.

So the reason is when we think about processing the data, whether it is in machine learning or deep

learning, we are performing some kind of vector matrix multiplication.

And we are trying to find the values of the parameters.

So in such scenarios, when you want to perform the mathematical operation on that given text data,

I cannot apply it on the raw text.

So I want to have the data in a numerical format, and that's where the text vectorization would come

into picture.

Now inside this text vectorization we've got various techniques.

We've got one hot encoding technique Countvectorizer technique TF-IDF technique, word embedding technique.

So these are all the various methods that we have got which we can use it to represent our text data.

All right.

So now that you have understood the importance of the text vectorization and why do we actually do it.

We'll stop the video over here.

In the next video, let's look into the method of representing this text data into a numerical format.

One by one will be understanding it as we proceed in our further part of our learning journey.

I'll see you next time.