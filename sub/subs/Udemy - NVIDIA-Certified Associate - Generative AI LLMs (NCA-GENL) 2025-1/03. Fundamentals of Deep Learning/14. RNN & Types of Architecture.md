# 14. RNN & Types of Architecture

---

WEBVTT

In the last video, we have looked into the challenges that we have.

If we are making use of feedforward neural network for the purpose of natural language processing.

So the solution is recurrent neural network.

So let's understand what is so special about this recurrent neural network and how does it process the

data.

Now in the scenario of this feedforward neural network, the way we process the data is something like

this.

I'll be sending the features independently, as you can see in this example.

So X1 is being sent independently.

X2 is also being sent independently.

Both x1 and x2 are sent independently together.

Now on this data we are going to perform the data transformation here.

When I say data transformation, I'll take the dot product between them and I'll apply the activation

function, and at the end I'll pass the data through various layers.

And at the end we'll have an output layer which will help me to determine the output.

So the core thing that you have to remember is in case of feedforward neural network the data is processed

independently.

So the important thing this is very important thing.

I'll mention it here in the notes as well.

In case of feedforward neural network features are processed independently.

So this is what we would observe.

So features are processed independently.

Okay so but we have to find the relationship between the features as well.

So let's see how we can do that.

So when I say finding the features independently, if I take the same example representation which we

have used earlier.

So suppose if I have the text that says as dog was crossing the road and uh, it was very tired, so

I'll be crossing my individual word that have got as the individual feature, and I'll be processing

it independently.

Okay.

That is the overall idea scenario of feed forward neural network.

Now in the scenario of this recurrent neural network, the way we process the data is entirely different.

Now to help you understand this.

So what we will do is in case of recurrent neural network, I'll be I'll be processing my individual

words in a sequence manner.

So if I call this as the entire collection of words.

So I'll be processing individual words at a time.

For example, I'll take the first word that I've got with me.

Okay.

This is the vector representation of the word dog.

Okay.

So what I'll have is I'll have the word dog.

So using this word dog I'll have the vector representation of the word dog.

And I'll send this word dog as an input for a neuron.

Okay I'll send this as an input for a neuron.

And if I want to represent that neuron.

So this is a neuron H which is my hidden representation I'll send this as an input for this neuron,

the vector representation.

And I'll get some output from this hidden representation I'll call this hidden representation as y okay.

So I'll send this hidden representation as y.

So the next thing that I will do is like once I process this text, okay, once I process this word

of dog, the next thing that I'll do is I'll process the next word.

So I'll have the same kind of network architecture that you see right now.

Okay, I'll have the same kind of network architecture.

I'll call this as H zero.

Okay.

And above one I'll call it as y zero.

Now this time I'm going to send in the next word.

That is the wars.

Okay.

Wars is my next word.

I'll send the next word over here as an input.

And this word will be having its vector representation.

So here while getting the vector representation I can make use of any technique.

I can make use of word embedding or tfidfvectorizer I can use any of the method.

So using that vector representation we will generate the hidden representation.

But most importantly, yes, while generating this hidden word representation, not only it will consider

my current word, but there will be a connection.

Okay, there will be a connection between my h zero and h one.

So the output that I have generated that is H0 will also be sent as an input when I'm when it is generating

the output for the second word.

So this connection is called as a recurrent connection.

So this is the recurrent connection okay.

And this process repeats.

So to illustrate the same for the other words I'll have the representation something like this.

Next, I'll be processing the next word that is crossing.

Okay, crossing.

Crossing will be the next word.

And this is going to while calculation of my H2.

In this scenario I'll consider my H1 and then I'll be generating the output as y2.

So you can clearly observe how the connections are happening in this scenario.

So then the word the and while processing the word the, I'll also consider my.

To find the output of h three.

I'll also consider my previous output that is h two and I'll generate the output as y three.

So this is the kind of structure that we will have in the scenario of recurrent neural network.

So this recurrent neural network has a special architecture where what it will do is it will feed back

the input in order to predict the output of the layer.

So when it is generating the output for the second word, that is second word that I have got right

now when I'm trying to do this in that scenario.

So what we will have is so it will also use the output from the previous word.

Now this is the idea of recurrent neural network.

Now this general way of recurrent neural network I'm sure it would be confusing to you.

So let's do one thing.

Let's consider the various use case and let me show you.

How does the overall network architecture would look like.

So what we will do right now is we will look into the types of recurrent neural network that will help

you to understand how does this recurrent connections would be.

And then we will look into the types of the other concepts of training.

Okay.

So when we talk about the types of recurrent neural network.

So one type is called as a 1 to 1 architecture okay.

This is called as a 1 to 1 architecture.

Now in the scenario of this 1 to 1 architecture it's a it's known as the vanilla neural network.

What we generally use it is so we will use a single input.

And for the single input we will generate a single output which means this part.

So the applications of this 1 to 1 architecture is next character prediction or next word prediction

next word prediction or next character prediction.

Next character prediction.

So these are the applications of this 1 to 1 architecture.

For example, if I send in the input as dog, I would expect what will be the next word like in this

scenario.

The next word will be the vector representation of the word was.

Okay.

So this is the typical kind of application that we'll be looking at in this scenario of 1 to 1 architecture.

So next the like the next step will be something like this.

Next step is getting the input as was okay predicting the next word as an output.

That is the crossing okay.

So this kind of architecture.

So where the application is a scenario of next word prediction or next character prediction given an

input word.

So this kind of architecture is called as a 1 to 1 architecture.

So in the scenario of this 1 to 1 architecture, for each input I will have the each output from the

individual neuron.

So these cells that is blue cells that you see right here, these are all called as a are an in cell

recurrent neural network cell Okay.

This is the application and the working of this 1 to 1 architecture.

Now, apart from this 1 to 1 architecture, there is another kind of an architecture that's called as

the many to one to many architecture.

Okay.

Let's see how does the one to many architecture look like?

So in the scenario of this one to many architecture, this is a type of neural network where I will

give a single input and I'll be generating multiple output.

So the application of this one to many architecture is image caption.

So image captioning is one of the application of this one to many architecture.

So let's understand how does this one to many architecture would look like.

So here in the scenario of this one to many architecture I will have a single input.

Okay.

I'll just have the single input.

And this single input is capable of generating the Or from the single input.

I am interested in generating the multiple outputs.

For example, if I input any image okay.

If I input any image, suppose the image is like this, which is a image which look like this.

Okay, so I'll be sending a single image as an input which is one input.

Okay I'll name this as an input okay.

And I'll use this as an input.

And I'll be generating multiple outputs.

so the output can be something as captioning the image.

So cat is cat is sleeping now.

So here what I have done is using a single image as an input.

I have generated multiple outputs.

So this kind of architecture is called as the one to many architecture.

I'll take a single input and using a single input I'm generating multiple outputs in a sequence.

Okay.

So this kind of architecture is called as a one to many architecture.

So if the task is to generate the output for each and every input, that's a 1 to 1 architecture.

If using a single input if I want to generate multiple outputs, then it's a one to many architecture.

So this is how the one to many architecture would look like.

So using a single input as x I'll generate the h naught, which is my hidden state, and y naught is

the output at my current state.

And using my h zero, I'll again generate one more hidden state and will calculate the y one.

Okay, so in this way the one to many architecture would look like.

So similarly if we progress and uh create the other look into the other aspects of or other types of

architecture, the other architecture that we will have.

The third type of architecture is, is a common architecture, which we call it as many to one architecture.

Okay.

It's called as many to one architecture.

So this is a common type of architecture which we use it in natural language processing task.

The application is sentiment classification.

So in this scenario I am interested to take multiple inputs for the multiple inputs.

I am interested in classifying a specific class.

For example, if I've got a text that says as movie is good.

Okay, so here using these three words okay, I'm interested in generating the single output.

So if I want to represent it.

So this is the illustration of many to one architecture.

So as you can clearly see I've got a sentence as movie is good.

So each time at each timestamp I'll be sending my individual words as an input, and at the end for

the final word, I'll be generating the output, which will classify in this example whether this given

text sequence belongs to a positive sentiment or not.

Okay, so this kind of architecture is called as a many to one architecture.

And lastly, there's another kind of architecture which is called as many to many architecture which

is called as a many to many architecture.

So in the scenario of this, many to many architecture, the application is machine translation, which

is also a natural language processing task.

So in this scenario of machine translation So what happens is I'll be getting multiple inputs okay.

I'll be getting multiple inputs and using my multiple inputs I want to generate the multiple outputs

as well as a sequence.

For example, if I have the input as coffee is good, okay.

If this is my input and let's assume I want to generate the output, I want to generate the output in,

which is the output in a different language, which is a typical machine learning task.

Then it would be something like this.

Suppose the task is conversion from English to German then.

So what I'll do is for each input I'll be generating H0H1, h2.

Then using my h2 I'll go ahead and generate the output in the other language.

So maybe I'll generate the y3.

Here.

The y3 is nothing but cafe okay.

And I'll call it as h4 okay.

Cafe East.

Good.

So here in this scenario, this is an example of many to many architecture.

I'm taking multiple inputs and then I'm generating the multiple outputs.

So this kind of architecture is called as the many to many architecture.

Now this many to many architecture is also known as encoder decoder architecture.

Okay.

This is also known as encoder decoder architecture.

The reason that we call it as encoder decoder architecture.

Because here the input in one language is being processed and I'm generating a hidden state representation.

So this H2 that I'm generating this has the information of both H0 and H1 as well, which combines the

entire information that I have got in the source data.

And then using this information, I'm trying to generate the output in the other language.

So this is my encoded data.

And using this encoded data I'm decoding it in the other language.

So this is also known as the encoder decoder architecture.

So these are the various types of recurrent neural network architecture that we have got.

So in this recurrent neural network architecture, the core idea is we will be sending the data in a

sequence, okay, where each input that I have got okay is being sent to my neuron.

And when I when the next input or the next word in the sequence is getting processed.

So it will remember what has happened in the past.

So what was my previous input.

And based on that the outputs will be calculated.

So using this format we are able to learn the relationship between the features in an effective way.

So that is the advantage of this recurrent neural network.

Now this recurrent neural network has the applications in natural language processing that is like sentiment

classification, image captioning.

That is text generation.

It is used in machine translation.

It can be used in time series prediction.

So the applications are limitless.

But we have the issues with this recurrent neural network.

So what are the issues that we have got.

So the issues in recurrent neural network is so one major issue, which is one of the challenge issue

that one of the major challenge that we have got right now is this it is vanishing gradient descent

or vanishing gradient problem.

So what happens is, if you think about it, let's consider this example.

If you consider this many to many architecture, when I have the output at this time stamp that is cafe.

So at this point, if I want to update my parameters then I'll have to come backwards.

Okay, I have to come backwards in a sequential data and which means this H3 is made up of H2.

H2 is made up, is made up of H0.

So as the networks goes deeper and deeper, the initial layers will not get updated.

The gradients becomes small and small, and as a result of this, the learning of the long data sequences

will become difficult.

And the another challenge that we have got with this recurrent neural network is exploding gradient

problem.

So this is the other scenario.

If the slope tends to grow exponentially instead of decaying, then in that scenario we actually call

it as an exploding gradient descent.

And this would raise when the large error gradients accumulate, and this would actually result in large

updates to the neural network.

And as a result of this, my model weights will not be able to converge.

So these are the problems that we have got with the recurrent neural network.

Now for solving the problems of exploding gradient descent.

So we we have found some solution.

So if I represent those solution for exploding gradient descent maybe proper initialization.

So using this initialization technique we can reduce it or truncated back propagation.

Applying the truncated back propagation will also help us in reducing the issues related to exploding

gradient descent and gradient clipping.

So these are the possible solutions that we have got for the exploding gradient descent.

Okay.

So this is the one way.

So I'll mention it in orange color.

Okay.

Now when it comes to when it comes to vanishing gradient descent.

So the ways that we can ways that we can actually reduce this vanishing gradient descent problem is.

So initialization gradient clipping choosing the right activation function.

Usage of proper activation function.

And the.

Another way that we can reduce the issues of vanishing gradient descent problem, which is one of the

major problem, is usage of LSTM neural network, which is nothing but long short term memory.

Neural based network.

Okay, so this is the these are the ways in which that we can reduce the issues of the recurrent neural

network.

So you have looked into the problem.

And the solution is one way or the architecture that we got it as a solution is the LSTM neural network.

So this LSTM neural network will help us in reducing the disadvantage or the issues that we have currently

faced with the recurrent neural network.

So let's understand what is the advantages that we are getting with this LSTM neural network.

Okay.

So let's see how how it this is beneficial, which will give us more intuition in solving the issues

with the recurrent neural network.

So let's see in the next video guys.