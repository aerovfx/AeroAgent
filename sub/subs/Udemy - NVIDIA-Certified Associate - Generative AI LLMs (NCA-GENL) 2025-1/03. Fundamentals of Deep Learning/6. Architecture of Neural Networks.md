# 6. Architecture of Neural Networks

---

WEBVTT

In this video, we are going to look into the common network architecture when we are performing the

deep learning for various tasks.

Now, this is an intuitive way to understand how we would structure a deep learning neural network.

When we talk about a deep learning neural network here, we can have n number of neurons, and we can

have n number of hidden layers in a given data set.

But there are some key aspects which would change.

And there are some key aspects which would always stay constant irrespective of the type of the task

that we are currently doing.

So depending on the task, something would change and something would stay as it is.

So let's understand the common template whenever we are performing the various tasks using the deep

learning.

So let's start with the binary classification.

See, in the case of binary classification here the number of input neurons.

This is very important, and this is common across each and every task that you will be doing.

The number of input neurons will be one per input feature.

If you remember in the first task that we have done that is a multi-class classification, I had an

individual image which is of the shape 28 by 28.

That means 2828.

That's the number of neurons that we need to have in the input layer.

And in our case.

So we have ensured that we have created an input layer which has the shape as 28 by 28.

And on top of that we had applied the flattening operation.

So when it comes to the number of input neurons, this is one per input feature.

That is the input feature coming to the hidden layers.

See.

Typically it would vary between 1 to 5.

Again it depends on the problem and the data set that we are working.

A number of neurons per hidden layer.

This would typically be between 10 to 100.

Again this is depending on the data set that we are working with.

And this is very important when it comes to the output layer.

See in the case of output layer for the binary classification the number of output neurons is one.

Coming to the activation.

So it works on ReLU activation function and the sigmoid.

But typically we work with ReLU activation function in case of the hidden layer.

Coming to the output layer, the activation function that we will use is the sigmoid activation function.

The reason because here I'm performing the task of binary classification where I've got only two tasks.

Okay, that is two classes.

So as a result of this, the output activation function is already the logistic or the sigmoid activation

function.

Now coming to the loss function, since we are working with the logistic based output, that is the

prediction.

So the loss function which is suitable for this is the binary cross entropy loss.

So this is the loss function that we will be using in the case of the compile method.

So this is the basic architecture of the binary classification task of deep learning.

Now coming to the multi-class classification in the case of multi-class classification.

So this would include the number of neurons.

Even in this case the number of neurons is one per input feature coming to the number of hidden layers.

It depends on the problem, typically between 1 to 5 and number of neurons per hidden layer.

It also depends on the problem, typically 10 to 100.

Coming to the number of neurons in the output.

So in the output layer it is one per label.

If I'm doing the multi-class classification and if I'm having ten classes, then the number of output

neurons is one per label.

Okay.

So hidden activation function in my hidden layer.

Even in this case ReLU is the activation function that we would use.

And sigmoid is the second preferred one.

But typically we use the ReLU.

Coming to the output layers activation function.

Since it is a multi-class classification, the output layer will have the activation function of softmax

and coming to the loss function, the loss function is categorical cross entropy.

So that's the loss function that we will be using in the case of multi-class classification.

Lastly, talking about regression, in the case of regression the output is very important.

The output here.

In the case of this regression, if I come directly to the output, see.

In this case there is no activation function okay.

There is no activation function.

So we'll generally do not specify any activation function.

If I want only the positive and the zero output then I can make use of the ReLU.

Now coming back from first here in case of input layer it is one per input feature.

Hidden layers is depending on the problem, 1 to 5 number of neurons per hidden layer is 10 to 100 and

coming to the hidden layers.

So in majority of cases we will make use of the ReLU activation function.

And the loss function is mean squared error or mean absolute error.

So these are the various loss functions that we can use it whenever we are constructing our deep learning

neural network.

So this was about the general architecture that we follow whenever we are creating a deep learning neural

network.

So I'll stop the video over here and let's continue in the next video.