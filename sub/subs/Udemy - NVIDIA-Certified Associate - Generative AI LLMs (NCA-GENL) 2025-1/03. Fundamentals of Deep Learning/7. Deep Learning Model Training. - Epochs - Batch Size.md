# 7. Deep Learning Model Training. - Epochs - Batch Size

---

WEBVTT

In this video, let's have a quick recap on one of the important topic that is batch size and the epochs.

So here we have already discussed about this batch size and the epochs in our previous learning video.

When we have built the intuition around the deep learning considering the importance of this topic.

So let's have a quick recap and a better understanding about this batch size and the epoch.

See, when we talk about an epoch, one epoch is when an entire data set has completed both forward

pass and a backward pass through the entire neural network only once.

So in my entire data set, if I perform forward pass and a backward pass, then I'll say one epoch is

completed.

Now remember, by increasing the number of epochs, it would mean that we are going to enable the neural

network to change the parameters to a greater number of times.

So as a result of this, we are going to have the neural network to perform better.

So that's the core intuition behind this e-box.

Now apart from this epoch, we have something called as a batch size.

Here batch size is nothing but the number of training instance that will be used by the model in order

to complete one forward pass and one backward pass.

So I'll have my data set.

From my data set I'm going to select an individual batch of data.

After I perform this selection of this individual batch of data, I'm going to perform one forward pass

and one backward pass in this neural network.

That is when I'm going to say that one batch of data have actually performed the activity of forward

pass and a backward pass.

So that's how we select a batch of data and perform this activity.

And we also have an activity called iteration.

What does iteration would mean.

Iteration means number of passes that has been performed on a network.

So one pass is nothing but one forward pass and one backward pass.

I know this, all these things are pure technical.

Let me help you to understand this with a better example.

Let's say I've got a data set in my data set.

I've got 20 instances.

And I'm going to say that the batch size is five.

Okay I'll say batch size as five, which means I have got 100 rows in my data set each time I'm going

to pick five row.

So if I'm picking five row then in my one epoch I'm going to have 20 batches.

The reason?

Because 100 by five is equal to 20.

Now each batch will pass through the algorithm.

So there will be 20 iterations per epoch okay.

20 iterations per epoch.

Now let's suppose if I have the number of epochs as three, which means the total number of iterations

is given by 20 into three, that is 60.

So this is the relationship between batch size epoch and the number of iteration.

Okay.

All these three things are the different different terminology.

And it conveys different meaning whenever we are implementing a neural network for the deep learning.

All right folks.

So with this we come to the end of this video.

Now in the next video let's talk about the hyperparameter tuning in the case of deep learning.