# 16. Attention Mechanism

---

WEBVTT

All right.

Now in this video, let's learn about the attention mechanism for the task of sequence to sequence learning.

Now when we talk about this attention mechanism, there's one research paper which you have to know

that's called as attention is all You need.

But before you go over there, that is the attention all you need research paper.

There is a important concept that you need to understand the that is the how does the attention would

happen in case of the sequence based learning.

So let's first understand the attention mechanism.

And this attention mechanism is being explained in the research paper which is named as neural machine

translation by jointly learning to align and translate.

So if you look into the abstract so the abstract clearly says neural machine translation is a recently

proposed approach for machine translation.

Here, Unlike the traditional statistical machine translation, the neural machine translation aims

at building a single neural network that can jointly tuned to maximize the translation performance.

The models proposed recently for neural machine translation often being belong to the encoder decoder

architecture, which is many to many architecture, and to encode a source text into a fixed length

vector from which a decoder generates a translation.

So in this paper, what they are trying to do is they are trying to use a fixed length vector.

Okay.

So this fixed length vector that we get this would actually be termed as a bottleneck.

That is what they are saying right now.

The reason that we call it as a bottleneck, because if you look into the many to many architecture,

this h to whatever the output that I generate right here from this H2, this is the fixed length okay.

This is of fixed length.

And using this fixed length I'm generating the output which is the translation in the other language

for the decoder.

So this actually becomes a bottleneck a bottleneck which means I am limited by the length of the output

of this encoder, which has the responsibility to learn all the inputs.

Hence we call it as a bottleneck.

Okay, so hence it is called as a bottleneck.

So what they are trying to do is as part of this research paper, they are proposing to extend this

by allowing a model to automatically search for parts of a source sentence, which are relevant for

predicting the target word instead of limiting to the single vector.

As part of this research paper, they have proposed so that we can get more information and whenever

we are generating the decoder, that is when we are trying to decode it at that time, we will pay attention

to the words that we are interested in.

Okay, with this approach, they were able to achieve the translation performance, which are comparable

to the existing state of the art phrase based system of on the task of English to French translation.

So this is one of the important approach which give raise to the transformer architecture.

So this is very important that you understand this research paper at a very high level.

So being this an advanced one I'll try to simplify it as much as possible so that you will have the

necessary insights from the research paper.

And you know what would happen in case of advanced transformer architecture.

Now let's go through this scenario.

See when we talk about this encoder decoder architecture.

So when we talk about this encoder decoder architecture this will be a many to many architecture.

And this will be composed of recurrent neural network.

or it can be composed of LSTM neural network okay.

It can be either composed of LSTM or RNN and using it we will be trying to perform the activity of encoder

to decoder architecture.

So if I want to represent this uh encoder decoder architecture, the way that I'll be doing right now

is we'll consider this example.

That is the encoder decoder architecture.

So let's let's mention these steps okay.

In case of my step one okay.

In case of my step one.

So what I'll do is I'll be generating the input.

So these are the two inputs that have got.

These are the three inputs that I've got.

Coffee is good.

So you are aware of the encoder I'll just say step one of decoder.

In the step one of decoder I'm going to remove these parts okay.

So I'll just keep a note of it I'll copy this okay.

And I'll mention and I'll keep multiple parts so that you will understand the how exactly the data is

getting flown.

Okay.

So in the step one of decoding.

So what we will have is we will have the output that is H2.

This H2 will be used in conjunction with this H2.

So I'll be going ahead and generating the first output that is Cafe.

So this is how I'll be performing the decoding.

So I'm going to use this H2 okay I'll use this H2 over here I'll represent it here I'll use this h2

using my H2.

I'll go ahead and I'll generate the decoder.

I mean I'll be generating the first output from the decoder.

So this will be my output from the decoder RNN and the first neuron from the decoder RNN.

Okay.

Now once this is done the step two of the decoder is something like this.

So for the step two of the decoder what we will do is.

So here we will still have the H2 over here.

Using this H2 I'll be generating the next word that is the k faced.

And my step three of decoder over here.

In this scenario in the step three of my decoder, I'll be using my H2 and using my H2.

I'll be generating the third word that is k for east.

Good.

Okay, so as you can clearly see, the entire dependency of generation of the output from the decoder

is dependent on this H2.

That is the last hidden state from my encoder.

Hence we call this as a bottleneck when we are performing the task of sequence to sequence training

task.

Now, what do we propose in the scenario of attention?

So in the scenario of attention, the way that we are proposing is a little different.

The.

So what we will do is so I'll add a heading so that you'll be able to clearly understand what is happening

in the scenario.

Okay.

I'll write it as with attention mechanism.

So with attention mechanism.

So the way that we would deal it right now is.

So let's have let's look into the step one of attention.

So let's say step one of decoding.

So in the step one of decoding I'm interested in generating the output of this H3.

I'm interested in generating the output of H3.

Now, in order to generate the output of H3 and the first word that is Cephei.

So what we will do is so we will make use of all the previous hidden state.

So it clearly means that.

So here when I'm generating the output of H3 I'll be using my H2.

Okay.

And I'll use my H1 and I'll be using my H0.

So instead of using one I'll be sending H0.

Okay.

H1 and h2.

So all these three data will be used as an input for my H3.

Okay.

Based on these three inputs, I'll be generating the output.

So let's say if I'm generating the decoder if I'm generating the decoder as cafe.

Now cafe is actually for the word coffee.

So that means in this scenario the h zero will have the highest preference to mention the highest preference.

I'll just write it in a different color.

So I'm using all three h, zero, h1 and h2.

But h1 will have the highest preference.

So at this time my decoder has the ability to refer to all the previous hidden states.

Okay, this is a scenario for the step one.

Now in the scenario of my step two.

Okay, in the scenario of this step two of my decoder.

So what we will have is this time.

So the second word is east.

The second word is east.

So for the second word is the word that is important for me is ease.

So I'll be giving more attention.

I'll be providing more attention on this H1 compared to H0.

And so when I'm generating the step two of my decoder at that time, I would still have the access to

my all the previous hidden state and I can pay more attention to the word, which is of my interest

in the same way for generating the last output, that is, for the generating the last output in this

scenario, that is good.

So at that time I'll be I'll be paying more attention to the word good compared to the other hidden

state.

So each time when I'm generating the output for my decoder, okay, each time when we are generating

the output of this decoder, at that time, we will have the choice to to decide as where exactly that

we want to pay the attention to.

So in this scenario, while generating the output at the third state, I have the complete control as

I want to pay more attention to the word good and I'm generating the output as good.

So I want to give more importance for the word ease.

If I'm generating the output as east, and I want to give more importance for the word coffee if I'm

generating the output as cafe.

So this is the overall idea behind this attention mechanism in a sequence learning technique.

So that is what has been explained in this scenario as well.

So this is the diagram that has been represented in the research paper X1X2X3X4 up to XT.

These are all the input state and h1 h2 h3 h4 up to h3.

These are all my hidden states now y t minus 1YT.

So what it says is you can think of the above part as the decoder part.

So when I'm generating the output from for my decoder I'll have an additional layer, which is an activation

layer, which will help me to focus on the individual hidden layer parts.

So for at each decoding step, I'll have the option to decide to which hidden state from the encoder

that I want to pay more attention to.

So this is the overall idea behind this attention mechanism in the sequence to sequence based model.

And you can refer this research paper in detail.

They have actually described how exactly that they were able to achieve.

And at the end of this research paper, they have also mentioned how the attention is actually working.

For example, for the word area, okay.

For the word area, they are actually paying more attention to the last zone or when I'm generating

the output as last zone as the translation translated word, I'll pay more attention to the word area.

Okay, so e t a.

So for that I'll be paying more attention to the word was signed.

So each time when I'm generating the output from the decoder, I'll be paying more attention to the

specific parts.

So this would help in overall remembering the data for the long sequences.

So this is the core intuition behind this technique of the implementation of attention mechanism.

I'll just come down and see whether we have any other important parts that I want to highlight.

Yeah.

So I think we have covered the important parts of this research paper.

The key thing that you have to remember from this research paper for your advanced natural language

processing is whenever we are decoding it.

So the decoder will have the ability to pay more attention for the required parts from the encoder so

that it can perform the decoding in a much more effective manner.

So this is the overall idea about this encoder decoder architecture with attention mechanism.

Okay.

This is the core principle behind this attention mechanism.

Now that you have understood this attention mechanism, there is one last bit about the transfer learning.

So what exactly is this transfer learning and how this transfer learning actually helped us in the field

of natural language processing, which gave rise to the building of transformers.

So let's talk about the transfer learning approach in the next video.