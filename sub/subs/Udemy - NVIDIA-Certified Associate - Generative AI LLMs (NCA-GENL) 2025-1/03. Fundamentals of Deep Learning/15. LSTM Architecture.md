# 15. LSTM Architecture

---

WEBVTT

So in the last session, you have understood that.

What are the disadvantages in recurrent neural network.

And the solution is LSTM neural network which is a special kind of neural network.

So let's understand what's so special about this LSTM neural network.

First of all let's understand the full form.

So LSTM is nothing but long short term memory neural network which is nothing but the long short term

memory neural network.

This is so this LSTM is a special kind of recurrent neural network.

And this is capable of learning the long term dependencies.

One of the main challenge that we had with recurrent neural network is when the sequence becomes bigger

and bigger.

My network was not capable of learning the long term dependencies, that is, dependencies from the

previous word to the current word.

This was one of the major challenge with recurrent neural network, Hence.

So researchers analyze this and they they came up with a solution that is called as the long short term

memory neural network.

And this network has been specifically designed to avoid the long term dependency problem.

So here when we talk about the recurrent neural network.

So in the scenario of recurrent neural network it's a chain of processing of the data set.

It just process the data from one neuron to the another neuron, which is a chain like structure which

we have seen earlier.

But what we will do is instead of direct hit zero that you see right now inside that network.

So we make use of various gates okay.

We make use of various gate.

So you don't have to learn deep about this LSTM neural network because we are not going in deep from

the from the NLP perspective as we are currently using the the Transformers and the other logic.

But let me just give you a quick idea about what is happening and why it is an important and why it

is a best one compared to the recurrent neural network.

So in this LSTM neural network we deal with various kinds of gate.

So the first step or the first gate that we have got is the forget layer or the forget gate.

Okay.

The for the first gate is what we call it as a forget gate.

So let's understand what exactly is this forget gate.

Now forget gate.

Is this individual part okay.

If I want to highlight it So I can just highlight it and show it to you.

So this highlighted part that you have seen that I've just did highlighted right now.

So this is my code for gate gate.

Okay.

This is my code for get gate.

Now what is the role of this forget gate is the forget gate is to is to just forget the things which

is not relevant for me while processing the current input.

Okay, this is responsible for removing the unnecessary information before it gets the new data.

So that's the overall idea right now.

And the other gate that we have got is the input gate.

Okay.

This is the other input gate that I have got right here.

Now here the input gate responsible for getting the input and whatever the information that is present

in my current input state, that will be calculated and it will be added for my long term memory.

Okay.

So here the input gate is the next gate that we will have okay.

So we will have the input gate.

The input gate is responsible for getting the current input and processing the current input data,

and adding it for my long term state.

And then we will have one more gate that's called as the output gate.

The output gate is responsible for getting the data of my current state.

Okay.

It is responsible for getting the data from my current state and generating the output at my current

time stamp.

So these are the important gates that we will have in the LSTM network.

So with the help of all these gates we are able to effectively process the data.

Okay.

So this is the overall idea behind this LSTM neural network.

We have got the forget gate which helped me to forget the things which are not important for me.

We have got the input gate which takes the current input and then generates the output.

And we have got the output gate which will generate the output at my current timestamp.

To help you understand this, suppose if I've got a text that says as dog was crossing the road and

it was very tired, let's consider this example.

Now if I've got an example text like this, what it says is.

Let's consider for the demonstration purpose I'm processing the word that is called as the road.

okay, and processing the word that is my road.

That is what I'm currently doing right now.

Okay, I'm processing the word, which is.

So I'm processing the word that is road right now.

So when I'm processing the word that is road, then here while processing this current word of road,

the words which are not important for me is this.

Okay, so while processing this word off road, okay, maybe the information from the previous previous

data, that is the word input is not having any importance for me because the word the okay, the word

the is actually a kind of a, uh, filler word or a stop word.

So I can ignore this.

So this removal of the information from the word the is being done by the forget gate and then by getting

the input and processing.

This current input will be done by the input gate, where I'll get the current input.

I'll add it to my long term memory because dog is currently crossing the road, and if I want to generate

the output at the current sequence, then I'll use my long term memory as well as my current input,

and I'll process the output from the output gate.

So we will always have a long term memory line that goes on.

And this will remember the long term dependencies that we have got on the text data.

So this is the overall idea behind this LSTM neural network okay.

So this is the overall idea team.

So with this LSTM neural network.

So this has got wider implication and this has got wider application.

This was used in various applications like Google search learning, the patterns in the data sets or

all these things were being utilized, and we replace the recurrent neural network with the LSTM neural

network.

So that was the great thing about this LSTM neural network.

And more importantly, we started using this LSTM neural network in the complex task like sequence to

sequence generation.

So sequence to sequence generation is nothing but the many to many architecture.

So in this many to many architecture, what we will have is we'll get multiple inputs and we will generate

multiple outputs.

That's the task where we wanted to focus more at that time, because this is one of the major challenge,

and this is one of the task where we want to progress more.

So the applications like machine translation were came into picture and we started using this many to

many architecture using the LSTM.

So we started using it and we started seeing a lot of benefits over there.

But we still had the challenge.

Even though we were making use of LSTM neural network, we had the challenge of long term dependencies,

even though this the long term dependencies were being captured.

When we talk about sequence to sequence generation, the long term dependencies were still being a challenge,

especially for this kind of task.

So what researchers came up is okay, they have analyzed the issues that we have got and what researchers

came up is okay.

Instead of analyzing this sequence to sequence task directly with the help of encoder decoder architecture.

So encoder decoder architecture is this.

This is my encoder decoder architecture which is a many to many architecture.

So instead of uh, instead of uh using directly which you see right now okay.

So instead of using this encoder decoder architecture like this, let's have one more scenario that's

called as an attention.

that's called as an attention.

And this gave rise to a research paper by the Google team, which is nothing, but attention is all

you need.

So that's the research paper.

And this changed the way that we looked into the task of this sequence to sequence processing.

So we had a challenge with LSTM.

Now we've got a solution that is the attention mechanism.

And this attention mechanism has been introduced from the research paper of attention is all you need.

So I'll stop the video over here.

In the next video let's look into this research paper of this.

Attention is all you need.

And let's try to understand what is the benefit that this is bringing into the table and how it is beneficial

for us when we are processing the long sequences.

So we learn about it in the next video.