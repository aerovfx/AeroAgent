# 9. Vanshing & Exploding Gradients - Initializations, Regularizations

---

WEBVTT

In this video, we are going to talk about the vanishing and the exploding gradient problems that we

are going to face whenever we are working with deep learning neural network.

See, when we talk about the deep learning neural network, we know that the back propagation algorithm

is the heart of this neural network.

Now when we talk about this back propagation, what happens is here we are going to have the features

and these features propagates towards the forward way.

That means it is going to propagate forward to get the output layer.

And at the end we are going to find the value of loss.

Now while finding the output in each neuron, it will get the transformation with the linear transformation

followed by the activation function that we have specified in each hidden layer.

And then it will reach the predicted output and from my predicted output, we are going to calculate

the training error or the training loss.

Now using the back propagation algorithm, we are going to propagate the values of the gradients backward

from my output layer to my input layer.

Calculating the error gradients on the way.

Now once the computation of the gradients of the cost function with respect to each parameter, that

is, weights and the bias term in my neural network is complete, the algorithm takes the gradient descent

step towards the minimum value to update the value of each parameter in my neural network using the

gradients.

So this is a key idea of the back propagation technique.

Now when this is happening, we are going to face two types of two types of problem.

One is a vanishing problem.

And the another problem that we are going to face is the exploding problem.

So let's start with vanishing problem as the back propagation algorithm advances downwards, or the

backwards from my output layer towards my input layer, the gradients become smaller and smaller, and

at some point it would approach zero, which eventually leaves the weights of the initial or the lower

layers never changed.

So as a result of this, the gradient descent never converges to the optimum.

This is actually known as the vanishing gradient problem.

So as the back propagation algorithm reaches the initial layers, the gradients will become actually

zero.

So if it becomes zero, you know already the formula.

So let's suppose theta one is the weights okay.

Theta one is equal to theta one minus eta times del by del theta one.

So if this is actually zero so this is nothing but theta one is equal to theta one.

There is no update in the parameters.

If there is no update in the parameters my model will not be improved towards the optimum value.

So this is the problem of vanishing gradient.

And we have something called as exploding gradient.

So in some cases the gradients keep on getting larger and larger.

So it keep on getting larger and larger as the back propagation algorithm progresses.

So as a result of this, very large updates will happen on my weights and the bias term, and this would

cause the gradient descent to diverge.

And this is known as the exploding gradients.

So these are the two common problems that we face in the case of vanishing gradients and the exploding

gradients in case of the training of deep learning neural network.

Now the solution for this is one is the proper weight initialization.

So proper weight initialization.

So this is one solution.

So in this case as per the researchers that is submitted by the various researchers like Xavier, Anthony

and Yoshua.

So they have actually proposed various initialization technique.

So here in the case of various initialization technique the suggested are usage of Xavier initialization

or Glorot initialization.

So using these various initialization technique.

So it has been found that the vanishing and the exploding gradients problem can be minimized.

Okay.

Now whenever we are working.

So inside the dense layer we will have the option to specify the kernel initializer.

And using the same we can actually initialize the values of this.

Uh, I would say individual hidden layers.

Okay.

Then the other methodology is usage of non-saturating activation functions.

So here in this case.

So what we can do is okay, especially whenever we are facing the vanishing gradient problems.

So in such cases it is observed to and it is recommended to make use of non-saturating functions like

ReLU and its alternatives.

Because if you make use of the activation functions like sigmoid or tan h at that time, we are going

to issue the chances and the problems of saturating activation functions.

Okay, you have already learned in detail about this activation functions.

And the other solution is batch normalization.

So in the case of batch normalization here what we'll be doing is before the data moves from one layer

to the next layer, whatever the data that gets passed through.

So we will perform the normalization of that given data.

Okay.

We will perform the normalization on the data by referring to the mean and standard deviation of that

batch.

Hence we call it as a batch normalization.

And the other technique is gradient clipping.

Here with this technique of gradient clipping.

So what we can do is we can say that if the value of gradient is less than the minimum set threshold

value, that is the clipped value, then we will use the minimum value.

And even on the other hand, if it is growing above then the upper limit that I have set, we can also

clip at the upper level as well.

So this is called as the gradient clipping.

Okay.

So with this we come to the end of this video.

So as part of your learning journey from this video you have understood the two common problems that

we generally face when we are training the deep learning neural network.

And now you have also learnt about the solutions for this reduction of vanishing and the exploding gradient

problem.

So we'll stop the video over here.

In the next video we are going to discuss about the another neural network architecture that is the

convolutional neural network architecture.

So I'll see you next time.