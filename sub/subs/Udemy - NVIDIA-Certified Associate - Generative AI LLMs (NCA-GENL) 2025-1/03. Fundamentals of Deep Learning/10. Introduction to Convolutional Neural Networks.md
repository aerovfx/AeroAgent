# 10. Introduction to Convolutional Neural Networks

---

WEBVTT

In this video we are going to learn about the Convolutional Neural network.

Now, before we go into this convolutional neural network, first you need to understand one of the

important term that is computer vision.

Because we make use of this convolutional neural network whenever we are performing the task related

to computer vision.

So what exactly is this computer vision.

See, when I talk about a vision, what is a vision?

Vision is ability to see the outside world.

And when I say computer vision, it is about giving the same ability to the computer about what is happening

in the outside world.

Now, obviously computer is a non-living being, so we have to give the ability to see the outside world.

And also we have to give the ability to process that information.

So here in case of computer vision.

So it's about giving the ability for the computers to see the outside world.

And how do we give.

We give it with the help of the hardware software and the analytical ability.

Now when I say hardware what does the hardware mean?

Hardware would mean a webcam camera to capture the outside world software.

It is nothing.

But, uh, it is going to help us in convert the image or video into a digital format.

And when I say analytical ability, that is where the deep learning neural networks like CNN comes into

picture, which gives the ability to understand and identify what is present in those given digital

image data.

So that's the overall idea about this computer vision.

And here, in order to perform this computer vision in an effective manner, we make use of this convolutional

neural network.

Now, before we directly go into this convolutional neural network, let's understand at a very high

level what does a convolution would mean.

Now when I say convolution, if I if I want to describe in a mathematical way, from a mathematical

perspective, convolution is nothing, but it's a mathematical operation on two functions.

Let's say f and g.

And this is going to produce a third function, which is going to express as how one has actually modified

the other shape.

So it's it's a two function, it says as how one has actually modified the other.

Now talking in terms of the task of image processing, we can say that it's just a manipulation of the

pixel values of an image, and that is what a convolution operation is.

Now, if you think about it and look into the definition, it might sound complex, but let me tell

you, you're actually using it in your day to day world here.

If you just think about it whenever you click any image.

So what do you normally do.

So you're going to open your favorite photo editing application.

And then you're going to edit the images.

Isn't it in the same way.

So I have actually clicked the image.

This is the image that you see before.

That is before the application of convolution operation or before the application of any special effect.

And if you look into the other side, that is after the application of special effect.

See, I was able to clearly identify the water bodies in the after image compared to the older image,

which means identification of water bodies is much easier in the second image compared to the other

image.

And here in the second image, even I am able to identify there are all green regions that are present,

which was difficult to identify in the old image.

So this operation of applying the effects or applying the special operation is what we call it as a

convolution operation.

As you have clearly seen, the reason that I have actually applied this operation on this given image,

because I want to make the body of the object that we are interested to become more clear by increasing

the brightness, by decreasing the blur.

And Anyone?

We want to highlight the objects by making it more visible and so on.

So these are the key things that we will be interested in, so that we can understand what are all present

in the images in an effective way.

Now that is the core principle behind this convolution operation.

And the same thing that we do it in real life as well, isn't it?

When we take any selfie before we upload it on our social media platform, we are going to edit the

image to to ensure that we are looking good on that image.

And then after we have ensured that we have added all the relevant special effects over there, then

we are going to use it for our purpose.

And this process of editing is what we call it as a convolution operation.

That's all guys.

Now, when we perform or when we apply this special effect, something is going to happen on these images.

To help you understand this, let me give you an example.

Let's say this is an image that have got.

Now on this image, assume that I'm going to apply some special effects.

So I'm going to apply the filters and I'll apply the filter let's say vivid warm.

So if I apply the filter it has changed the pixel intensity of the images.

Now let us assume that if I select the filter of mono, if I select the filter of mono, now the entire

image has been changed into a black and white form, isn't it?

If I select the filter as dramatic, cool.

So here if I select the filter of this dramatic tool, it is adding some cool theme over here.

So the idea is each filter is resulting in the different set of images that I see on the screen.

Okay, each filter is actually resulting in how my is actually changing as how the various objects in

this image would look like.

Now, if I want to talk about it in a technical way, I can say that when I apply the various filters,

it is actually modifying the pixel intensities of the image.

So it is modifying the pixel intensities of the image, and that is what is happening when we apply

any filter.

All right.

So now the question is how does these filters will be applied and why does it modify the pixel intensities.

So let's understand the mathematics behind it.

So how do we perform this mathematically when we are applying any special filters.

So what we do is in case of this convolution operation on top of my input data I'm going to select a

kernel.

I'll place my kernel on top of my image.

And I'm going to slide my kernel left to right and top to bottom.

I'll slide my kernel left to right and top to bottom.

And once I slide my kernel left to right and top to bottom, then while sliding it I'll take, I'll

take some of the mathematical operation.

The operation that we perform is a dot product.

I'll take the dot product between my image region and a kernel region, and whatever the output that

I get, that's the output of this specific operation of convolution.

Okay.

That is how we perform it in a mathematical way.

To give you an example, let's say I've got an image that you see on the left hand side, which is a

Taj Mahal image.

Now on this image, if I apply any convolution operation, assuming that this is how my pixel intensities

would look like for a kernel.

So I'll take this kernel.

I'll place it on this image okay.

And I'll move left to right and top to bottom.

And as a result of this here, this is how my transformed image would look like.

Now if you look into the transformed image, that is the output image that I'm getting right now from

this output image, it is very easier for me to trace out the edges and the overall structure or the

overall outline of this architecture, isn't it?

I can clearly say where exactly the doom is from this edges.

I can easily trace these edges, which was not clearly visible in the original image.

So by applying these kinds of convolution on the image data, the computer tries to learn the edges

and the patterns that are present in the images so that it can effectively understand what is present

in an image.

Now, as I mentioned, when the system is performing the convolution, this is how it is going to perform

this convolution operation.

Here the system is going to take a kernel and place on top of the image region.

So below behind I have the image region okay.

And on top we have the kernel.

So we'll place the kernel and we will move around left to right and top to bottom.

And once we move around left to right and top to bottom, whatever the outcome that I get the outcome.

Pixel intensities.

This is how the output pixel intensities would look like.

Now here the operation that we involve during this operation, that is the convolution operation is

the dot product.

Now this is an illustration of how it actually works, especially during the convolution operation.

What happens in this case is here in this case of the convolution.

So let me consider an example and I'll explain it for you.

So consider this image of convolution operation.

Now I have mentioned that I'll place my kernel on top of my image region.

And I'll take uh, the dot product between image and the kernel region.

So let's see whether it is correct or not.

So here the bigger numbers that you see these are all the pixel intensities of an image data.

And the bottom ones that you see right here.

These are all the pixel values.

That is the kernel values okay.

The kernel values and the pixel values.

So what I'll do I'll take a dot product between the image pixels as well as the kernel value which is

nothing but two into zero plus one into one plus zero into two plus one into two plus three into two

plus one into zero plus two into zero plus two into one plus three into two.

So if I do the computation two into zero is zero, one into one is one.

This is zero.

This is two okay.

This is six.

This is zero.

This is zero.

Okay.

This is two.

This is six.

So six plus two, eight, eight plus six is 14.

14 plus three is 17.

And that is what I see over here as an output as a result of application of this convolution operation.

So I'll take the dot product.

Dot product is nothing but the element wise multiplication between the kernel values and the pixel intensities.

And I'll perform the sum.

So this is called as a dot product.

So this dot product operation will be performed so that we can manipulate the image data.

Now here when we are performing the convolution operation in this case okay, we can see that my original

image is of the shape five by five.

And my new image is of the shape three by three.

That means the dimension of the image is actually reducing when we apply this convolution operation.

But if you think about it in real time.

So in that case, okay, in that case when we think about it in real life.

So when we apply any filter on an image data, we do not see the size change for that individual image,

isn't it.

The sides would stay as it is.

Now, what happens in those cases is we make use of a dummy layer around my current layer.

We call it as a padding layer.

So this is an intuition of convolution operation with a padding layer.

Now in this case I'm adding a dummy layer which is a padding layer.

Now by applying this padding layer I'm ensuring that my original image shape is actually retained even

after I perform this convolution operation.

So the shape will be retained even after the application of convolution operation.

Hence we like.

Hence this padding helps us in retaining the shape of the image data.

Now apart from padding, we also have something called as stride.

Now stride talks about the step size.

Okay, stride is nothing but the step size in the previous.

In the previous case, I was moving one step at a time.

Okay?

I was moving one pixel at a time, or I was moving one step at a time from left to right and top to

bottom.

Now in this case, I'm making use of stride is equal to two.

So with stride is equal to two I am I'm moving two pixels at a single time.

Okay.

That is the overall idea about the stride.

Now this operation that you see right here, the same operation will be applied on an image color image

as well.

So in case of the convolution operation on a cover color image, this is how the intuition would look

like.

I'll perform the same thing.

Okay, I'll place the kernel, uh, on my input image.

I'll generate the transformed output.

Let's assume that I've got three color channels RGB, and I'll perform the activity of convolution.

Whatever the outcome that I'll get, I'll perform the summation.

So this is going to result in the overall output of this convolution.

So this is the idea about the convolution operation on an color image data.

Okay.

So one thing for you to remember right now is when we talk about a convolution operation, we'll take

a kernel and we'll place it on top of the image data.

We'll slide the kernel from left to right and top to bottom.

And we will get the transformed pixel intensities of the image data.

So this transformed pixel intensities of the image data has a better representation of the image or

the important parts of the image compared to my original image.

To give you another example, on the left side, I've got the original image on.

I am applying a horizontal Sobel kernel as an example, and as a result of application of this kernel,

I am able to identify the edges in a much better way compared to my given image data.

Okay, this is the overall idea about the application of kernel.

That is the convolution operation.

Now in this convolutional neural network, we also make use of another layer that is called as the pooling

layer.

We use this pooling layer in order to summarize the image data and reduce the dimension of the image

data.

Now what we do is in case of pooling, there are actually two types of pooling.

One is maximum pooling and the other one is the average pooling.

Now in this case so we are actually applying the maximum pooling.

So what we do is in this case of maximum pooling.

So I'll take my image data okay.

And I'm going to define the pooling region I'll define my pooling region.

And once I define my pooling region here I'm going to say that okay.

In that pooled region what is the average of those values.

If I take the average then I'm going to call it as an average pooling okay.

If I take the average then it is an average pooling.

So if I take the if I take the maximum out of it, then we call it as a max pooling.

Now in this case I've selected a pooled region.

This is the two by two pooled region.

Now on this two by two pooled region I'm taking a maximum of this two by two region the maximum value

is five.

So in this way we perform the activity.

And this is called as a max pooling.

So these are the various types of pooling layers and the convolution layers that we would come across

when we are building the convolutional neural network.

The way that it would fit on our neural network is first we will have an input layer.

And for this input layer from this input layer, we get the image data as an input.

On top of this we will apply the series of convolution layers.

Then after we apply the series of convolution layers, then we will pass this convolution layers into

let's say pooling layers followed by convolution layer and at the end.

So we will apply a kind of an activity that's called as a flatten layer.

Okay.

This is going to convert into a single dimension output.

Then we will convert it into a dense layer.

And at the end, depending on the task that we are doing, we are going to define the number of neurons

with the dense layer and the relevant activation function.

So this is the overall structure of this convolutional neural network would look like you'll get a better

understanding as we start implementing it in the next video.

So in the next video we are going to implement the convolutional neural network on one of the very famous

dataset that is the cat dog image dataset.

So I'll see you in the next video.

So let's take up the hands on journey.