# 11. Implementation of CNN on CatDog Dataset

---

WEBVTT

In this video, we are going to implement the convolutional Neural network on a very famous dataset

that is the cat dog dataset, which means here we are going to build a classifier on the image data.

Now the dataset related to this notebook is already shared to you.

So if you extract the zip file that I've shared with you, you'll be able to see a zip file.

Dogs versus cats dot zip file.

You can use this local copy.

Or if you are running in Google Colab then just run the same code that I've shared over here.

So I'm actually downloaded a black backup backup copy on my Dropbox.

So this is the link of the Dropbox.

You can copy this link and paste it on the browser in case if you're having trouble in downloading the

code.

Okay.

So you can use it in this way and click on download.

So in this way you can download the files using the Dropbox link.

So in my case.

So I'm mentioning the Dropbox link for working in this Google Colab server.

And I'm performing the unzip.

And after I perform the unzip I'm removing the zip file.

And after that we'll perform the unzip on the train dot zip and test one dot zip and removing those

respective zip file.

And at the end this would result in the file structure where I'll have the dog dog images as well as

the cat images.

So let's wait for download to complete extraction to complete, and then we'll take a look into this

data set.

In the meantime, I'm going to load the necessary libraries.

So here I'll be loading the numpy library pandas library matplotlib seaborn library.

And in this case, since we are dealing with multiple images.

So I'm going to make use of image data generator.

Now to process my given data set.

I'm making use of two underscore categorical function and load image for loading the image.

And I'm using this train test split function to split the data set into two parts.

Now once this is done.

So I'm going to define by variables by saying as image width is 128, height is 128.

Okay, so image channel is three and I'm specifying my image size as image RGB size image width comma

height comma three.

So what it means is when we are working with image data.

So we expect that all the images should be of same shape.

And because of that reason to keep all the images of the same shape.

So I am I'm setting the image shape as 128 by 128 by three.

Now here, if you observe the folder structure at the end, I've got a train folder and a test one folder.

So this data set was originally downloaded by the Kaggle.com.

Hence you see the another file that is sample submission dot csv.

So we are interested in training data set if I expand this train folder the file of the individual image

is mentioned as class name dot image id followed by the image extension.

So what I can do is I can go through each and every file name that I've got in this folder, and then

once I've got every file in this folder so we can extract those class names.

So here this is the code where I'm going to extract the complete file names of this folder.

Then I'll get the individual category when I perform the split.

And after that I'm saving that category along with the file name on this data frame.

So if I display my data frame this is how the data frame would look like.

Dog.

This is the image file name and this is the category of that image.

Okay.

Now let's have a look into the overall distribution of each class.

I'll say data category.

Okay I'll say value underscore counts.

So we'll look into the frequency of each and every class.

So it is 12,000 512,500.

So which means in total I've got 25,000 images for each class.

We have got 12,500 images.

Now, what I'll do is I'll start by getting a sample image and we'll create a visualization for a sample

image.

So I'll start by writing as import random.

Okay.

Now once I import this random module I'm going to say sample is equal to random dot choice.

And I'll get a choice from the file names.

Okay.

File names is actually a Python list as you know.

Now once that is done I'm going to mention as image is equal to I'll use the function that is load img

and I'll say train a slash I'll get the sample okay.

so train is a folder name that we have got over here.

So inside this folder I'm specifying the sample file that have loaded.

Okay.

Once I load this image next I'm going to make use of plt dot I am show function.

And I'll mention this image and plt dot show.

So this is going to show a random image.

So this is one of the random images that has been loaded from this dataset.

If I execute the same cell so I'll get a different image okay.

This is an image of dog.

So this is how the data set would look like.

Now once this is done we are going to split the data into two parts training set and the test set.

So training data frame and the validation data frame okay.

Now once I split the data into two two parts okay I'm splitting in the ratio of 8020, which means I'll

have 20,000 rows on the training data, 5000 rows on the validation data.

And if I take a look into my train DF and display first few entries.

So this is how the first few entries of this train DF would look like.

I've got the file name and the category.

Now whenever we are dealing with the image data.

So the strategy that we follow is we will not load every data that I have got into my Ram.

So each and every batch of data that we want to load, only that data that we will be loading into the

Ram, we will process the same.

And once the processing is completed, then I'm going to load the next batch for processing.

So in order to do the same here, I'm going to say that I'll use the batch size as 32, the number of

epochs as seven, and then the total train.

As I am mentioning the train shape.

Okay, what is the shape of the data then?

Once I have defined this, I'm going to define an image data generator.

Okay I'm going to define an image data generator.

This is how an image data generator object would look like.

Now what I'm saying is when you are loading an image data, rotate individual image by 15 degrees in

a random and rescale the image.

So whatever the image pixel intensity is that I've got, normalize it by dividing by 255.

And I'm mentioning my shear range and zoom range.

So this is like the additional augmentation that I'm performing on the image data.

What is image augmentation?

It is nothing but using my existing image and on top of the existing image we are going to apply the

activity of transformation.

That is what we call it as an activity of image data generator and the augmentation of the image data.

So once I define this object of image data generator for my training generator I'm going to load this

image data by saying as train data gen dot flow from data frame, because I'm going to load the data

from the data frame.

So that is where we have the file names are.

And my x column is file name, y column is category.

And for the class mode I'm specifying as categorical.

So if I make use of the class mode as categorical.

In that case we have to ensure that we make use of sparse categorical cross entropy.

Okay.

So we will use the relevant activation function at the output layer.

Just remember we have got two options.

We've got binary as well as categorical for the example and the demonstration purpose I'm using it as

categorical.

Now once this is done okay I'll define the similar activities on the validation data.

And I'm going to create the validation data generator.

Remember in case of the validation data I'll just perform the rescaling I'll not perform the data augmentation.

Now once that is done we'll start by importing the necessary libraries and the layers.

Okay.

So what I'm going to do is I'm going to import the sequential I'll import the com 2d layer Maxpool 2D

layer dropout layer, flatten layer activation batch normalization.

So these are the various layers that I'll be loading okay.

Once I define the layers I'll start by defining the model as sequential okay.

Once I define my model as sequential to add any convolution layers on top of my image data.

So what we can do is we can just mention it in this way.

So I'm going to say model dot add.

Okay.

So here the layer that I want to add is a com 2D layer.

Now inside this conv 2d layer we can mention how exactly the con 2D layer operations will be.

For example, if I go to the documentation of this TensorFlow.

So I can specify what is the number of filters.

So I'm going to say that filters is equal to ten okay.

And we can also say that what should be the shape of my individual filter.

So I'll say kernel underscore what is the shape.

It is size kernel okay.

Then if I want I can also mention the strides.

And along with that we can also mention the padding.

Okay.

We can mention the padding.

We can mention the size.

So in this way we can mention the various properties of my convolution layer.

Now I can have multiple convolutional layers in this way.

And after I have got the multiple convolution layers, the general activity that we will do is we will

perform the max pooling.

Okay, I'll say max pooling 2D.

Okay.

Now here, after the specification of max pooling, 2D will also have the series of convolution layer

and the pooling layer.

And in the first layer that we are doing con con 2D layer, we will also have to specify the input shape.

Now since we are dealing with an image data.

So I'm going to say input underscore shape is equal to image underscore shape that we had defined earlier.

So in my case the image shape is nothing but my image size.

Okay.

This is how we have to specify the first layer.

Then here before we send the data to the next layer.

Well that is the flat.

That is the next layer.

In this case I'll send it to my flatten layer.

Then we can add further dense layers.

Okay I'll say dense and I'll say 20 okay.

Activation is equal to ReLU.

I can mention the activation function.

And then maybe I can add a final layer.

Okay.

Dense ten neuron.

And I'll say in this case not ten.

It's actually two neuron with my activation is equal to softmax because I'm making use of the categorical

uh, that means it will be loaded as a multi-class classification.

Okay.

In this way we can specify the model and then I can display the model summary.

This is just the example model structure that I'm creating to.

So this will define my overall architecture.

Okay it says conv 2d.

Okay.

Let me specify the same image size.

It's actually 128.

Okay.

This is how the image would look like.

Okay.

So this is how the model would look like.

So the number of parameters and you can see and this is the total number of parameters.

Now once we have defined this we can define the model dot compile method.

So here I'll perform the compilation model dot compile optimizer as my Adam loss as categorical cross

entropy metrics as accuracy.

Now, after this specification of the same, we can go ahead and perform the fit.

So to perform the fit I'm going to say history is equal to model dot fit.

And we will specify the train generator epochs is equal to epochs that we have defined earlier.

Then validation data is equal to val generator.

Okay.

Here then I'm going to say validation underscore steps is equal to validate is equal to batch size okay.

And I'm going to say steps per epoch is equal to total train divided by batch size.

Okay.

So in this way we can perform the fit.

So I think I'll have to specify how do we calculate this total validate.

See the total validate.

And total train is nothing but number of rows that we have got on our training data as well as the validation

data.

So if I just execute this this is going to perform the fit on my training data and performs the validation

on the validation data.

So this will take some time team.

So in order to perform the training.

So I highly recommend you to complete the training and have a look into the overall outcome.

Okay.

The key takeaway over here from this video is when we talk about implementing the convolutional neural

network at that time.

Yes.

At that time we will make use of the convolutional layers for the image classification task, especially

to learn the features from the image data.

Now, once the data passes through the convolutional layers, we will then perform the flattening.

And after the flattening we will have the dense layers.

So this is the overall structure of the convolutional neural network.

Now when we talk about the image data or any complex or any complex task, in the case of deep learning,

we generally do not train from the start.

So what we do is we make use of a technique that's called as a transfer learning technique.

So let us understand how does this transfer learning is going to work and how we can implement it for

a given data set.

So I'll see you in the next video.