# 17. Transfer Learning for Natural Language Data

---

WEBVTT

Okay, so till now you have learned some of the building blocks which led to the transformer architecture.

Now in this video we will look into the transfer learning technique, which also made a clear impact

when the transformer came into picture.

And this also played a major role in improving the overall efficiency of the transformer architecture.

So when we talk about the transfer learning.

So transfer learning is a common way to train the deep learning model, especially for the task of computer

vision.

So the general practice that we follow in case of computer vision is.

So suppose when we take the task as computer vision it let's assume it's a classifying the image data.

So what we'll be doing in this scenario is using this pre-trained neural network like the ResNet.

So we are going to adapt it for our given data set.

We will adapt it with our custom data.

So that's the overall idea behind this transfer learning technique.

And this is very common when we talk about the computer vision application.

So by using this transfer learning approach we are having a lot of advantages.

So we are allowing the network to make use of the knowledge that has been learned from the original

task.

So when we talk about the architecture, so when we talk about the architecture, when we are performing

the transfer learning technique.

So what we will do is suppose if I've got a model, suppose if I have a model that would look like this.

This is my deep learning model.

So what I'll be doing is I'll be splitting this deep learning model into two parts.

Okay.

We'll split this deep learning model onto the two parts.

Assuming that this is my deep learning model, I'll split into two parts.

We call it as a head.

And the other part we actually call it as a the body.

Okay, so this will be my body and this will be the head.

So we will split the deep learning model into the two parts that is head and body.

Now the body will contains the weight parameters and the head contains the required output specific

layer.

Okay, so this is the overall idea.

So during my training the weights of my trained neural network will have the overall features.

So this would actually contains the features.

And it has the overall learning from the given domain.

To give you an example, suppose this is my deep learning model and this has been trained on an ImageNet

data set.

So when I have trained it on the ImageNet data set on this deep learning model.

So this deep learning model has the complete features and it has learned the features.

And in my output layer I will have 1000 neurons assuming that it's a multi-class classification with

1000 neurons.

So this is the overall idea about this trained deep learning model.

Now what I'll do is I'll split this trained deep learning model like the ResNet model, and I'll split

it into two parts, okay.

And I'll split it into two parts into body and head.

Once I split the neural network into body and head.

Now what I'll do is let's say I've got a new task.

The new task is I want to classify cat or dog.

I want to classify it as cats or dog.

So what I'll do is I'll make use of the body.

Okay?

I'll make use of the body.

Now, this body is made up of the parameters which has learned from the ImageNet data set, that is,

from the domain of ImageNet data set.

Now I will have a different head.

I will have a new head for the demonstration purpose.

I'll choose a different color.

So I will have a different head and using this different head.

So in this scenario it's a binary classification.

So as a result of this I will have a single neuron I'll have a single neuron.

and I might add a few dense layers, which will enable my overall neural network to learn the patterns

in the entire data set.

So this approach is called as the transfer learning approach.

So what we are doing in the scenario of this transfer learning approach is we are making use of the

trained parameters on a specific domain.

We are using the same as a starting point to perform the fine tuning on a new similar task.

Now, by doing like this, we are able to train in a much faster way and we are able to get the best

result when we perform this task of transfer learning.

So this is very much better compared to the traditional supervised learning approach.

So in the overall computer vision scenario, as I mentioned, we'll be training our deep learning model

on a large scale data set like the ImageNet data set, which would contain the millions of images.

And we call this overall activity as a pre-training activity.

Okay, we'll make use of this pre-training activity.

And the main purpose of this pre-training activity is to teach the model the basic features of the images,

such as colors, edges, and so on.

Then we will use these pre-trained models.

We will get the body of the pre-trained model, and then we will fine tune it to the downstream task,

like classifying the image as whether it belongs to cat or dog.

And these model that have written right now these models are called as fine tuned models.

And these fine tuned models typically achieve higher accuracy than the traditional supervised machine

learning models, which are trained from the scratch.

It could be machine learning model, or it could be deep learning model as well, with the same amount

of labeled data.

Now, even though this kind of transfer learning approach became a standard approach in computer vision

for many years, still now, this was not the scenario with natural language processing.

So as a result of this, for each and every task, whenever we are building any NLP based application,

we would require large amount of labeled data.

Now, getting the large amount of labeled data was very difficult for us.

Having the complexity of languages and the text data.

Preparing the labeled data set was very difficult and also whenever we were achieving the performance

with large data, due to the data issues and the computation issues, we were unable to proceed further

and we were unable to reach the better accuracy on a large data set.

Now, what happened was in the year of 2017 and 2018, around those ages, various research groups proposed

the various approaches that finally made the transfer learning possible for NLP as well.

It started with an insight from the researchers at OpenAI.

So what they have done was the researchers at OpenAI, they obtained strong performance on sentiment

classification by using the features which are extracted from unsupervised pre-training approach.

So what they have done is they have performed the unsupervised pre-training.

So in the transfer learning of NLP based task.

So what we will have is we'll have a pre-training.

So in this initial training objective.

So what we'll be doing is we will take a very large corpus okay.

We will consider a very large corpus.

And by considering a very large corpus we are going to perform a dummy task.

Okay.

What is the dummy task?

The dummy task is given the current word.

What will be the next word.

Okay.

Consider a large corpus.

Build a model or train a model on the large corpus.

And this large corpus could be the Wikipedia data or the overall available text corpus that we have

got with us.

We will build a model on this very large corpus with dummy task of, given a current word, what will

be the next word.

So this is the dummy task that we are interested in performing.

And this approach is called as language modeling approach.

So the best part is when we want to perform this pre-training on the very large corpus, we don't have

to find the text data.

We don't have to require to get the large amount of labeled data.

What we can do is we can just extract the data from various sources, like choosing the Wikipedia site.

And what we will do is we will split the entire data text corpus into the individual tokens.

That is my individual words.

And I would say, given my current word zero, what will be the next word?

That is word number one.

So this is called as pre-training.

Now once the pre-training has been performed.

So next thing what we would do is we will perform the domain adaptation.

So we will perform the domain adaptation.

Now in this domain adaptation, what we will do is on the required task at hand.

On the required task at hand we will perform the we will perform the fit on the domain data.

Okay.

We will perform the fit on the domain data for example.

So once I have performed the pretraining on a very large general corpus, I'll just go ahead and perform

the similar activity on the domain that I'm interested in.

So it could be the IMDb data if I'm working on movie, and it could be the, uh, research, research

data if I'm working on scientific research based model.

So in this way we'll perform the domain adaptation.

So this is my step two.

So the step one is pre-training which is also known as language modeling.

And the step two is actually my domain adaptation.

And then we will have the third step which is in which is what we call it as a fine tuning.

So in this final step of fine tuning what we will do is we will make use of the trained language model

which has obtained the domain adaptation.

What we will do is we will add a classification layer assuming that it is a task of classification.

So I'm going to add a classification layer at the final layer, which will help me to perform the required

task.

That means we will take care.

of the task at hand.

Okay, we will perform the task at hand in the scenario of this fine tuning approach.

So this is the core approach.

And this core approach resulted in the best models that is possible with the help of natural language

data.

So by introducing this pretraining domain adaptation and fine tuning, we were able to get the higher

accuracies on the very complex task like sequence classification and the sequence generation, which

was actually a bottleneck in the pre era of transformers.

So with the attention as well as in combination of the transfer learning, we were able to achieve the

higher accuracies in the advanced language models and one such model, which actually changed the way

that we looked into the NLP field is the transformer model.

So let's understand what exactly is this transformer model.

And we will look into this transformer model at a very high level.

And then we will look into its implementation in the further section.

So I'll see you next time.