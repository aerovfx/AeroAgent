# 4. Intuition of Deep Learning Training

---

WEBVTT

In this video, let's go ahead and build the intuition behind this deep learning neural network.

See, when we talk about a neural network, it basically has an input layer hidden layer and an output

layer.

Now in my screen you clearly observe the page of TensorFlow playground.

So here we'll use this playground to deepen our intuition behind this deep learning neural network.

So in this screen you can observe that I've got two features x one and x two.

And in my given data I have the data which represents the concentric circle.

And and I have already done some settings in this page.

One thing that you can observe is the activation function.

I have set this as sigmoid.

So this activation function of sigmoid is very similar to the logistic regression algorithm, which

you are already aware of from the machine learning.

So what it does is whatever the input that I get, I'll get that input.

I'll perform an activity of linear summation.

So in this neuron what I'm actually doing is I'll take the dot product of inputs and weights.

And then we are going to add the bias term.

So even in this case as well in place of x1 and x2 we'll have a data.

And if I hover my mouse at this page see it says weight is 0.45.

And if I hover my mouse over here, here it says weight is -0.42.

So these are the weights okay.

Of the parameters x one and x two respectively.

Now apart from that just like we have a intercept term in case of linear regression algorithm, even

in case of an individual neuron we also have a bias term here.

In this case the bias is 0.1.

So I'll say b is equal to 0.1.

And at the end we have the weight as -0.49.

So these are the initialized values.

So what I can say is for an individual neuron each input that I get will have the respective weights.

And an individual neuron will have its own bias value.

Now the way this data will be processed is inside this neuron okay.

Inside this neuron I'll take the dot product between weights and the inputs.

So 0.45 into x one.

Since it is minus I'll say -0.42 into x two plus bias term that is 0.1.

So this is the output of this neuron of the linear operation.

Now what I'll do let's suppose if I call this as z.

Now I'm going to send this value of z to my sigmoid.

That is my logistic function.

So as a result of this I'll get the value between 0 to 1.

As simple as that.

That's all.

I'll take the linear summation of the input and the weights.

I'll add the bias term and I'll apply it to the activation function.

And I'll do the same activity for the next layer as well.

So here in this case once I do this activity okay.

So whatever the output that I get, that output as you know it's called as a predicted output.

Now in this case okay, if you observe closely this overall setup that have got it is very similar to

the logistic regression algorithm setup that I have got.

And I've got a data which is like a concentric circle.

And you know already that no matter how I create a decision boundary, I will not be able to split this

data in an effective way.

So to prove you the same, I'll perform the fit on the settings.

So here we have something called as learning rate.

So learning rate is the same learning rate which you have learned in the gradient descent algorithm,

which you have learned in our machine learning journey.

Here we are applying the gradient descent algorithm.

I'll perform the training if I perform the training.

See, this is how the data is getting split.

Now once I stop the training, if I hover my mouse on the value of weights, it is 1.8.

Here it is -0.60 If I take it to my bias term, bias is -4.4.

That means there is a change in values of the parameters.

Okay, there is a change in the values of parameters.

And these values are getting changed with the help of gradient descent technique where I will take the

derivative of my loss function with reference to my parameters, and I am updating the values of parameters,

and while updating I'll refer to my learning rate.

Okay, this is the core idea of training.

So finding the output is called as a forward pass.

And updating the parameters is called as a backward pass or a back propagation.

So I'll repeat this concept.

Forward pass is, I'll say, finding the y hat and value of loss.

Backward pass or a back propagation is nothing but updating the parameters.

So this is the basic of deep learning.

And in this case I'm making use of a single neuron.

If I use a single neuron this is known as a very simple setup okay.

Which is also known as a perceptron.

This is known as a perceptron setup.

Okay, now we say that with the help of deep learning, we can learn the complex patterns in the data

set.

So let us try to prove that.

Now what I'm going to do is we came to know that with the help of a single neuron, I am unable to find

the patterns in the data.

Now I'll increase the number of neurons to three.

And now, at the start, my training loss is given by 0.588.

Now I'll just go ahead and perform the training.

If I perform the training.

Observe closely.

See after few steps after, let's say 450 epochs.

If you look into the training loss, it is closer to zero.

And if you look into the visual representation, we can clearly see that we are able to separate the

blue data points with the orange data points, which means no matter how complex the data points are,

I'll be able to find the patterns in a given data set, provided I have sufficient number of neurons

in my neural network.

So this is the core idea for you to remember about the deep learning.

Are you with me?

I need to have the sufficient number of patterns that is, sufficient number of neurons in order to

learn the complex patterns in a given data set.

Okay, now here this part is clear.

Now there is something called as a learning rate.

So here what I'll do is this time I'll decrease the learning rate to 0.00001.

And I'll do the training.

Okay.

I'll do the training.

So if I begin the training with very small learning rate as 0.0000001, see, when my learning rate

is very slow, even the system has become 595 epoch.

Even if it reaches the 595 epoch, even at that time, the system is unable to move towards the best

value of parameters because the value of training loss is 0.624, which is worse than what I had earlier.

Okay, and if I increase the value of learning rate, let's say I'll change it to ten.

If I change it, the learning rate to ten.

And if I do the training.

See, the system is actually unable to reach the best values of parameters.

So it is getting stuck at some point, and it is unable to reach the best values, and it is unable

to separate the blue data points with the orange data points.

If I change the learning rate to, let's say 0.1, So within just 150 epochs itself, I am able to separate

the blue data points with the orange data points.

So from the examination perspective this is very important.

This intuition is very important if the learning rate is very large or if the learning rate is very

small, then I will it will be very difficult for me to find the patterns in the data.

If it is very large, I would miss out the best values of the parameters.

If it is very small, then I will take lot of time to find the patterns in the data.

This is the relationship of learning rate on the training of my deep learning neural network.

So are you with me?

Excellent.

So remember, this learning rate question is very important.

The kind of question which you would expect is.

So let's say we are training the neural network and we are even after 100 epochs, the model is not

reaching towards the best parameters.

Okay.

What could be the reason.

So one of the options will be learning rate is very small or learning rate is very large.

So ensure that you understand the intuition behind this learning rate.

Okay, now another key thing is why should we use the activation function?

Why can't I use the linear itself?

That means why should I apply the softmax or any other activation function?

Well, the reason that we have to apply the activation function because without this activation function,

this is as same as the linear regression, which means I will not be able to find the non-linear patterns

in the data.

I will not be able to find it c without any activation function.

It is as same as drawing a straight line.

No matter how I draw the straight line, I will not be able to separate the data points.

Hence activation function is very important whenever we are training the deep learning neural network.

Okay, now coming back to the basic thing, what exactly is this epoch now how it actually works.

So what it means is if I take my entire data set, okay, if I take my entire data set and perform the

forward pass for my entire data set, and then apply the back propagation.

If I do it once on my entire data set, then I would say that one epoch is completed.

Okay, that is the meaning of one epoch.

One forward pass plus one backward pass is equal to one epoch.

Now here comes an important concept.

That's the batch size.

Okay.

What is this batch size.

Well when I have I'll give you an example.

Okay.

Suppose if I have 50 rows in my given data set I can pick 50 rows together, perform the forward pass

and perform the backward pass.

Or what I can do is I can pick ten each time and on ten rows I'll perform forward propagation and backward

propagation.

Next I'll pick up another ten rows and I'll perform forward and backward.

I'll pick up another ten.

So like this I want to pick up ten, ten five times.

So once I pick up ten, 1010 five times, which batch size has ten, then I'll say one epoch is completed.

Okay, that's the relationship between my epoch and the batch size.

Okay, so with this visual representation, you now have a clear understanding as what are the various

parts of our neural network.

So if I get back to our code okay, we were implementing this in the last video.

Okay, let me just take you over here.

Okay.

I'll not take you to the output.

I'll just take you to the place where we have written the code.

So in the place of model dot fit.

Yeah, I mentioned Xtrain Ytrain epochs is equal to 30, right?

So what it actually means is in this case, for my entire data set, Ensure that you do it 30 times.

And here it has considered 1719.

What is 1719.

So by default if I do not specify anything okay, the system is going to determine the default batch

size.

Let me help you understand what is the default batch size.

See if you look into my training data there are 5500.

So in total it has splitted into actually 1719 batches for one epoch, which means what it is going

to do is okay.

What it is going to do is here.

In this case of 55,000, it is splitting in terms of the batches of 32.

So if I divide by 32 it is going to give me as 1718.75.

So with batch size as 32 it has created a total of 1719 batches.

Okay.

That is the value of 1719 in the training results.

And we have the value of loss.

That is my training loss training accuracy, the loss on validation data and the validation loss like

accuracy on the validation data and the validation loss.

So this is how we interpret the overall output that we get in our deep learning neural network training.

All right team.

So with this we come to the end of this video where you now have a better idea about the various intuitions

behind this deep learning neural network.

Now, in the next video, we will go ahead and understand about working with the activation functions,

because understanding this activation function is very important from the examination perspective.

So let's have an intuitive understanding in the same way that we have done as of now.

So I'll see you next time.