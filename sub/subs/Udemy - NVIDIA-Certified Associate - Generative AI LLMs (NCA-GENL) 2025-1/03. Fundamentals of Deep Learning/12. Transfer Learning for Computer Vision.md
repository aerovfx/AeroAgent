# 12. Transfer Learning for Computer Vision

---

WEBVTT

In this video we are going to learn about the transfer learning technique.

So let's start by understanding what exactly is this transfer learning technique.

So when I say transfer learning technique in machine learning it means usage of the learned parameters

which has been trained on one task on a similar task.

So this is called as a transfer learning.

Now in this case of this transfer learning okay.

So we'll be reusing the weights.

So think about this.

Let's say we have actually trained our algorithm to identify okay.

To identify for a given image whether it is cat or dog.

Now in case of transfer learning what I'm trying to say is that on this trained model of this, a model

that we have trained for classifying the input image as cat or dog, I can use the trained model in

usage and in performing the similar activity.

Let's say I can use this model as a base in order to classify my given input image as whether it would

contain bus or a car.

Okay, so what I have done is I have made use of the model which I have trained on an image data set,

and using the trained model, I'm going to apply it on a similar task.

So what is a similar task?

Even in this case, it's a classification task where I'm dealing with an image data.

So as a result of this, the learnings that you get from the original training, you will be using that

original learning as a base.

And on top of that you are going to learn the new activities.

This is called as the transfer learning.

So just like what you will be doing in this course as well, see, in our course you're going to build

a foundation.

Now with this foundation and the learning, you're going to implement it on your projects.

So that is a classic example of transfer learning.

So in the techniques while learning you might have learned on training the cat dog classification.

Now when you start working, maybe you start working in a medical domain where you will start classifying

the individual image as whether it's the X-ray patient, whether whether the whether the patient is

having a disease or not by looking into the image data.

So you have transferred the learning for training purpose into the practical implementation.

So this transfer learning operation is everywhere even in the real world.

And in deep learning perspective.

So when we talk about this transfer learning, so what we can do is we can use the transfer learning

for various cases.

So what we can do is we can train a model to reuse it.

That means training model to reuse it.

This is one way that I can make use of this transfer learning, or use the pre-trained model and use

that as a base in training the new data set or extraction of features.

So these are the various ways in which the transfer learning can be applied.

So when we talk about the training, a model to reuse it.

So what it means is let us assume that we have trained a model on an on an existing data.

Now when I receive any new data, I'm going to reuse my old image or old model that I have trained.

And on top of that old model, I'm going to perform the retraining it.

So this will help me to reduce the their training activity on a new image data and in case of a pre-trained

model.

So what I'll be doing is here in this case of pre-trained model.

So I'll make use of the already existing or the already trained models.

And on top of that I'm going to perform the fine tuning on the data that I have got with me.

And the last one here it says about the extraction of the features.

So when we say extraction of features, in this case of extraction of features, whatever the data that

we get on the model which has been already pre-trained.

So we'll send it over there for the pre-trained model.

And the output from the pre-trained model is used as an input for the further task that I would require.

So this is called as the extraction of features.

So these are the common ways in which we can perform the transfer learning.

Now in our learning journey that is in this video we are going to make use of the pre-trained model.

So one of the pre-trained model that we'll be using is we'll use the ResNet ResNet model.

So the model that I'm going to do is uses ResNet TensorFlow.

So this ResNet is one of the image classification pre-trained model that is available to us.

So we'll make use of this ResNet.

We'll import this ResNet.

And once we import this ResNet.

So we are going to instantiate this model.

We will use this model as a base.

And on top of that we are going to send in our new data set.

Now this ResNet has been introduced as part of this research paper, deep Residual Learning for Image

recognition.

And there are various architectures that are available right now for this ResNet.

So when this computer vision actually started at that time, to encourage the researchers to push and

to push more towards the development for the humanity.

Most of the, uh, companies have actually created the computation.

One such computation is an ImageNet computation, ImageNet.

If you go to that website of image ImageNet, you'll get a clear idea.

So ImageNet is an image database which is organized according to the WordNet hierarchy.

Okay, so this was actually instrumental in advancing the computer vision and the deep learning research.

So any model or any new research paper that was getting released in the domain of deep learning or computer

vision, they were actually using this ImageNet data set as a standard to validate how the model is

actually performing, and they have actually conducted various challenges as well.

If you click on the challenges, you would come to know in which year, what are the algorithms that

have actually run.

Okay, so that's the core idea team.

So the researchers have participated in this various challenges to ensure that their model is able to

predict the given class for a given given image.

Now in this case.

Okay, so in the ImageNet data set we have got the images belonging to the 1000 classes.

Okay.

It belongs to the 1000 classes.

So imagine the complexity of image data that we had at that time.

And at that time it was very essential for our development because like long back I would say ten,

15 years back at that time, we had a challenge of data set.

There was very minimal labeled data available with us at that time.

So this ImageNet.

So like along with the other companies, they actually teamed up together and created this data set

and the database so that the researchers can focus on the research and use this complex data set to

evaluate the algorithms and the train models.

So that was the whole purpose behind this ImageNet competition.

Now that you have a background about this, now let us go ahead and let's see how we can implement this

in our classifier.

Okay.

Will be will be actually implementing this classifier on the ResNet model.

So let's have a look into the ResNet architecture here.

Yeah this is the ResNet architecture.

So if I take you to the ResNet architecture it it actually says what is the ResNet architecture is made

up of.

Okay.

So this ResNet architecture is made up of the residual connections.

See you have a skip connections that you see here on this research paper.

So these skip connections are actually referred to as the residual connection.

So with this residual connection the idea is to get a better performing model than the earlier models

that we had okay.

So we are going to make use of one such model in our implementation.

So in order to implement the same here I'm going to use a variant of ResNet that is called as ResNet

50.

So you can check out this page about this ResNet 50.

So what is ResNet 50.

It has got the 50 layer.

See there are 50 layers in this ResNet.

So what it says is they have the con one layer.

And this is followed by max pooling layer.

They have the con two layer Com three layer.

So there are multiple convolutional layers.

Okay So they have clearly mentioned how the overall architecture will be okay.

You can check out this research paper if you are interested to learn more about this data set and its

performance on the various image data that we had earlier.

Now, in my case, I'm going to import this ResNet 50 architecture.

And I'm saying that load the weights as ImageNet weights.

And I'm saying that include top is equal to false, which means do not load the final layer.

Now why is it actually important.

So when we talk about the transfer learning what we do is.

So we are going to split the pre-trained model into two parts okay.

We are going to split the pre-trained model into two parts.

So we are going to call it in this way.

Okay.

So if I have a model architecture a deep learning model architecture for performing the task of classification,

we will divide.

We will define any model into two parts.

One is a feature extraction part or feature extraction body.

And the another part is the classification or a regression part.

So this classification part or a regression part is called as head.

Okay.

So we have the classification head and the feature extraction body.

So what we do is in this case okay.

So we will make use of the feature extraction and the classification body.

And in in.

Together we will perform the classification.

Now, in case of transfer learning let's assume that this is the feature extraction and this is the

classification head.

Now assuming that I am working with an ImageNet data set in the case of ImageNet data set okay I'm going

to have an RNN architecture.

So I'll have the series of layers, and at the end in the classification head, I'll have the 1000 neurons

with softmax activation function.

Now in case of the transfer learning, what we will do is we will make use of the same feature extraction

that is from the ResNet.

Okay.

And on top of this what we will do is we are going to add our own classification head.

Now in this case, I'm going to add a two neurons in my classification head, because in this case,

I am interested in performing the task of classification to either image belonging to either cat or

dog.

So for my existing body, okay, I'm going to add my own classification head And during the training,

what I'm going to do is I'm going to switch off that switch off the model updates for majority of layers.

I'm going to switch on the training for only minimum part of layers.

So by doing like this, all the previous learning that I had, it will be retained.

I will not be retraining the entire model.

I'll just perform the partial training on the initial layers, that is, the final layers of the network,

or the layers which are nearest to the output.

So the idea is when the data passes through the input layer.

So this data will pass us through the pre-trained model, which has the effective way in learning the

parameters from the data set.

And at the end in the final layers, I'm going to learn from my given data.

Okay, I'll be giving the opportunity to adjust it for the given data.

And this activity is called as fine tuning activity.

Okay.

We'll perform this fine tuning on new data.

So that means we will be adjusting it on the new data set using the pre-trained model.

This is the core idea behind this activity of transfer learning.

In case of transfer learning, we'll send in the input data okay.

And this input data will be sent to my pre-trained model.

And this pre-trained model will will be switched off during the training.

That means we'll just perform the forward pass.

And the output from over here will be tuned in the final layers of my neural network architecture.

Now to implement the same.

So I have written a code.

Now if I take you to the output see the last during the last implementation.

So this is how it would actually look like.

Okay give me a second.

Let me fix the same.

So this is the old output that we had got without the transfer learning approach.

In this case, the accuracy that we are able to reach at the end of seventh epoch is 71% accuracy.

Now I am importing the ResNet model and I'm saying that include top is equal to false, which means

I am not going to load the classification head, but the weights will be from the ImageNet training.

Now I'm going to say that x is equal to base dot output.

Whatever the output that I'm getting, I'm assigning to my x and I'm sending it to my global average

pooling 2D.

This will help me to reduce the number of dimensions.

And here I'm also adding a dropout layer.

So we use this dropout layer in order to avoid the overfitting on my neural network for the given training

data set.

And at the end I'm going to specify my dense layer and my model, which has got the input and the output.

Okay, then once this is done here, I'm going to perform the activity of compilation.

So here once this is done.

So here I'm actually going to specify the early stopping.

So this early stopping model checkpointing these are all called as the callbacks.

So this will be executed at the end of each epochs I'm making use of early stopping.

As I mentioned earlier during a learning journey so early stopping will ensure that we stop the training

when the training results is not improving.

That means on the validation data, the outcome is not improving.

In this case, I'm specifying as early stopping and I'm also reducing the learning rate on plateau.

So sometimes what happens is when the when the cost function reaches the plateau like structure, at

that time, it is going to move in a very slow manner.

So at that time we we are adjusting the learning rate in this case.

So this is called as reduced learning rate on plateau.

And I'm also saving the model at the end of each epoch if it is performing the best.

Now I am mentioning all those callbacks inside the callbacks parameter.

And if you expand it over here and look into the training results.

So let's wait for a single epoch to complete at the end of the single epoch.

Once it is completed, we will be able to see that a new model file will be saved over here in this

Google Colab instance.

Okay, we can see that a new model file will be saved.

So if I refresh it, see a model file is being saved.

Now what we will do is we will complete the training and once we complete the training, we are going

to save the most effective model.

That is the model which has given us the best accuracy.

I have executed in the previous session.

Let me bring back the other notebook where I have already done the execution.

So this is the other example where I had already done the execution, and the accuracy that I had got

last time was closer to 97% accuracy.

Then we can generate the prediction on the image data.

And here I'm considering an example image data.

This is how an example image data would look like, which is an image of dog.

And on top of this image data I'm going to pre-process the image and I'm generating the prediction here.

It says the prediction from the model is dog, which means our model is able to classify the image data

in a successful way.

Now once that is done.

So I have actually downloaded the best model from this, uh, Google Colab.

So if I want to download a best model you just have to go ahead over here, expand the File Explorer.

Click on this button and select as download.

So you'll have to wait for a while for the download to complete.

So with this we come to the end of this video on Transfer Learning.

Now in the next video, we will take a look into the sequence learning architectures that we have got

in deep learning.

That is the recurrent neural networks.

So I'll see you next time.