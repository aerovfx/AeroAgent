# 5. Activation Function

---

WEBVTT

In this video, we are going to learn about the activation functions in deep learning.

So as we have learned an activation function in a neural network, it tells us as how the weighted sum

of input is transformed into an output from a specific node or a neuron.

So till now, at least in our pictorial representation, I had made use of sigmoid activation function.

Apart from sigmoid, there are various other activation functions.

We have got tanh activation function.

We've got ReLU activation function, leaky ReLU activation function, slow activation function, softmax

activation function.

And we also have other activation functions like switch parameterized ReLU.

So these are the various activation functions that are actually supported by the TensorFlow library.

The idea is simple.

Whatever the outcome that I'll get after I perform the linear transformation, I'm going to send in

to these functions, which will again further transform the data by applying the various functions.

So let's understand how these functions would change my given data set in order to explain the same.

I've already created a notebook.

So this is a notebook which explains about the various activation functions that we have got in TensorFlow,

which will give us a better idea for us.

So I'll just import the necessary libraries.

And here I'm going to generate some random data points.

So just like we have got NP dot linspace, the TensorFlow library has the function called tf dot linspace,

which will help me to generate an equally spaced data points between start and the end value.

Okay.

so here tf dot linspace minus ten comma ten 100.

And then we'll display the shape.

And once that is done here on the data that have generated on top of that I'm applying the sigmoid activation

function.

And I'm trying to understand how the values are getting transformed.

Now there is no secret about this sigmoid activation function as we have explored in detail sigmoid

activation function.

If you look into it, it's very similar to the logistic regression.

So this is going to scale the values between 0 and 1.

So if I execute this code right here I'll be able to validate that this is going to result in an S-shaped

curve Okay.

See, if you observe, this is going to result in an S-shaped curve where the minimum value is zero

and the maximum value is equal to one.

So that is how we can scale the values in the range of zero and one.

Okay.

And we have got the hyperbolic tangent function by applying this hyperbolic tangent function on the

same data x.

If we observe.

This is also resulting in a shape curve, but the range is minus one to plus one.

So tanh function was actually one of the most used activation function in the earlier days, but we

do not use it these days because of the computation complexity that we have got.

Okay, so this is the hyperbolic tangent function.

Apart from that we have the ReLU activation function here in case of ReLU activation function.

If I have any output from the linear transformation, if it is less than zero, then I'll make it as

zero.

If it is greater than zero, I'll make it as the same value.

So max of x comma zero.

See, any value greater than x will be equal to x itself.

Any value less than zero okay.

It will be.

So here in case of ReLU activation function, whenever the linear transformation is becoming less than

zero, it is resulting in zero making it non-differentiable.

So in that case there is a variant in ReLU that is called as a leaky ReLU.

So using this leaky ReLU we can actually ensure that the values can be differentiable.

Values can be differentiable even if it is less than zero.

So this is the equation of this leaky ReLU okay.

And there is another equation that's called as elu which is also known as exponential linear unit.

So here the logic is y is equal to x if x is greater than zero.

But if it is less than zero then I'm applying the alpha multiplied with exponential of x minus one if

it is less than zero.

So this is the ELU activation function, and even in this case as well, this will allow my uh function

or the output to be differentiable, because this is not equal to zero.

Then the other one we have got is the selu, which is also known as scaled exponential linear unit.

So in this case, if the value is greater than zero, I'll return x.

That is, scale into x.

If it is less than zero, it is scale into alpha into exponential of x minus one.

So here alpha and scale are the predefined values okay.

They are the predefined constants where alpha is 1.67 and scale is 1.050.

So this is the overall output that I get if I apply my data on this cell activation function.

And we have got the switch activation function like all these activations are available inside this

tf.keras dot activations.

Okay.

So this is the switch activation function which is given by x into sigmoid.

So this is how the output would look like.

And we have the softmax activation function.

We generally use the softmax activation function whenever we are using on the output layer in order

to perform the activity of multi-class classification.

So this is the softmax.

So if you take up an example let's say I've got the values as 110 50 and ten.

So application of softmax will result in distribution of probability.

So it will give me such that.

So the total value of these probability scores will be equal to one.

Okay.

This is the overall idea about the various activation functions that are available inside our deep learning

neural network implementation of TensorFlow.

And even in case of PyTorch you can also see the similar activation functions.

So from the examination perspective, remember that activation functions will help us to scale the values

from linear values to the non-linear values as part of our experiment.

And out of all these activation functions, ReLU is the most widely used activation function.

And I'll also touch base on the activation functions on the individual layers in the next video.

Okay.

Remember ReLU is the most commonly used one.

Apart from that we also have the variants of ReLU activation functions as well.

And here when it comes to the optimizer, some of the key things that you need to remember is there

are various optimizers.

That is the learning algorithms that are available inside this TensorFlow library.

Out of this stochastic gradient descent is the one which is low speed and it has got good convergence.

Momentum gradient descent we have that's that's one more variant of stochastic gradient descent.

It has got a medium speed but it also has a good convergence.

Adagrad.

It has got high speed when it comes to convergence.

And when it comes to the convergence quality it is a little lowest.

Adam is the one which has got high speed and medium to high quality.

Now when we are training a very large neural network, we prefer speed.

So that's the reason Adam is the most widely used activation.

Sorry, Adam is the most widely used optimizer.

Okay.

This is something that you need to remember from the practical side.

Alrighty.

So this was about the activation functions in deep learning neural network and its role, and also the

various optimizers that we have got in the deep learning family.

So with this we come to the end of this video.

In the next video, let's have a look into the commonly used deep learning architecture, which we constructed

in order to find the patterns in the data set.

So I'll see you next time.