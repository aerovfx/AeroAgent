# 2. Introduction to Deep Learning

---

WEBVTT

So in this video we are going to start a learning journey with deep learning.

So when we talk about deep learning.

So as you have clearly seen these days, we are seeing some of the amazing, amazing applications that

we have right now.

We have got the systems which is able to understand the human input, and it is able to generate the

response, and we are able to see the system which is able to identify what are all present in the images.

So one thing that definitely comes to our mind is can the computers learn like humans?

Well, definitely.

I can say that the with the deep learning the computer are now gaining the ability to learn just like

the human being.

And if you think about it and look around the world around you.

So this applications of deep learning are being used in day to day life.

It is used in image tagging, like identifying the person's face in a given image.

It is used in machine translation.

It is used in recommending the post for an end user.

Not just that this deep learning is used in the applications like self-driving car text generation,

chat bot fraud detection, and many more, so the list is endless.

So in all these fields, the deep learning is helping us to analyze the data and find the patterns in

an effective way.

So as we progress in this video, we will understand what exactly is this deep learning?

The relationship between machine learning, deep learning and the artificial intelligence.

And what is the overall idea about this artificial neural networks, which is the core part of deep

learning?

And then we'll also see as how does the model is going to learn in the case of deep learning.

So let's start by understanding what is this deep learning.

Well I can say deep learning is a subset of machine learning here.

This is concerned with the algorithms which are inspired by a structure and a function of a brain that

is called as artificial neural networks.

So in this case, so the algorithms that we use, the learning patterns that will be used is inspired

by the structure and the function of the human brain.

Okay.

That is what a deep learning is.

In the case of machine learning we were making use of statistical techniques.

In the case of deep learning, we'll be making use of deep learning neural network.

So let's have a quick idea about what is the difference between artificial intelligence, machine learning

and deep learning.

See, in the case of artificial intelligence, it's a study of how to train the computers so that the

computer can do the things which at present the human can do.

So in this case, this is a system which can analyze the data that means which can find the patterns

in my given data set that is x and y.

And it can take the action.

So it can analyze the data and it can automatically take the action.

That is a system that's called as an artificial intelligence system.

And when we talk about machine learning, it's a subset of AI, which means it can only do the analysis.

It does not have the ability to take the action in order to find the pattern.

We make use of statistical models, and you have learned about the various statistical models that we

have got, depending on type of task and depending on type of data that we are dealing with.

So by finding the patterns in a given data set, we are going to make an informed decision.

That's the idea about this machine learning.

So this machine learning is going to produce the prediction.

And when we talk about this deep learning.

So this deep learning is a subset of machine learning here.

In order to find the patterns we make use of the deep learning neural network which is also known as

the artificial neural network.

Now, even this deep learning, since it is a subset of machine learning, even this can only predict

the action.

Or in other words, this can only generate the prediction.

There is no action, okay, it will only generate the prediction.

Even if action to be performed, it will just say okay, this is the action it has to perform.

It will not go ahead and perform the action It involves only prediction.

So the reason it involves only prediction, even in case of deep learning, because it's a subset of

machine learning.

Or in other words, I can say that this deep learning is a technique to realize the machine learning.

So here one main thing that I have mentioned is that this deep learning finds the patterns in the data,

just like we human beings do.

So let's understand how does a human brain process the information?

Now, if you think about it, our mysterious human brain is made up of millions and billions of cells,

and these billions of cells is actually known as the neuron, which are also known as the brain cell.

And these neurons are interconnected together in order to process the information.

Now, if you take a deeper look into an individual neuron, let's say an individual biological neuron,

this is how an individual biological neuron would look like in the middle.

I've got a nucleus.

Okay.

Here.

This is my nucleus.

Now, whatever the data that I would receive.

So let's us let us assume that here I would receive the inputs from various sensory organs.

So those sensory organs will reach the nucleus okay.

Through the dendrites.

So the outer region X1X2X3 up to x n.

It just talks about the dendrites.

So that is where the information will be received.

And inside the nucleus the these information will be processed.

Okay.

Now once this information has been processed, the data or the information, the processing, our human

brain will pass through this here.

It would pass through this axon and reaches the axon terminals.

Now at the end of axon terminal there is something called as synapses.

Okay.

There is something called as synapses.

So the processed information reaches the synapses.

And this synapses is going to decide okay, whether this information should flow to the next neuron

or not.

Okay.

This is how a biological neuron would look like.

So if I want to talk about it in a technical way, I can say that I'll receive the input over here in

my human brain cell.

I'll process this input.

This is where I'll process the input okay.

And I'm going to generate the output from the axon terminals.

So input process the input and generate the output.

The same structure that we will have inside the deep learning neural network.

So even if you observe the deep learning neural network, even in this case we will have an input layer.

We we generally represent it in the format of a layer.

So the initial layer that you see right here this is called as an input layer.

So through the input layer I'm going to receive the input.

Okay.

Now this received input is going to pass through the series of neurons.

Okay.

You can think of each circle right here as an individual neuron.

And just like the human brain cells are interconnected, even in the case of deep learning neural network,

we will have the neurons, neurons which are interconnected to each other.

So I'll have an input layer and the data will be processed inside this neuron.

And just like we have got the axon terminal.

So at the end of the network we have the output layer.

This is my output layer.

So input.

So this input is where the data is given to the network.

So this data can be an image data text data or a video data depending on the task that we are performing.

And in the middle we have the hidden layers.

And this is where the information processing will happen.

Now here this is going to perform all sort of computations on the features which are entered through

my input layer.

And once the information has been processed it will send the data to my output layer.

Now, in this case, the output layer is the one which gets the output from the hidden layer.

Okay, once it generates the output okay.

Once it generates the output here the system.

That means in case of deep learning neural network we can process this output.

And this output will be the prediction.

Now in the case of our human brain okay, I have mentioned that there is something called as synapses.

In the same way we have something called as an activation function in my hidden layers, which will

decide whether a particular neuron should be activated or not.

In other words, I'm going to decide how the information should flow from one neuron to the next neuron.

So this is the role of this activation function.

So overall in case of my neural network.

Okay.

So I'll have my input layer which process the which gets the input and sends the data to my hidden layer.

And in my hidden layer the data gets processed.

And during the processing we'll make use of this activation function to decide whether the neuron should

be activated or not.

And at the end, we are going to send this process data to the output layer.

Now this activation function, you can think of it as a mathematical function which will transform the

output from the neuron.

So this is very similar to the logistic function that you have seen earlier.

So similar to that logistic function we also have a sigmoid activation function.

If you think about it, whatever the data that gets processed inside that neuron.

So I'll send it inside the sigmoid activation function as an example.

And as you know from the previous learning journey of machine learning, if I send the data inside this

logistic function, it will scale it in the range of 0 to 1.

So the neuron will give me the output between 0 to 1.

In the same way, instead of logistic function, if I apply this tan hyperbolic function, it would

scale the values between minus one to plus one.

So overall you can think of the activation function or the mathematical function, which will help me

to scale the values.

We don't know yet as how the data will be processed, but we have understood that there is something

called as an activation function which will process the data.

Okay, that is what you need to remember as of now.

Now okay.

Once the output is given from the activation function, then data moves from left to right.

Okay.

So the input layer is where the data is given.

Hidden layer where we get the data from the input layer, process it and pass that overall information

to the output layer.

Activation function decides whether to fire a neuron or not.

And the output layer is the layer which bring up the information learned by the network to the outer

world.

To get the visual intuition, think about this image.

So I've got an input as a dog.

So this data will be sent to my input layer.

And this input is sent to my hidden layer.

And the processed data from the hidden layer is sent to my output layer for generation of prediction.

Okay, this is the overall intuition behind this deep learning neural networks.

So with this deep learning neural network we see the applications everywhere, to name a few.

This is used in automatic colorization of black and white images.

This is used in classification of the objects in an image, or detection of the objects in an image.

This is used in automatic image captioning.

So these are all some of the examples and the applications of deep learning neural network.

Now if you want to see the power of deep learning, you can also check out the movie.

The movie name is AlphaGo from the Google DeepMind.

So it is a very interesting movie where you would come to know the power of deep learning and the power

of neural network and its ability to find the patterns in the data.

And it was capable to, I would say, beat the top number one player in the strategy game that is go.

So it's an interesting movie, which will help you to understand the importance of deep learning neural

networks.

So with this overall understanding that you now have with the deep learning, let's see how we can implement

it from a technical side using the TensorFlow library.

That will give us a better way to understand its working in a practical manner.

So I'll see you in the next video.