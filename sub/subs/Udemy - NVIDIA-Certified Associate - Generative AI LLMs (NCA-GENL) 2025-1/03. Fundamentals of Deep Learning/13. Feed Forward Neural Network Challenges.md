# 13. Feed Forward Neural Network Challenges

---

WEBVTT

So in this video let's understand the concept of neural network.

Why is it important and what is the core principle behind this recurrent neural network.

So that's the learning journey in this video.

So let's understand the same now throughout your learning journey.

Till now you have actually made use of machine learning.

So using the machine learning concept.

So you are able to learn the patterns that are present in the data set.

So when we talk about machine learning one key feature that you have to remember is in case of machine

learning.

So we are going to work with the data set.

So if I want to describe how does the data would look like assuming that this is one column of the data.

And like this I've got multiple columns in my data set okay.

So each box over here represents the input okay.

And along with the representation of these inputs.

So I also have an output data.

So this is my Y which is my output okay.

And we will have the features I'll call it as x one okay.

We'll call this as an X1X2 okay.

And the x three.

So these are the three features that we have it on our data set.

Now when we talk about our text data.

So we will also have a similar pattern isn't it guys.

So X1X2X3 is nothing but the individual words I might have in my vocabulary.

Or it could be the word embedding representation of my individual words.

Now when we are processing the data, the way that we have processed the data is we have represented

these inputs, that is the features in a numerical format.

Okay.

Once we have represented the features in a numerical format, what we were doing is using this x and

y.

We were applying a machine learning model.

So assuming that this is my machine learning model.

So I was actually applying this entire data set on this machine learning model.

The idea is, by the application of this machine learning model, I am able to learn the patterns between

x and y.

So the intention is learn the patterns between input and output.

So this is what we were doing till now.

So finding the patterns between input and output.

That's the core idea of machine learning.

Now what happens is, in case of machine learning model, the way that I'm going to learn from the given

data set, it is dependent on the complexity of the machine learning model.

So we've got various machine learning model.

Now let me just name some of the common machine learning model for classification.

We've got log loss logistic regression Decision tree classifier.

And uh, we have got Naive Bayes.

And we also have the algorithm called.

KNN classifier that is k neighbors classifier.

And we have an algorithm called the random forest.

Bagging model boosting model.

So these are the various types of machine learning algorithms that we have got right now where I can

send my data, that is x and Y.

Having the features represented in a numerical format.

Apply this data on this machine learning model using the various options that I have got with me.

And then once I apply the machine learning model.

So I'll be finding the patterns between x and y.

This finding the pattern is dependent on the complexity of the machine learning model.

For example in case of logistic regression model.

So I would expect that the data is linearly separable and then linearly separable.

So that's one of the key thing.

And another thing is it expects that there is I like I can divide this data set by drawing a kind of

a linear plane.

So that's the intuition behind this logistic regression.

And the same is the scenario with decision tree classifier.

Now while working with decision tree classifier there are very high chances of overfit as well.

And while working with Naive Bayes algorithm, it has the assumption that my features are actually independent

of each other and KNN classifier if I'm using this technique.

So one of the disadvantage that I'll have is like I'll be saving my entire data set.

Now, once I save my entire data set, I'll be finding the distance from my new data point to the entire

training data set.

So computation will be very slow in case of KNN classifier and coming to random forest algorithm, this

is little better because here we are applying the bootstrapping and the aggregation in this scenario.

So bootstrapping plus aggregation.

Okay.

So as a result of this we'll get an advantage.

But still this is not of I would say the foolproof solution.

So what it has resulted is okay, researchers have come across with a flexible algorithm where we can

increase the complexity as per our requirement so that we can effectively learn the patterns that is

present in the data set.

So that is where the deep learning came into picture.

So the deep learning is a subset of machine learning.

So if I want to give you these notes I'll say this as a subset of machine learning.

Now being a subset of machine learning, what I'll be doing right now is I'll be finding the patterns

that is present in the data set with what we call it as a artificial neural networks.

So I'll be making use of the artificial neural network in order to find the patterns between my input

and output, that is x and y.

Okay, I'll find the patterns between x and y using this artificial neural network.

So as a result of this the advantage that we get is.

So since we are now using the artificial neural network, the advantages is can now learn the complex

pattern in the data set.

So here with this neural network we are able to classify the image in a much better way.

And we are able to learn the complex pattern in a data set and can fit the data into any domain.

And another advantage that we have come across when we are using this deep learning technique is in

this scenario.

So what we have found is so like when we are making use of the deep learning algorithm, there is no

feature engineering required or feature engineering or feature learning so or feature selection.

So feature selection is required because everything will be taken care of automatically.

So these are some of the uh major advantages that we have seen with deep learning neural networks.

Now now if I want to show you the pattern of this deep learning neural network and describe the parts

of this deep learning neural network.

So this is how a typical neural network would look like.

Okay, I'll take you with a screenshot so that you'll understand what is happening in case of neural

network.

So if I draw this so this is how a neural network would look like.

So in this scenario we'll have the inputs at the start Okay.

We will have the inputs.

I'll call this as x one and x two.

Okay.

So x one and x two are these two inputs.

So what I'll have is I'll have the connection between my inputs.

So assuming that I have it here.

So this is one input okay.

This is one input and this is another input.

So I have two input in this example.

Okay.

These two inputs are connected to a neural network.

These boxes that you see right here.

These boxes we call it as an individual neurons.

Okay.

These are called as individual neurons.

And in this layer we call it as an individual layer.

So this layer is called as the first hidden layer, because this is the first layer that I see.

This is my first hidden layer.

And the next layer that you see next to it is the second hidden layer.

Okay.

This is actually my second hidden layer.

So I've got x1 and x2.

so we call x1 and x2 are the inputs.

So overall the data that we call it as an input layer.

So in my input layer I have two inputs x one and x two.

This input layer is connected to my first hidden layer.

And in my first hidden layer we have four neurons.

Each box represents my individual neuron.

Hence you see this over here as four neuron.

And in my second hidden layer I've got two neuron.

So overall in this scenario I've got two hidden layers okay I've got two hidden layers.

And then at the end I will have an output okay.

So assuming that the output is one output I'll call it as y.

So this y output that you see right here we call this as an output layer.

So in case of deep learning neural network we'll be dealing with input layer hidden layer and the output

layer.

These are the parts of my deep learning neural network.

Now having these deep learning neural network which look like this.

The best part is using this complex neural network or the structure that you see right here.

We are able to learn the complex patterns that is present in the data set.

Now let me illustrate the power of deep learning to you.

So I'll take you through the example.

So this is the neural network playground now for the demonstration purpose.

So I have got two inputs x one and x two okay.

These are the two inputs.

And I have got one hidden layer in this example.

And there is one neuron.

Okay.

So here the pattern that we observe in this data set is there is a concentric circle.

And for the activation function I'll make use it as sigmoid.

So this overall structure is the simulation of logistic regression model.

So what I'm trying to do is with this given pattern of concentric circle, I'm trying to split the data

set or split this concentric circle which are two, which are having two classes.

I'm able to represent those classes with the help of a single logistic regression model.

Now think about it.

Is it realistically possible to perform the activity of classification using the logistic regression

model?

So just think about it.

See, when I say logistic regression model, I'm interested in drawing a straight line so I can draw

a straight line like this I can draw straight line like this.

So no matter where I draw the straight line, I will not be able to find the patterns in this data set

and split the data in an efficient way.

So this is the major challenge that we see right now with the help of a single logistic regression model.

So we call it as a sigmoid model from the neural network perspective.

If I do the training as well.

See, once I press the play button, the training is taking place.

Now.

It has created a decision boundary and it has drawn a decision boundary right here.

Now with the decision boundary that you see right here.

So one major thing that we would observe is so the model is not able to separate the classes in an efficient

way.

There are a lot of misclassifications.

So in this region especially there are a lot of misclassification.

The orange data points is being classified as the blue data points.

So there is a misclassification that is being taking place.

Since there are a lot of misclassifications that are happening in this scenario.

So what we have to do is we want a better model.

So what I'll be doing is, as I mentioned, the deep learning neural network is capable of learning

the more complex patterns in the data set.

So let's try to validate it whether the statement is correct or not.

So what I'll do this time is instead of having one hidden layer with one neuron, I'm going to increase

the number of neurons.

So I have that same two inputs in my input layer, but this time in my input layer.

In my hidden layer I'm having three neurons.

And in the output layer this is how it would look like.

Now I'll just begin the training and I want you to observe the overall outcome.

If I just click on the play button right now.

See, I'm now able to learn this pattern in an effective way.

So what it clearly proves is that the deep learning model is capable of learning the complex pattern

in the data set provided.

I have got the sufficient number of neurons in my given data set.

So if I've got the sufficient number of neurons in my neural network architecture, then I can learn

the complex pattern of any of any shape from the given data set.

So that is the overall idea.

And that's the advantage of this deep learning neural network.

Now with this thing in place okay, so we were able to learn the complex patterns in the data.

So what we have now understood is so I'll just come up.

So instead of using the machine learning model right now.

So what we can now do is so we can send that we can actually send the entire thing, the data that have

got into this deep learning neural network.

Okay.

I can send my data into the deep learning neural network.

I'm able to learn the complex pattern in this data set.

Now, this theory is good, but there are some challenges that we have got, especially when we talk

about the natural language processing.

So let's look into the challenges that we have got in applying the deep learning that is that is the

deep learning neural networks that we have got challenges of NLP with deep learning neural network.

So this kind of architecture that you see right now.

So this is actually known as feed forward neural network.

Okay.

This is known as the feed forward neural network.

The architecture that I've shown you right.

This is actually known as feed forward neural network.

So what is the challenges that we have got in NLP if we are applying this feed forward neural network.

So let's understand the same.

See, one of the major advantage that we see right now in if you are using this deep learning neural

network for NLP is the way that we are processing the data.

So just think about it.

Let's say I've got a data set okay I've got a text data that would look like this.

The text data says so dog was crossing the road and it was very tired.

Now when I have this text that would look like this.

Okay.

If I ask you what is this word it would refer to so as a human being, if I ask you what is this word

is actually referring to?

So obviously as a human being you can say that, hey, this word it is actually referring to the word

dog, because as a human being, it's very easy for us to say, okay, this word, it is actually referring

to the word dog.

But when you think about the data set and dividing the text into the individual features, so what does

it mean?

So what we'll be doing is instead of having the each and every word, so what we'll be doing in case

of natural language processing which you are aware of it already.

So you know that I'll be representing the every text that I have got with me with the help of a vector.

So I'll be representing the individual word in the format of a vector.

So I'll be representing it in the format of a vector.

So each word will have some vector representation.

So individual box represents the individual vectors of my individual words.

Okay.

That is what it would mean.

So for the demonstration purpose I'm just using some boxes like this.

So think that this is the vector representation of my individual words.

All of them will have the same length.

Okay.

So these boxes are nothing but the feature vectors okay I'm going to generate the feature vectors from

the given text data.

Now once I have the feature vectors presented with this feature vectors represented.

And so these feature vectors will now become x and I will have some output.

Okay I'll, I'll have some output data.

Maybe I'll call it as this will be my output that I want to predict.

Okay.

This will be my output that I want to predict.

Now in this scenario when I'm finding the relationship between X and Y, so I'll be processing the entire

X as a whole, and I'll not be able to learn the representation between the words that I have caught

in my sentence.

So are you able to follow along with me?

So when I have got the feature vectors for the text data, and I have got the Y when I apply the model.

Okay.

So let's say that I'll apply the model right now.

So it could be machine learning model model.

Or it could be deep learning model let's say deep learning model itself.

So when I apply a deep learning model by sending both x and y for my given data set okay.

So in this scenario okay, when I apply this x and y as a given data set, I'll try to learn everything

as a individual feature.

So this will be one feature okay I'll call it as x one okay I'll call it as x one okay.

And like likewise I'll have x two.

This will be my x one okay.

So then likewise I'll have X2X3 and so on.

So I'll be treating my individual features.

And the overall concept is each feature will be independent.

Each features will be independent when we are applying this feed forward neural network.

So dog will be one feature was will be another feature.

Crossing will be another feature.

There will be another feature.

Road will be another feature.

So each and every word will be the individual feature that we will have, and it will be treated independently.

When we are finding the patterns between the features and the target.

Now when we are working with the text data.

So we have to ensure that there is a relationship between the individual words so which we are unable

to find it with the help of this feedforward neural network.

So here in this scenario, in order to learn these patterns between the words, the way that we process

the data should be changed.

So which means I cannot simply go ahead and make use of this existing feedforward neural network.

So we have to find a better way to process these kind of text data, which is capable of remembering

as what has actually happened in my previous feature, considering that it should be able to process

the text data.

So we are in requirement of a neural network, which is capable of processing my previous features,

as well as understanding its relationship in the next word.

That's the overall idea or the that's the overall goal right now.

So the goal or the wish list that we now have is the goal is find the neural network, okay.

Find the neural network architecture, which is capable of remembering okay, which is capable of remembering

my previous input or the previous features.

So.

Or the neural network which is capable of understanding okay, the features are not actually independent.

Features are actually dependent to each other.

So that is the kind of a neural network that we are looking out for.

So that kind of neural network okay.

So that neural network is called as a recurrent neural network.

So recurrent neural network is a type of neural network.

It's actually a another type of neural network.

So what we will do is in the scenario of the recurrent neural network, we are going to get the data

in a specific time stamp.

We process the data in the format of a timestamp okay, while processing the data in the format of a

timestamp.

So each time when I'm processing the timestamp or each each time when I'm processing the individual

data, I'll, I'll be processing the current data as well as understanding what was my previous outcome.

So that is how we will be processing the data in the scenario of recurrent neural network.

All right guys.

So now in this video you have understood what are the challenges that we have got in the feedforward

neural network.

We'll stop the video over here in the next video.

Let's see what exactly is this recurrent neural network and how is it majorly different compared to

the feedforward neural network and why it is so special.

So I'll see you next time.