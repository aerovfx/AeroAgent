# 8. Hyperparameter Tuning in Deep Learning

---

WEBVTT

In this video, let us discuss about the hyperparameter tuning in the deep learning neural networks.

So you know already what exactly are these hyperparameters?

Hyperparameters are the variables that we need to set before we apply any learning algorithm on a given

data set.

So these are the parameters which will not be learned by a model or an algorithm.

So the challenge in case of deep learning is there is no magic number for every task.

The best numbers of the deep learning neural network depend on each task and the each data set.

Now, when we talk about building the neural network, you know already what are the various options

that we have got when we are constructing the neural network.

Once we create a model as a sequential and start adding the dense layers, I'll have the option to specify

the number of neurons in each layer, and I have the option to specify the activation function, number

of hidden layers, number of neurons in each layer.

So likewise we've got various options.

So here when I say hyperparameters in deep learning neural network so we can classify it into various

parts.

So we can say that the optimizer hyperparameters the optimizer hyperparameters is nothing but the parameter

or the technique that is being used to update the values of weights and bias term.

Now under this optimizers, the hyperparameters that we have got are learning rate.

We have got batch size.

We have the number of epochs.

So these are the optimizer hyperparameters.

And we also have the model specific hyperparameters to name a few.

We've got the number of hidden units of neuron and number of hidden layers.

The activation function.

These are the model specific hyperparameters.

So let's see how do we choose this various hyperparameters.

So let's start by having a look at the optimizer hyperparameters.

When we talk about the hyperparameters of this optimizers see one of the commonly used hyperparameter

is a learning rate, so the learning rate refers to the rate at which the updates of the weights and

bias terms will take place.

Here this weights and bias term.

As you know during the process of training, it gets updated.

Now if my learning rate is too small, as I have shown you in the intuition video as well, if it is

too small, then the optimal value, then it would take much larger time or a longer time.

It could be hundreds of hundreds or thousands of seconds, uh, or hundreds of thousands of epochs in

order to reach the ideal state.

Now, if the learning rate is too large, then the optimal value, then it would overshoot the ideal

state and our algorithm might not converge.

So it is very important that we are going to use the optimal learning rate whenever we are specifying

it inside the compile method.

Now in this example I am saying that optimizer is equal to tf dot keras dot optimizer dot sgd learning

rate as 0.01.

So this is how we can specify the learning rate whenever we are calling the compile method.

Then we have something called as batch size.

Now you know the technical working of the batch size from the last video from the hyper parameter perspective.

So batch size is the number of training instances that will be used by the model to complete one forward

and one backward pass.

Now when it comes to the tuning of this, a small mini batch size induces more noise during the training

or the error calculation.

And this is preferred in case if my training process is stopping at the local minima.

Okay, if I feel like my model hyperparameters are stopping at the local minima.

In that case, the small mini batch size is actually preferred.

Now we choose the.

If I choose a larger mini batch size, this is going to give me a computational boost, but it comes

at the expense of needing more memory for the training training process.

The reason?

Because it has to store my entire data set on my Ram, and then it will be able to process the data.

From the perspective of computing, it is always recommended that you choose the maximum size of batch

size that can be fit in the memory for the optimal performance of deep learning models.

So this is about the batch size.

Okay moving on.

We have got the epochs.

In most of the scenarios.

The epochs are actually not required to be tweaked.

The reason because whenever we are implementing it we will employ the early stopping.

So what is this early stopping.

Early stopping is actually a technique which we use it in deep learning training where we are going

to monitor the model training and stop the model training when the model performance stops improving

on the validation data set.

And that is called as a early stopping technique.

So these are the hyperparameter tuning techniques for the optimizers of the model.

Now let's have a look into the hyperparameter tuning for the model parameters.

So starting with number of hidden layers and the number of units in each hidden layer.

So as you know already, as the complexity of the data representation increases, this would definitely

require more number of neurons and more number of hidden layers.

In order to find the patterns between my given input and the target.

Now, even in this case, there is no magic number around this, but whenever you are determining the

number of hidden layers and the number of neurons so you can employ the strategies like experimentation,

try out with various values on the data set that is specific to you, and see if you can come up with

a number which is suitable for the given data set.

And use your past experience and the intuition in determining the number of layers and the units of

neurons in each task and while working, always prefer to increase depth.

Okay, always prefer to increase depth.

And you can also borrow the ideas from the research paper in determining what are the values that will

be suitable for the similar task.

So these are the ways in which you can determine the number of hidden layers and then units.

And coming to the activation function.

See in case of activation function.

So when we are talking about deep learning neural network, especially during the case of classifiers,

the sigmoid function and their combination, it really work well in the case of classifiers.

But as we have studied in our previous learning journey, sigmoid and tanh activation function are avoided

in some scenarios because of the vanishing gradient problem, which we will talk about it in our next

video.

Okay.

And then ReLU activation function is a general activation function which is most used activation function.

And even I have spoken about this multiple times.

So ReLU is one of the most used general activation function.

And in case if you observe any dead neurons in the neural network, that means if we see that the neurons

are not getting updated, then at that time Leakyrelu will be a best choice.

Because here, in case of leakyrelu, even when the values goes in the negative, it would still make

the function differentiable.

And this ReLU activation function is used only on the hidden layers.

Okay, it is used only on the hidden layers and always start with ReLU.

Then you can proceed with other activation functions based on the result.

Okay, so this was about the various hyperparameters that we can tune in the case of deep learning neural

network.

So with this we come to the end of this video.

In the next video let's talk about the vanishing and the exploding gradient problems whenever we are

working with deep learning neural network.