# 3. Introduction to Tensorflow & Create first Neural Network

---

WEBVTT

In this video, let's have a look into the basics of TensorFlow library.

Now implementation with the TensorFlow library is purely optional.

So just to let you know.

So this examination is not biased towards TensorFlow or PyTorch.

So frankly speaking it does not care whether you use the PyTorch or TensorFlow.

But the reason that I'm giving you this demonstration with the help of TensorFlow library, because

you need to know one to follow along with me and build an intuitive understanding about me.

Okay, about this deep learning if you want to get an intuitive understanding.

So as you as you have learned in with me till now, so I'll be explaining you in an intuitive way with

a practical example.

That's the reason I'm going.

I'm going over the basics of this TensorFlow so that you can follow along with me in this overall hands

on journey, when we are building the main intuitions from the deep learning neural network.

So this TensorFlow is a open source library from Google.

Okay, it has got a rich community and using this we can build the state of the art machine learning

models.

And we can also deploy ML powered applications.

So this is the main advantage of this TensorFlow library.

See some of the key advantages of this TensorFlow library is.

This TensorFlow library has a computational graph which is the automatic differentiation.

So with this ability.

So we don't have to manually apply the differentiation.

That is the application of back propagation.

It will be done automatically.

And this has got an efficient library management as this is backed by Google and it provides the debugging

ability and it provides the scalability on the big data, I can create the pipelines.

And for creation of visualization, we can also make use of TensorBoard for effectively monitoring our

deep learning model performance.

So these are the advantages of of using this TensorFlow.

Now in order to get started first thing that you need to do is just run the command pip install TensorFlow.

Okay.

You can directly go ahead and install the TensorFlow.

And that should be enough for you to get started with this TensorFlow library.

Now when we talk about the building of the deep learning neural network.

So when we are building a deep learning neural network using the TensorFlow library, we have to follow

some general guidelines.

So let's have a look into the general guidelines of building a neural network with TensorFlow.

Because that is what we will be building in order to understand the key intuition of forward pass,

backward pass and all the related aspects of deep learning.

So when we are building a model.

Now here, whenever we are doing any import, we import as TensorFlow as TF.

So inside the TF we have a Keras.

Now tf dot Keras is the TensorFlow's implementation of the Keras API, which is an high level API in

order to build and train the models, and which actually makes the TensorFlow an easier to use one.

Now, whenever we are creating the deep learning neural network in TensorFlow, we are going to make

use of sequential class.

Now this sequential class represents the linear stack of layers where I'll be stacking one layer next

to the another layer.

So we'll create the model as a sequential.

And once we create the model as a sequential, we are going to add the individual layers with the help

of model dot add.

To give you an example here, I have mentioned as model is equal to tf dot k.

And then for this model I'm going to add a dense layer where every neuron in my previous layer is connected

to every other neuron in the next layer.

That's what we call it as a dense layer.

Now by creating it as a dense layer with three neurons and the input shape as 784.

So this is how I'm adding my first dense layer over here.

Okay.

This is how we can add the layers for the sequential model.

Now inside this deep learning neural network we have learned that we can specify the activation function.

Now these activation functions help us to scale the values.

Now in order to specify the activation function, we can say that activation is equal to.

Then specify the activation function name.

So this TensorFlow Keras module supports some of these standard activation functions that are used across

in deep learning.

And if you want, you can also create your own activation functions as well.

So in order to create an activation function, you just have to subclass the tf dot Keras dot activations.

Now the commonly used activation functions are sigmoid, softmax, tanh and ReLU.

So these are the commonly used activation functions.

Now once we build the model by defining the architecture, the next thing that we generally do is we

will start by compiling the model.

In order to compile the model, we will just say model.compile, and we will have to specify the optimizer

loss function and the metrics.

So here optimizer is nothing.

But how should my model parameters be updated.

How does this model parameters to be updated is decided by this model optimizer.

Then loss function is nothing but the value that my model has to tune whenever it is performing the

activity of training.

And we have the metrics.

The metrics is nothing.

But how do we evaluate the model at each training step?

So these are the key commonly used attributes inside this compile method.

So we specify all these inputs to our compile method which will be used for model compilation here when

we talk about the optimizer.

So this is actually a learning algorithm.

It is an algorithm that will be used to update the weights and the bias terms of a model.

Some of the popular optimizers are SGD which is known as stochastic gradient descent.

We have the Rmsprop.

We have the Adam.

So these are the three most popular optimizers.

So out of this I would say Adam is the one which performs really well for most of the data.

Then at the second place we've got Rmsprop and the third we've got the stochastic gradient descent.

So we can say that Model.compile optimizer is equal to.

And then we can specify the optimizer name.

Now we have got the loss function.

So loss function is nothing but an objective function.

It is a value that my optimizer has to refer to in order to update the weights and bias terms of the

neurons.

Some of the popularly used loss functions are MSE, which is known as mean squared error.

Binary cross entropy.

For binary logarithmic loss, which is same as the log loss, or the categorical cross entropy for multi-class

logarithmic loss.

Okay.

And coming to the metrics.

So the metrics that we generally use is the accuracy okay.

So these are the three important inputs that we specify for the model dot compile method.

Now once we specify all these things we will find the patterns between the x and y.

In order to find the patterns we will make use of fit method and we will send in the x and y.

And we can also send in our validation data in order to validate the performance of the machine learning

algorithm, in this case the deep learning algorithm.

Okay.

So this is the overall way in which we are going to build the model.

Now that you have the foundation of this, let's go ahead and let's see how we can create our very first

neural network using TensorFlow.

And then I'll take you through the basics and the intuition behind its working, which is very important

for the examination perspective.

All right folks, so let me just launch my Jupyter notebook.

I'm going to launch my Jupyter notebook which has got the TensorFlow library installed.

So if you don't have it, just go ahead and run the command pip install TensorFlow.

Okay.

So I've started by importing this TensorFlow library.

So pip import TensorFlow as tf.

Now the data set that I'm going to use is the fashion mNIST data set.

So I'm going to say fashion underscore mNIST is equal to TF dot keras dot datasets dot fashion underscore

mNIST dot load underscore data.

Okay.

So this is going to load the fashion mNIST data.

Now this will be loaded in the format of a tuple for training and the test data.

So let me just separate the training and the test data.

I'll say x train full comma y train full comma.

Okay I'll just say x test.

Comma y test is equal to fashion mNIST.

Now what I'll do is I'm going to keep aside okay.

I'll just go ahead and keep aside the last 5000 rows as the test data.

That is the validation data.

And I'll keep it remaining as a training data.

I'll say x train comma y underscore train is equal to X train full here.

I'm going to go till -5000 okay.

Y train full -5000.

Okay.

In the same way from this x rainfall, I'm going to create x val and the y val.

That is my validation data.

So that means I'm splitting the data into three parts.

Okay.

Now once this data has been split let us go ahead and uh.

display the shape of this X train.

So if I display the shape of my X train.

So my X train is having 55,000 rows.

Okay.

And here in the 55,000 rows, uh, that means I have got 55,000 images.

And each image is of the shape 28 by 28.

So that's the shape of the image, Okay.

Now when we are dealing with the image data, the first thing that we normally do is we will perform

the min max scaler.

That is the normalization.

Now let's see the shape that we have got on this data that I have got.

Okay I'll say NP dot max x train and I'll also get the minimum value.

So this will help me to perform the normalization.

See the maximum value is 255 and the minimum value is zero.

So what we can do is I'll we can divide the training test and the validation data by 255.

This would ensure that we have the values in the range of zero and one.

Okay.

So we will have the value size zero and one.

So let's just do that activity.

See once this is done we can validate that my xtrain is having the maximum value as one and minimum

as zero.

Now we can go ahead and create a visualization as well.

I'll say import matplot lib dot pyplot as plt plt dot I am show I can just in exam I can just pick up

an example image data.

Okay.

And I'm going to say cmap is equal to binary.

So if I create a visualization.

So this will just display how does this image would look like.

See this contains the image of shoe.

So this data set that we have got is a fashion data set here in this fashion data set the class names

that we have got are t shirt top trouser pullover.

So these are the various class names that we have got.

Now to get a better idea about it okay.

let me just go ahead and create a visualization.

So if I create a custom function and create a visualization, see these are the random example images

that we have got on this data set.

We've got ankle boot.

This belongs to the t shirt or top.

This is dress sandal.

So these are the various example images that we have got on this data set.

So this is how our image data would look like.

So first thing just to have a recap each and every image that I have got.

This is of the shape of 28 comma 28.

Okay.

Since this is a black and white image, this is represented in the format of a two dimensional matrix.

Now, in order to build a model I'll start by specifying my model as sequential.

So I'm going to say from TensorFlow dot okay I'm going to import sequential next from TensorFlow because

I'm going to import the various layers I'm going to import my input layer, I'm going to import a dense

layer.

And I'm also going to import a flatten layer.

Now when we are sending the data to our neural network we have to first perform the activity of flattening.

So first thing is I'll create a model as sequential.

Now for this model we are going to add the individual layer.

I'll say model dot add.

First thing is I'll create an input layer for this input layer.

My input is of the shape.

So what is the shape of my input?

My input is of the shape 28 comma 28 because that's the shape of my individual image.

Next, before we send the data to our deep learning neural network, we have to perform the flattening.

That means we have to convert the multi-dimensional data into a single dimension.

Hence we'll just send the data to the flattening.

Alternatively, we can also pre-process the data.

Okay now I'll say model dot add.

And I'll add a dense layer.

So for dense layer I'll mention it as 100 comma activation is equal to ReLU.

So I'm going to apply the ReLU activation function.

And in the second time I'll mention as let's say 75, I'll keep that activation as ReLU.

And in the final layer I'm going to mention as ten neurons and the activation, I'll just mention it

as softmax.

Okay.

We'll mention the activation as softmax because in case of multi-class classification, see the task

that we are doing right now okay is a multi-class classification for this task of multi-class classification,

there are actually ten classes.

Hence, in my final layout I am saying that ten neurons.

Okay, once that is done, I'm going to display the summary of the model.

So if I display the summary this is how the summary would look like okay.

So here we have the total number of training parameters as 86835.

Okay.

This is how the total number of parameters in our neural network that we have to train.

So we can create a model okay.

Now if I want I can also go ahead and create a visualization as well.

I'll say tf dot keras, dot utils dot plot underscore model.

And we can just send send in the model and I'll say show shapes is equal to true.

Okay.

So it's not giving me the visualization okay.

What you can do is you can actually go ahead and uh, install this pi dot to create a visualization.

It will just show us the overall idea as what is the structure of the model.

Okay, that is the core idea team.

So coming back over here, we have seen as how we can construct the deep learning model using the TensorFlow.

Now once we have constructed okay, the next thing that we have to do is we'll have to perform the compilation.

I'll say Model.compile mention the loss function optimizer and the metrics.

Then we can go ahead and perform the activity of fit.

Okay, so I'll say history is equal to model dot fit.

Send in my x train y train number of epochs and my validation data.

So this would go ahead and perform the fit on the given data set.

So here.

Okay, now the training is happening.

I just switched from Jupyter Notebook to the usual colab to avoid the issues in the environment.

Okay, now the training is happening.

So what you have just seen is here.

As part of this video, we have just learned as how we can create a deep learning neural networks in

the TensorFlow.

So we can define the models as a sequential.

And once we define the model as a sequential.

Next thing that we will do is we will then go ahead and add the individual layers.

And after adding of individual layers will perform the compilation.

Then we can go ahead and perform the fit.

Alright team.

So this was about the basics of TensorFlow which would help you to understand the following code as

we progress in the learning journey.

So I'll stop the video over here.

In the next video let's have an intuitive understanding of how the data is getting processed inside

this deep learning neural network.

So I'll see you next time.