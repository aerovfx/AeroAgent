# 6. NVIDIA Ecosystem tools for LLM Model Training

---

WEBVTT

Now moving on.

Let's look at the Nvidia ecosystem tools for LM model training.

Now in order to perform the LM model training we have Nvidia AI platform for Llms.

So here we have got the Nemo framework, which is an open source framework which is designed for building,

training and deploying the state of the art conversational AI models, which will include the Llms.

So this actually provides the pre-built modules, recipes and tools for data preprocessing model training,

optimization and deployment.

And we have the Nemo Megatron, which is a powerful framework within Nemo that focuses on large scale

transformer based models.

So this has got some of the features such as.

It leverages the model parallelism technique, so that this will enable the efficient training of the

large language model across multiple GPUs or node.

Then we have something called as Nvidia Triton Inference Server.

So which is a high performance inference server that will enable the efficient deployment of Llms in

production environment.

So it supports wide range of deep learning frameworks.

And it allows for flexible scaling to meet the demanding workload.

Then we have the Nvidia AI enterprise.

This is an end to end software suite that provides the enterprise grade tools and support for deployment

and managing the AI workload, including the large language models in production environment.

Now, apart from that, we also can make use of specialized hardware and software so we can make use

of Nvidia A100 Tensor Core GPUs.

So they are generally known as the high performance CPU, specifically designed for accelerating the

AI workload, including the LLM training, and we have DGX system.

They are the AI supercomputers pre-integrated with the Nvidia GPUs, and it is also integrated with

networking and software stack two.

So they provide a powerful platform for training and deploying the large language models at scale.

Then tensor RT, which is an SDK for high performance deep learning inference and it optimizes the trained

LM models for deployment on Nvidia GPUs.

And this will improve the inference speed and this will reduce the latency.

Then we also have the additional tools and the resources.

So we have NGC catalog.

So this NGC catalog offers the pre-trained models containers help chart for various AI tasks.

This will include the large language model training and we have the Deep Learning Institute.

You can refer to the training materials and workshop and developer blog and forums.

So these are all the key things that you can utilize guys.

So to end this video I'll just give you an example workflow for LM training with Nvidia tools.

So for data preparation you can make use of data pre-processing from Nemo.

And for training you can make use of Nemo Megatron, which will help us to perform the training on Nvidia

GPUs.

We can optimize the model using Tensorrt, then deploy it using the Triton inference server.

So these are the key services which you always have to remember.

So you might see me repeating this multiple time, because this workflow will give you a better perspective

as how you can use the various services and the softwares that we have got in Nvidia for deployment.

So some of the key benefits of ecosystem of Nvidia are one is accelerated training, simplify development

and most importantly scaling and the enterprise grade support.

So with this we come to the end of this section on prompt Engineering guys.

So I'll see you next time.