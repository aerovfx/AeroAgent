# 1. What is Prompt Engineering

---

WEBVTT

In this section, we are going to learn about one of the interesting topic while working with large

language model that is prompt engineering.

So let us start by understanding what is this prompt engineering.

Now when I say prompt engineering, I can say it as a art and science of crafting the effective instruction

or the prompt so that you can guide the large language models and elicit the desired responses.

So I can say that this prompt engineering is one of the crucial skill that anyone should have.

Now, at its core, the prompt engineering involves designing inputting the text, and this clearly

should convey the intent to the large language model.

Now this should include the instruction where we should clearly define the task.

Now this is the task that the model is expected to perform.

We have to provide the context which means the relevant background information or the example, so that

the model can understand the request.

The constraint which specifies any limitation on the output such as length, format or style, and along

with that the output indicators.

So, using the specific phrases or tokens, we should indicate the desired type of response from the

large language model.

This can be summarized.

Translate.

writing a poem, and so on.

Now the question that you might think is why should we use this prompt engineering?

Or what is the overall idea behind this?

Well, by effectively crafting the prompt, we can actually significantly improve the accuracy and the

relevance of the output that is generated by these large language model and by crafting the prompts

in an efficient manner.

This will also allow us to customize the style tonality of the prompt, that is, the tonality of the

response from the large language model and even the format of the generated text, so that it will suit

our specific requirement.

And this will also help us in reduced bias.

So by carefully designing the prompt, it can also help us to mitigate the biases that might be present

in the model's training data and, more importantly, enhance control.

So with prompt engineering, it gives us more control over the lm's behavior.

This will enable us to use it more effectively for various tasks.

Now that you have understood about the basic idea about this prompt engineering, let's understand what

are the key techniques that we have got while performing this prompt engineering.

Now, during the prompt engineering, one of the key techniques that we have got is the zero shot prompt

engineering.

Now apart from that, we also have the few shot prompting and the chain of thought prompting.

So let's understand each of them.

Now when we say zero shot, this would mean that here in this case you will not provide the models or

you not provide the examples in the prompt.

Okay, so here what you will do is you will make use of the foundation model.

Now when I say foundation model, a model that has been already trained on a very large data set, you

will pick up that foundation model which has got the overall general knowledge.

And this is a model which has not been trained or tuned on a specific task.

Now for that model, you would go ahead and just provide the prompt to do a certain task and you will

not provide any example.

Now this is called as zero shot learning like an example can be generate me the translation of this

particular word.

Now in that scenario you are just prompting the large language model as what is the task it has to do.

So you're not giving any examples as this is the examples.

These are a set of prompt and responses.

Now go ahead.

Similarly generate the response for this prompt.

So here in this case you're making use of the model's knowledge itself.

On the other hand we have something called as few shot learning.

Now this is the another type of prompting.

Here.

You will provide few examples in the prompt that would provide the guidance for the model's behavior.

Then we have something called as chain of thought prompting.

Now when we say chain of thought prompting, this is about encouraging the model to reason step by step

before it produces an answer.

So this is called as chain of thought prompting.

So these are all the various types of prompting that we have got while working with large language model.

Then we have another technique in prompt engineering that's called as instruction tuning.

So when we say instruction tuning, this involves fine tuning the model on a data set of the given instruction

and the responses so that we can improve its ability to follow the instruction.

Now this is called as instruction tuning where you will provide a set of instruction.

Then the another most common technique that's called as a prompt template where we will provide a pre-designed

template so that we can create the.

We can actually quickly create the effective prompts for the common task.

The usage of the prompt template.

So as you can clearly see these techniques are used.

So when it comes to creation of the prompt so that we can effectively get the required output from the

large language model.

So that is the key takeaway from this slide.

Now apart from that we also have other techniques as well.

If I want to name some more technique we have something called as prompt chaining.

Now in the case of this prompt chaining, it involves breaking down the complex task into a sequence

of simpler prompt.

Now here each prompt will build on the output of the previous one.

So this is called as prompt chaining technique.

And there's another technique that's called as the system message.

Now when we say system message this will involve providing the high level description of the assistance

tool and the behavior so that we can guide its response.

So what are the best practices that we need to follow when we are performing this prompt engineering.

So one of the key thing is always be clear and specific because by clearly articulating the task, the

format and the constraint will be able to get the output that is best possible from that large language

model.

And more importantly, provide the context.

Give the model the relevant information so that it can understand the task and they understand the ask.

Third thing, experiment and iterate.

So always try different prompt refine them based on the model's responses, then prioritize the safety.

Always try to incorporate safety guidelines and the constraint in order to prevent the harmful output.

And lastly, always use community resources.

So explore online communities, forums and the resources for the tips and best practices of this prompt.

Engineering.

Because the LMS are improving at a rapid pace, so it's also important that you always follow the best

practices, which is suitable for the latest version of the LLM.

So I hope this gives you a basic idea about what is prompt engineering and the techniques that we can

follow in this prompt engineering.

From the examination standpoint, you should be aware of the key techniques that is generally used and

the definition of this prompt engineering.

So with this we come to the end of this video and I'll see you next time.