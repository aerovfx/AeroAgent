# 1. Giới thiệu và kiến ​​trúc YOLOv8

---

YOLO.

Đây là phiên bản mới nhất của YOLO được Ultralytics phát triển như một phiên bản tiên tiến, hiện đại

mô hình.

YOLO Eight xây dựng dựa trên sự thành công của các phiên bản trước, giới thiệu các tính năng và cải tiến mới

để nâng cao hiệu suất, tính linh hoạt và hiệu quả.

YOLO 8 thực hiện đầy đủ các nhiệm vụ AI về thị giác bao gồm phát hiện, phân đoạn, ước tính tư thế,

theo dõi, phân loại.

Tính linh hoạt này cho phép người dùng tận dụng tám khả năng của YOLO trên các ứng dụng và miền đa dạng.

Nguồn gốc của YOLO là một khung trí tuệ nhân tạo hỗ trợ nhiều tác vụ thị giác máy tính.

Khung YOLO tám có thể được sử dụng để thực hiện phát hiện và theo dõi đối tượng.

Phát hiện đối tượng là một nhiệm vụ liên quan đến việc xác định vị trí và loại đối tượng trong ảnh

hoặc luồng video.

Đầu ra của bộ dò tìm đối tượng là một tập hợp các hộp giới hạn bao quanh các đối tượng trong ảnh,

cùng với nhãn lớp và điểm tin cậy cho mỗi ô.

Phát hiện đối tượng là một lựa chọn tốt khi bạn cần xác định các đối tượng quan tâm trong một cảnh, nhưng không

cần biết chính xác vị trí của đối tượng hoặc hình dạng chính xác của nó trong trường hợp theo dõi đối tượng.

Ngoài ra, với tính năng phát hiện đối tượng, bạn còn theo dõi được đường đi của đối tượng.

YOLO tám cũng được sử dụng để phân đoạn phiên bản.

Phân đoạn phiên bản tiến một bước xa hơn so với phát hiện đối tượng và liên quan đến việc xác định từng cá nhân

các đối tượng trong ảnh và phân chia chúng khỏi phần còn lại của ảnh.

Đầu ra của mô hình phân đoạn phiên bản là một tập hợp các mặt nạ hoặc bộ đếm phác thảo từng đối tượng

trong hình ảnh, cùng với nhãn lớp và điểm tin cậy cho từng đối tượng.

Phân đoạn phiên bản rất hữu ích khi bạn không chỉ cần biết vị trí của các đối tượng trong hình ảnh mà còn

mà còn hình dạng chính xác của chúng là gì.

YOLO 8 cũng có thể được sử dụng để phân loại hình ảnh.

Phân loại hình ảnh là nhiệm vụ đơn giản nhất và bao gồm việc phân loại toàn bộ hình ảnh thành một trong một tập hợp

của các lớp được xác định trước.

Đầu ra của bộ phân loại hình ảnh là một nhãn lớp duy nhất và điểm tin cậy.

Phân loại hình ảnh rất hữu ích khi bạn chỉ cần biết hình ảnh thuộc về loại nào và không

cần biết các đối tượng của lớp đó nằm ở đâu hoặc hình dạng chính xác của chúng là gì.

YOLO tám cũng có thể được sử dụng để ước tính tư thế.

Ước tính tư thế là một nhiệm vụ liên quan đến việc xác định vị trí của các điểm cụ thể trong ảnh,

thường đề cập đến một điểm quan trọng.

Các điểm chính có thể đại diện cho nhiều phần khác nhau của đối tượng, chẳng hạn như các điểm nối, cột mốc hoặc các điểm đặc biệt khác.

tính năng.

Vị trí của các điểm chính thường được biểu diễn dưới dạng tập hợp tọa độ 2D hoặc 3D.

Đầu ra của mô hình ước tính tư thế là một tập hợp các điểm đại diện cho các điểm chính trên một đối tượng

trong hình ảnh, thường kèm theo điểm tin cậy cho từng điểm.

Ước tính tư thế là một lựa chọn tốt khi bạn cần xác định các phần cụ thể của một đối tượng trong một cảnh và

vị trí của chúng trong mối quan hệ với nhau.

Vậy là cuối cùng bạn đã đến với Ultralytics YOLO tám.

Phiên bản mới nhất của YOLO, được phát hành vào tháng 1 năm 2023, YOLO 8 vẫn chưa có bản phát hành chính thức

giấy nhưng lại tăng độ chính xác cao hơn và tốc độ nhanh hơn.

YOLO tám có năm biến thể dựa trên số lượng tham số.

Nó bao gồm các biến thể nano, nhỏ, trung bình, lớn và cực lớn của YOLO tám.

Bạn có thể sử dụng tất cả các biến thể để phát hiện đối tượng và phân đoạn đối tượng, v.v.

Các tính năng chính của YOLO tám bao gồm tăng cường dữ liệu khảm.

Tăng cường dữ liệu khảm như YOLO 4.

YOLO tám sử dụng tính năng tăng cường dữ liệu khảm để trộn bốn hình ảnh để cung cấp cho mô hình bối cảnh tốt hơn

thông tin.

Sự thay đổi trong YOLO phiên bản tám là quá trình tăng cường dừng lại trong mười kỷ nguyên đào tạo gần nhất để cải thiện

hiệu suất và các tính năng quan trọng khác trong YOLO 8 là phát hiện không có neo.

YOLO tám chuyển sang phát hiện không có neo để cải thiện khả năng khái quát hóa.

Vấn đề với việc phát hiện dựa trên mỏ neo là các hộp neo được xác định trước làm giảm tốc độ học tập.

của các tập dữ liệu tùy chỉnh.

Với tính năng phát hiện không có neo, mô hình dự đoán trực tiếp điểm giữa của đối tượng và giảm số lượng

của các dự đoán về hộp giới hạn.

Điều này giúp tăng tốc độ triệt tiêu không tối đa, đây là bước tiền xử lý loại bỏ các lỗi không chính xác.

dự đoán.

Một tính năng quan trọng khác trong YOLO 8 là mô-đun C2F.

Xương sống của mô hình YOLO 8 hiện bao gồm mô-đun AC2F thay vì AC31.

Chúng ta sẽ xem chi tiết mô-đun C2F này, sau đó chúng ta sẽ xem kiến trúc và các sửa đổi quan trọng khác

trong YOLO tám được tách rời.

Cái đầu.

Phần đầu không còn thực hiện việc phân loại nữa.

Và hồi quy cùng nhau.

Thay vào đó, nó thực hiện nhiệm vụ một cách riêng biệt, giúp tăng hiệu suất của mô hình.

Một tính năng quan trọng khác của YOLO là hàm mất mát được sửa đổi.

Có thể xảy ra hiện tượng mất điều chỉnh sai do phần đầu được tách rời tách biệt các nhiệm vụ phân loại và hồi quy.

Điều đó có nghĩa là mô hình có thể bản địa hóa một đối tượng trong khi phân loại đối tượng khác.

Giải pháp là bao gồm điểm căn chỉnh nhiệm vụ dựa vào đó mô hình biết được điểm tích cực và tiêu cực.

mẫu.

Điểm căn chỉnh nhiệm vụ nhân điểm phân loại với điểm giao nhau trên điểm công đoàn.

Giao điểm trên điểm kết hợp tương ứng với độ chính xác của dự đoán hộp giới hạn.

Dựa trên điểm căn chỉnh, mô hình sẽ chọn k mẫu dương tính hàng đầu và tính toán phân loại

mất mát khi sử dụng BCE.

Tổn thất BCE chỉ đơn giản là đo sự khác biệt giữa nhãn thực tế và nhãn dự đoán ở giao lộ hoàn chỉnh

về sự mất mát của công đoàn.

Xem xét mối liên hệ giữa hộp giới hạn được xác định trước với thực tế cơ bản về điểm trung tâm

và tỷ lệ khung hình.

Ngược lại, mất tiêu điểm phân phối tối ưu hóa việc phân phối ranh giới hộp giới hạn

bằng cách tập trung nhiều hơn vào các mẫu mà mô hình phân loại sai là âm tính giả.

Đây là kiến ​​trúc của Yolo et, kiến ​​trúc của YOLO.

Nó được xây dựng dựa trên phiên bản trước của YOLO phiên bản năm.

YOLO 8 sử dụng mạng lưới thần kinh tích chập có thể được chia thành hai phần chính là xương sống.

Ở đây bạn có thể nhìn thấy xương sống.

Và phần còn lại là đầu ở đây.

Bạn có thể nhìn thấy cái đầu.

Tại đây bạn có thể thấy các kim tự tháp đặc trưng khác nhau từ mạng kim tự tháp đặc trưng.

Và sau đó bạn có thể thấy các lớp tích chập và mô-đun C2F, đây là một trong những đóng góp chính

trong YOLO tám.

Vậy mô-đun C2F là gì.

Xương sống của mô hình hiện bao gồm mô-đun AC2F thay vì AC31.

Sự khác biệt giữa hai loại này là trong C2F, mô hình nối đầu ra của tất cả các nút cổ chai

mô-đun.

Ngược lại, trong C ba mô hình sử dụng đầu ra của mô-đun thắt cổ chai cuối cùng.

Hãy để tôi phóng to cái này để bạn có thể xem chi tiết về mô-đun C2F.

Vì vậy, ở đây bạn có thể xem chi tiết về mô-đun C2F.

Nó bao gồm một phép tích chập và sau đó là phép chia tách.

Và ở đây bạn có thể thấy nút cổ chai.

Mô-đun thắt cổ chai bao gồm các khối dư của nút cổ chai giúp giảm chi phí tính toán.

Trong các mạng học sâu như ResNet, điều này tăng tốc quá trình đào tạo và cải thiện độ dốc

dòng chảy.

Và sau đó bạn có thể thấy nút cổ chai thứ hai và sau đó bạn nối đầu ra.

Vì vậy, trong C2F, mô hình nối đầu ra của tất cả các mô-đun thắt cổ chai so với C ba,

trong đó mô hình sử dụng đầu ra của mô-đun thắt cổ chai cuối cùng.

Tương tự, ở đây bạn có thể thấy lớp tích chập và sau đó lại là mô-đun tích chập C2F và C2F

mô-đun tích chập mô-đun C2F.

Và cuối cùng bạn có thể thấy mô-đun đặc biệt là mô-đun tổng hợp kim tự tháp không gian.

Nhóm kim tự tháp không gian là một loại lớp gộp được sử dụng để giảm độ phân giải không gian của đối tượng địa lý

maps ESP được sử dụng để cải thiện hiệu suất phát hiện trên các vật thể nhỏ, vì nó cho phép mô hình nhìn thấy

các đối tượng ở nhiều quy mô.

YOLO phiên bản thứ tám sử dụng ESP để đạt được kết quả tốt hơn.

Hãy để tôi phóng to lại để bạn có thể thấy mô-đun ESP ở đây.

Bạn.

Đây là mô-đun C2F.

Và ở đây bạn có thể thấy nút thắt cổ chai và mô-đun SPF.

Vì vậy, ở đây bạn có thể thấy mô-đun SPF.

Mô-đun tổng hợp kim tự tháp không gian bao gồm các lớp tích chập và sau đó là các lớp tổng hợp tối đa.

Và sau đó bạn nối các tính năng.

Và các lớp tích chập bao gồm chuẩn hóa lô tích chập 2D.

Đây là thông tin chi tiết về phần cốt lõi của YOLO 8, nơi sửa đổi chính trong YOLO 8

xương sống của mô hình là mô-đun C2F.

Bây giờ hãy thảo luận về tám đầu YOLO.

Phần đầu tách rời của YOLO 8 không còn thực hiện phân loại và hồi quy cùng nhau nữa.

Thay vào đó, nó thực hiện nhiệm vụ một cách riêng biệt, giúp tăng hiệu suất của mô hình.

Phần đầu của YOLO 8 bao gồm nhiều lớp chập, theo sau là một loạt các lớp

các lớp được kết nối.

Các lớp này chịu trách nhiệm dự đoán các hộp giới hạn, điểm đối tượng và xác suất của lớp

for the objects detected in an image.

YOLO tám.

Tập trung vào các phần khác nhau của hình ảnh và điều chỉnh tầm quan trọng của các tính năng khác nhau dựa trên chúng.

sự liên quan đến nhiệm vụ.

Vì vậy, ở đây bạn có thể thấy các lớp chập khác nhau trong phần đầu.

Bạn có thể thấy các hoạt động u và C.

U có nghĩa là lấy mẫu lên và c có nghĩa là ghép nối.

Bạn cũng có thể thấy các lớp C, two, f và convolutional.

Một tính năng quan trọng khác của YOLO tám đầu là khả năng thực hiện phát hiện đối tượng đa quy mô.

Mô hình sử dụng mạng kim tự tháp đặc trưng để phát hiện các đối tượng có kích thước và tỷ lệ khác nhau trong một

hình ảnh.

Vì vậy, ở đây bạn có thể thấy các kim tự tháp đặc trưng khác nhau.

Mạng kim tự tháp đặc trưng bao gồm nhiều lớp phát hiện các đối tượng ở các quy mô khác nhau, cho phép

mô hình để phát hiện các vật thể lớn và nhỏ trong một hình ảnh.

Vì vậy, cuối cùng, ở đây bạn có thể thấy phát hiện trong đó YOLO tám thực hiện hộp phân loại và giới hạn

hồi quy riêng biệt để nhận ra các đối tượng.

Và bạn cũng có thể thấy dự đoán hộp giới hạn mà YOLO 8 sử dụng.

Ở đây bạn có thể thấy giao điểm C trên hợp cộng với tổn hao DF.

Xem phần giao nhau trên phần hợp là phần giao hoàn toàn với phần mất phần kết và DFL là phần phân phối

mất tiêu điểm cho các nhãn lớp.

Dự đoán.

YOLO tám sử dụng tổn thất BCA.

Sự mất mát.

Có thể xảy ra sai lệch do phần đầu tách rời tách biệt nhiệm vụ phân loại và hồi quy.

Điều đó có nghĩa là mô hình có thể bản địa hóa một đối tượng trong khi phân loại đối tượng khác.

Giải pháp là bao gồm điểm căn chỉnh nhiệm vụ dựa vào đó mô hình biết được điểm tích cực và tiêu cực.

mẫu.

Điểm căn chỉnh nhiệm vụ nhân điểm phân loại với điểm giao nhau trên điểm công đoàn.

Giao điểm trên điểm kết hợp tương ứng với độ chính xác của dự đoán hộp giới hạn.

Dựa trên điểm căn chỉnh, mô hình sẽ chọn k mẫu dương tính hàng đầu và tính toán phân loại

mất mát khi sử dụng BCE.

Tổn thất BCE chỉ đơn giản là đo lường sự khác biệt giữa nhãn thực tế và nhãn dự đoán.

Các hoàn thành giao nhau.

Hành động dựa trên luật công đoàn.

Xem xét mối liên hệ giữa hộp giới hạn được xác định trước với thực tế cơ bản về điểm trung tâm

và tỷ lệ khung hình.

Ngược lại, mất tiêu điểm phân phối tối ưu hóa việc phân phối ranh giới hộp giới hạn

bằng cách tập trung nhiều hơn vào các mẫu mà mô hình phân loại sai là âm tính giả.

Vì kiến ​​trúc của YOLO, nó được xây dựng dựa trên phiên bản YOLO phiên bản thứ năm trước đó.

Vậy sự khác biệt giữa YOLO và YOLO 5 là gì?

YOLO 8 đã thay thế mô-đun C 3 của YOLO 5 bằng mô-đun C2F.

YOLO tám đã thay thế sáu lớp chập sáu chéo đầu tiên của YOLO năm bằng ba chéo ba

các lớp chập trong đường trục.

YOLO tám dẫn đến các lớp chập.

Số lượng các lớp là mười và 14.

Trong YOLO năm.

YOLO tám đã thay thế phép chập một chéo một trong YOLO năm bằng phép chập ba chéo ba

trong tình trạng tắc nghẽn.

YOLO tám đã sử dụng phần đầu được tách rời và xóa nhánh Đối tượng giống như trong YOLO năm.

Bây giờ hãy thảo luận về các tính năng chính của YOLO.

YOLO tám cải thiện độ chính xác phát hiện đối tượng so với các phiên bản trước bằng cách kết hợp các kỹ thuật mới

và tối ưu hóa.

Như chúng ta đã thấy, YOLO 8 đạt được tốc độ suy luận nhanh hơn các mô hình phát hiện đối tượng khác trong khi

duy trì độ chính xác cao.

YOLO 8 hỗ trợ nhiều nền tảng khác nhau như Effientnet, ResNet và CSP darknet, mang đến cho người dùng

sự linh hoạt để chọn mô hình tốt nhất cho trường hợp sử dụng cụ thể của họ.

YOLO Eight sử dụng các kỹ thuật tăng cường dữ liệu nâng cao như mixup và cutmix để cải thiện độ mạnh mẽ

và khái quát hóa mô hình.

Kiến trúc YOLO 8 có khả năng tùy biến cao, cho phép người dùng dễ dàng sửa đổi cấu trúc mô hình

và các thông số phù hợp với nhu cầu của họ.

YOLO.

Nó cung cấp các mô hình được đào tạo trước để dễ dàng sử dụng và chuyển giao việc học trên các bộ dữ liệu khác nhau.