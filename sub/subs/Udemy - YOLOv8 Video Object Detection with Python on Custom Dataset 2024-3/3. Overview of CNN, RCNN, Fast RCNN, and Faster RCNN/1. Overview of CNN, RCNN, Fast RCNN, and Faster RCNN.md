# 1. Tổng quan về CNN, RCNN, RCNN nhanh và RCNN nhanh hơn

---

Như bạn đã biết rằng các phương pháp phát hiện đối tượng dựa trên deep learning, đặc biệt dựa trên phép tích chập

mạng lưới thần kinh và đạt được sự phổ biến đáng kể bằng cách đạt được kết quả hiện đại.

Vì vậy, trước khi chuyển sang các phương pháp phát hiện đối tượng, trước tiên chúng ta hãy có cái nhìn tổng quan về

mạng lưới thần kinh tích chập.

Việc sử dụng cơ bản và chính của CNN là.

Mạng lưới thần kinh tích chập là phân loại hình ảnh.

CNN có thể được sử dụng cho nhiều tác vụ như phát hiện đối tượng và phân đoạn hình ảnh, v.v. bạn sẽ sử dụng

dạng ngắn hơn của mạng nơ ron tích chập là CNN.

Kiến trúc đơn giản của CNN thường bao gồm lớp tích chập, lớp kích hoạt, lớp gộp

lớp và lớp được kết nối đầy đủ.

Lớp tích chập là khối xây dựng cốt lõi của CNN.

Nó áp dụng một tập hợp các bộ lọc hoặc hạt nhân có thể học được cho hình ảnh đầu vào, giúp phát hiện các loại khác nhau.

các đặc điểm hoặc mẫu có trong hình ảnh.

Mỗi bộ lọc thực hiện thao tác tích chập bằng cách trượt qua hình ảnh đầu vào và tính toán tích số chấm

giữa trọng lượng bộ lọc và các mảng hình ảnh tương ứng.

Lớp tích chập chịu trách nhiệm nắm bắt các phân cấp không gian của các tính năng trực quan.

Hạt nhân tích chập.

Bộ lọc của chúng tôi được áp dụng trên phần tử trung tâm hình ảnh của hạt nhân được đặt trên pixel nguồn.

Pixel nguồn sau đó được thay thế bằng tổng trọng số của chính nó và các pixel lân cận.

Sau lớp tích chập, một hàm kích hoạt được áp dụng theo từng phần tử để xuất bản đồ đặc điểm.

Hàm kích hoạt đưa tính phi tuyến tính vào mạng, cho phép nó tìm hiểu các mối quan hệ phức tạp

giữa các tính năng.

Các hàm kích hoạt thường được sử dụng bao gồm ReLU, đặt giá trị âm về 0 và giữ giá trị dương

các giá trị không thay đổi

Nói một cách đơn giản, hàm ReLU nhận giá trị đầu vào x.

Nếu x dương, hàm ReLU sẽ tự xuất ra x.

Nếu x âm, hàm ReLU cho kết quả bằng 0.

Điều này có nghĩa là tất cả các giá trị âm được ánh xạ về 0, trong khi các giá trị dương được giữ nguyên.

Ở đây bạn có thể thấy hàm tuyến tính được chỉnh lưu ReLU có giá trị lớn nhất bằng 0 hoặc x.

Vì vậy, nếu giá trị của x âm, nó sẽ trả về số 0.

Nếu không nó sẽ trả về số 0 của x.

Nếu giá trị x dương, lớp tổng hợp được sử dụng để lấy mẫu kích thước không gian của đối tượng địa lý

bản đồ trong khi vẫn giữ lại các tính năng quan trọng.

Nó làm giảm kích thước không gian của biểu diễn, giảm số lượng tham số và tính toán

chi phí.

Loại gộp phổ biến nhất là gộp tối đa, trong đó giá trị tối đa ở mỗi vùng cục bộ được giữ lại

và những thứ khác bị loại bỏ.

Ví dụ: hãy xem xét tổng hợp tối đa trên bản vá hình ảnh này với hai bộ lọc chéo hai và bước hai.

Vì vậy, ở đây bạn có thể thấy cửa sổ bộ lọc hai chéo hai và bạn sẽ lấy giá trị tối đa từ cửa sổ này

cửa sổ.

Và ở đây bạn có thể thấy số sáu và sải bước là hai.

Nó có nghĩa là bạn sẽ di chuyển về phía trước cửa sổ bằng cách dịch chuyển hai cột.

Và cửa sổ bộ lọc tiếp theo sẽ là cửa sổ này.

Vì vậy, bạn lại chọn giá trị lớn nhất và đó là 8.

Ở đây bạn có thể thấy và như vậy.

Việc kéo có thể là tổng hợp tối đa hoặc tổng hợp trung bình.

Trong nhóm tối đa, bạn lấy giá trị tối đa ra khỏi cửa sổ bộ lọc.

Ở đây bạn có thể thấy giá trị tối đa trong cửa sổ này là 20.

Và ở đây bạn có thể thấy 20 và 20 ở đây trong trường hợp gộp tối đa.

Và trong trường hợp gộp trung bình, bạn lấy trung bình của tất cả các phần tử này trong cửa sổ bộ lọc.

Và ở đây bạn có thể thấy trung bình cộng của 4 yếu tố này là 13.

Vì vậy, ở đây bạn có thể thấy kiến trúc CNN đơn giản.

Bạn đưa ra hình ảnh đầu vào, sau đó là lớp chập và sau đó là lớp gộp tối đa.

Và sau đó bạn có thể lặp lại nhiều lần các lớp tích chập và gộp tối đa này.

Và bạn có thể sử dụng chức năng kích hoạt ở giữa.

Vì vậy, sau khi áp dụng phép gộp tích chập và gộp tối đa, bạn làm phẳng bản đồ tính năng đầu ra.

Và sau đó bạn đưa vào lớp được kết nối đầy đủ của mạng.

Lớp được kết nối đầy đủ, còn được gọi là lớp dày đặc, lấy đầu ra làm phẳng từ lớp trước đó

các lớp và kết nối mọi nơ-ron với mọi nơ-ron ở lớp tiếp theo.

Nó cho phép mạng tìm hiểu các tổ hợp tính năng phức tạp và đưa ra dự đoán dựa trên chúng.

Số lượng nơ-ron trong lớp này phụ thuộc vào nhiệm vụ cụ thể và kích thước đầu ra mong muốn.

Vì vậy, cuối cùng bạn sẽ nhận được xác suất của các lớp đầu ra.

Vì vậy, sau khi lớp được kết nối đầy đủ, bạn áp dụng lớp phân loại hoặc lớp softmax để có được

xác suất của các lớp được dự đoán.

Vì vậy, chúng ta hãy có một cái nhìn tổng quan về các mạng lưới thần kinh tích chập phổ biến.

LeNet CNN.

Các ứng dụng thành công đầu tiên của mạng nơ ron tích chập được Yann phát triển vào những năm 1990.

Kiến trúc LeNet là kiến trúc tốt nhất vào thời điểm đó để đọc mã zip và chữ số, v.v. và sau đó là AlexNet Convolutional

Mạng nơ-ron, công trình đầu tiên phổ biến mạng nơ-ron tích chập trong thị giác máy tính

là AlexNet, được phát triển bởi.

Alex và Ji của AlexNet đã tham gia thử thách ImageNet vào năm 2012 và đã vượt trội đáng kể

người về nhì.

Mạng có kiến trúc rất giống với LeNet, nhưng sâu hơn, lớn hơn và có tính năng tích chập

các lớp xếp chồng lên nhau.

Tại đây bạn có thể thấy kiến ​​trúc của AlexNet.

Có năm lớp tích chập tích chập một, tích chập hai, tích chập ba, tích chập

bốn, và tích chập năm.

Và ở giữa lớp chập cũng có tổng hợp tối đa.

Tổng hợp tối đa.

Và ở cuối, bạn có thể thấy ba lớp FC sáu, FC bảy và FC tám được kết nối đầy đủ.

Một CNN nổi tiếng khác là GoogleLeNet.

Ilsvrc cho biết người chiến thắng thử thách năm 2014 là CNN của Google.

Đóng góp chính của nó là sự phát triển của một mô-đun khởi động giúp giảm đáng kể số lượng

của các tham số trong mạng so với AlexNet.

Ngoài ra, bài viết này sử dụng nhóm trung bình thay vì các lớp được kết nối đầy đủ ở đầu

mạng tích chập, loại bỏ một lượng lớn các tham số dường như không quan trọng lắm.

Ngoài ra còn có một số phiên bản tiếp theo của GoogLeNet.

Á quân tại Ilsvrc 2014 là mạng của Karen và Andrew, được gọi là mạng VGG.

Đóng góp chính của nó là cho thấy rằng độ sâu của mạng là một thành phần quan trọng cho sự phát triển tốt đẹp.

hiệu suất.

Mạng tốt nhất cuối cùng của họ chứa 16 lớp tích chập, được kết nối đầy đủ và các tính năng hấp dẫn trong

kiến trúc cực kỳ đồng nhất chỉ thực hiện ba vòng xoắn ba và hai vòng chéo hai

tổng hợp từ đầu đến cuối.

Mô hình được đào tạo trước của họ có sẵn để sử dụng plug and play trong quán cà phê.

Nhược điểm của mạng là việc đánh giá tốn kém hơn và sử dụng nhiều bộ nhớ và thông số hơn.

Hầu hết các tham số này đều nằm trong lớp được kết nối đầy đủ đầu tiên và từ đó người ta nhận thấy rằng những tham số này

các lớp được kết nối đầy đủ có thể được loại bỏ mà không làm giảm hiệu suất, giảm đáng kể số lượng

của các thông số cần thiết.

Kiến trúc ResNet là mạng nơ-ron tích chập được sử dụng phổ biến nhất ResNet, còn được gọi là

mạng còn lại, được phát triển bằng cách tuyên bố rằng nó là người chiến thắng trong thử thách Ilsvrc 2015.

Nó có tính năng bỏ qua các kết nối đặc biệt và sử dụng nhiều tính năng chuẩn hóa hàng loạt.

Resnet hiện là mô hình mạng nơ-ron tích chập hiện đại nhất và là mạng mặc định

lựa chọn sử dụng mạng nơ ron tích chập trong thực tế.

Vì vậy, ở đây bạn có thể thấy kiến ​​trúc Vgg16.

Có các lớp chập và sau đó là lớp gộp.

Và sau đó là sự lặp lại của các lớp chập và gộp.

Và cuối cùng có các lớp được kết nối đầy đủ.

Và sau đó là kiến ​​trúc của GoogLeNet.

Và đây là kiến ​​trúc của mạng nơ ron tích chập được sử dụng phổ biến nhất, đó là ResNet.

Với các kết nối bỏ qua, bạn có thể thấy sự lặp lại của các lớp tích chập và gộp tối đa.

Và ở đây bạn có thể thấy các kết nối bỏ qua.

Đó là tổng quan ngắn gọn về mạng lưới thần kinh tích chập.

Bây giờ chúng ta hãy đề cập đến các mạng lưới thần kinh tích chập dựa trên khu vực.

Chúng còn được gọi là CNN của chúng tôi dựa trên mạng lưới thần kinh tích chập.

Như chúng ta đã thấy trước đây, một số mạng lưới thần kinh tích chập.

Họ CNN của chúng tôi thường được sử dụng để phát hiện đối tượng và phân đoạn phiên bản.

Vì vậy, trước khi đi vào chi tiết về CNN của chúng tôi, hãy xem sự khác biệt chính giữa CNN và CNN của chúng tôi là gì

các kiến trúc học sâu.

CNN được thiết kế chủ yếu để phân loại hình ảnh, nơi họ học cách phân loại toàn bộ hình ảnh thành

các lớp hoặc danh mục được xác định trước khác nhau.

Chúng không có cơ chế tích hợp để định vị và phát hiện các đối tượng trong ảnh.

Mặt khác, CNN của chúng tôi được thiết kế đặc biệt để phát hiện vật thể.

Nó tuân theo cách tiếp cận hai giai đoạn bao gồm việc tạo ra các đề xuất khu vực, sau đó phân loại và tinh chỉnh

những đề xuất này để phát hiện các đối tượng.

R-cnn kết hợp các thuật toán đề xuất vùng với CNN để bản địa hóa các đối tượng trong hình ảnh.

Thuật toán đề xuất vùng được sử dụng phổ biến nhất là tìm kiếm có chọn lọc.

Chúng ta sẽ thấy nó một cách chi tiết.

Vì vậy, thuật toán đề xuất vùng trong R-cnn là điểm khác biệt chính để bản địa hóa các đối tượng trong ảnh

so với CNN.

CNN được đào tạo bằng cách sử dụng các hình ảnh được gắn nhãn với nhãn loại sự thật cơ bản tương ứng.

Quá trình đào tạo tập trung vào việc tối ưu hóa trọng số mạng để phân loại chính xác hình ảnh thành các thành phần được xác định trước.

danh mục hoặc lớp học.

Mặt khác, R-cnn bao gồm các bước bổ sung trong quá trình đào tạo.

Nó yêu cầu bước đào tạo trước ban đầu trong đó CNN được đào tạo trên tập dữ liệu quy mô lớn, chẳng hạn như

ImageNet để phân loại hình ảnh, sau đó sử dụng thuật toán đề xuất vùng như tìm kiếm chọn lọc

để tạo ra các đề xuất đối tượng tiềm năng và các đề xuất này được gắn nhãn với đối tượng tương ứng của chúng

các lớp và tọa độ hộp giới hạn được tinh chỉnh.

CNN tinh chỉnh các đề xuất được gắn nhãn này để tìm hiểu cách phân loại và tinh chỉnh các khu vực đề xuất

CNN xử lý hình ảnh một cách độc lập và không xem xét thông tin không gian của đối tượng.

Chúng có thể được áp dụng cho các hình ảnh có kích thước bất kỳ, nhưng chúng thiếu hiệu quả trong việc định vị và phát hiện nhiều hình ảnh.

các đối tượng trong một ảnh.

R-cnn, đặc biệt là các biến thể sau này như R-cnn nhanh và R-cnn nhanh hơn, đã cải thiện hiệu quả

phát hiện đối tượng bằng cách giới thiệu các mạng đề xuất vùng và trích xuất tính năng dùng chung.

Bằng cách chia sẻ tính toán cho nhiều đề xuất vùng, R-cnn giảm đáng kể khả năng suy luận

thời gian so với các phương pháp dựa trên CNN.

R-cnn bao gồm bốn kiến trúc mạng nơ-ron tích chập sâu dựa trên vùng như R-cnn,

R-cnn nhanh, R-cnn nhanh hơn và mặt nạ R-cnn.

Bây giờ chúng ta hãy xem chi tiết về kiến ​​trúc R-cnn.

Đây là kiến ​​trúc của mô hình R-cnn.

Mạng lưới thần kinh tích chập dựa trên khu vực của chúng tôi.

Mô hình R-cnn bao gồm ba mô-đun chính.

Mô-đun một là đề xuất khu vực.

Mô-đun này tạo và trích xuất các đề xuất khu vực độc lập về danh mục, chẳng hạn như các hộp giới hạn ứng viên.

Mô-đun thứ hai là tính toán các tính năng CNN, trình trích xuất tính năng của chúng tôi.

Mô-đun này trích xuất tính năng từ từng vùng ứng cử viên bằng cách sử dụng mạng nơ ron tích chập sâu hoặc

Mô-đun CNN ba hoạt động như một bộ phân loại.

Mô-đun ba là trình phân loại giúp phân loại các đặc điểm thành một trong các lớp danh từ.

CNN của chúng tôi lấy hình ảnh đầu vào làm đầu vào ban đầu và sau đó trong mô-đun đề xuất khu vực, R-cnn tạo ra

đề xuất khu vực độc lập.

Trong mô-đun đề xuất khu vực.

Hình ảnh được chia thành nhiều vùng bằng thuật toán đề xuất vùng như tìm kiếm chọn lọc.

Các vùng được đề xuất này là các đối tượng tiềm năng có thể chứa các đối tượng quan tâm.

Các vùng này có thể chứa các đối tượng mục tiêu và chúng có kích thước khác nhau, do đó mô-đun này tạo ra

khoảng 2000 đề xuất khu vực cho mỗi hình ảnh bằng cách sử dụng tìm kiếm có chọn lọc.

Vì vậy, hãy đi vào chi tiết tìm kiếm có chọn lọc.

Tìm kiếm có chọn lọc được áp dụng để trích xuất các đề xuất khu vực.

Mục tiêu là phát hiện các đối tượng ở bất kỳ quy mô nào và xem xét nhiều tiêu chí nhóm như phát hiện

sự khác biệt về màu sắc, kết cấu, độ sáng, v.v. vì vậy ý tưởng là sử dụng nhóm từ dưới lên

các vùng hình ảnh để tạo ra một hệ thống phân cấp các vùng từ nhỏ đến lớn.

Vì vậy, trong bước một, tìm kiếm chọn lọc sẽ tạo ra phân đoạn phụ ban đầu.

Tại đây, bạn có thể xem hình ảnh đầu vào và trước tiên hãy tạo phân đoạn ban đầu, sau đó tìm kiếm chọn lọc.

Tạo nhiều vùng dựa trên các vùng được phân đoạn, mỗi vùng thuộc về tối đa một đối tượng.

Vì vậy, những khu vực này được gọi là đối tượng ứng cử viên.

Vì vậy, tìm kiếm chọn lọc kết hợp đệ quy các vùng tương tự thành các vùng lớn hơn.

Nó sử dụng thuật toán tham lam từ tập hợp các vùng.

Chọn hai cái giống nhau nhất.

Kết hợp chúng thành một khu vực lớn hơn.

Lặp lại cho đến khi chỉ còn lại một vùng.

Điều này mang lại một hệ thống phân cấp gồm các vùng lớn liên tiếp, giống như chúng ta mong muốn.

Vì vậy, ở đây bạn có thể thấy hình ảnh đầu vào và sau đó là phân đoạn ban đầu.

Và sau một số lần lặp lại và trong hình ảnh cuối cùng sau nhiều lần lặp lại.

Vì vậy, tìm kiếm chọn lọc kết hợp đệ quy các vùng tương tự thành các vùng lớn hơn.

Vì vậy, tìm kiếm có chọn lọc sử dụng vùng được phân đoạn được tạo để tạo ra các vị trí đối tượng ứng cử viên.

Vì vậy, ở đây bạn có thể thấy phân đoạn ban đầu và vị trí đối tượng ứng viên tương ứng của nó.

Và sau một số lần lặp lại, phân đoạn và vị trí đối tượng ứng cử viên tương ứng của nó.

Và cuối cùng là phân khúc tinh tế hơn và các vị trí đối tượng ứng cử viên của nó.

Đó là cách tìm kiếm có chọn lọc hoạt động.

Ban đầu, nó tạo ra các vùng được phân đoạn và sau đó tạo ra các vị trí đối tượng ứng cử viên dựa trên đó.

các vùng bị phân đoạn.

Sau đó, trong giai đoạn thứ hai, R-cnn trích xuất một vectơ đặc trưng có độ dài cố định từ mỗi vùng được trích xuất vùng

đề xuất có thể có kích thước bất kỳ, do đó, vùng được trích xuất này trước tiên sẽ bị biến dạng để có đặc điểm có độ dài cố định

vector từ mỗi khu vực theo yêu cầu của CNN.

Mỗi vùng được đề xuất được biến dạng độc lập để có kích thước cố định và đi qua một dây thần kinh tích chập

mạng.

CNN trích xuất các tính năng cấp cao từ khu vực, thường sử dụng mạng được đào tạo trước như

VGG, ResNet, do đó, vectơ đặc trưng chiều cao từ mỗi đề xuất khu vực sẽ được trích xuất ở phần cuối cùng

mô-đun của R-cnn.

Vectơ đặc trưng được trích xuất từ ​​​​giai đoạn thứ hai được đưa vào SVM tuyến tính để dự đoán các lớp.

Ngoài việc phân loại vùng R-cnn và còn thực hiện hồi quy hộp giới hạn.

Để có được tọa độ khu vực dự đoán.

Như vậy CNN của chúng ta cũng dự đoán được tọa độ tinh chỉnh x, y, chiều rộng, chiều cao của đối tượng trong phạm vi đề xuất

vùng, điều chỉnh đề xuất vùng ban đầu để căn chỉnh chính xác hơn với ranh giới đối tượng.

Vì vậy, đầu ra cuối cùng của kiến trúc R-cnn bao gồm các nhãn lớp đối tượng và các nhãn tương ứng của nó.

tọa độ hộp giới hạn.

Tại đây bạn có thể thấy nhãn lớp và bạn sẽ nhận được vị trí đối tượng ở dạng hộp giới hạn

tọa độ chứa tọa độ x, y và chiều rộng và chiều cao của đối tượng.

Vì vậy CNN của chúng tôi rất đơn giản và có thể mở rộng.

CNN của chúng tôi cải thiện điểm chính xác trung bình.

Nhưng có một số hạn chế với R-cnn.

R-cnn bao gồm quy trình đào tạo nhiều giai đoạn bao gồm việc chuẩn bị và vận hành

ba mô hình riêng biệt.

Việc đào tạo tốn kém về không gian và thời gian vì các đặc điểm được trích xuất từ mỗi đề xuất vùng trong

mỗi hình ảnh và được ghi vào đĩa.

Việc phát hiện đối tượng chậm vì việc đưa ra dự đoán bằng cách sử dụng CNN sâu trên rất nhiều đề xuất khu vực là điều khó khăn.

rất chậm.

Vì vậy, để giải quyết hạn chế này với R-cnn, kiến ​​trúc R-cnn nhanh được đề xuất.

Vì vậy chúng ta hãy đi vào chi tiết về Fast R-cnn.

Fast R-cnn là phiên bản mở rộng của R-cnn nhằm giải quyết các vấn đề và hạn chế trong R-cnn.

Đây là kiến ​​trúc của Fast R-cnn.

Fast R-cnn là phiên bản cải tiến của khung phát hiện đối tượng R-cnn.

Nó giải quyết một số hạn chế của R-cnn và giới thiệu một kiến ​​trúc hiệu quả hơn.

Fast R-cnn được đề xuất dưới dạng một mô hình duy nhất thay vì một đường ống, để tìm hiểu và đưa ra các vùng cũng như phân loại

trực tiếp để làm cho R-cnn nhanh hơn.

Quy trình đào tạo được cải tiến bằng cách hợp nhất ba mô hình độc lập thành một khung được đào tạo chung

và tăng kết quả tính toán được chia sẻ.

Được đặt tên là R-cnn nhanh, thay vì trích xuất vectơ đặc trưng CNN một cách độc lập cho từng đề xuất khu vực,

mô hình này tổng hợp chúng thành một CNN chuyển tiếp qua toàn bộ hình ảnh và đề xuất khu vực.

Chia sẻ ma trận tính năng này.

Sau đó, ma trận đặc trưng tương tự được phân nhánh để sử dụng cho việc học bộ phân loại đối tượng và giới hạn

hộp hồi quy.

Tóm lại, việc chia sẻ tính toán tăng tốc R-cnn.

Đóng góp chính trong R-cnn nhanh là vùng của lớp tổng hợp lợi ích so với

R-cnn.

Vì vậy, hãy đi vào chi tiết về ROI hoặc khu vực tổng hợp sở thích ROI hoặc khu vực tổng hợp sở thích

là một kiểu gộp tối đa để chuyển đổi đặc điểm trong vùng được chiếu của hình ảnh có kích thước bất kỳ thành kích thước nhỏ

cửa sổ cố định, giả sử h thành W.

Vùng đầu vào được chia thành các lưới H và W xấp xỉ mọi cửa sổ con có kích thước chiều cao được chia

cho h thành chiều rộng chia cho w.

Sau đó áp dụng tổng hợp tối đa trong mỗi lưới, để mỗi vùng được đề xuất được ánh xạ tới bản đồ tính năng có kích thước cố định

sử dụng tính năng gộp ROI.

Vì vậy, trích xuất tổng hợp ROI của biểu diễn tính năng có kích thước cố định cho từng khu vực.

Đề xuất.

Bằng cách chia vùng đề xuất thành một lưới và chọn các đặc điểm tiêu biểu nhất từ mỗi vùng

ô lưới.

Điều này cho phép mạng điều chỉnh các đặc điểm của vùng với các đề xuất vùng tương ứng.

Fast R-cnn lấy đầu vào của toàn bộ hình ảnh và một tập hợp các đề xuất khu vực.

Vì mô-đun đề xuất vùng tương tự như CNN, trong đó hình ảnh được chia thành nhiều vùng

các đề xuất sử dụng thuật toán đề xuất khu vực như tìm kiếm có chọn lọc và các khu vực được đề xuất được

ứng cử viên đối tượng tiềm năng.

Không giống như R-cnn, R-cnn nhanh thực hiện trích xuất tính năng trên toàn bộ hình ảnh bằng cách sử dụng mạng thần kinh tích chập

mạng.

Trước mắt, CNN trích xuất đặc điểm cấp cao từ toàn bộ hình ảnh và tạo bản đồ đặc điểm, do đó

mạng nơ ron tích chập sâu xử lý toàn bộ hình ảnh với một số phép gộp tích chập và tổng hợp tối đa

các lớp để tạo ra một bản đồ đặc trưng tích chập.

Vì vậy, đây là khu vực của lớp tổng hợp lợi ích.

Sau đó, mỗi khu vực được đề xuất sẽ được ánh xạ tới một bản đồ đặc trưng có kích thước cố định bằng cách sử dụng khu vực quan tâm.

Khu vực quan tâm tổng hợp trích xuất một đại diện tính năng có kích thước cố định cho từng khu vực.

Đề xuất bằng cách chia vùng đề xuất thành các lưới và sau đó chọn các đặc điểm tiêu biểu nhất

từ mỗi ô lưới.

Điều này cho phép mạng điều chỉnh các đặc điểm của vùng với các đề xuất vùng tương ứng.

Sau đó, bước cuối cùng là phân loại vùng và hồi quy hộp giới hạn.

Đối tượng địa lý gộp vùng quan tâm từ bước trước được đưa vào hai nhánh riêng biệt.

Nhánh đầu tiên thực hiện phân loại, trong đó các lớp được kết nối đầy đủ sẽ dự đoán xác suất của

vùng thuộc về từng lớp đối tượng được xác định trước.

Nhánh thứ hai thực hiện hồi quy hộp giới hạn, dự đoán tọa độ tinh chỉnh của đối tượng

trong khu vực đề xuất.

Vì vậy, đầu ra cuối cùng của kiến trúc R-cnn nhanh là một tập hợp các phát hiện đối tượng, mỗi phát hiện bao gồm

lớp đối tượng, nhãn và tọa độ hộp giới hạn được tinh chỉnh tương ứng của nó.

Vì vậy, đây là so sánh hiệu quả của R-cnn và R-cnn nhanh.

Tại đây bạn có thể thấy thời gian đào tạo của R-cnn tính bằng giờ.

Đó là khoảng 84 giờ và R-cnn nhanh chỉ khoảng chín giờ.

Vì vậy, hiệu quả được cải thiện nhiều trong trường hợp R-cnn nhanh so với R-cnn.

Và tương tự ở đây bạn có thể thấy thời gian test R-cnn mất quá nhiều thời gian.

Mặt khác, R-cnn nhanh rất nhanh, do đó R-cnn nhanh nhanh hơn nhiều so với

Kiến trúc R-cnn.

Vậy là bạn đã thấy những hạn chế trong R-cnn.

R-cnn là một đường ống nhiều giai đoạn.

Việc đào tạo rất tốn kém và việc phát hiện đối tượng cũng chậm.

Vì vậy, để giải quyết những vấn đề này, Fast R-cnn đã được đề xuất.

Fast R-cnn có độ chính xác trung bình trung bình cao hơn so với huấn luyện từ đầu đến cuối giai đoạn đơn R-cnn.

Không cần lưu trữ đĩa để lưu vào bộ nhớ đệm tính năng, nhưng vẫn còn chỗ để cải thiện tốc độ nhanh

R-cnn.

Trong các đề xuất R-cnn nhanh là nút cổ chai tính toán trong các hệ thống phát hiện, bởi vì vùng

đề xuất được tạo riêng bởi một mô hình khác và điều đó rất tốn kém.

Vì vậy, để giải quyết vấn đề này, R-cnn ra đời nhanh hơn.

Vì vậy, bây giờ chúng ta hãy hướng tới kiến ​​trúc R-cnn nhanh hơn để giải quyết vấn đề trong R-cnn nhanh.

R-cnn nhanh hơn là phiên bản mở rộng của R-cnn nhanh.

Đây là kiến ​​trúc của R-cnn nhanh hơn.

R-cnn nhanh hơn đã giới thiệu một mạng lưới đề xuất khu vực.

Mạng còn lại giống như mạng R-cnn nhanh.

Vì vậy, giải pháp tăng tốc là tích hợp thuật toán đề xuất vùng vào mô hình CNN.

R-cnn nhanh hơn đang hoạt động chính xác.

R-cnn nhanh hơn xây dựng một mô hình thống nhất duy nhất bao gồm RPN, đó là mạng đề xuất khu vực

và R-cnn nhanh với các lớp tính năng tích chập được chia sẻ.

Kiến trúc được thiết kế để vừa đề xuất vừa cải tiến đề xuất khu vực như một phần của quá trình đào tạo,

được gọi là Mạng đề xuất khu vực hoặc RPN.

Sau đó, các vùng này được sử dụng phối hợp với mô hình R-cnn nhanh trong một thiết kế mô hình duy nhất.

Những cải tiến này vừa giảm số lượng đề xuất khu vực vừa đẩy nhanh quá trình vận hành thời gian thử nghiệm

của mô hình đến gần thời gian thực với hiệu suất hiện đại.

Mặc dù R-cnn nhanh hơn là một mô hình thống nhất duy nhất nhưng kiến ​​trúc bao gồm hai mô-đun.

Tại đây bạn có thể thấy RPN là khu vực, đề xuất, mạng và R-cnn nhanh.

Fast R-cnn được sử dụng để trích xuất các đặc điểm từ các vùng được đề xuất và xuất ra hộp giới hạn

và nhãn lớp.

Cả hai mô-đun RPN và CNN đều hoạt động trên cùng một đầu ra của mạng nơ-ron tích chập sâu được đào tạo trước

mô hình có thể là VGG hoặc ResNet.

Mạng đề xuất khu vực hoạt động như một cơ chế chú ý cho mạng R-cnn nhanh, thông báo cho

mạng thứ hai về nơi cần tìm hoặc chú ý.

RPN là một mạng riêng biệt hoạt động trên bản đồ tính năng đầu ra của CNN đường trục.

Nó tạo ra các đề xuất khu vực là những ứng cử viên đối tượng tiềm năng.

RPN là một mạng riêng biệt hoạt động trên bản đồ tính năng đầu ra của CNN đường trục.

Nó tạo ra đề xuất vùng, là các đối tượng tiềm năng, bằng cách trượt một cửa sổ nhỏ gọi là

một điểm neo trên bản đồ đặc điểm và dự đoán xem mỗi điểm neo có chứa một đối tượng hay không.

RPN dự đoán xác suất của tính đối tượng cho mỗi mỏ neo và cũng tinh chỉnh tọa độ neo

để phù hợp hơn với các đối tượng.

Hộp giới hạn.

Vì vậy, cải tiến quan trọng trong R-cnn nhanh hơn là việc tích hợp RPN hoặc mạng đề xuất khu vực vào

kiến trúc chia sẻ quy trình trích xuất tính năng với các thành phần phát hiện đối tượng.

Điều này cho phép đào tạo từ đầu đến cuối và cải thiện hiệu quả bằng cách loại bỏ nhu cầu về khu vực riêng biệt

thuật toán đề xuất.

Vì vậy, ở đây bạn có thể quan sát tốc độ thời gian thử nghiệm của R-cnn, R-cnn nhanh và R-cnn nhanh hơn.

Vì vậy, R-cnn nhanh rất hiệu quả về mặt tốc độ so với R-cnn và R-cnn nhanh.

Vì vậy, một số hạn chế của R-cnn được giải quyết trong R-cnn nhanh và hạn chế của R-cnn nhanh

đã được giải quyết trong R-cnn nhanh hơn.

Vì vậy, R-cnn nhanh hơn và có điểm chính xác trung bình trung bình cao nhất.

Tính toán các đề xuất với mạng nơ-ron tích chập sâu được gọi là mạng đề xuất vùng RPN, hợp nhất

RPN và R-cnn nhanh chóng vào một mạng duy nhất, cho phép đề xuất khu vực gần như miễn phí.