# 2. Cách YOLO hoạt động để phát hiện đối tượng

---

Bây giờ hãy xem YOLO hoạt động như thế nào để phát hiện đối tượng.

YOLO xử lý toàn bộ hình ảnh trong một lần duy nhất để dự đoán các hộp giới hạn.

Và lớp học của nó.

Làm cho việc phát hiện đối tượng trở nên hiệu quả về mặt tính toán.

Vì vậy, hãy xem cơ chế.

Nó hoạt động như thế nào.

YOLO đưa ra dự đoán về một hình ảnh chỉ trong một lần truyền.

Điều này tốt hơn các phương pháp truyền thống sử dụng cửa sổ trượt trên toàn bộ hình ảnh.

Tích chập.

Đề xuất khu vực của chúng tôi được sử dụng ở nhiều địa điểm để bản địa hóa các đối tượng.

Vì vậy, theo các phương pháp truyền thống, bạn xác định một cửa sổ có kích thước tùy ý và trượt nó qua hình ảnh.

Ở mỗi bước, bạn phân loại xem cửa sổ có chứa đối tượng bạn quan tâm hay không.

Cách YOLO thực hiện điều này bằng cách chia hình ảnh thành lưới S chéo S, như hiển thị bên dưới.

Trong hình ảnh mà mỗi ô lưới chịu trách nhiệm tạo hộp giới hạn và điểm tin cậy

đầu ra, YOLO chia hình ảnh đầu vào thành lưới S chéo S.

Nếu tâm của một đối tượng rơi vào một ô lưới thì ô lưới đó có nhiệm vụ phát hiện ra

đối tượng.

Mỗi ô lưới dự đoán các hộp giới hạn và điểm tin cậy cho các hộp đó.

Điểm tin cậy này phản ánh mức độ tin cậy của mô hình, rằng mô hình có chứa một đối tượng và

nó cho rằng hộp được dự đoán chính xác đến mức nào, vì vậy bạn có thể coi mỗi ô lưới là một hộp neo.

Vì vậy, thực ra bạn sẽ giới thiệu khái niệm về hộp neo so với cửa sổ trượt trong kiểu truyền thống.

phương pháp.

Vậy hộp neo là gì.

Hộp neo là các hộp giới hạn được xác định trước với kích thước, tỷ lệ khung hình và vị trí cụ thể.

được sử dụng làm mẫu tham chiếu trong quá trình phát hiện đối tượng.

Các hộp neo này được đặt ở nhiều vị trí khác nhau trên một hình ảnh, thường ở dạng lưới.

Như bạn có thể thấy, YOLO chia hình ảnh này thành bảy lưới chéo, vì vậy mỗi lưới đóng vai trò như một mỏ neo

hộp.

Vì vậy, những hộp neo này chụp các vật thể có kích thước và hình dạng khác nhau.

Trong quá trình đào tạo và suy luận.

Các hộp neo được sử dụng để dự đoán vị trí và hình dạng của các đối tượng liên quan đến các hộp tham chiếu này.

Tuy nhiên, bạn phải hiểu rằng hộp neo không phải là hộp giới hạn.

Bạn muốn dự đoán một hộp giới hạn và để làm được điều này, bạn sẽ sử dụng hộp neo làm công cụ trợ giúp, do đó, giới hạn

các hộp đại diện cho các vùng thực tế trong hình ảnh bao gồm các đối tượng quan tâm và các hộp neo

là các hộp giới hạn tham chiếu được sử dụng để dự đoán vị trí và hình dạng đối tượng trong quá trình phát hiện đối tượng.

Vì vậy, mỗi ô dự đoán các hộp giới hạn và độ tin cậy của từng hộp.

Vì vậy, mỗi hộp giới hạn được biểu diễn bằng một tập hợp các tham số x, y, w, h trong đó xy là tâm của

hộp và w là chiều rộng và h là chiều cao của hộp và mỗi hộp giới hạn dự đoán một mục tiêu

điểm cho thấy độ tin cậy rằng hộp chứa một vật thể.

Vì vậy, đối tượng p về cơ bản biểu thị xác suất mà hộp chứa một đối tượng.

YOLO dự đoán nhiều hộp giới hạn trên mỗi ô lưới tại thời điểm đào tạo.

Bạn chỉ muốn một bộ dự đoán hộp giới hạn chịu trách nhiệm cho từng đối tượng.

YOLO chỉ định một người dự đoán chịu trách nhiệm dự đoán một đối tượng dựa trên dự đoán đó.

điểm giao nhau cao nhất hiện nay về điểm công đoàn với sự thật cơ bản.

Điều này dẫn đến sự chuyên môn hóa giữa các yếu tố dự đoán hộp giới hạn.

Mỗi yếu tố dự đoán sẽ tốt hơn trong việc dự báo các kích thước nhất định.

Tỷ lệ khung hình là các lớp đối tượng cải thiện điểm tổng thể, vì vậy mỗi hộp neo hoặc ô cũng dự đoán

phân bố xác suất trên các lớp khác nhau.

Vì vậy YOLO sử dụng kích hoạt softmax để dự đoán xác suất của lớp.

Vì vậy, ví dụ trong hình ảnh ở đây bạn có thể thấy chiếc xe.

Và đây là chiếc xe đạp.

Và ở đây bạn có thể nhìn thấy bến tàu.

Vì vậy, mỗi ô cũng dự đoán phân bố xác suất trên các lớp khác nhau.

Ví dụ ô này thuộc về dock.

Và ở đây bạn có thể thấy xác suất của con chó là 0,8 và xác suất của con mèo là 0.

Và xe đạp bằng 0 vì ô này chứa đối tượng con chó.

Vì vậy, mỗi hộp neo hoặc ô lưới trong ảnh sẽ tính toán giá trị độ tin cậy tọa độ x, y và

vectơ xác suất của lớp.

Vì vậy, ô đầu tiên đề cập đến giá trị độ tin cậy, không gì khác ngoài một nhãn quyết định xem có

đối tượng có nằm trong ô lưới hay không.

Vì vậy, nó có thể là 0 hoặc 1.

Nếu câu trả lời là có hoặc một thì chúng ta chuyển sang dự đoán giá trị của các ô giới hạn trong x, y,

định dạng w, h, trong đó x, y là tọa độ tâm của hộp giới hạn và w và h đề cập đến

đến chiều rộng và chiều cao của các hộp giới hạn.

Và cuối cùng, chúng ta có vectơ phân bố xác suất của lớp, chứa điểm dự đoán cho

mỗi nhãn đối tượng nằm trong khoảng từ 0 đến 1.

Sau đó YOLO kết hợp các dự đoán về hộp và lớp.

Sau đó, bạn sẽ thực hiện phát hiện ngưỡng và Ngăn chặn không tối đa hoặc NMS.

Vậy triệt tiêu không tối đa là gì?

Sau khi dự đoán được thực hiện, bước xử lý hậu kỳ có tên là Ngăn chặn không tối đa sẽ được áp dụng.

Về cơ bản, việc ngăn chặn không tối đa là NMS loại bỏ các hộp giới hạn dư thừa và giữ lại hộp có

độ tin cậy cao nhất cho từng đối tượng.

Vì vậy, đầu ra cuối cùng của YOLO là một tập hợp các hộp giới hạn.

Vì vậy, ở đây bạn có thể thấy hộp giới hạn xung quanh con chó, hộp giới hạn xung quanh xe đạp và hộp giới hạn

hộp xung quanh xe.

Vì vậy, ở đây bạn có thể thấy lớp của hộp giới hạn này là dog, chứa đối tượng dog có điểm số

0,80.

Tương tự, xe đạp và ô tô.

Vì vậy, đầu ra cuối cùng của YOLO là một tập hợp các hộp giới hạn cho từng đối tượng được liên kết với lớp

và điểm tin cậy.