# 1. Giới thiệu về Gia đình YOLO

---

YOLO đang phát triển theo thời gian để nhận dạng đối tượng theo thời gian thực.

Vì vậy, gia đình YOLO có các biến thể khác nhau, bao gồm YOLO phiên bản hai, YOLO phiên bản ba, YOLO

phiên bản bốn, phiên bản YOLO năm, phiên bản YOLO sáu, phiên bản YOLO bảy và phiên bản mới nhất của

YOLO là YOLO phiên bản thứ tám.

Bây giờ chúng ta hãy tiếp tục xem chi tiết về các biến thể YOLO này của gia đình YOLO.

YOLO là một thuật toán nhận dạng đối tượng theo thời gian thực phổ biến, thống nhất, đã cách mạng hóa lĩnh vực này

của thị giác máy tính.

YOLO nhanh và hiệu quả, khiến nó trở thành sự lựa chọn tuyệt vời cho nhiệm vụ phát hiện đối tượng theo thời gian thực.

YOLO đã đạt được hiệu suất hiện đại trên nhiều tiêu chuẩn khác nhau và đã được áp dụng rộng rãi ở

các ứng dụng thế giới thực khác nhau.

YOLO là một thuật toán phát hiện đối tượng theo thời gian thực hiện đại được Joseph và cộng sự của ông giới thiệu vào năm 2015.

các thành viên khác trong bài nghiên cứu nổi tiếng của họ, You Only Look Once Unified phát hiện đối tượng theo thời gian thực.

YOLO đề xuất mạng lưới thần kinh tích chập sâu từ đầu đến cuối giúp dự đoán các hộp giới hạn

và xác suất của lớp tất cả cùng một lúc.

YOLO khác với cách tiếp cận được thực hiện bởi các thuật toán phát hiện đối tượng trước đó, sử dụng lại, phân loại

các lớp để thực hiện phát hiện.

Vì vậy, theo một cách tiếp cận cơ bản khác để nhận dạng đối tượng, YOLO đã đạt được công nghệ tiên tiến

kết quả, đánh bại thuật toán phát hiện đối tượng thời gian thực khác bằng một khoảng cách lớn.

Một trong những ưu điểm chính của YOLO là tốc độ suy luận nhanh, cho phép nó xử lý hình ảnh một cách dễ dàng.

thời gian thực.

Nó rất phù hợp cho các ứng dụng như giám sát video, xe tự lái và thực tế tăng cường.

Ngoài ra, YOLO có kiến trúc đơn giản và yêu cầu dữ liệu đào tạo tối thiểu, giúp dễ dàng

thực hiện và thích ứng với nhiệm vụ mới.

Đây là kiến ​​trúc YOLO.

YOLO là một hệ thống phát hiện đối tượng theo thời gian thực, xử lý hình ảnh trong một lần chuyển tiếp qua

mạng lưới thần kinh tích chập.

Thuật toán YOLO lấy hình ảnh làm đầu vào và sau đó sử dụng mạng nơ ron tích chập sâu đơn giản.

Như vậy bạn có thể thấy kiến ​​trúc YOLO tương tự như GoogLeNet.

YOLO có tổng thể 24 lớp tích chập, bốn lớp tổng hợp tối đa và hai lớp được kết nối đầy đủ.

Ở đây bạn có thể thấy một lớp chập và sau đó lại là một lớp chập.

Và sau đó bạn có thể thấy bốn lớp chập.

Và sau đó có mười lớp chập, sau đó là sáu lớp chập, rồi hai lớp.

Vì vậy, tổng cộng có 24 lớp tích chập trong kiến ​​trúc YOLO.

Và bạn cũng có thể thấy lớp tổng hợp tối đa.

YOLO có bốn lớp gộp tối đa một rồi hai, ba và bốn.

Và cuối cùng bạn có thể thấy lớp được kết nối đầy đủ.

Vì vậy kiến ​​trúc YOLO có hai lớp được kết nối đầy đủ.

Vì vậy, YOLO lấy một hình ảnh đầu vào.

Thay đổi kích thước hình ảnh đầu vào thành 448 x 448 trước khi đi qua mạng tích chập.

Ở đây bạn có thể thấy kiến trúc YOLO lấy hình ảnh đầu vào có kích thước 448 x 448.

Vì vậy, YOLO sử dụng mạng lưới thần kinh tích chập làm xương sống.

Tích chập một chéo một lần đầu tiên được áp dụng để giảm số lượng kênh, sau đó được thực hiện theo

bằng phép chập ba chéo ba để tạo ra đầu ra hình khối.

Chức năng kích hoạt dưới mui xe là ReLU, ngoại trừ lớp cuối cùng sử dụng kích hoạt tuyến tính

chức năng.

Một số kỹ thuật bổ sung, chẳng hạn như chuẩn hóa hàng loạt và loại bỏ, tương ứng, thường xuyên hóa

mô hình hóa và ngăn chặn nó khỏi bị trang bị quá mức.

YOLO có một đầu phát hiện chịu trách nhiệm dự đoán xác suất của hộp và lớp giới hạn.

Nó bao gồm một loạt các lớp chập và kết thúc bằng một lớp được kết nối đầy đủ, vì vậy lớp cuối cùng

lớp đại diện cho đầu phát hiện.

Đầu ra của đầu phát hiện là một lưới các ô.

Mỗi ô có trách nhiệm dự đoán một số hộp giới hạn nhất định.

Toàn bộ hình ảnh được chia thành một lưới và mỗi ô dự đoán các hộp giới hạn dựa trên các đối tượng có mặt

trong ô đó.

Mỗi hộp giới hạn dự đoán phân bố xác suất trên các lớp khác nhau.

YOLO sử dụng kích hoạt softmax để dự đoán xác suất của lớp.

Mỗi hộp giới hạn dự đoán một điểm về tính khách quan, biểu thị độ tin cậy rằng hộp chứa một

đối tượng, do đó, 20 lớp chập đầu tiên của mô hình được đào tạo trước bằng ImageNet.

Bằng cách kết nối một lớp tổng hợp trung bình tạm thời và lớp được kết nối đầy đủ, mô hình được đào tạo trước này sẽ được chuyển đổi

để thực hiện việc phát hiện.

Vì nghiên cứu trước đây cho thấy rằng việc thêm các lớp tích chập và kết nối vào mạng được đào tạo trước

cải thiện hiệu suất, do đó lớp được kết nối đầy đủ cuối cùng của YOLO dự đoán cả xác suất và giới hạn của lớp

tọa độ hộp, do đó đầu ra cuối cùng của YOLO là một tập hợp các hộp giới hạn, mỗi hộp được liên kết với

một lớp học và điểm tự tin.

Nếu bạn muốn nghiên cứu tài liệu nghiên cứu của YOLO thì có thể tham khảo tài liệu nghiên cứu đã xuất bản.

Bạn chỉ nhìn một lần.

Phát hiện đối tượng theo thời gian thực thống nhất của Joseph.