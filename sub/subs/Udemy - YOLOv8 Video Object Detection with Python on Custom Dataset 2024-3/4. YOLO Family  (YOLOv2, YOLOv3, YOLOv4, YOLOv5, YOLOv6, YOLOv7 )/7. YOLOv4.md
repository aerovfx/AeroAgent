# 7. YOLOv4

---

Yolov4 là phiên bản thứ tư của thuật toán phát hiện đối tượng Yolo, được giới thiệu vào năm 2020 dưới dạng cải tiến

hơn YOLO ba.

YOLO bốn đã giới thiệu những cải tiến như đầu phát hiện không có neo mới, chức năng tăng cường mất mát mới,

v.v... cải tiến chính của YOLO 4 so với YOLO 3 là việc sử dụng kiến trúc CNN mới có tên là

mạng CSP.

Mạng CSP là viết tắt của Cross Stage Partial Network và là một biến thể của kiến trúc ResNet được thiết kế đặc biệt

cho các nhiệm vụ phát hiện đối tượng.

Nó có cấu trúc tương đối nông chỉ có 54 lớp chập.

Tuy nhiên, nó có thể đạt được kết quả hiện đại trên nhiều tiêu chuẩn phát hiện đối tượng khác nhau.

Cả YOLO ba và YOLO bốn đều sử dụng các hộp neo với tỷ lệ và tỷ lệ khung hình khác nhau để phù hợp hơn

kích thước và hình dạng của đối tượng được phát hiện.

YOLO 4 giới thiệu một phương pháp mới để tạo ra các hộp neo được gọi là k có nghĩa là phân cụm.

Nó liên quan đến việc sử dụng thuật toán phân cụm để nhóm các hộp giới hạn thực tế thành các cụm và

sau đó sử dụng trọng tâm của cụm làm hộp neo.

Điều này cho phép các hộp neo được căn chỉnh chặt chẽ hơn với kích thước và hình dạng đối tượng được phát hiện.

Trong khi cả YOLO ba và YOLO bốn đều sử dụng hàm mất tương tự để huấn luyện mô hình, YOLO bốn giới thiệu

một Tum mới gọi là mất GMM.

Đây là một biến thể của chức năng mất tiêu cự và được thiết kế để cải thiện hiệu suất của mô hình khi mất cân bằng

bộ dữ liệu.

YOLO bốn cũng cải thiện kiến ​​trúc của mạng kim tự tháp tính năng FPN được sử dụng trong YOLO ba.

Vì vậy, sự cải tiến của YOLO 4 so với YOLO 3 là việc sử dụng kiến trúc mạng nơ-ron tích chập mới

được gọi là mạng CSP hoặc mạng một phần xuyên giai đoạn, như thể hiện trong hình và mạng CSP là một biến thể của

Kiến trúc ResNet được thiết kế đặc biệt cho nhiệm vụ phát hiện đối tượng.

Nó có cấu trúc tương đối nông chỉ có 54 lớp chập.

Tuy nhiên, nó có thể đạt được kết quả hiện đại trên nhiều tiêu chuẩn phát hiện đối tượng khác nhau.

Mạng CSP bao gồm một khối dày đặc một phần.

Ở đây bạn có thể thấy khối dày đặc một phần.

Và một lớp chuyển tiếp một phần.

Ở đây bạn có thể thấy lớp chuyển tiếp trong một khối dày đặc một phần.

Các bản đồ đặc trưng của lớp cơ sở trong một giai đoạn được chia thành hai phần thông qua các kênh.

Trong lớp chuyển tiếp một phần, đầu ra của lớp dày đặc sẽ trải qua lớp chuyển tiếp.

Sau đó, đầu ra của quá trình chuyển đổi này sẽ được nối và trải qua một lớp chuyển tiếp khác và sau đó

tạo ra đầu ra.

Tại đây bạn có thể thấy sự so sánh giữa yolov4 được đề xuất và các máy dò vật thể hiện đại khác.

Yolov4 chạy nhanh hơn gấp đôi so với Efficiencydet với hiệu suất tương đương trên trục y.

Trên trục y, bạn có thể thấy độ chính xác trung bình và trên trục x là khung hình trên giây và hiệu suất

của yolov4 so với Efficiencydet và YOLO ba được cải thiện.

Vì vậy, YOLO 4 chạy nhanh hơn gấp đôi so với Efficiencydet.

YOLO phiên bản bốn đã được xuất bản trong một bài nghiên cứu vào năm 2020.

Tại đây bạn có thể xem tài liệu YOLO phiên bản bốn Tốc độ tối ưu và độ chính xác của phát hiện đối tượng.

Vì vậy, vui lòng đọc tài liệu nghiên cứu này nếu bạn muốn nghiên cứu sâu hơn về YOLO phiên bản bốn.

Tại đây bạn sẽ tìm thấy tất cả thông tin chi tiết về kiến ​​trúc, bộ dữ liệu cũng như kết quả.

YOLO phiên bản bốn so với các máy dò đối tượng khác.

Và thông tin chi tiết hơn về mạng CSP mà Yolov4 đang sử dụng, các bạn có thể tham khảo tài liệu CSP.

Net một xương sống mới có thể nâng cao khả năng học tập của mạng nơ-ron tích chập.

Vì vậy bài viết này đã được xuất bản vào năm 2019.

Vì vậy, ở đây bạn sẽ thấy tất cả các chi tiết về mạng CSP.

Như bạn có thể thấy mật độ kiến ​​trúc và mật độ một phần xuyên giai đoạn.

Và bạn cũng có thể tìm thấy thông tin chi tiết về kết quả.

Tại đây bạn có thể xem kết quả và với các tập dữ liệu khác nhau.

Vì vậy, vui lòng đọc bài viết này vì Yolov4 đang sử dụng mạng CSP làm mạng đường trục.