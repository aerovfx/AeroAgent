# 5. Tính toán các số liệu hiệu suất (Độ chính xác, Thu hồi, mAP chính xác trung bình trung bình)

---

Vì vậy, hãy thảo luận về các số liệu hiệu suất để phát hiện đối tượng.

Vậy các chỉ số hiệu suất là gì.

Số liệu hiệu suất là công cụ chính để đánh giá tính chính xác và hiệu quả của các mô hình phát hiện đối tượng.

Họ làm sáng tỏ mức độ hiệu quả của một mô hình có thể xác định và định vị các đối tượng trong hình ảnh.

Ngoài ra, chúng giúp hiểu được cách xử lý dương tính giả và âm tính giả của mô hình.

Những hiểu biết sâu sắc này rất quan trọng để đánh giá và nâng cao hiệu suất của mô hình.

Bây giờ bạn sẽ khám phá các số liệu hiệu suất khác nhau để phát hiện đối tượng.

Vì vậy, cái đầu tiên là giao nhau trên hợp, về cơ bản là thước đo định lượng sự chồng chéo

giữa hộp giới hạn được dự đoán và hộp giới hạn thực tế.

Nó đóng một vai trò cơ bản trong việc đánh giá tính chính xác của việc định vị đối tượng.

Và sau đó là độ chính xác trung bình.

Độ chính xác trung bình tính toán diện tích dưới đường cong thu hồi độ chính xác, cung cấp một giá trị duy nhất

gói gọn độ chính xác và hiệu suất thu hồi của mô hình, sau đó có độ chính xác trung bình.

Độ chính xác trung bình trung bình mở rộng khái niệm về độ chính xác trung bình bằng cách tính các giá trị AP trung bình

trên nhiều lớp đối tượng.

Điều này rất hữu ích trong các kịch bản phát hiện đối tượng đa lớp để cung cấp đánh giá toàn diện về mô hình

hiệu suất, sau đó là độ chính xác và thu hồi.

Độ chính xác định lượng tỷ lệ tích cực thực sự trong số tất cả các dự đoán tích cực.

Đánh giá khả năng của mô hình để tránh các kết quả dương tính giả.

Mặt khác, thu hồi tính toán tỷ lệ kết quả tích cực thực sự trong số tất cả kết quả tích cực thực tế,

đo lường khả năng của mô hình để phát hiện tất cả các phiên bản của một lớp.

Và sau đó là điểm F1.

Điểm F1 là giá trị trung bình hài hòa của độ chính xác và khả năng thu hồi, mang lại đánh giá cân bằng về một mô hình

hiệu suất trong khi xem xét cả dương tính giả và âm tính giả.

Bây giờ, hãy tính toán mức thu hồi chính xác và độ chính xác trung bình trung bình của mô hình được đào tạo khi xác thực

tập dữ liệu.

Vì vậy, hãy nhập YOLO từ siêu phân tích và sau đó tải trọng số của mô hình.

Và mô hình có trọng số tốt nhất nằm ở con đường này.

Vậy mô hình bằng YOLO.

Sau đó, chỉ định đường dẫn và xác thực mô hình bằng chức năng xác thực dấu chấm mô hình.

Vì vậy, trong hàm này không cần đối số.

Và tập dữ liệu cũng như cài đặt đã được ghi nhớ trong khi đào tạo vì bạn đã cung cấp tệp Yaml,

và nó sẽ sử dụng tập dữ liệu xác thực để xác thực mô hình này và nó sẽ tính toán độ chính xác

thu hồi và đo độ chính xác trung bình trung bình để phát hiện đối tượng.

Vì vậy, ví dụ: nếu tôi đi đến tệp dấu chấm Yaml tập dữ liệu của chúng tôi.

Vì vậy, ở đây bạn có thể xem dữ liệu xác nhận.

Và nếu bạn muốn tính toán độ chính xác thu hồi và độ chính xác trung bình trung bình.

Và sau đó bạn sẽ chỉ định đường dẫn của tập dữ liệu ở đây.

Bởi vì chức năng xác thực dấu chấm của mô hình sẽ sử dụng đường dẫn này để xác thực hình ảnh.

Vì vậy, về cơ bản chức năng xác nhận dấu chấm mô hình.

Sử dụng hình ảnh xác thực để tính toán số liệu hiệu suất.

Vì vậy, ví dụ: nếu bạn muốn tính toán độ chính xác thu hồi và độ chính xác trung bình của số liệu hiệu suất

trên dữ liệu thử nghiệm, khi đó bạn sẽ chỉ định đường dẫn của hình ảnh thử nghiệm phía trước các đối số xác thực.

Vì vậy, ở đây bạn sẽ chỉ định đường dẫn của hình ảnh thử nghiệm.

Vì vậy, hãy chạy cái này.

Và điều này sẽ tính toán mức thu hồi chính xác và độ chính xác trung bình trung bình.

Vì vậy, hãy chạy mã này.

Và ở đây bạn có thể thấy nó đang quét các hình ảnh xác nhận.

Ở đây bạn có thể thấy nó đang quét các hình ảnh xác nhận.

Và sau đó là tính điểm.

Vì vậy, ở đây bạn có thể thấy các phiên bản hình ảnh lớp và sau đó là các hộp giới hạn.

Thu hồi chính xác có nghĩa là độ chính xác trung bình với ngưỡng 50% và sau đó là ngưỡng chính xác trung bình

giữa 5295.

Vì vậy, hãy xem chi tiết về các số liệu này.

Vì vậy, về cơ bản đây là những số liệu khôn ngoan về đẳng cấp.

Vì vậy, hãy để tôi đưa ra một số chi tiết cho bạn ở đây.

Vì vậy, về cơ bản đây là những số liệu khôn ngoan về đẳng cấp.

Vì vậy, những điều này rất hữu ích khi bạn đang cố gắng hiểu mô hình hoạt động tốt như thế nào đối với từng

lớp, đặc biệt là trong tập dữ liệu với nhiều loại đối tượng đa dạng.

Vì vậy, lớp ở đây đại diện cho lớp đối tượng, chẳng hạn như.

Ở đây bạn có thể nhìn thấy xe buýt cứu thương và xe máy.

Và về cơ bản, hình ảnh và hình ảnh sẽ cho bạn biết số lượng hình ảnh trong bộ xác thực

chứa lớp đối tượng.

Và sau đó là các trường hợp.

Vì vậy, các phiên bản cung cấp số lần lớp xuất hiện trên tất cả các hình ảnh trong quá trình xác thực

thiết lập.

Và sau đó là ma trận hộp giới hạn.

Và ở đây bạn có thể thấy.

Vì vậy, các số liệu về hộp giới hạn này bao gồm cả việc thu hồi độ chính xác có nghĩa là độ chính xác trung bình sẽ cung cấp thông tin chi tiết

vào hiệu suất của mô hình trong việc phát hiện các đối tượng.

Vì vậy, có độ chính xác trung bình thu hồi chính xác với 50 và độ chính xác trung bình trung bình với ngưỡng 5295.

Vì vậy, độ chính xác là độ chính xác của các đối tượng được phát hiện cho biết có bao nhiêu phát hiện đúng.

Và việc thu hồi là khả năng của mô hình để xác định tất cả các trường hợp của đối tượng trong ảnh và có ý nghĩa

độ chính xác trung bình với 50 được tính tại điểm giao nhau trên ngưỡng kết hợp là 0,50.

Đó là thước đo độ chính xác của mô hình chỉ xem xét các phát hiện dễ dàng và độ chính xác trung bình trung bình là gì

trong khoảng từ 50 đến 95.

Về cơ bản đó là giá trị trung bình của độ chính xác trung bình được tính tại các giao điểm khác nhau trên

ngưỡng kết hợp nằm trong khoảng từ 0,52 đến 0,95 và nó cung cấp cái nhìn toàn diện về hiệu suất của mô hình

qua các mức độ khó phát hiện khác nhau.

Vì vậy, ở đây bạn có thể thấy đối với tất cả các lớp, độ chính xác của hộp là 0,72, thu hồi là 0,55 và độ chính xác trung bình trung bình

với ngưỡng 50% là 0,62 và độ chính xác trung bình trung bình là 5295.

Ngưỡng là 0,46.

Vì vậy, đây là độ chính xác trung bình trung bình và dễ dàng phát hiện.

Vì thế điểm cao hơn.

Và đây là độ chính xác trung bình trung bình với việc phát hiện khó khăn.

Vì thế điểm số sẽ thấp hơn một chút.

Vì vậy, tương tự đối với xe cứu thương, bạn có thể thấy điểm chính xác trung bình thu hồi chính xác.

Và tương tự đối với xe cứu thương, xe buýt, ô tô, xe máy và tương tự

cho xe tải.

Vì vậy, đối với độ chính xác của xe tải là 0,62, mức thu hồi là 0,33, độ chính xác trung bình trung bình là 0,44.

Vì vậy, bằng cách này, bạn có thể tính toán độ chính xác trung bình thu hồi chính xác cho các hộp giới hạn.

Vì vậy, bằng cách này, bạn có thể tính toán các số liệu khôn ngoan của lớp bao gồm độ chính xác trung bình thu hồi chính xác

của các hộp giới hạn để bạn có thể thấy mô hình đang hoạt động như thế nào để bạn có thể quan sát mô hình

thành tích của tất cả các lớp.