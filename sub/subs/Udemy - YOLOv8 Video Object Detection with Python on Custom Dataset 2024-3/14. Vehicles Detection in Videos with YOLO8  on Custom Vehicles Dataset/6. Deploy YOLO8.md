# 6. Triển khai YOLO8

---

Bạn có thể xuất mô hình YOLO sang bất kỳ định dạng được hỗ trợ nào bằng đối số định dạng.

Mục tiêu cuối cùng của việc đào tạo một mô hình là triển khai nó cho các ứng dụng trong thế giới thực.

Chế độ xuất trong Ultralytics YOLO 8 cung cấp nhiều tùy chọn linh hoạt để xuất dữ liệu đã đào tạo của bạn

mô hình sang các định dạng khác nhau, giúp nó có thể triển khai trên nhiều nền tảng và thiết bị khác nhau.

Bạn có thể xuất sang nhiều định dạng, bao gồm O double N, x Tensorrt core ML.

Vì vậy, nếu bạn muốn xem các định dạng xuất được hỗ trợ, hãy truy cập tài liệu của Ultralytics.

Và nếu bạn vào các chế độ và trong các chế độ, bạn có thể thấy phần xuất.

Và ở đây trong quá trình xuất, bạn sẽ thấy các định dạng xuất.

Vì vậy, các định dạng xuất YOLO có sẵn được đưa ra trong bảng.

Bạn có thể xuất ở định dạng PyTorch trong Torchscript hoặc trong OpenVINO Tensorrt core ml tf save model

tf đồ thị dev tf lite, tpu tf dot js song song song song NC đôi n.

Vì vậy, để đào tạo mô hình, bạn có thể triển khai mô hình của mình theo định dạng được hỗ trợ để bạn có thể

chọn định dạng theo yêu cầu triển khai của bạn.

Theo mặc định, mô hình được đào tạo của chúng tôi nằm trong PyTorch và bạn cũng có thể chỉ định các đối số khác bằng

xuất khẩu.

Như bạn có thể thấy.

Định dạng.

Để xuất kích thước hình ảnh, hãy sử dụng Keras cho mô hình lưu TF, tối ưu hóa tối ưu hóa Torchscript cho thiết bị di động và

tương tự các lập luận khác.

Bạn có thể sử dụng những đối số này.

Vì vậy, bây giờ bạn sẽ xuất mô hình đã được đào tạo sang dạng lưỡng bội.

Bạn có thể xuất ở các định dạng khác nhau.

Bạn có thể xuất mô hình YOLO đã được đào tạo trong PyTorch, Torchscript hoặc X và tương tự ở đây bạn có thể thấy

các định dạng khác tensor core ml, tf lite, tf edge, tf dot js, tf dot js, mái chèo và nccn.

Vì vậy, hãy xuất mô hình YOLO ở định dạng O double x.

Vì vậy, với lệnh YOLO, tâm trạng hiện ở chế độ xuất.

Và đây là đường dẫn mô hình, mô hình có trọng số tốt nhất.

Và sau đó định dạng để xuất chỉ mục âm thanh này.

Vì vậy, hãy chạy cái này.

Và điều này sẽ xuất mô hình ở dạng O double X.

Và mô hình của chúng tôi theo mặc định ở định dạng PyTorch.

Vì vậy, nó đang khởi tạo để xuất mô hình từ PyTorch sang định dạng X gấp đôi.

Vì vậy, ở đây bạn có thể thấy.

Vì vậy, ở đây bạn có thể thấy nó bắt đầu xuất với double X và sau đó kết quả đã được lưu vào phần huấn luyện

kết quả phát hiện xe và trọng lượng.

Vì vậy chúng ta hãy vào thư mục phát hiện xe và trọng lượng.

Vì vậy, ở đây bạn có thể thấy các thư mục phát hiện và trọng lượng xe.

Và ở đây bạn có thể thấy dấu chấm o double x tốt nhất.

Vậy là bạn đã xuất mô hình từ best dot PD ở định dạng PyTorch hai định dạng O double X.

Vì vậy, tương tự, bạn có thể xuất mô hình đã đào tạo từ định dạng này sang định dạng khác.