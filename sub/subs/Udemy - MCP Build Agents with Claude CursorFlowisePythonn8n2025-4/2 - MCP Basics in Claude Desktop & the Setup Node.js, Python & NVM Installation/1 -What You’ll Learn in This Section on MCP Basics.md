# 1 -What You’ll Learn in This Section on MCP Basics translated

---

In this section we will basically cover a lot and I want to give you a quick overview.

If I want to learn something new, a great starting point is always an official documentation

and behalf of a really great official documentation of the model context protocol

and I want to guide you through it. As soon as these basics are down, as soon as you have a great

overview we will install some things that we need. We will install no JS on our local machine because

most of the servers work with a command that is called npx and npx is of course a command that runs

via no JS. We will also install nvm to upgrade or downgrade your versions. This is really important

if you run into problems. We will install of course Cloud desktop because this is the first host

and this section is especially for Cloud desktop and we will also install VS Code because VS Code

is a code editor and with VS Code we can edit our Cloud desktop config files. As soon as everything

is set up I want to give you an overview of Cloud desktop so the interface what's important what you

need to activate and how you can generally work with this tool. Then we will immediately include

our first MCB server. We will use the easiest to implement the server that gives Cloud access to

our local machine. This means you can move files and do a lot of cool things on your machine.

Then I want to show you a nice quick step. We will install the MCB installer because the MCB

installer makes it really easy to install a lot of model context protocols so that you don't have

to change this config file all day long. Cloud can help us with this. After that we will include

our first servers that run with the UVX command. The UVX command runs inside of UV. UV is a

package manager in Python and that's why we need to install Python. We will also install the UV

package manager and we will also install Pi ENV to manage our Python versions if it's needed.

Then I will show you all the great libraries where you can find a lot of pre-built MCB servers

right now at this minute, nearly 15,000 different servers so I am sure you will find something for you.

After that I will show you how you can integrate MCB servers that need an API key.

Then I will show you one of the greatest MCB servers that lets you connect 7,000 apps. The server

comes from Zapier but it has a gigantic downfall at least in Cloud Esktop and you need to know about

it and you also need to know how you can work around this downfall. And of course I also need to

show you that some servers will depreciate over time. MCB is an open-source standard and the

developers of the servers can eventually decide to no longer maintain the servers. And you should not

get stuck if something like this happens. And I also need to include this right now. MCB servers

can be dangerous. If you give too much access, for example on your local machine, you can

delete stuff that you do not want to delete. If you use servers that you find like somewhere in the

internet there is also the possibility that there is malicious code in the servers. There are two

specific attacks it's called tool poisoning and MCB Rackpulls. I just want to throw this out

right now before you include every single server under the sun. And at the end of this course we

also do a deep dive into the security issues with MCBs. But for right now please don't connect to

every server. Stick mostly with the official servers or with the servers that I show you in this

course and then you are safe. And as soon as you understand all these problems at the end of

this course you can connect to every server that you want. And I see you of course in the next

video where we do a quick overview of the whole documentation so that you get the holistic overview.