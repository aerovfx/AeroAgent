# 17 -n8n Community Node Common Issues with Prompts & Resource translated

---

In this video I want to talk about the downside of the community node.

It's not really a downside of the community node itself, it's more like a downside of the servers that we got.

You already saw it. We can include, of course, this MCP execute tool.

So if you come on this node, you can actually come on operation and use a lot of different things.

You have the execute tool and you use this to execute tools just like you have shown in the previous video.

Then you have get prompt. And here we do have a small little problem.

Because right now at this minute we do not really have a lot of prompts that we can connect to.

And I have to tell you we need to program for example a server for ourself in order to get this prompt.

And then include, of course, also our connection.

So right now it makes not really a lot of sense to dive deeper into prompts because we need to

develop a server in Python for example or in TypeScript in order that we can include prompts in this

and add an tool. And then we have list prompts and list prompts if it is of course also just possible if

you do have prompts and if you do have your own server. Then we have list resource templates, list resource

and the read resource. If you use once again read resources, of course you need to be for URL that

you want to read and you also need to connect right here. And right now at this minute we also

do not really have a lot of MCB servers that we can use to connect with these. So basically we have

to develop a server for ourself so that this thing is working. And if we develop our server for our

self it also makes not really a lot of sense to connect it in an end. We can use it directly for

example in cloud desktop, in cursor or wherever. Or you want to have a yai agent. Maybe you want to

integrate such an agent into a web page. It's also relatively easy to integrate the stuff in web

pages because you get here. If you make this public like a host chat version or the embedded chat version

you can come onto this documentation and you can embed it with this code into web pages.

That's not really the point of this video. The point of this video is to simply tell you that this

thing does not work with all the things. If you put for example make your own chat application or

integrate it somewhere it can make sense if other people use the yai agent for example on the

web page or on the host chat. It could make sense to integrate some pre-programmed prompts. But most

of the time if you use the yai agent just for yourself or maybe just in an end end it also makes not

really a lot of sense to include here resources and prompts. So long story short if we want to

include resources and prompts we need to program such a server for ourself. And we will do this of course

later in the course. And if it makes sense for you you can also integrate this prompt templates

or resources also into your MCP client from an add-in. But most of the time it makes not a lot of sense.

Some use case that I can think of is for example if you develop your own server you include a lot of

tools and you want to host the server so that also other people can use this and you use an add-in

in order to create an agent. And the agent is integrated into a web page and you want that people

that come to this web page have specific pre-programmed prompts then it could make sense. But nonetheless

for right now inside of an add-in we do not have the possibility to include prompts really easy like

at all you we need to program a server for ourself. And of course we will do this later in the course.

This is not for right now.