# 004. 3.3 Tạo chuỗi phân nhánh nhanh chóng

---

Hãy cùng tìm hiểu khái niệm về nhánh

chuỗi, và bạn đã thấy một vài ví dụ rất

nhanh chóng trong phân đoạn trước, nhưng hãy để

tôi thực sự đi qua chi tiết hơn.

Vì vậy, tôi sẽ sử dụng cái đầu tiên

ví dụ hoặc tệp đầu tiên ở đây được gọi là chuỗi phân nhánh.

Hãy để tôi làm những thứ này một chút

tốt hơn một chút để bạn đọc.

Và về cơ bản trong ví dụ này, từ trên xuống,

thực ra, trước khi chúng ta đi vào ví dụ, hãy

tôi xác định thay đổi nhánh trong ngữ cảnh

của linechain và thậm chí cả trong các khuôn khổ tương tự.

Thay đổi chi nhánh là một cách

để giới thiệu logic có điều kiện vào

đường ống xử lý. Hãy nghĩ về họ

như câu lệnh if và else.

Và mặc dù chúng tôi thực sự đang sử dụng Python và lập trình

ngôn ngữ ở đây, bạn có thể quen thuộc với if và else.

Điều này dành riêng cho chuỗi của bạn, không phải

các lệnh hoặc các tùy chọn if và khác trong Python.

Vì vậy, một lần nữa, hãy nghĩ đến

chúng như những câu lệnh điều kiện.

Và sau đó dựa trên đầu vào

hoặc kết quả của lần trước

bước, một chuỗi chi nhánh chắc chắn có thể

hướng quy trình làm việc xuống một

của một số con đường có thể, hoặc

cái mà chúng tôi gọi là chuỗi con.

Vì vậy, trong ví dụ này, điều này rất hữu ích

bất cứ khi nào bạn cần xử lý các loại khác nhau

đầu vào hoặc tình huống theo những cách khác nhau. cho

ví dụ: trong bối cảnh an ninh mạng, bạn có thể sử dụng

một nhánh để định tuyến một sự cố tới

một nhà phân tích cụ thể dựa trên loại

phần mềm độc hại dựa trên việc có mạng xã hội hay không

tấn công kỹ thuật, lừa đảo, vi phạm dữ liệu, v.v.

Bây giờ, trong kịch bản này mà bạn thấy

trước mặt bạn, mục tiêu là

để phân loại một lỗ hổng nhất định, cho

ví dụ: một lỗ hổng thực thi mã từ xa chưa được vá.

Hãy để tôi làm điều này tốt hơn một chút

để bạn đọc. Vì vậy, thực hiện gói từ ở đây,

và lỗ hổng thực thi mã từ xa chưa được vá này, chúng tôi sẽ

để lập bản đồ thành các mức độ nghiêm trọng khác nhau, rất cao,

trung bình hoặc thấp, sau đó chúng tôi sẽ tạo ra

một phản ứng hoặc kế hoạch cụ thể dựa trên mức độ nghiêm trọng đó.

Trong trường hợp này, chúng tôi

thực sự đang vượt qua điều đó.

Bạn là một chuyên gia hacker có đạo đức và nhà phân tích bảo mật

và bạn sẽ tạo ra kế hoạch kiểm tra bút và

ví dụ về các công cụ và báo cáo về các biện pháp giảm thiểu khẩn cấp

dựa trên một lỗ hổng nghiêm trọng. Vì vậy mẫu ở đây

dành cho mẫu lỗ hổng nghiêm trọng và sau đó chúng tôi

có một cái khác cho các lỗ hổng nghiêm trọng cao. Và bạn

thấy rằng phần giữ chỗ ở đây để tìm lỗ hổng và

chúng ta sẽ đi qua điều đó trong giây lát.

Nhưng sau đó bạn có một cái khác cho mức trung bình

và thấp, khi đó bạn có một nhánh có thể chạy được.

Đây là cốt lõi của chi nhánh

logic và những gì LangChain thực sự sử dụng để

cho phép bạn xác định một bộ

các điều kiện và sau đó là chuỗi con tương ứng.

Vì vậy nếu tôi đi xuống bên dưới, bạn có

một chuỗi phân loại ở đây, do đó chuỗi phân loại

Có một lỗ hổng và sau đó bạn thấy

mẫu phân loại dựa trên các mẫu đó. Bạn có sự khác biệt

chi nhánh. Vì vậy, bạn có một cái để phê bình,

một cho mức cao, một cho mức trung bình, một cho mức thấp.

Và sau đó ở đây những gì chúng ta đang làm

là chúng tôi đang vận hành chuỗi dựa trên

để thực thi mã từ xa chưa được vá

tính dễ bị tổn thương. Trong trường hợp này nó là Apache

máy chủ web httpD và sau đó

kết quả của chuỗi đó sẽ được gọi.

Vì vậy, nếu chúng ta chạy cái này

kịch bản, chúng ta sẽ có được suy luận

tương tự như thế này Vì vậy, về cơ bản

chúng tôi có kế hoạch kiểm tra thâm nhập

để hủy bản vá thực thi mã từ xa

lỗ hổng trong máy chủ web Apache HTTPD.

Bạn nhìn thấy mục tiêu, phạm vi,

quy tắc tham gia, phương pháp luận. Họ thậm chí còn

cung cấp cho bạn một bảng cho phương pháp luận

để trinh sát, xác định điểm yếu, khai thác, sau khai thác,

báo cáo và khuyến nghị, v.v. Sau đó

ví dụ về các công cụ sử dụng như nmap,

Nikto, khung Metasploit, v.v. Như

thực tế là bạn có thể thực sự

đính kèm một máy chủ MCP nhất định

công cụ và sau đó tạo một tác nhân.

Và chúng ta sẽ đi qua, bạn biết đấy,

các đại lý sau này trong khóa học, nhưng bạn có thể tạo

các đại lý thực sự chọn những công cụ này dựa trên

loại nhiệm vụ mà bạn có thể có.

Nhưng trong ví dụ này chúng tôi chỉ làm một việc cơ bản

kế hoạch kiểm tra bút và sau đó bạn sẽ thấy một bài kiểm tra bút

báo cáo đó là một ví dụ cho Apache HTTPD này

giải quyết lỗ hổng RCE. Bây giờ điều này rất đơn giản.

Bạn thấy rằng nó có các chi tiết về lỗ hổng, một

tóm tắt điều hành, nhân tiện, đó là một

phương pháp thực hành tốt nhất cho bất kỳ báo cáo pentest nào hiện có.

Có một bản tóm tắt điều hành và sau đó tất cả

chi tiết kỹ thuật sau. Nhưng đây chỉ là xung quanh

một lỗ hổng, vì vậy nó không thực sự là cuộc sống thực

tình huống này vì trong bài kiểm tra bút thông thường bạn

sẽ có nhiều lỗ hổng. Nhưng bạn nhận được

vấn đề, phải không? Bạn có thể làm điều này trong

một cách rất, rất có tổ chức bằng cách sử dụng các ứng dụng chạy được này

trình tự và các kỹ thuật chuỗi nhánh này

để thực hiện loại nhiệm vụ này.

Và trong ví dụ này tất nhiên là bằng bút

thử nghiệm. Bây giờ tôi có một ví dụ khác trong kho GitHub.

Hãy để tôi đi qua

ở đây trên cái này.

Thực ra, điều tôi sắp làm là tôi sẽ

cứ chạy cho nó chạy thôi vì hơi mất thời gian.

Tôi sẽ chạy tập lệnh này trong khi tôi

giải thích những gì chúng ta có trước mặt chúng ta.

Vì vậy, rất giống với cái khác. Chúng tôi là

sử dụng các mẫu lời nhắc LangChain, nhánh có thể chạy được, có thể chạy được.

Tất nhiên, rất giống với cái kia

một nơi bạn chỉ đang sử dụng GPT4.1 mini.

Bạn có thể sử dụng mô hình mới hơn hoặc phức tạp hơn

hoặc mô hình phức tạp như mô hình tư duy hoặc lý luận

mô hình. Nhưng trong trường hợp này chúng tôi chỉ đang thực hiện một

ví dụ rất cơ bản và ví dụ này là để săn tìm mối đe dọa.

Và trong trường hợp này chúng ta thực sự có một Persona

rằng bạn là chuyên gia săn mối đe dọa chuyên về phân tích mạng.

Vậy cái này dành cho IP

chỉ báo, do đó chỉ báo cho các địa chỉ IP khác nhau

và có thể thực hiện chi tiết

săn lùng mối đe dọa dựa trên các chỉ số khác nhau. Phải.

Bây giờ chỉ báo thứ hai về cơ bản là dựa trên DNS.

Vì vậy, nó cho biết bạn là một thợ săn mối đe dọa chuyên nghiệp chuyên

trong DNS và phân tích lưu lượng truy cập web và bạn

sẽ tạo ra một kế hoạch săn lùng mối đe dọa chi tiết cho

miền cụ thể. Và cái này là

chỉ số thực tế mà chúng tôi sắp cung cấp.

Và sau đó bạn thấy ở đây, bạn biết đấy,

điều đó sẽ bao gồm, nó rất cụ thể, bạn

biết, sẽ bao gồm các kỹ thuật thu thập thông tin về miền,

Phương pháp phân tích truy vấn DNS, kiểm tra lưu lượng truy cập web

phương pháp và blah blah, blah, blah. các

rõ ràng hơn rằng bạn đang ở trong

hướng dẫn, đại lý của bạn càng tốt

hoặc quy trình làm việc theo chu kỳ của bạn sẽ như vậy. Phải.

Vậy cái này là cái khác, Đó là một hàm băm

chỉ số. Vì vậy, nó là một thợ săn mối đe dọa chuyên phân tích phần mềm độc hại.

Và sau đó cái này

là một chỉ số hành vi.

Và đây là điều chúng tôi đang làm

là chúng tôi đang làm một mẫu phân loại.

Vì vậy chúng tôi đang phân loại địa chỉ IP,

tập tin miền, hàm băm và mẫu hành vi.

Và bạn thấy phần giữ chỗ chỉ báo ở đây và sau đó nó nói,

bạn biết đấy, chỉ trả lời bằng loại phân loại và không có gì khác.

Sau đó chúng ta thực sự định nghĩa một hàm

để xác định loại chỉ báo chính xác hơn.

Vì vậy chúng ta sẽ xem xét

Địa chỉ IP và sau đó trả về phân loại.

Và đây là nơi mọi thứ trở nên nhiều hơn

thú vị. Đây là nhánh có thể chạy được mà chúng tôi đã giải thích

trước đây. Phải. Vì vậy trong trường hợp này sẽ là

một danh sách các điều kiện mà chúng tôi sẽ xác định.

Và trong trường hợp này, một cho ip, một

dành cho miền Persona, miền còn lại dành cho khắc nghiệt.

Và sau đó chúng tôi thực sự đặt

mọi thứ cùng nhau ở bên dưới. Phải.

Vì vậy, việc đầu tiên thực sự là tạo một bản đồ chạy

phân loại và bảo toàn dữ liệu đầu vào, giống như tôi đã đưa ra nhận xét ở đây.

Một cách khác là tạo ra một hàm về cơ bản

sẽ định tuyến đến nhánh thích hợp dựa trên phân loại.

Vì vậy và cuối cùng về cơ bản chúng tôi

thực sự có những ví dụ, đây là

địa chỉ IP hư cấu, nhưng 45.1.32.192.1.12 và

thì tên miền đáng ngờ là Malwaredomain.net

rồi băm và sau đó

một mô tả về hành vi thay thế.

Và đó là sự thực thi PowerShell

với các lệnh được mã hóa theo sau là các kết nối mạng

được thực hiện với các cổng bất thường trong mạng.

Phải. Vậy một lần nữa tất cả đều được tạo nên

kịch bản hư cấu. Nhưng như bạn có thể thấy

chúng ta có thể đặt các dấu hiệu thỏa hiệp, chạy chúng

phân tích trong một chuỗi chi nhánh, vì vậy trong

một nhánh có thể chạy được, và cuối cùng

in chuỗi săn lùng mối đe dọa

ví dụ và tất nhiên là suy luận.

Vậy hãy xem nào

xảy ra bất cứ khi nào tôi chạy nó.

Cố lên.

Ở đây chúng ta có ví dụ về chuỗi chi nhánh săn lùng mối đe dọa.

Ví dụ này minh họa cách sử dụng chuỗi nhánh để tạo

kế hoạch săn lùng mối đe dọa chuyên biệt. Giống như chúng tôi đã đề cập trước đây,

chỉ báo, chỉ báo đầu tiên là địa chỉ IP.

Và sau đó nó được gửi đến

LLM để thực hiện phân tích.

Và về cơ bản thì trích dẫn đó không được trích dẫn Persona

được tạo ra bởi thợ săn mối đe dọa đã tạo ra một cuộc săn lùng

lên kế hoạch điều tra việc này. Vậy là gì

các nguồn dữ liệu ban đầu để kiểm tra từ tường lửa

ghi nhật ký vào NetFlow và nguồn cấp dữ liệu thông tin về mối đe dọa,

vân vân và sau đó là các hiện vật để xem xét

các chiến lược tương quan mạng với các chiến lược khác

điểm dữ liệu và sau đó truy vấn tìm kiếm cụ thể.

Ngay cả đối với những thứ như Splunk,

nó sẽ cung cấp cho bạn truy vấn Splunk.

Trên thực tế, bạn thực sự có thể làm điều này một cách

theo cách mà chính nhân viên đó thực sự đang thực hiện tất cả các truy vấn này cho bạn.

Và bạn biết đấy, tương tác

với Splunk trong trường hợp này,

và bạn thấy nó

thực sự đang xem xét các tìm kiếm khác nhau.

Và nó cũng có một ví dụ

đối với elaticsearch, bạn thấy ở đây rằng

ví dụ, và một ví dụ khác cho

Bộ bảo vệ Microsoft dành cho Săn bắn nâng cao tại điểm cuối.

Và trong trường hợp này

bạn thấy một ví dụ ở đó

Bây giờ đến chỉ báo khác, một chỉ báo khác

nhánh của chuỗi có thể chạy được, bạn thấy rằng

trong trường hợp này là dành cho tên miền và sau đó

nó đi qua và làm những việc rất giống nhau,

nhưng trong trường hợp này là để thu thập thông tin về miền

kỹ thuật và sau đó phân tích truy vấn cho điều đó

tên miền và thậm chí cả các truy vấn tìm kiếm cụ thể trong

các công cụ và mô phỏng EDR phổ biến như Splunk và

Đàn hồi và vân vân, giống như bạn đã thấy

trước đây. Và tất nhiên nếu bạn tiếp tục

có một chỉ số khác. Trong trường hợp này nó thực sự là

hàm băm hoặc phần mềm độc hại và cái đó thực sự được gọi

hoặc hướng dẫn LLM thực sự sử dụng

mẫu dành cho thợ săn mối đe dọa đó

đặc biệt là tìm kiếm phân tích phần mềm độc hại,

hoặc thực hiện phân tích phần mềm độc hại.

Bạn thấy rất giống

cái trước đó, bao gồm cả cái

Tìm kiếm Splunk và elaticsearch,

Bộ bảo vệ Microsoft, v.v.

Và cuối cùng bạn thực sự thấy

việc đóng cửa đó và các khuyến nghị để ngăn chặn

các hoạt động độc hại và như vậy. Vậy cái này

có thể rất, rất mạnh mẽ bất cứ khi nào bạn thực sự cố gắng

để tự động hóa rất nhiều thứ trong

mạng và sử dụng LLM để thực hiện các nhiệm vụ đó.

Và chuỗi chi nhánh là điều mà nhiều người

tổ chức và nhiều cá nhân thực sự sử dụng bất cứ khi nào

bạn đang tương tác với LLM và đặc biệt

bất cứ khi nào bạn đang cố gắng thực sự tự động hóa một

nhiệm vụ từ đầu đến cuối, giống như

bạn đang nhìn thấy ở đây trên màn hình.

Trong phần tiếp theo, chúng ta sẽ đi qua

chuỗi nhắc song song và một vài ví dụ về điều đó.