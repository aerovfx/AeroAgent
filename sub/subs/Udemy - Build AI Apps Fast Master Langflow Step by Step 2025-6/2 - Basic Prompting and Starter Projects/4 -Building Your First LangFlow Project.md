# 4 -Xây dựng dự án LangFlow đầu tiên của bạn đã được dịch

---

Không, Juve đã thử sức mạnh của Lanclow, tôi muốn bạn hiểu điều gì xảy ra đằng sau

những cảnh khi bạn tạo dự án này có tên là Nhắc cơ bản.

Để đạt được điều này, cách nào tốt hơn là tạo lại từ đầu kịch bản tương tự như nó

sử dụng Juve được tạo ra trước đó?

Chúng ta hãy đi tới thư mục Flows của khóa học, thư mục này sẽ đưa Juve đến bảng điều khiển chính của thư mục này.

Tôi sẽ tạo một quy trình mới để bắt đầu một quy trình làm việc mới, nhưng lần này tôi sẽ chỉ cho bạn

cách bắt đầu xây dựng nó từ đầu, điều này sẽ cung cấp cho bạn một khung vẽ hoàn toàn trống rỗng.

Là một phần của quy trình công việc đầu tiên này, bạn có thể thực hiện một số hành động, chẳng hạn như thay đổi tên của

luồng ở trên cùng, hãy đổi tên thành luồng đầu tiên của tôi.

Khi bạn thay đổi tên, bạn có thể thấy rằng những thay đổi đã được lưu.

Mặt khác, ở bên trái, bạn sẽ tìm thấy một phần có tên Thành phần, trong đó các thành phần khác nhau

các thành phần được phân loại mà bạn có thể thả và thả vào khung vẽ.

Bây giờ, để giúp bạn hiểu rõ hơn nên sử dụng thành phần nào khi tạo luồng trong Lanclow,

Tôi đang đưa bạn đến trang web trò chuyện GPT.

Trong trường hợp cụ thể này, bạn có thể nhận thấy một số thành phần hoặc thành phần mà bạn cần để sử dụng chat GPT.

Đầu tiên là hộp văn bản, nơi bạn có thể nhập thông tin để đặt bất kỳ câu hỏi nào cho mô hình AI.

Khi Juve đặt câu hỏi, Juve có quyền lựa chọn cao nhất là thay đổi mô hình AI.

Trong trường hợp cụ thể này, tôi không có sẵn vì tôi chưa đăng nhập.

Mô hình AI đó sẽ nhận đầu vào, xử lý phù hợp và cuối cùng,

bạn sẽ nhận được một đầu ra. Ví dụ, hãy hỏi George Lucas là ai?

Bạn có một trường đầu vào trong đó truy vấn Juve kiểu Juve, mô hình AI xử lý thông tin này,

và tạo phản hồi hiển thị cho bạn trong cùng một cửa sổ trò chuyện.

Vì vậy, bạn có ba thành phần, đầu vào, bộ phận xử lý và đầu ra.

Đây là điều tôi cần bạn lặp lại ở Lanclow.

Bạn có thể thấy rằng trong các danh mục ở phía bên trái, có một phần được gọi là đầu vào.

Hiện tại có hai loại đầu vào có sẵn trong Lanclow.

Đầu tiên được gọi là đầu vào trò chuyện, cho phép bạn tương tác thông qua cửa sổ trò chuyện,

cũng như cung cấp một trường để nhập văn bản. Hãy thử kiểu nhập văn bản này bằng cách

kéo và thả thành phần. Bây giờ thành phần này đã có sẵn trên khung vẽ của bạn.

Một tùy chọn khác là nhấp vào biểu tượng này và thành phần sẽ tự động được thêm vào quy trình làm việc của bạn.

Điều bạn sẽ nhận thấy khi sử dụng các thành phần khác nhau là mỗi thành phần đều có những tính năng riêng.

Ví dụ: nút này cung cấp mô tả về cách sử dụng và tiện ích của thành phần cụ thể đó.

Ngoài ra, trong trường hợp cụ thể này, bạn có tùy chọn xác định một văn bản cố định mà bạn có thể

sử dụng như một phần của dòng chảy này. Ví dụ, ở đây tôi sẽ viết một hướng dẫn.

Bạn sẽ có thể xem nội dung tin nhắn trong trường hợp quá dài bằng cách nhấp vào biểu tượng này.

Bạn có lời nhắc, hướng dẫn hoặc văn bản bạn đang nhập để trở thành một phần của thành phần này.

Chúng ta có thể nhấp vào hoàn tất chỉnh sửa và thao tác này sẽ đặt văn bản bạn mô tả như một phần của văn bản này

Một tùy chọn khác với mỗi nút là nhấn nút này ở đầu thành phần,

chạy lệnh hiện tại cho nút. Bấm vào đây. Hãy chú ý nếu mọi việc diễn ra tốt đẹp,

sao chép một mô tả hoặc một số thông báo cho biết thao tác đã thành công.

Bây giờ, tôi sẽ chỉ cho bạn đầu ra của thành phần này hoặc cách thực thi nó ở đâu? Thông thường, ở nút

bạn sẽ tìm thấy ở đây một số tùy chọn đầu ra được tạo khi một thành phần được thực thi.

Nếu bạn bấm vào nút này có nội dung kiểm tra đầu ra, bạn có thể thấy rằng đầu ra được tạo ra như một phần

của thành phần xuất hiện ở đây. Về cơ bản nó giống với văn bản bạn đã nhập, vì thành phần này

chỉ cho phép bạn xác định một thông báo để chuyển đến thành phần sau.

Ở trên cùng, bạn sẽ tìm thấy một số nút bổ sung, chẳng hạn như cho phép bạn chỉnh sửa

mã. Nếu bạn đã biết cách viết code bằng Python thì chắc hẳn bạn đã hiểu thành phần này là gì

là dành cho. Bạn có thể điều chỉnh nó theo nhu cầu của bạn. Ở đây, bạn cũng có một hướng dẫn hoặc một nút

được gọi là điều khiển, cho phép bạn phân loại thành phần nếu nó có các tùy chọn bổ sung để

sửa đổi các thông số khác nhau tùy thuộc vào những gì bạn cần. Có điều gì đó quan trọng bạn cần

để biết. Không phải tất cả các thuộc tính của thành phần luôn được hiển thị trong giao diện đồ họa.

Một số chỉ có thể được cấu hình bằng nút này được gọi là điều khiển. Bạn có một lựa chọn khác để đóng băng

thành phần này và bạn cũng thấy một nút cho phép bạn truy cập vào nhiều tùy chọn hơn để lưu thành phần này,

sao chép nó, sao chép nó, thu nhỏ nó và thậm chí bạn có thể thấy nó xuất hiện thay đổi như thế nào khi bạn

giảm thiểu nó. Bạn cũng có thể tải xuống thành phần hoặc xóa nó nếu không cần nữa.

Điều này sẽ tương đương với việc nhập văn bản, như thể bạn đang tự viết tin nhắn trong cuộc trò chuyện

Trò chuyện GPT. Khi bạn có dữ liệu đầu vào, hầu hết bạn sẽ xử lý hướng dẫn này bằng mô hình AI

như trò chuyện GPT làm. Bạn sẽ tìm thấy những mô hình này trong danh mục được gọi là mô hình.

Hãy mở rộng danh mục này và bạn sẽ thấy rằng bạn có một số lượng lớn các nhà cung cấp

có sẵn để sử dụng để xử lý AI. Chúng tôi có một số mô hình nổi tiếng. Ví dụ,

những thứ đó từ Log, Google, Azure, OpenAI và Amazon. Trong trường hợp này, để trình diễn,

Tôi sẽ sử dụng thành phần có tên OpenAI. Hãy kéo và thả nó. Và bạn có thể thấy rằng điều này

thành phần có các tùy chọn cấu hình khác với thành phần trước đó. Mỗi thành phần,

và tôi muốn bạn ghi nhớ điều này, phải được cấu hình theo nhu cầu của bạn cho

xử lý tương ứng. Trong thành phần này, bạn sẽ nhận thấy rằng bạn có thể xác định đầu vào

tương đương với việc viết lời nhắc trong trò chuyện GPT. Tôi cũng có một thông báo hệ thống giúp

hướng dẫn mô hình biết nó sẽ hoạt động như thế nào trong các tình huống nhất định. Bạn cũng có thể chọn một cách khác

làm mẫu nếu bạn muốn. Ví dụ: chọn mô hình GPT cho Omini. Và một lần nữa, ở đây bạn có thể thiết lập

khóa API để đảm bảo mọi thứ hoạt động chính xác. Tôi sẽ chỉ cho bạn cách cấu hình cái này

thành phần. Như bạn có thể nhớ, bạn đã có đầu vào là thành phần mà chúng tôi đã xác định trước đó.

Và chúng tôi muốn mô hình AI phân tích hoặc sử dụng. Trong trường hợp này, tôi phải kết nối cả hai nút để

thành phần OpenAI lấy thông tin từ thành phần được gọi là nhập văn bản. Chúng ta làm điều này như thế nào?

Điều bạn có thể nhận thấy trong các thành phần là mỗi thành phần có các đầu nối hoặc điểm khác nhau.

màu sắc. Ví dụ: thành phần này bao gồm hai nút, mỗi nút có một màu khác nhau.

Điều này là do các đầu nối phải tương thích với nhau để bạn có thể liên kết các thành phần khác nhau.

Một cách dễ dàng để lọc các nút tương thích là chọn một trong các nút bạn muốn tham gia,

sẽ tự động làm nổi bật những cái tương thích. Hãy xem điều gì xảy ra khi tôi nhấp vào nút này.

Bạn có thể thấy rằng trong vùng canvas, tôi đã chọn các nút tương thích. bạn có thể

thấy rằng ba nút này tương thích vì chúng mong đợi một kiểu nhập văn bản. Ở bên trái, bạn có thể

cũng thấy rằng tôi đã chọn một số nút tương thích với nút này. Vì vậy, bạn có thể dễ dàng xác định cái nào

tương thích và kéo chúng vào canvas nếu cần. Một khía cạnh quan trọng khác bạn nên biết

là trong một thành phần bên trái có các nút đầu vào nhận thông tin và trên

bên phải là các nút đầu ra. Ví dụ: trong thành phần có tên OpenAI, bạn có ba nút đầu vào

và ở bên phải hai nút đầu ra. Bây giờ, bạn có nhớ rằng khi chạy nút đầu tiên,

bạn đã nhận được văn bản bạn viết dưới dạng đầu ra là thành phần này. Đầu ra này là những gì tôi muốn bạn sử dụng

làm đầu vào trong thành phần OpenAI. Vì vậy, mô hình AI có thể xử lý nó. Vì vậy, kết nối đầu ra của

thành phần đầu vào của thành phần OpenAI trong thuộc tính được gọi là đầu vào. Hãy để ý cách này,

Tôi chặn những hộp văn bản này. Bạn không còn có thể gõ bất cứ điều gì vào hộp bởi vì bây giờ văn bản từ

thành phần nhập văn bản được tự động lấy làm đầu vào cho thuộc tính này được gọi là massage kể từ nút

được kết nối. Nếu bạn chọn kết nối giữa cả hai thành phần và nhấn phím xóa,

bạn sẽ thấy rằng mối quan hệ bạn đã tạo trước đó sẽ tự động bị xóa và bạn có thể viết

text lại như một phần của thuộc tính này. Hãy kết nối hai thành phần. Đối với tài sản được gọi là

massage hệ thống, bạn có thể tạo một nút khác thuộc loại kiểu nhập văn bản để kết nối nó với thuộc tính này hoặc

viết văn bản bạn muốn trực tiếp trong hệ thống massage. Bây giờ, chúng ta sẽ để nó như thế này.

Chúng tôi đã chọn mô hình mà chúng tôi muốn sử dụng. Bây giờ mình sẽ nhập API key cho thành phần này.

Khi bạn đã thực hiện xong việc này, đã định cấu hình nút này, hãy nhấn nút để chạy thành phần.

Bạn có thể thấy rằng thành phần đầu tiên thực thi rất nhanh vì nó xử lý thông tin

rằng thành phần OpenAI này không thể xử lý văn bản đầu vào nếu không nhận được kết quả trước

từ thành phần trước đó vì nó cần đợi nó thực thi để có được

thông tin sẽ đóng vai trò là đầu vào cho thành phần tiếp theo. Bạn có thể thấy rằng bạn không có

bất kỳ vấn đề nào khi chạy thành phần. Bạn có thể xem lần thực thi cuối cùng, thời lượng của nó và các chi tiết khác.

Nếu bạn muốn xem phản hồi từ thành phần này là gì, bạn có thể vào phần có nội dung

kiểm tra các kết quả đầu ra. Trong trường hợp cụ thể này, bạn có hai kết quả đầu ra và chúng tôi sẽ chỉ cho bạn những gì

đầu ra massage là. Bạn có thể thấy rằng chúng tôi đã có phản hồi trò chuyện kiểu GPT hiển thị cho bạn

thông tin về đầu vào. Bạn cũng có một phần khóa nơi bạn có thể kiểm tra bất kỳ lỗi nào

hãy đóng cái này lại. Bạn cũng có đầu ra thứ hai tương ứng với đầu ra của mô hình bạn

được sử dụng trước đây. Bạn sẽ nhận thấy rằng đầu ra trong trường hợp này khác với đầu ra trong trường hợp massage

bởi vì ở đây bạn nhận được tất cả thông tin chính xác từ phản hồi của mô hình AI, chẳng hạn như bạn đang sử dụng mô hình nào

đã sử dụng nhiệt độ. Ví dụ ở đây bạn có ghế tôi đã sử dụng và các thông tin liên quan khác

mà bạn có thể cần vào một lúc nào đó. Chúng tôi biết thành phần này như thế nào, tuy nhiên, lý tưởng nhất là bạn có thể

để hiển thị phản hồi mà bạn quan tâm vì hiện tại bạn có hai phản hồi.

Đối với điều này, trong phần thành phần bên dưới khu vực đầu ra, bạn cũng sẽ tìm thấy một thành phần có tên

đầu ra văn bản mà bạn sẽ kéo và thả. Điều này chỉ có một thuộc tính được gọi là văn bản. Ví dụ,

thuộc tính massage này tương thích với đầu ra văn bản. Vì vậy, tôi chỉ cần kéo và thả nó.

Bạn có thể thấy rằng thành phần này cho biết rằng nó sẽ hiển thị đầu ra văn bản trong

sân chơi. Vì vậy, với điều này, bạn có thể bắt đầu thực thi thành phần này. Đến đây quá trình xử lý đã xong.

Bạn có thể thấy ở đây nó đang tạo ra phản hồi để gửi nó đến nút đầu ra. Ở đây chúng tôi có

chỉ có một đầu ra là massage bạn có thể nhìn thấy trên màn hình. Nó rất giống với những gì chúng tôi

được tạo tại thời điểm trước đó, nhưng bây giờ nó được hiển thị như một phần của thành phần cụ thể này cho

phát đầu ra văn bản.