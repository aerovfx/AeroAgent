# Chương 10. Tóm tắt Học tập tăng cường sâu trong thực tế, Phiên bản video đã được dịch

---

Bản tóm tắt.

Mạng thần kinh đồ thị là các mô hình học máy hoạt động trên dữ liệu có cấu trúc đồ thị.

Đồ thị là cấu trúc dữ liệu bao gồm một tập hợp các đối tượng, được gọi là nút và mối quan hệ giữa các đối tượng, được gọi là các cạnh.

Loại biểu đồ tự nhiên là một mạng xã hội trong đó các nút là các cá nhân và các cạnh giữa các nút thể hiện tình bạn.

Ma trận kề là ma trận có kích thước, dấu hai chấm n lần n, trong đó n là số nút trong biểu đồ mã hóa khả năng kết nối giữa mỗi cặp nút.

Truyền tin nhắn là một thuật toán để tính toán các bản cập nhật cho các tính năng của nút bằng cách tổng hợp lặp đi lặp lại thông tin từ các nút lân cận của nút.

Thành kiến ​​quy nạp là thông tin trước đó chúng ta có về một số tập hợp dữ liệu.

Chúng tôi sử dụng chúng để hạn chế mô hình của mình hướng tới việc học các loại mẫu nhất định.

Ví dụ, trong chương này, chúng tôi sử dụng khuynh hướng quy nạp quan hệ.

Chúng ta nói một hàm, f, là bất biến đối với một số phép biến đổi, g, khi đầu ra của hàm không thay đổi, khi đầu vào được biến đổi lần đầu tiên bởi g dấu hai chấm f của g của x bằng f của x.

Chúng ta nói một hàm, f, tương đương với một phép biến đổi nào đó, g, khi áp dụng phép biến đổi cho đầu vào, cũng giống như việc áp dụng phép biến đổi cho đầu ra, f của g của x bằng g của f của x.

Các mô hình chú ý được thiết kế để tăng khả năng diễn giải và hiệu suất của các mô hình học máy bằng cách buộc mô hình chỉ xem xét, chú ý đến một tập hợp con của dữ liệu đầu vào.

Bằng cách kiểm tra những gì mô hình học để chú ý đến, chúng ta có thể hiểu rõ hơn về cách nó đưa ra quyết định.

Mô hình tự chú ý, mô hình hóa sự chú ý giữa các đối tượng hoặc nút, trong một đầu vào thay vì chỉ mô hình chú ý đến các phần khác nhau của đầu vào.

Điều này đương nhiên dẫn đến một dạng mạng lưới thần kinh đồ thị, vì trọng số chú ý có thể được hiểu là các cạnh giữa các nút.

Khả năng tự chú ý của nhiều đầu cho phép mô hình có các cơ chế chú ý độc lập, mỗi đầu có thể tham gia vào một tập hợp con khác nhau của dữ liệu đầu vào.

Điều này cho phép chúng tôi vẫn nhận được trọng số chú ý có thể hiểu được nhưng làm tăng băng thông thông tin có thể truyền qua mô hình.

Lý luận quan hệ là một hình thức lý luận dựa trên các đối tượng và mối quan hệ giữa các đối tượng, thay vì sử dụng các hệ quy chiếu tuyệt đối.

Ví dụ: một cuốn sách ở trên cùng, liên hệ cuốn sách với cái bàn, thay vì nói cuốn sách ở vị trí 10 và cái bàn ở vị trí 8, một khung tham chiếu tuyệt đối.

Inner.product là tích giữa hai vectơ dẫn đến một giá trị vô hướng duy nhất.

Tích ngoài là tích giữa hai vectơ tạo thành ma trận.

Ký hiệu Einstein hay Einstein, chúng ta hãy mô tả các tích tensor tổng quát được gọi là phép rút gọn tensor bằng cú pháp đơn giản dựa trên việc dán nhãn các chỉ số của tensor.

Học tập Double-Q ổn định quá trình đào tạo bằng cách tách biệt việc lựa chọn hành động và cập nhật giá trị hành động.