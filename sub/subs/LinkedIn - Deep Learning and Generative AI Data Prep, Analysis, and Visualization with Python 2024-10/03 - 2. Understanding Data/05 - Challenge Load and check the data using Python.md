# 05 - Thử thách Load và kiểm tra dữ liệu bằng Python

---

(nhạc sôi động)

- [Người tường thuật] Trong thử thách này,

chúng tôi thực hiện kiểm tra dữ liệu ban đầu trên tập dữ liệu viễn thông.

Các tập tin khóa học cho các bộ dữ liệu

và Notebook Jupyter được đặt

trong kho lưu trữ GitHub của khóa học.

Đầu tiên, hãy tìm Phòng thí nghiệm thử thách trong GitHub,

và đối với bài tập này, đó sẽ là 02_05_begin.ipynb,

và đó là phần mở rộng cho Jupyter Notebook.

Sau đó tải sổ ghi chép thí nghiệm xuống máy tính xách tay của bạn.

Sau đó truy cập Google Colab

và tải sổ ghi chép thí nghiệm thử thách từ máy tính xách tay của bạn lên.

Hình ảnh này hiển thị phòng thí nghiệm thử thách đã được tải lên.

Các tập tin khóa học cho các bộ dữ liệu

cũng có trong khóa học GitHub repo.

Nhấp vào tệp Telecom_dataset.csv

và nó sẽ đưa bạn đến đầu ra mà bạn nhìn thấy ở bên phải.

Để tải tập tin xuống,

nhấp vào mũi tên xuống ở góc trên bên phải.

Hình ảnh này minh họa cách tải tập dữ liệu lên Colab.

Đầu tiên bạn click vào biểu tượng upload file.

Một cửa sổ tải xuống từ máy tính xách tay của bạn sẽ mở ra,

sau đó bạn chọn tệp Telecomdata.csv.

Nó sẽ xuất hiện, nhưng nếu không, hãy làm mới trình duyệt của bạn.

Trong thử thách này, có ba bước để kiểm tra dữ liệu,

nhập các thư viện cần thiết, tải dữ liệu,

và thực hiện thăm dò ban đầu.

Đây là mục lục của Jupyter Notebook.

Xem lại nó và lưu ý thứ tự của các mục.

Trong bài tập số một, bạn nhập bốn thư viện Python,

Gấu trúc, NumPy, matplotlib và Seaborn.

Trong bài tập số hai,

bạn tải tập dữ liệu viễn thông vào khung dữ liệu Pandas.

Dưới đây là các nhiệm vụ kiểm tra dữ liệu.

Đầu tiên, bạn hiển thị năm hàng đầu tiên của khung dữ liệu.

Sau đó, bạn hiển thị năm hàng cuối cùng của khung dữ liệu.

Bạn hiển thị thông tin về khung dữ liệu,

bạn hiển thị khung dữ liệu, hình dạng, kiểu dữ liệu.

Bạn hiển thị số liệu thống kê.

Bạn kiểm tra các giá trị trùng lặp, bạn kiểm tra không có giá trị nào,

bạn kiểm tra các giá trị duy nhất và bạn kiểm tra các giá trị ngoại lệ.

Nhập mã vào ô

để biết các bước cần thiết để kiểm tra dữ liệu của bạn.

Đây là một ví dụ về một ô mã đã hoàn thành.

Sử dụng danh sách này như một tài liệu tham khảo.

Ngoài ra, hãy tham khảo sổ ghi chép giải pháp để biết các đề xuất về mã.

Vì vậy, hãy tiếp tục và thử điều này và khi bạn đã sẵn sàng,

hãy cùng tôi xem video tiếp theo để xem giải pháp nhé.