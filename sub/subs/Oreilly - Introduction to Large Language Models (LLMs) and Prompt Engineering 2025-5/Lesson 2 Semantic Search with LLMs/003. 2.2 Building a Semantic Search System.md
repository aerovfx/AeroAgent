# 003. 2.2 Xây dựng hệ thống tìm kiếm ngữ nghĩa

---

Vì vậy, hãy đi vào phần của chúng tôi

ví dụ mã hóa đầu tiên,

đó sẽ là một

tăng cường truy xuất ngữ nghĩa

thế hệ bằng cách sử dụng các mô hình ngôn ngữ lớn.

Và đây sẽ là hệ thống của chúng tôi tại

tầm nhìn 10.000 foot. Chỉ để xem nó.

Trước khi bắt đầu viết mã, chúng ta sẽ bắt đầu

ở trên cùng, nơi khi chúng tôi thêm tài liệu,

số một, chúng ta sẽ đi

để nhúng tài liệu đó

và có khả năng cắt chúng nếu chúng ta phải làm vậy.

Nhưng cuối cùng chúng tôi lấy từng phần hoặc

tài liệu, nhúng chúng bằng cách sử dụng một số hệ thống,

ví dụ: OpenAI và

sau đó chúng ta sẽ lưu trữ nó

vector hoặc tất cả những thứ đó

vectơ vào một số cơ sở dữ liệu.

Trong trường hợp của chúng tôi, chúng tôi sẽ

sử dụng cơ sở dữ liệu tên là Pinecone,

có bậc miễn phí

truy cập. Nó không phải là nguồn mở.

Có những lựa chọn thay thế nguồn mở,

nhưng quả thông thì đẹp

một cách dễ dàng để thiết lập,

đặc biệt là để tạo mẫu

và các loại dự án sở thích.

Nhưng nó sẽ tăng quy mô nếu bạn muốn trả

tiền. Nó cũng có quy mô sản xuất.

Chúng tôi sẽ lưu trữ các vectơ đó trong Pinecone để

được lấy lại sau ở nửa dưới.

Bởi vì ở nửa dưới,

khi ai đó hỏi một câu hỏi như

làm cách nào để thực hiện đặt lại mật khẩu?

Và có lẽ

tài liệu mà chúng tôi đã nhúng

là từ khách hàng của bạn

hỗ trợ nền tảng kiến thức.

Vâng, điều đó sẽ xảy ra

một số API, có thể là API nhanh,

hoặc có thể chúng ta sẽ làm điều đó chỉ bằng Python,

nhưng nó sẽ tấn công hệ thống nào đó,

Vì vậy, nếu đó là 1536

vectơ đầu ra từ OpenAI,

nó sẽ là một vectơ khác của

kích thước 1536 đại diện cho truy vấn.

Sau đó chúng ta sẽ lấy nó

vector từ OpenAI và so sánh

nó chống lại tất cả

khối tài liệu trong Pinecone,

cơ sở dữ liệu vector của chúng tôi.

Nó sẽ lấy lại

kết quả hàng đầu dựa trên sự tương đồng về ngữ nghĩa,

trả lại chúng cho người dùng,

có khả năng xếp hạng chúng bằng cách sử dụng bộ mã hóa chéo.

Đó là số bốn ở dưới đó.

Một lần nữa, đó là tùy chọn về mặt kỹ thuật,

nhưng việc xếp hạng lại sẽ thường xuyên

dẫn đến danh sách cuối cùng chính xác hơn

tài liệu và sau đó chúng tôi trở lại

những tài liệu đó trở lại cho người dùng.

Vì vậy có một vài bộ phận chuyển động,

nhưng vào cuối

ngày tất cả những gì chúng tôi đang làm là

tham gia nhúng

động cơ trong ví dụ này,

OpenAI, nhưng chúng ta sẽ thấy nó

cũng được thực hiện với Nguồn mở.

Vì vậy, chúng ta đang ở trong mã của mình. Bây giờ,

trong phần còn lại của bài học,

chúng tôi sẽ sử dụng mã Python trong

sổ ghi chép jupyter, giống như bạn thấy ở đây.

Và tôi sẽ cố gắng hết sức để

đi qua nhiều nhất có thể,

Tôi đã bao gồm các ý kiến ​​ở đâu

cần thiết. Nhưng hãy tiếp tục và bắt đầu.

Vì vậy, bây giờ chúng tôi chỉ muốn thiết lập một

hệ thống tìm kiếm ngữ nghĩa thực sự cơ bản.

Và trong bài học tiếp theo chúng ta sẽ

để cải thiện hệ thống đó bằng cách thêm

trong một số xếp hạng RE tùy chọn và

xem xét một số trình nhúng nguồn mở.

Nhưng bây giờ, hãy bắt đầu cơ bản.

Vì vậy, chúng tôi sẽ có một số hàng nhập khẩu ở đây.

Tôi gần như có tất cả

của hàng nhập khẩu mà chúng tôi đang

sẽ cần cho

vài bài học tiếp theo ở đây,

để tôi có thể đi qua chúng. Chúng tôi

sẽ nhập OpenAI.

Vì vậy, trình nhúng đầu tiên của chúng tôi

chúng ta sẽ xem xét là

sẽ là nguồn đóng

nhúng các mô hình từ OpenAI.

Và sau đó như tôi đã nói, chúng ta sẽ thêm vào

một số trình nhúng mã nguồn và mở sau này

bài học cũng như một số cơ bản khác

Các gói Python ở đây như DateTime,

hashlib, biểu thức chính quy.

Chúng tôi muốn có thể đọc được một số

Khóa API từ hệ thống của chúng tôi. Vì vậy, ôi,

lần nhập khẩu thứ sáu này ở đây là một cái gì đó

mà chúng ta sẽ sử dụng trong bài học tiếp theo.

Nhưng lớp mã hóa chéo này

sẽ là lớp học đó

chúng ta sẽ sử dụng để

khởi tạo hệ thống xếp hạng RE của chúng tôi.

TQDM cho một số thanh tiến trình, NumPy,

PyTorch, ghi nhật ký để ghi nhật ký và sau đó là Pinecone.

Như tôi đã đề cập trước đây, Pinecone

sẽ là cơ sở dữ liệu vector của chúng tôi. Ngoài ra,

như tôi đã nói trước đó, chúng tôi

không cần phải sử dụng Pinecone.

Tôi thích nó vì nó thực sự dễ dàng

thiết lập với. Ít nhất là cho đến hôm nay,

vẫn còn phiên bản miễn phí

Quả thông bạn có thể sử dụng ở mức giới hạn.

Và nếu bạn muốn đi vào

sản xuất, nó có thể mở rộng quy mô khá dễ dàng từ đó.

Nhưng tất nhiên bạn có thể sử dụng bất kỳ

cơ sở dữ liệu vector mà bạn muốn.

Vì vậy, hãy bắt đầu. Vì vậy chúng tôi sẽ

bắt đầu bằng cách khởi tạo các khóa API của chúng tôi.

Vì vậy, ở đây tôi sẽ lưu trữ

khóa Pinecone của chúng ta thành một biến.

Và ở đây tôi sẽ thiết lập

nâng cấp ứng dụng khách OpenAI của chúng tôi. Bây giờ,

khi chúng tôi sử dụng OpenAI trong

bài học sắp tới,

chúng ta sẽ thấy dòng này

mã được sao chép rất nhiều.

Vì vậy, để bắt đầu với gói OpenAI của chúng tôi,

chúng tôi sẽ khởi tạo những gì chúng tôi

gọi điện cho khách hàng cho Pinecone.

Chúng ta sẽ cần xác định một

vài biến ở đây. Vì vậy, về cơ bản,

thay vào đó biến cơ bản nhất của chúng tôi ở đây là

tên của chỉ mục hoặc cơ sở dữ liệu của chúng tôi.

Bây giờ ở đây tôi chỉ

gọi nó là bài kiểm tra tìm kiếm ngữ nghĩa,

nhưng tất nhiên bạn có thể gọi

đây là bất cứ điều gì bạn muốn. Bây giờ,

một điều tùy chọn bạn có thể đặt

là một không gian tên. Bây giờ theo mặc định,

không gian tên là

mặc định. Nhưng điều đó đang được nói,

nếu bạn muốn một cách logic

tách một số khác nhau

cơ sở dữ liệu mà bạn có

trong một chỉ mục duy nhất,

bạn có thể làm như vậy với

không gian tên. Chúng tôi sẽ không làm điều đó,

nhưng chỉ để cho bạn biết, bây giờ công cụ của chúng tôi

sẽ là mô hình nhúng của chúng tôi. Bây giờ,

về mặt kỹ thuật thì động cơ sẽ được

một biến cho ứng dụng khách OpenAI của chúng tôi.

Và trong trường hợp này tôi có

được xác định, ít nhất là vào hôm nay,

Mô hình nhúng gần đây nhất của OpenAI

và mô hình nhúng lớn nhất gần đây nhất,

Tôi có thể thêm vào, cái được gọi là Văn bản

Nhúng 3 lớn. Ngoài ra, tính đến hôm nay,

có ba cách nhúng khác nhau

các mô hình để sử dụng từ OpenAI. Đây là lớn nhất,

gần đây nhất và theo OpenAI,

mô hình nhúng mạnh mẽ nhất mà họ có.

Vì vậy, chúng tôi sẽ đặt điều đó vào

kiểm tra. Giống như ứng dụng khách OpenAI của chúng tôi,

cuối cùng chúng ta sẽ khởi tạo một

ứng dụng khách Pinecone sử dụng khóa API của chúng tôi,

giống như chúng tôi đã làm trước đây. Được rồi,

mọi thứ nên được thiết lập ngay bây giờ.

Từ bây giờ chúng tôi muốn thiết lập

một số chức năng trợ giúp chỉ để

kiểu như làm cho mã trở thành một

sạch sẽ hơn một chút trên đường đi.

Vậy điều đầu tiên là

chúng ta sẽ tạo ra

một chức năng để lấy các phần nhúng từ OpenAI.

Thực ra nó chỉ là một cái bọc

dựa trên một lệnh gọi OpenAI khác.

Vì vậy, chúng tôi sẽ có được các phần nhúng và nhận được

embeddingsố ít như tên cho thấy.

Chức năng đầu tiên sẽ

lấy một lần lặp của

văn bản thô và xuất ra một

có thể lặp lại các vectơ.

Và cái thứ hai của chúng tôi chỉ là

sẽ tham gia một lần

đoạn văn bản và xuất ra một vectơ duy nhất.

Vì vậy, chúng ta có thể nhúng từ xin chào và chúng ta có thể

nhận phần nhúng cho các cụm từ xin chào và xin chào.

Và đối với cái đầu tiên của chúng tôi,

chiều dài của phần nhúng và điều này

sẽ rất quan trọng đối với

khối mã tiếp theo là 3072.

Hiện nay, các phiên bản trước của

Các phần nhúng OpenAI có kích thước 1536.

Và nói một cách, nó không

thực sự quan trọng là những vectơ này dài bao nhiêu.

Một số người có thể nói rằng càng lớn

các vectơ thì chúng sẽ càng tốt.

Vì vậy, chúng ta đang ở trong mã của mình. Bây giờ, đối với

remainder of our lessons, we will be using Python code in a jupyter notebook, just like you see here. And I will do my best to walk through as Much as I can, I have included comments where necessary. But let's go ahead and get started. So for now we just want to set up a really basic semantic search system. And in our next lesson we're going to enhance on that system by adding in some optional RE ranking and looking at some open source embedders. But for now, let's start basic. So we'll have some imports here. I pretty much have all of the imports that we're going to need for the next few lessons here, so I can walk through them. We are going to be importing OpenAI. So our first embedder that we're going to be looking at is going to be the closed source embedding models from OpenAI. And then like I said, we'll add on some open and source embedders in a later lesson as well some other basic Python packages here like DateTime, hashlib, regular expressions. We want to be able to read in some API keys from our system. So os, this sixth import here is something that we're going to use next lesson. But this cross encoder class is going to be the class that we're going to use to instantiate our RE ranking system. TQDM for some progress bars, NumPy, PyTorch, logging for logging and then Pinecone. So as I mentioned previously, Pinecone is going to be our vector database. Also, as I said earlier, we don't have to use Pinecone. I like it because it's really easy to get set up with. As of today at least, there is still a free version of Pinecone you can use to a limit. And if you do want to go into production, it scales quite easily from there. But of course you can use any vector database that you would like. So let's get started. So we'll start by instantiating our API keys. So here I'm just going to store our Pinecone key into a variable. And here I'm going to set up our OpenAI client. Now, as we use OpenAI in our lessons going forward, we will see this line of code replicated a lot. So to get started with our OpenAI package, we will instantiate what we call a client for Pinecone. We're going to need to define a few variables here. So basically, rather our most basic variable here is the name of our index or our database. Now for here I've just called it semanticsearch test, but of course you can call this whatever you'd like. Now, an optional thing you can set is a namespace. Now by default, the namespace is default. But that being said, if you would like to logically separate some of the different databases that you have within a single index, you may do so with the namespace. We are not going to do that, but just to let you know, now our engine is going to be our embedding model. Now, engine is technically going to be a variable for our OpenAI client. And in this case I have defined, as of Today at least, OpenAI's most recent embedding model and largest most recent embedding model, I might add, which is called Text Embedding 3 Large. Also, as of today, there are three different embedding models to use from OpenAI. This is the largest, most recent, and according to OpenAI, most powerful embedding model they have. So we'll put that to the test. Like our OpenAI client, we will finally then instantiate a pinecone client using our API key, just like we did before. Okay, everything should be set up now. From now we want to set up some helper functions just to kind of make the code a little bit cleaner along the way. So the first thing is we're going to create a function to grab embeddings from OpenAI. So really it's just a wrapper on top of another OpenAI call. So we'll have get embeddings and get embeddingsingular as the name suggests. The first function will take in an iterable of raw text and output an iterable of vectors. And our second one is just going to take in a single piece of text and output a single vector. So we can embed the word hi and we can get embeddings for the phrases hi and hello. And for our first one, the length of the embedding and this is going to be important for the next code block is 3072. Now, the previous versions of OpenAI embeddings had size 1536. And to put it one way, it doesn't really matter how long these vectors are. Some people might say that the larger the vectors, the better they will be. This is simply not true. The quality of the embeddings is always going to boil down to a testing data set is can you actually measure and quantify how effective the vectors are at performing a certain task? The reason these embeddings can get quite large, especially when it comes to closed source embedding models like OpenAI, is that they need to be ready to handle all different types of tasks all over the world. Some for clustering, some for semantic search like we're doing today, some for question and answering. They don't know they OpenAI. They don't know what people are going to necessarily use these embeddings for. So they need to be able to capture many different types of meanings. So that being said, we now know the size of our embeddings 3072. With that information, we can then create our index or our database in Pinecone. To do this, we'll do a simple check to see if you haven't already made that index. And I as of running this code, had not made it yet. It will create an index using the Pinecone Create index with the name that we set the dimensions that need to be defined up front. So 3072 in our case, this is the dimension that we have to keep from now on, the metric that we want to use for similarity. In our case we're going to be using cosine similarity. Now this is not the only option, but cosine similarity is by far the most popular option because well, a it's scale invariant, meaning the size, the magnitude of the vectors will not affect the cosine between those angles, which can be quite handy. And also it's a more efficient calculation to make, especially when all of the vectors are of unit length of length one. Now, technically none of that really matters for what we're doing right here, right now. What is important to know is that cosine similarity is the most generic, but also one of the most powerful similarity metrics when comparing vectors. So we'll use cosine similarity. The last part here is just specifying what kind of server I want to use. So I'm going to be using aws servers in US West 2 region. Now of course you are free to change this depending on where you are, where you want your servers to be. But this is where I've done it. Grab our index. Great. When you first instantiate an index, you might want to use the describe index stats. This is part of Pinecone's package just to tell you what's going on in that index. So here it tells us our dimensionality of 3072, our fullness, which is going to be a metric of how full that index is. The namespaces, which by default there are none. Well technically speaking there is a single namespace called default. There are no non default namespaces is what the index is telling us. And we have zero vectors inside, which makes sense. We just made it our next set of helper Functions is going to be about just getting data ready for Pinecone. And the first thing we want to do is make a hash function. So this is just a very simple hash using hashlib, because we want to take raw text and use that and turn it into some sort of unique identifier. Because Pinecone is going to use some ID to deduplicate text. So if you upload the same piece of text twice, we would want Pinecone to be able to reject that second piece of text and say, hey, I already have this stored. So as a simple way to create IDs out of text, I'll use a hash function. Our next function, Prepare for Pinecone, is going to be a wrapper that takes in an iterable of raw text. The engine that we want to use for OpenAI and I leave this as a optional parameter because by default we'll use the three large embedding. But of course you can use this to use the ETA 2, which is still technically available, or the three small, which is of size 1536. But either way, whatever engine you choose, it's going to grab the date time as it is at the time of running this function. It will grab the embeddings using our helper function. It will then return this list. Now, this list might seem arbitrary, but this is the list that Pinecone is expecting when uploading text. So it wants an ID for each string. In our case, that's gonna be a hash of the text. It wants the actual embedding, which we created up here. And then it also optionally will take in a dictionary of metadata. Now, optionally, metadata can be blank, of course, but for us we're gonna include two important pieces of text or data, I should say. One is the actual raw text of that that the vector was created from. Because eventually we are going to need to actually read the raw text from the database. Now, if you want to make this even more performant, you could store the text on a local database on your own time, on your own dime, on your own premises. But we will make use of Pinecone's options here and grab the raw text from them. Also optionally is I usually like to record when that text was actually uploaded. This is mostly because in real production use cases, ideally you're going to want to be able to update text. Let's say if it's a knowledge base article, you might want to be able to update that piece of text. And knowing when you last uploaded it will help. So in general, I like to keep the actual date of upload in the database as well. So with this list, we are now actually ready to upload data to our Pinecone instance. So if I had a really simple iterable of text here, let's call it just a list with the string hi in it. If I pass that text through our prepare

Đây là một tìm kiếm ngữ nghĩa

hệ thống. Chúng tôi đã sử dụng LLM,

trong trường hợp này là sự nhúng của OpenAI

mô hình, bên dưới đó là LLM.

Chúng tôi đã sử dụng OpenAI

nhúng sản phẩm vào chỉ mục

978 tài liệu và chúng tôi

vừa mới thử nghiệm

chỉ trong một lần kiểm tra rằng

đưa ra một truy vấn chúng tôi

có thể lấy tài liệu từ Pinecone.

Và ít nhất trong trường hợp này,

chúng tôi có thể truy xuất đúng ngữ cảnh.

Trong bài học tiếp theo, chúng ta

đang định tìm kiếm

ở một cái nhìn toàn diện hơn về việc có hay không

nguồn ngữ nghĩa

hệ thống đang hoạt động tốt

bằng cách kiểm tra không chỉ một trong các ví dụ,

nhưng tất cả những

ví dụ trong bộ thử nghiệm của chúng tôi.

Và chúng tôi sẽ tiếp tục thực hiện

những điều thú vị hơn bằng cách thêm

trong RE tùy chọn đó

bảng xếp hạng và trình nhúng mã nguồn mở.

Nhưng bây giờ, hãy tận hưởng

hệ thống tìm kiếm ngữ nghĩa.