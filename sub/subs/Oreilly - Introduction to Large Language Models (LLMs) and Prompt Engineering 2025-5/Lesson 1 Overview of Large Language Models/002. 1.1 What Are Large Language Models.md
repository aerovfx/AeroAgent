# 002. 1.1 Mô hình ngôn ngữ lớn là gì

---

Bạn ơi, chúng ta sẽ bắt đầu ở nơi mà hầu hết

mọi thứ bắt đầu. Lúc đầu thì không hẳn,

nhưng chỉ cần lùi lại đủ xa để bạn có thể

có được bối cảnh cho vị trí của chúng ta ngày hôm nay.

Hãy bắt đầu vào năm 2001,

nơi lần đầu tiên được phổ biến

học sâu cho

mô hình hóa ngôn ngữ ra đời.

Vào khoảng thời gian này, các nhà nghiên cứu và

các học viên đều đang bắt đầu

để hiểu rằng sự xuất hiện

kết hợp các mô hình học sâu,

đã tương đối phổ biến

hồi đó, gần đây tôi nên nói,

và mô hình hóa ngôn ngữ, thực ra là

một thuật ngữ chúng ta sẽ sớm định nghĩa,

Bạn ơi, chúng ta sẽ bắt đầu ở nơi mà hầu hết

things begin. Not quite at the beginning, but just far back enough for you to get the context for where we are today. Let's start in 2001, where the first popularized deep learning for language modeling comes about. Around this time, researchers and practitioners alike are starting to understand that an emergence of combining deep learning models, which were relatively popularized back then, recently I should say, and language modeling, which is actually a term we'll be defining quite shortly, go pretty well together. That really jumpstarts a new era in modern natural language processing. The next big step function, I would argue, is in around 2013 where we start to see the rise of a new process called Word2Vec. And there are branches of Word2Vec as well around that time, but it represents one of the first, not the first, but one of the most popular methods of encoding raw text into machine readable numbers or vectors. 2013 also starts to showcase a rise in research popularity around showcasing what people are doing and can do and maybe shouldn't be doing with natural language processing with a combination of deep learning and and Word2Vec at their disposal. This is also around the time we start to see people talking again, not for the first time, but more and more about biases and ethical fairness of language modeling as it relates to deep learning. The next step function comes pretty short after. Between 2014 and 2017, we start to see a rise in popularity of a specific kind of deep learning architecture as it relates to natural language processing. This is referred to as a sequence to sequence model, which is not new necessarily for this time. However, around 2017, in the latter half of that era, we start to see the use of a new form of mathematical mechanism called attention being applied to language models that are deep learning models which are sequence to sequence. I know that was a bit of a mouthful, but the real takeaway here is that between 2014 and 2017, researchers and again practitioners alike are starting to recognize the power of a certain mathematical mechanism, attention, especially as it relates to natural language processing. Which leads us to 2017, which is our major step function that really this whole video and everything that we'll be talking about is based on. This is the introduction of a new deep learning architecture called the Transformer. The paper in 2017 was actually called Attention Is all youl Need. So the title of the paper wasn't even Transformer, it was about the attention mechanism. And it postulates that a deep learning mechanism may only require massive amounts, relatively speaking of attention mechanisms, as opposed to what former state of the art, models like RNNs and LSTMs were which. Which did not use attention unless you specifically wanted to add them to it to enhance their power. So 2017, we start to see this new architecture coming about called the transformer. And the transformer with an image here is a sequence to sequence model, which means it takes in a sequence of data and outputs a sequence of data. Now, I'm being pretty vague there, but in our terms, a sequence of data will almost always. Almost always be tokens or subunits of words and phrases thrown in for natural language processing purposes. So in our case, the transformer for the most part will be taking in tokens or sequences of tokens and outputting sequences of tokens. Now, this transformer model ends up being the parent of of most large language models that we know today, like GPT or Bert or T5 and many more, which we will see throughout the course of our video series. I promised you a definition of language modeling and I'm going to give you one. But first, an example. If I were to show you this phrase, if you don't blank at the sign, you will get a ticket. Your brain's probably already done the math or the natural language processing here, but if I ask you to fill in the blank, you might have a few options. Now, I'm going to guess that most of you probably thought stop. If you don't stop at the sign, you will get a ticket. I'll also bet that some of you, if not the majority of you, also thought to yourself, well, it could be stop. It's probably stop. But that's not the only word that would fit. I could say yield, and it would technically still be grammatically and more importantly, semantically correct. The sentence would still make sense if you said yield. It's just not as common of a term as stopping at a sign, but both technically work. Congratulations, you're a language model, because in a language modeling task, a model is trained to do a pretty basic task, again, relatively speaking, and that task is to predict a missing word in a sequence of words. I'll be honest here. Throughout our time together, I'll probably be using words like word and tokens, sometimes interchangeably towards the beginning once we get to our code segments. However, I'm going to be extremely deliberate in how I use those terms because they are not the same. A word may be a token, but a token is not necessarily a word. And we will see examples of that in, you know, we'll see a bunch of examples of those, but a lot of Words that we think of today don't really exist in the vocabulary of large language models. And they, the large language models have to think about them or process them in units or subunits, I should say. But for now, a language modeling task is predicting missing words from a sequence of words. And in general, there are two types of language models, autoregressive and autoencoding. These terms pop up all the time, not just in research, but in practitioners areas and repositories like hugging face, which we will see plenty of throughout our time together. Now, the difference between them is actually pretty simple, but they have profound effects on how the language models work and what they can actually do for you. When it comes to our code, an autoregressive model is still a language model. It's still predicting a feature token or word. But the difference is, unlike the example I just gave you, an autoregressive model only has access to context before or after the blank, but never both. So the most common autoregressive model, like GPT or language Llama from Meta AI, both autoregressive models, they are called forward predictive autoregressive language models. It was a bit of a mouthful. The forward predictive means that the blank is always at the end of the sentence or phrase or PDF or document or whatever. But that blank, the thing that it is trying to predict, is always at the end of the sequence. That is a forward predictive. It's autoregressive because it has no context after the blank. It has to only look back at what it has already seen and use that context for the next token it wants to predict. And the autoregressive is the family of miles that we're talking about. Now this is as opposed to autoencoding models. Auto encoding models is exactly like the the example I gave you previously. The blank can technically be at the beginning or the end. That's allowed. But in an autoencoding language modeling task, the blank can be anywhere, especially in the middle of the phrase. So the model is trained to fill in the blank with access to context on either side of the blank. Now, if you think it through for a little bit more time, you'll realize autoencoding tasks are both easier and harder than autoregressive tasks. They're easier in the sense that, well, if you have the blank in the middle of a sentence, you have a lot of access to context. Because if you think about the past example, if you don't blank at the sign, you will get a ticket. If you remove the context after the blank and just say if you don't, you don't really have much to go on, right? You only have three words. If you don't, I don't know if you don't mind, if you don't want, if you don't have to. Without the context after the blank, you're kind of out of luck. So in that regard, autoencoding models have it easier in that they have access to more context. That being said, to teach a model to perform an autoencoding task is is actually a little bit harder than autoregressive because autoencoding is really a superset of autoregressive because the blank is allowed to be at the beginning or the end for an autoencoding model. So an autoencoding task is autoregressive plus the blank can be anywhere else. So you have this kind of give and take where it is technically easier of a task to fill in the blank. However, to train a model to be as comprehensive as an autoregressive model will require more training and more data, because it has to learn to predict not just forward predictively, but anywhere in that sentence. So it's a give and take, as most things are in life. To break it down slightly further, or rather to give some more examples, autoregressive is more or less synonymous with predicting the next word in a sentence, like an autocomplete.

OpenAI, ít nhất là vào thời điểm đó

ghi âm, không tạo ra các mô hình văn bản mã hóa tự động.

Họ không tồn tại, họ không tạo ra những thứ đó.

Ít nhất họ không làm chúng theo cách

ý nghĩa nguồn mở. Và nếu họ sử dụng chúng,

chúng được sử dụng trong một nhiệm vụ lớn hơn, như

để tạo hình ảnh, như Dall E2,

mà chúng ta sẽ không nói đến quá nhiều.

Nhưng một phần của hệ thống đó

là phần văn bản mã hóa,

nhưng phần đó là một phần của hệ thống lớn hơn

và không nhất thiết phải được sử dụng trừ khi nhà phát triển

muốn xé nó ra và sử dụng nó

cho một cái gì đó khác trong một nhiệm vụ khác.

Vì vậy hầu hết mọi người sẽ chọn

tự động mã hóa hoặc tự động hồi quy

và sẽ có ít công ty hơn

giải quyết sự kết hợp.

Nhưng nếu họ thành công với sự kết hợp đó,

sau đó họ là những công ty như Google,

ai làm ra T5 thực sự có thể

hiển thị các cuộc biểu tình lớn hơn

về khả năng của

mô hình ở kích thước nhỏ hơn.