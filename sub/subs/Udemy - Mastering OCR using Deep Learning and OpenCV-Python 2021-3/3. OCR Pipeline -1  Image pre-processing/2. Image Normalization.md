# 2. Image Normalization

---

In this video, let's understand the image normalization technique in detail.

So normalization is a process that changes the range of pixel intensity values.

Because mostly we will be dealing with 8-bit images, so let's understand normalization using the 8-bit case. Now,

as we know that for an 8-bit image, the intensity value ranges from zero to 255.

So by using normalization, we try to change this range from zero to 255 to any range closer to zero to one.

Now you may ask, why do we need this?

What's the use of this?

So if you normalize the data, the deep learning models tend to converge faster.

And also, this improves the training stability of the model.

In other words, this solves the problem of exploding and vanishing gradients that you normally face while

training your model.

As we will be dealing with the deep learning models in the next pipeline components, whether it be text

detection or recognition, so this normalization step becomes extremely crucial.

Now let's discuss some of the most commonly used normalization techniques.

So one of the simplest things that you can do is divide the image by the maximum value.

For instance, for an 8-bit image, the maximum value is 255.

So you divide every pixel by 255 as shown here.

Second, we can use Min-Max Scaling, this is one of the most common ways to normalize the data.

In this, we scale the intensity values to a range of zero to one. Here, the minimum image value gets mapped

to zero, while the maximum image value gets mapped to one.

So here is a formula for this where X_min

and X_max refers to the minimum and maximum image value, while X' refers to the normalized

output.

You can also use standardization or z-score normalization. In this, we scale the intensity values

to have zero mean and unit variance.

Here is a formula for this where mu refers to the mean and sigma denotes the standard deviation of your image.

Now, let's see how to implement each of these techniques using OpenCV Python.

So let's first import the openCV and the numpy library.

So if you remember, we discussed the three most common normalization techniques that are divide by Max,

Min-Max Scaling and standardization or Z score normalization.

Here I have also written the formula along with each technique so that it's useful while coding.

Now let's implement each of these techniques.

So let's first start with divide by max.

So let's create a function divide underscore by underscore max that takes the image as input.

So if you remember, in this we divide each pixel by the maximum value.

So for an 8-bit image, the maximum value is 255.

So we simply return image divided by 255.

So this is divide by max function.

Now let's implement the Min-Max Scaling.

Again, let's create a min underscore max function that again takes the image as input.

So as can be seen from the formula, we need to calculate the minimum and maximum image value and so this

can be easily done using numpy np.min and np.max functions.

So image underscore min equals np.min of image and image underscore max equlas np.max

of image.

So once the minimum and maximum image value is  calculated, we simply return the normalized value as shown

in the formula.

So this is Min Max Scaling.

Finally, for standardization.

Let's again create a standardize function that again takes the image as input. In this, as can be seen

from the formula, we subtract the image mean and divide by the standard deviation.

So, mean can be calculated using image dot mean function while the standard deviation can be calculated

using image dot std function.

So once the mean and standard deviation is calculated, we simply return the normalized value as given

by the formula image minus mean divided by standard deviation.

Now you can run these functions and see the normalized results, so this is how you normalize your image.

I hope you understood this.

Thanks.