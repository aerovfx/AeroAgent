# 4. Image Resizing

---

In this video, let's understand image resizing in detail. Image resizing as clear from the name

simply means changing the size of the image.

For instance, see this example where the right image is half the size of the left image.

Now, you may ask, why to resize the image or what's the use of this?

So if you have ever done any computer vision task, then you must know that most of the deep learning

models only accepts the same size images as input.

But mostly you will encounter the images that have different sizes.

So in order to make these images compatible with the input shape of the network, you need to resize

these images.

That's why resizing is a crucial step at both training and prediction time.

I hope you understood this.

Now, let's see how to do this using OpenCV python.

So OpenCV provides a built-in function, cv2.resize that changes the size of the image.

This function takes as input the image that we wanted to resize.

the output image size and three optional arguments.

where the first two arguments fx and fy denotes the scaling factor in the X and Y direction.

And this refers to the interpolation method to be used. By default, this function uses bilinear interpolation

as denoted by this flag.

cv2.INTER_LINEAR.

You can also use other interpolation methods, such as nearest neighbor interpolation, Bicubic interpolation,

etc..

Now, in this function, you either need to provide dsize or fx and fy as input, and the other one

is automatically calculated.

So let's take an example to understand what this means.

Suppose you have an input image of size 400 comma 400 and let's say you want

to double its size or you want to resize it to 800 comma 800.

So there are two ways by which you can do this, so either you can provide the output image size

directly using the dsize argument

For instance, see here where the dsize is set to 800 comma 800, or you can use the scaling factors

fx and fy as shown here.

Note that in this case, the dsize argument should be set to none.

So you can either use the dsize argument or fx and fy argument to resize the image. Both of these will resize the image

to 800 comma 800.

Now, what if you provide both dsize and fx and fy arguments?

So in this case, the image will be resized according to the value provided in the dsize argument.

Now, let's implement all this in code.

So let's say we want to double the size of this image, so let's see how to do this.

So let's first import the OpenCV library and read this image that we want to resize.

Now, if you remember, there are two ways by which we can do this, either using the dsize argument.

Or using the fx and fy arguments.

So let's first implement this using the dsize argument. For this, you first need to know the size of the

image.

This can be done using image dot shape.

So the size of this image is (300, 300).

Now, to resize this image OpenCV provides cv2.resize function that takes as input the image

and the output image size, which for this case will be double of (300,300).

That is six hundred.

Six hundred.

So, as discussed, this function returns the resized image as output.

Now, to verify that the image is doubled, let's print the shape of this resized image, the shape

comes out to be six hundred, six hundred that we expected.

Now let's see how to implement this using fx and fy arguments.

Again, we use the cv2.resize function.

But if you remember this time, we need to pass dsize as none.

and fx equals to two and fy equals to two because we are doubling the image size.

Again, to verify

let's print the shape of this resized image.

This again comes out to be six hundred, six hundred. So this is how you resize the image using

OpenCV Python. Here,

I have used a default interpolation method.

You can use any. Thanks.