# 4. Implementation of CRNN model using OpenCV-Python

---

In this video, let's understand how to perform text recognition using the CRNN or CNN+RNN+CTC

model that we just discussed and OpenCV-Python.

For this, let's use this segmented image as an example and see how to recognize the text present in this image.

So let's get started.

So let's first import the 'OpenCV' and the 'numpy' library and read this segmented image.

Now, as we have already discussed, that OpenCV provides several pre-trained models such as EAST

YOLO, Faster-RCNN, etc., etc..

So instead of creating the entire model from scratch, we can directly download these pre-trained models

and then do the inference.

So in this page, again, search for CRNN and you will get the link for downloading the pre trained

model.

So here is the link.

I have also provided the link in the theory section.

So go to this link and download the pre-trained model.

Once the downloading is finished, let's load this model using CV2.dnn.readNet function.

So this loads the pre trained CRNN model into memory.

Now, let's prepare the input image in order to make it compatible with the model.

So if you remember, as we have already discussed, that color doesn't imply anything for extra recognition.

So let's first convert this to greyscale.

This can be easily done using OpenCV.

cv2.CvtColor function.

For other changes, let's use the cv2.dnn.blobFromImage function similar to how we did in the

EAST implementation.

For this model, we will use this scale factor this size and this mean for image preparation.

This is what is being used while training this model.

So once the image is prepared, let's pass it through the network and extract the per timestep scores.

So if you remember, in order to pass the input, we use model.setInput method.

While for predictions, we use the model.forward method.

This method, takes as input the name of layes for which the output is needed, if you don't pass any

argument to this function, then this runs the forward pass for the whole network.

This function output's the per timestep scores.

So if you just print the shape of these scores, then you will find that we have 26 steps and for each

of these time steps, this pre-trained model outputs the probability over thirty seven characters.

These 37 characters include 36 characters starting from '0' to 'z' as shown and one blank characters.

For this case, let's use hyphen as a blanket as this is not present in our alphabet's set.

Let's denote the complete set of thirty seven characters via the variable charset.

Now, the last thing that we need to do is convert these scores to the final text.

So if you remember, we discussed that there are several heuristics available, such as best path decoding,

beam-search, prefix-search, etc., etc. that can do this.

Here, let's use the best path decoding approach. In this

first we take the most likely output at each time step and then apply the mapping rule to produce

the final output.

I hope you remember this from the previous video.

Now let's implement this.

So to find the most likely output we simply iterate over each time step, then find the index of the

maximum value using np.argmax function and finally append the character present at that index to produce

the most likely output.

So this completes the first step.

Now let's apply the mapping rule.

So if you remember from the previous video in the mapping rule, we first merged the repeating characters

and then removed the blank character.

So let's take an example to understand this logic.

Suppose this is the most likely output we obtained.

For the mapping rule, we need to check two conditions. First, if the current character is blank or

not, if it is blank, we then ignore it, otherwise keep it.

Second, we check if the current character is equal to the previous character or not.

If it is equal, then we ignore it, otherwise keep it.

Now let's apply this.

So let's first create a list to store the final output for the first character because there is no previous

character,

so we just need to check the first condition since this is not a blank charackter

so we keep this character.

For the second character, because it's not blank and it's not equal to the previous character, so

we also keep it. Similarly for the third character, because if it is equal to the previous character,

so we discard it.

For the fourth and fifth character, because this is blank, so we remove it finally for the fifth character

because it is not either blank and not equal to the previous character,

so we keep it.

So this is the final output we obtained after the mapping.

So this is how it works.

Now, let's implement this.

So as discussed for the first character, we only check one condition if it is not blank, we keep it.

While for the other characters, we check two conditions.

If it is not blank and the previous character is not the same as the current character, only then we

keep it.

Finally, we join all the characters to produce the final output.

Now, let's combine these two functions to form the best path decoding approach. So as we know, first

we take the most likely output at each time step and then apply the mapping rule to obtain the final

text.

So this completes the best path decoding function.

Now, let's call this function and print the final output next.

So as you can see, that this model predicts the correct text.

So this is how you can perform text recognition using CRNN and OpenCV-Python. Because this is a beginner

level course,

so we just used the pre-trained model.

But in the next course, we will build our own CRNN model from scratch using tensorflow

and train it.

Thanks.