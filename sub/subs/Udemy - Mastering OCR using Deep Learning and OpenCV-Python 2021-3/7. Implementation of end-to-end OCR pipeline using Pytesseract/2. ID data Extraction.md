# 2. ID data Extraction

---

In this video, let's discuss an important use case of optical character recognition that is I.D.

data extraction.

So given any I.D., we need to extract the data such as name, date of birth, etc, etc. for this video.

Let's take this dummy I.D. as an example and let's extract the name and date of birth from this dummy I.D.

So let's see how to do this. Here

I have the same code from the previous pytesseract video.

So let's run this.

If you remember, this will extract all the text from the ID.

So in this case, as you can see, that the text is correctly extracted, the name is Mahatma SIngh and

the date of birth is 01/01/1901.

But suppose if the text is not extracted correctly, then you should either check the page segmentation

method or apply suitable preprocessing technique.

I hope you remember this from the previous video.

Now, while performing OCR, it's always better to save the recognized text in a text file because the

text formatting might change depending on the IDE you use.

So this can be done by opening a text file in a right mode and then writing the text to that file using

right method.

So here, this will right the text to the structured_output.txt file.

Now let's open this file.

So here is the recognized text.

Now let's extract the name and date of birth from this text.

For this, let's first create a dictionary named as extracted_data that will store the extracted

name and date of birth.

Now, to extract the data, let's first open this structured_output.txt file and read all the lines.

This can be done by using the readlines function that returns the list of all the lines present in the

file.

Now, to extract the name and date of birth, what we will do is we will iterate line by line and check

if the line contains the text 'Name' or 'DOB'.

If it contains, then we will extract all the text after these two words.

Now, let's implement this.

So to iterate over this list,

we use the for loop as 'for line in line_lst'.

So as you can see that each line contains a new line character.

So let's first remove this.

This can be done using line.strip() function.

So once we have removed the new line character, let's apply conditions, so let's start with the name.

So if 'Name' is present in line_strip, we extract all the text after 'Name'. To do this, let's

split this line by space and pick all the words leaving the first one.

So this will give us a list containing two words, Mahatma and Singh. We then join these two words

with space to obtain the final text.

Finally, let's save this in the dictionary.

with the key name.

Similarly for date of birth, if 'DOB' is present in line_strip, we again split this line and this

time we pick the last word.

So let's again save this with the key date of birth.

So let's run this and print the dictionary extracted_data.

So as you can see that both the name and date of birth are extracted correctly.

Sometimes the output from the pytesseract or any other OCR system may contain different text case.

For instance, as you can see that the text name has 'N' capital and other letters small.

But in the output, sometimes you may get different case. Such as some letters in lower case while

others in uppercase or all the letters in lowercase or uppercase, etc., etc. So to handle all the

variations, it's better to convert all the text to the same case either lowercase or uppercase, For

instance,

here I have converted all the text to lowercase and then search for name and date of birth in this lowercase

text.

So this is how simple it is to extract the data.

I hope you understood this.

Thanks.