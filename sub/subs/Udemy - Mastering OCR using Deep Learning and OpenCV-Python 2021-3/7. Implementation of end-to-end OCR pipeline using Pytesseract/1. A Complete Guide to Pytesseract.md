# 1. A Complete Guide to Pytesseract

---

So till now, we have covered all the components in the OCR pipeline. So if you remember, in order to

extract the text from the images, we first need to pre-process the image, then detect the text regions,

recognize the text present in those regions and finally restructure the recognized text.

So this whole process takes a lot of effort.

But what if I tell you that there are some tools or softwares available that does everything for us

in an end-to-end fashion, that is, given an input image

These software's or tools will directly give us the final restructured text.

All the steps, such as text detection, text recognition or restructuring will be implicitly implemented

by these softwares and we need not to worry about them.

So one such software that is open source and free is Tesseract. This was developed by Hewlett-Packard

Laboratories in 1980s and they opensource this in 2005 and since 2006, this is being developed and

maintained by Google.

The latest stable version is four point one point one.

In this version, they have added a new neural net plus LSTM based OCR engine and support for

some additional languages and scripts. In total, Tesseract can recognize one hundred and sixteen languages.

The only downside is that this doesn't give good results on the low quality images and the images having

unstructured text as shown.

So in that case, we need to switch back to the methods we discussed. Tesseract is mainly executed

from the command line interface.

So in order to run this using Python, we need to use the Pytesseract library.

Pytesseract is basically a wrapper for Google's Tesseract OCR engine. In this course, we will use this

library.

So let's first see how to install this.

For this, you first need to install the Tesseract OCR engine. To do this, search Tessdoc in Google and

then go to this installation link.

So this will open up the official Tesseract documentation that contains the instructions for installation on Linux,

macOS, Windows, etc., etc., For instance, to install on Linux, you just need to run these commands.

So this will install the Tesseract engine to your system.

Similarly for Mac OS, you have two options, either using Mac ports or homebrew. With Mac ports

You need to run this command in order to install the Tesseract, while, with homebrew

You need to run this command.

For Windows, you need to install the Tesseract installer for Windows for that, click on the link provided

by Tesseract at U.B. Mannheim.

So here,

You can see two links, one for the 32 bit and the other one for the 64 bit. As you can see that this

is the setup for the version five point zero point zero Alpha.

But if you want to install the stable versions, you can click here.

But for now, let's download this 64 bit version.

So after downloading is finished, let's install this setup.

So select the language that you want.

Then click on next, then click on I agree to accept the license terms. Now install for anyone using

this computer.

Keep it as it is.

Click on next.

Now, by default, this provides support for English language.

But if you want to recognize some other languages, you can do this by checking these two boxes.

That is additional script data and additional language data.

For instance, let's say I also want to detect the Chinese language. For this,

Let's check these boxes labeled with Chinese language.

So what this will do is download the training data for these languages.

After selecting the additional languages and scripts, click on next.

Then choose the folder in which to install the Tesseract. Remember this location as this will be useful

later.

So after selecting the location.

Click on next.

Now, choose a start menu folder. Keep it as default. Click on Install.

So this will install the Tesseract engine in your system.

So this completes the first step. Let's install the PyTesseract library. For this search PyTesseract in

Google and click on this link provided by pypi.

So this will open up the PyTesseract documentation. Now to install PyTesseract you have many options.

Either you can install PyTesseract using PIP, you can use git, you can even install it from the source

or you can use conda.

If you are using pip to install PyTesseract, use this command pip install PyTesseract. With git

use this command, while for conda, use conda install PyTesseract

So let's use Anaconda to install this. For this, open up the anaconda terminal and activate the environment,

if any.

Then to install PyTesseract, simply use pip install PyTesseract command.

So this will install the PyTesseract library into your environment.

So now we have both Tesseract engine and PyTesseract library.

Now let's see how to use them for optical character recognition.

So let's take this image as an example and see how to use this library.

So let's first import OpenCV and PyTesseract library and read this image.

Now, to verify that this image has been correctly read, let's display this image.

So this is the image from which we want to extract the text.

Now, in order to extract the text using PyTesseract, you just need to do two things.

First, you need to tell PyTesseract where the Tesseract engine is located.

So I hope you remember the path where you have installed Tesseract.

In my case, this is located in C program files.

Tesseract OCR and inside this folder, there is a tesseract executable file.

So if you have not changed the path during installation, then your path should also be this.

So you need to tell PyTesseract the location of this Tesseract.exe file.

So this can be done in two ways, either by adding this path in your path environment variable or by

explicitly telling PyTesseract this path.

So let's add the path using the second method.

So this can be done by setting

PyTesseract

dot PyTesseract dot Tesseract underscore cmd equals to this path.

So this completes the first step.

So once you have done this, then you can simply extract the text using by PyTesseract dot image to string

function.

This takes as input the image.

This function returns the recognized text as output.

So let's print this next.

So this is the recognized text from the image. To compare.

Let's also display the image.

See how we are able to extract almost all the text correctly, this is how simple it is, just two lines

of code.

So if you remember, as discussed by default, this function recognizes the English language text, but

if you want to recognize some other language, then you can specify this using lang argument.

In this argument, you need to specify the language that you want to recognize.

So in order to get the list of all available languages, you can use the function PyTesseract dot

get underscore languages.

This function returns the list of all languages that you have downloaded by installing Tesseract.

Now, what if you have multiple languages in an image?

In that case, use the plus sign to specify multiple languages, for instance, suppose image contains

two languages, English and Chinese simplified.

So you need to write lang equals ENG for English plus CHI underscore sim for Chinese simplified.

So this is how you recognize multiple languages in an image.

I hope you understood this.

So by default, Tesseract expects a page of text when it segments an image, but in real life, you may

encounter images that have vertical or circular text or images that have sparse text, etc.,

etc.

So for different types of text, Tesseract provides different page segmentation methods.

So here is the complete list of supported page segmentation modes.

By default, Tesseract uses fully automatic page segmentation mode, you have other options, such

as for vertically aligned text.

use this method, while for images containing single word use this, for sparse text use these modes,

etc., etc..

So let's take an example to understand the page segmentation method importance and how this affects

the results.

So here I have an image that has sparse text.

Now let's compare the results using different page segmentation modes.

So let's first run this using the default page segmentation method.

So by this, we obtained this text as output.

Clearly, this is not correct.

So let's try out different page segmentation method.

First, let's check out how to specify the page segmentation method in this function.

So to specify different page segmentation methods, you can use the config parameter as config equals r

in inverted commas, minus minus PSM space followed by value.

Here, PSM refers to the page segmentation method.

Because in this case, the image contains sparse text, so let's use psm value as 11. So, specify PSM

equals 11 in this function.

Now let's again run this function and check the result.

So as you can see that by specifying the correct psm value, we obtained much better results.

So now you can try changing different PSM values and see how the results vary for better understanding.

If you remember, we discussed that Tesseract doesn't give good results on low-quality images and the images having

unstructured text, so if you are not getting good quality output from Tesseract, then what to do?

So first thing you should check is whether you have used correct page segmentation method or not.

Second, you should apply some preprocessing techniques to improve the quality of input.

For instance, you can remove noise deskew the image, rescale it, etc, etc. So always remember that

preprocessing greatly affects the Tesseract result.

For instance, for this image, let's just convert it to grayscale and see the results.

So this can be done using the cv2.cvtColor function, as we have done earlier.

Now, instead of passing the color image, let's pass this grayscale image and check the results.

So as you can see, that the results have improved.

Now, let's again apply some other preprocessing technique, so let's use the thresholding technique.

So if you remember, this can be done using cv2.threshold function.

So after thresholding, you need to pass this threshold image to the image to string function.

So let's run this.

Now, in order to compare the results, let's print this output text in a new cell.

So as you can see that this time we got the correct text as output.

So I hope from this example you understood how important this preprocessing step is.

Now, other than image to string function PyTesseract also provide several other methods, such as image

to data, image to boxes, etc., etc. You can read about these from the PyTesseract official documentation.

The link for this is provided in the theory section, so please go through them.

So I hope you understood how to use the PyTesseract library and how to improve the results. In the next

video,

Let's see how to perform the ID and invoice data extraction using PyTesseract.