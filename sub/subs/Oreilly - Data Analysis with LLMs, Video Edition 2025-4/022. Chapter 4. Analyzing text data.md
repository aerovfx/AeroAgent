# 022. Chương 4. Phân tích dữ liệu văn bản đã dịch

---

Chương 4.

Phân tích dữ liệu văn bản

Chương này đề cập đến việc phân loại văn bản.

Trích xuất thông tin.

Phân cụm tài liệu.

Dữ liệu văn bản có mặt khắp nơi và chứa thông tin có giá trị.

Ví dụ: hãy nghĩ đến các bài báo, email, bài đánh giá hoặc có lẽ cuốn sách mà bạn đang đọc.

đọc.

Tuy nhiên, việc phân tích văn bản thông qua các phương tiện tính toán vẫn còn khó khăn cho đến tận vài năm trước.

Xét cho cùng, không giống như các ngôn ngữ hình thức như Python, ngôn ngữ tự nhiên không được thiết kế

để máy tính có thể phân tích dễ dàng.

Thế hệ mô hình ngôn ngữ mới nhất cho phép phân tích văn bản ở hầu hết các cấp độ con người

nhiều nhiệm vụ phổ biến.

Trong một số trường hợp, hiệu suất của các mô hình ngôn ngữ để phân tích và tạo văn bản có

thậm chí còn được chứng minh là vượt qua khả năng của con người.

Trong chương này, chúng ta sẽ xem cách sử dụng các mô hình ngôn ngữ lớn để phân tích văn bản.

Theo một số cách nhất định, phân tích dữ liệu văn bản là một ứng dụng rất tự nhiên của mô hình ngôn ngữ.

Họ đã được đào tạo về số lượng lớn văn bản và có thể được áp dụng trực tiếp để phân tích văn bản,

nghĩa là không cần tham khảo các công cụ bên ngoài để phân tích dữ liệu thực tế.

Chương này đề cập đến một số cách phân tích văn bản phổ biến, phân loại tài liệu văn bản,

trích xuất dữ liệu dạng bảng từ văn bản và phân cụm các tài liệu văn bản thành các nhóm có ngữ nghĩa tương tự nhau.

tài liệu.

Đối với mỗi trường hợp sử dụng này, chúng ta sẽ xem mã ví dụ và thảo luận về các biến thể cũng như tiện ích mở rộng.

Phân loại, trích xuất thông tin và phân cụm là ba loại văn bản quan trọng

phân tích, nhưng không phải là thứ duy nhất bạn có thể cần trong thực tế.

Tuy nhiên, làm việc thông qua các ví dụ trong chương này sẽ cho phép bạn tạo các tùy chỉnh

đường ống xử lý dữ liệu cho dữ liệu văn bản dựa trên mô hình ngôn ngữ.

Mục 4.1 Sơ bộ

Đảm bảo hệ thống của bạn được thiết lập đúng cách cho các dự án mẫu.

Các ví dụ sau sử dụng chuỗi mô hình GPT của OpenAI, Được truy cập qua Thư viện Python của OpenAI.

Thư viện này đã được thảo luận chi tiết trong chương 3.

Đảm bảo làm theo hướng dẫn trong chương 3 để có thể thực thi mã ví dụ.

Cảnh báo.

Thư viện Python của OpenAI đang thay đổi nhanh chóng.

Mã trong chương này đã được thử nghiệm với phiên bản 1.29 của Thư viện Python OpenAI,

nhưng có thể không hoạt động với các phiên bản khác nhau.

Ngoài thư viện OpenAI, chúng ta sẽ sử dụng thư viện pandas phổ biến.

Pandas là một thư viện phổ biến để xử lý dữ liệu dạng bảng mà chúng ta sẽ sử dụng làm đầu vào và đầu ra

định dạng.

Chúng tôi sẽ chỉ sử dụng chức năng cơ bản từ thư viện đó và giải thích các lệnh tương ứng

khi chúng xảy ra trong mã.

Hãy chắc chắn rằng gấu trúc đã được cài đặt.

Ví dụ: thử nhập gấu trúc trong trình thông dịch Python.

Nếu không, hãy cài đặt nó bằng cách nhập lệnh sau vào terminal.

Xem mã này.

Cuối cùng, đối với phần cuối cùng của chương này, bạn sẽ cần các thuật toán phân cụm từ

thư viện Scikit Learn.

Chạy lệnh sau trong terminal để cài đặt phiên bản thích hợp.

Xem mã này?

Các phần sau chứa mã cho ba dự án nhỏ sử dụng mô hình ngôn ngữ để

phân tích văn bản.

Không cần phải nhập mã.

Bạn có thể tìm thấy tất cả mã trên trang web đồng hành của cuốn sách trong phần tài nguyên cho việc này

chương.

Mặc dù bạn có thể thực thi mã trên dữ liệu của riêng mình nhưng cuốn sách này có kèm theo một số ví dụ mẫu

các bộ dữ liệu chúng tôi sử dụng trong các ví dụ cũng như trên trang web đồng hành.

Và bây giờ là lúc sử dụng các mô hình ngôn ngữ để phân loại văn bản.