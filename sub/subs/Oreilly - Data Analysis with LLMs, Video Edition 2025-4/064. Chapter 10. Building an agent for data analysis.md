# 064. Chương 10. Xây dựng tác nhân phân tích dữ liệu được dịch

---

Phần 10.4, Xây dựng tác nhân phân tích dữ liệu.

Trong phần này, chúng tôi sẽ sử dụng Langchain để xây dựng một tác nhân phân tích dữ liệu.

Tác nhân này sẽ có thể truy cập các nguồn dữ liệu khác nhau bằng dữ liệu có cấu trúc và không cấu trúc

dữ liệu.

Hơn nữa, đại lý sẽ quyết định truy cập nguồn nào trong số những nguồn đó và theo thứ tự nào.

Nó thậm chí có thể sử dụng thông tin thu được từ một nguồn để truy vấn nguồn thứ hai, ví dụ:

để truy cập cơ sở dữ liệu có cấu trúc về doanh số bán trò chơi điện tử nhằm xác định trò chơi được bán nhiều nhất ở

một năm cụ thể và sau đó sử dụng tên trò chơi để truy vấn trang web để biết thêm thông tin.

Phần 10.4.1, Tổng quan.

Tác nhân Phân tích dữ liệu của chúng tôi triển khai phương pháp mà chúng tôi đã thảo luận trong phần trước.

Nó sử dụng mô hình ngôn ngữ để quyết định công cụ nào sẽ được gọi theo thứ tự nào và với đầu vào nào

các thông số.

Trong kịch bản ví dụ của chúng tôi, chúng tôi sẽ cung cấp cho tác nhân các công cụ để truy cập cơ sở dữ liệu quan hệ,

cũng như thu thập thông tin về cấu trúc của nó, chẳng hạn như tên của các bảng có sẵn.

Chúng tôi cũng cung cấp cho đại lý một công cụ cho phép tìm kiếm trên web, khai thác tìm kiếm hiện có

động cơ ở chế độ nền.

Kết hợp lại với nhau, chúng ta có một tác nhân có thể truy vấn cơ sở dữ liệu quan hệ và sử dụng web để lấy

thông tin liên quan đến nội dung cơ sở dữ liệu.

Hãy bắt đầu cuộc thảo luận của chúng ta bằng cách mô tả chi tiết hơn về các công cụ mà chúng tôi sẽ cung cấp cho

đại lý.

Tổng cộng, đại lý sẽ có quyền truy cập vào năm công cụ sau.

Các bảng danh sách SQLDB liệt kê tất cả các bảng trong cơ sở dữ liệu quan hệ.

Lược đồ SQLDB trả về lược đồ SQL của một bảng được đặt tên bảng.

Trình kiểm tra truy vấn SQLDB cho phép tác nhân xác thực truy vấn SQL.

Truy vấn SQLDB đánh giá một truy vấn SQL và trả về kết quả truy vấn.

Tìm kiếm truy vấn SQLDB cho phép tác nhân tìm kiếm trên web thông qua các từ khóa, trả về văn bản web.

Bốn công cụ đầu tiên giúp tác nhân truy cập cơ sở dữ liệu quan hệ.

Công cụ cuối cùng cho phép tác nhân lấy thông tin từ web.

Với một nhiệm vụ do người dùng chỉ định, tác nhân quyết định sử dụng mô hình ngôn ngữ cơ bản

trong số các công cụ này sẽ được gọi và theo thứ tự nào.

Hình 10.3 minh họa kịch bản này.

Hình 10.3

Tác nhân dữ liệu sử dụng nhiều công cụ để khám phá cấu trúc và truy vấn cơ sở dữ liệu quan hệ.

Ngoài ra, tác nhân có thể truy xuất văn bản web thông qua công cụ tìm kiếm trên web.

Mục 10.4.2 tạo tác nhân với Langchain.

Tạo một đại lý với Langchain rất nhanh chóng.

Langchain thậm chí còn cung cấp các công cụ xây dựng chuyên dụng cho các tác nhân truy cập cơ sở dữ liệu có cấu trúc.

Chúng tôi sẽ sử dụng các tính năng đó trong đoạn mã sau.

Các tác nhân được thực hiện thông qua các mô hình ngôn ngữ.

Để tạo một tác nhân, trước tiên chúng ta phải tạo một đối tượng mô hình ngôn ngữ.

Xem mã này?

Chúng tôi đang tạo một mô hình ngôn ngữ AI mở của trò chuyện kiểu.

Chính xác hơn, chúng tôi lại đề cập đến mô hình GPT-4O.

Tiếp theo, chúng ta tạo một đối tượng đại diện cho cơ sở dữ liệu quan hệ của mình.

Chúng tôi sẽ truy vấn cơ sở dữ liệu ánh sáng SQL được lưu trữ trên đĩa.

Giả sử DBPath lưu trữ đường dẫn của tệp cơ sở dữ liệu tương ứng.

Thông thường các tệp như vậy có hậu tố .db.

Chúng ta có thể tạo một đối tượng cơ sở dữ liệu bằng đoạn mã sau.

Xem mã này?

Chúng tôi đã đề cập đến bốn công cụ để truy cập cơ sở dữ liệu quan hệ.

May mắn thay, tất cả các công cụ này sẽ được tạo tự động từ đối tượng cơ sở dữ liệu.

Tuy nhiên, chúng ta vẫn cần tạo một công cụ để tìm kiếm trên web.

Chúng tôi sẽ sử dụng thành phần tích hợp của Langchain, công cụ API SERP.

Để sử dụng công cụ này, trước tiên bạn cần đăng ký tài khoản trên trang web SERP API.

Khi bạn đang duyệt trình duyệt, hãy truy cập SERP API.com, nhấp vào nút đăng ký và tạo một tài khoản tương ứng

tài khoản.

Để thực thi mã được trình bày tiếp theo, bạn sẽ cần truy xuất khóa truy cập API của mình, có sẵn

tại liên kết này.

Bạn cũng cần cài đặt tiện ích mở rộng Langchain để kích hoạt công cụ tìm kiếm trên web.

Đi đến thiết bị đầu cuối và chạy lệnh sau.

Xem mã này?

Sau đó, tất cả những gì bạn cần là đoạn mã Python sau để tạo một công cụ cho web

tìm kiếm, giả sử rằng LLM chứa đối tượng mô hình ngôn ngữ đã tạo trước đó và khóa SERP AI,

khóa truy cập API SERP.

Xem mã này?

Chức năng tải công cụ được sử dụng cho các công cụ tiêu chuẩn bằng cách chuyển tên của các công cụ mong muốn

như các tham số.

Trong trường hợp này, chúng tôi chỉ cần công cụ tìm kiếm trên web và chúng tôi chỉ chuyển một mục duy nhất trong danh sách

tên công cụ, API SERP.

Sau lệnh gọi tải công cụ, chúng tôi lưu trữ kết quả trong các công cụ bổ sung, một danh sách các công cụ có

một mục duy nhất, công cụ tìm kiếm trên web.

Bây giờ chúng ta đã có tất cả các thành phần cần thiết để tạo một tác nhân bằng Langchain.

Giả sử rằng DB chứa đối tượng cơ sở dữ liệu, được tạo trước đó và LLM mô hình ngôn ngữ

tạo ra trước đó.

Chúng tôi khởi tạo một tác nhân để truy cập dữ liệu dựa trên SQL bằng mã sau đây.

Xem mã này?

Lệnh tác nhân CreateSQL là một chức năng tiện lợi được Langchain cung cấp để tạo tác nhân

để truy cập dữ liệu dựa trên SQL.

Bốn công cụ được đề cập trước đây để truy cập cơ sở dữ liệu quan hệ, hữu ích cho việc truy xuất tên bảng,

hiển thị sơ đồ bảng, xác thực các truy vấn SQL và cuối cùng là phát hành chúng, được thêm tự động

mà không cần chúng tôi phải thêm chúng một cách rõ ràng.

Chỉ có một công cụ nữa mà chúng tôi muốn ngoài các công cụ tập trung vào SQL, đó là khả năng tìm kiếm trên web.

Những công cụ như vậy được chỉ định trong danh sách thông qua tham số đầu vào công cụ bổ sung, thiết lập chi tiết

gắn cờ thành đúng cho phép chúng tôi theo dõi quá trình suy nghĩ, dẫn đến tác nhân gọi cụ thể

công cụ.

Chúng ta sẽ thấy một số ví dụ đầu ra sau.

Loại tác nhân, Công cụ OpenAI trong trường hợp này, cũng xác định lời nhắc chính xác để sử dụng

như trình phân tích cú pháp nào sẽ được sử dụng để ánh xạ đầu ra của mô hình ngôn ngữ tới các lệnh gọi công cụ.

Sau khi tạo tác nhân, chúng tôi sử dụng đoạn mã sau để áp dụng tác nhân cho một tác vụ cụ thể.

Chúng tôi giả định rằng tác vụ có thể thay đổi lưu trữ mô tả ngôn ngữ tự nhiên của tác vụ mà chúng tôi muốn

để giải quyết.

Xem mã này.

Phần 10.4.3, Mã hoàn chỉnh cho tác nhân phân tích dữ liệu.

Liệt kê 10.2 tập hợp tất cả những điều này lại với nhau.

Sau khi đọc khóa truy cập API SERP API, cũng như đường dẫn đến tệp cơ sở dữ liệu và

một câu hỏi từ dòng lệnh, nó tạo ra một đối tượng mô hình ngôn ngữ, một đối tượng, sau đó là cơ sở dữ liệu,

hai, công cụ tìm kiếm trên web, ba, và cuối cùng là tác nhân, bốn.

Nó gọi tác nhân số 5 cho câu hỏi đầu vào.

Đầu ra do tác nhân tạo ra kết thúc bằng câu trả lời cho câu hỏi đó hoặc bằng

thông báo lỗi nếu tác nhân không thể tìm thấy câu trả lời.

Liệt kê 10.2, Tác nhân Phân tích Dữ liệu với khả năng tìm kiếm trên web.

Phần 10.4.4, Dùng thử.

Hãy xem nó hoạt động như thế nào trong thực tế.

Tải xuống mã cho Danh sách 10.2 từ trang web Books Companion.

Là mã, bạn sẽ cần Cơ sở dữ liệu SQL Light để thử tác nhân dữ liệu.

Chúng ta sẽ sử dụng Cơ sở dữ liệu SQL Light từ Chương 5 để lưu trữ thông tin về trò chơi điện tử.

Bạn có thể tìm thấy tệp tương ứng trên trang web Books Companion trong Games SQL Light

Liên kết.

Mở terminal và chuyển sang thư mục chứa mã.

Chúng tôi sẽ giả định rằng tệp cơ sở dữ liệu, Games.db, nằm trong cùng thư mục.

Trong đoạn mã sau, thay thế khóa API Serp bằng khóa tìm kiếm của bạn, có sẵn tại liên kết này.

Xem mã này.

Bạn sẽ thấy đầu ra như sau.

Kết quả bạn nhìn thấy có thể hơi khác do nội dung web thay đổi, những thay đổi nhỏ về

mô hình GPT-4O và một số yếu tố khác.

Xem mã này.

Hãy nhớ rằng chúng tôi đã chuyển đầu ra của tác nhân sang chế độ dài dòng.

Điều đó có nghĩa là đầu ra chứa nhật ký đầy đủ các công cụ được tác nhân gọi ra, cũng như

quá trình suy luận của tác nhân.

Chúng ta hãy xem xét kỹ hơn kết quả đầu ra để xem điều gì đã xảy ra.

Đầu tiên, tác nhân lấy danh sách các bảng có sẵn trong cơ sở dữ liệu quan hệ, sử dụng

Bảng danh sách SQL db, một.

Rõ ràng, đó là một bước hợp lý khi phải đối mặt với cơ sở dữ liệu mới.

Kết quả của việc gọi công cụ cho thấy cơ sở dữ liệu chỉ chứa một bảng duy nhất,

gọi là Trò chơi.

Đặc vụ trở nên tò mò về nội dung bảng.

Nó cũng gọi công cụ lược đồ db SQL để lấy thêm thông tin về bảng Trò chơi.

Lưu ý rằng công cụ này sử dụng các tham số đầu vào, cụ thể là tên của bảng cần điều tra.

Nhật ký hiển thị giá trị của tất cả các tham số đầu vào cho mỗi lệnh gọi dao.

Lệnh gọi công cụ lược đồ SQL db trả về lệnh SQL đã được sử dụng để tạo

bảng Trò chơi cùng với một mẫu nhỏ nội dung của bảng.

Tiếp theo, tác nhân xem xét một truy vấn SQL để lấy thông tin liên quan về câu hỏi đầu vào.

Trò chơi nào được bán nhiều nhất năm 2016 và nó được chơi như thế nào?

Trong bước đầu tiên, nó xác nhận rằng truy vấn sau đây đúng về mặt cú pháp bằng cách gọi

công cụ kiểm tra truy vấn SQL db.

Xem mã này?

Đồng thời, tác nhân sử dụng cơ hội để phản ánh tính hữu ích của truy vấn trong

xem xét.

Bằng chứng là đầu ra, bảng Trò chơi chứa thông tin chúng ta cần.

Tôi sẽ truy vấn trò chơi có doanh thu toàn cầu cao nhất vào năm 2016.

Có vẻ lạ khi thay vào đó, một mô hình ngôn ngữ lại có thể được hưởng lợi từ kiểu độc thoại này.

viết ra lời gọi công cụ một cách trực tiếp.

Tuy nhiên, người ta đã chứng minh rằng việc cho phép các tác nhân đưa ra lý do rõ ràng về vấn đề hiện tại

và các bước họ đang thực hiện để giải quyết vấn đề đó có thể cải thiện hiệu suất của họ.

Đó cũng là điều đang xảy ra ở đây.

Cuối cùng, tác nhân quyết định sử dụng truy vấn đã được xác thực trước đó để lấy thông tin từ

cơ sở dữ liệu bằng công cụ truy vấn SQL db.

4.

Truy vấn SQL trả về trò chơi tạo ra nhiều doanh thu nhất trong năm 2016.

FIFA 17, một trò chơi mô phỏng bóng đá do Electronic Arts sản xuất.

Nhưng câu hỏi đầu vào yêu cầu nhiều hơn thế.

Đây là trò chơi được bán nhiều nhất năm 2016 và nó được chơi như thế nào.

Phần thứ hai của câu hỏi không thể được trả lời từ nội dung cơ sở dữ liệu.

Theo ghi nhận của mình, đại lý nhận ra điều đó và thay vào đó cố gắng truy cập web.

Nó đưa ra yêu cầu tìm kiếm trên web bằng công cụ tìm kiếm chuỗi tìm kiếm cách chơi FIFA

17.

5.

Lưu ý rằng tác nhân có thể tự động tạo chuỗi tìm kiếm phù hợp từ

kết quả của truy vấn SQL và câu hỏi đầu vào.

Kết quả tìm kiếm trên web là tập hợp các đoạn văn bản được hiển thị ở đầu ra

chứa thông tin về cách chơi FIFA 2017.

Cuối cùng, tác nhân sử dụng thông tin được tìm kiếm trên web trả về, kết hợp với thông tin

từ cơ sở dữ liệu SQL để đưa ra câu trả lời cuối cùng.

6.

Đáp án cuối cùng xác định FIFA 17 là trò chơi phổ biến nhất năm 2016 và chứa thông tin chi tiết

hướng dẫn cách chơi tốt.

Chúng ta đã thấy rằng tác nhân có thể thực hiện một chuỗi phức tạp các lệnh gọi công cụ để tìm ra kết quả mong muốn

trả lời mà không cần phải chỉ định quy trình thực hiện thủ công.

Nếu bạn quan tâm, hãy thử truy vấn đại lý thêm một vài điều nữa, có thể phức tạp hơn

câu hỏi và xem liệu nó có thể trả lời chúng hay không.