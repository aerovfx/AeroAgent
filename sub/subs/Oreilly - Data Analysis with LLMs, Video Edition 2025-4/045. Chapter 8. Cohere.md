# 045. Chương 8. Cohere dịch

---

Mục 8.2, mạch lạc.

Chúng ta đã thảo luận ngắn gọn về ảo giác ở Chương 2.

Về cơ bản, ảo giác xảy ra khi các mô hình ngôn ngữ bịa đặt vì chúng không

có quyền truy cập vào dữ liệu có liên quan đến nhiệm vụ hiện tại.

Công ty khởi nghiệp Canada cohere đặc biệt nhấn mạnh vào việc tránh những ảo giác như vậy bằng cách sử dụng

một phương pháp gọi là nối đất.

Căn cứ vào câu trả lời của một mô hình ngôn ngữ có nghĩa là liên kết nó với dữ liệu thực, từ đó giảm bớt

cơ hội tạo ra sản phẩm sáng tạo không dựa trên thực tế.

Cohere hỗ trợ nhiều loại trình kết nối, cho phép các mô hình của nó truy cập dữ liệu bên ngoài.

Ví dụ: hỗ trợ tìm kiếm trên web cũng như truy cập vào nhiều cơ sở dữ liệu khác nhau.

Trong nội bộ, cohere truy cập các nguồn dữ liệu đó và cung cấp thông tin cho mô hình ngôn ngữ

phù hợp với yêu cầu.

Nhưng còn tuyệt vời hơn nữa, cohere hiển thị cho bạn tất cả các nguồn dữ liệu được sử dụng để tạo ra câu trả lời của bạn.

Nếu bạn thuộc loại người hay nghi ngờ và khi cần có câu trả lời từ các mô hình ngôn ngữ, bạn

nói chung là như vậy, bạn có thể theo dõi các tài liệu tham khảo và xác nhận rằng chúng hỗ trợ

câu trả lời được tạo ra.

Chúng ta hãy xem tất cả những điều đó hoạt động như thế nào trong thực tế.

Đã đến lúc trò chuyện với mô hình CMDR plus của Cohere.

Mục 8.2.1, trò chuyện với CMDR plus.

Tại thời điểm viết bài, CMDR plus là một trong những mẫu mới nhất của cohere.

Như thường lệ, bạn có thể dùng thử qua giao diện web và sử dụng nó qua Python khi xử lý các dữ liệu lớn.

lượng dữ liệu.

Đầu tiên, chúng ta sẽ thử giao diện web.

Để làm điều đó, hãy truy cập cohere.com và nhấp vào Thử ngay.

Sau khi đăng ký tài khoản xong nhấn vào Playground.

Bạn sẽ thấy giao diện web trong Hình 8.2.

Hình 8.2, giao diện web chat với mô hình ngôn ngữ CMDR plus của cohere.

Hình 8.2 hiển thị nơi nhập lời nhắc và nút tạo câu trả lời.

Còn cửa sổ bên phải chứa nút Web Connector thì sao?

Đây là nơi bạn chỉ định các trình kết nối sẽ sử dụng khi tạo câu trả lời của mình.

Trình kết nối cho phép kết hợp truy cập các nguồn dữ liệu bên ngoài.

Chúng ta có thể kích hoạt hoặc hủy kích hoạt Trình kết nối Web bằng cách chuyển đổi nút tương ứng.

Trình kết nối này cho phép cohere truy vấn web tương tự như những gì tất cả chúng ta sẽ làm khi

cố gắng trả lời một câu hỏi khó liên quan đến kiến thức thực tế.

Hãy dùng thử và xem câu trả lời cho các câu hỏi thực tế thay đổi như thế nào nếu Trình kết nối Web được

được kích hoạt hoặc vô hiệu hóa.

RAG là gì?

Bạn có thể nhận thấy rằng trang web của Cohere quảng cáo RAG một cách nổi bật, nhưng đó là gì?

RAG là viết tắt của Thế hệ tăng cường thu hồi.

Điều đó có nghĩa là khi tạo câu trả lời, chúng tôi tăng cường đầu vào được mô hình ngôn ngữ sử dụng

với dữ liệu chúng tôi lấy từ nguồn bên ngoài.

Phần 8.2.2 Thư viện Python

Go here cung cấp một thư viện Python tương tự như những thư viện chúng ta đã thấy trong các phần trước.

Nhập lệnh sau vào terminal để cài đặt thư viện cần thiết.

Xem mã này?

Liệt kê 8.2, có sẵn dưới dạng mục mạch lạc trên trang web, chứa mã cho một câu hỏi đơn giản

giao diện trả lời

Người dùng nhập khóa truy cập và câu hỏi trên dòng lệnh.

Hãy truy cập liên kết này để lấy chìa khóa truy cập của bạn.

Trong danh sách 8.2, sau khi định cấu hình thư viện cohere bằng khóa truy cập, một, chúng tôi tạo

một câu trả lời bằng cách sử dụng thư viện mạch lạc, hai.

Lưu ý tham chiếu đến trình kết nối trong lệnh gọi hàm trò chuyện.

Ở đây, chúng tôi chỉ định danh sách các trình kết nối, cho phép mô hình của cohere truy cập dữ liệu từ bên ngoài

nguồn.

Các trình kết nối được chỉ định dưới dạng một danh sách, nghĩa là chúng ta có thể cho phép truy cập không chỉ một mà còn

đến vô số kết nối.

Ở đây, chúng tôi sử dụng trình kết nối với tìm kiếm trên web ID.

Đó chính là trình kết nối tìm kiếm trên web mà chúng tôi đã sử dụng trên giao diện web ở phần trước.

Cuối cùng, thứ ba, chúng tôi in câu trả lời do mô hình tạo ra và danh sách các nguồn web

được tư vấn để tạo ra câu trả lời, cùng với các truy vấn được đưa ra để lấy các tài liệu đó.

Điều đó cho phép chúng tôi xác minh rằng câu trả lời được tạo ra thực sự được ngụ ý bởi tài liệu nguồn.

Trong danh sách 8.2, trả lời các câu hỏi bằng thư viện Python của cohere.

Hãy thử nó.

Chuyển sang thư mục chứa mã và chạy lệnh sau trong terminal.

Thay thế ba dấu chấm bằng khóa truy cập mạch lạc của bạn.

Xem mã này?

Bạn sẽ nhận được câu trả lời tương tự như sau, được viết tắt một chút.

Xem mã này?

Chúng ta hãy có một cái nhìn gần hơn.

Phần đầu tiên của đầu ra, một, là câu trả lời do mô hình tạo ra.

Câu trả lời có vẻ hợp lý, nhưng chúng ta có thể tin được không?

Đây là nơi các phần còn lại của đầu ra phát huy tác dụng.

Phần giữa, hai, cung cấp cho chúng ta thông tin trên web được cohere sử dụng để thông báo cho kết quả được tạo

câu trả lời.

Các truy vấn web đó được chọn tự động dựa trên câu hỏi đầu vào.

Trong trường hợp này, các truy vấn web tương ứng chính xác với câu hỏi đầu vào.

Điều này không nhất thiết đúng đối với văn bản đầu vào dài hơn.

Ở cuối đầu ra, số ba, chúng tôi tìm thấy các đoạn văn bản và URL của tài liệu được truy xuất

thông qua các truy vấn trước đó.

Trong trường hợp này, đoạn văn bản được lấy từ tài liệu web.

Ví dụ, Stephen Paul Jobs sinh ngày 24 tháng 2 năm 1955 tại San Francisco, California.

Tạo một trường hợp tốt để hỗ trợ câu trả lời từ mô hình.