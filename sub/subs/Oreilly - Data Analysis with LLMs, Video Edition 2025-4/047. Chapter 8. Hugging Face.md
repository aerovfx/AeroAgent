# 047. Chương 8. Ôm Mặt dịch

---

Section 8.4, Hugging Face

Các nhà cung cấp như OpenAI đầu tư hàng triệu đô la để đào tạo các mô hình như GPT-40.

Kết quả của tất cả quá trình đào tạo tốn kém đó là các giá trị cho các tham số mô hình giúp mô hình hoạt động tốt nhất.

Sau khi đầu tư hết số tiền đó, chắc chắn bạn sẽ không muốn chia sẻ kết quả đào tạo một cách miễn phí phải không?

Do đó, các mô hình như GPT-40 thường đóng, có nghĩa là OpenAI không chia sẻ các giá trị tham số thu được từ quá trình đào tạo.

Lưu ý rằng OpenAI đã chia sẻ các mô hình khác, chẳng hạn như Whisper.

Thay vào đó, OpenAI xử lý lời nhắc cho bạn trên cơ sở hạ tầng của chính nó đồng thời tính phí xử lý cho bạn.

Đó là cách cuối cùng chúng tôi chi trả cho tất cả các khóa đào tạo mô hình đắt tiền đó.

Tuy nhiên, ngày càng có nhiều nhà cung cấp mô hình ngôn ngữ phải đối mặt với sự cạnh tranh từ khu vực nguồn mở cực kỳ sôi động.

Các trường đại học, công ty khởi nghiệp và những người đam mê đều đào tạo mô hình của riêng họ và thường đưa ra mô hình cũng như các giá trị tham số của chúng.

Điều đó cho phép bạn tự chạy các mô hình đó cục bộ trên cơ sở hạ tầng chuyên dụng của riêng bạn.

Đối với các mẫu máy nhỏ hơn, một chiếc máy tính xách tay có GPU thường là đủ.

Đối với các mô hình lớn hơn, bạn có thể cần sử dụng cụm GPU hoặc nhờ đến các nhà cung cấp đám mây chạy các mô hình nguồn mở đó cho bạn.

Bên cạnh những lợi thế tài chính tiềm năng, việc chạy các mô hình trên cơ sở hạ tầng của riêng bạn có thể rẻ hơn.

Những cân nhắc khác có thể khiến các mô hình đang chạy cục bộ trở thành cách duy nhất để tạo mô hình với mô hình lớn hơn.

Ví dụ: bạn có thể không muốn tin cậy các nhà cung cấp bên ngoài với dữ liệu đặc biệt nhạy cảm.

Nếu bạn không muốn gửi dữ liệu của mình, chạy cục bộ là lựa chọn duy nhất.

Thông thường, các mô hình nguồn mở nhỏ hơn đáng kể so với các mô hình do nhà cung cấp đám mây cung cấp.

Điều đó có lý vì xét cho cùng, ai có vài triệu đô la để đào tạo một người mẫu?

Tuy nhiên, do có quá nhiều mô hình sẵn có nên thường có thể tìm thấy một mô hình nguồn mở chuyên dụng trên thị trường.

Ví dụ: nền tảng Hugging Face Transformers có hơn 1 triệu mẫu máy biến áp tại thời điểm viết bài.

Bất kể nhiệm vụ nào bạn đang phải đối mặt, rất có thể bạn sẽ tìm thấy đúng mô hình.

Trong phần này, chúng ta sẽ xem xét nền tảng Ôm Mặt và xem cách sử dụng các mô hình của nó tại địa phương.

Phần 8.4.1 Nền tảng web

Truy cập HuggingFace.co

Ôm mặt Transformers cung cấp nhiều tài nguyên khác nhau xung quanh các mô hình máy biến áp.

Điều đó không chỉ bao gồm các mô hình mà còn cả các tập dữ liệu mà bạn có thể sử dụng để huấn luyện các mô hình của riêng mình cũng như các dịch vụ đám mây cho phép bạn chạy các mô hình nguồn mở trên Cơ sở hạ tầng đám mây Ôm Mặt.

Hiện tại, chúng tôi quan tâm đến các mô hình. Bấm vào nút Mô hình để xem danh sách các mô hình được hiển thị trong Hình 8.4.

Hình 8.4

Tổng quan về các mẫu máy biến áp ôm mặt. Bấm vào bộ lọc Nhiệm vụ để thu hẹp lựa chọn. Nhấp vào Mô hình trong danh sách Mô hình để xem chi tiết.

Chúng tôi đang xem danh sách hơn 1 triệu mẫu máy biến áp và khi số lượng mẫu máy tăng lên hàng ngày, bạn có thể sẽ thấy nhiều hơn nữa.

Ôi! Đó là một vài quá nhiều. Hãy thu hẹp chúng lại.

Ở phía bên trái của màn hình là các tùy chọn bộ lọc khác nhau để đưa danh sách xuống những mẫu máy bạn thực sự quan tâm.

Chẳng hạn, chúng ta có thể lọc theo loại nhiệm vụ mà chúng ta cần mô hình thực hiện.

Điều này bao gồm Phân loại văn bản, chẳng hạn như Phân loại bài đánh giá theo cảm xúc, Trả lời câu hỏi bằng hình ảnh, ví dụ: Hình ảnh có hiển thị quả táo không và Chuyển lời nói thành văn bản, ví dụ: Chuyển câu hỏi bằng giọng nói thành văn bản.

Đối với hầu hết các nhiệm vụ chúng ta đã thảo luận trong cuốn sách này, bạn có thể tìm thấy một mô hình chuyên dụng. Nhấp vào bất kỳ nhiệm vụ tiêu chuẩn nào để chỉ xem các mô hình giải quyết nhiệm vụ đó.

Khi bạn nhấp vào một trong các mô hình còn lại, bạn sẽ thấy mô tả mô hình chi tiết như được hiển thị trong Hình 8.5 cho mô hình Blip của Salesforce, một mô hình xử lý hình ảnh để tạo chú thích phù hợp.

Ở phía bên trái là phần mô tả kỹ lưỡng về mô hình, cùng với các liên kết đến các giấy tờ liên quan và các mẫu mã chỉ cho bạn cách sử dụng mô hình trên các nền tảng phần cứng khác nhau, tức là cục bộ.

Bên phải là giao diện cho phép bạn thử mô hình trên một vài bức ảnh mẫu.

Hình 8.5 mô tả chi tiết mô hình Blip của Salesforce tại link này. Đọc mô tả bên trái hoặc thử mô hình qua giao diện bên phải.

Phần 8.4.2, Thư viện Python. Hiện chúng tôi sắp chạy các mô hình máy biến áp trên cơ sở hạ tầng địa phương của mình. Tất nhiên, hiệu suất bạn nhận được sẽ phụ thuộc vào đặc tính của phần cứng bạn đang sử dụng.

Tuy nhiên, ngay cả với sức mạnh tính toán vừa phải, bạn vẫn có thể làm việc với các mô hình mà chúng tôi sắp thử.

Nhưng trước tiên chúng ta phải cài đặt Thư viện Hugging Face Transformers. Nhập các lệnh sau vào terminal của bạn.

Xem mã này?

Thư viện Transformers dựa trên PyTorch, một framework máy học phổ biến. Nếu bạn chưa cài đặt PyTorch, hãy chạy lệnh sau trong terminal của bạn. Nếu không, bạn sẽ nhận được thông báo lỗi khi cố chạy đoạn mã sau.

Xem mã này?

Thế là xong phần thiết lập. Chúng tôi đã sẵn sàng sử dụng Thư viện Máy biến áp ôm mặt mà chúng tôi nhập thông qua máy biến áp nhập khẩu. Thư viện Transformers cung cấp rất nhiều tính năng và nhiều cách khác nhau để sử dụng mô hình trong kho lưu trữ của nó hoặc để đào tạo các mô hình của riêng bạn cho vấn đề đó.

Trong phần này, chúng tôi sẽ chỉ đề cập đến một nhóm nhỏ trong số đó nhưng cũng đủ để tạo ấn tượng đầu tiên.

Giả sử rằng bạn đã tìm thấy một mô hình trong kho lưu trữ Mô hình khuôn mặt ôm mà bạn muốn thử.

Để làm cho mọi thứ cụ thể hơn, giả sử chúng ta đang nói về mô hình Roberta để phân loại cảm tính, do Đại học Cardiff đưa ra. Bạn có thể tìm thấy mô hình đó tại liên kết này.

So với GPT-40 và hầu hết các mẫu khác được thảo luận trong phần này, đây là một mẫu khá nhỏ. Tuy nhiên, nó chuyên dùng để phân tích văn bản nhằm xác định cảm xúc cơ bản.

Mặc dù nó ít chung chung hơn nhiều so với GPT-40 và các mẫu tương tự, nhưng nó thực hiện một nhiệm vụ và thực hiện khá tốt. Ví dụ: nếu bạn đang muốn phân loại các bài đánh giá, bạn có thể thấy mô hình này rất phù hợp.

Cách dễ nhất để sử dụng mô hình thông qua Thư viện Transformers là thông qua một đường dẫn. Lệnh sau tạo quy trình phân loại cảm tính dựa trên mô hình Roberta.

Xem mã này?

Như bạn thấy, chúng tôi chỉ định mô hình bằng cách sử dụng phần cuối cùng của URL, tên tài khoản cung cấp mô hình, Cardiff NLP, theo sau là ID của chính mô hình đó.

Khi sử dụng mã này lần đầu tiên, Thư viện Transformers sẽ tự động tải xuống mô hình từ kho lưu trữ mô hình công khai của nó.

Lưu ý rằng mã này hoạt động một phần vì mô hình mà chúng tôi đang đề cập đến được liên kết với một lớp nhiệm vụ cụ thể.

Đối với các mô hình khác, bạn có thể phải chỉ định loại nhiệm vụ mà bạn muốn chúng giải quyết dưới dạng tham số đầu vào riêng biệt.

Chúng tôi đã tạo ra một đường ống. Bây giờ chúng ta có thể sử dụng nó để phân loại văn bản. Ví dụ, như vậy, chúng tôi giả sử rằng biến văn bản gạch dưới để phân loại gạch dưới có chứa văn bản cần phân loại.

Xem mã này?

Chúng tôi có tất cả những gì cần thiết để xây dựng một ứng dụng đơn giản giúp phân loại các đánh giá dựa trên cảm xúc cơ bản là tích cực, nghĩa là đánh giá tốt hay tiêu cực, nghĩa là đánh giá xấu.

Danh sách tiếp theo hiển thị mã tương ứng. Bạn có thể tìm thấy nó dưới dạng vật phẩm ôm mặt trên trang web của cuốn sách.

Liệt kê 8.4. Phân loại tình cảm bằng cách ôm mặt Transformers.

Bạn có thể nhận thấy sự khác biệt so với tất cả các mã mà chúng ta đã thấy trước đây. Chúng tôi không cần chỉ định khóa truy cập vì các mẫu khuôn mặt ôm đã được cung cấp công khai và chúng tôi đang chạy chúng trên cơ sở hạ tầng của riêng mình.

Không cần phải cung cấp bất kỳ loại thông tin xác thực nào. Thay vào đó, đầu vào duy nhất là văn bản đánh giá mà chúng tôi muốn phân loại.

Mã này bao gồm các đoạn được thảo luận trước đó. Nó tạo ra một đường ống. Một, sử dụng nó để phân loại văn bản đầu vào, hai, và cuối cùng in ra kết quả. Ba.

Bạn có thể thử bằng cách chuyển sang thư mục chứa trong thiết bị đầu cuối của bạn và nhập ví dụ như sau.

Xem mã này.

Khi chạy mã lần đầu tiên, bạn có thể phải đợi vài phút trong khi Thư viện Transformers tải xuống mô hình mà bạn đang tham chiếu. Nhưng đừng lo lắng. Thư viện lưu trữ mô hình đã tải xuống để bạn không phải đợi khi chạy mã lần thứ hai.

Sau khi quá trình xử lý kết thúc, bạn sẽ thấy kết quả như thế này.

Xem mã này.

Điều đó chắc chắn đúng đối với đầu vào mẫu. Đánh giá ngắn gọn và tiêu cực 100%. Hãy thử nó với một vài đánh giá khác nhau và so sánh kết quả đầu ra với những gì bạn nhận được với các mẫu như GPT-40.

Trong phần lớn các trường hợp, kết quả phân loại sẽ khá giống nhau. Tất nhiên, GPT-40 là một mô hình chung hơn nhiều và có thể được sử dụng để giải quyết nhiều nhiệm vụ khác.

Nhưng miễn là bạn quan tâm đến việc phân loại đánh giá, mô hình này mang đến sự cân bằng thú vị giữa chất lượng và chi phí.