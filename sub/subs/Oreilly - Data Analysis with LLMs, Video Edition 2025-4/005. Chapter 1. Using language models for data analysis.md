# 005. Chương 1. Dịch thuật sử dụng mô hình ngôn ngữ để phân tích dữ liệu

---

Phần 1.4, sử dụng mô hình ngôn ngữ để phân tích dữ liệu.

Vậy làm cách nào để sử dụng các mô hình ngôn ngữ cụ thể để phân tích dữ liệu?

Cuốn sách này xem xét hai khả năng.

Đầu tiên, chúng ta có thể sử dụng mô hình ngôn ngữ trực tiếp trên dữ liệu.

Điều này có nghĩa là mô hình ngôn ngữ nhận dữ liệu mà chúng tôi muốn phân tích như một phần của lời nhắc,

cùng với hướng dẫn về cách phân tích cần thực hiện.

Thứ hai, chúng ta có thể sử dụng mô hình ngôn ngữ một cách gián tiếp để phân tích dữ liệu.

Ở đây, mô hình ngôn ngữ không trực tiếp nhìn thấy dữ liệu,

nghĩa là chúng tôi không đưa toàn bộ dữ liệu vào lời nhắc.

Thay vào đó, chúng tôi sử dụng mô hình ngôn ngữ để viết mã xử lý dữ liệu,

được thực hiện bằng các công cụ xử lý dữ liệu chuyên dụng,

Việc sử dụng cách tiếp cận nào phụ thuộc vào thuộc tính dữ liệu và nhiệm vụ.

Chúng ta hãy xem xét kỹ hơn về cả hai phương pháp.

Section 1.4.1, using language models directly on data.

Cách tiếp cận tự nhiên nhất để phân tích dữ liệu bằng mô hình ngôn ngữ

là đưa dữ liệu trực tiếp vào dấu nhắc.

Đây là những gì chúng tôi đã làm trong Phần 1.3.2.

Để phân tích bài đánh giá, chúng tôi đưa văn bản đánh giá vào lời nhắc,

cùng với hướng dẫn về những việc cần làm với văn bản.

Chúng ta có thể sử dụng cách tiếp cận tương tự cho các loại dữ liệu khác ngoài văn bản.

Ví dụ: khi sử dụng các mô hình đa phương thức như GPT-40,

chúng ta có thể chỉ cần đưa những hình ảnh vào để phân tích,

thay vì có hướng dẫn phân tích trong lời nhắc.

Thông thường, chúng tôi không muốn phân tích một bức ảnh hoặc bài đánh giá nào,

nhưng là cả một bộ sưu tập của chúng.

Ví dụ: giả sử rằng chúng tôi muốn phân loại toàn bộ bộ sưu tập đánh giá,

xác định cho mỗi người trong số họ xem đánh giá là tích cực hay tiêu cực.

Trong những trường hợp như vậy, chúng tôi thường áp dụng cách tiếp cận sau:

được triển khai bằng Python bằng thư viện Python của OpenAI,

hoặc một thư viện tương đương cho phép người dùng gửi lời nhắc đến mô hình của nhà cung cấp khác.

Chúng tôi tải các bài đánh giá để phân loại và tạo một lời nhắc cho mỗi bài đánh giá.

Sau đó, chúng tôi gửi những lời nhắc đó đến mô hình ngôn ngữ,

trích xuất kết quả phân loại từ câu trả lời do mô hình tạo ra cho mỗi lần đánh giá,

và lưu kết quả vào một tập tin trên đĩa.

Trong kịch bản này, chúng tôi muốn giải quyết cùng một nhiệm vụ,

phân loại đánh giá, đối với nhiều tài liệu văn bản, nghĩa là đánh giá.

Như bạn có thể tưởng tượng, do đó, lời nhắc cho các đánh giá khác nhau sẽ có một số điểm tương đồng.

Mặc dù văn bản rà soát để phân loại thay đổi theo từng thời kỳ,

mô tả nhiệm vụ và các phần khác của lời nhắc vẫn giữ nguyên.

Để tạo lời nhắc trong Python, chúng tôi sử dụng mẫu lời nhắc.

Mẫu lời nhắc chỉ định lời nhắc liên quan đến một nhiệm vụ cụ thể cần giải quyết.

Trong ví dụ của chúng tôi, chúng tôi sẽ sử dụng mẫu lời nhắc để phân loại đánh giá là tích cực hoặc tiêu cực.

Mẫu lời nhắc chứa phần giữ chỗ để thể hiện các phần của lời nhắc thay đổi tùy thuộc vào dữ liệu đầu vào.

Xem xét mẫu nhắc nhở của chúng tôi để phân loại đánh giá,

có lẽ chúng ta nên bao gồm một trình giữ chỗ cho văn bản đánh giá.

Sau đó, khi tạo lời nhắc bằng Python, chúng tôi thay thế phần giữ chỗ đó bằng văn bản của bài đánh giá hiện tại để phân loại.

Ví dụ: chúng ta có thể sử dụng mẫu lời nhắc sau để phân loại các bài đánh giá.

Danh sách 1.2, mẫu nhắc nhở để phân loại đánh giá máy tính xách tay.

Mẫu lời nhắc này tóm tắt lời nhắc mà chúng tôi thấy để phân loại một đánh giá cụ thể,

hãy xem danh sách 1.1 trong phần 1.3.2.

Một lần nữa, chúng tôi cung cấp bối cảnh, thực tế là chúng tôi đang phân loại các bài đánh giá máy tính xách tay,

một và hướng dẫn mô tả nhiệm vụ cần giải quyết, cũng như định dạng đầu ra, hai.

Chúng tôi cũng cung cấp một số đánh giá mẫu kèm theo các kết quả phân loại liên quan, ba và bốn.

Mặc dù việc rà soát để phân loại thay đổi nhưng tùy theo đầu vào mà chúng ta không cần phải thay đổi các đánh giá ví dụ.

Những đánh giá đó chỉ minh họa nhiệm vụ mà mô hình ngôn ngữ cần giải quyết.

Cuối cùng, 5. Chúng ta có một phần giữ chỗ cho văn bản đánh giá.

Khi lặp lại các bài đánh giá khác nhau, chúng tôi tạo lời nhắc cho từng bài đánh giá bằng cách thay thế văn bản đánh giá cho phần giữ chỗ này.

Mẫu lời nhắc ví dụ chỉ có một phần giữ chỗ duy nhất.

Nói chung, một số phần của lời nhắc có thể thay đổi tùy thuộc vào dữ liệu đầu vào.

Nếu vậy, chúng tôi sẽ giới thiệu phần giữ chỗ cho từng phần đó và thay thế tất cả chúng để tạo lời nhắc.

Hình 1.2 tóm tắt cách chúng tôi sử dụng mẫu nhắc nhở khi phân tích dữ liệu trực tiếp bằng mô hình ngôn ngữ.

Đối với mỗi mục dữ liệu, chẳng hạn như đánh giá để phân loại, chúng tôi thay thế phần giữ chỗ trong mẫu lời nhắc để tạo lời nhắc.

Chúng ta cũng có thể nói rằng chúng ta khởi tạo một lời nhắc.

Sau đó, chúng tôi gửi lời nhắc này đến mô hình ngôn ngữ để giải quyết nhiệm vụ phân tích dữ liệu mà chúng tôi quan tâm.

Hình 1.2 sử dụng mô hình ngôn ngữ trực tiếp để phân tích dữ liệu.

Mẫu lời nhắc mô tả nhiệm vụ phân tích.

Nó chứa các phần giữ chỗ được thay thế bằng dữ liệu để phân tích.

Sau khi thay thế phần giữ chỗ, lời nhắc thu được sẽ được gửi tới mô hình ngôn ngữ để tạo đầu ra.

Mục 1.4.2 phân tích dữ liệu thông qua các công cụ bên ngoài.

Đưa dữ liệu trực tiếp vào lời nhắc không phải lúc nào cũng là cách tiếp cận hiệu quả nhất.

Đối với một số loại dữ liệu, có sẵn các công cụ chuyên dụng để xử lý các thao tác nhất định trên dữ liệu đó một cách rất hiệu quả.

Trong những trường hợp đó, việc sử dụng mô hình ngôn ngữ để viết mã xử lý dữ liệu thường hiệu quả hơn là phân tích dữ liệu trực tiếp.

Mã do mô hình ngôn ngữ tạo ra sau đó có thể được thực thi bằng công cụ chuyên dụng.

Chúng tôi sẽ áp dụng phương pháp này cho dữ liệu có cấu trúc.

Đối với dữ liệu có cấu trúc như bảng dữ liệu và đồ thị, có sẵn các công cụ xử lý dữ liệu chuyên dụng hỗ trợ nhiều hoạt động phân tích.

Những hoạt động đó, chẳng hạn như lọc và tổng hợp dữ liệu, có thể được thực hiện rất hiệu quả trên dữ liệu có cấu trúc.

Ngay cả khi có thể thực hiện các hoạt động tương tự một cách đáng tin cậy với các mô hình ngôn ngữ, thì điều này không xảy ra, chúng tôi cũng không muốn làm điều đó vì các khoản phí chúng tôi phải trả cho các nhà cung cấp như OpenAI,

tỷ lệ thuận với kích thước của dữ liệu đầu vào.

Việc xử lý các tập dữ liệu có cấu trúc lớn, chẳng hạn như các bảng có hàng triệu hàng, sử dụng mô hình ngôn ngữ, cực kỳ tốn kém.

Trong các chương tiếp theo, chúng ta thảo luận về các loại công cụ sau để xử lý dữ liệu có cấu trúc.

Hệ quản trị cơ sở dữ liệu quan hệ.

Lưu trữ và xử lý dữ liệu quan hệ, tức là tập hợp các bảng dữ liệu.

Hầu hết các hệ thống quản lý cơ sở dữ liệu quan hệ đều hỗ trợ SQL, ngôn ngữ truy vấn có cấu trúc.

Chúng tôi sẽ sử dụng các mô hình ngôn ngữ để dịch các câu hỏi về dữ liệu sang các truy vấn trong SQL.

Hệ thống quản lý dữ liệu đồ thị xử lý dữ liệu đồ thị biểu diễn các thực thể và mối quan hệ giữa chúng.

Các hệ thống quản lý dữ liệu đồ thị khác nhau hỗ trợ các ngôn ngữ truy vấn khác nhau.

Trong chương 5, chúng ta xem cách sử dụng mô hình ngôn ngữ để dịch các câu hỏi về dữ liệu thành các truy vấn bằng ngôn ngữ mật mã, được hỗ trợ bởi hệ thống quản lý dữ liệu đồ thị Neo4j.

Ví dụ: giả sử chúng ta muốn kích hoạt mẫu OMPT của LayUser để dịch nhiệm vụ phân tích cơ sở dữ liệu quan hệ, tức là một tập hợp các bảng dữ liệu.

Có lẽ một bảng chứa kết quả của một cuộc khảo sát và chúng tôi muốn cho phép người dùng tổng hợp các câu trả lời từ các nhóm người trả lời khác nhau.

Kết quả khảo sát được lưu trữ trong hệ thống quản lý cơ sở dữ liệu quan hệ, loại công cụ phù hợp nhất với loại dữ liệu này.

Bằng cách sử dụng các mô hình ngôn ngữ, chúng tôi có thể cho phép người dùng đặt câu hỏi về dữ liệu bằng ngôn ngữ tự nhiên, nghĩa là bằng tiếng Anh đơn giản.

Mô hình ngôn ngữ đảm nhiệm việc chuyển những câu hỏi đó thành các truy vấn chính thức.

Chính xác hơn, do dữ liệu được lưu trữ trong hệ thống quản lý cơ sở dữ liệu quan hệ, chúng tôi muốn chuyển những câu hỏi đó thành truy vấn SQL.

Một lần nữa, chúng tôi giới thiệu mẫu lời nhắc cho nhiệm vụ mà chúng tôi quan tâm. Ở đây chúng tôi quan tâm đến việc dịch văn bản sang SQL, nghĩa là chúng tôi muốn sử dụng mô hình ngôn ngữ để dịch các câu hỏi bằng ngôn ngữ tự nhiên sang truy vấn SQL.

Mặc dù nhiệm vụ, dịch văn bản sang SQL và dữ liệu, cơ sở dữ liệu chứa kết quả khảo sát vẫn cố định nhưng các câu hỏi của người dùng sẽ thay đổi theo thời gian.

Do đó, chúng tôi giới thiệu một trình giữ chỗ cho câu hỏi của người dùng trong mẫu lời nhắc của chúng tôi. Về nguyên tắc, mẫu lời nhắc sau đây sẽ cho phép chúng tôi dịch các câu hỏi trên dữ liệu khảo sát của mình thành các truy vấn SQL.

Liệt kê 1.3, mẫu nhắc nhở để dịch các câu hỏi sang SQL.

Đầu tiên, lời nhắc mô tả cấu trúc dữ liệu của chúng tôi. 1. Điều này là cần thiết để cho phép hệ thống viết các truy vấn chính xác, ví dụ: các truy vấn tham chiếu đến tên chính xác của các bảng và cột trong các bảng đó.

Mô tả trong mẫu ví dụ được viết tắt. Chúng ta sẽ xem cách mô tả chính xác cấu trúc của cơ sở dữ liệu quan hệ trong các chương sau.

Tiếp theo, mẫu lời nhắc chứa câu hỏi cần dịch sang. Đây là phần giữ chỗ để cho phép người dùng đặt các câu hỏi khác nhau bằng cách sử dụng cùng một mẫu lời nhắc. Cuối cùng, mẫu lời nhắc chứa mô tả nhiệm vụ ngắn gọn. 3. Chúng tôi muốn chuyển câu hỏi sang truy vấn SQL.

Hình 1.3 tóm tắt quá trình dịch văn bản sang SQL. Đưa ra một mẫu lời nhắc tương ứng, chúng tôi thay thế câu hỏi của người dùng cho phần giữ chỗ, dịch câu hỏi sang truy vấn SQL thông qua mô hình ngôn ngữ và cuối cùng thực hiện truy vấn trong hệ thống quản lý cơ sở dữ liệu quan hệ. Kết quả truy vấn được hiển thị cho người dùng.

Hình 1.3 sử dụng mô hình ngôn ngữ gián tiếp để xây dựng giao diện ngôn ngữ tự nhiên cho dữ liệu dạng bảng. Mẫu lời nhắc chứa phần giữ chỗ cho các câu hỏi về dữ liệu.

Sau khi thay thế phần giữ chỗ, lời nhắc kết quả sẽ được sử dụng làm đầu vào cho mô hình ngôn ngữ. Mô hình chuyển câu hỏi thành một truy vấn SQL được thực thi thông qua hệ thống quản lý cơ sở dữ liệu quan hệ.