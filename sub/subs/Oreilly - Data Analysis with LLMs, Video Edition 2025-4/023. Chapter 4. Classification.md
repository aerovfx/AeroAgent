# 023. Chương 4. Phân loại dịch

---

Mục 4.2 Phân loại

Vì vậy, bạn đang lên kế hoạch cho buổi tối thứ bảy của mình và cân nhắc xem có nên đi xem

cài đặt mới nhất của nhượng quyền phim yêu thích của bạn.

Nhưng nó có đáng không?

Nguồn cấp dữ liệu truyền thông xã hội của bạn liên tục tràn ngập các nhận xét từ bạn bè của bạn và của bạn bè bạn.

bạn bè, mở rộng trải nghiệm xem phim của họ.

Bạn có thể duyệt qua chúng một cách thủ công, đọc từng cái một để hiểu rõ hơn liệu

Ý kiến đa số về bộ phim là tích cực hay tiêu cực, nhưng ai có thời gian để làm điều đó?

Các mô hình ngôn ngữ không thể giúp chúng ta tự động hóa tác vụ này sao?

Quả thực họ có thể.

Những gì chúng ta có ở đây là một ví dụ về một trong những vấn đề xử lý văn bản cổ điển nhất.

Chúng tôi có một văn bản và muốn phân loại nó, ánh xạ nó vào một trong các nhóm danh mục cố định.

Trong trường hợp này, văn bản cần phân loại là một bài đánh giá phim.

Chúng tôi muốn xếp nó vào loại tích cực, nghĩa là người viết cho rằng đây là một bộ phim hay và

bạn nên đi xem nó.

Hoặc tiêu cực, hãy tiết kiệm tiền của bạn.

Điều đó có nghĩa là chúng tôi có hai loại.

Bảng 4.1 trình bày các đoạn trích từ một số ví dụ đánh giá với các nhãn lớp liên quan.

Một bài đánh giá khen ngợi một bộ phim được thực hiện tốt rõ ràng là tích cực, trong khi một bài đánh giá mô tả

phim rõ ràng là yếu, rẻ tiền, tiêu cực.

Bạn có thể tìm thấy những đánh giá này và một số đánh giá khác trong tệp tương ứng trên sách đi kèm

trang web.

Bảng 4.1.

Trích xuất từ ​​các bài đánh giá phim và nhãn lớp liên quan.

Thêm đánh giá phim chỉ là một trong nhiều trường hợp sử dụng để phân loại văn bản.

Một ví dụ khác, hãy tưởng tượng bạn đang cố gắng sắp xếp hộp thư đến email của mình.

Chẳng phải sẽ rất tốt nếu tự động phân loại email dựa trên nội dung của chúng sao?

sử dụng các danh mục tùy chỉnh như công việc, sở thích, chăm sóc trẻ em, v.v.?

Đó là một ví dụ khác về phân loại văn bản, lần này có nhiều hơn hai danh mục.

Ví dụ cuối cùng, hãy tưởng tượng rằng bạn đang tạo một trang web cho phép người dùng thoát khỏi

bình luận văn bản.

Tất nhiên bạn không muốn hiển thị những bình luận có khả năng gây khó chịu và muốn lọc

chúng tự động xuất hiện.

Một lần nữa, điều đó có nghĩa là bạn đang phân loại nhận xét văn bản thành một trong hai loại, xúc phạm

và không gây khó chịu.

Bây giờ chúng ta sẽ xem các mô hình ngôn ngữ có thể dễ dàng được sử dụng như thế nào cho từng kịch bản.

Tổng quan về phần 4.2.1.

Chúng tôi sẽ tập trung vào việc phân loại các bài đánh giá phim hoặc bất kỳ loại đánh giá nào thành tích cực,

bộ phim hay và những đánh giá tiêu cực về việc ở nhà.

Để làm được điều đó, chúng tôi sẽ sử dụng các mô hình ngôn ngữ của OpenAI.

Chúng tôi giả định rằng chúng tôi đã thu thập các bài đánh giá để phân loại vào một tệp trên đĩa.

Mã chúng tôi phát triển sẽ lặp lại tất cả các đánh giá, phân loại từng đánh giá bằng mô hình ngôn ngữ,

và trả về kết quả phân loại cho mỗi lần xem xét.

Nhưng làm thế nào chúng ta có thể phân loại đánh giá?

Chúng ta sẽ sử dụng thư viện Python của OpenAI, được trình bày trong chương 3.

Để phân loại mỗi đánh giá, trước tiên chúng tôi sẽ tạo lời nhắc.

Lời nhắc mô tả một nhiệm vụ cho một mô hình ngôn ngữ.

Trong trường hợp của chúng tôi, nhiệm vụ đó chỉ định đánh giá cho một trong hai danh mục của chúng tôi, tích cực hoặc tiêu cực.

Ví dụ, hãy xem lời nhắc sau đây làm ví dụ.

Xem mã này.

Lời nhắc này chứa phần đánh giá để phân loại một, một câu hỏi mô tả việc phân loại

nhiệm vụ thứ hai và câu lệnh cuối cùng mô tả định dạng đầu ra mong muốn, thứ ba.

Chúng tôi sẽ xây dựng lời nhắc kiểu này cho mỗi lần đánh giá, gửi lời nhắc tới ngôn ngữ

mô hình và hy vọng nhận được một trong hai câu trả lời có thể có, tích cực hoặc tiêu cực.

Hình 4.1 minh họa quy trình phân loại cấp cao cho mỗi lần đánh giá.

Hình 4.1.

Đối với mỗi đánh giá, chúng tôi tạo lời nhắc chứa đánh giá cùng với hướng dẫn mô tả

nhiệm vụ phân loại.

Lấy lời nhắc làm đầu vào, mô hình ngôn ngữ sẽ xuất ra nhãn lớp để đánh giá.

Phần 4.2.2, tạo lời nhắc.

Sau khi xem xét, chúng tôi tạo lời nhắc hướng dẫn mô hình ngôn ngữ phân loại nó.

Tất cả các lời nhắc mà chúng tôi tạo ra để phân loại đều tuân theo cùng một mẫu lời nhắc.

Xin nhắc lại, mẫu lời nhắc là gì?

Chúng tôi đã đề cập ngắn gọn về các mẫu nhắc nhở trong chương 1.

Mẫu lời nhắc là văn bản chứa phần giữ chỗ.

Bằng cách thay thế văn bản thực tế cho các phần giữ chỗ này, chúng tôi nhận được lời nhắc mà chúng tôi có thể gửi tới

mô hình ngôn ngữ

Chúng tôi cũng nói rằng lời nhắc sẽ khởi tạo mẫu lời nhắc nếu có thể nhận được lời nhắc

bằng cách thay thế phần giữ chỗ của mẫu.

Lời nhắc ví dụ từ phần trước sẽ khởi tạo mẫu lời nhắc sau.

Xem mã này?

Mẫu của chúng tôi chỉ chứa một trình giữ chỗ duy nhất, văn bản đánh giá để phân loại, một.

Đối với mỗi bài đánh giá, chúng tôi sẽ thay thế phần giữ chỗ này bằng văn bản đánh giá thực tế.

Chúng tôi cũng hướng dẫn mô hình ngôn ngữ về những việc cần làm với văn bản đánh giá, hai.

Kiểm tra xem tình cảm cơ bản là tích cực hay tiêu cực và xác định định dạng đầu ra,

ba.

Bước thứ hai rất quan trọng vì có thể có nhiều cách để thể hiện tình cảm cơ bản.

Ví dụ: P là tích cực và N là tiêu cực hoặc câu trả lời dài hơn chẳng hạn như đánh giá là tích cực.

Nếu chúng ta không yêu cầu mô hình ngôn ngữ sử dụng một định dạng đầu ra cụ thể một cách rõ ràng thì nó có thể

chọn bất kỳ khả năng nào trong số này.

Trong kịch bản của chúng tôi, cuối cùng chúng tôi muốn tổng hợp các kết quả phân loại để tìm hiểu phần lớn

ý kiến, hầu hết mọi người có thích bộ phim hay không và tổng hợp kết quả từ mỗi bài đánh giá

trở nên đơn giản hơn nhiều nếu tất cả các phân loại tuân theo cùng một định dạng đầu ra.

Hàm sau tuân theo mẫu để tạo lời nhắc cho một đánh giá nhất định, được chỉ định

làm văn bản tham số đầu vào.

Xem mã này?

Kết quả của hàm là lời nhắc, khởi tạo mẫu để xem xét đầu vào.

Mục 4.2.3, gọi mô hình.

Tiếp theo, chúng tôi gửi lời nhắc được tạo đến mô hình ngôn ngữ để có được giải pháp.

Chính xác hơn, chúng tôi đang sử dụng mẫu GPT-4O của OpenAI, mẫu mới nhất của OpenAI tại thời điểm

viết.

Vì đây là một trong những mô hình trò chuyện của OpenAI, được tối ưu hóa cho tương tác nhiều bước với người dùng,

chúng tôi sử dụng điểm cuối hoàn thành trò chuyện để liên lạc với mô hình.

Như đã thảo luận chi tiết hơn trong chương 3, điểm cuối này mong đợi đầu vào là lịch sử của

tin nhắn trước đó, ngoài tên mẫu máy cụ thể.

Ở đây, chúng tôi chỉ có một thông báo trước đó, lời nhắc.

Chúng tôi phân loại nó dưới dạng thông báo của người dùng, khuyến khích mô hình giải quyết bất kỳ nhiệm vụ nào được mô tả

trong tin nhắn.

Ví dụ: chúng ta có thể gửi lời nhắc đến mô hình ngôn ngữ và thu thập câu trả lời bằng cách sử dụng lệnh sau

đoạn mã, giả sử rằng lời nhắc đó chứa văn bản lời nhắc được tạo trước đó.

Bạn có thấy mã này không?

Tuy nhiên, sử dụng mã này trực tiếp là có vấn đề.

Các mô hình GPT của OpenAI được lưu trữ trực tuyến và truy cập từ xa.

Điều này tạo cơ hội cho những nỗ lực thất bại để đạt được điểm cuối tương ứng, ví dụ:

do mất kết nối tạm thời.

Do đó, cách tốt nhất là cho phép thử lại một vài lần khi gọi mô hình.

Đặc biệt, khi xử lý các tập dữ liệu lớn cần nhiều lệnh gọi liên tiếp để mở

Trong mô hình của AI, khả năng xảy ra ít nhất một cuộc gọi không thành công sẽ tăng lên.

Thay vì làm gián đoạn quá trình tính toán với một ngoại lệ, tốt hơn là đợi vài giây

trước khi bắt đầu thử lại.

Đây là phiên bản hoàn chỉnh của mã trước đó, một hàm gọi mô hình ngôn ngữ

với các lần thử lại tự động.

Xem mã này?

Chức năng LM cuộc gọi cho phép tối đa ba lần thử lại với độ trễ tăng dần giữa các lần thử đó.

Độ trễ này được thực hiện bằng lệnh gọi hàm time.sleep bằng thư viện thời gian của Python bất cứ khi nào

gặp phải một ngoại lệ, chẳng hạn như cho biết mất kết nối tạm thời.

Sau ba lần thử lại, hàm này không thành công với một ngoại lệ, giả định một cách bi quan rằng

bất kỳ vấn đề nào ngăn cản chúng tôi liên hệ với openAI sẽ không được giải quyết sớm.

Bất cứ khi nào cuộc gọi thành công, hàm sẽ trả về kết quả tương ứng.

Trong 4.2.4, mã phân loại từ đầu đến cuối.

Đã đến lúc kết hợp tất cả lại với nhau.

Danh sách tiếp theo hiển thị mã phù hợp với quy trình phân loại mà chúng ta đã thảo luận.

Nó cũng chứa chức năng tạo lời nhắc, hai và một để gọi ngôn ngữ

mô hình, ba.

Liệt kê 4.1.

Phân loại văn bản đầu vào theo cảm tính, tích cực tiêu cực.

Đầu tiên, hãy thảo luận về các thư viện được sử dụng trong danh sách 4.1.

Chúng tôi sẽ sử dụng lại các thư viện đó cho các dự án sau, vì vậy sẽ rất hợp lý nếu có một sự tiếp cận gần hơn

hãy nhìn vào chúng và tại sao chúng ta cần chúng ở đây.

Chúng tôi muốn bắt đầu mã của mình từ dòng lệnh, chỉ định các tham số có liên quan, ví dụ:

đường dẫn của dữ liệu đầu vào, làm đối số.

Thư viện argPars có các hàm hữu ích để chỉ định và đọc các đối số dòng lệnh như vậy.

Tiếp theo, chúng ta cần thư viện openAI, đã thảo luận ở chương 3, để gọi mô hình ngôn ngữ của openAI

từ Python.

Thư viện của gấu trúc hỗ trợ các thao tác tiêu chuẩn trên dữ liệu dạng bảng.

Tất nhiên, dữ liệu dạng bảng không phải là trọng tâm của chúng ta trong chương này.

Tuy nhiên, chúng tôi sẽ lưu trữ tài liệu văn bản và siêu dữ liệu liên quan dưới dạng hàng và bảng, do đó, gấu trúc

thư viện có ích.

Cuối cùng, như đã thảo luận trước đó, chúng tôi sử dụng thư viện thời gian để triển khai các lần thử lại bị trì hoãn

khi gọi mô hình ngôn ngữ.

Trong phần 4.2.5, phân loại tài liệu.

Việc phân loại một tài liệu văn bản duy nhất, 4, kết hợp hai chức năng đã thảo luận trước đó.

Đưa ra một văn bản đầu vào để phân loại, trước tiên mã sẽ tạo một dấu nhắc tương ứng, gọi

để tạo lời nhắc và sau đó tạo câu trả lời phù hợp thông qua lệnh gọi đến mô hình ngôn ngữ,

gọi để gọi LLM.

Kết quả được coi là nhãn lớp và được trả về cho người dùng.

Bây giờ chúng ta ghép nó lại với nhau, 5.

Phần mã này được thực thi khi gọi mô-đun Python từ dòng lệnh và

sử dụng các chức năng chúng tôi đã giới thiệu.

Điều kiện if ban đầu, 5, đảm bảo rằng đoạn mã sau chỉ được thực thi khi gọi

trực tiếp mô-đun, thay vì nhập nó từ một mô-đun khác.

Đầu tiên, 6.

Chúng tôi xác định các đối số dòng lệnh.

Ở đây chúng ta chỉ cần một đối số, một đường dẫn đến tệp .csv chứa dữ liệu cần phân loại.

Chúng tôi giả định rằng mỗi hàng chứa một tài liệu văn bản và văn bản cần phân loại được chứa

trong cột văn bản.

Chúng tôi phân tích cú pháp các đối số dòng lệnh và cung cấp giá trị của chúng trong biến args.

Tiếp theo, chúng tôi tải dữ liệu đầu vào từ đĩa, 7.

Chúng tôi giả định rằng dữ liệu được lưu trữ dưới dạng tệp .csv, giá trị được phân tách bằng dấu phẩy, nghĩa là dòng tiêu đề

với tên cột, theo sau là dòng chứa dữ liệu.

Các trường được phân tách bằng dấu phẩy, như tên cho thấy.

Ở đây, thư viện pandas rất hữu ích và cho phép chúng ta tải dữ liệu đó chỉ bằng một

lệnh.

Biến df sau đó chứa khung dữ liệu gấu trúc chứa dữ liệu từ tệp đầu vào.

Chúng tôi truy xuất cột văn bản khung dữ liệu, 8 và áp dụng chức năng phân loại được xác định trước đó

vào mỗi hàng, sử dụng phương pháp áp dụng gấu trúc.

Cuối cùng là 9.

Chúng tôi tạo và in ra số liệu thống kê tổng hợp.

Số lần xuất hiện cho mỗi câu trả lời do mô hình tạo ra và viết kết quả

phân loại thành một tệp, result.csv.

Phần 4.2.6, chạy mã.

Trên trang web Books Companion, hãy tải xuống tệp review.csv.

Tệp này chứa một số lượng nhỏ các bài đánh giá phim mà chúng tôi có thể sử dụng để phân loại.

Tệp chứa hai cột, văn bản đánh giá và cảm tính liên quan, phủ định cho tiêu cực

tình cảm, và thể hiện tình cảm tích cực.

Tất nhiên, mục tiêu của chúng tôi là tự động phát hiện những cảm xúc như vậy.

Tuy nhiên, việc có được sự thật cơ bản cũng cho phép chúng ta đánh giá chất lượng của các phân loại.

Bạn có thể kiểm tra mã để phân loại như được mô tả tiếp theo.

Các lệnh sau đã được thử nghiệm trên hệ điều hành Linux.

Sử dụng thiết bị đầu cuối, thay đổi thư mục chứa mô-đun Python, liệt kê 1.py, với

mã nhập ngũ 4.1.

Sau đó, chạy lệnh sau, thay thế Python bằng tên trình thông dịch Python của bạn,

chẳng hạn như Python 3, nếu cần.

Xem mã này?

Ở đây, chúng tôi giả định rằng tệp đầu vào, review.csv, được lưu trữ trong cùng kho lưu trữ với mã.

Nếu không, bạn phải thay thế đường dẫn tương ứng cho tên tệp.

Thông thường, mã sẽ không mất quá vài giây để thực thi.

Chỉ thêm một chút nếu kết nối của bạn không ổn định, cần phải thử lại.

Nếu thực hiện thành công, đầu ra duy nhất bạn sẽ thấy tóm tắt số lượng nhãn được chỉ định

cho mỗi trong hai lớp có thể.

Sau khi thực thi mã, bạn sẽ tìm thấy tệp result.csv trong cùng một kho lưu trữ.

Ngoài các cột của tệp đầu vào, tệp kết quả còn chứa một cột lớp mới.

Cột này chứa các kết quả phân loại, tích cực và tiêu cực.

Ở đây, nhãn được bộ phân loại của chúng tôi gán cho cảm xúc về sự thật cơ bản.

Bạn sẽ thấy rằng sự phân loại nhất quán trong phần lớn các trường hợp.

Không tệ cho một vài dòng mã Python phải không?

Phần 4.2.7, thử phương sai.

Tại thời điểm này, bạn nên thử thêm một chút với mã và dữ liệu để có được

cảm nhận tốt hơn về cách nó hoạt động.

Ví dụ: hãy thử tự mình viết một vài bài phê bình phim.

Đối với những đánh giá nào thì việc phân loại là đáng tin cậy và nó thách thức ở đâu?

Ngoài ra, hãy thử một vài biến thể của lời nhắc, hướng dẫn nào sẽ mang lại độ chính xác cao hơn và

làm suy giảm hiệu suất.

Để chỉ lấy một biến thể mẫu, hãy thử xóa phần lời nhắc xác định đầu ra

định dạng chính xác, câu trả lời dòng, tích cực hoặc tiêu cực.

Bây giờ hãy thử chạy chương trình với lời nhắc đã thay đổi.

Điều gì xảy ra?

Rất có thể, bạn sẽ thấy nhiều hơn hai nhãn trong kết quả phân loại của mình, trong

đầu ra của chương trình, bao gồm cả các chữ viết tắt, ví dụ P và N.

như những câu trả lời quá chi tiết.

Ví dụ: trong quá trình thử nghiệm, GPT 4O đã tạo ra các câu trả lời như cảm tính của bài đánh giá này

là tích cực.

Trong chương 9, chúng tôi đánh giá tác động của các gợi ý khác nhau đến chất lượng đầu ra của mô hình.

Bạn cũng có thể muốn thay đổi mô hình được sử dụng để trích xuất.

Còn việc sử dụng một trong những phiên bản mô hình nhỏ hơn, chẳng hạn như GPT 3.5, rẻ hơn đáng kể

mỗi mã thông báo được xử lý?

Và cấu hình model như thế nào?

Liệt kê 4.1 chỉ sử dụng hai tham số, tên model và lịch sử thông báo, cả hai đều

được yêu cầu.

Tuy nhiên, trong chương 3, chúng ta đã thấy nhiều thông số cấu hình khác nhau có thể áp dụng ở đây.

Ví dụ, hãy thử thay đổi thông số nhiệt độ.

Ví dụ: cài đặt nhiệt độ về 0 sẽ cho bạn kết quả xác định hơn hoặc hạn chế

độ dài của đầu ra mong muốn.

Trong một số ít trường hợp, mô hình GPT có thể tạo văn bản đầu ra dài hơn phân loại mong muốn

kết quả, bao gồm một mã thông báo duy nhất.

Bạn có thể tránh điều đó bằng cách giới hạn độ dài đầu ra bằng tham số mã thông báo tối đa.

Đồng thời, thay vì chỉ hạn chế định dạng đầu ra thông qua các hướng dẫn trong

lời nhắc, bạn có thể tăng khả năng xảy ra hai kết quả có thể xảy ra, tích cực và

âm, sử dụng tham số sai lệch logic.

Chúng ta sẽ thảo luận sâu hơn về việc điều chỉnh mô hình trong chương 9.

Là một biến thể khác, hãy thử thay đổi nhiệm vụ phân loại.

Ví dụ: việc phân loại bằng cách sử dụng một nhóm danh mục khác là tương đối dễ dàng.

Tất cả những gì cần làm là thay đổi hướng dẫn trong lời nhắc, phác thảo tất cả các tùy chọn trả lời

như trước đây.

Bằng cách thay đổi một vài dòng mã, bạn thậm chí có thể có được một công cụ phân loại linh hoạt

cho phép người dùng chỉ định nhiệm vụ phân loại và các lớp tương ứng dưới dạng bổ sung

đối số dòng lệnh.

Ví dụ: ngoài các bài đánh giá phim, bạn có thể sử dụng công cụ này để phân loại các bài báo

vào một trong một số danh mục chủ đề hoặc để phân loại email là khẩn cấp hoặc không khẩn cấp.

Đến bây giờ, bạn hy vọng đã bị thuyết phục rằng các mô hình ngôn ngữ cho phép phân loại văn bản một cách tương đối

chất lượng cao và chi phí triển khai vừa phải, đã đến lúc mở rộng phạm vi của chúng tôi sang các lĩnh vực khác nhau

nhiệm vụ.