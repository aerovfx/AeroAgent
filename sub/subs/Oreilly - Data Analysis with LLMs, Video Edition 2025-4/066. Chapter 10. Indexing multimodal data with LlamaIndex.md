# 066. Chương 10. Lập chỉ mục dữ liệu đa phương thức với bản dịch LlamaIndex

---

Phần 10.6, lập chỉ mục dữ liệu đa phương thức với Lama Index.

Chuỗi Lang hoàn toàn không phải là khung duy nhất giúp sử dụng các mô hình ngôn ngữ để phân tích dữ liệu dễ dàng hơn.

Trong phần này, chúng ta thảo luận về một framework khác mới xuất hiện gần đây và nhanh chóng trở nên phổ biến, Lama Index.

Phần 10.6.1, tổng quan.

Lama Index tỏa sáng trong các trường hợp sử dụng trong đó các mô hình ngôn ngữ cần truy cập vào các bộ sưu tập dữ liệu lớn,

có thể tích hợp nhiều loại dữ liệu khác nhau.

Trong những trường hợp như vậy, nói chung là không nên hoặc thậm chí có thể đưa trực tiếp tất cả dữ liệu vào mô hình ngôn ngữ.

Thay vào đó, chúng ta cần một cơ chế có thể nhanh chóng xác định dữ liệu liên quan cho một nhiệm vụ nhất định,

chỉ truyền dữ liệu liên quan đến mô hình ngôn ngữ.

Đúng như tên gọi, Lama Index lập chỉ mục dữ liệu để nhanh chóng xác định các tập hợp con có liên quan.

Chính xác hơn, Lama Index liên kết các phần dữ liệu, chẳng hạn như các đoạn văn bản, với các vectơ nhúng.

Chúng ta đã thảo luận ngắn gọn về việc nhúng các vectơ trong chương 4.

Nói tóm lại, vectơ nhúng biểu thị ngữ nghĩa của văn bản dưới dạng vectơ được tính toán bằng mô hình ngôn ngữ.

Nếu hai tài liệu có các vectơ nhúng giống nhau, khoảng cách giữa các vectơ nhỏ thì chúng ta giả định rằng chúng thảo luận về các chủ đề tương tự nhau.

Quy trình xử lý dữ liệu Lama Index điển hình bao gồm các bước sau.

Đầu tiên, nó tải dữ liệu, có thể ở nhiều định dạng khác nhau và thực hiện tiền xử lý.

Ví dụ: quá trình xử lý trước có thể yêu cầu chia tài liệu văn bản dài thành các phần nhỏ hơn để xử lý thuận tiện hơn.

Tiếp theo, Lama Index lập chỉ mục dữ liệu.

Như đã thảo luận trước đây, điều này có nghĩa là liên kết các khối dữ liệu với các vectơ nhúng.

Theo mặc định, Lama Index sử dụng các mô hình ngôn ngữ khá nhỏ, ví dụ như các mô hình ADA mở của AI, để tính toán các vectơ nhúng.

Điều này làm cho bước lập chỉ mục trở nên rẻ tiền. Hơn nữa, Lama Index có thể lưu trữ chỉ mục đã tạo, các vectơ nhúng trên đĩa để tránh phải tạo lại chúng cho mỗi tác vụ mới.

Lama Index cung cấp hỗ trợ cho nhiều trường hợp sử dụng khác nhau dựa trên chỉ mục được tạo. Chẳng hạn, nó có thể sử dụng dữ liệu được lập chỉ mục để trả lời các câu hỏi bằng ngôn ngữ tự nhiên.

Đưa ra một câu hỏi làm đầu vào, trước tiên nó sẽ tính toán một vectơ nhúng cho văn bản câu hỏi.

Sau đó, nó so sánh vectơ biểu thị câu hỏi với các vectơ được tính toán trước biểu thị các khối dữ liệu. Nó xác định các mục dữ liệu có vectơ giống nhau nhất.

Dữ liệu liên quan được đưa vào lời nhắc cùng với câu hỏi đầu vào.

Mục tiêu là tạo ra câu trả lời cho câu hỏi, khai thác dữ liệu liên quan làm bối cảnh.

Trong khi các mô hình nhỏ được sử dụng để lập chỉ mục thì chúng tôi thường sử dụng các mô hình lớn hơn để tạo ra phản hồi cuối cùng.

Hình 10.4 minh họa đường dẫn xử lý dữ liệu này.

Hình 10.4, các bước chính của quy trình xử lý dữ liệu Lama Index điển hình.

Lama Index tải dữ liệu của chỉ mục để cho phép truy xuất nhanh.

Khi đưa ra một câu hỏi, Lama Index xác định các mục dữ liệu có liên quan và gửi chúng cùng với câu hỏi đầu vào đến mô hình ngôn ngữ để tạo ra câu trả lời.

Phần 10.6.2, cài đặt Lama Index.

Hãy triển khai quy trình từ phần cuối cùng trong Python. Để sử dụng Lama Index, trước tiên chúng ta phải cài đặt một vài gói. Đi đến thiết bị đầu cuối của bạn và chạy lệnh sau.

Xem mã này?

Điều đó sẽ giúp bạn thiết lập các gói cốt lõi của Lama Index. Tuy nhiên, bạn sẽ sử dụng Lama Index để phân tích tập hợp các định dạng dữ liệu đa dạng.

Để cho phép Lama Index truy cập và phân tích tất cả chúng một cách chính xác, bạn cần cài đặt một vài gói bổ sung. Chạy các lệnh sau trong terminal của bạn.

Xem mã này.

Những thư viện này cần thiết để phân tích tài liệu .pdf và bản trình bày PowerPoint, tất cả những thứ đó chúng ta sẽ cần cho dự án sau.

Phần 10.6.3, triển khai hệ thống trả lời câu hỏi đơn giản.

Bạn trở lại Banana và đối mặt với một vấn đề đầy thách thức.

Là một công ty toàn cầu, Banana có nhiều đơn vị khác nhau. Sếp của bạn muốn bạn phân tích dữ liệu từ các đơn vị khác nhau, chẳng hạn như để so sánh hiệu suất của chúng.

Tuy nhiên, các đơn vị khác nhau có những ưu tiên khác nhau về định dạng dữ liệu.

Một số đơn vị xuất bản kết quả của họ dưới dạng tài liệu văn bản đơn giản, trong khi những đơn vị khác thường tạo ra các bài thuyết trình PowerPoint phức tạp.

Làm cách nào để bạn tích hợp tất cả các định dạng dữ liệu khác nhau đó? May mắn thay, Lama Index đã khiến việc đó trở nên dễ dàng.

Nhìn vào danh sách 10.4. Chỉ trong vài dòng mã Python, nó sẽ xử lý tác vụ. Mã chấp nhận các tham số đầu vào sau.

Một liên kết đến một kho lưu trữ dữ liệu. Kho lưu trữ này có thể chứa nhiều loại tệp khác nhau.

Một câu hỏi để trả lời. Lama Index sẽ sử dụng dữ liệu trong kho để trả lời.

Sau khi phân tích cú pháp các tham số đó từ dòng lệnh, chúng tôi tải dữ liệu từ kho lưu trữ đầu vào. Hai.

May mắn thay, Lama Index thực hiện bước này rất đơn giản. Không cần thêm xử lý cho các loại tệp khác nhau, v.v.

Thay vào đó, chỉ cần chuyển đường dẫn thư mục là đủ. Tiếp theo, chúng tôi lập chỉ mục dữ liệu chúng tôi vừa tải. Ba.

Theo mặc định, Lama Index sử dụng mô hình ADA của OpenAI để tính toán các vectơ nhúng.

Ví dụ: chuyển đổi và phân chia dữ liệu, chia tài liệu văn bản lớn thành các phần đủ nhỏ để các mô hình ADA của OpenAI xử lý, đều được xử lý tự động.

Bây giờ chúng ta tạo một công cụ truy vấn ở đầu chỉ mục. Bốn. Công cụ này sẽ tự động truy xuất dữ liệu liên quan đến câu hỏi đầu vào bằng chỉ mục.

Cuối cùng, chúng tôi sử dụng công cụ được tạo trước đó để trả lời câu hỏi đầu vào, số năm và in kết quả.

Liệt kê 10.4. Một hệ thống trả lời câu hỏi đơn giản với Lama Index.

Mặc dù Lama Index cung cấp nhiều cách khác nhau để định cấu hình và chuyên môn hóa từng bước của quy trình này cũng như tạo các quy trình khác, nhưng việc sử dụng cài đặt mặc định trong mỗi bước sẽ dẫn đến mã đặc biệt ngắn gọn.

Mục 10.6.4. Đang thử nó.

Hãy thử quy trình của chúng tôi bằng cách sử dụng một số dữ liệu mẫu. Bạn có thể tải xuống danh sách 10.4 từ trang web Books Companion.

Đồng thời tải xuống tệp chuối report.zip từ trang web và giải nén nó trong cùng thư mục với mã. Sử dụng liên kết báo cáo chuối.

Nhìn vào bên trong thư mục. Bạn sẽ tìm thấy các báo cáo kinh doanh ngắn gọn trong các tài liệu text.pdf và bản trình bày PowerPoint.

Đã đến lúc trả lời một số câu hỏi. Mở terminal của bạn và thay đổi thư mục chứa mã và thư mục báo cáo chuối sau khi giải nén. Bây giờ hãy chạy lệnh sau.

Xem mã này.

Bạn sẽ thấy đầu ra như sau. Xem mã này.

Hãy xem tập tin tương ứng. Bạn sẽ thấy rằng đơn vị trồng chuối quả thực đã kiếm được 30 triệu đô la Mỹ. Hãy thử yêu cầu các đơn vị khác, Pissang Raja và Cavendish.

Bạn sẽ thấy kết quả chính xác. Bạn thậm chí có thể thử các câu hỏi phức tạp hơn, chẳng hạn như đơn vị nào kiếm được nhiều tiền nhất vào năm 2023. Yêu cầu so sánh các tệp khác nhau.

Một lần nữa, hệ thống sẽ có thể tìm thấy câu trả lời chính xác.