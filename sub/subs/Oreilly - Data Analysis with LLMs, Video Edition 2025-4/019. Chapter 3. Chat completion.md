# 019. Chương 3. Hoàn thành trò chuyện được dịch

---

Phần 3.4, Hoàn tất trò chuyện.

Hầu như tất cả mã trong cuốn sách này đều sử dụng cùng chức năng của thư viện OpenAI Python,

Hoàn tất trò chuyện.

Với tính năng Hoàn thành cuộc trò chuyện, mô hình của bạn sẽ tạo một lượt hoàn thành cho một cuộc trò chuyện, được cung cấp làm đầu vào.

Đầu vào có thể chứa nhiều loại dữ liệu khác nhau, chẳng hạn như văn bản và hình ảnh.

Chúng ta sẽ khai thác những đặc điểm đó trong các chương sau, nhưng hãy hạn chế sử dụng văn bản để

khoảnh khắc.

Hoàn thành trò chuyện cũng được sử dụng trong nền giao diện web Chat GPT của OpenAI.

Lấy Lịch sử trò chuyện làm đầu vào, bao gồm tin nhắn mới nhất cũng như tin nhắn trước đó,

có thể chứa ngữ cảnh liên quan, mô hình sẽ tạo ra câu trả lời phù hợp nhất.

Để sử dụng Hoàn thành trò chuyện từ Python, trước tiên chúng ta cần một định dạng để mô tả Lịch sử trò chuyện.

Đây là một phần thông tin đầu vào mà chúng tôi đang cung cấp cho quá trình Hoàn thành trò chuyện.

Trong thư viện Python của OpenAI, Trò chuyện được biểu diễn dưới dạng danh sách tin nhắn.

Mỗi tin nhắn lần lượt được biểu diễn dưới dạng một từ điển Python.

Từ điển Python này chỉ định các giá trị cho một số thuộc tính quan trọng của thông báo.

Ít nhất, chúng ta cần chỉ định hai thuộc tính quan trọng cho mỗi tin nhắn.

Thuộc tính roll xác định nguồn của tin nhắn.

Thuộc tính nội dung, chỉ định nội dung của tin nhắn.

Hãy bắt đầu bằng cách thảo luận về thuộc tính cuộn.

Như bạn đã biết từ chương trước, cuộc trò chuyện với mô hình GPT là một chuỗi tin nhắn qua lại,

xen kẽ giữa các tin nhắn do người dùng viết và các tin nhắn do người mẫu viết.

Theo đó, chúng ta có thể chỉ định giá trị user cho thuộc tính roll để xác định thông báo

như được viết bởi người dùng.

Ngoài ra, chúng ta có thể chỉ định trợ lý giá trị để đánh dấu tin nhắn được tạo bởi ngôn ngữ

mô hình.

Giá trị thứ ba có thể có cho thuộc tính roll là system.

Những tin nhắn như vậy thường được sử dụng khi bắt đầu lịch sử trò chuyện.

Chúng nhằm mục đích truyền đạt các hướng dẫn chung cho mô hình, độc lập với các nhiệm vụ cụ thể

do người dùng gửi.

Ví dụ: một thông báo hệ thống điển hình có thể có nội dung, bạn là trợ lý hữu ích,

nhưng phiên bản chuyên biệt hơn.

Ví dụ: bạn là trợ lý dịch các câu hỏi về tập dữ liệu thành truy vấn SQL,

cũng có thể.

Chúng tôi sẽ không sử dụng thông báo hệ thống trong cuốn sách này nhưng hãy thử nghiệm và thử thêm

thông báo hệ thống của riêng bạn để xem liệu chúng có ảnh hưởng đến kết quả đầu ra của mô hình hay không.

Thuộc tính nội dung chỉ định nội dung của tin nhắn.

Trong chương này, chúng ta sẽ giới hạn nội dung văn bản.

Trong các chương sau, chúng ta sẽ thấy cách sử dụng các mô hình ngôn ngữ để xử lý các loại đa dạng hơn.

của nội dung.

Trong các mẫu mã sau đây, chúng tôi sẽ chỉ cần chỉ định một tin nhắn duy nhất trong lịch sử trò chuyện của mình.

Thông báo này chứa các hướng dẫn mô tả một nhiệm vụ mà mô hình ngôn ngữ sẽ giải quyết,

cũng như thông tin ngữ cảnh có liên quan.

Ví dụ: lịch sử trò chuyện sau đây khuyến khích mô hình tạo một câu chuyện cho chúng tôi.

Xem mã này?

Danh sách tin nhắn chỉ chứa một tin nhắn duy nhất.

Thông báo này được đánh dấu là có nguồn gốc từ người dùng, một và mô tả thông tin trước đó

nhiệm vụ được đề cập trong nội dung của nó.

Thứ hai, như một câu trả lời, chúng tôi mong đợi mô hình sẽ tạo ra một câu chuyện theo sau

hướng dẫn đầu vào.

Làm cách nào chúng tôi có thể gọi một mô hình để hoàn thành trò chuyện?

Điều này có thể được thực hiện chỉ bằng một vài dòng mã Python.

Đầu tiên, chúng ta cần nhập thư viện OpenAI Python, một và tạo đối tượng khách, hai.

Xem mã này?

Chúng tôi sẽ sử dụng đối tượng client cho tất cả các lệnh gọi mô hình ngôn ngữ sau đây.

Mã trước đó xuất hiện trong hầu hết các mẫu mã của chúng tôi.

Hãy nhớ rằng bạn có thể cần phải chuyển thủ công khóa truy cập OpenAI làm tham số khi tạo

máy khách, trừ khi bạn chỉ định khóa truy cập của mình trong một biến môi trường, đây là khuyến nghị

cách tiếp cận.

Sau khi tạo ứng dụng khách, chúng tôi có thể đưa ra yêu cầu hoàn thành trò chuyện, như được hiển thị ở đây.

Xem mã này?

Chúng tôi sử dụng hàm client.chat.completions.create để tạo yêu cầu mới.

Tham số mô hình, một, chỉ định tên của mô hình mà chúng ta muốn sử dụng để hoàn thành.

Trong trường hợp này, chúng tôi đang chọn mô hình GPT4O của OpenAI, mô hình này có thể xử lý dữ liệu đa phương thức.

Chúng ta sẽ sử dụng mô hình này cho hầu hết các mẫu mã trong cuốn sách này.

Tiếp theo, chúng tôi chỉ định lịch sử trò chuyện làm đầu vào thông qua tham số của tin nhắn.

Thứ hai, đây là lịch sử trò chuyện đã thảo luận trước đó, hướng dẫn mô hình tạo một câu chuyện.

Hãy đặt tất cả lại với nhau.

Danh sách sau đây, có sẵn dưới dạng danh sách hai trong phần ba chương trên cuốn sách

trang web đồng hành, sử dụng GPT4O để tạo câu chuyện.

Liệt kê 3.2, sử dụng GPT4O để hoàn thành trò chuyện.

Chạy mã sẽ tạo ra kết quả như sau.

Câu chuyện chính xác của bạn có thể khác do sự ngẫu nhiên.

Xem mã này?

Hãy thảo luận về các thành phần khác nhau của kết quả đó.

Đầu tiên, chúng ta có một danh sách các lựa chọn thay thế hoàn thành, một, các đối tượng được lựa chọn loại.

Trong trường hợp của chúng tôi, danh sách đó chỉ chứa một mục duy nhất.

Đây là hành vi mặc định, mặc dù chúng tôi có thể yêu cầu nhiều lần hoàn thành thay thế

bằng cách thiết lập các tham số cấu hình phù hợp được thảo luận trong phần tiếp theo.

Cờ lý do đã hoàn thành, hai, cho biết lý do ngừng tạo đối với mỗi lần hoàn thành.

Ví dụ: điều này có thể là do đã đạt đến giới hạn độ dài đối với văn bản được tạo.

Giá trị dừng cho biết mô hình ngôn ngữ có thể tạo đầu ra hoàn chỉnh,

trái ngược với việc đạt đến giới hạn chiều dài.

Thông điệp thực tế, ba nội dung, được viết tắt và rất có thể, bạn sẽ thấy khác nhau

câu chuyện nếu bạn gọi mã nhiều lần.

Bên cạnh việc hoàn thành, kết quả còn chứa siêu dữ liệu và số liệu thống kê sử dụng.

Chính xác hơn là chúng ta tìm thấy giá trị cho các thuộc tính sau.

Số lượng mã thông báo được tạo, số lượng mã thông báo, số lượng mã thông báo trong đầu vào,

tổng số mã thông báo, số lượng mã thông báo được đọc và tạo.

Tại sao chúng ta lại quan tâm đến số lượng token?

Bởi vì giá của hầu hết các mô hình AI mở tỷ lệ thuận với số lượng mã thông báo được đọc và

được tạo ra.

Ví dụ: tại thời điểm viết bài, chi phí sử dụng GPT-40 là 5 USD trên một triệu mã thông báo được đọc và

15 USD cho mỗi triệu token được tạo ra.

Lưu ý sự khác biệt về giá giữa mã thông báo được đọc và được tạo.

Thông thường, như trong trường hợp này, việc tạo mã thông báo đắt hơn việc đọc mã thông báo.

Giá cả không chỉ phụ thuộc vào số lượng token mà còn phụ thuộc vào mô hình được sử dụng.

Ví dụ: thay thế GPT-40 bằng mẫu turbo GPT-3.5, GPT kém mạnh mẽ hơn một chút

phiên bản, cắt giảm chi phí theo hệ số 10.

Để phân tích lượng lớn dữ liệu bằng mô hình ngôn ngữ, hãy chọn kích thước mô hình phù hợp cho

nhiệm vụ và ví của bạn.