# 029. Chương 5. Giao diện truy vấn ngôn ngữ tự nhiên để phân tích doanh số trò chơi được dịch

---

Phần 5.2, giao diện truy vấn ngôn ngữ tự nhiên để phân tích doanh số trò chơi.

Chúng ta trở lại Banana, nơi bạn nhận ra rằng sếp của mình tình cờ là một trò chơi máy tính

người đam mê.

Sếp của bạn không chỉ thích dành buổi tối để chơi mà còn thích phân tích dữ liệu về

trò chơi máy tính.

Vừa có được bộ dữ liệu về doanh số bán game trên máy tính, sếp của bạn đang tìm cách trích xuất

số liệu thống kê thú vị.

SQL là ngôn ngữ được lựa chọn để phân tích dữ liệu dạng bảng, nhưng sếp của bạn không cảm thấy thoải mái

viết truy vấn SQL.

Biết về chuyên môn của bạn liên quan đến mô hình ngôn ngữ và phân tích dữ liệu, sếp của bạn

hỏi liệu có thể xây dựng một giao diện dịch các câu hỏi về

trò chơi máy tính thành các truy vấn SQL tương ứng.

Sau khi suy nghĩ về nó, bạn nhận ra rằng đây có thể là một dự án thú vị có thể thực hiện được

dễ dàng với các mô hình ngôn ngữ.

Hãy bắt đầu.

Phần 5.2.1, thiết lập cơ sở dữ liệu SQL lite.

Trước khi có thể phân tích dữ liệu dạng bảng bằng truy vấn SQL, trước tiên chúng ta phải tải dữ liệu đó vào

RDBMS, một công cụ xử lý bảng dữ liệu một cách hiệu quả.

Trong tiểu mục này, chúng ta sẽ xem cách tải dữ liệu về trò chơi máy tính vào SQL lite, một

RDBMS phổ biến.

Bước đầu tiên, hãy đảm bảo SQL lite đã được cài đặt.

Chúng tôi sẽ sử dụng SQL lite phiên bản 3 trong phần này.

Bạn có thể kiểm tra xem SQL lite 3 đã được cài đặt chưa bằng cách nhập nội dung sau vào terminal của bạn.

Xem mã này.

Nếu bạn nhận được thông báo như thế này thì không cần cài đặt thêm.

Xem mã này?

Nếu bạn thấy thông báo lỗi, hãy truy cập liên kết này.

Chọn phiên bản phù hợp với hệ điều hành của bạn, tải xuống tất cả các phiên bản có liên quan

các tệp và làm theo hướng dẫn để cài đặt SQL lite.

Trong trường hợp gặp sự cố, hãy nhấp vào mục cài đặt SQL lite trong phần Chương 5 của sách

trang web đồng hành.

Bạn sẽ tìm thấy hướng dẫn chi tiết về cách cài đặt SQL lite trên các nền tảng khác nhau.

Sau đó, chạy lệnh trước để đảm bảo rằng SQL lite được cài đặt đúng cách.

Tiếp theo, chúng tôi muốn tạo một cơ sở dữ liệu quan hệ bằng SQL lite.

Cơ sở dữ liệu quan hệ về cơ bản là một tập hợp các bảng dữ liệu.

Bạn có thể coi mỗi bảng dữ liệu như một bảng tính đơn giản.

Chúng tôi đã đặt tên các cột trong bảng được liên kết với một kiểu dữ liệu và có thể nhiều hàng trong bảng

chứa các giá trị cho mỗi cột.

Ví dụ: bảng 5.1 chứa thông tin về trò chơi điện tử và mỗi hàng thể hiện

một trò chơi.

Bảng có bốn cột, tên, nền tảng, năm và thể loại.

Ba trong số các cột, tên, nền tảng và thể loại chứa các chuỗi là văn bản.

Cột năm chứa các số đại diện cho năm mà trò chơi được phát hành.

Bảng 5.1, bảng dữ liệu mẫu.

Mỗi hàng trong bảng mô tả một trò chơi điện tử.

Bảng 5.1 trình bày một mẫu nhỏ của tập dữ liệu mà bạn có thể tìm thấy trên trang web đồng hành với sách.

Tìm mục trò chơi trong phần Chương 5.

Chúng tôi sẽ sử dụng tập dữ liệu đó trong các phần sau để tạo giao diện truy vấn ngôn ngữ tự nhiên,

cho phép người dùng phân tích nó bằng các lệnh ngôn ngữ tự nhiên.

Đầu tiên chúng ta cần tải dữ liệu đó vào hệ thống cơ sở dữ liệu SQLLite.

Mẹo.

Chúng ta sẽ thực hiện tất cả các bước cần thiết để tải dữ liệu dạng bảng vào SQLLite.

Điều này rất hữu ích nếu bạn muốn tải dữ liệu khác với dữ liệu được thảo luận ở đây.

Nếu bạn không muốn tạo cơ sở dữ liệu của riêng mình, bạn có thể bỏ qua phần này và tải xuống games.dv

thay vào đó từ trang web đồng hành của sách.

Hãy bắt đầu giao diện dòng lệnh SQLLite.

Nhập lệnh sau vào terminal và nhấn enter.

Xem mã này.

Lệnh này tạo cơ sở dữ liệu mới được lưu trữ trong tệp games.db.

Đồng thời, nó mở giao diện dòng lệnh SQLLite.

Bây giờ chúng ta có thể hướng dẫn công cụ SQLLite tải tập dữ liệu mà chúng ta sẽ sử dụng trong các phần sau.

Đầu tiên chúng ta phải nói với SQLLite một chút về cấu trúc của dữ liệu.

Chúng tôi muốn tải một phiên bản mở rộng của Bảng 5.1 chứa các cột bổ sung.

Chạy lệnh sau để mô tả cấu trúc của dữ liệu.

Xem mã này?

Lệnh này mô tả cấu trúc của một bảng có tên Trò chơi 1 vì nó sẽ lưu trữ

thông tin về trò chơi điện tử.

Trong ngoặc đơn, chúng tôi chỉ định danh sách đầy đủ các cột trong bảng được phân tách bằng dấu phẩy 2.

Mỗi cột được xác định bởi tên cột, ví dụ như thứ hạng, tên hoặc thể loại, theo sau là

theo kiểu cột.

Ví dụ: int nghĩa là cột lưu trữ số nguyên, trong khi text nghĩa là cột lưu trữ

dữ liệu văn bản.

Tất cả các hàng của bảng phải cung cấp giá trị cho từng cột thuộc loại dữ liệu thích hợp.

Sau khi xác định cấu trúc bảng, chúng ta có thể tải dữ liệu vào bảng từ file .csv

trong Trò chơi trên trang web sách.

Tải xuống tập tin nếu bạn chưa có.

Chúng ta sẽ giả định rằng file được lưu trong thư mục gạch chéo tải xuống gạch chéo video games.csv

cho các lệnh sau.

Chỉ cần thay thế đường dẫn đó bằng đường dẫn tệp trên hệ thống của bạn.

Tải dữ liệu bằng lệnh sau, vẫn ở giao diện sql light.

Xem mã này?

Lệnh đầu tiên chuẩn bị ánh sáng sql để tải dữ liệu từ tệp .csv, áp dụng cho

tập tin chúng tôi muốn tải.

1.

Lệnh tiếp theo, 2, nhập dữ liệu.

Tham số đầu tiên là đường dẫn đến tệp mà chúng tôi muốn tải dữ liệu từ đó và tham số thứ hai

tham số là tên bảng dữ liệu mà chúng ta muốn nạp file vào.

Trong trường hợp này, chúng ta tham chiếu bảng có cấu trúc mà chúng ta đã xác định trước đó.

Trò chơi.

Để kiểm tra xem dữ liệu đã được tải thành công hay chưa, trường hợp này sẽ xảy ra nếu bạn không thấy

một thông báo lỗi, hãy chạy lệnh sau trong ánh sáng sql.

Xem mã này?

Bạn sẽ thấy kết quả 16599 nếu tất cả dữ liệu đã được tải.

Nếu bạn thấy số thấp hơn, hãy kiểm tra thông báo lỗi trong đầu ra đèn sql.

Bạn có thể thoát khỏi bảng điều khiển ánh sáng sql thông qua lệnh .quit.

Đừng quên tiền tố lệnh của bạn bằng . để làm cho nó hoạt động.

Đối với các phần sau, chúng tôi sẽ giả định rằng dữ liệu đã được tải và được lưu trữ

trong một tệp ánh sáng sql có tên games.db.

Phần 5.2.2, kiến ​​thức cơ bản về sql.

Chắc chắn, chúng ta có thể sử dụng mô hình ngôn ngữ để dịch câu hỏi sang truy vấn sql, được hiểu

bằng ánh sáng sql.

Chúng ta có thể tin tưởng vào bản dịch của nó không?

Nghĩ kỹ hơn về điều đó, bạn nhận ra rằng sẽ không có hại gì nếu có ít nhất một số kiến thức cơ bản về sql

để xác minh đầu ra của mô hình ngôn ngữ trước khi trình bày giao diện với sếp của bạn.

Đó là những gì chúng ta sẽ làm trong phần này.

Tất nhiên, phần giới thiệu đầy đủ về sql nằm ngoài phạm vi của cuốn sách này.

Hãy xem cơ sở dữ liệu của trang web giảng dạy.com để có phần giới thiệu chi tiết hơn về điều này

tác giả cuốn sách.

Chúng ta sẽ thảo luận một số vấn đề cơ bản về sql trong phần này để giúp triển khai truy vấn ngôn ngữ tự nhiên của chúng ta

giao diện.

truy vấn sql được sử dụng để phân tích bảng dữ liệu.

Kết quả truy vấn có thể được lấy từ một bảng duy nhất hoặc bằng cách kết hợp dữ liệu từ nhiều bảng.

Trong cơ sở dữ liệu mẫu được tạo ở phần trước, chúng ta có một bảng duy nhất.

Ví dụ: bằng cách sử dụng truy vấn sql, chúng ta có thể đếm các hàng có thuộc tính nhất định, ví dụ:

tất cả các trò chơi của cùng một nhà phát hành, chẳng hạn như lọc dữ liệu, chỉ hiển thị các trò chơi đã được phát hành

vào năm 2017 hoặc thực hiện các hoạt động tổng hợp khác nhau, chẳng hạn như đối với mỗi nhà xuất bản, hãy tính toán

thu nhập trung bình trên mỗi trò chơi.

Truy vấn sql thường mô tả một bảng để tạo, sử dụng dữ liệu đã có sẵn

trong cơ sở dữ liệu.

Hầu hết các truy vấn mẫu trong phần này đều có cấu trúc sau.

Xem mã này?

Điều khoản From 2, mã theo sau từ khóa From và trước từ khóa Wear, mô tả

nguồn dữ liệu được sử dụng để phân tích.

Ví dụ: Mệnh đề From có ​​thể chứa danh sách các tên bảng được phân tách bằng dấu phẩy.

Trong cơ sở dữ liệu mẫu của chúng tôi, chúng tôi chỉ có một bảng duy nhất để xử lý.

Đối với các truy vấn nâng cao, cùng một tên bảng có thể xuất hiện nhiều lần trong Mệnh đề Từ,

tạo các bản sao khác nhau của cùng một bảng.

Các truy vấn chúng ta sẽ gặp trong các phần sau sẽ chứa một mục duy nhất trong

Từ Clause, bàn trò chơi.

Điều khoản Wear 3 xác định các vị từ trên các bảng xuất hiện trong Điều khoản From.

Ví dụ: nó có thể chứa một điều kiện hạn chế phạm vi đối với các trò chơi từ một nhà phát hành nhất định.

Điều khoản hao mòn có thể chứa các điều kiện đơn giản, nghĩa là các điều kiện có thể được biểu thị bằng

bất đẳng thức hoặc bất đẳng thức trên một cột trong bảng, cũng như các điều kiện phức tạp, kết nối nhiều

điều kiện đơn giản bằng toán tử AND hoặc OR.

Những điều kiện đó được sử dụng để lọc các hàng từ các bảng trong Mệnh đề Từ.

Các hàng không thỏa mãn điều kiện trong Điều khoản hao mòn sẽ bị loại bỏ và không xuất hiện

trong kết quả truy vấn.

Cuối cùng, chúng tôi chỉ định các cột của bảng kết quả mong muốn trong Điều khoản Chọn, một.

Chính xác hơn, chúng tôi chỉ định danh sách các định nghĩa cột được phân tách bằng dấu phẩy.

Các cột có thể được xác định bằng tên cột, một cột xuất hiện ở một trong các bảng trong

Mệnh đề Từ hoặc bằng một biểu thức phức tạp hơn.

Ví dụ: một biểu thức số học kết nối nhiều cột.

Ngoài ra, chúng ta có thể chỉ định các tổng hợp như Đếm sao trong Mệnh đề chọn,

tổng hợp sau đếm số lượng hàng.

Kết quả truy vấn chứa một bảng với các cột được chỉ định, điền nội dung vào chúng

tương ứng với định nghĩa cột.

Ví dụ: giả sử chúng tôi muốn đếm tất cả các trò chơi được xuất bản vào năm 2017.

Trong trường hợp này, kết quả truy vấn của chúng tôi phải chứa một cột có tổng số lượng.

Ngoài ra, Điều khoản Wear của chúng tôi phải chứa điều kiện lọc giới hạn phạm vi của chúng tôi đối với trò chơi

từ năm 2017.

Tất nhiên, Điều khoản From của chúng tôi chứa tên của bảng duy nhất trong cơ sở dữ liệu của chúng tôi, trò chơi.

Truy vấn sau đây tạo ra kết quả mong muốn.

Xem mã này?

Để làm cho mọi thứ phức tạp hơn một chút, hãy giả sử rằng chúng ta muốn tính tổng

số lượng bán hàng ở Châu Âu và Nhật Bản cho tất cả các trò chơi được phát hành vào năm 2017 bởi một nhà phát hành cụ thể,

Kích hoạt.

Kết quả truy vấn mong muốn của chúng tôi chứa hai cột, tên trò chơi và số lượng bán hàng.

Vì bảng dữ liệu của chúng tôi có hai cột với doanh số bán hàng ở Châu Âu và doanh số bán hàng ở Nhật Bản, nên doanh số bán hàng ở EU

và doanh số bán hàng tại Nhật Bản, theo định nghĩa trong bảng ở phần trước, chúng ta có thể mô tả

cột kết quả mong muốn bằng cách cộng chúng, doanh số bán hàng tại EU cộng với doanh số bán hàng tại Nhật Bản.

Vì chúng tôi muốn giới hạn phạm vi của mình đối với các trò chơi từ năm 2017 và từ Activision, chúng tôi có thể sử dụng một tổ hợp

vị từ trong Điều khoản Wear, năm bằng 2017 và nhà xuất bản bằng Activision.

Lưu ý việc sử dụng dấu nháy đơn xung quanh tên Activision.

Chúng ta cần sử dụng dấu nháy đơn để giới hạn chuỗi trong điều kiện truy vấn thay vì số.

Truy vấn sau đây tạo ra kết quả mong muốn.

Xem mã này.

Ngoài ra, khi chỉ định các tổng hợp, chúng ta có thể tính toán các tổng hợp đó cho các

nhóm hàng được xác định bởi các giá trị được chia sẻ trong các cột nhất định.

Để làm như vậy, chúng tôi thêm mệnh đề nhóm cuối cùng vào mẫu truy vấn trước đó, theo sau là

danh sách các cột được phân tách bằng dấu phẩy được sử dụng để tạo thành nhóm.

Ví dụ: giả sử chúng ta muốn tính toán doanh số bán trò chơi cho từng thể loại, chẳng hạn như

như chiến lược hoặc hành động, một cách riêng biệt.

Chúng ta có thể sử dụng truy vấn sau để trả về một hàng có tổng hợp cho từng danh mục trò chơi.

Lưu ý rằng chúng tôi cũng thêm cột thể loại vào mệnh đề chọn để đảm bảo rằng chúng tôi có thể

liên kết các con số với thể loại chính xác.

Xem mã này.

Kiến thức cơ bản về SQL trong phần này rõ ràng là không đủ để viết các truy vấn SQL của riêng bạn,

ngoại trừ một số trường hợp đơn giản.

Tuy nhiên, chúng tôi không thực sự muốn viết các truy vấn SQL của riêng mình.

Thay vào đó, chúng tôi muốn dựa vào mô hình ngôn ngữ.

Phần giới thiệu này sẽ giúp bạn hiểu các truy vấn được tạo bởi mô hình ngôn ngữ tại

mức độ trừu tượng cao và quan sát cách mô hình ngôn ngữ ánh xạ các câu hỏi tới các truy vấn

có thể là một cách hay để bắt đầu tự học SQL.

Trong phần tiếp theo, chúng ta sẽ bắt đầu bằng cách tạo một trình dịch đơn giản, dịch các câu hỏi

vào các truy vấn SQL trên cơ sở dữ liệu của trò chơi.

Tổng quan về phần 5.2.3.

Sau khi đã nắm được một số kiến thức cơ bản về SQL để kiểm tra đầu ra của mô hình ngôn ngữ, bây giờ chúng ta sẽ

làm việc trên trình dịch văn bản sang SQL của chúng tôi.

Chúng tôi sẽ sử dụng trình dịch của mình thông qua dòng lệnh.

Chúng tôi chỉ định một câu hỏi làm đầu vào và lý tưởng nhất là muốn có một truy vấn SQL, dịch câu hỏi của chúng tôi

như đầu ra.

Nếu chạy truy vấn đầu ra trong SQL Lite, chúng ta sẽ nhận được câu trả lời cho câu hỏi ban đầu của mình.

Tất nhiên, giao diện này là thủ công và không thuận tiện lắm khi sử dụng.

Lý tưởng nhất là chúng tôi muốn nó tự động thực hiện các truy vấn và hiển thị kết quả tương ứng

trực tiếp trong giao diện truy vấn của chúng tôi.

Chúng ta sẽ tạo một giao diện như vậy ở phần sau.

Hiện tại, chúng tôi chỉ tập trung vào vấn đề cốt lõi là chuyển câu hỏi thành truy vấn.

Hiện tại, mục tiêu duy nhất của chúng tôi là dịch các câu hỏi về trò chơi máy tính.

Do đó, chúng tôi sẽ mã hóa cứng cấu trúc của cơ sở dữ liệu đích.

Một lần nữa, chúng tôi sẽ khái quát hóa điều đó trong dự án tiếp theo của chúng tôi.

Trong nội bộ, để dịch câu hỏi đầu vào, trước tiên chúng tôi sẽ tạo lời nhắc.

Lời nhắc này mô tả tác vụ dịch và chứa tất cả các chi tiết liên quan để dịch.

Ví dụ: cấu trúc cơ sở dữ liệu mục tiêu của chúng tôi.

Trong hầu hết các trường hợp, việc gửi lời nhắc này đến mô hình ngôn ngữ sẽ tạo ra bản dịch chính xác

Truy vấn SQL.

Chúng tôi có thể vẫn cần phải thực hiện một chút công việc để trích xuất truy vấn này từ những nội dung có thể quá dài dòng.

đầu ra được tạo ra bởi mô hình ngôn ngữ.

Hãy thảo luận chi tiết hơn về các bước này, bắt đầu bằng các lời nhắc.

Phần 5.2.4 Tạo lời nhắc dịch văn bản sang SQL.

Chúng ta cần truyền tải thông tin gì đến mô hình ngôn ngữ để dịch thành công?

Rõ ràng chúng ta cần xác định rõ câu hỏi muốn dịch.

Ngoài ra, chúng ta cần chỉ định hệ thống đích, SQL Lite và mô tả cấu trúc của

cơ sở dữ liệu đích.

Hiện tại, chúng tôi mã hóa cứng cấu trúc cơ sở dữ liệu.

Chúng ta có thể chỉ cần cung cấp mô hình ngôn ngữ với định nghĩa bảng, tạo bảng mà chúng ta

được sử dụng ở Mục 5.2.1.

Mô hình ngôn ngữ sẽ hiểu cách lệnh này ánh xạ tới cấu trúc bảng.

Bằng cách gửi lời nhắc kèm theo hướng dẫn dịch sang mô hình chứa tất cả

các loại thông tin đã đề cập trước đó, mô hình ngôn ngữ sẽ có thể tạo ra

một truy vấn SQL tương ứng.

Hãy sử dụng mẫu lời nhắc sau đây.

Xem mã này?

Mẫu lời nhắc này chứa tất cả các thông tin được mô tả trước đó.

Đầu tiên, nó mô tả cơ sở dữ liệu đích bằng cách cung cấp các lệnh SQL được sử dụng để tạo

các bảng liên quan, trong trường hợp này là một bảng duy nhất.

Lưu ý rằng đây không phải là trình giữ chỗ vì hiện tại, giao diện truy vấn của chúng tôi chỉ cần

để làm việc cho một cơ sở dữ liệu có cấu trúc mà chúng tôi mã hóa cứng trong mẫu.

Tiếp theo, mẫu lời nhắc chứa mô tả nhiệm vụ, hai.

Mục tiêu là dịch các câu hỏi thành các truy vấn SQL.

Cuối cùng, mẫu chứa câu hỏi cần dịch, ba.

Ở đây, chúng tôi sử dụng phần giữ chỗ, được biểu thị bằng dấu ngoặc vuông.

Điều này cho phép chúng tôi sử dụng cùng một mẫu lời nhắc cho nhiều câu hỏi khác nhau mà sếp của chúng tôi có thể hỏi.

dữ liệu.

Đoạn mã sau tạo lời nhắc theo mẫu trước đó.

Xem mã này?

Đưa ra câu hỏi cần dịch làm đầu vào, mã sẽ thêm mô tả về cơ sở dữ liệu,

một, sau đó là hướng dẫn dịch, hai, và cuối cùng là câu hỏi cần dịch,

ba.

Kết quả là sự kết hợp của tất cả các phần nhắc, bốn.

Mục 5.2.5, mã hoàn chỉnh.

Danh sách tiếp theo chứa mã đầy đủ cho giao diện truy vấn ngôn ngữ tự nhiên của chúng tôi.

Nó sử dụng hàm tạo dấu nhắc đã thảo luận trước đó, cũng như hàm gọi

mô hình ngôn ngữ, hai, mà chúng ta đã biết từ các chương trước.

Mục 5.1.

Dịch các câu hỏi về trò chơi điện tử thành các truy vấn SQL.

Danh sách này đọc một câu hỏi về trò chơi máy tính từ dòng lệnh, số ba.

Bằng cách sử dụng câu hỏi đầu vào, nó sẽ tạo ra một dấu nhắc, bốn câu, hướng dẫn mô hình ngôn ngữ

để dịch câu hỏi thành một truy vấn SQL.

Nó gửi lời nhắc đến mô hình ngôn ngữ và nhận được câu trả lời là năm.

Câu trả lời thô từ GPT40 thường chứa các giải thích xen kẽ với truy vấn SQL

cuối cùng chúng tôi quan tâm đến.

Để có được truy vấn riêng, chúng ta phải trích xuất nó từ câu trả lời thô, sáu.

Ở đây, chúng tôi khai thác thực tế là GPT40 bao quanh các truy vấn SQL giữa bộ ba điểm đánh dấu

backticks, SQL và ba backticks.

Khi tương tác với các mô hình GPT thông qua giao diện web GPT trò chuyện, nội dung giữa các mô hình đó

điểm đánh dấu được hiển thị dưới dạng hộp mã.

Biểu thức chính quy ba dấu backtick, SQL, sau đó mở dấu ngoặc đơn, dấu chấm, dấu hoa thị, đóng

dấu ngoặc đơn, theo sau là ba dấu ngoặc ngược, truy vấn SQL giữa các điểm đánh dấu, sử dụng

Hàm Python, redot fin Doll, để trả về danh sách kết quả khớp cho biểu thức chính quy này.

Cờ redot all là bắt buộc để đảm bảo rằng dấu chấm khớp với tất cả các ký tự, bao gồm cả

dòng mới, có thể xuất hiện trong truy vấn SQL.

Chúng tôi sử dụng kết quả phù hợp đầu tiên làm truy vấn của mình, nghĩa là chúng tôi ngầm giả định rằng ít nhất

một trận đấu được trả lại và trận đấu đầu tiên là phù hợp.

Phần 5.2.6, thử xem.

Được rồi, đã đến lúc thử dịch văn bản sang SQL của chúng ta.

Trong terminal, chuyển sang thư mục chứa mã Python.

Chúng tôi sẽ giả định rằng mã được lưu trữ trong một tệp có tên là liệt kê 1.py mà bạn có thể tải xuống

từ trang web đồng hành.

Chạy lệnh sau.

Xem mã này.

Kết quả là bạn sẽ nhận được truy vấn SQL sau.

Xem mã này?

Truy vấn đó có đúng không?

Hãy cùng tìm hiểu.

Trong terminal, chuyển sang kho chứa tệp cơ sở dữ liệu SQL light, games.db.

Sau đó mở cơ sở dữ liệu thông qua giao diện dòng lệnh SQL light.

Xem mã này?

Bây giờ, cuối cùng chúng ta cũng có thể thử truy vấn được tạo bởi trình dịch văn bản sang SQL của chúng ta.

Nhập truy vấn và nhấn enter.

Bạn sẽ thấy số lượng trò chơi được lưu trữ trong cơ sở dữ liệu là 16.599.

Bạn có thể muốn thử một vài câu hỏi khác.

Ví dụ: xem liệu bạn có thể đếm số lượng trò chơi theo nhà xuất bản hoặc trò chơi cụ thể hay không

của một thể loại cụ thể.

Rất có thể đối với hầu hết các câu hỏi đề cập đến cơ sở dữ liệu đơn giản này, GPT-40 sẽ có khả năng

đủ để cung cấp một bản dịch chính xác.

Ông chủ của bạn sẽ được hạnh phúc.

Giao diện chúng tôi tạo trong phần này vẫn còn hạn chế về nhiều mặt.

Đầu tiên, bạn phải thực hiện lại chương trình cho mỗi câu hỏi mới.

Thứ hai, bạn phải sao chép và thực hiện thủ công từng truy vấn đã dịch vào hệ thống cơ sở dữ liệu

giao diện.

Thứ ba và quan trọng nhất, nếu bạn muốn chuyển sang một tập dữ liệu khác, bạn sẽ

phải thay đổi mẫu lời nhắc của bạn theo cách thủ công.

Trong phần tiếp theo, chúng ta sẽ xem cách khắc phục những hạn chế này.