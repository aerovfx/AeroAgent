# 004 2. Tìm hiểu cách giải mã tệp JSON và tìm nạp dữ liệu bằng SwiftUI en

---

Trong lớp học này, chúng ta sẽ tìm hiểu khái niệm lập trình thiết yếu dành cho nhà phát triển ứng dụng.

Chính xác hơn là cách giải mã dữ liệu Json bằng Swift Codable.

Chúng tôi sẽ phân tích dữ liệu Json và đưa thông tin này về từng loài động vật châu Phi vào một trang

xem.

Như bạn có thể thấy trong video xem trước này, chúng tôi sẽ đạt được tiến bộ lớn trong việc phát triển ứng dụng này bằng cách

kết thúc bài giảng này.

Nhưng trước khi chúng ta bắt đầu, tôi mong bạn hãy kiên nhẫn với tôi từ đầu đến cuối lớp học này.

Hôm nay chúng ta sẽ đề cập đến một chủ đề khá quan trọng và đó là lý do tại sao bài giảng này dài hơn một chút.

Tuy nhiên, đây vẫn là một hướng dẫn thực tế, nhưng lần này chúng ta sẽ tạo dáng một hoặc hai lần.

Mặc dù tôi sẽ giải thích cho bạn nhiều hơn về một số khái niệm lập trình hơn bình thường như chúng tôi đã làm.

rất nhiều việc phải làm

Vì vậy, không cần phải đắn đo thêm nữa, hãy khởi chạy Xcode và bắt đầu viết mã.

Chúng tôi.

Ảnh bìa.

Đầu tiên chúng ta sẽ tạo nguyên mẫu cho toàn bộ bố cục.

Sau đó chúng ta sẽ đề cập đến việc phân tích cú pháp Json.

Như tôi đã nói trước đây, trong hướng dẫn trước, chúng tôi thu thập tất cả các chế độ xem nhỏ hơn hoặc có thể tái sử dụng trong

thư mục chuyên dụng.

Như đã nói, hãy tạo một nhóm mới trên bảng Project Navigator.

Cung cấp cho nó chế độ xem tên.

Sau đó, như thường lệ, hãy tạo chế độ xem Swiftui mới cho ảnh bìa.

Đặt tên cho nó.

Chế độ xem ảnh bìa.

Bây giờ hãy lưu nó vào thư mục xem như tôi làm.

Đẹp.

Sau đó mình sẽ tạo một dấu lệnh mới có vạch chia ngang cho từng khối mã chính

trong tập tin này.

Hãy theo tôi và vào.

Đánh dấu.

Của cải.

Đánh dấu.

Bạn bè.

Đánh dấu.

Xem trước.

Loại nhận xét đặc biệt này được sử dụng để chia chức năng thành các phần có ý nghĩa và dễ điều hướng.

Bạn có thể tiếp cận các phần mã này trên thanh công cụ hoặc trong bản đồ nhỏ một cách dễ dàng.

Trong các video sau, tôi sẽ không lãng phí thời gian để đề cập đến nó một lần nữa mà tôi sẽ sử dụng lệnh này

đánh dấu thường xuyên.

Bây giờ chúng ta hãy thêm công cụ sửa đổi bố cục vào cửa sổ xem trước nhé?

Nhập bố cục xem trước.

Chiều rộng cố định.

400.

Chiều cao.

300.

Siêu.

Chúng ta có thể phóng to cửa sổ này trong khung vẽ nếu cần.

Sau đó, thay thế chế độ xem văn bản giữ chỗ bằng một hình ảnh tĩnh mới cho mục đích tạo mẫu.

Nhập hình ảnh.

Che sư tử.

Có thể thay đổi kích thước.

Thu nhỏ để điền vào.

Được rồi.

Cho đến nay, rất tốt.

Bây giờ hãy nhúng hình ảnh này vào chế độ xem tab mới.

Vào Chế độ xem tab.

Bình luận mới và các tab.

Bây giờ hãy di chuyển hình ảnh lên trong chế độ xem này.

Sau đó đợi một giây và xem kết quả trong bản xem trước.

Tất nhiên, đây không phải là điều chúng ta mong muốn.

Để khắc phục, hãy thêm công cụ sửa đổi này vào kiểu xem tab của chế độ xem tab.

Phong cách xem trang.

Bây giờ thì tốt hơn nhiều rồi.

Bây giờ chúng ta cần lặp lại hình ảnh này.

Cách dễ nhất để làm điều đó là ra lệnh và nhấp vào tên hình ảnh và chọn tùy chọn lặp lại từ

menu ngữ cảnh như tôi làm.

Sau đó nhập cho mỗi lệnh mới.

Kết thúc vòng lặp.

Công việc tuyệt vời.

Như bạn có thể nhận thấy, các chấm nhỏ hiển thị dưới dạng chỉ báo về chế độ xem tab trang ở phần dưới cùng của

hình ảnh.

Cái hay của việc phát triển giao diện người dùng nhanh là chúng ta có thể thấy ngay những gì đang xảy ra bằng cách sử dụng bản xem trước trực tiếp.

Bây giờ, hãy bắt đầu bản xem trước trực tiếp này bằng cách nhấp vào nút của nó trên thanh công cụ của cửa sổ xem trước này.

Khi chúng tôi khởi động nó, bạn có thể nhận thấy rằng nó khởi chạy trình mô phỏng tòa nhà và các chỉ báo trang là

ra khỏi ranh giới hình ảnh.

Nhưng theo tôi thì đó không phải là vấn đề lớn.

Chỉ cần vuốt sang phải để sử dụng chức năng của thành phần giao diện người dùng mới này.

Hãy xem, nó hoạt động mà không gặp bất kỳ trục trặc nào.

Tất nhiên, đây chỉ là dòng lặp lại năm lần trong chế độ xem tab trang này.

Cơ bản về Jason.

Và khi hoàn tất thiết kế bố cục, cuối cùng chúng ta cũng có thể nói về Jason và học cách phân tích cú pháp

đưa dữ liệu của nó vào chế độ xem này theo chương trình bằng giao thức Codable của Swift.

Nghe có vẻ thú vị phải không?

Tôi hy vọng như vậy.

Vậy Jason là gì?

Chà, Jason là viết tắt của ký hiệu đối tượng JavaScript.

Đó là một định dạng tệp nhỏ gọn, dễ đọc để cấu trúc dữ liệu.

Nói chung, nó chủ yếu được sử dụng để truyền dữ liệu giữa máy chủ và ứng dụng bằng cách sử dụng một số loại

giao diện người dùng ứng dụng được gọi là API.

Không chỉ vậy, chúng ta còn có thể sử dụng nó để lưu trữ và phân tích dữ liệu Json trực tiếp bên trong gói ứng dụng cục bộ.

Bạn biết đấy, đó là định dạng dựa trên văn bản thể hiện các loại thông tin khác nhau, nhưng bạn cũng nên

biết về nó.

Tệp liền kề đó bao gồm các cặp giá trị khóa và nó sử dụng dấu ngoặc, dấu ngoặc đơn và dấu hai chấm để phân tách

các phần tử của nhau.

Cấu trúc dữ liệu liền kề có thể biểu diễn các loại khác nhau như số chuỗi, bao gồm số nguyên và

double, sau đó là mảng boolean, từ điển và thậm chí là tùy chọn vì các nhà phát triển ứng dụng đã may mắn cho chúng tôi là

rằng mỗi loại Jason này có thể ánh xạ tới một loại Swift tương ứng.

Ngoại lệ duy nhất là kiểu dữ liệu, nhưng chúng ta luôn có thể chuyển đổi ngày tháng ở dạng số hoặc chuỗi

của dữ liệu này.

Được rồi.

Trước khi bắt đầu tìm hiểu cách phân tích tệp Json, trước tiên chúng ta sẽ xem nhanh Json đơn giản này

cấu trúc.

Cái này chứa một số cặp giá trị khóa trong đó các giá trị là các loại cơ bản, như chuỗi hoặc số.

Ngoài ra còn có một giá trị biểu thị một mảng chuỗi trong ví dụ đơn giản này để sử dụng bất kỳ tệp Json nào.

Đầu tiên, chúng ta cần tạo một mô hình dữ liệu tương ứng trong Swift.

Với mô hình dữ liệu này, chúng ta có thể dễ dàng giải mã bất kỳ dữ liệu nào bằng chức năng giải mã và mã hóa tích hợp của Swift.

Để làm được điều đó, chúng ta có thể chạy bộ giải mã Json cùng với API có thể mã hóa.

Điều đáng nói là API có thể mã hóa này sẽ thực hiện phần công việc khó khăn để tạo đầu ra

mà chúng ta sẽ cần sử dụng trong Swift UI sau này.

Nhân tiện, mã hóa này là một bí danh loại bao gồm các giao thức có thể mã hóa và có thể giải mã được.

Tôi khá tự tin rằng khi hoàn thành dự án Châu Phi này, bạn sẽ làm quen với tất cả những điều cơ bản

về cách sử dụng Json trong giao diện người dùng Swift.

Xử lý dữ liệu Json là một phần cơ bản của quá trình phát triển ứng dụng.

Vì vậy, chúng ta sẽ đi sâu vào chủ đề này trong các phần sắp tới của khóa học.

Bây giờ là lúc giải mã tệp Json đầu tiên trong dự án Châu Phi này.

Mô hình dữ liệu.

Bây giờ hãy điều hướng đến trình điều hướng Dự án và mở nhóm dữ liệu để xem tất cả các tệp Json được đính kèm vào đây

dự án.

Để có quan điểm thực tế, hãy chọn tệp Json dot bìa và hãy xem nhanh dữ liệu

cấu trúc của Json này.

Như bạn có thể nhận thấy, đây là một tệp dữ liệu cơ bản và dễ hiểu.

Mỗi đối tượng chỉ có hai phần tử.

Đầu tiên là khóa ID, là một loại số.

Cái thứ hai là khóa tên, là một kiểu chuỗi.

Bây giờ chúng ta cần tạo một tệp mô hình Swift mới phù hợp với cấu trúc dữ liệu này.

Tôi thực sự khuyên bạn nên nhấp vào nút Thêm trình chỉnh sửa vào bên phải ở góc trên cùng bên phải của

thanh công cụ như tôi chỉ cho bạn.

Với điều này, chúng ta có thể thấy tệp Json ở phía bên trái của trình chỉnh sửa khi làm việc trên mô hình dữ liệu

ở phía bên phải của nó.

Tôi có xu hướng sử dụng chức năng này khá thường xuyên trong trường hợp này vì tôi thấy nó khá thiết thực.

ngay cả trên màn hình nhỏ bé của tôi.

Tất nhiên bây giờ chúng ta cần tạo một tệp swift mới cho mô hình này.

Để làm điều đó trước tiên, hãy chọn thư mục dự án.

Sau đó, hãy tạo một nhóm mới cho tất cả các tệp mô hình.

Đặt tên cho nó theo mô hình.

Sau đó, hãy tạo tệp swift thực tế như tôi đã chỉ cho bạn.

Chọn mẫu tệp Swift trống trên cửa sổ bật lên này và đặt tên cho mẫu ảnh bìa.

Sau đó bấm vào nút Tạo.

Tuyệt vời.

Hãy bắt tay vào công việc.

Đầu tiên, hãy tạo một cấu trúc mới.

Nhập ảnh bìa cấu trúc.

Hãy để id int.

Đặt tên String.

Tuyệt vời.

Các thuộc tính ID và tên bên trong cấu trúc khớp với các khóa của tệp Json và

đó chính xác là mục tiêu chính của chúng tôi.

Bây giờ để phân tích cú pháp và điền dữ liệu thành công, chúng ta cần thêm vào phần này hai giao thức quan trọng

với nó, cụ thể là các giao thức có thể mã hóa và nhận dạng được.

Vì vậy, hãy làm điều đó.

Nhập có thể mã hóa.

Có thể nhận dạng được.

Hãy xem, mô hình ảnh bìa này đã sẵn sàng để sử dụng để giải mã dữ liệu Json.

Được rồi.

Tôi nghĩ việc nhớ thuộc tính ID và tên này khá dễ dàng nên chúng ta cũng không cần phải mở

tệp mô hình này hoặc tệp dữ liệu Json.

Chỉ cần đóng chúng lại như tôi làm.

Giải mã.

Có nhiều cách để giải mã tệp Json.

Một cách dễ dàng, lười biếng hoặc một giải pháp phù hợp nhưng lâu dài hơn.

Tôi sẽ chỉ cho bạn cái tốt hơn, nhưng trước khi chúng ta bắt đầu viết mã, bạn nên thực hành tốt

trải qua các bước hợp lý mà chúng ta cần thực hiện để giải quyết mục tiêu lập trình.

Và chúng ta có thể làm điều đó ngay bây giờ.

Cảm ơn giả.

Bạn có thể hỏi mã giả là gì?

Về cơ bản, trong khoa học máy tính, mã giả là sự mô tả bằng ngôn ngữ đơn giản về các bước trong thuật toán.

hoặc hệ thống khác.

Thế thôi.

Bây giờ hãy để tôi xác định các bước cần thiết mà chúng ta cần thực hiện để giải mã tệp Json cục bộ.

Được rồi.

Nhưng trước khi làm điều đó, hãy để tôi làm rõ mục tiêu chính mà chúng ta muốn đạt được.

Chúng tôi muốn giải mã dữ liệu Json cục bộ, sau đó sử dụng dữ liệu này để đưa vào chế độ xem Swiftui.

Ngoài ra, chúng tôi muốn tạo ra một thuật toán mà chúng tôi có thể sử dụng lại không chỉ một lần mà nhiều lần.

một lần nữa trong dự án của chúng tôi.

Hơn nữa, mục tiêu chính của chúng tôi là giải mã không chỉ một tệp Json cụ thể mà còn nhiều tệp Json

với các cấu trúc dữ liệu khác nhau.

Để đơn giản hóa mục tiêu phức tạp này, trước tiên chúng ta sẽ hoàn thành mục tiêu thứ nhất và thứ hai trong phần này.

bài giảng cụ thể.

Nhưng đừng lo lắng, trong lớp tiếp theo chúng ta cũng sẽ đề cập đến mục tiêu thứ ba.

Các bước.

Được rồi, đây là mã giả bằng tiếng Anh đơn giản của tôi.

Thuật toán phải thực hiện các nhiệm vụ sau một cách an toàn.

Đầu tiên, tìm tệp Json trong gói ứng dụng.

Thứ hai, tạo thuộc tính cho dữ liệu.

Thứ ba, tạo bộ giải mã Json.

Thứ tư, giải mã dữ liệu và thu thập thông tin bằng thuộc tính mới.

Thứ năm, trả lại dữ liệu sẵn sàng để sử dụng.

Và đây là cách tôi có thể chia nhỏ mục tiêu phức tạp này thành nhiều bước nhỏ hợp lý.

Chúng ta có thể sử dụng phương pháp này để tạo một hàm mới ở đâu đó trong tệp swiftui mà chúng ta có thể gọi khi

chúng ta cần phải làm vậy.

Tuy nhiên, chúng ta có thể thực hiện điều này một cách hiệu quả hơn nếu sử dụng chức năng này như một tiện ích mở rộng và đó là

chính xác những gì chúng ta sẽ làm

Được rồi.

Nhưng chúng ta đang nói về loại phần mở rộng nào?

Bạn có thấy bước đầu tiên trong danh sách này là xác định vị trí tệp trong quá trình phát triển ứng dụng iOS không?

Chúng ta cần sử dụng gói.

Bạn biết đấy, Apple sử dụng các gói trong iOS và macOS để trình bày các ứng dụng, khung, plugin, mã thực thi

hoặc các tài nguyên liên quan như hình ảnh, âm thanh, video hoặc các tệp khác.

Trong trường hợp của chúng tôi, đây là các tệp Json cục bộ.

Điều đó thật đáng buồn.

Chúng tôi sẽ mở rộng chức năng gói bằng tiện ích mở rộng bộ giải mã Json tùy chỉnh của mình.

Bạn có muốn tìm hiểu làm thế nào để làm điều đó?

Tôi biết bạn là vậy.

Vì vậy, hãy làm điều đó.

Phần mở rộng gói.

Điều đầu tiên, hãy quay lại Xcode và tạo một nhóm mới cho tiện ích mở rộng.

Đặt cho nó phần mở rộng tên.

Sau đó, hãy tạo một tệp swift mới như thường lệ.

Đặt tên nó là Codable.

Phần mở rộng gói.

Và lưu nó vào nhóm mới.

Được rồi, bây giờ hãy tạo một tiện ích mở rộng gói mới.

Nhập mã sau đây với chức năng giải mã mới bên trong nó.

Sự mở rộng.

Bó.

Cám ơn.

Giải mã.

Gạch dưới chuỗi tập tin.

Trở lại.

Mảng ảnh bìa.

Như bạn có thể nhận thấy, hàm giải mã này chỉ có một tham số.

Chúng tôi quyết định gọi nó là tập tin đơn giản, là một kiểu chuỗi.

Nhân tiện, với việc sử dụng dấu gạch dưới này, chúng ta không cần phải nhập tên của tham số mỗi

thời điểm chúng ta gọi hàm.

Chúng ta chỉ phải nhập tên file Json thực tế mà không cần nhập tên tham số, nhưng bạn sẽ thấy điều này như thế nào

dù sao thì sau này cũng hoạt động.

Vì vậy, chúng tôi sử dụng tham số tệp này để báo cho bộ giải mã biết tệp json nào cần phân tích và giải mã.

Trong trường hợp này đây là một loạt các hình ảnh bìa.

Tôi hy vọng rằng mọi thứ đã rõ ràng cho đến nay.

Bây giờ trước khi tiếp tục viết mã theo cách đặc biệt, chúng ta có thể sử dụng mã giả mà bạn đã quen thuộc

với nó.

Hãy viết ra các bước chính của thuật toán này dưới dạng các lệnh mới.

Bước đầu tiên, bình luận mới.

Thứ nhất, xác định vị trí tệp Json.

Bước thứ hai, bình luận mới.

Thứ hai, tạo thuộc tính cho dữ liệu.

Bước thứ ba, bình luận mới.

Thứ ba, tạo một bộ giải mã.

Bước thứ tư, bình luận mới.

Số bốn, tạo thuộc tính cho dữ liệu được giải mã.

Bước thứ năm lệnh mới.

Số năm, trả lại dữ liệu sẵn sàng để sử dụng.

Hãy xem, đây là bản thiết kế hoặc thuật toán của bộ giải mã.

Bây giờ, hãy triển khai các hướng dẫn được viết bằng tiếng Anh đơn giản này sang mã thực.

Chúng ta có nên không?

Nhập mã sau đây.

Bảo vệ.

Đặt URL bằng url của tệp tài nguyên có phần mở rộng bằng 0.

Khác.

Lỗi nghiêm trọng.

Không thể định vị tệp trong gói.

Như bạn có thể thấy, chúng tôi đang sử dụng câu lệnh bảo vệ của Swift khi tạo hằng số URL để xử lý

những lỗi có thể xảy ra.

Vì vậy, nếu chúng tôi viết sai chính tả tên tệp Json hoặc nó không tồn tại trong gói ứng dụng thì dự án đang chạy của chúng tôi

sẽ gặp sự cố với một thông báo lỗi nghiêm trọng cảnh báo cho chúng tôi.

Trong trường hợp đó, chúng ta sẽ luôn biết mình đã mắc lỗi gì trong mã của mình.

Tất nhiên, chúng ta không nên buộc ứng dụng của mình gặp sự cố trong một ứng dụng đã sẵn sàng sản xuất và có nhiều cách để

xử lý từng tình huống.

Ôi chàng trai.

Bạn biết đấy, xử lý lỗi thích hợp là một chủ đề quan trọng trong quá trình phát triển ứng dụng.

Nhưng để hoàn thiện hơn, tôi sẽ giới thiệu dần dần qua các dự án sắp tới.

Được rồi, bây giờ quay lại mã hóa thuật toán.

Bây giờ hãy nhập mã bảo vệ sau Hãy để dữ liệu bằng thử nội dung dữ liệu dấu chấm hỏi của URL.

Khác.

Lỗi nghiêm trọng.

Không tải được tệp từ gói.

Với thuộc tính được tính toán này, chúng tôi cố gắng đặt dữ liệu Json làm kiểu dữ liệu vào hằng số mới này.

Lý do đằng sau đó là chúng tôi không thể biết liệu tệp Json có bị hỏng hay không.

Nếu nó bị hỏng và chúng tôi không thể sử dụng nó làm dữ liệu thì ứng dụng của chúng tôi sẽ gặp sự cố lần nữa, nhưng lần này với một lỗi khác

thông báo lỗi.

Vì vậy, chúng tôi sẽ được thông báo về những gì đã xảy ra.

Được rồi, chúng ta hãy tiếp tục với bước thứ ba nhé?

Đi vào.

Đặt bộ giải mã bằng bộ giải mã Json.

Theo ý kiến ​​​​của tôi, nó khá tự giải thích.

Chúng tôi vừa tạo một hằng số mới với phương thức giải mã liền kề mà chúng tôi sẽ chạy trong phần tiếp theo.

bước.

Tiếp theo nhập bảo vệ hãy tải bằng thử bộ giải mã dấu chấm hỏi giải mã mảng ảnh bìa chấm tự.

Từ dữ liệu.

Alice.

Lỗi nghiêm trọng.

Không giải mã được tệp từ gói.

Bây giờ, tôi có thể nói với bạn rằng đây là phần chính của thuật toán này và hãy phân tích nó một chút.

Đầu tiên, chúng ta tạo một hằng số sẽ chứa dữ liệu Json đã được giải mã, nhưng để thực hiện điều đó một cách an toàn, Json

phương pháp giải mã trước tiên cần thử thao tác giải mã này.

Bộ giải mã Json cần biết hai thông tin quan trọng để thực hiện điều đó.

Đầu tiên, bộ giải mã yêu cầu thông tin về loại mà chúng ta cần giải mã.

Trong trường hợp cụ thể này, nó là một mảng ảnh bìa.

Hãy chú ý rằng loại này phải tuân theo giao thức có thể giải mã được, nếu không nó sẽ

không hoạt động chút nào.

Thông tin tiếp theo là dữ liệu nguồn.

Bạn có thể thấy đây là tệp Json trong gói ứng dụng của chúng tôi được tham chiếu bởi URL của nó trong trường hợp cả hai loại dữ liệu hay còn gọi là

mô hình dữ liệu và nguồn dữ liệu là hợp lệ.

Sau đó phương thức giải mã này sử dụng Swift Codable API sẽ giải mã dữ liệu Json thô và đưa ra kết quả

vào tài sản mới này.

Đối với chúng tôi, chúng tôi chỉ còn một bước cuối cùng để hoàn thành phần mở rộng bộ giải mã Json.

Điều mà trình biên dịch khó chịu trong suốt thời gian này là thiếu giá trị trả về trong hàm.

Chúng ta hãy sửa nó, được không?

Đi vào.

Trở lại tải.

Tuyệt vời.

Lỗi đã biến mất và chúng tôi cũng đã hoàn thành phần mở rộng bộ giải mã Json được thiết kế tốt này.

Kết quả cuối cùng của hàm này sẽ là một mảng ảnh bìa được tuần tự hóa mà chúng ta có thể sử dụng trong Swift

Tệp UI ở mọi nơi và mọi lúc.

Bạn nghĩ gì về nó?

Đến cuối toàn bộ phần này, bạn sẽ thấy việc tạo tiện ích mở rộng tiện dụng này có giá trị như thế nào.

Thực hiện.

Bây giờ, không cần phải đắn đo thêm nữa, hãy sử dụng tiện ích mở rộng này để đưa dữ liệu thực vào chế độ xem tab trang.

Đầu tiên, mở file xem ảnh bìa.

Như tôi đã làm.

Sau đó, hãy tạo một thuộc tính mới cho ảnh bìa thực tế.

Nhập mã sau đây.

Hãy che hình ảnh.

Mảng ảnh bìa.

Bằng gói chính.

Mã.

Bao gồm Dot Jason.

Như bạn có thể thấy, khá dễ dàng để sử dụng tiện ích mở rộng gói của chúng tôi và giải mã một tệp Json nhất định.

Tất cả những gì chúng ta cần làm là gọi hàm giải mã bên trong phần mở rộng gói.

Điều này sẽ giải mã tệp Json chấm bìa và lưu trữ mảng ảnh bìa đã giải mã vào tệp mới này

tài sản để sử dụng tiếp.

Bây giờ hãy điều hướng đến vòng lặp for each và thay thế phần giữ chỗ bằng thuộc tính này như tôi đã chỉ cho bạn.

Nhập cho từng ảnh bìa.

Và cuối cùng thay thế tên hình ảnh được mã hóa cứng bằng thuộc tính động của chúng tôi.

Nhập tên mục hình ảnh.

Cách tốt nhất để kiểm tra mã của chúng tôi, xem nó có hoạt động hay không, là bắt đầu xem trước và kiểm tra trực tiếp

giao diện người dùng tab trang.

Vì vậy, hãy làm điều đó.

Và nó đây.

Chúng ta có thể vuốt sang phải, sau đó quay lại sang trái và xem ảnh bìa được đưa vào tab trang như thế nào

xem.

Tuyệt vời.

Những chạm cuối cùng.

Bây giờ là lúc nhúng chế độ xem ảnh bìa này vào chế độ xem nội dung.

Mở tệp xem nội dung và xóa văn bản giữ chỗ.

Sau đó, nhập mã sau Chế độ xem điều hướng.

Lệnh mới.

Kết thúc điều hướng.

Bây giờ hãy tạo một danh sách mới bên trong chế độ xem điều hướng này.

Danh sách.

Bình luận mới, kết thúc danh sách.

Và cuối cùng, nhúng chế độ xem ảnh bìa vào danh sách này.

Nhập Chế độ xem ảnh bìa.

Ối.

Trang.

Khung cảnh thật nhỏ bé.

Chúng ta hãy sửa nó bằng một công cụ sửa đổi khung mới, được không?

Khung.

Chiều cao.

300.

Bây giờ nó tốt hơn nhiều, nhưng vẫn chưa hoàn hảo.

Như bạn có thể thấy, có một khoảng đệm mặc định xung quanh chế độ xem trên cùng.

Nhưng này, chúng ta có thể dễ dàng ghi đè hành vi mặc định này của chế độ xem danh sách bằng cách thêm công cụ sửa đổi vào

nhập hàng danh sách cụ thể.

Hãy chèo theo bộ.

Miếng lót cạnh.

Số không hàng đầu.

Số 0 dẫn đầu.

Số không dưới cùng.

Trailing zero là tốt.

Công cụ sửa đổi này sẽ loại bỏ phần đệm và ảnh bìa sẽ mở rộng kích thước của nó theo chiều ngang sang trái

và cạnh phải của màn hình.

Cuối cùng, thêm công cụ sửa đổi tiêu đề thanh điều hướng mới vào chế độ xem danh sách.

Tiêu đề thanh điều hướng.

Châu phi.

Chế độ hiển thị.

Lớn.

Tuyệt vời.

Chỉ còn một điều nữa trước khi chúng ta bắt đầu ngày thi và bài kiểm tra cuối cùng.

Hóa ra tôi đã nói với bạn điều gì đó không đúng khi chúng tôi tạo chế độ xem ảnh bìa.

Và bây giờ chúng ta cần sửa chữa lỗi lầm của mình.

Vui lòng mở chế độ xem ảnh bìa và điều hướng đến phần sửa đổi của hình ảnh ở đó.

Chúng ta cần trao đổi thang đo để phù hợp, sửa đổi thành tỷ lệ, để điền vào sửa đổi.

Nhân tiện, như tôi chỉ cho bạn, đây là phương pháp phù hợp để làm cho hình ảnh chạm vào các cạnh ngang

khi chúng ta đang sử dụng khung có chiều cao cố định.

Đang thử nghiệm.

Bây giờ, chúng ta sẽ kết thúc bài giảng dài này bằng việc thử nghiệm ứng dụng.

Bạn đã sẵn sàng cho nó chưa?

Sau đó, hãy xây dựng và chạy ứng dụng Châu Phi trong trình mô phỏng Xcode.

Sau khoảng thời gian ra mắt đầu tiên, chúng ta có thể vuốt sang phải và sang trái ảnh bìa trong tab trang.

Thật tuyệt vời phải không?

Tôi thực sự hy vọng rằng bạn có ý tưởng tốt hơn về cách phân tích mã và điền dữ liệu từ tệp Json.

Trong bài học tiếp theo, chúng ta sẽ mở rộng chức năng của bộ giải mã Json này và làm cho nó có khả năng giải mã

không chỉ một loại tệp mà cả các tệp Json khác.

Từ đó, không gì có thể ngăn cản bạn giải mã bất kỳ tệp Json nào với tư cách là nhà phát triển ứng dụng.

Nói xong hẹn gặp lại các bạn ở buổi học tiếp theo.