# 7. Làm việc với biểu thức chính quy trong Python

---

Trong bài giảng này, chúng ta sẽ tìm hiểu thêm về cách làm việc với các biểu thức chính quy trong Python.

Trước tiên, chúng ta cần nhập mô-đun Orrie ở đầu mã để có thể làm việc với các mô-đun thông thường.

biểu thức.

Bài giảng này sẽ đề cập đến các hàm hình thành mà chúng ta có thể sử dụng cho các biểu thức chính quy trong tìm kiếm Python,

tìm tất cả các phần chia và phụ.

Chúng tôi đã xem xét một ví dụ về chức năng tìm kiếm ở một số bài giảng trước đó.

Nhưng chúng ta hãy xem một ví dụ khác.

Hãy nhớ lại rằng chức năng tìm kiếm cho phép tìm kiếm một chuỗi văn bản phù hợp với một biểu thức chính quy.

Hãy tạo một biến có tên là text và đặt nó bằng chuỗi.

Xin chào thế giới.

Sau đó, chúng ta sẽ tạo một biến có tên là result sẽ được đặt bằng Origo Search.

Đối số đầu tiên sẽ là một chuỗi có dấu ngoặc vuông mở và đóng, bên trong đó chúng ta sẽ

thêm El Camino.

Điều này chỉ có nghĩa là nó sẽ khớp với chữ L hoặc chữ O nếu chúng xuất hiện trong chuỗi.

Đối số thứ hai chúng tôi sẽ cung cấp là văn bản.

Cuối cùng, hãy in ra kết quả khi chúng ta chạy mã.

Chú ý rằng đã có một trận đấu.

Trận đấu là ký tự EL đầu tiên trong chuỗi của chúng tôi.

Vậy còn tất cả các chữ L và O khác trong văn bản thì sao?

Chức năng tìm kiếm sẽ chỉ khớp với chuỗi con đầu tiên phù hợp với tiêu chí.

Nếu muốn lấy danh sách tất cả các kết quả trùng khớp, chúng ta có thể sử dụng hàm cuối cùng.

Điều này sẽ trả về một danh sách có tất cả các chuỗi con khớp với mẫu biểu thức chính quy đã chỉ định.

Hãy thay đổi hàm được gọi để tìm tất cả rồi chạy mã.

Như bạn có thể thấy, được in ra bảng điều khiển là mỗi chuỗi con phù hợp với tiêu chí đã cho.

Hàm phân tách cho phép chúng ta phân tách một chuỗi tại một dấu phân cách nhất định.

Ví dụ: giả sử chúng ta muốn chia chuỗi hello world của mình theo khoảng trắng.

Chúng ta sẽ thay đổi hàm đang được sử dụng thành hàm phân tách và chuyển vào dấu gạch chéo ngược ký tự khoảng trắng

làm dấu phân cách để phân chia.

Khi chúng ta chạy mã, hãy lưu ý rằng chúng ta nhận được một danh sách gồm hai từ hello và world được phân chia bởi

khoảng trắng nằm giữa chúng trong chuỗi văn bản của chúng tôi.

Chức năng phụ cho phép chúng ta thực hiện, tìm kiếm và thay thế.

Ví dụ: giả sử thay vì chia chuỗi tại khoảng trắng, chúng ta muốn thay thế khoảng trắng

ký tự có dấu gạch nối hoặc dấu gạch ngang.

Chúng tôi sẽ thay đổi chức năng.

Được gọi tới sub và truyền một dấu gạch ngang là đối số thứ hai của hàm để bây giờ có ba đối số

đang có mặt.

Khi chúng ta chạy mã, hãy lưu ý rằng các từ hello và world có dấu gạch ngang giữa chúng thay vì dấu cách.