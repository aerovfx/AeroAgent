# 4. Toán tử và Cú pháp

---

Trong bài giảng này, chúng ta tìm hiểu về các toán tử được hỗ trợ và bác bỏ khác nhau, như chúng ta đã học trước đó

Trong phần này có 12 toán tử hoặc ký tự đặc biệt mà RegExp nhận dạng và gán đặc biệt

nghĩa là.

Chúng ta hãy xem qua từng công việc họ làm và xem ví dụ về cách sử dụng chúng trong các dự án Regex 101

và công cụ trực tuyến miễn phí mà chúng tôi đang sử dụng để tập hợp và chạy các biểu thức chính quy cho đến nay trong phần này

phần.

Như chúng ta đã tìm hiểu trước đây trong khóa học, ký tự dấu gạch chéo ngược được sử dụng để thoát văn bản.

Đó là nhân vật trốn thoát.

Khi chúng ta muốn chứng tỏ rằng ký tự đứng sau nó là ký tự đặc biệt có ý nghĩa đặc biệt.

Ví dụ: chúng tôi sử dụng ký tự dấu gạch chéo ngược.

Nhân vật chết một mình không có ý nghĩa gì đáng kể.

Tuy nhiên, khi chúng ta thêm dấu gạch chéo lên phía trước nó, nó được dùng để khớp với bất kỳ ký tự chữ số nào bên trong

của hộp văn bản chuỗi mẫu trong giao diện một trên một của biểu thức chính quy.

Chúng tôi sẽ thêm một ví dụ.

Chuỗi ABC một, hai, ba.

Sau đó, chúng ta có thể thêm dấu gạch chéo ngược D vào trường đầu vào làm biểu thức chính quy của mình.

Như bạn có thể thấy, biểu thức này khớp với tất cả các ký tự chữ số.

Toán tử cà rốt cho phép chúng ta thực hiện lựa chọn dựa trên điểm bắt đầu của chuỗi.

Giả sử chúng ta muốn chọn một chuỗi nếu nó bắt đầu bằng chữ A. Chúng ta có thể sử dụng biểu thức cà rốt

AI rồi gõ vào chuỗi ví dụ ABC.

Nó sẽ được chọn vì chúng ta sử dụng toán tử cà rốt với ký tự AI và chuỗi này bắt đầu bằng

chữ A..

Cà rốt cũng cho phép chúng ta chỉ định các ký tự mà chúng ta muốn loại trừ khỏi vùng chọn.

Để làm điều này, chúng ta cũng cần sử dụng dấu ngoặc vuông có củ cà rốt bên trong.

Ví dụ: nếu muốn loại trừ tất cả 26 chữ cái trong bảng chữ cái, chúng ta có thể thêm dòng sau

biểu thức chính quy, về cơ bản có nghĩa là Không chọn bất kỳ chữ cái nào.

Nếu chúng ta có một chuỗi có số và chữ cái trong hộp văn bản chuỗi ví dụ, hãy lưu ý rằng tất cả

các số được chọn.

Những lá thư thì không.

Chúng ta cũng có thể làm ngược lại điều này.

Chúng ta có thể thay đổi biểu thức chính quy thành các số từ một đến chín để loại trừ tất cả các chữ số.

Lưu ý rằng bây giờ tất cả các chữ cái trong chuỗi mẫu của chúng tôi đã được chọn và các chữ số được loại trừ khỏi

lựa chọn để có được hiệu ứng tương tự với một biểu thức chính quy khác, chúng ta có thể sử dụng dấu gạch chéo ngược

ký tự mà chúng ta đã học cách đây vài phút bên trong dấu ngoặc khi kết hợp với toán tử cà rốt.

Điều này có tác dụng tương tự, loại trừ tất cả các chữ số khỏi lựa chọn của chúng tôi.

Ký tự pha loãng và biểu thức chính quy sẽ cho phép chúng ta chọn một chuỗi dựa trên phần cuối của nó.

Mẫu mà chúng tôi chỉ định sẽ được so sánh với phần cuối của chuỗi đầu vào.

Nếu trùng khớp thì chuỗi đó sẽ được chọn, nếu không thì chuỗi đó sẽ không được chọn.

Ví dụ: chúng tôi sẽ sử dụng mẫu sau.

Hãy chia nhỏ điều này có nghĩa là gì.

Dấu hoa thị chấm sẽ khớp với bất kỳ ký tự nào.

Chúng tôi sử dụng biểu thức này để nghĩa là phần đầu của chuỗi này có thể là bất cứ thứ gì theo nghĩa đen.

Khi đó death chính là dãy ký tự ở cuối chuỗi để cho máy tính biết rằng chúng ta

chỉ muốn chọn chuỗi có cái chết kết thúc.

Chúng tôi đã sử dụng ký hiệu đô la để xem biểu thức này hoạt động như thế nào.

Hãy sử dụng nó trên chuỗi ví dụ ABC DSF.

Như bạn sẽ thấy, chuỗi được chọn vì nó kết thúc bằng cái chết.

Nếu chúng ta thay đổi các ký tự và biểu thức chính quy, ABC nhận thấy rằng chuỗi đó không được chọn.

Điều này là do ABC nằm ở đầu chuỗi chứ không phải ở cuối chuỗi.

DOD dùng để so khớp bất kỳ ký tự nào, ngoại trừ một dòng là dòng mới, DOD sẽ không chọn

một dòng mới, nhưng nó sẽ chọn bất kỳ ký tự nào khác.

Ví dụ: chúng ta sẽ sử dụng DOD làm biểu thức chính quy, sau đó chúng ta có thể nhập bất kỳ chuỗi nào.

Ví dụ: Apple nhận thấy rằng nó khớp với từng ký tự trong chuỗi.

Chúng ta gõ vào các thanh dọc dùng để thực hiện điều kiện.

Hoặc chúng ta có thể sử dụng toán tử này để xâu chuỗi các mẫu biểu thức chính quy lại với nhau, nghĩa là chọn bất kỳ ký tự nào khớp với nhau

một mẫu hoặc một mẫu khác.

Ví dụ: chúng ta có thể chỉ định biểu thức A hoặc B, biểu thức này sẽ báo cho công cụ biểu thức chính quy chọn bất kỳ lần xuất hiện nào

của ký tự A và bất kỳ sự xuất hiện nào của ký tự đều được chú ý.

Nếu chúng ta gõ vào chuỗi ví dụ ABC, A và B được chọn nhưng C thì không.

Dấu chấm hỏi cho phép chúng ta chọn bất kỳ chuỗi nào khớp với 0 hoặc một lần xuất hiện của mẫu.

Ví dụ: chúng tôi sẽ sử dụng mẫu ví dụ sau.

Lưu ý rằng mẫu này sẽ khớp với chuỗi ví dụ apple và apple nhưng sẽ không khớp với apple.

Điều này là do chỉ cho phép 0 hoặc một ký tự kết thúc và ví dụ không khớp này sử dụng hai ký tự.

Dấu hoa thị cho phép chúng ta chọn bất cứ thứ gì khớp với 0 hoặc nhiều lần xuất hiện của mẫu.

Ví dụ: hãy sử dụng biểu thức dấu hoa thị b, chuỗi này sẽ khớp với chuỗi B vì A có thể xuất hiện bằng 0

hoặc nhiều lần trước chuỗi B cũng sẽ khớp với A, B và AB.

Dấu cộng cho phép chúng ta chọn bất kỳ chuỗi nào chứa một hoặc nhiều lần xuất hiện của mẫu đã chỉ định.

Không giống như ví dụ trước, A cộng B sẽ không khớp với chuỗi ví dụ B, bên cộng cần ít nhất

một lần xuất hiện của A trong mẫu sẽ hoạt động.

Tuy nhiên, trong trường hợp này, nó khớp với chuỗi.

Dấu ngoặc A, B và AB được sử dụng để khớp ký tự.

Nếu một chuỗi có bất kỳ ký tự nào nằm trong dấu ngoặc thì các ký tự đó sẽ được chọn.

Như chúng ta đã tìm hiểu trước đó trong video này, dấu ngoặc có thể được kết hợp với toán tử cà rốt để cho biết

máy tính ký tự cần được loại trừ khỏi vùng chọn.

Mẫu ABC và kiểu dấu ngoặc vuông cho phép chúng ta chọn bất kỳ sự xuất hiện nào của A, B hoặc C bên trong một

chuỗi.

Dấu ngoặc đơn hoạt động tương tự như dấu ngoặc, nhưng toàn bộ biểu thức trong dấu ngoặc đơn cần phải khớp

nếu không thì chuỗi sẽ không được chọn.

Vì vậy, nếu chúng ta có ABC bên trong dấu ngoặc đơn thì chỉ những lần xuất hiện của toàn bộ mẫu, ABC sẽ là

đã chọn.

Lưu ý rằng các cá nhân A, B và C sẽ không được chọn.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu về các đối tượng biểu thức chính quy trong Python.