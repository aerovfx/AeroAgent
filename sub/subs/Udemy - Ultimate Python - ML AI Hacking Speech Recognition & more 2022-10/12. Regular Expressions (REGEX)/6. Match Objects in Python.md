# 6. So khớp các đối tượng trong Python

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về các đối tượng so khớp trong Python gợi nhớ từ bài giảng trước khi chúng ta chạy

mã, chúng tôi nhận được cái được gọi là đối tượng khớp.

Nói một cách đơn giản, đối tượng so khớp là một đối tượng chứa thông tin về tìm kiếm và kết quả nếu có.

không phù hợp với mẫu đã chỉ định.

Lưu ý rằng không có gì sẽ được trả về thay vì một đối tượng phù hợp.

Khoảng mà chúng ta thấy chính là khoảng từ đầu đến cuối của trận đấu.

Lưu ý rằng văn bản chuỗi chứa 20 ký tự.

Bộ dữ liệu span cho biết từ 0 đến 20, với phần cuối là không bao gồm.

Vì vậy, nó cho chúng ta biết rằng các chỉ số từ 0 đến 19 của trận đấu kể từ khi lập chỉ mục bắt đầu bằng 0 trong Python

khi làm việc với đối tượng khớp, chúng ta có thể sử dụng thuộc tính chuỗi để lấy chuỗi được truyền vào hàm.

Ví dụ: hãy in ra kết quả của chuỗi kết quả khi chúng ta chạy mã.

Lưu ý rằng chuỗi chúc mừng sinh nhật vui vẻ của chúng ta đã được in.

Chuỗi này là toàn bộ văn bản mà chúng tôi đang tìm kiếm để chỉ lấy phần phù hợp.

Chúng ta có thể sử dụng chức năng nhóm.

Hãy in ra một cuộc gọi nhóm kết quả.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy Chuỗi Chúc mừng sinh nhật của chúng tôi đã được in lại.

Đối với ví dụ này, điều xảy ra là chuỗi văn bản chính và kết quả khớp hoàn toàn giống nhau.

Nói cách khác, toàn bộ văn bản khớp với mẫu mà chúng tôi đang tìm kiếm.

Hàm spin sẽ trả về một bộ dữ liệu chứa vị trí bắt đầu và kết thúc của trận đấu.

Ví dụ: hãy in ra lệnh gọi vòng quay kết quả.

Khi chúng tôi chạy mã, hãy lưu ý rằng một bộ có giá trị 0 và 20 được in ra số 0 là điểm bắt đầu

giá trị và hai mươi là giá trị cuối cùng.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu về cách làm việc với các biểu thức chính quy trong Python.