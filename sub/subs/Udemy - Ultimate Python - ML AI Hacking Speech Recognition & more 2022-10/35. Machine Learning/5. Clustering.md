# 5. Phân cụm

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về phân cụm và cách triển khai loại máy học này

thuật toán và phân cụm Python nhóm dữ liệu thành các cụm dựa trên các đặc điểm tương tự, ví dụ:

cụm và có thể được sử dụng để tạo chiến dịch quảng cáo được nhắm mục tiêu dựa trên nhân khẩu học của khách hàng.

K có nghĩa là phân cụm là một thuật toán học máy không giám sát.

Vì vậy, điều này có nghĩa là nó cố gắng phân loại dữ liệu mà không cần đào tạo.

Nó chọn một số điểm ngẫu nhiên trong một cụm điểm, tính trung bình xu hướng của chúng và sau đó xác định các điểm mới.

trung tâm cụm bằng cách tính toán mức trung bình của các điểm được chỉ định.

Hãy mở terminal hoặc chương trình nhắc lệnh và CD vào thư mục chứa tệp python của chúng ta

được lưu trữ.

Sau đó chúng ta cần cài đặt mô-đun PANDAS.

Chúng tôi sẽ gõ PIP, cài đặt gấu trúc.

Khi chúng tôi nhấn enter, mô-đun sẽ bắt đầu cài đặt trên máy tính của chúng tôi.

Sau khi hoàn tất, chúng ta sẵn sàng mở PI và bắt đầu viết một số mã ở đầu mã.

Chúng ta sẽ có một số báo cáo nhập khẩu.

Chúng tôi sẽ coi các trọng tài quan trọng, ENPI và gấu trúc là PD.

Sau đó, chúng ta sẽ nhập từ biểu đồ PI nhập khẩu matplotlib dưới dạng PLT từ trình tạo mẫu bắt đầu dữ liệu của České learn data.

Chúng tôi sẽ nhập make blob's và từ cụm Escalon, chúng tôi sẽ nhập ketamine.

Tiếp theo chúng ta sẽ tạo các biến X và Y và đặt chúng bằng một lệnh gọi.

Để tạo blob với mẫu cuối bằng hai trăm.

Pastan là trung tâm đối số đầu tiên bằng bốn được truyền vào dưới dạng cụm đối số thứ hai CTD bằng

điểm 0 trong bảy số 0 làm đối số thứ ba và trạng thái ngẫu nhiên bằng năm mươi Pastan làm đối số thứ tư.

Tiếp theo chúng ta sẽ gõ Paltalk phân tán và cắt lát X bắt đầu từ đầu và kết thúc bằng 0 rồi sau đó

cắt X từ đầu đến một.

Sau đó chúng ta sẽ tạo một tên biến W X và đặt nó bằng một danh sách trống.

Tiếp theo sẽ đến một vòng lặp for cho tất cả trong phạm vi một dấu phẩy mười bên trong vòng lặp for, chúng ta sẽ tạo một biến

đặt tên là K có nghĩa và đặt nó bằng K có nghĩa là cụm cuối bằng nhau.

Tôi đã chuyển vào làm đối số đầu tiên net bằng K có nghĩa là cộng cộng với Pastan làm đối số tối đa thứ hai

bằng hai trăm Pastan làm đối số thứ ba và init bằng mười được truyền vào làm đối số thứ tư

và trạng thái ngẫu nhiên bằng 50 mì ống làm đối số thứ năm.

Tiếp theo chúng ta sẽ gọi các canings fit và pass trong X.

Sau đó chúng ta sẽ gõ W seastarappend và truyền vào K nghĩa là quán tính bên ngoài vòng lặp for.

Chúng ta sẽ vẽ biểu đồ bằng cách gọi Paltalk và chuyển vào phạm vi một dấu phẩy mười làm đối số đầu tiên

và w CSF làm đối số thứ hai cho tiêu đề.

Chúng tôi sẽ chỉ định phương pháp elbo.

Sau đó, chúng ta sẽ gắn nhãn số trục x của cụm và truy cập w trục Y, viết tắt của cụm từ trong cụm

tổng các bình phương.

Cuối cùng, chúng ta sẽ hiển thị cốt truyện bằng cách gọi PLT Dot show.

Khi chúng tôi chạy mã.

Lưu ý rằng cụm blob của chúng tôi hiển thị, cũng như biểu đồ đường ánh xạ chúng.

Phương thức khuỷu tay được gọi tên như vậy vì nếu bạn để ý cốt truyện thì nó có hình khuỷu tay

với một khúc cua trong đó.

Trong bài giảng tiếp theo chúng ta sẽ tìm hiểu về hệ thống tư vấn và cách triển khai loại máy này

thuật toán học tập trong Python.