# 6. Hệ thống gợi ý

---

Trong bài giảng này chúng ta sẽ tìm hiểu về hệ thống tư vấn và cách triển khai loại máy này.

thuật toán học trong Python, hệ thống gợi ý là loại thuật toán học máy chính thứ tư

mà chúng ta sẽ học cách triển khai bằng Python.

Đây là cách các trang web như Netflix có thể đề xuất phim dựa trên các phim khác mà bạn đã xem

đã xem.

Bên dưới trang web là một thuật toán hệ thống đề xuất, một thuật toán thống kê phức tạp

được viết riêng để giới thiệu phim và chương trình cho bạn.

Chúng tôi sẽ sử dụng dữ liệu mẫu từ phim thực để viết chương trình hệ thống gợi ý.

Bước đầu tiên là tải xuống hai tệp CSV đính kèm bài giảng này dưới dạng tài nguyên khóa học, phim

CSV đó và xếp hạng CSV đó rồi nhấp và kéo chúng vào thư mục hiện đang chứa tệp python của chúng tôi.

Sau đó, chúng ta sẵn sàng bắt đầu viết một số mã ở đầu chương trình.

Chúng tôi sẽ nhập trọng tài, ENPI và PANDAS làm PD.

Tiếp theo, chúng ta sẽ tạo một biến có tên dữ liệu xếp hạng và đặt nó bằng cách đọc CSV với tên của

tập tin được gửi qua dưới dạng một chuỗi.

Trong trường hợp này, tên tệp của chúng tôi là xếp hạng CSFI.

Sau đó, chúng ta sẽ tạo một biến có tên tên phim và đặt nó bằng để đọc CSV và truyền vào chuỗi

phim csv tên của tất cả các tệp csv khác.

Sau đó, chúng ta sẽ tạo một biến có tên là dữ liệu phim và đặt nó bằng để hợp nhất với dữ liệu xếp hạng.

Pastan làm đối số đầu tiên đặt tên phim Pastan là tài liệu thứ hai và ID phim không bằng Pastan

như đối số thứ ba.

Tiếp theo, chúng ta sẽ tạo một tên biến.

Xếp hạng có nghĩa là đếm và đặt nó bằng khung dữ liệu đó với dữ liệu phim được nhóm theo tiêu đề ở xếp hạng

của bộ phim và sau đó gọi cho tôi về nó.

Về cơ bản, chúng tôi đang nhóm các phim theo tiêu đề, truy cập các giá trị xếp hạng và tính giá trị trung bình

hoặc trung bình của các giá trị đó.

Tiếp theo, chúng tôi sẽ đặt số lượng trung bình xếp hạng ở số lượng xếp hạng chính bằng khung dữ liệu PD với dữ liệu phim

được nhóm theo tiêu đề ở số lượng dấu chấm xếp hạng.

Sau đó, chúng tôi sẽ tạo một biến có tên là xếp hạng phim của người dùng mà chúng tôi sẽ đặt cho dữ liệu phim, xoay vòng

bảng và chuyển vào chỉ mục ID người dùng làm cột đối số đầu tiên bằng tiêu đề làm đối số thứ hai

và các giá trị bằng rating làm đối số thứ ba.

Tất cả chương trình sẽ có thể giới thiệu những bộ phim khác cho chúng tôi như Batman Beyond Return of the Joker.

Cụ thể, nó sẽ cung cấp cho chúng tôi các đề xuất khác dựa trên các bộ phim có xếp hạng tương tự.

Hãy tạo một biến có tên Batman Beyond Ratings và đặt nó bằng với xếp hạng phim của người dùng ở khóa

Batman Beyond Return của Joker.

Hai nghìn.

Vui lòng đảm bảo nhập chính xác như tôi nếu không mã sẽ không hoạt động.

Sau đó, chúng tôi sẽ tạo một biến có tên phim như Batman Beyond và đặt nó bằng với xếp hạng phim của người dùng

ghi điểm và vượt qua xếp hạng Batman Beyond.

Sau đó, chúng ta sẽ tạo một biến có tên là tương quan Batman Beyond và đặt nó bằng dữ liệu ngày PD

đóng khung với những bộ phim như Batman Beyond.

Pastan là đối số đầu tiên và các cột tương đương với Pastan là đối số thứ hai.

Sau đó, chúng ta sẽ gọi mối tương quan là Batman ngoài dấu chấm và a có tại chỗ bằng đúng đã được thông qua và

thì chúng ta sẽ thiết lập mối tương quan Batman vượt quá tương quan Batman vượt quá tham gia với xếp hạng có nghĩa là

đếm ở lần đánh giá đã qua.

Và cuối cùng chúng ta sẽ in ra mối tương quan Batman Beyond Dopehead.

Khi chúng tôi chạy mã, hãy lưu ý rằng một danh sách các phim được in ra có xếp hạng tương tự

với vai Batman Beyond Return của Joker.

Công việc tuyệt vời.

Bạn vừa viết một chương trình máy học đề xuất phim bằng mã Python.

Trong bài giảng tiếp theo, chúng ta sẽ xem xét tóm tắt những gì chúng ta đã học trong phần này của khóa học.