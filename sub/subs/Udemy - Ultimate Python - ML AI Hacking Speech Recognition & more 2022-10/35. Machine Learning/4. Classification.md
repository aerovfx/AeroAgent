# 4. Phân loại

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về phân loại và cách triển khai loại hình học máy này

thuật toán phân loại Python.

Nói một cách đơn giản là khi máy tính có thể sử dụng máy học để phân biệt giữa các đối tượng khác nhau.

Ví dụ, chúng ta sẽ viết một chương trình có hình ảnh quả táo và hình ảnh quả cam,

và máy tính sẽ tìm ra phân loại nhị phân nào ở đâu.

Chúng tôi có hai loại dữ liệu và chương trình sắp xếp các phiên bản thành một trong hai loại.

Ví dụ: tác vụ phân loại nhị phân có thể xác định bài đăng trên Facebook là tích cực hay tiêu cực

bằng cách phân tích từ ngữ và biểu tượng cảm xúc được sử dụng trong bài viết.

Phân loại nhiều lớp tương tự như phân loại nhị phân, trong đó dữ liệu được phân loại theo

chương trình, trong khi phân loại nhị phân hoạt động với hai lớp dữ liệu.

Phân loại nhiều lớp hoạt động với nhiều hơn hai lớp dữ liệu.

Một ví dụ về chương trình sử dụng phân loại nhiều lớp là xác định loại động vật nào được mô tả

bằng một hình ảnh.

Vì có nhiều loại động vật như hổ, gấu, sư tử, v.v. nên điều này sẽ được xem xét

một nhiệm vụ phân loại nhiều lớp cho một chương trình học máy.

Để đơn giản, một chương trình sẽ được phân loại nhị phân vì sẽ chỉ có hai lựa chọn táo

và cam.

Bước đầu tiên là tải xuống thư mục đính kèm bài giảng này dưới dạng tài nguyên khóa học và nhấp vào và

kéo nó vào thư mục chứa tệp Python của chúng tôi.

Thư mục này có hình ảnh những quả táo và cam sẽ được sử dụng.

Sau đó, chúng ta cần mở dấu nhắc lệnh hoặc thiết bị đầu cuối và máy tính và CD của chúng ta vào thư mục nơi

tập tin python của chúng tôi là.

Sau đó, chúng ta sẽ thực thi lệnh PIP PIP install socket dash image.

Khi chúng tôi nhấn enter, mô-đun này sẽ bắt đầu cài đặt trên máy của chúng tôi.

Sau khi hoàn tất, chúng ta đã sẵn sàng chuyển sang Pichon và bắt đầu viết một số mã Python tại

đầu chương trình.

Chúng ta sẽ có một số báo cáo nhập khẩu.

Chúng tôi sẽ nhập biểu đồ DCPI của matplotlib dưới dạng PLT, chúng tôi sẽ nhập ENPI trọng tài, chúng tôi sẽ nhập hệ điều hành và chúng tôi

sẽ nhập công việc.

Sau đó, từ České Image Io, chúng ta sẽ nhập và đọc từ České Magicked Transform.

Chúng tôi sẽ nhập kích thước từ các bộ sưu tập, chúng tôi sẽ nhập bộ đếm và từ lựa chọn mô hình Escalon.

Chúng tôi sẽ nhập phần chia thử nghiệm tàu.

Python cần dữ liệu RGV trong cuộc đua trọng tài để xử lý khía cạnh thị giác máy tính của chương trình.

Vì vậy, chúng ta sẽ tạo một hàm dư chịu trách nhiệm tải hình ảnh từ tệp

đường dẫn, thay đổi kích thước chúng, thêm nhãn và siêu dữ liệu cho chúng, sau đó ghi từ điển vào dưa chua

tập tin.

Sử dụng mô-đun công việc, chúng ta sẽ sử dụng từ khóa Python def và hàm Nimar thay đổi kích thước tất cả và nó sẽ nhận

các thông số.

Nguồn Piccolino bao gồm Wyk với giá trị mặc định là một năm mươi và chiều cao với giá trị mặc định là

không có.

Bên trong hàm này, chúng ta sẽ tạo một biến có tên là chiều cao mà chúng ta sẽ đặt bằng chiều cao.

Nếu không phải khác, chúng ta sẽ đặt nó theo chiều rộng được truyền vào hàm.

Lưu ý ví dụ ở đây về toán tử ternary Python.

Thay vì câu lệnh IF, chúng ta có thể đặt giá trị biến này dựa trên một điều kiện trong một dòng.

Sau đó, chúng ta sẽ tạo một biến có tên là data, chúng ta sẽ đặt biến này bằng một từ điển mới bằng cách sử dụng

chức năng dict.

Tiếp theo, chúng tôi sẽ thêm mô tả khóa vào từ điển dữ liệu của mình với giá trị được thay đổi kích thước, sau đó là chiều rộng

và chiều cao Hình ảnh Frood và OJB.

Đây chỉ là dữ liệu cho chúng ta biết rằng hình ảnh đã được thay đổi kích thước.

Sau đó, chúng ta sẽ thêm khóa cho tên tệp và dữ liệu nhãn, nhưng chúng ta sẽ gán chúng cho giá trị của một

danh sách trống bây giờ.

Khi đó chúng ta sẽ có một biến tên là Pick, một cái tên mà chúng ta nói bằng một chuỗi có phần định danh là

mỗi thư mục con trong thư mục nguồn.

Chúng tôi sẽ kiểm tra xem thư mục con có nằm trong tệp include hay không.

Nếu vậy, chúng tôi sẽ in thư mục con ra bàn điều khiển.

Sau đó, chúng ta sẽ có một biến có tên là đường dẫn hiện tại sẽ được đặt thành đường dẫn từ thư mục nguồn

đến khớp thư mục con bằng cách sử dụng chức năng nối đường dẫn mô-đun hệ điều hành cho mỗi tệp trong thư mục.

Tại đường dẫn này chúng ta sẽ kiểm tra xem đuôi file là JPEG hay PMG.

Nếu vậy, chúng ta sẽ tạo một biến có tên item để chứa hình ảnh.

Chúng ta sẽ gửi nó đến một cuộc gọi đến hàm inbreed với đường dẫn hiện tại được nối với tên tệp để đọc

trong tập tin đó.

Sau đó chúng ta sẽ thiết lập.

Tôi thực hiện lệnh gọi hàm thay đổi kích thước với AM Pastan làm tài liệu đầu tiên và bộ dữ liệu đang giữ

các biến có và chiều cao làm tài liệu thứ hai.

Tiếp theo, chúng ta sẽ đặt nhãn bằng thư mục con được cắt bằng dấu hai chấm âm 4 được chuyển vào phần

dấu ngoặc vuông.

Sau đó chúng ta sẽ thiết lập hồ sơ thành tập tin.

Cuối cùng, chúng tôi sẽ đặt dữ liệu bằng với dữ liệu hình ảnh mà chúng tôi đọc và sau đó thay đổi kích thước.

Sau đó, chúng tôi sẽ kết xuất dữ liệu vào tệp dưa chua mà chúng tôi đã tạo bên ngoài chức năng của mình.

Chúng ta sẽ tạo một đường dẫn dữ liệu có tên biến.

Nó sẽ là hình ảnh chém về phía trước của hoa quả.

Bên trong thư mục hình ảnh là các thư mục quả táo và cam mà chúng ta sẽ tham khảo sau.

Sau đó chúng ta sẽ thiết lập Alstott Lyster theo đường dẫn dữ liệu vừa thiết lập để chương trình biết đây là

nơi để tìm những hình ảnh mà nó sẽ sử dụng trong chương trình.

Tiếp theo, chúng ta sẽ tạo một tên cơ sở tên biến mà chúng ta đã nói bằng giá trị chuỗi Fruit.

Sau đó, chúng ta sẽ có một biến được đặt tên bằng.

Mà nói bằng 80.

Tiếp theo, chúng ta sẽ có một biến có tên là include mà chúng ta sẽ đặt bằng một tập hợp có các giá trị chuỗi,

táo và cam, tên của các thư mục và cạnh của thư mục hình ảnh của chúng tôi.

Tiếp theo, chúng ta sẽ tạo Kolu hoặc thay đổi kích thước tất cả chức năng và chuyển vào nguồn đường dẫn dữ liệu và tên Pikul của

tên cơ sở hoặc chiều rộng của with và sau đó bao gồm.

Sau đó chúng ta sẽ có một biến trong dữ liệu và đặt nó bằng Joblingload với tên của dưa chua

file pastan để tệp được tải, chúng tôi sẽ in ra số lượng mẫu mà chúng tôi có thể lấy được

bằng cách kiểm tra độ dài của dữ liệu.

Chúng tôi sẽ in ra các khóa mà chúng tôi có thể lấy được bằng cách kiểm tra các khóa dữ liệu.

Sau đó chúng ta sẽ in ra mô tả, hình ảnh, hình dạng và nhãn cho dữ liệu.

Trong trường hợp này, nhãn của chúng tôi là táo và cam.

Tiếp theo, chúng ta sẽ gọi hàm đếm và truyền dữ liệu vào nhãn khóa.

Sau đó, chúng tôi sẽ đặt nhãn thành lệnh gọi NPR duy nhất và truyền dữ liệu tại nhãn khóa.

Điều này sẽ cung cấp cho chúng tôi các giá trị nhãn duy nhất.

Tiếp theo, chúng ta sẽ tạo một biến có tên là hình và đặt nó bằng một hình mới.

Sau đó, chúng ta sẽ tạo một biến có tên X và đặt nó bằng hình cộng các trục với một danh sách có

giá trị không không một và một quá khứ.

Và sau đó chúng ta sẽ gọi kích thước tập hợp hình tăng lên và chuyển vào 15 làm đối số đầu tiên.

Và đối với chúng tôi, lập luận thứ hai.

Sau đó, chúng ta sẽ có một biến có tên X mà chúng ta nói bằng NPR và truyền dữ liệu theo data.

Sau đó, chúng ta sẽ tạo một biến có tên Y đặt bằng dữ liệu NPR và truyền dữ liệu tại nhãn.

Sau đó, chúng ta cần tạo các biến X train, X test, Y train và Y test và đặt chúng làm train test

chia với X được truyền vào làm đối số đầu tiên Y được truyền vào.

Tài liệu thứ hai có phải là kích thước kiểm tra điểm 0 đối với Pastan vì việc xáo trộn đối số thứ ba bằng đúng không,

được chuyển làm đối số thứ tư và trạng thái ngẫu nhiên bằng 42 Pastan làm đối số thứ năm.

Bây giờ chúng ta cần một hàm chịu trách nhiệm vẽ các thanh tiến trình.

Chúng ta sẽ tạo một hàm có tên là thanh cốt truyện và nó sẽ nhận ba tham số y loc với giá trị mặc định

của trái và tương đối.

Với giá trị mặc định là true.

Bên trong hàm này, chúng ta sẽ cấp độ với biến mà chúng ta sẽ đặt bằng điểm 0.

Sau đó, chúng ta sẽ sử dụng toán tử bậc ba để đặt giá trị của N thành âm 0,5 nếu LOC còn lại.

Ngược lại, chúng ta sẽ đặt nó bằng 0,5.

Tiếp theo, chúng ta sẽ đặt duy nhất và tính bằng Northport duy nhất với Y Pastan làm đối số đầu tiên và

số lần trả về bằng đúng.

Pastan là đối số thứ hai.

Sau đó, chúng ta sẽ tạo một chỉ mục được sắp xếp theo tên biến và đặt nó bằng NP Doug Short với quá khứ duy nhất.

Và sau đó chúng ta sẽ tạo một tên biến duy nhất và đặt bằng duy nhất tại chỉ mục được sắp xếp.

Nếu tương đối là đúng, chúng tôi sẽ đặt số đếm bằng một trăm lần đếm ở chỉ số được sắp xếp chia

của Lenoy.

Sau đó, chúng tôi sẽ đặt văn bản nhãn Y thành số phần trăm.

Ngược lại, chúng ta sẽ đặt số lượng bằng số lượng tại chỉ mục được sắp xếp.

Sau đó chúng ta sẽ thiết lập văn bản nhãn Y để đếm.

Tiếp theo, chúng ta sẽ tạo một biến có tên X temp và đặt nó bằng expedite, sắp xếp và truyền vào Len

Độc đáo.

Sau đó, chúng tôi sẽ gọi cho Paltalk Bar và người X tạm thời cộng với thời gian kết thúc.

Vì đối số đầu tiên được tính là tài liệu thứ hai nên một dòng bằng trung tâm là đối số thứ ba alpha

bằng điểm bảy làm đối số thứ tư và chiều rộng bằng chiều rộng làm đối số thứ năm.

Và sau đó chúng ta sẽ gọi Peltier's six và chuyển một nhiệt độ X duy nhất và vòng quay bằng 45.

Sau đó, chúng ta sẽ đặt nhãn X cho tất cả ô theo loại trái cây và nhãn Y cho văn bản nhãn Y.

Ngoài chức năng này, chúng tôi sẽ đặt tiêu đề cho ô của mình thành số lượng ảnh.

Sau đó, chúng ta sẽ gọi thanh cốt truyện và truyền vào tàu y với giá trị trái thấp.

Sau đó chúng ta sẽ gọi test, bar, pass và y với giá trị bên phải thấp hơn.

Tiếp theo, chúng ta sẽ gọi PLT Legend và chuyển vào một danh sách có chú giải cho biết màu nào đại diện cho

ảnh đào tạo và ảnh đại diện cho ảnh kiểm tra.

Cuối cùng, chúng ta sẽ hiển thị cốt truyện bằng cách gõ plz show khi chạy mã.

Lưu ý rằng chúng ta thấy một biểu đồ thanh.

Chúng ta hãy hiểu cách giải thích những kết quả này.

Bên trong cặp đựng táo có sáu bức ảnh về những quả táo.

Bên trong thư mục cam.

Có bốn hình ảnh quả cam cho tập dữ liệu huấn luyện.

Bạn thấy rằng có tổng cộng sáu hình ảnh đã được sử dụng.

Ba quả táo và ba quả cam.

Đây là màu xanh lam.

Chúng tôi bắt đầu với mười bức ảnh.

Nếu sáu chiếc được sử dụng trong huấn luyện thì tổng cộng còn lại bốn chiếc.

Chúng tôi sẽ gọi vào.

Chúng tôi có sáu bức ảnh Apple, nhưng ba bức ảnh được sử dụng trong đào tạo.

Vì vậy, chúng tôi có ba trong thử nghiệm của chúng tôi.

Chúng tôi bắt đầu với bốn quả cam.

Quá trình đào tạo của chúng tôi chiếm ba, vì vậy chúng tôi chỉ còn lại một.

Có vẻ như máy tính đã xác định chính xác rằng có ba quả táo và một quả cam trong bài kiểm tra

nhóm.

Công việc tuyệt vời.

Bạn vừa viết một chương trình phân loại có thể xác định chính xác hình ảnh là quả táo hay quả cam.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu về phân cụm và cách triển khai loại máy học này

thuật toán trong Python.