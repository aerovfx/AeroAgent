# 5. Định vị các phần tử

---

Trong bài giảng này, chúng ta tìm hiểu về cách định vị các phần tử bằng trình điều khiển web Selenium.

Có một số chức năng khác nhau của selen chỉ để xác định vị trí các phần tử.

Chúng tìm phần tử theo ID, tìm phần tử theo tên, tìm phần tử theo XPath, tìm phần tử theo liên kết,

văn bản tìm phần tử theo liên kết một phần văn bản tìm phần tử theo thẻ, tên, tìm phần tử theo tên lớp và tìm

phần tử bằng bộ chọn CFS tìm phần tử theo ý tưởng cho phép chúng ta lấy phần tử bằng thuộc tính ID của nó find

phần tử theo tên rất hữu ích khi chúng ta biết tên của phần tử đó.

Tìm phần tử bằng XPath cho phép chúng ta tìm một phần tử bằng đường dẫn nút Tmall của nó.

XPath là ngôn ngữ được sử dụng để định vị các nút trong tài liệu XML và HTML có thể được chuyển đổi thành ngôn ngữ đó.

Biến thể XML x html trong đó mỗi phần tử có thể duyệt được bằng cách sử dụng phần tử tìm XPath theo liên kết.

Text cho phép chúng ta tìm kiếm một phần tử dựa trên văn bản và thẻ neo tìm phần tử theo từng phần

văn bản liên kết cho phép chúng tôi lấy một phần tử khi chúng tôi biết một phần văn bản liên kết của nó, tìm phần tử theo tên thẻ cho phép chúng tôi

định vị một phần tử bằng tên thẻ của nó, tìm phần tử theo tên lớp cho phép chúng ta định vị một phần tử thông qua

tên lớp của nó.

Tìm nguyên tố bằng điện phân CSFs.

Xác định vị trí một phần tử bằng cú pháp bộ chọn CSF.

Hãy xem một ví dụ thực tế về việc định vị một phần tử trên một trang web.

Bước đầu tiên là mở trình duyệt web và điều hướng đến một URL mới.

Tôi sẽ đến Mooresville Dotcom.

Tuyệt vời.

Chúng ta thấy tất cả những điều thú vị này trên trang, nhưng làm cách nào để sử dụng selen để tương tác với chúng?

Làm cách nào để xác định một phần tử trên trang?

Google Chrome thực sự đã tích hợp sẵn các công cụ dành cho nhà phát triển cho phép chúng tôi xem mã nguồn HTML của bất kỳ

trang web.

Chúng ta sẽ nhấp vào ba dấu chấm ở góc trên cùng bên phải và đi đến các công cụ khác.

Công cụ dành cho nhà phát triển.

Thao tác này sẽ mở ra một bảng ở bên trái hiển thị một trang và ở bên phải chúng ta thấy mã nguồn HTML.

Lưu ý rằng có một phần tử meta có tên title.

Đây là yếu tố chúng tôi muốn nắm bắt, vì vậy hãy tìm hiểu cách thực hiện điều đó.

Ở đầu mã, chúng ta sẽ nhập từ trình điều khiển web nhập selen.

Sau đó chúng ta sẽ tạo một biến, đặt tên cho driver và đặt nó bằng Web driver Dot Chrome.

Tiếp theo, chúng tôi sẽ gọi Driver aGet và chuyển đồng euro cho Marvel Dotcom.

Sau đó chúng ta sẽ tạo một biến có tên Ellem và đặt nó bằng driver, tìm phần tử theo tên

và chuyển vào tiêu đề chuỗi.

Chúng tôi sử dụng chức năng này vì chúng tôi biết tên của thành phần trên tiêu đề trang Web, chúng tôi có thể thêm một số

print câu lệnh để in ra thông tin về phần tử chứng minh rằng chúng ta đã xác định đúng phần tử.

Chúng ta sẽ in ra nội dung thành phần bằng cách gõ thuộc tính Ellem get và chuyển nội dung chuỗi vào.

Sau đó chúng ta sẽ in ra tên tag bằng cách gõ tên tag Ellem.

Cuối cùng, chúng tôi muốn đóng trang web để nhập quy trình làm việc của drive khi chạy mã.

Lưu ý rằng nội dung phù hợp đã được in và kim loại được in dưới dạng thẻ, nếu chúng ta quay lại trang Web.

trang và kiểm tra phần tử đó.

Lưu ý rằng thông tin này là chính xác.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu về cách sử dụng trọng số trong trình điều khiển selen.