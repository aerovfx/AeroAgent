# 7. Tự động hóa AI chơi Pac-Man trên Web

---

Trong bài giảng này, chúng tôi sẽ sử dụng selen để tự động hóa A.I. chơi Pacman trên Web.

Đúng vậy.

Trong video này, bạn sẽ học cách viết một A.I. chơi PAC Man trực tuyến để đơn giản.

Nó sẽ chọn ngẫu nhiên phím mũi tên nào cần nhấn để điều hướng Pakman qua mê cung ở trên cùng của

mã.

Chúng tôi sẽ có một số tuyên bố quan trọng mà chúng tôi sẽ lấy từ trình điều khiển web nhập selen, từ selen,

trình điều khiển web, khóa nhập dot com và dot, nhập luồng, nhập ngẫu nhiên và nhập thời gian.

Sau đó chúng ta sẽ tạo một driver tên biến và đặt nó bằng driver web chrome.

Sau đó chúng ta sẽ gọi driver doget và pass in the world cho Google dot com.

Tiếp theo, chúng ta sẽ tạo một biến có tên Ellem và đặt nó bằng với phần tử được xác định bởi trình điều khiển theo tên và

chuyển vào hàng đợi chuỗi.

Sau đó chúng ta sẽ gọi Ellem rõ ràng.

Sau đó chúng ta sẽ gõ Pakman vào thanh tìm kiếm bằng cách gọi Ellem gửi chìa khóa và chuyển vào

chuỗi Pakman.

Tiếp theo, chúng ta sẽ mô phỏng thao tác nhấn thực thể bằng cách gõ 11. gửi các phím và chuyển các phím trả về.

Sau đó, chúng ta sẽ nhấp vào liên kết đầu tiên trên trang web bằng cách sử dụng kỹ thuật mà chúng ta đã học trước đó trong phần này.

phần.

Điều này sẽ mở ra trò chơi Pakman.

Bây giờ chúng ta cần mô phỏng việc nhấn các phím Mũi tên.

Chúng ta sẽ tạo một biến có tên là move và đặt nó bằng một danh sách chứa các phím giá trị nằm bên trái các phím

phím đó xuống, phím bên phải và phím chấm lên.

Sau đó, chúng ta cần một vòng lặp while vô hạn nên chúng ta sẽ gõ walcher bên trong vòng lặp while.

Chúng tôi sẽ nhập phần tử tìm trình điều khiển bằng bộ chọn CSF và chuyển vào iframe chuỗi.

Trò chơi Pakman của Google thực sự được lưu trữ bên trong phần tử iFrame.

Sau đó, chúng ta sẽ gọi hàm gửi và chuyển vào lựa chọn ngẫu nhiên những gì chúng ta di chuyển qua.

Và chúng tôi muốn mô phỏng một quy trình chính cứ sau nửa giây nên chúng tôi sẽ gõ hết thời gian ngủ và vượt qua

ở số không điểm năm.

Khi chúng tôi chạy mã.

Lưu ý rằng nó tự động tìm kiếm và mở trò chơi Pacman.

Sau đó cứ nửa giây nó lại dùng một mũi tên để nhấn để khiến Pacman di chuyển theo hướng đó.

Đây là một ví dụ khác về sức mạnh mà selen mang lại khi tự động hóa các hoạt động của trình duyệt Web.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu về Selenium Web Driver API.