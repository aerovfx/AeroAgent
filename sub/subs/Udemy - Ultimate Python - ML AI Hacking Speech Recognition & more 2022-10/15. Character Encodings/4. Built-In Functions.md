# 4. Chức năng tích hợp

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về một số ký tự và hàm mã hóa tích hợp mà Python

cho phép chúng tôi truy cập vào.

Để bắt đầu, chúng ta sẽ muốn mở một bảng ký tự ASCII mà chúng ta có thể tham khảo.

Chúng ta hãy mở trình duyệt Web và vào bảng ASCII dotcom.

Bạn sẽ nhận thấy rằng có một bảng ASCII.

Hãy xem lại cách sử dụng nó khi chúng ta đã tìm hiểu qua một số video trước đây.

ASCII chứa tổng cộng một trăm hai mươi tám ký tự.

Hãy chú ý số không ba, một trăm hai mươi bảy.

Và cột ngoài cùng bên trái.

Cột hiển thị các giá trị thập lục phân hay còn gọi là hex cho các ký tự này.

Cột OCT hiển thị biểu diễn số nguyên bát phân của các ký tự này.

HTML hiển thị thông tin mã hóa HTML cho các ký tự này và char hiển thị chính các ký tự đó.

Đối với ví dụ đầu tiên mà chúng tôi đang thực hiện, chúng tôi sẽ tham khảo ký hiệu số sáu mươi bốn trên

bảng ASCII.

Hãy mở trang về hàm word cho phép chúng ta chuyển đổi một chuỗi thành một điểm mã ASCII cơ số mười.

Nói một cách đơn giản, điều này có nghĩa là chúng ta gán cho nó một chuỗi và nó trả lại cho chúng ta số của chuỗi đó trên

Bảng ký tự ASCII mà chúng ta vừa xem xét.

Hãy nhớ lại rằng con số tương ứng với biểu tượng tại là sáu mươi bốn.

Chúng ta có thể in ra kết quả của lệnh gọi hàm bảng và chuyển vào biểu tượng thêm.

Khi chúng tôi chạy mã.

Lưu ý rằng giá trị ASCII của at là 64 đã được in ra bảng điều khiển.

Hàm tiếp theo mà chúng ta sẽ tìm hiểu là hàm bin.

Ben sẽ lấy số và đưa cho chúng ta biểu diễn nhị phân của nó.

Ví dụ, chúng ta biết rằng giá trị nhị phân của mười là một không một không.

Làm thế nào để chúng ta biết nhị phân này dựa vào.

Vì vậy, vị trí đầu tiên bên phải là số không.

Vị trí thứ hai bên phải là một.

Khe này đại diện cho hai trong cơ sở, hai hệ thống.

Sau đó đến một số không khác.

Khe này đại diện cho bốn trong cơ sở, hai hệ thống.

Nhưng vì cho đến nay nó vẫn là số 0 nên chúng ta chỉ có tổng giá trị bằng ô thứ tư.

Kết thúc là một.

Điều này tượng trưng cho tám trong cơ sở, hai hệ thống, tám cộng với hai bằng mười.

Để chứng minh điều này, hãy in ra kết quả của lệnh gọi hàm bin với 10 quá khứ.

Và khi chúng tôi chạy mã, hãy lưu ý rằng Olby one 010 đã được in ra từng cái một hoặc chúng tôi mong đợi.

Nhưng còn bepart cũ thì sao?

Olby là số đứng trước bất kỳ số nhị phân nào trong Python.

Đây chỉ là chiếc máy tính cho chúng ta biết rằng mọi thứ sau Olby thực chất là mã nhị phân.

Tiếp theo, chúng ta sẽ tìm hiểu về hàm ASCII của Python, hàm này cung cấp cho chúng ta chuỗi ASCII của một đối tượng.

Ví dụ: hãy in ra ASCII bằng Python Pastan.

Ngoại trừ việc chú ý những gì tôi đã làm.

Tôi đã thêm cái cũ với hai dấu chấm phía trên nó.

Tôi sẽ hướng dẫn bạn cách lấy ký tự này ngay sau đây, nhưng trước khi làm điều đó, tôi sẽ chạy mã.

Lưu ý rằng từ python đã được in ra bảng điều khiển, nhưng thay vì đặc biệt đó, ồ, chúng ta nhận được một giá trị

của dấu gạch chéo ngược sáu.

Đây là giá trị chuỗi Unicode cho ký tự đặc biệt đó.

Để chứng minh điều này, hãy in ra chuỗi dấu gạch chéo ngược ngoại trừ sáu trên bảng điều khiển.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy chữ O nhỏ trong tiếng Latinh có dấu phụ được in ra.

Điều này chứng tỏ chuỗi gạch chéo ngược x f six tương ứng với ký tự đặc biệt đó.

Trong bài giảng tiếp theo chúng ta sẽ tìm hiểu về chuỗi ký tự trong Python.