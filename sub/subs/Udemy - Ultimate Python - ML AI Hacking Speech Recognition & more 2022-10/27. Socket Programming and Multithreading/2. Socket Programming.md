# 2. Lập trình socket

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về lập trình socket socket được sử dụng để gửi tin nhắn qua một máy tính.

mạng.

Chúng được sử dụng cho những gì được biết đến trong ngành khi IPC tham gia quá trình liên lạc.

Có lẽ việc sử dụng socket phổ biến nhất là để liên lạc giữa máy khách và máy chủ.

Có hai giao thức chính mà chúng ta nói đến khi gửi tin nhắn qua mạng.

Chúng là TCP và UDP.

TCP là giao thức điều khiển truyền dẫn.

Nó đáng tin cậy và có giao hàng theo thứ tự.

UDP là viết tắt của giao thức dữ liệu người dùng nghiệt ngã.

Nó không đáng tin cậy nhưng nhanh hơn TCP.

Nếu một ngân hàng gửi thông tin về tài khoản của khách hàng giữa máy khách và máy chủ, họ sẽ

sử dụng TCP để đảm bảo thông tin được truyền đi chính xác.

Nếu một trò chơi trực tuyến có nhịp độ nhanh đang gửi dữ liệu giữa máy khách và máy chủ của nó thì nó sẽ sử dụng UDP vì

sẽ gây gián đoạn hơn nếu truyền chậm hơn là hơi không chính xác.

Hãy nói về cách máy khách và máy chủ giao tiếp trên một hệ thống.

Ổ cắm máy chủ lắng nghe các kết nối từ ổ cắm máy khách.

Nó chấp nhận một khách hàng.

Máy khách kết nối với máy chủ.

Tại thời điểm này, một cái gì đó quan trọng xảy ra.

Chúng tôi gọi nó là sự bắt tay máy chủ khách và đó là sự bắt tay ba bước diễn ra để đảm bảo rằng

cả hai bên đều có thể gửi và nhận tin nhắn.

Máy khách gửi một tin nhắn mẫu mà máy chủ nhận được.

Sau đó, máy chủ sẽ gửi cho khách hàng một tin nhắn mà khách hàng nhận được.

Cuối cùng, máy khách gửi thêm một tin nhắn để hoàn tất quá trình bắt tay, cho máy chủ biết rằng nó đã nhận được

thông điệp của nó.

Cả hai giao tiếp theo mã trong chương trình.

Sau khi giao tiếp hoàn tất, cả máy khách và máy chủ đều đóng ổ cắm của họ để hoạt động với ổ cắm.

Trong Python, chúng ta có thể sử dụng mô-đun ổ cắm ở đầu mã, hãy nhập mô-đun ổ cắm.

Tiếp theo, chúng ta sẽ tạo hai biến không đổi.

Hãy tạo một biến có tên là máy chủ và đặt nó bằng một hai bảy không không một.

Đây là một máy chủ ví dụ điển hình được sử dụng để kiểm tra thông tin liên lạc.

Sau đó chúng ta sẽ đặt cổng bằng ba nghìn.

Lưu ý rằng cổng phải trên mười hai mươi ba vì mười, hai mươi ba trở xuống được gọi là

cổng đặc quyền được dành riêng cho mục đích sử dụng đặc biệt.

Bây giờ chúng ta cần viết một số mã máy chủ bằng Python để khởi động ổ cắm máy chủ.

Chúng ta sẽ gõ bằng socket socket và chuyển vào socket afine đó làm tài liệu đầu tiên và socket soc

luồng là tài liệu thứ hai.

Sau đó, chúng tôi sẽ sử dụng từ khóa đó làm từ khóa để chỉ định bí danh của S để chúng tôi có thể tham chiếu đến ổ cắm này cho

phần còn lại của chương trình bằng cách chỉ cần gõ các chữ cái thụt vào bên dưới cùng với câu lệnh, chúng ta sẽ gọi estcourt

liên kết và chuyển vào máy chủ làm tài liệu đầu tiên và cổng làm tài liệu thứ hai.

Sau đó chúng ta sẽ gọi trạng thái không trạng thái và sau đó chúng ta sẽ tạo các biến Carna và Adir, viết tắt của

kết nối và địa chỉ tương ứng.

Chúng ta sẽ đặt những giá trị này bằng nhau để bắt đầu, ngoại trừ sau đó chúng ta sẽ nhập Carna vào bên trong câu lệnh về chiều rộng.

Chúng tôi sẽ in ra cường độ kết nối và sau đó là địa chỉ.

Sau đó chúng ta sẽ thực hiện một vòng lặp vô hạn bằng cách gõ.

Mặc dù đúng bên trong vòng lặp while, chúng ta sẽ đặt dữ liệu bằng kết nối, nhận và chuyển trong mười,

hai mươi bốn.

Nếu không có dữ liệu nào chúng tôi phá vỡ, chúng tôi sẽ gửi tất cả dữ liệu qua kết nối.

OK, bây giờ chúng ta muốn khởi động máy chủ.

Hãy mở một dòng lệnh.

Chúng tôi sẽ thay đổi thư mục thành thư mục chứa tệp Python trong đó.

Để làm điều này, chúng ta gõ CD và sau đó là đường dẫn đến thư mục đó.

Sau đó chúng ta sẽ gõ Python rồi ví dụ chấm p theo tên file và phần mở rộng file P.

Khi chúng tôi chạy mã, nhận thấy rằng thiết bị đầu cuối có vẻ bị treo.

Điều này thật tuyệt vời.

Điều đó có nghĩa là máy chủ của chúng tôi đã khởi động thành công và đang lắng nghe các kết nối.

Bây giờ chúng tôi muốn nhận xét về phần máy chủ của mã, nhưng để lại máy chủ sân bay và portlands

đã đến lúc viết mã máy khách.

Chúng ta sẽ gõ bằng socketed socket và chuyển vào socketed affinitas tài liệu đầu tiên và luồng soc socket

là tài liệu thứ hai và sau đó sử dụng từ khóa as trong bí danh.

Bên trong câu lệnh này, chúng tôi sẽ gọi là Estcourt Connect và pass, máy chủ và cổng.

Sau đó chúng ta sẽ gọi Estcourt gửi tất cả và gửi một chuỗi nhị phân kèm theo văn bản.

Nếu bạn thấy máy khách và máy chủ này được kết nối thì chúng tôi sẽ tạo một biến dữ liệu và đặt

nó bằng CV câu chuyện S và vượt qua trong mười hai mươi bốn.

Cuối cùng chúng ta sẽ in ra dữ liệu đã nhận và sau đó là dữ liệu.

Khi chúng tôi chạy mã này, bạn sẽ nhận thấy hai điều xảy ra.

Đầu tiên, máy chủ chấp nhận kết nối, như chúng ta có thể thấy từ bản in trong dòng lệnh.

Thứ hai, client in ra thông báo.

Nếu bạn thấy máy khách và máy chủ này được kết nối với nhau thì bạn làm rất tốt.

Bạn vừa học cách sử dụng lập trình socket để kết nối máy khách và máy chủ.

Trong bài giảng tiếp theo chúng ta sẽ tìm hiểu về đa luồng.