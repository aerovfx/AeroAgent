# 3. Đa luồng

---

Trong bài giảng này chúng ta sẽ tìm hiểu về đa luồng trong lập trình máy tính, một mối đe dọa là một mối đe dọa độc lập

con đường.

Mã đó có thể được thực thi trên nhiều luồng khi chúng ta sử dụng một vài luồng này để có thể

chạy mã đồng thời hay còn gọi là thực thi mã song song.

Tuy nhiên, chúng ta không cần phải giữ tất cả mã chạy trên luồng chính.

Chúng tôi có khả năng tạo một luồng và sau đó chỉ định mã mà chúng tôi muốn chạy trên luồng đó.

Mã này sẽ chạy song song với mã đang được thực thi trên luồng chính.

Một luồng được sử dụng để thực thi mã và nó có quyền truy cập vào tài nguyên và dữ liệu.

Trong luồng đó, mỗi luồng có ngăn xếp cuộc gọi riêng.

Mã không thể chạy mà không có chủ đề nếu bạn không tạo chủ đề của riêng mình.

Tất cả mã bạn viết đều chạy trên luồng chính.

Threading được xem là một chủ đề lập trình python nâng cao nhưng lại là một chủ đề quan trọng đối với bất kỳ ai

mong muốn trở thành lập trình viên chuyên nghiệp.

Python cung cấp cho chúng ta mô-đun luồng để viết mã hoạt động với các luồng trước khi chúng ta đi sâu vào đa luồng

hoặc làm việc với nhiều chủ đề.

Hãy xem một ví dụ cơ bản về cách chúng ta có thể tạo và lưu trữ luồng ở đầu chương trình.

Chúng tôi sẽ nhập mô-đun luồng.

Sau đó, chúng ta sẽ tạo một hàm hoạt động cho luồng mà bên trong chúng ta sẽ in Telo ra bảng điều khiển.

Tiếp theo, chúng ta sẽ tạo một biến có tên T, một biến mà chúng ta đặt bằng với việc xâu chuỗi luồng đó làm đối số

chúng ta sẽ chuyển vào hàm bằng mục tiêu cho luồng.

Cuối cùng, chúng ta sẽ gọi T1 dot start.

Khi chúng tôi chạy mã, bạn sẽ thấy rằng lời chào được in ra bàn điều khiển.

Bây giờ chúng ta đã có kinh nghiệm làm việc với một luồng, hãy xem một ví dụ cơ bản về đa luồng

lên ở đầu mã.

Chúng ta sẽ nhập không gian tên luồng, sau đó chúng ta sẽ định nghĩa một hàm có tên là hàm cho luồng,

nhưng lần này nó sẽ nhận một tham số có tên là NUM.

Bên trong hàm này, chúng ta sẽ in num ra bàn điều khiển.

Sau đó, chúng ta sẽ tạo một biến có tên T1 và đặt nó bằng với luồng đó.

Hàm này sẽ nhận hai đối số.

Mục tiêu sẽ là hàm cho luồng và luôn là một mảng có số một.

Tham số của OG được chỉ định cho một luồng phải là tham số có thể lặp lại, nghĩa là một bộ sưu tập có thể được lặp lại

kết thúc.

Sau đó chúng ta sẽ gọi t một dấu chấm bắt đầu để bắt đầu luồng một.

Tiếp theo chúng ta sẽ tạo một biến có tên T two và đặt nó bằng luồng thread.

Chúng ta sẽ chuyển mục tiêu của hàm cho luồng làm đối số đầu tiên.

Và Ogg's bằng một danh sách chứa giá trị bằng hai là đối số thứ hai.

Sau đó chúng ta sẽ gọi T để bắt đầu.

Sau đó, chúng ta sẽ tạo một biến có tên T3 và đặt nó bằng với việc xâu chuỗi luồng đó với mục tiêu là hàm

cho chủ đề.

Pastan là đối số đầu tiên và OBJ bằng một mảng có giá trị là ba làm đối số thứ hai.

Sau đó chúng ta sẽ gọi T là bắt đầu ba chấm.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy các giá trị.

Một, hai và ba đã được in ra bảng điều khiển.

Tạo nhiều luồng bằng tay như thế này có thể tẻ nhạt và tốn thời gian nếu chúng ta muốn làm việc với

nhiều chủ đề.

Rất may, Python cho chúng ta một cách để tự động hóa quá trình tạo và bắt đầu các luồng.

Chúng tôi thực hiện điều này bằng cái được gọi là nhóm luồng.

Người thực thi chúng tôi có thể giữ một số mã hiện có của chúng tôi.

Chúng tôi sẽ xóa mọi thứ ngoài hàm định nghĩa hàm luồng ở trên cùng của

mã, chúng ta cần nhập fut đồng thời. mô-đun.

Sau đó chúng ta sẽ gõ đồng thời dấu chấm fut. người thực thi nhóm luồng đó và đặt các công nhân khớp thành ba

và sau đó nhập là người thực thi được thụt lề dưới dòng này, chúng tôi sẽ nhập bản đồ thực thi và truyền vào hàm cho

chủ đề là tài liệu đầu tiên và từ một đến bốn của kiểm lâm viên là tài liệu thứ hai.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng nó đã thực hiện điều tương tự.

Nó tạo ra ba luồng và mỗi luồng in một số ra bàn điều khiển.

Tuy nhiên, chúng tôi đã làm điều đó chỉ với một vài dòng mã, lần này với ít nỗ lực hơn một, hai hoặc nhiều hơn.

Thứ ba, cố gắng truy cập vào cùng một tài nguyên được chia sẻ.

Điều này có thể dẫn đến các vấn đề.

Một vấn đề phổ biến được gọi là tình trạng chủng tộc.

Vì mỗi luồng độc lập và không biết liệu có bất kỳ luồng nào khác ngoài chính nó hay không,

nó chỉ lo lắng về mã hoặc chủng tộc của chính nó.

Tình trạng có thể xảy ra khi hai hoặc nhiều tài nguyên cố gắng truy cập vào cùng một phần dữ liệu hoặc tài nguyên.

Đối với ví dụ tiếp theo này, chúng ta sẽ xem xét điều gì sẽ xảy ra nếu luồng cố gắng cập nhật cơ sở dữ liệu

giá trị.

Như bạn sẽ thấy, điều này có thể có vấn đề ở phần đầu của mã.

Chúng tôi sẽ nhập các mô-đun thời gian và tương lai đồng thời.

Sau đó chúng ta sẽ định nghĩa một lớp cơ sở dữ liệu bên trong lớp này.

Chúng ta sẽ triển khai hàm Init có tham số self.

Bên trong đây chúng ta sẽ đặt giá trị tự học bằng 0.

Sau đó, chúng ta sẽ định nghĩa một hàm và cập nhật có hai tham số, self và name.

Bên trong chức năng này, chúng tôi sẽ in ra bên phải đang bắt đầu cập nhật.

Sau đó, chúng tôi sẽ đặt bản sao cục bộ bằng giá trị tự học.

Tiếp theo, chúng tôi sẽ tăng bản sao cục bộ lên một.

Sau đó, chúng ta sẽ gọi thời gian chờ là ngủ và chuyển vào 0 điểm một.

Sau đó, chúng tôi sẽ đặt giá trị tự học thành bản sao cục bộ.

Cuối cùng, chúng tôi sẽ in ra bên phải đang hoàn tất cập nhật.

Đó là tất cả đoạn mã chúng ta cần để giả mạo một bản cập nhật cơ sở dữ liệu.

Bây giờ hãy viết mã luồng bằng Python.

Đầu tiên, chúng ta sẽ tạo một biến, đặt tên cho cơ sở dữ liệu và đặt nó thành đối tượng cơ sở dữ liệu.

Tiếp theo chúng ta sẽ in ra cửa hàng cập nhật thử nghiệm.

Giá trị là và sau đó là giá trị cơ sở dữ liệu.

Tiếp theo, chúng ta sẽ gõ đồng thời với các chấm tương lai, chuỗi chấm, người thực thi nhóm và công nhân tối đa peson bằng nhau

two làm người thực thi, thì chúng ta cần một vòng lặp for để lập chỉ mục trong phạm vi một dấu phẩy ba bên dưới ở đây chúng ta sẽ

người thực thi lệnh gọi dot gửi và chuyển vào cơ sở dữ liệu dot update làm tài liệu đầu tiên và lập chỉ mục làm tài liệu thứ hai

tài liệu.

Cuối cùng, chúng tôi sẽ in ra giá trị kết thúc cập nhật thử nghiệm và sau đó là giá trị trong cơ sở dữ liệu.

Vì vậy, chúng ta phải có các luồng sẽ cập nhật giá trị cơ sở dữ liệu này.

Bạn mong đợi kết quả sẽ như thế nào vì mỗi luồng đang cập nhật giá trị bằng cách thêm một giá trị vào đó?

Đúng ra phải là hai.

Hãy cùng tìm hiểu bằng cách chạy mã.

Như bạn có thể thấy, giá trị kết thúc là một.

Tại sao lại thế này?

Hãy cùng theo dõi đoạn mã để tìm hiểu.

Chủ đề một bắt đầu chạy.

Nó thêm một cái vào bản sao cục bộ của nó, sau đó nó sẽ ngủ.

Khi đó, luồng đó sẽ khởi động một cạnh, một cạnh là bản sao cục bộ của nó.

Sau đó nó ngủ.

Thread một sẽ quay lại nơi chúng ta đã dừng lại, thay đổi giá trị trong cơ sở dữ liệu thành một rồi chuyển sang

bắt đầu từ nơi nó đã dừng lại.

Nó cho rằng cơ sở dữ liệu hiện đang giữ giá trị bằng 0, giá trị này được ghi nhớ từ trước khi nó hoạt động.

ngủ, vì nó không biết gì về luồng một vì các luồng thực thi độc lập.

Do đó, nó cũng cập nhật giá trị cơ sở dữ liệu thành một.

Đây là ví dụ thực tế về tình trạng tương tranh trong đó các vấn đề về thời gian mã ảnh hưởng đến nhiều luồng

đang cố gắng truy cập vào cùng một tài nguyên.

Bây giờ chúng ta đã hiểu vấn đề, chúng ta cần tìm ra giải pháp để đảm bảo rằng hai luồng không

có thể nói là dẫm lên ngón chân của nhau.

Trong Python, chúng tôi đã giải quyết vấn đề này bằng cách sử dụng cái được gọi là khóa.

Chỉ một luồng có thể có khóa tại bất kỳ thời điểm nào và tất cả các luồng phải đợi đến lượt sử dụng

khóa để khắc phục tình trạng cuộc đua.

Trong mã hiện tại của chúng tôi, chúng tôi chỉ cần thêm ba dòng ở đầu mã.

Theo các câu lệnh nhập khác của chúng tôi, chúng tôi sẽ nhập mô-đun luồng vào bên trong mạng.

Sau khi giá trị tự học về 0, chúng ta sẽ đặt khóa tự học để xâu chuỗi khóa đó.

Cuối cùng, bên trong bản cập nhật, chúng tôi sẽ thêm tính năng tự khóa và sau đó thụt mã bên dưới nó.

Bây giờ, khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng cơ sở dữ liệu đã được cập nhật về giá trị chính xác là 2.

Trong bài giảng tiếp theo, chúng ta sẽ xem xét tóm tắt những gì chúng ta đã học trong phần này của khóa học.