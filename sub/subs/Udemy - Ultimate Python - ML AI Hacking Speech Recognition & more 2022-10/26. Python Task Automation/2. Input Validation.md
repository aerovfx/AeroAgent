# 2. Xác thực đầu vào

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về xác thực đầu vào bằng Python ở phần trước của khóa học, chúng ta đã học

về đầu vào.

Ví dụ: chúng ta đã học được rằng chúng ta có thể sử dụng hàm nhập và chuyển một chuỗi dưới dạng thông báo tới

người dùng.

Vì vậy, nếu chúng tôi muốn người dùng nhập một số, chúng tôi có thể tạo một biến có tên là đầu vào của người dùng và đặt nó

bằng hàm đầu vào với chuỗi.

Nhập một số trong quá khứ và sau đó chúng tôi sẽ in ra đầu vào của người dùng cộng với năm với đầu vào của người dùng chuyển thành số nguyên

để nó có thể được thêm vào năm trước khi được in.

Khi chạy mã, chúng ta có thể nhập một số rồi nhấn enter và chúng ta sẽ thấy rằng năm đã được thêm vào

vào nó và được in ra bàn điều khiển.

Nhưng điều gì sẽ xảy ra nếu người dùng nhập thứ gì đó không phải là số?

Ví dụ: hãy chạy lại chương trình và nhập một chuỗi.

Lần này khi chúng ta nhấn enter, hãy lưu ý rằng chúng ta gặp lỗi.

Nếu người dùng nhập đầu vào không hợp lệ vào một chương trình thì đó là đầu vào mà chương trình không mong đợi, chẳng hạn như

chuỗi nơi mong đợi INT.

Chương trình của chúng tôi có nguy cơ gặp lỗi và bị hỏng.

Đây là lý do chúng ta cần thêm xác thực đầu vào vào chương trình của mình, xác thực đầu vào, kiểm tra

giá trị đó được người dùng nhập hoặc được định dạng chính xác.

Nó giúp chúng ta ngăn chặn lỗi hoặc lỗ hổng bảo mật.

Bây giờ chúng ta có thể thêm xác thực đầu vào cho chương trình của mình theo cách thủ công.

Nhưng tại sao lại phát minh lại bánh xe?

Bằng cách sử dụng mô-đun đầu vào PI cộng, chúng ta có thể có quyền truy cập vào các chức năng đã tích hợp xác thực đầu vào

vào chúng.

Chúng ta cần chạy lệnh PIP để cài đặt mô-đun này.

Vì vậy, hãy mở lệnh, nhắc một thiết bị đầu cuối trên máy tính của chúng ta và thay đổi thư mục thành python của chúng ta

tập tin hiện đang được lưu trữ bằng lệnh CD.

Sau đó chúng ta sẽ chạy lệnh PIP cài đặt PI input plus.

Bây giờ chúng ta đã cài đặt mô-đun PI input plus, hãy xem cách chúng ta có thể sử dụng nó để tạo mã

chúng tôi đã viết an toàn hơn ở đầu mã, chúng tôi sẽ nhập PI input plus dưới dạng PI IP.

Điều này chỉ có nghĩa là thay vì gõ PI input plus mỗi lần chúng ta muốn tham chiếu một mô-đun, chúng ta sẽ

có thể gõ IP.

Khi đó thay vì gọi hàm đầu vào của Python, chúng ta sẽ thay thế bằng đầu vào P IP.

Và bây giờ hãy chạy mã khi chúng ta nhập một chuỗi.

Bây giờ hãy lưu ý rằng chương trình cho chúng ta biết những gì chúng ta đã nhập không phải là số nguyên và sau đó là dấu nhắc chương trình

chúng tôi một lần nữa để nhập số nguyên thay vì bị lỗi.

Giả sử chúng tôi muốn hiển thị cho người dùng danh sách các lựa chọn để lựa chọn.

Chúng ta có thể sử dụng đầu vào PI cộng với chức năng menu đầu vào.

Đầu tiên, chúng ta sẽ sử dụng hàm print của Python để in ra câu hỏi lập trình yêu thích của bạn là gì

ngôn ngữ?

Sau đó, chúng ta sẽ tạo một biến có tên là đầu vào của người dùng và đặt nó bằng menu đầu vào P IP.

Hàm này có một danh sách các tùy chọn.

Chúng tôi sẽ cung cấp cho nó một danh sách có chuỗi Python, Java và C Sharp.

Cuối cùng, chúng tôi sẽ in ra thông báo mà người dùng đã chọn và sau đó nối dữ liệu đầu vào của người dùng vào chuỗi đó.

Khi chúng tôi chạy mã, bạn có thể nhập bất kỳ lựa chọn nào trong số này.

Vì bạn đang tham gia khóa học này nên tôi hy vọng câu trả lời của bạn là Python.

Tôi chỉ đùa thôi, hãy gõ tùy chọn nào bạn thích.

Khi nhấn enter, chúng ta có thể thấy tùy chọn mà chúng ta đã chọn được in trên bảng điều khiển xăng dầu.

Đầu vào, vâng.

Không có chức năng nào đưa ra cho người dùng lựa chọn có hoặc không để trả lời câu hỏi.

Ví dụ: hãy in ra câu hỏi.

Bạn có thích khóa học này không?

Sau đó, chúng ta sẽ tạo một biến có tên là đầu vào của người dùng và đặt nó bằng đầu vào P IP.

Đúng.

Không, cuối cùng chúng tôi sẽ in ra người dùng đã chọn và sau đó nối nó với đầu vào của người dùng.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng chúng tôi có thể nhập Y hoặc N rồi nhấn enter dưới dạng phản hồi có hoặc không.

Và sau đó lựa chọn của chúng tôi được in ra dựa trên mã mà chúng tôi đã viết.

Đây chỉ là một vài ví dụ về các hàm đầu vào mà PI input plus cho phép chúng ta truy cập.

Các chức năng khác bao gồm nhập liệu, nhập chuỗi, nhập ngày giờ, email và nhập mật khẩu.

Tôi thực sự khuyên bạn nên xem xét một hoặc hai chức năng khác mà mô-đun này cung cấp và bạn

hãy thử một ví dụ với nó để truy cập tài liệu mới nhất về PI input plus và nhận thêm thông tin

trên đó.

Các bạn có thể vào link sau PI input plus đọc Docs style gạch chéo n gạch chéo mới nhất để tổng hợp

những gì chúng ta đã học trong bài giảng này.

Nếu không xác thực đầu vào, chương trình của bạn có thể gặp lỗi nếu người dùng nhập nội dung không mong muốn.

Thay vì phát minh lại bánh xe, chúng ta có thể sử dụng mô-đun hiện có để xử lý xác thực đầu vào cho Python.

Lựa chọn phổ biến nhất là PI input plus và PI input plus có chức năng cho phép người dùng nhập

đầu vào đa dạng.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu về cách tự động hóa các tập tin tổ chức trên máy tính bằng cách sử dụng

Mã Python.