# 6. Boolean (bool)

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về kiểu dữ liệu bắt nạt và cách xử lý nó trong Python.

Chúng ta đã có cái nhìn sơ lược về boolean trong Python.

Có hai giá trị mà boolean có thể có là true và false.

Lưu ý rằng trong Python rất quan trọng để đảm bảo rằng true được đánh vần bằng chữ T viết hoa và false được đánh vần

bằng chữ F viết hoa nếu không mã của bạn sẽ không hoạt động.

Đây là một ví dụ về cách tạo một biến có tên bool one và dấu của giá trị true.

Đây là một ví dụ khác về việc tạo một biến có tên bool two và gán cho nó một giá trị sai, tương tự

về cách chúng ta đã học về hàm trong bài giảng trước mà chúng ta đã sử dụng để chuyển đổi một giá trị dấu phẩy động

vào một số nguyên python cung cấp cho chúng ta hàm ball để sử dụng hàm bool mà chúng ta truyền vào bất kỳ giá trị nào và

sau đó Python sẽ chuyển đổi nó thành giá trị boolean của nó.

Phần hướng dẫn này khá thú vị.

Ví dụ, giá trị boolean tương đương với một là bao nhiêu?

Hãy chuyển một cái vào hàm bool và xuất nó ra bàn điều khiển bằng hàm in python.

Khi chúng tôi chạy mã, bạn sẽ thấy rằng true được in ra bảng điều khiển.

Bất kỳ số nào, dương hay âm, sẽ đánh giá thành giá trị boolean là true.

Ví dụ: hãy chuyển vào số âm rồi chạy mã.

Bạn sẽ nhận thấy rằng true lại được xuất ra bàn điều khiển.

Số 0 là số duy nhất có giá trị boolean sai.

Để minh họa ví dụ này, hãy chuyển số 0 thành boolean bằng hàm bool của Python rồi chạy

mã.

Như bạn có thể thấy, false được xuất ra bàn điều khiển.

Các bộ sưu tập trong Python cũng có thể được chuyển đổi thành giá trị Boolean.

Tôi biết chúng ta chưa nói quá nhiều về các bộ sưu tập.

Bài giảng sau đây về danh sách sẽ bắt đầu một loạt bài giảng về từng bộ sưu tập mà Python

hỗ trợ.

Một bộ sưu tập trống được đánh giá là sai một bộ sưu tập có bất kỳ thứ gì trong đó được đánh giá là đúng.

Ví dụ: chúng ta có thể chuyển một danh sách trống vào hàm book và in kết quả ra bảng điều khiển.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy kết quả sai đó.

Bây giờ hãy thêm chuỗi tự do A, B và C vào danh sách của chúng tôi.

Bây giờ, khi chúng ta chạy mã, hãy lưu ý rằng true được xuất ra bàn điều khiển.

Trong bài giảng tiếp theo chúng ta sẽ tìm hiểu về bộ sưu tập là gì và các loại bộ sưu tập khác nhau được hỗ trợ

bằng ngôn ngữ lập trình Python.