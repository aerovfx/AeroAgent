# 8. Danh sách (danh sách)

---

Trong bài giảng này, chúng ta sẽ tìm hiểu về danh sách là gì và cách làm việc với chúng trong Python.

Danh sách là gì?

Nói một cách đơn giản, danh sách là một tập hợp dữ liệu được sắp xếp theo thứ tự.

Nếu bạn quen thuộc với các ngôn ngữ lập trình khác, hãy nghe Python tương đương với việc tăng lương.

Chúng ta hãy xem một ví dụ cơ bản.

Đầu tiên, chúng ta sẽ tạo một biến có tên là bộ sưu tập và đặt nó bằng một danh sách có các phần tử là một,

hai và ba.

Nếu muốn xem hiện tại có bao nhiêu phần tử trong danh sách của mình, chúng ta có thể sử dụng hàm land.

Ví dụ: chúng tôi sẽ gọi một hàm trong danh sách bộ sưu tập của mình và in nó ra bảng điều khiển.

Giả sử chúng ta muốn lấy một trong các giá trị ra khỏi danh sách để đơn giản, hãy thử lấy

phần tử đầu tiên trong danh sách của chúng ta, phần tử mà chúng ta có thể thực hiện việc này bằng cái được gọi là lập chỉ mục trong lập trình.

Các bộ sưu tập có thứ tự như danh sách có một khái niệm gọi là lập chỉ mục, nghĩa là mỗi phần tử của chúng

được lưu trữ ở một vị trí được đánh số cụ thể trong kho lưu trữ chỉ mục bộ sưu tập về 0.

Trong danh sách của chúng tôi, chúng tôi sẽ nói phần tử một là chỉ số bằng 0.

Phần tử thứ hai ở chỉ mục một và phần tử thứ ba ở chỉ mục thứ hai.

Khi chúng ta muốn lấy một phần tử có chỉ mục nhất định, trước tiên chúng ta nhập tên của danh sách, đó là

thu thập trong trường hợp này.

Sau đó, chúng ta sử dụng dấu ngoặc vuông bên trong là chỉ mục của phần tử mà chúng ta muốn truy cập.

Ví dụ: chúng tôi muốn truy cập phần tử tại Index 0.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng một mã đã được in ra bảng điều khiển.

Nếu chúng tôi thay đổi chỉ mục thành một và chạy mã, bạn sẽ nhận thấy rằng hai đã được in ra bảng điều khiển.

Và theo logic tương tự, việc thay đổi chỉ mục thành chỉ mục và chạy mã sẽ dẫn đến ba

được in ra bàn điều khiển.

Một sai lầm phổ biến của những người mới bắt đầu lập trình là họ quên rằng việc lập chỉ mục bắt đầu từ con số 0 và họ

cuối cùng sử dụng chỉ mục một để cố gắng tham chiếu phần tử đầu tiên trong danh sách.

Có một cách khác để tham chiếu các phần tử và đó là sử dụng chỉ mục phủ định.

Việc lập chỉ mục phủ định bắt đầu ở cuối danh sách và bắt đầu chỉ mục đó.

Ví dụ: số âm, chúng ta có thể in ra bộ sưu tập ở số âm và chúng ta sẽ thấy ba

đã được xuất ra bàn điều khiển.

Nếu chúng ta muốn truy cập cả hai trong danh sách của mình thì chúng ta có thể làm như vậy bằng cách sử dụng chỉ mục phủ định nếu chúng ta thay đổi

chỉ mục, chúng ta đang tham chiếu hai số âm rồi chạy mã.

Bây giờ bạn sẽ nhận thấy rằng hai là đầu ra.

Tôi nghĩ bạn hiểu ý.

Rõ ràng là chúng ta có thể truy cập phần tử một với chỉ số âm ba, nhưng tôi sẽ không làm bạn nhàm chán với ví dụ đó.

Chúng ta cũng có thể sử dụng nhiều chỉ mục khi làm việc với danh sách.

Ví dụ: giả sử chúng ta muốn lấy hai phần tử đầu tiên trong danh sách của mình.

Cách chúng tôi làm điều đó như sau.

Chúng ta bắt đầu với tên của danh sách và dấu ngoặc vuông giống như trước.

Sau đó đến chỉ số bắt đầu.

Chúng tôi muốn bắt đầu với chỉ số 0.

Sau đó là dấu hai chấm, rồi đến chỉ mục cuối, chỉ dành riêng.

Điều này có nghĩa là danh sách con mà chúng tôi nhận được sẽ bao gồm các phần tử ít hơn chỉ mục này một phần tử.

Vì vậy, vì chúng ta chỉ muốn lấy các phần tử một và hai nên chúng ta chỉ định chỉ mục vì chương trình sẽ

sau đó nắm các phần tử và chỉ số bằng 0 và một.

Nhưng đừng nhớ, giá trị cuối cùng là độc quyền, không bao hàm.

Chúng tôi cũng có thể chọn không chỉ định phần khởi động và giá trị trong trường hợp này.

Nếu chúng tôi không chỉ định giá trị bắt đầu thì giá trị đó sẽ tự động bắt đầu ở đầu danh sách của chúng tôi.

Ví dụ: loại bỏ giá trị bắt đầu bằng 0.

Chúng tôi sẽ có tác dụng tương tự như mã hiện tại khi chạy mã.

Lưu ý rằng đầu ra danh sách con tới bảng điều khiển chứa các phần tử một và hai.

Chúng ta cũng có thể bỏ qua giá trị cuối, nghĩa là danh sách con sẽ về cuối danh sách.

Ví dụ: loại bỏ cả hai và chạy mã sẽ dẫn đến toàn bộ danh sách được xuất ra dưới dạng

danh sách phụ mà chúng tôi đã lấy bằng kỹ thuật này.

Nếu chúng ta muốn thay đổi giá trị của một mục trong danh sách thì sao?

Chúng ta có thể làm như vậy bằng cách sử dụng chỉ mục của nó.

Ví dụ: giả sử chúng tôi muốn thay đổi giá trị ba trong danh sách của mình thành bốn quy tắc tham chiếu đầu tiên

tập hợp ở chỉ mục hai, là vị trí của giá trị ba trong danh sách, sau đó chúng tôi sẽ sử dụng

dấu bằng để đặt thành bốn.

Tiếp theo, chúng tôi sẽ in ra giá trị của bộ sưu tập tại chỉ mục khi chúng tôi chạy mã.

Bạn sẽ nhận thấy rằng bốn đã được in, có nghĩa là mã của chúng tôi đã hoạt động.

Hãy tìm hiểu cách lặp qua tất cả các mục trong danh sách.

Chúng tôi gọi đây là việc lặp lại các mục trong danh sách.

Để thực hiện điều này, chúng ta có thể sử dụng vòng lặp for.

Bước đầu tiên là nhập phạm vi sắt, sau đó chuyển vào hàm LEN và sau đó là bộ sưu tập.

Điều này yêu cầu Python tạo một biến mà tôi gọi là biến lặp và sau đó thực thi vòng lặp for

số lần của các phần tử trong bộ sưu tập trên mỗi lần lặp.

Tôi sẽ thay đổi giá trị của nó thành thành phần trong bộ sưu tập mà nó hiện đang có.

Bên trong vòng lặp for.

Chúng tôi sẽ in ra bộ sưu tập tại chỉ mục tức là.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy giá trị là một, hai và ba được in ra ở lần đầu tiên.

lần lặp của vòng lặp for.

Chúng tôi bắt đầu từ đầu danh sách của chúng tôi.

Mục đầu tiên trong danh sách của chúng tôi là một, vì vậy tôi trở thành một mục và sau đó chúng tôi in một mục ra bảng điều khiển.

Sau đó lần lặp thứ hai xảy ra.

Mục danh sách thứ hai là hai, vì vậy hai mục sẽ được in ra bảng điều khiển.

Cuối cùng là lần lặp thứ ba trên lần lặp đó, giá trị của ba lần lặp này.

Vì vậy, ba được in ra bàn điều khiển.

Điều gì sẽ xảy ra nếu chúng ta muốn kiểm tra xem một mục có trong danh sách hay không, chúng ta sẽ thực hiện việc đó như thế nào?

Chúng ta có thể kết hợp các khái niệm của câu lệnh if và python trong từ khóa để đạt được hiệu quả mong muốn.

Chúng ta bắt đầu bằng câu lệnh if và nói if có trong bộ sưu tập.

Điều này sẽ kiểm tra xem phần tử một có trong bộ sưu tập của chúng tôi hay không.

Nếu điều này đúng, chúng ta sẽ in cụm từ one is in the list ra bảng điều khiển.

Ngược lại, chúng tôi sẽ in cụm từ, một cụm từ không có trong danh sách ra bảng điều khiển khi chúng tôi chạy mã, bạn sẽ

lưu ý rằng câu lệnh if được đánh giá là đúng vì một câu lệnh nằm trong danh sách và cụm từ một nằm trong

list đã được xuất ra bàn điều khiển.

Bây giờ chúng ta đã hiểu chung về cách làm việc với danh sách trong Python, hãy tìm hiểu về

các chức năng khác nhau mà chúng ta có thể sử dụng trên danh sách.

Nếu muốn thêm một phần tử vào cuối danh sách, chúng ta có thể sử dụng hàm chắp thêm.

Ví dụ: hãy sử dụng hàm chắp thêm của Python để thêm phần tử thứ tư vào cuối danh sách của chúng ta.

Sau đó, chúng ta sẽ sử dụng chức năng in của Python để in danh sách của chúng ta ra bảng điều khiển.

Để loại bỏ tất cả các phần tử trong danh sách, chúng ta có thể sử dụng hàm clear trong đó chúng ta sẽ sử dụng hàm clear

trên danh sách bộ sưu tập của chúng tôi và sau đó in bộ sưu tập ra bảng điều khiển.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng danh sách của chúng tôi hiện trống.

Thực ra chúng ta hãy cố tình sắp xếp các thành phần trong danh sách không theo thứ tự.

Đối với ví dụ tiếp theo này, chúng ta sẽ sắp xếp thứ tự của các phần tử hai, ba và một để sắp xếp các phần tử

của một danh sách.

Trong Python, chúng ta có thể sử dụng hàm sắp xếp, ví dụ: hãy gọi hàm sắp xếp trong danh sách của chúng ta và

sau đó in nó ra bàn điều khiển.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng danh sách đã được sắp xếp.

Để đảo ngược danh sách, chúng ta có thể sử dụng hàm Python để đảo ngược.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng danh sách được in với các phần tử theo thứ tự ngược lại.

Giả sử chúng ta muốn đếm số lần phần tử thứ hai xuất hiện trong danh sách.

Để làm điều này, chúng ta có thể sử dụng hàm count của Python.

Chúng tôi sẽ in ra kết quả cuộc gọi, tài khoản thu hộ và chuyển vào khi chạy mã.

Bạn sẽ nhận thấy rằng một cái đã được in cho hội đồng.

Điều này có ý nghĩa vì có một phiên bản của phần tử thứ hai trong danh sách, chúng ta có thể kết hợp hai danh sách

bằng cách sử dụng chức năng mở rộng.

Đầu tiên, chúng ta sẽ tạo một danh sách khác có tên Bộ sưu tập hai và gán cho nó một danh sách có giá trị cho năm

và sáu.

Sau đó, chúng ta có thể kết hợp hai danh sách bằng cách thực hiện mở rộng bộ sưu tập và sau đó chuyển bộ sưu tập vào bên trong

của dấu ngoặc đơn.

Sau đó chúng tôi sẽ in ra bộ sưu tập cho hội đồng.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng bộ sưu tập không chỉ chứa các phần tử gốc của nó mà còn chứa

cũng chứa tất cả các phần tử từ bộ sưu tập đến.

Chúng ta đã xem xét cách truy xuất một phần tử theo chỉ mục của nó, nhưng nếu chúng ta muốn truy xuất thì sao?

chỉ số của một phần tử?

Để làm điều này, chúng ta có thể sử dụng hàm chỉ mục của Python chẳng hạn.

Chúng ta có thể tìm thấy chỉ mục của mục thứ hai trong danh sách bằng cách gọi hàm mục trên danh sách bộ sưu tập

và đi vào.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng một mã đã được in vì chỉ mục của Phần tử thứ hai trong bộ sưu tập của chúng tôi

danh sách là một.

Để chèn một mục vào danh sách, chúng ta có thể sử dụng chức năng chèn của Python.

Ví dụ: nếu chúng tôi muốn chèn Phần tử chín vào danh sách của mình ở chỉ số 0, chúng tôi sẽ gọi bộ sưu tập

chèn và chuyển số 0 làm đối số đầu tiên và số chín là tài liệu thứ hai.

Chúng ta cũng hãy in bộ sưu tập cho hội đồng.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng chín đã được thêm làm thành phần đầu tiên trong danh sách của chúng tôi nhờ vào

chức năng chèn.

Bây giờ chúng ta đã biết cách thêm các mục vào danh sách, nhưng điều gì sẽ xảy ra nếu chúng ta muốn xóa các mục?

Để làm điều này, chúng ta có thể sử dụng các hàm bật và xóa của Python.

Nếu muốn xóa một mục ở một chỉ mục nhất định, chúng ta có thể sử dụng hàm pop.

Ví dụ: chúng ta có thể bật phần tử ở Chỉ số 0.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy phần tử đầu tiên đã bị xóa khỏi danh sách.

Hàm xóa của Python cho phép chúng ta xóa một phần tử khỏi danh sách theo giá trị của nó.

Ví dụ: nếu muốn xóa phần tử one khỏi danh sách, chúng ta có thể sử dụng hàm delete và

vượt qua trong một.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng phần tử một không còn trong danh sách của chúng tôi.

Điều quan trọng cần lưu ý là bạn có thể có các mục thuộc nhiều loại khác nhau trong danh sách Python.

Hoàn toàn hợp lệ khi chúng ta có một danh sách chứa một chuỗi số nguyên bool hoặc thậm chí một danh sách khác.

Danh sách là một trong những bộ sưu tập quan trọng nhất và được sử dụng nhiều nhất trong Python.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu bộ dữ liệu là gì và cách làm việc với chúng trong Python.