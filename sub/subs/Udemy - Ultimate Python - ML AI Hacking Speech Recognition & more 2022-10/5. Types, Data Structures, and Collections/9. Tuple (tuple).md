# 9. Tuple (tuple)

---

Trong bài giảng này, chúng ta sẽ tìm hiểu bộ dữ liệu là gì và cách làm việc với chúng trong Python, một bộ dữ liệu giống như một

list là một bộ sưu tập có thứ tự.

Tuy nhiên, không giống như danh sách, các bộ dữ liệu không thể thay đổi được.

Điều này có nghĩa là một khi chúng ta tạo một bộ, chúng ta không thể thay đổi hoặc sửa đổi nó.

Hãy nhớ cách danh sách sử dụng dấu ngoặc vuông và phân tách các giá trị của chúng bằng dấu phẩy?

Các bộ dữ liệu tương tự nhau, nhưng sử dụng dấu ngoặc đơn thay vì dấu ngoặc vuông làm ví dụ cơ bản để tạo

một bộ dữ liệu trong Python.

Chúng ta có thể tạo một biến có tên là bộ sưu tập và đặt nó bằng một bộ giá trị.

Một, hai và ba.

Lập chỉ mục với các bộ dữ liệu hoạt động giống như cách thực hiện với bốn danh sách.

Ví dụ: chúng tôi có thể in ra bộ sưu tập tại Index 0 và chạy mã và bạn sẽ nhận thấy rằng một

đã được in ra bàn điều khiển.

Lập chỉ mục tiêu cực cũng giống nhau.

Nếu chúng tôi in ra kết quả của bộ sưu tập và lập chỉ mục số âm rồi chạy mã, bạn sẽ nhận thấy rằng

ba đã được in ra bàn điều khiển.

Xin lưu ý rằng các bộ dữ liệu là bất biến hoặc không thể thay đổi.

Chúng ta không thể sử dụng việc lập chỉ mục tập hợp một phần tử trong bộ dữ liệu thành một giá trị mới.

Ví dụ: cố gắng thay đổi phần tử ở chỉ số 0 thành 4 và sau đó chạy mã sẽ

dẫn đến một lỗi.

Lỗi cụ thể là lỗi loại, thông báo cho chúng tôi rằng đối tượng bộ dữ liệu không hỗ trợ việc gán mục.

Tuy nhiên, có một cách giải quyết để thay đổi giá trị trong bộ dữ liệu.

Về cơ bản, chúng tôi chuyển đổi bộ dữ liệu thành một danh sách, thay đổi giá trị và sau đó chuyển đổi lại thành bộ dữ liệu.

Bước đầu tiên là tạo một biến khác và chuyển bộ dữ liệu của chúng ta thành một danh sách.

Tôi sẽ tạo một biến có tên là bộ sưu tập và đặt nó bằng bộ sưu tập được chuyển đổi thành danh sách.

Sau đó chúng ta thay đổi giá trị mà chúng ta muốn thay đổi.

Trong trường hợp này, tôi sẽ thay đổi phần tử ở chỉ số 0 thành 4.

Sau đó, chúng tôi đặt biến bộ sưu tập ban đầu của mình bằng bộ sưu tập để chuyển đổi nó trở lại thành một bộ dữ liệu.

Và cuối cùng chúng ta in bộ sưu tập ra console để xem kết quả.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng phần tử đầu tiên trong bộ dữ liệu của chúng tôi hiện là số 4 thay vì

một điều chứng minh rằng mã của chúng tôi hoạt động.

Xin lưu ý rằng vì bộ dữ liệu không thể thay đổi nên chúng tôi không thể thêm các mục vào bộ dữ liệu.

Điều này sẽ gây ra lỗi và không hợp lệ trong mã Python để lặp qua các phần tử trong bộ dữ liệu mà chúng ta có thể sử dụng

một vòng lặp for.

Ví dụ, chúng ta sẽ nói với tất cả trong bộ sưu tập và bên trong vòng lặp for chúng ta sẽ in khuôn.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng kết quả là mỗi phần tử đã được in ra bảng điều khiển

của vòng lặp for để kiểm tra xem một mục có tồn tại trong bộ hay không, chúng ta có thể sử dụng từ khóa.

Ví dụ: nếu hai nằm trong bộ dữ liệu, chúng tôi sẽ in thành bộ dữ liệu ra bảng điều khiển.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng thực tế có hai trong bộ sưu tập.

Vì câu lệnh được xuất ra bảng điều khiển nên chúng ta có thể nối hai bộ dữ liệu lại với nhau bằng toán tử dấu cộng.

Trong ví dụ này, chúng ta sẽ tạo một biến có tên Collection two và gán cho nó một bộ giá trị

cho năm và sáu.

Sau đó, chúng tôi sẽ sử dụng chức năng in Python và chuyển vào bộ sưu tập cộng với bộ sưu tập khi chúng tôi chạy mã.

Bạn sẽ nhận thấy có hai bộ, bốn bộ được nối với nhau thành một bộ lớn để đếm xem một phần tử bao nhiêu lần

xảy ra trong tuple.

Chúng ta có thể sử dụng hàm đếm.

Ví dụ: chúng ta sẽ in ra kết quả của tài khoản thu thập và chuyển một kết quả vào hàm đếm

để đếm số lần xuất hiện trong tuple.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng một mã đã được in.

Vì phần tử 1 xuất hiện một lần trong bộ dữ liệu của chúng ta nên hàm chỉ mục sẽ trả về vị trí của phần tử đó.

giá trị được chỉ định trong bộ dữ liệu của chúng tôi.

Ví dụ: chúng ta có thể in ra kết quả của chỉ mục bộ sưu tập và chuyển một kết quả vào hàm chỉ mục

để lấy chỉ mục của phần tử một trong bộ dữ liệu của chúng tôi.

Khi chúng tôi chạy mã, bạn sẽ nhận thấy rằng số 0 được in vì chỉ mục của phần tử một trong bộ dữ liệu của chúng tôi

là số không.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu từ điển là gì và cách làm việc với chúng trong Python.