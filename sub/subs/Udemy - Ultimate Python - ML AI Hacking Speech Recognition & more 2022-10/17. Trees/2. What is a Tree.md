# 2. Cây là gì

---

Trong bài giảng này chúng ta sẽ tìm hiểu cây là gì, các nút của cây được nối với nhau bằng các cạnh.

Có một số thuật ngữ mà chúng ta cần hiểu để làm việc với nút gốc của cây.

một cái cây.

Chỉ có một gốc trên mỗi cây và chỉ có một đường dẫn từ gốc đến bất kỳ nút lá nào.

Lá là một nút không có con nào ghé thăm, kiểm tra giá trị của nút.

Một điều khiển ở cấp độ nút, việc tạo nút.

Có ba quy tắc chính mà tất cả các cây đều tuân theo để trở thành một cây bắt đầu từ bất kỳ nút nào, bất kỳ nút nào.

nút khác trong cây có thể đạt được.

Không có chu trình và số cạnh ít hơn số nút.

Hãy cùng tìm hiểu về các loại cây khác nhau.

Cây nhị phân là nơi mỗi nút có thể có tối đa hai con và chúng ta có lợi ích về thứ tự

mảng để tìm kiếm và danh sách liên kết để chèn và xóa.

Cây nhị phân hoàn chỉnh là cây nhị phân trong đó mọi cấp độ của cây đều được điền đầy đủ, nhưng có lẽ

không phải cấp độ cuối cùng và cấp độ cuối cùng được lấp đầy.

Từ trái sang phải, cây nhị phân đầy đủ có nghĩa là mọi nút đều có 0 hoặc 2 nút con và không có nút nào

chỉ có một đứa con.

Cây nhị phân đầy đủ và đầy đủ vừa đầy đủ vừa đầy đủ.

Tất cả các nút lá sẽ ở cùng một cấp độ và cấp độ này có số lượng nút tối đa mà nó phải có

chính xác hai đến K trừ một nút trong đó K là số cấp.

Cây tìm kiếm nhị phân có nghĩa là nút con bên trái phải có giá trị nhỏ hơn giá trị nút cha và

nút con bên phải phải có giá trị lớn hơn giá trị nút cha.

Đống là một loại cây mà việc chèn luôn đi từ trên xuống dưới, từ trái sang phải.

Nếu nó không đúng thứ tự, hãy trao đổi nó với cha mẹ và tiếp tục tăng lên heap.

Với quy trình triển khai heap này, chúng ta có thể lưu trữ một mảng.

Cây ác là cây tìm kiếm nhị phân cân bằng.

Cân bằng có nghĩa là kiểm tra chiều cao bên trái và bên phải của một số cây và không có sự khác biệt

nhiều hơn một để tự cân bằng.

Nó có thể thực hiện các thao tác sau xoay trái, xoay phải, xoay trái phải và xoay phải

xoay trái.

Cây AVL là cây tìm kiếm nhị phân.

Chúng duy trì sự cân bằng thông qua các phép quay bằng cách đảm bảo rằng chiều cao của tất cả các tầng con của nút không khác nhau nữa.

hơn một.

Nhiều nhất, một cây được đảm bảo rằng các thao tác chèn, xóa và tìm kiếm sẽ luôn có thời gian chạy là:

không đăng nhập.

Bất kể thứ tự chèn vào cây là gì, cây splay sẽ mang lại thông tin được truy cập gần đây nhất

item vào gốc của cây và làm cho mục được tìm kiếm gần đây có thể truy cập được và có thời gian cố định.

Nó nhanh hơn nhiều cho việc tra cứu, dễ thực hiện hơn cây ác, vì logic xoay là

dễ dàng hơn nhiều và cây hiển thị sẽ tự định hình lại dựa trên việc tra cứu.

Cây ternary là cây có tối đa 3 con, cây có màu đỏ đen.

Ngoài dữ liệu và con trái và con phải, các nút của cây đỏ đen còn chứa một màu

các nút nil màu đỏ hoặc đen rời đi thay vì giá trị null.

Mỗi nốt nhạc đều có màu đỏ hoặc đen, mỗi móng tay được gọi là màu đen.

Nếu một nút có màu đỏ thì cả hai nút con của nó đều có màu đen.

Mọi đường dẫn từ một nút đến lá con đều chứa cùng một số nút đen.

Cây đỏ đen tự cân bằng và việc chèn hoặc xóa xảy ra.

Các nút được xoay và đổi màu.

Cây giá trị phổ quát là cây có các giá trị giống nhau.

Cây là một loại cây.

Nó lưu trữ các ký tự tạo nên một từ.

Sẽ rất hữu ích khi tìm ra tất cả các vấn đề về từ ngữ với loại cây này.

Chúng tôi không tra cứu từng tiền tố từ gốc.

Đúng hơn, chúng tôi xây dựng dựa trên các cuộc gọi trong quá khứ.

Trong bài giảng tiếp theo chúng ta sẽ học cách tạo cây bằng mã python.