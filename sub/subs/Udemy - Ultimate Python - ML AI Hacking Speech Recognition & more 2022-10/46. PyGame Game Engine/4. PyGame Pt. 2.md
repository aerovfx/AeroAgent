# 4. PyGame Pt. 2

---

Trong bài giảng này chúng ta sẽ làm việc với pygame, cụ thể bài giảng này sẽ tập trung vào việc tải, hiển thị

và hình ảnh chuyển động khi nhấn và cắm phím.

Bước đầu tiên là tải xuống hai hình ảnh đính kèm bài giảng này dưới dạng mẫu tài nguyên khóa học,

hình ảnh ký tự dấu chấm PMG và hình ảnh môi trường mẫu dấu chấm PNG và kéo và thả chúng vào thư mục

hoặc tệp python của chúng tôi được lưu trữ, sau đó chúng tôi sẵn sàng bắt đầu viết mã.

Ở đầu mã, chúng tôi sẽ nhập pygame.

Bước tiếp theo là gọi pygame dot trong mạng để khởi tạo công cụ trò chơi pygame.

Sau đó, chúng ta sẽ tạo một biến có tên with và đặt nó bằng 960 và một biến có tên Height và đặt

nó bằng 540.

Sau đó, chúng ta sẽ tạo một biến có tên Klok và đặt nó bằng đồng hồ thời gian pygame.

Tiếp theo chúng ta sẽ tạo một biến có tên là screen thay vì bằng pygame, dot display, dot set mode

và chuyển vào một bộ giá trị có chiều cao bằng dấu phẩy.

Sau đó chúng ta sẽ có một biến có tên BGI chịu trách nhiệm giữ hình nền.

Chúng tôi sẽ đặt nó bằng pygame, hình ảnh chấm, tải dấu chấm và chuyển trong môi trường mẫu chuỗi PNG.

Sau đó, chúng ta sẽ tạo một biến có tên image và đặt nó bằng pygame, dot image, dot loading và pass

trong chuỗi ký tự mẫu hình ảnh dấu chấm png.

Tiếp theo, chúng ta sẽ thu nhỏ hình ảnh xuống bằng cách đặt hình ảnh bằng pygame, chuyển đổi tỷ lệ dấu chấm và truyền

một hình ảnh làm đối số đầu tiên và một bộ dữ liệu làm đối số thứ hai với các giá trị.

Ba trăm dấu phẩy.

Ba năm mươi.

Lưu ý rằng đây là chiều rộng bằng dấu phẩy cho hình ảnh vừa chia tỷ lệ.

Hãy tạo một biến có tên X mà chúng ta nói bằng 0 và một biến có tên Y là 250.

Chúng sẽ được sử dụng để theo dõi vị trí của biến hình ảnh lưu trữ hình ảnh cho ký tự.

Bước cuối cùng là tạo một vòng lặp vô hạn bằng cách gõ while true.

Và đây là nơi chúng tôi thêm mã để xử lý các sự kiện trong trò chơi như nhấn phím và thoát khỏi trò chơi.

Chúng ta sẽ có một vòng lặp for cho sự kiện trong sự kiện pygame.

Vào bên trong vòng lặp for.

Chúng tôi sẽ kiểm tra xem loại sự kiện có phải là pygame squit hay không.

Nếu vậy, chúng ta sẽ gọi pygame Arquit và sau đó sử dụng hàm quit của Python để đóng chương trình.

Nếu loại sự kiện là pygame Dorcy bị hỏng, chúng tôi sẽ kiểm tra xem phím đang được nhấn có bị lệch hay không.

Được rồi, nếu vậy, chúng ta sẽ sử dụng toán tử trừ bằng để giảm X đi 50 elif khóa là mũi tên phải

chìa khóa.

Chúng ta sẽ sử dụng toán tử cộng bằng để tăng X lên 50.

Sau đó chúng ta sẽ sử dụng chức năng Bullet có nhiệm vụ hiển thị và di chuyển hình ảnh.

Chúng tôi sẽ nhập screen blit và chuyển BGI làm tài liệu đầu tiên và bộ dữ liệu không có dấu phẩy bằng 0 làm

tài liệu thứ hai.

Sau đó, chúng ta sẽ gọi lại blit và chuyển một hình ảnh làm đối số đầu tiên và bộ dữ liệu có X dấu phẩy Y pastan

khi giá trị x thay đổi.

Khi nhấn phím mũi tên trái và phải, dòng này sẽ di chuyển nhân vật của chúng ta sang trái và phải.

Sau đó, chúng ta sẽ gọi pygame là màn hình lật theo sau là đồng hồ, tích tắc và chuyển sang số sáu mươi.

Khi chúng ta chạy mã, chú ý cả hình nền và ký tự đều được hiển thị.

Nếu chúng ta nhấn phím mũi tên phải thì nhân vật sẽ di chuyển sang phải.

Nếu chúng ta bấm vào mũi tên trái, nhân vật sẽ di chuyển sang trái.

Công việc tuyệt vời.

Bạn vừa học cách hiển thị và di chuyển hình ảnh bằng pygame.

Trong bài giảng tiếp theo chúng ta sẽ có thêm một số kinh nghiệm làm việc với pygame.