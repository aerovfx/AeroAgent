# 8. Mã hóa ứng dụng

---

Trong bài giảng này, chúng ta sẽ viết mã thực hành cho dự án mà chúng ta đang xây dựng bằng cách sử dụng Python

và Kivi.

Điều đầu tiên chúng ta cần làm là tải hình ảnh kèm theo bài giảng này làm tài liệu khóa học

rồi kéo nó vào thư mục lưu trữ tệp Python của chúng tôi.

Hình ảnh này dành cho màn hình giật gân của ứng dụng.

Và nếu bạn không thực hiện bước này, bạn sẽ gặp lỗi khi cố chạy mã.

Chúng ta cũng cần đảm bảo rằng chúng ta xóa tệp CSV mẫu mà chúng ta đã viết trong bài giảng trước để

nó không ảnh hưởng đến đoạn mã mà chúng ta sắp viết ở đầu chương trình.

Chúng tôi sẽ nhập một số công cụ Kivi mà chúng tôi cần.

Sau đó, chúng tôi sẽ nhập ứng dụng Kiviat từ tiện ích Kiviat UI Eckstut.

Chúng tôi sẽ nhập tiện ích từ nút bắt đầu Kiviat UI.

Chúng tôi sẽ nhập nút.

Từ Kivi, WAAX, hình ảnh đó chúng ta sẽ nhập hình ảnh từ bố cục lưới stock Kivi WAAX, chúng ta sẽ nhập

bố cục lưới từ đồ họa Kivi DOT, chúng tôi sẽ nhập màu, hình elip và đường thẳng.

Cuối cùng, chúng ta sẽ nhập windows từ cửa sổ Kiviat Coorg.

Tiếp theo, chúng ta sẽ tạo một số biến không đổi cho chương trình của mình.

Chúng ta sẽ tạo một biến có tên Red và đặt nó bằng một bộ giá trị bằng 0 và 0.

Sau đó, chúng ta sẽ tạo một biến có tên Orange và đặt nó bằng một bộ dữ liệu có giá trị là một điểm 0

sáu bốn và không.

Tiếp theo, chúng ta sẽ tạo một biến có tên là màu vàng và đặt nó bằng một bộ giá trị một, một

và số không.

Sau đó, chúng ta sẽ tạo một biến có tên Green và đặt nó bằng một bộ dữ liệu có giá trị bằng 0,

một và không.

Tiếp theo, chúng ta sẽ tạo một biến có tên Blue và đặt nó bằng một bộ giá trị 0, 0 và

một.

Sau đó, chúng ta sẽ tạo một biến có tên Dark Blue và đặt nó bằng một bộ dữ liệu có giá trị 0 point

hai chín không và không điểm năm một.

Tiếp theo, chúng ta sẽ tạo một biến có tên là Purple và đặt nó bằng một bộ dữ liệu có các giá trị một 0 và

một.

Sau đó, chúng ta sẽ có một biến tên là White và đặt nó bằng một bộ giá trị một, một và một.

Cuối cùng, chúng ta sẽ có một biến có tên Black và đặt nó thành một bộ dữ liệu có các giá trị 0, 0 và

không.

Sau đó chúng ta sẽ có một số biến không đổi cho kích thước.

Chúng ta sẽ có một biến có tên là tập nhỏ bằng hai.

Sau đó, chúng ta sẽ có một biến có tên là phương tiện mà chúng ta đã nói bằng sáu.

Tiếp theo chúng ta sẽ có một biến có tên là lớn mà chúng ta đặt bằng 12.

Hai biến toàn cục cuối cùng mà chúng ta sẽ tạo sẽ là kích thước và màu sắc hoặc kích thước.

Biến sẽ có giá trị mặc định là nhỏ và biến màu của chúng ta sẽ có giá trị mặc định là màu trắng.

Tiếp theo, chúng ta sẽ tạo một lớp sẽ xử lý logic để vẽ lên màn hình.

Đúng vậy.

Chúng tôi đang bắt đầu chạy.

Hãy sử dụng lớp Kiewa để đặt tên cho lớp sơn phụ tùng khác.

Nó sẽ mở rộng lớp widget Kiviat bên trong lớp này.

Chúng ta sẽ định nghĩa nó trên hàm touch có hai tham số là self và touch.

Bên trong hàm này, chúng ta sẽ gõ bằng canvas tự học bên trong câu lệnh về chiều rộng.

Chúng ta sẽ tạo một màu và chuyển màu dấu hoa thị.

Sau đó chúng ta sẽ thiết lập đường kính theo kích thước.

Tiếp theo, chúng ta sẽ vẽ một hình elip có vị trí có x tạm dừng của điểm chạm x trừ đường kính trên

đến vị trí Y của điểm chạm y trừ đi đường kính trên hai và kích thước có chiều cao và chiều rộng của đường kính.

Sau đó chúng ta sẽ vẽ một đường thẳng dựa trên touch với các điểm touch dot x và touch dot y và chiều rộng

về đường kính.

Về cơ bản, chúng tôi vẽ một vòng tròn nơi người dùng chạm vào.

Sau đó, nếu họ kéo ngón tay, chúng ta sẽ vẽ một đường cùng màu.

Đây là một trong những cách chúng tôi mô phỏng logic vẽ của các ứng dụng như Draw Something và vẽ.

Sau đó, chúng ta sẽ tạo một hàm có tên là di chuyển bằng cảm ứng, có hai tham số là self và touch.

Bên trong chức năng này, chúng tôi vẽ đường khi người dùng di chuyển ngón tay, chức năng chạm xuống, tay cầm

người dùng nhấn ngón tay của họ xuống.

Chức năng di chuyển khi chạm sẽ vẽ đường nối giữa các điểm khi họ kéo ngón tay.

Tiếp theo, chúng ta sẽ tạo lớp chính chịu trách nhiệm xử lý logic ứng dụng.

Chúng ta sẽ sử dụng từ khóa class và đặt tên cho lớp của chúng ta là ứng dụng vẽ của tôi.

Nó sẽ mở rộng lớp ứng dụng của Kiri.

Chúng ta sẽ định nghĩa hàm init cho lớp này.

Nó sẽ nhận hai tham số là self và dấu hoa thị, dấu hoa thị KW args bên trong hàm init.

Điều đầu tiên chúng ta sẽ làm là gọi siêu hàm và truyền vào ứng dụng Paint của tôi làm đối số đầu tiên

và self làm đối số thứ hai.

Về điều này, chúng ta sẽ gọi nó là hàm mạng với dấu hoa thị, dấu hoa thị, kw đối số trong quá khứ.

Và bước tiếp theo sẽ là tạo thuộc tính gốc và đặt nó bằng một widget.

Sau đó, chúng ta sẽ tạo một nút màu đỏ và đặt thành nút có màu nền là đỏ.

Tiếp theo, chúng ta sẽ có một biến có tên là nút màu cam mà chúng ta sẽ đặt thành nút có màu nền

màu cam.

Sau đó chúng ta sẽ có một nút màu vàng có thể thay đổi mà chúng ta sẽ đặt bằng một nút có nền

màu vàng của quá khứ.

Và tiếp theo chúng ta sẽ có một biến có tên là nút màu xanh lá cây mà chúng ta đặt bằng một nút có nền

màu xanh lá cây.

Sau đó, chúng ta sẽ có một biến có tên Blue Button cho một nút có màu nền là xanh lam.

Tiếp theo, chúng ta sẽ có một nút màu xanh đậm được đặt thành một nút có màu nền là màu xanh đậm.

Sau đó, chúng ta sẽ tạo một biến có tên Nút màu tím mà chúng ta đã nói với một nút có nền

màu tím.

Sau đó, chúng ta sẽ có một biến có tên là nút màu trắng mà chúng ta đã nói đến một nút có màu nền là

trắng.

Sau đó chúng ta sẽ có một biến nút màu đen được đặt thành một nút có màu nền đen.

Sau đó đến các nút cho kích thước.

Chúng ta sẽ có một biến có tên là nút nhỏ được đặt thành một nút có văn bản là s.

Sau đó, chúng ta sẽ có một biến có tên Medium Button cho một nút có sự kiện văn bản.

Sau đó, chúng ta sẽ có một nút lớn tên biến được đặt thành một nút có văn bản eval.

Cuối cùng là nút lưu, nút này sẽ được đặt thành a.

Với dòng chữ lưu, sau đó chúng ta sẽ có một hàm xử lý khi người dùng bấm vào

một người gọi, chúng ta sẽ đặt tên cho hàm điều khiển hàm được người gọi nhấp vào và nó sẽ nhận hai đối số, Self

và BTM.

Bên trong hàm này, chúng ta sẽ sử dụng từ khóa toàn cục cho biến màu.

Sau đó chúng ta sẽ đặt màu bằng màu nền của nút được nhấn vào.

Tiếp theo, chúng ta sẽ có một hàm xử lý khi nhấn vào nút nhỏ.

Nó sẽ được đặt tên là kích thước tay cầm, bấm nhỏ và sẽ nhận hai tham số là bản thân và nút.

Bên trong hàm này, chúng ta sẽ sử dụng từ khóa toàn cục cho biến kích thước và sau đó đặt kích thước thành nhỏ.

Sau đó, chúng ta sẽ tạo một hàm có tên là kích thước tay cầm.

Nhấp vào phương tiện có hai tham số tự và nút.

Bên trong hàm này, chúng ta sẽ sử dụng từ khóa toàn cục cho biến kích thước và sau đó đặt kích thước thành trung bình.

Cuối cùng, chúng ta sẽ có một hàm xử lý khi người dùng nhấn vào nút lớn có tên là kích thước tay cầm

bấm vào lớn.

Hàm này sẽ nhận 2 tham số là self và BTM.

Bên trong hàm này, chúng ta sẽ sử dụng từ khóa toàn cục cho biến kích thước và sau đó đặt kích thước bằng nhau

đến lớn.

Tiếp theo, chúng ta sẽ tạo một hàm có tên Handal.

Giả sử bạn đã nhấp vào thẻ điều khiển đó khi nhấp vào nút lưu, nó sẽ có hai tham số giống nhau

rằng tay cầm hoặc chức năng nhấp chuột của chúng tôi đã chấp nhận bản thân và BTN.

Bên trong hàm này, chúng ta sẽ gọi ảnh chụp màn hình dấu chấm cửa sổ và truyền chuỗi cho tên tệp

chúng tôi sẽ gọi dấu chấm ảnh chụp màn hình là PNG.

Trong trường hợp này, chúng ta cần tạo một hàm sẽ được gọi khi nhấp vào nút bắt đầu để

rằng nó tải cấp độ trò chơi chính.

Chúng ta sẽ sử dụng từ khóa def và tên hoặc hàm xử lý nút bắt đầu được bấm.

Nó sẽ có hai tham số là self và touch.

Bên trong chức năng này, chúng tôi sẽ xóa widget.

Sau đó, chúng ta sẽ tạo một thuộc tính Painter và đặt nó vào một đối tượng Paint Widget.

Sau đó chúng tôi sẽ gọi cho phụ huynh tự học, thêm widget và chuyển vào họa sĩ tự học.

Sau đó, chúng ta sẽ tạo một tên biến cho bố cục và đặt nó bằng bố cục lưới có một hàng mười ba

cột và một cột mặc định có số năm mươi.

Sau đó chúng ta sẽ gọi cho phụ huynh tự học, thêm widget và chuyển vào bố cục.

Tiếp theo, chúng ta sẽ liên kết nút màu đỏ với chức năng bấm màu của tay cầm.

Khi nhấn nút, chúng ta sẽ làm tương tự với nút màu cam.

Nút màu vàng.

Nút xanh, nút xanh, nút xanh đậm, nút tím, nút trắng và nút đen tương ứng.

Sau đó, chúng ta sẽ liên kết nút nhỏ để có một trình xử lý nhấn có kích thước tay cầm bấm nhỏ vừa

để có một trình xử lý nhấn có kích thước tay cầm.

Nhấp vào nút trung bình và nút lớn để có trình xử lý nhấn có kích thước tay cầm.

Bấm vào lớn.

Sau đó chúng ta sẽ liên kết nút lưu với chức năng lưu đã nhấp vào tay cầm.

Sau đó, chúng ta cần thêm từng nút vào bố cục.

Sử dụng chức năng thêm widget, chúng ta sẽ thêm nút màu đỏ.

Nút màu cam.

Nút màu vàng.

Nút xanh, nút xanh.

Nút màu xanh đậm.

Nút màu tím.

Nút trắng.

Nút đen, nút nhỏ, nút trung bình, nút lớn và nút lưu vào bố cục bằng cách sử dụng thêm

chức năng tiện ích.

Tiếp theo chúng ta sẽ thực hiện chức năng xây dựng.

Bên trong hàm này, chúng ta sẽ có một biến có tên IMG mà chúng ta đặt bằng một hình ảnh có nguồn

thiết lập để vẽ ab giật gân chấm màu xanh lá cây PNG.

Lưu ý rằng đây là tên của hình ảnh đính kèm bài giảng này như một tài nguyên khóa học.

Nếu vì lý do nào đó bạn đã thay đổi tên của hình ảnh này thì xin lưu ý rằng bạn sẽ phải

thay đổi chuỗi này để phản ánh bất kỳ tên nào bạn đã đặt.

Chúng ta cũng sẽ đặt thuộc tính kích thước thành một bộ có giá trị 8 bảy mươi lăm và sáu hai mươi hai.

Kích thước của hình ảnh mẫu của chúng tôi.

Sau đó chúng ta sẽ gọi phụ huynh tự học thêm widget và chuyển IMG.

Sau đó, chúng ta sẽ tạo một biến có tên BTM và đặt nó thành một nút có dòng chữ bắt đầu có kích thước là

một bộ dữ liệu có giá trị chiều rộng là 200 và giá trị chiều cao là 80, phông chữ có kích thước 50 và

vị trí 311 dấu phẩy số 0.

Lưu ý rằng bộ dữ liệu này là vị trí xy.

Vì vậy, vị trí X là 311 và vị trí Y là 0.

Sau đó, chúng ta cần gọi B10 liên kết và truyền nhấn bằng các tay cầm tự học.

Nút bắt đầu được nhấp.

Sau đó, chúng tôi sẽ gọi cho các bậc phụ huynh tự học, thêm tiện ích và chuyển BTE và cuối cùng chúng tôi sẽ quay lại

cha mẹ tự học.

Tiếp theo, chúng ta sẽ tạo một đối tượng từ lớp my Paint Shop và sau đó gọi hàm run trên đó.

Một điều chúng ta cần đảm bảo là chúng ta không có nút dấu chấm ở dòng thứ chín mươi tám sau

chúng ta gõ nút lưu nếu không sẽ khiến mã của chúng ta chạy không chính xác.

Khi chúng tôi chạy mã, bạn sẽ thấy màn hình bắt đầu tải nấu ăn, nút bắt đầu sẽ tải

phần chính của ứng dụng.

Chúng ta có thể chọn một màu có kích thước nhỏ hơn và sử dụng chúng để vẽ một hình ảnh, viết tin nhắn hoặc bất cứ điều gì khác mà bạn muốn.

cảm thấy thích làm.

Sau khi hoàn thành bức ảnh của mình, chúng ta có thể sử dụng nút lưu để lưu hình ảnh để tìm hình ảnh đã lưu.

Đơn giản chỉ cần mở thư mục.

Hiện đang lưu trữ tệp Python của bạn.

Và tìm tên mà bạn đặt cho nó, khi chúng ta click đúp vào ảnh để xem thì nó báo hình ảnh

bạn đã tạo trong chương trình.

Trong bài giảng tiếp theo, chúng ta sẽ tìm hiểu cách xây dựng và xuất ứng dụng mà chúng ta đã tạo sang thiết bị di động.