# 11 danh mục tự động hóa email

---

Trong video trước,

chúng tôi hiểu các giao thức email

và các hoạt động liên quan.

Trong video này, bạn sẽ hiểu

các danh mục của

tự động hóa email.

Hãy bắt đầu, với

sự giúp đỡ của email

tự động hóa trong studio,

bạn có thể tạo và

gửi tin nhắn tự động,

truy xuất tin nhắn,

và trích xuất dữ liệu.

Tự động hóa email cũng

giúp quản lý tin nhắn,

lưu tệp đính kèm email và

thông tin cần thiết trong

một sự xác định rõ ràng và

dạng có cấu trúc.

Studio bao gồm

hoạt động gửi,

truy xuất và

sắp xếp tin nhắn.

Có năm

các danh mục có sẵn.

Đầu tiên là tạo ra và

gửi tin nhắn tự động.

Việc này được thực hiện bằng cách sử dụng SMTP,

Outlook, Ghi chú của IBM,

và Trao đổi.

Thứ hai là lấy lại

tin nhắn và trích xuất dữ liệu.

Việc này được thực hiện bằng cách sử dụng POP3,

Outlook, IMAP, IBM

Ghi chú và trao đổi.

Thứ ba là quản lý tin nhắn.

Việc này được thực hiện bằng cách sử dụng Outlook,

IMAP, IBM Notes và Exchange.

Thứ tư là

lưu tệp đính kèm.

Việc này được thực hiện bằng cách sử dụng

hoạt động email chung.

Thứ năm là lưu tin nhắn.

Việc này được thực hiện bằng cách sử dụng

hoạt động email chung.

Có hai cái chung

hoạt động email

có sẵn để tiết kiệm

tệp đính kèm email và email

tin nhắn trên ổ đĩa cục bộ.

Đầu tiên là lưu tệp đính kèm.

Nó lưu thư

đính kèm tin nhắn

vào thư mục được chỉ định.

Nếu thư mục không

tồn tại, nó được tạo ra.

Nếu không có thư mục nào được chỉ định,

các bản tải xuống đã được lưu

trong thư mục dự án.

Các tập tin trong thư mục được chỉ định trong

cùng tên với

phần đính kèm bị ghi đè.

Các thuộc tính quan trọng

là: đường dẫn thư mục,

tin nhắn và file đính kèm.

Thuộc tính đường dẫn thư mục

xác định đường dẫn đầy đủ

của thư mục chứa

tệp đính kèm sẽ được lưu.

Thuộc tính tin nhắn xác định

đối tượng tin nhắn thư

tệp đính kèm của ai

sắp được cứu.

Thuộc tính đính kèm

giúp truy xuất tệp đính kèm.

Thứ hai là lưu

tin nhắn thư.

Nó lưu tin nhắn email

vào thư mục được chỉ định.

Nếu thư mục không

tồn tại, nó được tạo ra.

Nếu không có thư mục nào được chỉ định,

các bản tải xuống đã được lưu

trong thư mục dự án.

Các tập tin trong

thư mục được chỉ định với

cùng tên với

tin nhắn được ghi đè.

Các thuộc tính quan trọng là

: thư và đường dẫn tập tin.

Thuộc tính đường dẫn tệp xác định

đường dẫn đầy đủ ở đâu

tin nhắn thư

đối tượng cần được cứu.

Tin nhắn thư

tài sản xác định

tin nhắn thư

đối tượng cần lưu.

Đó là tất cả cho video này.

Trong video tiếp theo bạn sẽ thấy

một cuộc biểu tình trên

làm thế nào để trích xuất từ ​​

chủ đề và nội dung của email bằng cách sử dụng

thư Nhận IMAP

hoạt động tin nhắn

và lưu trữ nó trong tệp CSV.

Tôi sẽ khuyến khích bạn đi

thông qua việc trình diễn và

cố gắng lặp lại nó trên

sở hữu. Cảm ơn bạn đã xem.