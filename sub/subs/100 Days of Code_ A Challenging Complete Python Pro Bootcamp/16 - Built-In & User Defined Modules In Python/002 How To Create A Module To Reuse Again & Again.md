# 002 Cách tạo một mô-đun để sử dụng lại nhiều lần

---

Xin chào tất cả mọi người.

Chào mừng trở lại.

Trong bài học này, chúng ta sẽ xem cách tạo một mô-đun và sử dụng lại nó trong Python để tạo một mô-đun.

Chúng ta phải viết.

Mã Python trong tệp.

Và sau đó lưu nó với phần mở rộng.py.

Ví dụ.

Chúng ta hãy tạo một mô-đun có tên Toán học gạch dưới.

Đi đến thư mục mô-đun toán học.

Trong con nhện này và sau đó nhấp chuột phải.

Và sau đó bấm vào mới.

Và sau đó.

Chọn tệp Python.

Bây giờ hãy đặt cho nó một cái tên.

Bản đồ Hoạt động gạch dưới.

Và sau đó chấm pi.

Nhưng hoạt động gạch dưới.

Và sau đó.py.

Và bấm vào Lưu.

Vì vậy, đây là của chúng tôi.

Hoạt động gạch dưới toán học mô-đun.

Mỗi tệp python đều có thể hoạt động như một mô-đun python.

Nhưng ở đây chúng tôi đang trình bày bản demo về cách tạo, nhập, tải lại và sử dụng lại nó.

Chúng ta hãy viết một số mã bên trong phép toán gạch dưới này.

Ở đây chúng tôi có một số mã được xác định trước để tiết kiệm thời gian.

Đây.

Trong mô-đun toán học, chúng ta có thể tạo các hàm lớp.

Phương pháp.

Các biến và cả cấu trúc dữ liệu như danh sách, bộ dữ liệu, chuỗi, số.

Bộ, từ điển, vân vân.

Chúng ta có thể tạo bất kỳ loại đối tượng nào trong mô-đun Python.

Ở đây tôi đã tạo ra một số.

Các biến dễ dàng sử dụng trong mô-đun khác như danh sách và bộ dữ liệu.

Danh sách này bao gồm phép cộng, phép trừ, phép chia, phép nhân và phép tính lũy thừa.

Là danh sách các phần tử và một số giá trị bên trong bộ dữ liệu.

Một, hai, ba, bốn, năm.

Lên đến tám.

Đây là những.

Bộ giá trị.

Và đây là danh sách các giá trị.

Chức năng gạch dưới bản đồ.

Danh sách gạch dưới là tên được đặt cho danh sách này và một số giá trị gạch dưới là tên được đặt cho danh sách này

tuple và bản đồ này.

Mô-đun hoạt động gạch dưới có trong thư mục làm việc hiện tại.

Đó là module toán học.

Mô-đun gạch dưới toán học.

Đây là thư mục dự án.

Ở đây chúng tôi đang có.

Hai mô-đun Python.

Matt.

Hoạt động gạch dưới và giới thiệu mô-đun gạch dưới.

Chúng ta hãy chuyển sang phần giới thiệu gạch dưới mô-đun nơi chúng ta đang làm việc.

Như vậy là chúng ta đã tạo xong module và đặt tên là thao tác gạch dưới Python trong thư mục làm việc hiện tại

và sau khi tạo mô-đun.

Chúng ta phải nhập để sử dụng các đối tượng của nó trong tệp python này.

Vì vậy, để nhập mô-đun và sử dụng các biến của nó, chúng ta cần sử dụng từ khóa nhập.

Nhập khẩu.

Bằng phép toán gạch dưới.

Đây là.

Nhập khẩu.

Phương pháp.

Sau khi nhập, chúng tôi đang sử dụng phương thức thuộc tính mà chúng tôi đã sử dụng.

Trong trường hợp trước đó, đó là căn bậc hai dấu chấm toán học.

Vì vậy, chúng tôi đang sử dụng cùng một phương pháp để sử dụng các biến hoặc đối tượng của nó.

Mô-đun hoạt động gạch dưới toán học này bao gồm các biến, danh sách và bộ dữ liệu có tên math,

gạch dưới, hàm, danh sách gạch dưới và một số giá trị gạch dưới.

Chúng ta hãy sử dụng phương thức thuộc tính để tái sử dụng chúng.

Hoạt động gạch dưới điên rồ.

Đây là tên mô-đun.

Chấm.

Tên hàm hoặc tên biến hoặc tên phương thức.

Danh sách dấu gạch dưới của hàm toán học và sau đó phép toán gạch dưới dấu chấm một số giá trị.

Và chúng tôi đang lưu trữ những thứ này.

Danh sách các giá trị và bộ dữ liệu với.

Các tên mới như hàm gạch dưới toán học và giá trị bên trong này.

Mô-đun gạch dưới tệp python giới thiệu.

Hãy để chúng tôi nhập và lưu.

Danh sách và bộ dữ liệu.

Và đổi tên chúng bằng các tên biến khác nhau.

Và sau đó in.

Để xem danh sách các giá trị và bộ dữ liệu.

Như vậy ở đây chúng ta có thể thấy chúng ta đã import thành công.

Thao tác gạch dưới của Mac và chúng tôi đã sử dụng lại thành công.

Danh sách các giá trị và bộ dữ liệu sử dụng phương thức thuộc tính.

Dưới đây là danh sách các giá trị.

Và để.

Theo giải thích mô-đun Python.

Mô-đun chứa mã như các lớp, hàm, phương thức, v.v.

Như đã nói trước đó.

Một biến như danh sách, bộ dữ liệu, từ điển, vân vân.

Và bây giờ chúng ta hãy xem làm thế nào.

Mô-đun thao tác gạch dưới toán học sẽ được tìm kiếm bằng Python hoặc nói chung.

Mô-đun sẽ được tìm kiếm như thế nào trong Python.

Bất cứ khi nào một mô-đun được nhập vào Python, trình thông dịch sẽ tìm kiếm một số vị trí.

Đầu tiên, nó sẽ kiểm tra các mô-đun tích hợp.

Nếu không tìm thấy, nó sẽ tìm danh sách các thư mục được xác định trong đường dẫn dấu chấm của hệ thống.

Trình thông dịch Python tìm kiếm các mô-đun.

Theo cách sau.

Đầu tiên là nó tìm kiếm các mô-đun trong thư mục hiện tại có nghĩa là thư mục làm việc hiện tại

hoặc thư mục.

Nếu không tìm thấy mô-đun trong thư mục hoặc thư mục làm việc hiện tại thì trình thông dịch python

tìm kiếm từng thư mục trong đường dẫn python biến shell.

Cổng trăn này.

Có thể được nhìn thấy trong tập tin python.

Thư mục thực thi đó là bin.

Và trong biến hệ thống hoặc trong một số trường hợp Pythonpath là một biến môi trường bao gồm một danh sách

của các thư mục.

Và vị trí thứ ba mà trình thông dịch tìm kiếm là kiểm tra Python.

Danh sách các thư mục phụ thuộc vào quá trình cài đặt được định cấu hình tại thời điểm cài đặt Python có nghĩa là

danh sách các thư mục được định cấu hình tại thời điểm cài đặt Python có nghĩa rõ ràng là tệp python.

Các gói hoặc mô-đun được hỗ trợ tại thời điểm đó.

Cài đặt Python.

Nếu chúng ta nhập mô-đun hệ thống.

Và kiểm tra xem.

Nhưng rồi chúng ta sẽ thấy.

Danh sách của.

Thư mục.

Rằng trình thông dịch sẽ tìm kiếm mô-đun cần thiết.

Vì vậy, ở đây chúng ta có thể thấy danh sách các thư mục nơi trình thông dịch sẽ tìm kiếm mô-đun.

Tại mỗi thư mục được phân cách bằng dấu phẩy và được đặt bên trong.

Dấu phẩy đảo ngược duy nhất là C.

Chém dữ liệu chương trình anaconda ba python ba 9.zip rồi c.

Anaconda.

Nhìn thấy.

Và sau đó xem thư viện Anaconda, rồi đến các gói trang web.

Anaconda ba rồi xem.

Gói trang web Thư viện Anaconda3.

Và vân vân.

Và nhìn vào đây.

Nó cũng đang tìm kiếm trong thư mục làm việc hiện tại là hướng dẫn Udemy Python.

Đây là cách trình thông dịch python tìm kiếm.

Nhưng sẽ tìm kiếm.

Các mô-đun trong Python.

Kế tiếp.

Hãy để chúng tôi xem.

Làm thế nào để đổi tên.

Một mô-đun và tái sử dụng nó.

Chúng ta đã thấy cách đổi tên bằng phương pháp tên bí danh.

Vì vậy, không cần phải hiển thị một lần nữa.

Nhưng để minh họa, tôi sẽ trình bày những gì chúng tôi có thể.

Nhập mô-đun python là phép toán gạch dưới toán học với tên bí danh là EMP hoặc EMP hoạt động.

Phép toán gạch dưới dưới dạng.

Vì vậy khi sử dụng bí danh thay cho tên mô-đun ban đầu, chúng ta phải sử dụng tên bí danh này.

Vì vậy, thay cho phép toán gạch dưới, chúng ta có thể sử dụng dấu gạch dưới m chấm toán học.

Danh sách gạch dưới chức năng.

Vì vậy, đây là cách chúng ta có thể đổi tên.

Và tái sử dụng mô-đun ban đầu.

Bây giờ làm thế nào để sử dụng lại mô-đun Python.

Sau khi sửa đổi mô-đun gốc.

Có nghĩa là khi chúng tôi cập nhật một mô-đun, chúng tôi phải làm như vậy.

Sử dụng chức năng tải lại.

Vì vậy, đây là điều rất quan trọng mà chúng ta cần nhớ đối với các mô-đun do người dùng xác định.

Và chức năng tải lại này là.

Trình bày bên trong phương thức thư viện nhập.

Vì vậy, chúng ta hãy sửa đổi phép toán gạch dưới mô-đun.

Những gì chúng tôi vừa tạo ra.

Đi tới Mac, thao tác gạch dưới và sửa đổi bằng chức năng do người dùng xác định.

Sau khi sửa đổi, hãy thử gọi các đối tượng mô-đun mà không tải lại nghĩa là không tải lại nó

sử dụng chức năng tải lại.

Vì vậy chúng ta hãy chuyển sang.

Phù hợp với hoạt động gạch dưới.

Mô-đun.

Đầu tiên theo các bước được xác định ở đây.

Hãy để chúng tôi sửa đổi hoạt động gạch dưới toán học của mô-đun.

Chúng ta hãy chuyển sang phép toán gạch dưới.

Đây là bước một.

Và bước hai, chúng ta cần thay đổi.

Mô-đun có chức năng do người dùng xác định.

Và ở bước ba, chúng ta cần nhập mà không cần sử dụng chức năng tải lại.

Bước một, chúng ta hãy chuyển sang phép toán, phép gạch dưới và sau đó tạo hàm do người dùng xác định.

Đó là bước hai.

Hãy để chúng tôi tạo chức năng do người dùng xác định.

Vì vậy, chúng tôi đã tạo ra.

Hàm do người dùng định nghĩa là hàm gạch dưới toán học.

Bây giờ chúng ta đã tạo thành công hàm gạch dưới toán học do người dùng định nghĩa.

Phải mất hai giá trị.

Số một.

Số hai từ người dùng.

Và thực hiện phép cộng trừ phép nhân chia và trả về bội số.

Đầu ra.

Cộng, trừ.

Phép chia và phép nhân.

Đối tượng.

Chúng nằm trong hàm gạch dưới toán học.

Chức năng do người dùng xác định.

Bây giờ là bước thứ ba.

Quay lại phần giới thiệu gạch dưới mô-đun.

Và sau khi sửa đổi, hãy thử gọi các đối tượng mô-đun mà không cần tải lại.

Vậy chúng ta hãy gọi.

Hàm gạch dưới toán học của mô-đun.

Hàm nào do người dùng xác định bên trong mô-đun phép toán gạch dưới toán học này.

Vì vậy, đây là tệp hoặc mô-đun toán học gạch dưới bên trong mà chúng tôi đã tạo một người dùng xác định

cùng với danh sách và phép toán gạch dưới bộ dữ liệu.

Đây là chức năng do người dùng xác định bên trong đó.

Chúng tôi đã tạo ra một số.

Các đối tượng như cộng, trừ, chia và nhân.

Với hai số được lấy bởi .

Người dùng.

Bây giờ chúng ta cần đánh giá những điều này.

Phương pháp.

Cộng, trừ, chia và nhân.

Từ chức năng gạch dưới bản đồ.

Nằm trong mô-đun hoạt động gạch dưới toán học.

Chúng ta hãy nhập phép toán gạch dưới mô-đun.

Từ việc nhập khẩu đó.

Hàm toán học hàm gạch dưới.

Vì vậy, chúng tôi đã nhập thành công cái này, nhưng chúng tôi cần kiểm tra xem liệu chúng tôi có thể làm được hay không.

Việc sử dụng.

Không có chức năng tải lại.

Hàm gạch dưới Matt.

Vậy từ hàm này chúng ta có phép cộng vì bên trong hàm này chúng ta có phép cộng, phép trừ,

phép chia và phép nhân.

Hãy để chúng tôi.

Sử dụng phương thức thuộc tính để.

Sử dụng chúng để làm toán, cộng trừ, chia, nhân và gán hàm gạch dưới

với các biến mới như thế nào.

Phép toán cộng trừ, phép chia, phép toán nhân và phép gọi

chúng bằng cách sử dụng các câu lệnh in.

Hãy để chúng tôi xem liệu nó có hoạt động mà không cần tải lại hay không.

Chọn và chạy bằng phím F9.

Vì vậy, chúng tôi đã gặp lỗi.

Đối tượng hàm lỗi thuộc tính không có thuộc tính bổ sung.

Điều này là do chúng tôi chưa tải lại cái này.

Thao tác gạch dưới Matt sau khi sửa đổi bên trong tệp giới thiệu gạch dưới mô-đun.

Vì vậy, chúng ta hãy tải lại bằng cách sử dụng hàm tải lại bên trong phương thức thư viện nhập được tích hợp sẵn

phương pháp.

Vì vậy, ở đây chúng tôi đã làm như vậy.

Vì vậy hãy nhìn vào đây.

Vì vậy, bây giờ sau khi sửa đổi.

Chúng tôi đang sử dụng phương thức tải lại với phương thức tải lại từ thư viện nhập.

Nhập tải lại và sau đó bên trong tải lại.

Sử dụng tên mô-đun là phép toán gạch dưới.

Đây là phép toán gạch dưới tên mô-đun và chúng ta phải sử dụng tên mô-đun đó bên trong phần tải lại

phương pháp.

Vì vậy, chúng tôi có.

Đã sử dụng tên mô-đun đó bên trong phương thức tải lại.

Hãy để chúng tôi chạy cái này.

Như vậy chúng ta đã tải lại thành công.

Mô-đun hoạt động gạch dưới toán học.

Bây giờ chúng ta hãy nhập hàm toán học do người dùng xác định hàm gạch dưới toán học nằm bên trong dấu gạch dưới toán học

vận hành bằng cách sử dụng máy bơm và nhập từ khóa từ.

Nhập hoạt động gạch dưới Matt.

Hàm gạch dưới Matt.

Hãy để chúng tôi chạy cái này.

Chúng tôi đã thành công.

Đã nhập hàm gạch dưới toán học.

Và bây giờ chúng ta phải gọi.

Hoặc chúng ta cần phải giải nén.

Trước các kết quả đầu ra được trả về bởi câu lệnh return, phép cộng, phép trừ, phép nhân,

phép chia, phép nhân.

Chúng tôi biết điều đó.

Khi câu lệnh return trả về.

Nhiều đầu ra.

Nó trả về nhiều kết quả đầu ra ở dạng.

Một bộ giá trị.

Sau đó, chúng ta cần trích xuất chúng bằng cách sử dụng bộ dữ liệu, giải nén như cộng, gạch dưới, phép toán, phép trừ,

phép tính, phép chia, phép tính và phép tính nhân.

Hoặc chúng ta cũng có thể sử dụng phương thức thuộc tính để trích xuất chúng.

Chúng ta hãy thử phương pháp này.

Chúng ta đã giải nén thành công.

Nhưng chúng ta cần phải làm vậy.

Nhập.

Giá trị cho hai số, số một.

Và số hai, chúng ta hãy nhập, ví dụ: 12 cho số đầu tiên.

Và ba cho số thứ hai.

Hoặc bốn cho số thứ hai.

Được rồi bây giờ.

Sau khi giải nén, chúng ta cần hiển thị chúng bằng câu lệnh print.

Hãy để chúng tôi hiển thị chúng.

Vâng, chúng tôi đã nhận được đầu ra.

12 cộng 4 chẳng là gì ngoài 16.

Cộng, trừ, tám và chia ba, rồi nhân.

48.

Hãy để chúng tôi.

Sử dụng phương pháp DOT.

Đó là phương thức thuộc tính để nhập giống nhau.

Nhưng nó giống như vậy.

Phép cộng trừ, chia và nhân.

Vì điều đó.

Trước tiên chúng ta cần phải làm vậy.

Gọi hàm đó sau khi nhập.

Chúng ta hãy gọi hàm gạch dưới toán học vì hàm gạch dưới toán học này do người dùng xác định sẽ trả về.

Đối với kết quả đầu ra, cộng, trừ, chia và nhân.

Do đó, trước tiên chúng ta phải làm.

Nó được gọi.

Bằng cách chọn nó và chạy nó.

Hàm gạch dưới toán học.

Chúng ta hãy nhấn F9.

Hãy để chúng tôi.

Sử dụng phương thức thuộc tính để.

Tái sử dụng các kết quả đầu ra.

Chúng ta hãy vào.

Một lần nữa, con số 12.

Và lần này đối tượng ba bộ dữ liệu không có phép trừ thuộc tính.

Vì chúng ta là vậy.

Trích xuất cá nhân.

Đối tượng từ hàm do người dùng xác định.

Nhưng.

Hàm do người dùng xác định đang quay trở lại.

Đối với các phương pháp.

Cộng, trừ, chia và nhân bằng cách sử dụng câu lệnh return.

Do đó chúng ta chỉ cần giải nén bằng cách giải nén tuple.

Vì vậy, hãy để chúng tôi.

Kết nối cái này và sử dụng phương pháp giải nén tuple.

Những gì chúng tôi đã sử dụng.

Trong trường hợp trước.

Và bây giờ chúng ta hãy giải nén chúng và in bằng câu lệnh print.

Vì vậy, điều này hoạt động.

Vậy đây là cách chúng ta có thể.

Biến đổi.

Mô-đun.

Và nhập khẩu bằng cách sử dụng.

Phương pháp tải lại.

Sau khi sửa đổi, chúng ta phải sử dụng chức năng tải lại bên trong thư viện nhập.

Và sau đó nếu chúng ta sử dụng câu lệnh return.

Vậy thì chúng ta phải làm vậy.

Sử dụng phương pháp giải nén tuple để trích xuất nhiều kết quả đầu ra.

Nếu bạn không sử dụng câu lệnh return.

Sau đó chúng ta có thể sử dụng các phương thức thuộc tính.

Giống như toán học, gạch dưới, hàm trừ dấu chấm, chia dấu chấm, cộng dấu chấm và phép nhân đó.

Vì chúng ta đã sử dụng câu lệnh return để trả về nhiều kết quả đầu ra nên chúng ta phải sử dụng giải nén tuple

phương pháp trích xuất nhiều đầu ra từ hàm do người dùng xác định.

Cảm ơn đã xem bài học này.

Trong bài học tiếp theo, chúng ta sẽ thấy.

Cách sử dụng chức năng thư mục.

Và một số điều quan trọng.

Chức năng hệ thống.

Điều đó giúp ích.

Tìm kiếm các mô-đun cụ thể được tích hợp sẵn.

Các mô-đun Python.