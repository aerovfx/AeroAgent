# 3 -4.3 LSTM & GRUs.en Hoa Kỳ

---

WEBVTT

Mạng lưới thần kinh tái phát rất mạnh nhưng chúng có những điểm yếu được ghi chép rõ ràng.

Vấn đề quan trọng nhất là họ không có khả năng nắm bắt được sự phụ thuộc lâu dài do độ dốc biến mất

khi trình tự dài.

Ảnh hưởng của các bước thời gian đầu nhanh chóng mờ đi, khiến mạng không thể học các mẫu

trải dài theo thời gian.

Độ dốc bùng nổ là một vấn đề khác có thể gây mất ổn định trong quá trình đào tạo.

Để giải quyết những thách thức này, các nhà nghiên cứu đã phát triển các kiến trúc có cơ chế cổng, cụ thể là dài

mạng bộ nhớ ngắn hạn, LSTM và các đơn vị tái phát có kiểm soát Grus.

Những mô hình này giới thiệu các thành phần chuyên dụng để kiểm soát luồng thông tin, cho phép chúng

nhớ dữ liệu quan trọng trong thời gian dài trong khi quên đi những chi tiết không liên quan.

Hiện tại, chúng là lựa chọn tiêu chuẩn cho các nhiệm vụ lập mô hình trình tự trước máy biến áp.

Sự đổi mới trọng tâm đằng sau LSTM và Grus là khái niệm về cổng.

Cổng là các thành phần có thể học được để kiểm soát cách thông tin truyền qua mạng ở mỗi bước thời gian.

Một cổng quyết định giữ thông tin, cập nhật hay loại bỏ nó.

Cơ chế này cung cấp cho mạng khả năng kiểm soát chi tiết đối với bộ nhớ bằng cách ghi nhớ hoặc quên có chọn lọc.

Các đơn vị có cổng cho phép mạng lưu trữ thông tin quan trọng qua các chuỗi dài trong khi bỏ qua các thông tin không liên quan.

chi tiết.

Điều này ngăn chặn vấn đề biến mất gradient vì các tín hiệu quan trọng có thể truyền không đổi qua

nhiều bước thời gian.

Về bản chất, các cổng hoạt động giống như các công tắc quyết định mạng chú ý đến điều gì, cung cấp giải pháp

đến những hạn chế cốt lõi của RNN vanilla.

Một ô LSTM chứa một số thành phần phối hợp với nhau để quản lý bộ nhớ.

Cổng quên quyết định phần nào của trạng thái ô trước đó sẽ bị loại bỏ.

Cổng đầu vào xác định thông tin mới nào từ đầu vào hiện tại và trạng thái ẩn sẽ được lưu trữ.

Những cập nhật này sửa đổi trạng thái ô, hoạt động như một đường dẫn bộ nhớ dài hạn mang thông tin

qua nhiều bước thời gian.

Cuối cùng, cổng đầu ra xác định thông tin nào sẽ được gửi đến trạng thái ẩn và cuối cùng

tới đầu ra của mạng.

Kiến trúc này cho phép LSTM duy trì thông tin trong thời gian dài, khắc phục độ dốc biến mất

vấn đề và cho phép mạng học các phụ thuộc dài hạn một cách hiệu quả.

LSTM được xác định bởi một tập hợp các phương trình mô tả cách các cổng tương tác.

Cổng quên tính toán Ft quyết định phần nào của bộ nhớ trước đó.

Để giữ cổng đầu vào.

Nó quyết định nên thêm bao nhiêu bộ nhớ CT mới.

CT trạng thái ô sau đó được cập nhật bằng cách kết hợp bộ nhớ được giữ lại từ quá khứ và ứng cử viên mới.

Cuối cùng, cổng đầu ra OT xác định trạng thái ẩn HT, TT, được tạo ra bằng cách áp dụng tan

h sang trạng thái ô được cập nhật và chia tỷ lệ theo cổng.

Những phương trình này đảm bảo rằng thông tin được truyền đi một cách có kiểm soát, duy trì các tín hiệu quan trọng

và ngăn chặn sự biến mất độ dốc trên các chuỗi dài.

Mặc dù LSTM mạnh mẽ nhưng chúng cũng phức tạp.

Các đơn vị định kỳ có cổng hoặc Grus được giới thiệu như một giải pháp thay thế đơn giản hơn.

Grus kết hợp trạng thái ẩn và trạng thái ô thành một vectơ duy nhất, làm giảm độ phức tạp.

Thay vì 3 cổng, Grus chỉ sử dụng 2 cổng cập nhật và cổng reset.

Cổng cập nhật kiểm soát lượng thông tin trong quá khứ được chuyển tiếp, trong khi cổng đặt lại

kiểm soát mức độ quên của trạng thái trước đó khi kết hợp đầu vào mới.

Mặc dù có thiết kế đơn giản hơn nhưng Grus thường thực hiện ngang bằng với LSTM trong nhiều tác vụ và nhanh hơn

để huấn luyện do có ít tham số hơn.

Điều này khiến chúng trở nên hấp dẫn đối với các ứng dụng đòi hỏi hiệu quả tính toán, các phương trình Gru

đơn giản hơn so với LSTM.

Cổng cập nhật ZT xác định trạng thái ẩn trước đó là bao nhiêu.

Để giữ cổng đặt lại, RT quyết định lượng thông tin trong quá khứ cần loại bỏ khi tính toán

trạng thái ẩn ứng viên.

Trạng thái ứng cử viên HT sau đó được tính toán bằng cách sử dụng trạng thái ẩn đã sửa đổi được đặt lại và đầu vào hiện tại.

Cuối cùng, trạng thái ẩn mới HT được tính bằng sự kết hợp có trọng số của trạng thái ẩn cũ và

trạng thái ứng cử viên được kiểm soát bởi cổng cập nhật.

Công thức tinh tế này cho phép Grus nắm bắt các phần phụ thuộc theo thời gian trong khi yêu cầu ít tham số hơn

hơn LSTM, làm cho chúng có hiệu quả tính toán trong khi vẫn mạnh mẽ.

LSTM và Grus đều giải quyết được những hạn chế của RNN thông thường, nhưng chúng khác nhau về thiết kế.

LSTM sử dụng ba cổng và trạng thái ô riêng biệt, giúp chúng linh hoạt hơn và kiểm soát bộ nhớ hơn.

Mặt khác, Grus đơn giản hóa việc này chỉ bằng hai cổng và không có trạng thái ô riêng biệt, khiến chúng

hiệu quả hơn để đào tạo.

LSTM có xu hướng hoạt động tốt hơn một chút trong các tác vụ yêu cầu kiểm soát bộ nhớ chi tiết, trong khi Grus thì

được ưu tiên trong các tình huống mà hiệu quả và sự hội tụ nhanh hơn là quan trọng.

Trong thực tế, sự lựa chọn thường phụ thuộc vào thử nghiệm vì cả hai kiến trúc đều hoạt động tốt trên

một loạt các nhiệm vụ liên quan đến dữ liệu tuần tự.

LSTM và Grus được sử dụng rộng rãi trong các lĩnh vực nhận dạng và tổng hợp giọng nói.

Họ mô hình hóa các tín hiệu âm thanh tuần tự để phiên âm chính xác và giọng nói có âm thanh tự nhiên.

Trong dịch máy.

Chúng ánh xạ các chuỗi trong ngôn ngữ này sang ngôn ngữ khác, tạo thành xương sống của hệ thống dịch thuật.

Trước khi Transformers tiếp quản việc tạo văn bản và chatbot, LSTM và Grus tạo ra sự mạch lạc và

phản hồi nhận biết ngữ cảnh, một bước tiến lớn cho AI đàm thoại trong lĩnh vực tài chính.

Họ phân tích dữ liệu chuỗi thời gian lịch sử để dự đoán các xu hướng như giá cổ phiếu hoặc rủi ro thị trường.

Khả năng nắm bắt sự phụ thuộc dài hạn của chúng khiến chúng phù hợp với hầu hết mọi dữ liệu tuần tự

nhiệm vụ.

Mặc dù thành công nhưng LSTM và Grus vẫn xử lý các trình tự theo từng bước, khiến chúng chậm hơn

cho đầu vào dài so với các mô hình song song như máy biến áp.

Cơ chế ghi nhớ của họ, mặc dù hiệu quả nhưng bị hạn chế so với khả năng tự chú ý, có thể nắm bắt được

sự phụ thuộc trực tiếp giữa hai vị trí bất kỳ trong một chuỗi.

Việc đào tạo LSTM và GPU trên đầu vào dài có thể tốn kém về mặt tính toán và hiệu suất của chúng thường

tụt hậu so với máy biến áp trong các nhiệm vụ quy mô lớn.

Vì những lý do này, máy biến áp đã thay thế phần lớn LSTM và Grus trong hệ thống giọng nói và NLP hiện đại.

Tuy nhiên, LSTM và Grus vẫn quan trọng trong các ứng dụng quy mô nhỏ hơn và là bước đệm về mặt khái niệm

hướng tới sự hiểu biết các mô hình dựa trên sự chú ý.

Tóm lại, LSTM và Grus đã giới thiệu các kiến trúc có kiểm soát nhằm giải quyết các hạn chế chính của vanilla

RNN.

LSTM sử dụng cổng quên đầu vào và đầu ra, cùng với trạng thái ô riêng biệt để quản lý bộ nhớ dài hạn.

Grus đơn giản hóa việc này bằng cách chỉ sử dụng hai cổng cập nhật và đặt lại trong khi kết hợp các trạng thái ẩn và trạng thái ô thành

một.

Cả hai mô hình đều nắm bắt được sự phụ thuộc lâu dài một cách hiệu quả và đã được áp dụng trong lời nói, NLP và thời gian

dự đoán hàng loạt.

Trong khi Transformers đã trở thành kiến trúc thống trị cho các nhiệm vụ theo trình tự, LSTM và Grus vẫn

các khái niệm cần thiết để hiểu sự phát triển của mô hình trình tự.

Chúng đại diện cho cầu nối giữa các mô hình lặp lại đơn giản và các mạng dựa trên sự chú ý hiện đại.