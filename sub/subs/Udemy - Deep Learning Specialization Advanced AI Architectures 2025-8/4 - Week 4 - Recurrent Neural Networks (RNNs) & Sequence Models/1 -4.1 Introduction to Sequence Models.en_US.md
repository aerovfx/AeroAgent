# 1 -4.1 Giới thiệu về Sequence Models.en US

---

WEBVTT

Các mô hình trình tự được thiết kế để xử lý dữ liệu khi thứ tự quan trọng.

Không giống như các mạng nơ-ron thông thường xử lý đầu vào một cách độc lập, các mô hình chuỗi xem xét các mối quan hệ

giữa các phần tử trong một chuỗi.

Điều này rất cần thiết vì trong các tác vụ như nhận dạng giọng nói, lập mô hình ngôn ngữ hoặc dự đoán giá cổ phiếu,

ý nghĩa của một yếu tố phụ thuộc rất nhiều vào những gì đến trước hoặc đến sau.

Các mô hình trình tự có khả năng ánh xạ các trình tự đầu vào thành các trình tự đầu ra, nắm bắt cả các chuỗi ngắn hạn

và sự phụ thuộc lâu dài.

Chúng đã trở nên thiết yếu trong việc dự báo chuỗi thời gian.

Xử lý ngôn ngữ tự nhiên và nhận dạng giọng nói.

Nói tóm lại, các mô hình trình tự là nền tảng của hệ thống AI hiểu hoặc tạo ra dữ liệu theo thời gian.

Một trong những lý do chính khiến chúng ta cần mô hình trình tự là vì thứ tự rất quan trọng trong ngôn ngữ tự nhiên.

Những từ giống nhau theo một thứ tự khác nhau có thể có nghĩa là những thứ hoàn toàn khác nhau.

Ví dụ: chó cắn người và người cắn chó.

Trong nhận dạng giọng nói.

Tín hiệu âm thanh là dạng sóng tuần tự trong đó mỗi âm thanh phụ thuộc vào âm thanh trước và sau nó.

Trên thị trường tài chính, giá cổ phiếu phụ thuộc vào những chuyển động và xu hướng trước đó chứ không chỉ phụ thuộc vào giá hiện tại.

Các mô hình truyền thống coi đầu vào là độc lập không nắm bắt được bối cảnh này, đó là lý do tại sao các mô hình chuyên biệt

mô hình trình tự là cần thiết.

Chúng được thiết kế để duy trì trật tự tuần tự và tìm hiểu sự phụ thuộc theo thời gian hoặc vị trí.

Các mô hình trình tự có thể xử lý các mối quan hệ đầu vào đầu ra khác nhau trong cài đặt 1-1.

Một đầu vào duy nhất tạo ra một đầu ra duy nhất, như trong phân loại truyền thống từ một đến nhiều.

Một đầu vào duy nhất tạo ra một chuỗi, chẳng hạn như tạo chú thích cho hình ảnh.

Trong nhiều đối một, một chuỗi đầu vào tạo ra một đầu ra duy nhất, giống như dự đoán cảm xúc từ một câu

hoặc dự báo thời tiết ngày mai từ dữ liệu trong quá khứ.

Cuối cùng, trong nhiều đối nhiều, cả đầu vào và đầu ra đều là các chuỗi, như trong dịch ngôn ngữ, trong đó

một câu tiếng Anh được ánh xạ tới một câu tiếng Pháp.

Các chế độ khác nhau này cho thấy tính linh hoạt của các mô hình trình tự và giải thích lý do tại sao chúng được áp dụng rộng rãi.

trên khắp các miền.

Dữ liệu chuỗi thời gian là một trong những trường hợp sử dụng phổ biến nhất cho các mô hình chuỗi.

Ở đây, đầu vào được sắp xếp theo thứ tự các bước thời gian như giá cổ phiếu hàng ngày, dữ liệu thời tiết hàng giờ hoặc từng phút.

nhu cầu điện phút.

Thách thức là dự báo các giá trị trong tương lai dựa trên các xu hướng trong quá khứ.

Các mô hình trình tự vượt trội ở điểm này vì chúng nắm bắt được các mẫu và sự phụ thuộc theo thời gian.

Ví dụ, trong tài chính, các mô hình dự đoán biến động chứng khoán dựa trên dữ liệu lịch sử.

Về năng lượng, họ dự báo mức tiêu thụ điện bằng cách tìm hiểu các mô hình hàng ngày và theo mùa.

Không giống như các mô hình tĩnh, các mô hình trình tự có thể thích ứng với các luồng dữ liệu đang phát triển, khiến chúng trở nên lý tưởng cho các ứng dụng thực tế.

vấn đề dự báo thế giới

Nhận dạng giọng nói là một ứng dụng quan trọng khác của mô hình trình tự.

Tín hiệu âm thanh là dạng sóng tuần tự trong đó ý nghĩa được mã hóa theo thứ tự và thời lượng của âm thanh.

Hệ thống nhận dạng giọng nói phải nắm bắt chuỗi âm vị và ánh xạ chúng thành từ.

Ví dụ: cụm từ hello world được nhận dạng bằng cách học thứ tự và thời gian của các âm vị.

Các mô hình trình tự cũng được sử dụng trong nhận dạng người nói, trong đó mục tiêu là xác định ai đang nói

dựa trên mẫu giọng nói.

Xử lý các phụ thuộc tầm xa là rất quan trọng trong lời nói, vì ý nghĩa của âm thanh có thể phụ thuộc vào

bối cảnh một vài bước trước đó.

Các mô hình trình tự có thể thực hiện được các ứng dụng này bằng cách duy trì trật tự trong tín hiệu đầu vào.

Xử lý ngôn ngữ tự nhiên có lẽ là lĩnh vực có ảnh hưởng nhất trong đó các mô hình trình tự được sử dụng.

Văn bản vốn có tính tuần tự, vì ý nghĩa phụ thuộc vào thứ tự của các từ.

Các mô hình trình tự nắm bắt những phụ thuộc này, cho phép các ứng dụng như dịch máy, trong đó

một câu bằng tiếng Anh được ánh xạ sang tiếng Pháp.

Họ cũng hỗ trợ chatbot và AI đàm thoại bằng cách tạo ra phản hồi dựa trên ngữ cảnh.

Tóm tắt văn bản là một ứng dụng khác trong đó một đoạn văn dài được cô đọng thành một phiên bản ngắn hơn trong khi

bảo tồn ý nghĩa.

Ngày nay, máy biến áp thống trị các nhiệm vụ NLP, nhưng chúng được xây dựng trên nền tảng mô hình hóa trình tự.

Hiểu các mô hình trình tự là chìa khóa để hiểu cách các hệ thống AI hiện đại như GPT, Bert và

công cụ dịch thuật hoạt động.

Mặc dù các mô hình trình tự rất mạnh mẽ nhưng chúng cũng có những thách thức.

Một khó khăn là xử lý độ dài đầu vào thay đổi.

Câu và tín hiệu âm thanh có thể khác nhau về kích thước, đòi hỏi kiến ​​trúc linh hoạt.

Một thách thức khác là nắm bắt được sự phụ thuộc tầm xa, trong đó ý nghĩa của một từ hoặc âm thanh phụ thuộc vào

về điều gì đó đã xảy ra nhiều bước trước đó.

Các mạng tái phát truyền thống gặp khó khăn với vấn đề này do độ dốc biến mất hoặc bùng nổ, làm suy yếu

khả năng học các chuỗi dài.

Các mô hình trình tự cũng yêu cầu cơ chế bộ nhớ để giữ lại bối cảnh trong quá khứ trong khi xử lý các đầu vào mới.

Vượt qua những thách thức này đã dẫn đến những đổi mới như LSTM, Grus và Transformers, chúng tôi sẽ

khám phá sau trong mô-đun này.

Trước khi học sâu, các tác vụ theo trình tự được xử lý bằng các mô hình xác suất như Mô hình Markov ẩn

hoặc HMM.

Những mô hình này giả định rằng trạng thái hiện tại chỉ phụ thuộc vào trạng thái trước đó, một thuộc tính được gọi là

giả định Markov.

HMM được sử dụng rộng rãi trong nhận dạng giọng nói và một phần của việc gắn thẻ giọng nói.

Tuy nhiên, chúng bị hạn chế về khả năng nắm bắt các phụ thuộc phức tạp hoặc các mẫu tầm xa.

Khi các nhiệm vụ trở nên đòi hỏi khắt khe hơn, HMM được thay thế bằng các mô hình trình tự dựa trên học sâu như RNN

và LSTM, có thể mô hình hóa các mối quan hệ thời gian phức tạp hơn.

Tuy nhiên, điều quan trọng là phải thừa nhận HMM là nền tảng để xây dựng mô hình trình tự hiện đại.

Sự phát triển của các mô hình trình tự bắt đầu với các mạng thần kinh tái diễn, hay RNN, đưa ra

niệm về bộ nhớ bằng cách mang các trạng thái ẩn qua các bước thời gian.

Tuy nhiên, RNN gặp khó khăn với các chuỗi dài do độ dốc biến mất.

Điều này dẫn đến các mạng bộ nhớ ngắn hạn dài, LSTM và các đơn vị tái phát có kiểm soát.

Grus, đã giới thiệu các cơ chế kiểm soát để quản lý luồng thông tin hiệu quả hơn.

Trong khi LSTM và Grus là công nghệ tiên tiến trong nhiều năm thì sự ra đời của máy biến áp đã thay đổi mọi thứ.

Máy biến áp xử lý các trình tự song song thay vì từng bước và cơ chế tự chú ý của chúng

cho phép họ nắm bắt các phụ thuộc tầm xa một cách hiệu quả.

Ngày nay, hầu hết các mô hình trình tự tiên tiến đều dựa trên máy biến áp, bao gồm Bert, GPT và T5.

Sự thay đổi này đại diện cho kỷ nguyên hiện đại của mô hình trình tự.

Tóm lại, các mô hình trình tự được thiết kế để xử lý dữ liệu ở những nơi có thứ tự quan trọng.

Chúng nắm bắt sự phụ thuộc theo thời gian hoặc vị trí, khiến chúng trở nên cần thiết cho các nhiệm vụ như chuỗi thời gian

dự báo, nhận dạng giọng nói và xử lý ngôn ngữ tự nhiên.

Các mô hình trình tự phải đối mặt với những thách thức như độ dài đầu vào thay đổi, độ dốc biến mất và phạm vi dài

phụ thuộc, nhưng những đổi mới như LSTM và Transformers đã giải quyết được những vấn đề này.

Trong lịch sử, mô hình trình tự đã phát triển từ các mô hình xác suất như HMM đến các mạng lặp lại như

RNN cho đến các kiến trúc tiên tiến như máy biến áp đang thống trị lĩnh vực này ngày nay.

Hiểu các mô hình trình tự đặt nền tảng cho việc xây dựng các hệ thống AI có thể xử lý, hiểu,

và tạo ra dữ liệu theo thời gian.

Hỗ trợ mọi thứ, từ dự đoán chứng khoán đến trợ lý ảo.