# 4 -4.4 Cơ chế chú ý.en US

---

WEBVTT

Mặc dù RNN, LSTM và Grus đã cải tiến mô hình trình tự nhưng chúng vẫn gặp phải những hạn chế.

Thông tin trong các mô hình này chảy tuần tự qua các trạng thái ẩn, tạo ra nút cổ chai theo trình tự

mọc dài hơn.

Thông tin ban đầu có xu hướng bị lãng quên hoặc bị pha loãng, khiến mô hình khó nắm bắt được lâu dài.

sự phụ thuộc của thuật ngữ.

Ví dụ, trong một câu dài, chủ ngữ có thể cách động từ nhiều từ và mạng lưới

có thể không kết nối chúng một cách chính xác.

Sự chú ý được đưa ra như một cơ chế để khắc phục những vấn đề này.

Thay vì nén tất cả thông tin vào một trạng thái ẩn duy nhất, sự chú ý cho phép mô hình tập trung vào

trực tiếp trên các phần có liên quan nhất của trình tự.

Điều này cải thiện đáng kể hiệu suất của các tác vụ như dịch thuật, tóm tắt và trả lời câu hỏi.

Ý tưởng trung tâm của sự chú ý là không phải tất cả đầu vào trong một chuỗi đều có tầm quan trọng như nhau đối với một

nhiệm vụ.

Trong nhận thức của con người, chúng ta tập trung có chọn lọc vào những thông tin phù hợp nhất và bỏ qua những thông tin còn lại.

Tương tự, cơ chế chú ý gán các trọng số khác nhau cho các phần khác nhau của chuỗi đầu vào,

làm nổi bật những yếu tố quan trọng nhất.

Những trọng số này có thể học được, nghĩa là mô hình sẽ phát hiện ra trong quá trình đào tạo những từ hoặc bước thời gian nào

quan trọng nhất đối với một dự đoán.

Ví dụ, trong bản dịch, mô hình có thể tập trung vào các từ cụ thể trong câu nguồn khi tạo ra

mỗi từ trong câu đích.

Bằng cách cho phép sự tập trung có chọn lọc này, sự chú ý mang lại sự linh hoạt và sức mạnh hơn so với cách tập trung truyền thống.

các mô hình tuần tự

Một trong những ứng dụng thành công đầu tiên của sự chú ý là dịch máy.

Trước sự chú ý.

Các mô hình trình tự dựa vào việc mã hóa toàn bộ câu nguồn thành một trạng thái ẩn duy nhất,

sau đó được sử dụng để tạo ra bản dịch.

Nút thắt này hạn chế hiệu suất, đặc biệt đối với các câu dài.

Với sự chú ý, thay vì dựa vào một trạng thái ẩn được nén, bộ giải mã có thể tham dự tất cả các nguồn

trạng thái ẩn khi tạo ra mỗi từ.

Điều này cho phép mô hình căn chỉnh các từ trong câu mục tiêu với các từ phù hợp nhất trong nguồn

câu.

Kết quả là bản dịch chính xác hơn và xử lý các câu dài tốt hơn.

Bước đột phá này đã cho thấy sức mạnh thực sự của sự chú ý và mở đường cho Transformers.

Chú ý hoạt động bằng cách so sánh ba truy vấn thành phần, khóa và giá trị.

Truy vấn thể hiện những gì mô hình đang tìm kiếm ở một bước nhất định, chẳng hạn như vị trí giải mã hiện tại.

Trong bản dịch.

Các khóa đại diện cho các phần thông tin ứng cử viên thường được lấy từ chuỗi đầu vào.

Các giá trị mang thông tin thực tế mà chúng tôi muốn truyền đi.

Cơ chế chú ý tính toán độ tương tự giữa truy vấn và từng khóa, tạo ra các trọng số

chỉ ra sự liên quan.

Sau đó, các trọng số này được áp dụng cho các giá trị và tổng có trọng số được tạo ra làm đầu ra.

Quá trình này cho phép mô hình tập trung động vào các phần khác nhau của đầu vào, tùy thuộc vào

nhiệm vụ trong tầm tay.

Có một số loại cơ chế chú ý được phân biệt bằng cách chúng tính toán độ tương tự giữa

truy vấn và khóa.

Chú ý bổ sung, còn được gọi là chú ý bahdanau, sử dụng mạng chuyển tiếp nguồn cấp dữ liệu nhỏ để kết hợp các truy vấn

và các phím, sau đó áp dụng một hàm phi tuyến tính.

Sự chú ý nhân hoặc luong chú ý Căng thẳng sử dụng tích số chấm giữa truy vấn và khóa, trong đó

nhanh hơn và hoạt động tốt trong thực tế.

Transformers đã giới thiệu sự chú ý của sản phẩm chấm theo tỷ lệ, giúp sửa đổi sản phẩm chấm bằng cách chia tỷ lệ cho nó

xuống để tránh các giá trị quá lớn.

Khi làm việc với các vectơ nhiều chiều, mặc dù cách tính toán khác nhau nhưng ý tưởng cốt lõi vẫn là

cùng gán trọng số cho các phần tử đầu vào khác nhau và kết hợp chúng thành một vectơ ngữ cảnh.

Một trong những đặc tính mạnh mẽ của sự chú ý là trọng lượng mà nó gán cho có thể được hình dung.

Các trọng số này thể hiện vị trí mà mô hình đang tập trung ở mỗi bước, cung cấp cái nhìn có thể hiểu được về

quá trình ra quyết định của nó trong dịch thuật.

Ví dụ: bản đồ chú ý có thể hiển thị những từ nguồn nào được căn chỉnh với từng từ mục tiêu giống như

một bảng căn chỉnh.

Những hình ảnh trực quan này không chỉ hữu ích để hiểu mô hình mà còn để gỡ lỗi và cải thiện

hiệu suất.

Họ chứng minh rằng mô hình không đối xử bình đẳng với tất cả các từ mà tập trung chủ động vào

phần liên quan của trình tự.

Sự chú ý mang lại một số lợi thế so với các phương pháp tiếp cận dựa trên RNN truyền thống.

Đầu tiên, nó nắm bắt được sự phụ thuộc dài hạn hiệu quả hơn nhiều vì mô hình có thể tham chiếu trực tiếp

bất kỳ phần nào của đầu vào bất kể khoảng cách.

Thứ hai, nó tránh được tình trạng tắc nghẽn khi nén toàn bộ chuỗi vào một trạng thái ẩn duy nhất, thay vào đó

sử dụng hệ thống cân linh hoạt.

Thứ ba, sự chú ý có tính song song cao khi được triển khai trong các mô hình máy biến áp, cho phép thực hiện nhanh hơn

huấn luyện trên tập dữ liệu lớn.

Cuối cùng, sự chú ý luôn cải thiện hiệu suất trong nhiều nhiệm vụ, bao gồm dịch thuật,

tóm tắt, nhận dạng giọng nói và trả lời câu hỏi.

Những lợi ích này giải thích tại sao sự chú ý đã trở thành một trong những đổi mới quan trọng nhất trong lĩnh vực sâu hiện đại.

học tập.

Trong xử lý ngôn ngữ tự nhiên, sự chú ý đã cách mạng hóa cách các mô hình xử lý các tác vụ tuần tự.

Hệ thống dịch máy được cải thiện đáng kể khi có sự chú ý vì các mô hình có thể căn chỉnh

từ trong các ngôn ngữ hiệu quả hơn.

Hệ thống tóm tắt tài liệu hiện nay dựa vào sự chú ý để tập trung vào những phần quan trọng nhất của văn bản,

tạo ra các bản tóm tắt mạch lạc trong câu hỏi.

Trả lời nhiệm vụ.

Sự chú ý giúp mô hình xác định các đoạn có liên quan nhất trong tài liệu để trả lời truy vấn.

Chatbots và AI đàm thoại cũng được hưởng lợi từ sự chú ý bằng cách tập trung linh hoạt vào các phần khác nhau

của lịch sử đối thoại, cho phép phản hồi chính xác hơn theo ngữ cảnh.

Nói tóm lại, sự chú ý đã trở thành một công cụ phổ biến trong các ứng dụng NLP.

Mặc dù sự chú ý bắt đầu từ NLP, nhưng nó đã mở rộng sang nhiều lĩnh vực khác.

Trong thị giác máy tính, sự chú ý được sử dụng để tạo chú thích cho hình ảnh, trong đó các mô hình tập trung vào các vùng cụ thể

của hình ảnh khi tạo văn bản.

Bộ chuyển đổi tầm nhìn mở rộng sự chú ý hoàn toàn đến các nhiệm vụ tầm nhìn, vượt trội hơn các mạng tích chập

ở một số điểm chuẩn.

Trong nhận dạng giọng nói, sự chú ý sẽ căn chỉnh tín hiệu âm thanh với văn bản được phiên âm.

Học tăng cường cũng đã chú ý đến việc xử lý các chiến lược dài hạn.

Cuối cùng, hệ thống AI đa phương thức kết hợp tầm nhìn, ngôn ngữ và âm thanh, sử dụng cơ chế chú ý để

tích hợp các kiểu dữ liệu khác nhau.

Những ứng dụng này thể hiện tính linh hoạt của sự chú ý trên nhiều lĩnh vực khác nhau, củng cố nó

như một nguyên tắc cốt lõi của AI hiện đại.

Tóm lại, sự chú ý được đưa ra như một giải pháp cho các vấn đề chính của RNN và LSTM, đặc biệt là

khó khăn của họ trong việc xử lý sự phụ thuộc lâu dài và nút thắt thông tin của các trạng thái ẩn.

Chú ý hoạt động bằng cách gán trọng số cho các phần khác nhau của đầu vào, cho phép mô hình tập trung có chọn lọc

trên những thông tin phù hợp nhất.

Điều này giúp có thể nắm bắt được các yếu tố phụ thuộc tầm xa, cải thiện độ chính xác và đào tạo hiệu quả hơn.

Mặc dù lần đầu tiên nó đạt được thành công trong lĩnh vực dịch máy, nhưng kể từ đó, sự chú ý đã lan rộng khắp NLP, máy tính.

tầm nhìn, học tăng cường giọng nói và AI đa phương thức.

Quan trọng nhất, sự chú ý tạo thành nền tảng của máy biến áp, thứ thống trị nghiên cứu AI hiện đại

và các ứng dụng.

Hiểu rõ sự chú ý là bước quan trọng trước khi đi sâu vào kiến ​​trúc máy biến áp.