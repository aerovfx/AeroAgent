# 2 -4.2 Khái niệm cơ bản về RNN – Truyền ngược qua thời gian.en US

---

WEBVTT

Mạng thần kinh tái phát hay RNN là các kiến trúc chuyên biệt được xây dựng để xử lý dữ liệu tuần tự.

Không giống như các mạng truyền thẳng truyền thống, RNN duy trì trạng thái ẩn hoạt động như bộ nhớ, cho phép

chúng để lưu trữ thông tin về các bước thời gian trước đó.

Điều này làm cho chúng đặc biệt hữu ích cho các nhiệm vụ như lập mô hình ngôn ngữ, dự báo chuỗi thời gian và

nhận dạng giọng nói.

Một tính năng chính khác của RNN là chia sẻ tham số theo các bước thời gian.

Điều này có nghĩa là các trọng số giống nhau được áp dụng ở mỗi bước trong chuỗi, giúp RNN hoạt động hiệu quả và có thể mở rộng

với độ dài trình tự khác nhau.

RNN là một bước đột phá đáng kể trong lĩnh vực học sâu vì chúng cho phép mô hình hóa các phần phụ thuộc

trong dữ liệu được sắp xếp và mở đường cho các mô hình trình tự tiên tiến hơn như LSTM và Grus.

RNN được cấu trúc xoay quanh khái niệm xử lý dữ liệu đầu vào từng bước.

Tại mỗi bước thời gian, mạng nhận được đầu vào và kết hợp nó với trạng thái ẩn từ bước trước đó.

bước.

Điều này tạo ra cả đầu ra và trạng thái ẩn được cập nhật.

Trạng thái ẩn là thứ cho phép RNN ghi nhớ thông tin theo thời gian.

Kết nối định kỳ này tạo thành một vòng lặp, cho phép mô hình lưu trữ ngữ cảnh và các phần phụ thuộc giữa

bước thời gian.

Toàn bộ chuỗi được xử lý từng bước, với trạng thái ẩn sẽ truyền thông tin về phía trước.

Kiến trúc này làm cho RNN rất phù hợp cho các tác vụ tuần tự, mặc dù nó cũng đặt ra những thách thức

chẳng hạn như độ dốc biến mất, mà chúng ta sẽ xem xét lại sau trong quá trình lan truyền về phía trước.

RNN xử lý một phần tử của chuỗi tại một thời điểm.

Ở mỗi bước, vectơ đầu vào XT được kết hợp với trạng thái ẩn ht trước đó thông qua một hàm có trọng số.

tổng và hàm kích hoạt.

Điều này tạo ra PT trạng thái ẩn mới, hoạt động như bộ nhớ cho bước thời gian hiện tại.

Từ trạng thái ẩn này, mạng cũng có thể tạo ra đầu ra Y, có thể đại diện cho một dự đoán

chẳng hạn như từ tiếp theo trong câu.

Quá trình này lặp lại từng bước trên toàn bộ chuỗi, chuyển tiếp thông tin qua

các trạng thái ẩn.

Cơ chế này cho phép RNN nắm bắt các phụ thuộc theo thời gian và duy trì bối cảnh khi chúng di chuyển qua

trình tự.

Công thức toán học của RNN có thể được biểu diễn bằng hai phương trình chính.

Đầu tiên, trạng thái ẩn HT được tính như một hàm của đầu vào XT hiện tại và trạng thái ẩn trước đó.

trạng thái HT một.

Cụ thể, HT bằng f của w x nhân với xt cộng w h nhân với HT một cộng với số hạng sai lệch.

Ở đây, F thường là hàm kích hoạt phi tuyến tính như tanh và h hoặc ReLU.

Phương trình thứ hai mô tả đầu ra y, được tính từ trạng thái ẩn HT bằng cách sử dụng trọng số

ma trận Y và độ lệch theo sau là hàm kích hoạt g.

Hàm g phụ thuộc vào nhiệm vụ.

Softmax được sử dụng cho các nhiệm vụ phân loại, trong khi sigmoid có thể được sử dụng cho đầu ra nhị phân.

Các phương trình này xác định cách RNN truyền thông tin theo thời gian.

Để huấn luyện RNN, chúng tôi sử dụng một phương pháp gọi là lan truyền ngược theo thời gian hoặc bptt.

Đây là một phần mở rộng của lan truyền ngược được sử dụng trong các mạng chuyển tiếp nhưng được điều chỉnh cho dữ liệu tuần tự

trong bptt.

Mạng không được kiểm soát trong tất cả các bước thời gian và hàm mất mát được tính toán trên toàn bộ chuỗi.

Sau đó, các gradient được truyền ngược qua từng bước thời gian, chảy qua các kết nối lặp lại.

Vì các tham số được chia sẻ theo thời gian nên độ dốc từ mỗi bước sẽ tích lũy và được áp dụng cho

cập nhật các ma trận trọng số tương tự.

Điều này cho phép mô hình học hỏi từ các phần phụ thuộc trong toàn bộ chuỗi.

Tuy nhiên, Bptt cũng đưa ra những thách thức như độ dốc biến mất và bùng nổ khi xử lý

trình tự dài.

Hãy chia Bptt thành các bước.

Đầu tiên, quá trình chuyển tiếp được tính toán qua tất cả các bước thời gian, tạo ra các đầu ra và trạng thái ẩn.

Sau đó, tổng tổn thất thường được tính bằng tổng tổn thất riêng lẻ trong mỗi bước thời gian.

Khi tổn thất được tính toán, các lỗi sẽ được truyền ngược qua chuỗi.

Ở mỗi bước, gradient không chỉ được truyền tới các trọng số ở bước đó mà còn thông qua hàm hồi quy.

kết nối với các bước thời gian trước đó.

Điều này có nghĩa là độ dốc tích lũy khi chúng di chuyển ngược thời gian.

Cuối cùng, việc cập nhật trọng số được thực hiện bằng cách sử dụng gradient tích lũy.

Quá trình này cho phép RNN tìm hiểu các phần phụ thuộc trải dài qua nhiều bước thời gian, mặc dù nó trở nên khó khăn.

tính toán chuyên sâu cho các chuỗi rất dài.

Một trong những vấn đề lớn nhất của lan truyền ngược theo thời gian là vấn đề độ dốc biến mất, vì độ dốc

chảy ngược qua nhiều bước thời gian.

Chúng được nhân nhiều lần với ma trận trọng số.

Nếu giá trị của các trọng số này nhỏ hơn một thì độ dốc sẽ co lại theo cấp số nhân.

Điều này có nghĩa là các bước thời gian trước đó hầu như không nhận được cập nhật độ dốc, khiến mô hình không thể học được

sự phụ thuộc lâu dài.

Trong thực tế, điều này hạn chế các RNN vani chỉ nắm bắt được các phần phụ thuộc trong phạm vi ngắn.

Ví dụ: RNN có thể thực hiện dự đoán tốt từ tiếp theo trong một cụm từ ngắn, nhưng nó sẽ gặp khó khăn

để nhớ chủ đề của một câu dài.

Vấn đề độ dốc biến mất là động lực chính để phát triển các kiến trúc tiên tiến hơn như

LSTM và Grus.

Bài toán gradient bùng nổ ngược lại với vấn đề gradient biến mất.

Ở đây, khi độ dốc chảy ngược lại, chúng tăng lớn theo cấp số nhân do lặp đi lặp lại phép nhân với

trọng lượng lớn.

Điều này dẫn đến việc cập nhật cân nặng cực lớn, khiến quá trình tập luyện trở nên không ổn định.

Sự mất mát có thể dao động dữ dội hoặc thậm chí phân kỳ hoàn toàn, ngăn cản mạng hội tụ.

Độ dốc bùng nổ đặc biệt phổ biến trong các chuỗi dài hoặc các mạng được khởi tạo kém.

Một giải pháp phổ biến là cắt bớt độ dốc, trong đó độ dốc được thu nhỏ lại nếu chúng vượt quá một ngưỡng nhất định.

Điều này giúp kiểm soát các cập nhật và ổn định quá trình đào tạo.

Mặc dù nó không giải quyết được vấn đề cơ bản là nắm bắt được sự phụ thuộc lâu dài, trong khi RNN thì

một bước đột phá trong việc xử lý dữ liệu tuần tự.

Vanilla RNN có những hạn chế lớn.

Đầu tiên, họ kém trong việc học các phụ thuộc dài hạn do độ dốc biến mất.

Thứ hai, độ dốc bùng nổ khiến việc đào tạo không ổn định nếu không có các kỹ thuật bổ sung như cắt bớt.

Thứ ba, việc huấn luyện RNN trên các chuỗi dài tốn kém về mặt tính toán vì việc truyền ngược qua

thời gian phải xử lý từng bước trong trình tự.

Những thách thức này đã khiến các nhà nghiên cứu phát triển các kiến trúc tiên tiến hơn như trí nhớ ngắn hạn dài hạn.

mạng hoặc LSTM và các đơn vị định kỳ có kiểm soát hoặc Grus.

Các mô hình này đã giới thiệu các cơ chế để quản lý bộ nhớ và luồng gradient tốt hơn, khiến chúng trở nên hiệu quả hơn nhiều.

hiệu quả cho các nhiệm vụ tuần tự trong thế giới thực.

Tóm lại, mạng nơ-ron hồi quy được thiết kế để xử lý dữ liệu tuần tự bằng cách duy trì một ẩn

trạng thái mang thông tin qua các bước thời gian.

Quá trình lan truyền thuận xử lý từng đầu vào một, chuyển bộ nhớ qua trạng thái ẩn.

Việc huấn luyện đạt được bằng cách truyền ngược theo thời gian, trong đó các lỗi được truyền ngược qua

toàn bộ trình tự.

Mặc dù điều này cho phép RNN nắm bắt được sự phụ thuộc theo thời gian nhưng chúng phải đối mặt với những thách thức nghiêm trọng khi biến mất

và độ dốc bùng nổ.

Những vấn đề này hạn chế khả năng học hỏi sự phụ thuộc lâu dài của họ và khiến việc đào tạo trở nên khó khăn.

Bất chấp những hạn chế này, RNN đại diện cho một khối xây dựng thiết yếu trong lịch sử mô hình hóa trình tự,

và hiểu biết các nguyên tắc cơ bản của chúng giúp chúng ta khám phá các mô hình tiên tiến hơn như LSTM, Grus và

cuối cùng là Transformers.