# 2 -1.2 Khái niệm cơ bản về mạng thần kinh.en Hoa Kỳ

---

WEBVTT

Mạng lưới thần kinh là trung tâm của học sâu.

Chúng được lấy cảm hứng lỏng lẻo từ bộ não con người, được tạo thành từ hàng tỷ tế bào thần kinh kết nối với nhau

trong mạng nơ-ron nhân tạo.

Chúng tôi thể hiện ý tưởng này bằng các lớp, lớp đầu vào nhận dữ liệu, một hoặc nhiều lớp ẩn

thông tin xử lý đó và lớp đầu ra tạo ra kết quả cuối cùng.

Mỗi kết nối giữa các tế bào thần kinh đều có trọng lượng và độ lệch, quyết định sức mạnh và ảnh hưởng của

tín hiệu được truyền qua.

Mạng học bằng cách điều chỉnh các trọng số này trong quá trình đào tạo để giảm sai sót trong dự đoán.

Về cơ bản, mạng lưới thần kinh là hệ thống nhận dạng mẫu mạnh mẽ, cho dù nó nhận dạng một

con mèo trong một hình ảnh hoặc dự đoán giá cổ phiếu.

Quá trình bắt đầu bằng việc cung cấp dữ liệu vào các lớp này, truyền tín hiệu về phía trước và sau đó cập nhật

trọng số để làm cho mạng thông minh hơn theo thời gian.

Perceptron là khối xây dựng đơn giản nhất của mạng lưới thần kinh và được giới thiệu lần đầu tiên vào những năm 1950.

Nó nhận nhiều đầu vào, nhân chúng với các trọng số tương ứng, thêm một số hạng sai lệch và sau đó áp dụng

một chức năng kích hoạt.

Kết quả là một đầu ra nhị phân, thường đại diện cho quyết định có hoặc không.

Ví dụ, hãy tưởng tượng một perceptron được huấn luyện để nhận biết liệu một hình ảnh có chứa một con mèo hay không.

Mỗi pixel hoạt động như một đầu vào.

Perceptron xử lý chúng và sau đó xuất ra một kết quả cho trường hợp phát hiện được mèo hoặc xuất ra kết quả bằng 0 cho trường hợp không phát hiện được mèo.

Mặc dù khả năng của một perceptron bị hạn chế nhưng nó chỉ có thể giải quyết các vấn đề có thể phân tách tuyến tính.

Nó đặt nền móng cho mạng lưới thần kinh đa lớp.

Những cấu trúc sâu hơn này, được gọi là perceptron đa lớp, cho phép chúng ta xử lý các vấn đề phi tuyến tính phức tạp hơn nhiều.

Mối quan hệ của bạn trong dữ liệu.

Mặc dù perceptron là một khái niệm mang tính đột phá nhưng nó cũng có những hạn chế.

Điều quan trọng nhất là một perceptron duy nhất chỉ có thể giải quyết các vấn đề có thể phân tách tuyến tính.

Ví dụ: nó có thể dễ dàng mô hình hóa các cổng logic đơn giản như Andy hoặc nơi các đầu vào có thể được phân tách bằng

một đường thẳng trên đồ thị.

Tuy nhiên, nó không giải quyết được các hàm phức tạp hơn như XOR, trong đó ranh giới quyết định không tuyến tính.

Hạn chế này đã làm chậm quá trình nghiên cứu mạng lưới thần kinh trong nhiều năm, cho đến khi các nhà nghiên cứu nhận ra rằng bằng cách xếp chồng

nhiều perceptron thành các lớp, chúng ta có thể tạo ra các mạng có khả năng giải quyết các vấn đề phi tuyến tính.

Những mạng đa lớp này, cái mà ngày nay chúng ta gọi là các tri giác đa lớp, mở khóa sức mạnh thực sự của độ sâu

học tập.

Các chức năng kích hoạt rất quan trọng đối với mạng lưới thần kinh.

Nếu không có chúng, mạng lưới thần kinh sẽ chỉ đơn giản là một tập hợp các phương trình tuyến tính và bất kể có bao nhiêu lớp

bạn thêm vào, đầu ra vẫn sẽ tuyến tính.

Điều này hạn chế nghiêm trọng khả năng của mô hình trong việc tìm hiểu các mối quan hệ phức tạp.

Các chức năng kích hoạt đưa ra tính phi tuyến tính, cho phép mạng nắm bắt được các mẫu phức tạp trong

dữ liệu.

Ví dụ: chúng có thể giúp phát hiện các ranh giới quyết định cong hoặc các biến thể tinh vi trong hình ảnh và văn bản.

Chúng cũng kiểm soát phạm vi đầu ra của nơ-ron, giữ cho các giá trị ổn định và có ý nghĩa.

Nói tóm lại, các chức năng kích hoạt là thứ khiến deep learning thực sự trở nên mạnh mẽ.

Họ biến đổi mạng lưới thần kinh từ những máy tính đơn giản thành những hệ thống học tập linh hoạt có khả năng giải quyết

nhiệm vụ đa dạng, từ nhận dạng chữ số viết tay đến dịch toàn bộ ngôn ngữ.

Hàm kích hoạt sigmoid là một trong những hàm được sử dụng rộng rãi đầu tiên trong mạng lưới thần kinh.

Công thức của nó là lấy một chia cho một cộng với E thành âm x, ánh xạ bất kỳ đầu vào nào vào một phạm vi giữa

0 và 1.

Điều này hữu ích khi chúng ta muốn lập mô hình xác suất, vì kết quả đầu ra có thể được diễn giải trực tiếp.

như một khả năng.

Ví dụ: giá trị gần với ngày 1 tháng 5 nghĩa là có khả năng xảy ra gian lận cao, trong khi giá trị gần bằng 0 nghĩa là khó xảy ra.

Tuy nhiên, hàm sigmoid có một nhược điểm lớn khi đầu vào rất lớn hoặc rất nhỏ,

độ dốc trở nên cực kỳ nhỏ, một vấn đề được gọi là độ dốc biến mất.

Điều này làm chậm quá trình học tập trong các mạng sâu hơn.

Mặc dù sigmoid có những ứng dụng riêng, đặc biệt là trong các lớp đầu ra của mô hình phân loại nhị phân, nhưng nó có

phần lớn đã được thay thế ở các lớp ẩn bằng các hàm kích hoạt hiệu quả hơn như ReLU.

Đơn vị tuyến tính chỉnh lưu hoặc ReLU là hàm kích hoạt phổ biến nhất trong học sâu hiện đại.

Công thức của nó là đầu ra đơn giản bằng 0 nếu đầu vào âm, nếu không thì nó bằng đầu vào.

Sự đơn giản này làm cho ReLU vừa hiệu quả vừa mạnh mẽ.

Một trong những ưu điểm chính của nó là nó làm giảm vấn đề biến mất độ dốc thường thấy trong các hàm sigmoid hoặc tan,

cho phép các mạng học nhanh hơn nhiều.

Vì điều này, ReLU đã trở thành lựa chọn mặc định trong hầu hết các lớp mạng thần kinh ẩn hiện nay.

Tuy nhiên, ReLU không hoàn hảo.

Đôi khi nó có thể gặp phải vấn đề ReLU sắp chết, trong đó các nơ-ron xuất ra số 0 cho tất cả các đầu vào và dừng lại.

học tập.

Mặc dù vậy, tốc độ và tính hiệu quả của nó khiến nó trở thành xương sống của hầu hết các mô hình học sâu, từ

thị giác máy tính đến xử lý ngôn ngữ tự nhiên.

Tiếp tuyến hyperbol hoặc hàm kích hoạt là một lựa chọn phổ biến khác trong mạng lưới thần kinh.

Công thức của nó tạo ra các kết quả đầu ra nằm trong khoảng từ âm đến dương, làm cho nó ở giữa.

Thuộc tính này mang lại lợi thế cho nó so với sigmoid vì nó cân bằng đầu ra dương và âm,

có thể giúp tối ưu hóa hội tụ nhanh hơn.

Giống như sigmoid, tanh thường được sử dụng trong các mô hình cần đầu ra có giới hạn trơn tru.

Tuy nhiên, tanh cũng gặp phải vấn đề biến mất gradient khi đầu vào trở nên rất lớn hoặc rất nhỏ.

Mặc dù nó ít được sử dụng phổ biến trong các kiến trúc hiện đại so với ReLU, Tange vẫn xuất hiện trong một số

các trường hợp, đặc biệt là trong các mạng thần kinh tái phát, trong đó hành vi tập trung vào số 0 của nó mang lại sự ổn định.

Tóm lại, H cải thiện dựa trên sigmoid, nhưng dần dần được thay thế bằng các kích hoạt hiệu quả hơn

trong học sâu.

Tuyên truyền chuyển tiếp là quá trình gửi dữ liệu qua mạng lưới thần kinh để tạo ra dự đoán.

Nó bắt đầu với lớp đầu vào, trong đó mỗi đầu vào được nhân với trọng số tương ứng của nó và tính tổng

với một sự thiên vị.

Tổng có trọng số này sau đó được chuyển qua hàm kích hoạt để tạo ra đầu ra.

Đầu ra đó trở thành đầu vào cho lớp tiếp theo và quá trình này lặp lại trên tất cả các lớp của

mạng.

Ở lớp cuối cùng, chúng tôi thu được dự đoán của mô hình, cho dù đó là xác suất, nhãn lớp,

hoặc một giá trị số.

Lan truyền thuận về cơ bản là quá trình suy nghĩ của mạng lấy đầu vào thô, xử lý

nó thông qua các thông số đã học và đưa ra câu trả lời.

Trong quá trình đào tạo, chúng tôi so sánh kết quả này với nhãn thực để đo lỗi, từ đó dẫn đến

quá trình học tập ở bước tiếp theo.

Tuyên truyền ngược.

Lan truyền ngược, viết tắt của lan truyền ngược các lỗi, là cơ chế cho phép hệ thống thần kinh

mạng để tìm hiểu sau khi lan truyền thuận tạo ra các dự đoán về tỷ lệ, chúng tôi tính toán sự khác biệt

giữa dự đoán và giá trị thực bằng cách sử dụng hàm mất mát.

Lỗi này sau đó được truyền ngược lại qua mạng bằng cách sử dụng phép tính và quy tắc dây chuyền.

Lan truyền ngược tính toán độ dốc về cơ bản mỗi trọng số góp phần gây ra lỗi bao nhiêu.

Sau đó, mạng sẽ cập nhật trọng số của nó theo hướng giảm thiểu lỗi này, thường sử dụng

thuật toán tối ưu hóa như giảm độ dốc.

Quá trình lan truyền thuận theo sau là quá trình lan truyền ngược này lặp lại qua nhiều lần lặp, dần dần

tinh chỉnh các trọng số để mạng cải thiện dự đoán của nó.

Lan truyền ngược là công cụ học tập của mạng lưới thần kinh, biến dữ liệu thô và trọng số ngẫu nhiên thành

mô hình thông minh được đào tạo cao.

Hãy mang mọi thứ lại với nhau.

Perceptron đóng vai trò là khối xây dựng cơ bản của mạng lưới thần kinh, Xử lý đầu vào thông qua

trọng số và độ lệch.

Các chức năng kích hoạt như sigmoid, tanh và ReLU giới thiệu tính phi tuyến tính, cho phép mạng

để nắm bắt các mẫu phức tạp.

Lan truyền thuận thể hiện giai đoạn dự đoán của mạng trong đó dữ liệu truyền từ đầu vào đến đầu ra.

Mặt khác, lan truyền ngược là giai đoạn học trong đó các lỗi được tính toán và trọng số

được cập nhật để cải thiện hiệu suất.

Cùng với nhau, những khái niệm này tạo thành nền tảng của tất cả các mô hình học sâu, cho dù chúng ta đang xây dựng một

phân loại đơn giản hoặc một mô hình máy biến áp lớn.

Các cơ chế cơ bản vẫn giữ nguyên.

Các lớp perceptron

Chức năng kích hoạt.

Chuyển tiếp và cập nhật ngược.

Hiểu những điều cơ bản này là điều cần thiết trước khi chuyển sang các kiến trúc nâng cao hơn như tích chập

và các mạng tái phát.