# 1 -5.1 Transformer Architecture.en US

---

WEBVTT

Năm 2017, Vaswani và các đồng nghiệp đã giới thiệu mô hình máy biến áp trong bài báo đột phá của họ.

là tất cả những gì bạn cần.

Mô hình này đã thay thế các mạng lặp lại như RNN và LSTM bằng cơ chế tự chú ý mới.

Thay vì xử lý các chuỗi theo từng bước, máy biến áp sẽ xử lý song song toàn bộ chuỗi

trong khi tìm hiểu sự phụ thuộc giữa các mã thông báo.

Sự đổi mới này giúp việc đào tạo nhanh hơn nhiều và cho phép các mô hình nắm bắt được các yếu tố phụ thuộc tầm xa nhiều hơn

một cách hiệu quả.

Transformers kể từ đó đã trở thành nền tảng của gần như tất cả các mô hình ngôn ngữ tự nhiên hiện đại,

bao gồm Bert, GPT, T5 và vô số người khác.

Chúng cũng được ứng dụng rộng rãi trong thị giác máy tính và AI đa phương thức.

Để thực sự hiểu AI hiện đại, trước tiên chúng ta phải hiểu cách thức hoạt động của máy biến áp.

Bắt đầu với khả năng tự chú ý và chú ý nhiều đầu, máy biến áp có kiến trúc bộ giải mã mã hóa,

mặc dù trong nhiều trường hợp chỉ sử dụng bộ mã hóa hoặc bộ giải mã.

Tùy thuộc vào ứng dụng, bộ mã hóa xử lý chuỗi đầu vào trong khi bộ giải mã tạo ra

đầu ra.

Cốt lõi của máy biến áp là cơ chế tự chú ý, cho phép mỗi mã thông báo tham gia vào mọi hoạt động

mã thông báo khác trong chuỗi.

Sự chú ý nhiều đầu mở rộng ý tưởng này bằng cách chạy song song nhiều thao tác chú ý, thu thập

những mối quan hệ khác nhau.

Ngoài ra, mỗi lớp bao gồm các mạng chuyển tiếp để thêm các lớp phi tuyến tính và chuẩn hóa

để ổn định việc đào tạo.

Bởi vì máy biến áp không sử dụng phép lặp hoặc tích chập nên chúng dựa vào mã hóa vị trí để giữ lại

thông tin về thứ tự của các token.

Cùng với nhau, các thành phần này làm cho máy biến áp trở nên mạnh mẽ và linh hoạt.

Tự chú ý là trái tim của máy biến áp.

Trong cơ chế này, mỗi mã thông báo trong chuỗi đầu vào tương tác với mọi mã thông báo khác để tính toán theo ngữ cảnh

các đại diện.

Ví dụ, trong câu the cat sat on the mat, từ cat có thể tham khảo sat và mat một cách trực tiếp,

bất kể khoảng cách.

Sự tự chú ý tạo ra sự thể hiện có trọng số của trình tự trong đó các mã thông báo quan trọng nhận được mức cao hơn

điểm chú ý.

Điều này khắc phục hạn chế của RNN, xử lý mã thông báo một cách tuần tự và cho phép thu thập dữ liệu dài

phụ thuộc phạm vi.

Một ưu điểm lớn khác là khả năng tự chú ý có thể được tính toán song song cho tất cả các mã thông báo, đặc biệt là

tăng tốc độ đào tạo so với các mô hình tuần tự.

Cơ chế tự chú ý thường được mô tả bằng cách sử dụng các truy vấn, khóa và giá trị cho mỗi mã thông báo.

Mô hình tính toán vectơ truy vấn, vectơ khóa và vectơ giá trị.

Truy vấn thể hiện những gì mã thông báo đang tìm kiếm.

Chìa khóa đại diện cho tính năng của từng mã thông báo trong chuỗi, điểm chú ý được tính bằng

so sánh truy vấn với các khóa, thường thông qua tích số chấm.

Những điểm số này sau đó được chuẩn hóa bằng hàm softmax để tạo thành trọng số chú ý.

Cuối cùng, tổng trọng số của các giá trị sẽ tạo ra biểu diễn đầu ra cho mã thông báo đó.

Khung này cho phép mô hình quyết định một cách linh hoạt những mã thông báo nào phù hợp nhất để hiểu

bối cảnh.

Về mặt toán học, sự tự chú ý được thể hiện dưới dạng sự chú ý q k v q softmax qq q dq v.

Ở đây, tích số chấm qq đo lường mức độ tương tự giữa truy vấn và khóa, tạo ra điểm đại diện cho

sự liên quan.

Chia cho căn bậc hai của thứ nguyên chính sẽ ngăn chặn các giá trị quá lớn có thể gây mất ổn định

đào tạo.

Hàm softmax chuẩn hóa các điểm này thành phân bố xác suất, đảm bảo chúng có tổng bằng

một.

Cuối cùng, các trọng số này được áp dụng cho các giá trị để tính toán đầu ra dưới dạng tổng có trọng số.

Công thức tao nhã này làm nền tảng cho khả năng của người biến áp trong việc tìm hiểu các phần phụ thuộc phức tạp một cách hiệu quả.

Sự chú ý của nhiều đầu mở rộng khả năng tự chú ý bằng cách thực hiện song song nhiều thao tác chú ý, mỗi thao tác

với các thông số đã học khác nhau.

Mỗi người đứng đầu có thể tập trung vào các khía cạnh khác nhau của trình tự.

Ví dụ: trong một tác vụ dịch thuật, một đầu có thể sắp xếp các mối quan hệ động từ chủ ngữ, trong khi một đầu khác có thể sắp xếp các mối quan hệ giữa chủ ngữ và động từ.

tập trung vào tính từ và danh từ.

Đầu ra của các đầu này được nối và chiếu trở lại không gian chiều của mô hình.

Điều này cho phép máy biến áp nắm bắt được một tập hợp phụ thuộc phong phú hơn so với một đầu chú ý duy nhất có thể

cung cấp.

Sự chú ý của nhiều đầu làm tăng đáng kể tính biểu cảm của mô hình và là một trong những lý do khiến máy biến áp

thực hiện rất tốt các nhiệm vụ đa dạng.

Mỗi lớp bộ mã hóa trong máy biến áp bao gồm một mô-đun tự chú ý, theo sau là mạng chuyển tiếp nguồn cấp dữ liệu.

Các kết nối dư được sử dụng xung quanh cả hai thành phần để ổn định quá trình học tập và cải thiện luồng gradient.

Chuẩn hóa lớp được áp dụng để chuẩn hóa kết quả đầu ra, ngăn ngừa sự phân kỳ trong quá trình đào tạo.

Mạng chuyển tiếp nguồn cấp dữ liệu bao gồm hai lớp tuyến tính có độ phi tuyến tính, thường là ReLU, ở giữa.

Máy biến áp được chế tạo bằng cách xếp chồng nhiều lớp bộ mã hóa lên nhau, cho phép mô hình

để nắm bắt sự phụ thuộc ngày càng phức tạp.

Thiết kế mô-đun này giúp máy biến áp có thể mở rộng quy mô, cho phép đào tạo các mạng sâu hơn một cách hiệu quả.

Bộ giải mã tương tự như bộ mã hóa nhưng có hai điểm khác biệt quan trọng.

Đầu tiên, nó sử dụng sự tự chú ý một cách che giấu để mỗi vị trí chỉ có thể tham gia vào các vị trí trước đó, ngăn chặn

mô hình nhìn trộm về phía trước trong quá trình đào tạo.

Thứ hai, bộ giải mã bao gồm một cơ chế chú ý của bộ giải mã bộ mã hóa, cho phép nó chú ý đến

bộ mã hóa xuất ra trong khi tạo chuỗi đích.

Giống như bộ mã hóa, nó cũng bao gồm các mạng chuyển tiếp và chuẩn hóa.

Bộ giải mã tạo ra chuỗi đầu ra từng bước bằng cách sử dụng mã thông báo và bộ mã hóa được tạo trước đó

đầu ra dưới dạng bối cảnh.

Điều này làm cho máy biến áp có hiệu quả cao đối với các tác vụ như dịch thuật và tạo văn bản.

Máy biến áp có nhiều ưu điểm so với các mô hình trình tự trước đó.

Bởi vì chúng xử lý song song toàn bộ chuỗi nên chúng được đào tạo nhanh hơn nhiều so với RNN và LSTM.

Cơ chế tự chú ý của họ cho phép họ nắm bắt được sự phụ thuộc tầm xa một cách hiệu quả, ngay cả trong

trình tự rất dài.

Máy biến áp có quy mô cực kỳ tốt.

Các mô hình lớn hơn được đào tạo trên nhiều dữ liệu hơn luôn đạt được hiệu suất tốt hơn, điều này đã thúc đẩy sự gia tăng

của các mô hình ngôn ngữ lớn như GPT.

Cuối cùng, máy biến áp đã đạt được kết quả tiên tiến về xử lý ngôn ngữ tự nhiên, máy tính

tầm nhìn và học tập đa phương thức, biến chúng thành kiến trúc thống trị trong AI hiện đại.

Tóm lại, máy biến áp được chế tạo dựa trên nguyên tắc tự chú ý và chú ý nhiều đầu,

cho phép các mô hình tập trung vào các phần có liên quan của trình tự một cách linh hoạt.

Kiến trúc bao gồm các bộ mã hóa và giải mã, mỗi bộ có các lớp tự chú ý, chuyển tiếp,

chuẩn hóa và kết nối dư bằng cách xử lý các chuỗi song song, máy biến áp đạt được tốc độ nhanh hơn

đào tạo và mô hình phụ thuộc tầm xa tốt hơn RNN và LSTM.

Khả năng mở rộng của họ đã tạo ra những bước đột phá trong các nhiệm vụ NLP, tầm nhìn và đa phương thức.

Các mô hình ngôn ngữ lớn ngày nay, bao gồm GPT và Bert, là hậu duệ trực tiếp của kiến ​​trúc này.

Hiểu về Transformers là điều cần thiết để làm chủ AI hiện đại.