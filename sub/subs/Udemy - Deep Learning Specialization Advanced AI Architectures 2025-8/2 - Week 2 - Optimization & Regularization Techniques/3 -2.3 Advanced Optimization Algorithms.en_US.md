# 3 -2.3 Thuật toán tối ưu hóa nâng cao.en US

---

WEBVTT

Giảm dần độ dốc là nền tảng của tối ưu hóa học sâu, nhưng trên thực tế, nó có một số thiếu sót.

Nó có thể gặp khó khăn với độ dốc nhiễu, mất rất nhiều thời gian để hội tụ và bị mắc kẹt ở cực tiểu cục bộ kém.

hoặc điểm yên ngựa.

Để khắc phục những hạn chế này, các nhà nghiên cứu đã phát triển các thuật toán tối ưu hóa tiên tiến.

Các thuật toán này xây dựng trên nền tảng giảm dần độ dốc nhưng bổ sung thêm các cải tiến như tốc độ học thích ứng, động lượng

theo dõi hoặc lập kế hoạch kích thước bước thông minh hơn.

Kết quả là việc đào tạo nhanh hơn, ổn định hơn và chính xác hơn, đặc biệt là trên các mô hình lớn và phức tạp.

Ngày nay, hầu hết tất cả các hệ thống deep learning đều sử dụng một số hình thức tối ưu hóa nâng cao, với các thuật toán như

Rmsprop, Adam và Adam là phổ biến nhất.

Ngoài ra, chiến lược lập kế hoạch tốc độ học đóng vai trò quan trọng trong việc tinh chỉnh tốc độ

một mô hình thích nghi trong quá trình đào tạo.

Cùng với nhau, những phương pháp này tạo thành bộ công cụ giúp việc học sâu trở nên thiết thực và có thể mở rộng.

Trước khi đi sâu vào các trình tối ưu hóa nâng cao, hãy tóm tắt ngắn gọn các biến thể cơ bản của việc giảm độ dốc.

Giảm độ dốc hàng loạt sử dụng toàn bộ tập dữ liệu để tính toán độ dốc ở mỗi bước, ổn định

nhưng rất chậm đối với các tập dữ liệu lớn, giảm độ dốc ngẫu nhiên hoặc cập nhật trọng số SGD sau mỗi mẫu,

điều này làm cho nó nhanh hơn nhiều nhưng rất ồn ào.

Giảm độ dốc hàng loạt nhỏ mang lại sự cân bằng bằng cách sử dụng các lô dữ liệu nhỏ, kết hợp hiệu quả

với các bản cập nhật mượt mà hơn.

Tất cả các trình tối ưu hóa nâng cao đều được xây dựng dựa trên nền tảng lô nhỏ này, bổ sung thêm các cơ chế để cải thiện hơn nữa

hiệu suất.

Ví dụ: họ có thể điều chỉnh tốc độ học tập một cách thích ứng, kết hợp động lượng hoặc xử lý độ dốc thưa thớt

hiệu quả hơn.

Những đổi mới này cho phép các mô hình học sâu hiện đại đào tạo trên các tập dữ liệu khổng lồ trong phạm vi hợp lý.

các khung thời gian.

Rmsprop, viết tắt của lan truyền bình phương trung bình gốc, được giới thiệu để giải quyết một số hạn chế

độ dốc giảm dần tiêu chuẩn.

Động lực chính của nó là điều chỉnh tốc độ học tập cho từng tham số riêng lẻ.

Không giống như các phương pháp truyền thống sử dụng tốc độ học cố định cho tất cả các trọng số, Rmsprop chuẩn hóa các bản cập nhật

sử dụng đường trung bình động của gradient bình phương.

Điều này ngăn tốc độ học tập trở nên quá lớn về kích thước với độ dốc cao nhất quán,

và có kích thước quá nhỏ với độ dốc thấp liên tục.

Rmsprop đặc biệt hiệu quả đối với các mục tiêu không cố định, chẳng hạn như các mục tiêu thường gặp

mạng lưới thần kinh, trong đó độ dốc có thể thay đổi đáng kể theo các bước thời gian.

Bằng cách mở rộng quy mô các bản cập nhật một cách thích ứng, rmsprop giúp ổn định quá trình học tập và tăng tốc độ hội tụ, giúp

nó là một trong những trình tối ưu hóa sớm nhất được áp dụng rộng rãi trong học sâu.

Rmsprop hoạt động bằng cách duy trì đường trung bình động giảm dần theo cấp số nhân của gradient bình phương cho mỗi

tham số.

Tại mỗi lần cập nhật, gradient được chia cho căn bậc hai của mức trung bình này cộng với một hằng số nhỏ để

tránh chia cho số 0.

Kết quả là tốc độ học thích ứng được điều chỉnh tự động cho từng tham số.

Cơ chế này làm giảm nguy cơ bùng nổ các bản cập nhật và đảm bảo quá trình đào tạo ổn định hơn.

Ví dụ: nếu một vật nặng luôn có độ dốc lớn.

Rmsprop làm giảm tốc độ học tập hiệu quả, ngăn ngừa sự mất ổn định.

Mặt khác, các tham số có độ dốc nhỏ hơn nhận được các cập nhật tương đối lớn hơn, giúp chúng nắm bắt được

lên.

Sự cân bằng này làm cho Rmsprop có hiệu quả cao, đặc biệt đối với các bài toán có độ dốc thay đổi đáng kể

qua các thông số khác nhau hoặc theo thời gian.

Trình tối ưu hóa Adam là phiên bản cải tiến của trình tối ưu hóa Adam.

Bản thân Adam kết hợp động lượng với tốc độ học tập thích ứng, khiến nó trở thành một trong những công cụ tối ưu hóa phổ biến nhất

trong học sâu.

Tuy nhiên, Adam tích hợp chính quy hóa L2 hoặc giảm trọng số theo cách có thể gây ra sai lệch, dẫn đến

đến khái quát hóa dưới mức tối ưu.

Adam W giải quyết vấn đề này bằng cách tách sự suy giảm trọng số khỏi các cập nhật độ dốc.

Sự phân tách này đảm bảo rằng sự phân rã trọng số đóng vai trò như sự chính quy hóa thực sự thay vì can thiệp vào

quá trình tối ưu hóa.

Kết quả là sự hội tụ tốt hơn và cải thiện khả năng khái quát hóa trên nhiều nhiệm vụ.

Trên thực tế, Adam W đã trở thành trình tối ưu hóa mặc định cho nhiều kiến trúc hiện đại, bao gồm cả Transformer

dựa trên các mô hình trong xử lý ngôn ngữ tự nhiên.

Tính hiệu quả và ổn định của nó khiến nó trở thành lựa chọn phù hợp để đào tạo mạng sâu trên quy mô lớn.

Adam và Adam W có nhiều điểm tương đồng, nhưng điểm khác biệt chính của họ nằm ở cách họ xử lý tình trạng giảm cân.

Trong Adam, việc giảm trọng lượng được áp dụng như một phần của Cập nhật gradient.

Điều này tạo ra sự tương tác giữa tối ưu hóa và chính quy hóa có thể làm sai lệch các cập nhật trọng số, dẫn đến

đến mức khái quát hóa kém hơn.

Mặt khác, Adam W tách riêng sự suy giảm trọng số khỏi các phép tính độ dốc, đảm bảo rằng việc chính quy hóa

được áp dụng độc lập.

Sự tách biệt này tạo ra các bản cập nhật sạch hơn và cho phép giảm trọng lượng hoạt động như dự kiến.

Thu nhỏ trọng số để giảm việc trang bị quá mức mà không ảnh hưởng đến tốc độ học thích ứng của trình tối ưu hóa.

Trong thực tế, Adam W thường mang lại kết quả tốt hơn, đặc biệt đối với các mô hình lớn được huấn luyện trên dữ liệu phức tạp

bộ.

Sự cải tiến này đã giúp Adam trở thành người tối ưu hóa được lựa chọn trong các lĩnh vực như xử lý ngôn ngữ tự nhiên

và thị giác máy tính, nơi mà sự khái quát hóa mạnh mẽ là điều cần thiết.

Tốc độ học là một trong những siêu tham số quan trọng nhất trong việc huấn luyện mạng lưới thần kinh.

Nếu nó quá cao, mô hình có thể vượt quá giải pháp tối ưu.

Nếu nó quá thấp, việc tập luyện sẽ trở nên chậm chạp một cách đáng kinh ngạc.

Tỷ lệ học tập cố định thường không hoạt động tốt trong toàn bộ quá trình đào tạo.

Đó là lúc việc lập kế hoạch tỷ lệ học tập xuất hiện.

Lịch trình tốc độ học tập điều chỉnh tốc độ học tập một cách linh hoạt khi quá trình đào tạo diễn ra.

Ngay từ đầu, các bước lớn hơn sẽ giúp mô hình học hỏi nhanh chóng.

Sau đó, các bước nhỏ hơn cho phép tinh chỉnh gần mức tối thiểu.

Sự cân bằng này cải thiện cả tốc độ và độ chính xác.

Các chiến lược lập lịch phổ biến bao gồm phân rã từng bước trong đó tốc độ học giảm theo các khoảng thời gian cố định.

Ủ cosine, giúp giảm tốc độ một cách trơn tru theo đường cong cosine và khởi động lại ấm áp, giúp

định kỳ thiết lập lại tốc độ học tập để khuyến khích sự khám phá.

Lập kế hoạch tốc độ học tập là một công cụ đơn giản nhưng mạnh mẽ để nâng cao hiệu suất đào tạo.

Hai trong số các kỹ thuật lập kế hoạch tốc độ học được sử dụng rộng rãi nhất là phân rã bước và ủ cosine.

Trong phân rã bước, tốc độ học giảm đi một hệ số cố định sau một số kỷ nguyên nhất định.

Ví dụ: tốc độ học tập có thể giảm một nửa sau mỗi 10 kỷ nguyên.

Cách tiếp cận này đơn giản nhưng có thể dẫn đến những thay đổi đột ngột trong động lực học tập.

Mặt khác, ủ cosine làm giảm tốc độ học dần dần theo đường cong cosine,

dẫn đến sự hội tụ mượt mà hơn.

Sự mượt mà này cho phép trình tối ưu hóa đạt được tiến bộ ổn định mà không cần phải nhảy đột ngột.

Trong hiệu suất.

Ủ cosine đặc biệt hiệu quả khi kết hợp với khởi động lại ấm, giúp thiết lập lại quá trình học tập

định kỳ để thoát khỏi mức tối thiểu nông.

Cả hai phương pháp đều cung cấp những cách thực tế để kiểm soát tốc độ học tập và được sử dụng rộng rãi trong đào tạo

mạng lưới sâu.

Khởi động lại ấm áp là một kỹ thuật lập kế hoạch tốc độ học tập mạnh mẽ khác.

Ý tưởng là định kỳ đặt lại tốc độ học lên giá trị cao hơn trong quá trình đào tạo.

Điều này giúp mô hình không bị mắc kẹt trong các điểm cực tiểu cục bộ kém và khuyến khích việc khám phá thêm

khung cảnh mất mát.

Thông thường, khởi động lại ấm được kết hợp với ủ cosine, trong đó tốc độ học giảm dần

dọc theo đường cong cosin trước khi được thiết lập lại.

Mỗi chu kỳ sẽ dài hơn một chút, cho phép khám phá rộng rãi ngay từ đầu và tinh chỉnh sau này

trong đào tạo.

Sự kết hợp này đã được chứng minh là đặc biệt hiệu quả trong thị giác máy tính và xử lý ngôn ngữ tự nhiên,

trong đó các mô hình thường cần thoát khỏi bối cảnh tối ưu hóa phức tạp bằng cách cung cấp sự cân bằng giữa việc khám phá

và sự hội tụ.

Khởi động lại ấm áp cải thiện cả hiệu quả đào tạo và độ chính xác cuối cùng.

Tóm lại, các thuật toán tối ưu hóa nâng cao giải quyết các hạn chế của việc giảm độ dốc cơ bản.

Rmsprop điều chỉnh tốc độ học trên mỗi tham số bằng cách sử dụng đường trung bình động, mang lại hiệu quả cao cho các ứng dụng không cố định

vấn đề.

Adam cải thiện Adam bằng cách tách sự suy giảm trọng số khỏi các cập nhật độ dốc, dẫn đến khả năng khái quát hóa tốt hơn

trong các kiến trúc hiện đại.

Lập kế hoạch tốc độ học tập sẽ tự động điều chỉnh kích thước bước trong quá trình đào tạo bằng các phương pháp như phân rã bước,

ủ cosine và khởi động lại ấm, cải thiện cả tốc độ và độ ổn định hội tụ.

Cùng với nhau, những kỹ thuật này tạo thành một bộ công cụ tối ưu hóa mạnh mẽ cho phép các mô hình học sâu

đào tạo hiệu quả trên các tập dữ liệu lớn và kiến trúc phức tạp.

Hiểu và áp dụng các thuật toán này là rất quan trọng đối với bất kỳ ai muốn xây dựng công nghệ hiện đại

Hệ thống AI.