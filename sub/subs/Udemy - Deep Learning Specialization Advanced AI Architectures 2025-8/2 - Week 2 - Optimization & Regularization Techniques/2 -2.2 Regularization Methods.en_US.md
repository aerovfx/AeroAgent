# 2 -2.2 Phương pháp chính quy hóa.en US

---

WEBVTT

Một trong những rủi ro lớn nhất trong việc đào tạo mô hình sâu là trang bị quá mức khi mạng ghi nhớ quá trình đào tạo

dữ liệu thay vì học các mẫu chung.

Để giải quyết vấn đề này, chúng tôi sử dụng một tập hợp các kỹ thuật được gọi là chính quy hóa.

Chính quy hóa đưa ra các sửa đổi trong quá trình đào tạo để làm cho các mô hình mạnh mẽ hơn và có khả năng khái quát hóa

tới những dữ liệu chưa được nhìn thấy.

Những sửa đổi này có thể bao gồm việc thêm các hình phạt vào trọng lượng, bỏ kết nối một cách ngẫu nhiên hoặc

mở rộng tập dữ liệu.

Mục tiêu chính là sự cân bằng.

Chúng tôi muốn mô hình chính xác nhưng không dễ vỡ.

Mạnh mẽ nhưng không quá tự tin.

Chính quy hóa không chỉ cải thiện tính khái quát hóa mà còn ổn định quá trình đào tạo, làm cho mạng

ít nhạy cảm hơn với tiếng ồn hoặc các ngoại lệ.

Trong phần này, chúng ta sẽ khám phá ba kỹ thuật chính quy hóa L1 và L2 được sử dụng rộng rãi.

Ủy quyền, bỏ học và tăng cường dữ liệu, mỗi tính năng này đều khắc phục tình trạng trang bị quá mức từ một thiết bị khác nhau

góc.

Chính quy hóa L1.

Thường được gọi là Lasso, hoạt động bằng cách cộng một hình phạt bằng tổng các giá trị tuyệt đối của

trọng số của mô hình đối với hàm mất mát.

Hình phạt này khuyến khích mạng giảm các kết nối không cần thiết bằng cách đẩy nhiều trọng số đến mức chính xác.

không.

Kết quả là, việc chính quy hóa L1 thúc đẩy tính thưa thớt, nghĩa là chỉ giữ lại những tính năng quan trọng nhất

hoạt động trong mô hình.

Điều này đặc biệt hữu ích khi chúng ta muốn lựa chọn tính năng tự động.

Ví dụ: trong tập dữ liệu có hàng trăm biến đầu vào, việc chính quy hóa L1 có thể giúp mô hình bỏ qua

những cái không liên quan.

Tuy nhiên, L1 đôi khi có thể dẫn đến các giải pháp không ổn định, đặc biệt nếu đầu vào có mối tương quan cao.

Bất chấp hạn chế này, nó vẫn là một công cụ mạnh mẽ để kiểm soát độ phức tạp và cải thiện tính khái quát hóa.

Chính quy hóa L2, còn được gọi là hồi quy sườn, thêm một hình phạt bằng tổng bình phương

của mọi trọng lượng.

Không giống như L1, điều khiển các trọng số về 0, L2 khuyến khích các trọng số nhỏ nhưng khác 0.

Điều này dẫn đến các mô hình mượt mà hơn trong đó không có trọng lượng nào chiếm ưu thế.

Bằng cách truyền bá tầm quan trọng trên các tính năng.

Chính quy hóa L2 làm giảm tình trạng quá khớp và giúp mô hình khái quát hóa tốt hơn dữ liệu mới.

Ví dụ: trong nhận dạng hình ảnh, L2 đảm bảo mô hình không phụ thuộc quá nhiều vào một pixel cụ thể

hoặc tính năng mà thay vào đó xem xét sự kết hợp cân bằng hơn vì tính ổn định và hiệu quả của nó.

L2 là phương pháp chính quy hóa được sử dụng phổ biến nhất trong học sâu.

Trên thực tế, khi bạn thấy thuật ngữ giảm trọng số trong các thuật toán tối ưu hóa như Adam hoặc SGD, nó thường

đề cập đến việc chính quy hóa L2 đang được áp dụng.

Cả việc chuẩn hóa L1 và L2 đều được thiết kế để giảm việc trang bị quá mức, nhưng chúng đạt được điều này một cách hơi khó khăn.

những cách khác nhau.

L1 thúc đẩy tính thưa thớt bằng cách đưa nhiều trọng số về 0, điều này khiến nó trở nên tuyệt vời trong việc lựa chọn tính năng

khi chúng ta muốn những mô hình đơn giản hơn.

Mặt khác, L2 phân phối ảnh hưởng đồng đều hơn bằng cách thu nhỏ tất cả các trọng số mà không loại bỏ

hoàn toàn chúng, tạo ra các mô hình mượt mà hơn.

Trong thực tế, một số mô hình thậm chí còn kết hợp cả hai phương pháp này trong một kỹ thuật được gọi là lưới đàn hồi.

được hưởng lợi từ cả độ thưa thớt và độ mịn.

Việc lựa chọn giữa L1 ​​và L2 tùy thuộc vào vấn đề hiện tại.

Nếu tập dữ liệu của bạn có nhiều tính năng không liên quan thì L1 có thể hiệu quả hơn.

Nếu bạn muốn những mẫu xe ổn định với sự phân bổ trọng lượng mượt mà, L2 thường là lựa chọn tốt hơn.

Cả hai phương pháp đều đóng vai trò quan trọng trong việc xây dựng các hệ thống học sâu mạnh mẽ.

Dropout là một trong những kỹ thuật chính quy hóa hiệu quả và sáng tạo nhất cho deep learning.

Ý tưởng rất đơn giản nhưng mạnh mẽ.

Trong quá trình huấn luyện, chúng tôi thả ngẫu nhiên một tỷ lệ tế bào thần kinh, tạm thời loại bỏ chúng và các kết nối của chúng

từ mạng.

Điều này ngăn cản các nơ-ron phụ thuộc quá nhiều vào nhau, buộc mạng phải học những thứ dư thừa

và các đại diện mạnh mẽ.

Bằng cách đó, việc bỏ học sẽ giảm nguy cơ trang bị quá mức và cải thiện khả năng khái quát hóa cho dữ liệu không nhìn thấy được.

Ví dụ: nếu tỷ lệ bỏ học được đặt thành 50% thì trong mỗi bước huấn luyện, một nửa số nơ-ron sẽ được chuyển sang

tắt một cách ngẫu nhiên.

Tuy nhiên, trong quá trình suy luận, tất cả các nơ-ron đều hoạt động nhưng đầu ra của chúng được điều chỉnh theo tỷ lệ tương ứng.

Bỏ học đã trở thành một phương pháp tiêu chuẩn trong việc đào tạo các mạng nơ-ron lớn, đặc biệt là trong máy tính.

nhiệm vụ xử lý ngôn ngữ tự nhiên và thị giác.

Dropout hoạt động bằng cách tạo ra một mạng mỏng khác nhau trên mỗi lần lặp đào tạo bằng cách chuyển ngẫu nhiên

tắt các tế bào thần kinh.

Mỗi lần chuyển tiếp sẽ đào tạo một phiên bản nhỏ hơn của mạng đầy đủ một cách hiệu quả qua nhiều lần lặp.

Điều này buộc mô hình phải phân phối việc học theo nhiều con đường khác nhau thay vì dựa vào một con đường duy nhất.

con đường thống trị.

Tại thời điểm thử nghiệm, tính năng bỏ học bị tắt và toàn bộ mạng được sử dụng nhưng trọng số được chia tỷ lệ theo tài khoản

để điều chỉnh giai đoạn đào tạo.

Theo một nghĩa nào đó, hoạt động bỏ học giống như huấn luyện một tập hợp gồm nhiều mạng nhỏ hơn và sau đó kết hợp chúng lại

thành một mô hình duy nhất, mạnh mẽ hơn.

Hành vi giống như tập hợp này là một trong những lý do chính khiến việc bỏ học cải thiện khả năng khái quát hóa một cách hiệu quả.

Tăng cường dữ liệu là một kỹ thuật mạnh mẽ để giảm việc trang bị quá mức bằng cách mở rộng đào tạo một cách giả tạo

tập dữ liệu Thay vì sửa đổi mô hình, chúng tôi sửa đổi dữ liệu bằng cách áp dụng các phép biến đổi như phép quay,

lật, dịch, cắt xén hoặc thêm tiếng ồn.

Điều này cho phép mô hình tiếp cận nhiều loại đầu vào hơn, buộc mô hình phải tìm hiểu các mẫu tổng quát hơn thay vì

hơn là ghi nhớ những chi tiết cụ thể.

Tăng cường dữ liệu đặc biệt phổ biến trong các tác vụ thị giác máy tính.

Ví dụ: một hình ảnh của một con mèo có thể được lật, xoay hoặc làm mờ một chút để tạo ra nhiều hình ảnh.

mẫu đào tạo.

Điều này đảm bảo rằng mạng nhận ra con mèo bất kể hướng hoặc ánh sáng.

Bằng cách tăng kích thước hiệu quả và tính đa dạng của tập dữ liệu, việc tăng cường dữ liệu sẽ cải thiện độ bền của mô hình

mà không yêu cầu thu thập thêm dữ liệu trong thế giới thực, việc này thường tốn kém hoặc không thực tế.

Trong khi việc tăng cường dữ liệu thường gắn liền với thị giác máy tính.

Nó có giá trị không kém trong các tác vụ xử lý ngôn ngữ tự nhiên và âm thanh trong NLP.

Việc tăng cường có thể liên quan đến việc thay thế các từ bằng từ đồng nghĩa, loại bỏ các từ một cách ngẫu nhiên hoặc sử dụng bản dịch ngược.

Dịch một câu sang ngôn ngữ khác và dịch ngược lại để tạo ra các biến thể trong quá trình xử lý âm thanh.

Các kỹ thuật tăng cường bao gồm thay đổi cao độ, kéo dài thời gian hoặc thêm tiếng ồn nền vào

làm cho các mô hình trở nên vững chắc hơn với các điều kiện của thế giới thực.

Các chiến lược này mở rộng tính đa dạng của tập dữ liệu trên các phương thức khác nhau, làm cho các mô hình ít nhạy cảm hơn

thu hẹp điều kiện đào tạo.

Trên thực tế, việc tăng cường dữ liệu cho phép các tập dữ liệu nhỏ hoạt động giống như các tập dữ liệu lớn hơn nhiều, cải thiện khả năng khái quát hóa

và giảm việc trang bị quá mức mà không tốn chi phí thu thập thêm dữ liệu thực.

Điều này làm cho nó trở thành một công cụ quan trọng để đào tạo các mô hình trong các lĩnh vực khan hiếm dữ liệu được gắn nhãn.

Mỗi phương pháp chính quy hóa có một cách tiếp cận khác nhau để giảm tình trạng trang bị quá mức.

Chính quy hóa L1 và L2 hoạt động bằng cách hạn chế kích thước của các trọng số, ngăn không cho mô hình trở thành

quá phức tạp.

Mặt khác, Dropout ngăn cản các nơ-ron dựa vào nhau quá nhiều, buộc mạng phải hoạt động

để phát triển các con đường dự phòng và mạnh mẽ.

Tăng cường dữ liệu giải quyết vấn đề từ góc độ dữ liệu, mở rộng tập dữ liệu để đảm bảo

mô hình nhận thấy sự đa dạng hơn trong quá trình đào tạo.

Trong thực tế, các phương pháp này thường được kết hợp.

Ví dụ: một mô hình thị giác máy tính có thể sử dụng chính quy hóa L2 để đảm bảo tính ổn định, loại bỏ để đảm bảo độ chắc chắn,

và tăng cường dữ liệu để mở rộng tập dữ liệu bằng cách tận dụng nhiều kỹ thuật chính quy hóa.

Chúng ta có thể xây dựng các mô hình không chỉ chính xác mà còn có khả năng phục hồi dữ liệu mới, chưa được nhìn thấy.

Tóm lại, việc chính quy hóa là điều cần thiết để đào tạo các mô hình deep learning có khả năng khái quát hóa tốt cho

dữ liệu mới.

Chính quy hóa L1 và L2 làm giảm việc trang bị quá mức bằng cách thêm các hình phạt vào các trọng số, với L1, thúc đẩy

thưa thớt và L2 thúc đẩy độ mịn.

Dropout tăng thêm tính ngẫu nhiên trong quá trình đào tạo bằng cách tắt nơ-ron, buộc mạng phải học dư thừa

và các đại diện mạnh mẽ.

Việc tăng cường dữ liệu sẽ mở rộng tập dữ liệu một cách giả tạo, đảm bảo mô hình được tiếp xúc với sự đa dạng hơn

và ít có khả năng ghi nhớ các mẫu đào tạo.

Mỗi phương pháp này giải quyết tình trạng trang bị quá mức theo một cách riêng và khi kết hợp với nhau, chúng sẽ nâng cao đáng kể

độ bền của mô hình.

Hiểu và áp dụng các kỹ thuật chính quy hóa là một trong những kỹ năng quan trọng nhất để xây dựng

hệ thống học sâu thực tế, đáng tin cậy.