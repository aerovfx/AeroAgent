# 1 -2.1 Những thách thức trong việc đào tạo Deep Models.en US

---

WEBVTT

Các mô hình deep learning cực kỳ mạnh mẽ nhưng việc đào tạo chúng không phải lúc nào cũng đơn giản.

Khi các mô hình ngày càng sâu hơn và phức tạp hơn, chúng trở nên khó tối ưu hóa hơn.

Hai trong số những vấn đề phổ biến nhất là độ dốc không ổn định và khả năng khái quát hóa kém.

Độ dốc không ổn định xảy ra khi các tín hiệu toán học được sử dụng cho việc học, được gọi là độ dốc,

co lại gần bằng 0 hoặc phát triển không kiểm soát, khiến việc đào tạo trở nên kém hiệu quả hoặc thậm chí là không thể thực hiện được.

Khả năng khái quát hóa kém xảy ra khi các mô hình ghi nhớ dữ liệu huấn luyện quá chặt chẽ hoặc không nắm bắt được

các mẫu hoàn toàn.

Những thử thách này có thể khiến học sinh học chậm, dự đoán không chính xác hoặc thất bại trong quá trình huấn luyện.

Hiểu những vấn đề này là bước đầu tiên để giải quyết chúng, đó là lý do tại sao chúng tôi dành toàn bộ

phần dành cho các thách thức như độ dốc biến mất và bùng nổ, trang bị quá mức và trang bị thiếu.

Sau đó, chúng ta sẽ khám phá các kỹ thuật tối ưu hóa và chính quy hóa được thiết kế để giải quyết những rào cản và

làm cho các mô hình deep learning vừa chính xác vừa đáng tin cậy.

Trong mạng lưới thần kinh, độ dốc là động cơ thúc đẩy việc học.

Độ dốc đơn giản là đạo hàm của hàm mất đối với trọng số.

Nó cho chúng ta biết cần điều chỉnh trọng số đó bao nhiêu để giảm lỗi trong quá trình truyền ngược.

Độ dốc được tính toán từng lớp, chảy ngược qua mạng.

Bằng cách sử dụng các độ dốc này, các thuật toán tối ưu hóa như giảm độ dốc sẽ cập nhật các trọng số và giúp

mạng lưới học hỏi.

Tuy nhiên, vấn đề nảy sinh khi các gradient này trở nên cực kỳ nhỏ hoặc cực kỳ lớn khi chúng

truyền qua nhiều lớp.

Độ dốc nhỏ có nghĩa là mạng hầu như không học được, trong khi độ dốc lớn khiến việc cập nhật trọng số không ổn định.

Những vấn đề này được gọi là độ dốc biến mất và bùng nổ và chúng có thể hạn chế nghiêm trọng tính hiệu quả

của mạng sâu nếu không được quản lý đúng cách.

Vấn đề độ dốc biến mất xảy ra khi độ dốc trở nên cực kỳ nhỏ khi chúng được truyền ngược lại

thông qua các lớp của một mạng lưới sâu.

Điều này đặc biệt phổ biến trong các kiến ​​trúc rất sâu hoặc mạng lưới thần kinh tái phát.

Khi độ dốc biến mất, việc cập nhật trọng số trở nên không đáng kể, điều đó có nghĩa là các lớp trước đó trong mạng

ngừng học tập.

Kết quả là mạng có thể mất một thời gian rất dài để hội tụ hoặc không thể huấn luyện hoàn toàn.

Các hàm kích hoạt như sigmoid và tanh h góp phần gây ra vấn đề này, vì độ dốc của chúng bị san phẳng

out cho các giá trị đầu vào lớn, đẩy độ dốc về 0 một cách hiệu quả.

Độ dốc biến mất là một trong những trở ngại lớn nhất đối với việc học sâu trong nhiều thập kỷ, cho đến khi kích hoạt mới

các chức năng như ReLU và các phương pháp khởi tạo được cải tiến đã giúp giảm bớt vấn đề.

Vấn đề độ dốc bùng nổ ngược lại với độ dốc biến mất ở đây.

Độ dốc tăng theo cấp số nhân khi chúng truyền ngược qua mạng.

Điều này thường dẫn đến cập nhật trọng số cực lớn, khiến mô hình dao động dữ dội hoặc thậm chí phân kỳ.

hoàn toàn.

Thay vì học các mẫu hữu ích, mạng trở nên không ổn định, tạo ra những dự đoán vô nghĩa.

Độ dốc bùng nổ đặc biệt phổ biến trong các mạng thần kinh tái phát, trong đó có cùng trọng số

được áp dụng nhiều lần theo thời gian, làm tăng thêm sai số.

Chúng cũng có thể xảy ra trong các mạng tiếp liệu rất sâu với khả năng khởi tạo trọng số kém.

Để giải quyết vấn đề bùng nổ độ dốc, các kỹ thuật như cắt độ dốc thường được sử dụng để giới hạn mức tối đa

giá trị mà một gradient có thể nhận.

Giữ quá trình luyện tập ổn định và có kiểm soát, độ dốc biến mất và bùng nổ nghe có vẻ trái ngược nhau,

nhưng cả hai đều dẫn đến cùng một kết quả.

Học tập kém.

Trong trường hợp độ dốc biến mất, các giá trị co lại nhiều đến mức trọng số ở các lớp đầu hầu như không thay đổi,

để lại những lớp đó chưa được đào tạo đầy đủ.

Mặt khác, việc bùng nổ độ dốc khiến các bản cập nhật trở nên quá lớn, khiến mô hình

không ổn định và không có khả năng hội tụ.

Cả hai trường hợp đều ngăn mạng nắm bắt hiệu quả các mẫu trong dữ liệu.

Những vấn đề này nêu bật sự cân bằng tinh tế cần thiết khi đào tạo các mô hình sâu với ít điều chỉnh,

và mô hình sẽ không học được quá nhiều điều chỉnh và nó trở nên hỗn loạn.

May mắn thay, các giải pháp như chức năng kích hoạt tốt hơn, khởi tạo trọng số cẩn thận, chuẩn hóa

các kỹ thuật và việc cắt độ dốc cung cấp các cách để giữ độ dốc trong phạm vi lành mạnh.

Overfitting là một trong những thách thức phổ biến nhất trong việc đào tạo các mô hình sâu.

Nó xảy ra khi mạng ghi nhớ các ví dụ huấn luyện thay vì học các mẫu chung.

Kết quả là mô hình hoạt động rất tốt trên dữ liệu huấn luyện nhưng không đưa ra dự đoán chính xác

trên dữ liệu chưa nhìn thấy.

Điều này được gọi là khái quát hóa kém.

Quá khớp thường xảy ra do các mô hình quá phức tạp so với dữ liệu có sẵn, có quá nhiều tham số

tương ứng với số lượng mẫu.

Đây là vấn đề về phương sai cao, có nghĩa là mô hình quá nhạy cảm với những biến động nhỏ trong quá trình huấn luyện

thiết lập.

Mặc dù việc trang bị quá mức có thể được giảm bớt bằng các kỹ thuật như chính quy hóa, bỏ học và tăng cường dữ liệu.

Nhận ra nó là bước đầu tiên.

Một dấu hiệu rõ ràng của việc trang bị quá mức là khi độ chính xác trong huấn luyện tiếp tục được cải thiện trong khi độ chính xác của việc xác nhận

từ chối.

Underfitting là đối diện của overfitting.

Trong trường hợp này, mô hình quá đơn giản để nắm bắt được độ phức tạp của dữ liệu.

Kết quả là nó hoạt động kém không chỉ trên dữ liệu thử nghiệm mà còn trên tập huấn luyện.

Underfitting là một vấn đề sai lệch cao.

Mô hình đưa ra các giả định quá đơn giản và bỏ qua các tính năng quan trọng trong dữ liệu.

Ví dụ: sử dụng mô hình tuyến tính để phân loại hình ảnh động vật sẽ mang lại độ chính xác kém vì

dữ liệu đòi hỏi các mối quan hệ phi tuyến tính, phức tạp hơn.

Sự phù hợp có thể được giải quyết bằng cách tăng độ phức tạp của mô hình, thêm nhiều lớp hoặc nơ-ron hơn hoặc cho phép

mô hình để đào tạo lâu hơn.

Giống như trang bị quá mức, trang bị thiếu là dấu hiệu cho thấy sự cân bằng giữa độ phức tạp của mô hình và dữ liệu chưa được đáp ứng.

đã đạt được.

Trang bị quá mức và thiếu trang bị là hai mặt của cùng một đồng tiền.

Quá khớp xảy ra khi mô hình trở nên quá phức tạp.

Ghi nhớ dữ liệu đào tạo thay vì khái quát hóa.

Underfitting xảy ra khi mô hình quá đơn giản và không nắm bắt được các mẫu quan trọng.

Kịch bản lý tưởng là ở đâu đó ở giữa, nơi mô hình đủ phức tạp để tìm hiểu các mối quan hệ có ý nghĩa,

nhưng không phức tạp đến mức nó ghi nhớ tiếng ồn.

Đạt được sự cân bằng này là một thách thức trọng tâm trong học máy.

Một trong những cách tốt nhất để theo dõi sự cân bằng này là so sánh hiệu suất đào tạo và xác nhận.

Nếu độ chính xác đào tạo cao nhưng độ chính xác xác nhận thấp.

Mô hình này quá phù hợp.

Nếu cả hai đều thấp thì mô hình chưa phù hợp.

Đạt được sự cân bằng phù hợp là điều cho phép các mô hình deep learning khái quát hóa một cách hiệu quả cho những điều mới, chưa từng thấy.

dữ liệu.

Các khái niệm về trang bị quá mức và thiếu trang bị gắn chặt với phương sai sai lệch.

Đánh đổi.

Mức độ phù hợp tương ứng với độ lệch cao, trong đó mô hình đưa ra các giả định quá đơn giản và bỏ qua

độ phức tạp và dữ liệu.

Quá khớp tương ứng với phương sai cao, trong đó mô hình quá nhạy cảm với nhiễu trong quá trình huấn luyện

thiết lập.

Mô hình tối ưu tìm ra sự cân bằng phù hợp, giảm thiểu cả độ lệch và phương sai.

Sự đánh đổi này là trọng tâm của học máy.

Bằng cách lựa chọn cẩn thận kiến trúc mô hình, thời gian đào tạo và áp dụng các kỹ thuật như chính quy hóa,

tăng cường dữ liệu hoặc dừng sớm, chúng ta có thể tìm thấy điểm phù hợp.

Nắm vững sự đánh đổi này là điều cần thiết để xây dựng các hệ thống deep learning không chỉ hoạt động tốt

về dữ liệu huấn luyện mà còn khái quát hóa một cách hiệu quả cho các ứng dụng trong thế giới thực.

Tóm lại, việc đào tạo các mô hình sâu đi kèm với một số thách thức.

Độ dốc biến mất và bùng nổ khiến mạng khó học hiệu quả, đặc biệt

trong các kiến trúc rất sâu hoặc lặp lại.

Quá khớp xảy ra khi các mô hình ghi nhớ dữ liệu huấn luyện thay vì khái quát hóa, trong khi khớp thiếu xảy ra

khi các mô hình quá đơn giản để nắm bắt được các mẫu có ý nghĩa.

Cùng với nhau, những thách thức này nêu bật khó khăn trong việc cân bằng giữa sự ổn định và tính khái quát hóa sâu sắc.

học tập.

Sự đánh đổi phương sai sai lệch cung cấp một khuôn khổ hữu ích để hiểu những vấn đề này.

Trong các phần sắp tới, chúng ta sẽ đi sâu vào các kỹ thuật tối ưu hóa và chính quy hóa được thiết kế để giải quyết

những thách thức này, cho phép các mô hình học sâu học hiệu quả và khái quát hóa tốt những điều chưa được nhìn thấy

dữ liệu.