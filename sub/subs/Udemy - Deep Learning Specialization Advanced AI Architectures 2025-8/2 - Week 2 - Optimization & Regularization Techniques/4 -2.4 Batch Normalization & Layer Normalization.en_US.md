# 4 -2.4 Chuẩn hóa hàng loạt & Chuẩn hóa lớp.en US

---

WEBVTT

Đào tạo.

Mạng lưới thần kinh sâu có thể không ổn định, đặc biệt khi dữ liệu truyền qua nhiều lớp.

Các giá trị hoặc kích hoạt trong mỗi lớp có thể khác nhau về quy mô, có thể gây nổ hoặc

gradient biến mất trong quá trình lan truyền ngược.

Sự không ổn định này làm cho việc học không hiệu quả và có thể ngăn mạng hội tụ về mức tối ưu.

giải pháp.

Để giải quyết thách thức này, các nhà nghiên cứu đã phát triển các kỹ thuật chuẩn hóa bằng cách chuẩn hóa các kích hoạt.

Chúng tôi đảm bảo rằng việc phân phối giá trị nằm trong phạm vi được kiểm soát.

Điều này giúp ổn định việc đào tạo, tăng tốc độ hội tụ và cải thiện khả năng khái quát hóa.

Trong phần này, chúng ta sẽ tập trung vào hai phương pháp chuẩn hóa hàng loạt và chuẩn hóa lớp được sử dụng rộng rãi.

Cả hai kỹ thuật đều nhằm mục đích giữ cho kích hoạt mạng thần kinh được cân bằng, nhưng chúng khác nhau về cách bình thường hóa

và chúng hoạt động tốt nhất trong những tình huống nào.

Chuẩn hóa hàng loạt, hay định mức theo lô, được giới thiệu vào năm 2015 và nhanh chóng trở thành một trong những tiêu chuẩn có ảnh hưởng nhất

những đổi mới trong học tập sâu.

Ý tưởng đơn giản là bình thường hóa việc kích hoạt cho từng tính năng trên một loạt mẫu đào tạo.

Điều này được thực hiện trước khi áp dụng hàm kích hoạt, thường là giữa phép biến đổi tuyến tính và

kích hoạt phi tuyến tính.

Bằng cách tiêu chuẩn hóa các kích hoạt, BatchNorm giảm cái gọi là dịch chuyển đồng biến nội bộ, trong đó

Việc phân phối đầu vào cho mỗi lớp thay đổi khi mạng học hỏi.

Sự thay đổi này thường làm chậm quá trình đào tạo và yêu cầu điều chỉnh thông số cẩn thận.

BatchNorm giải quyết vấn đề này bằng cách giữ đầu vào ổn định, cho phép mạng đào tạo nhanh hơn và hiệu quả hơn

độ chính xác.

Kể từ đó, nó đã trở thành một thành phần tiêu chuẩn trong nhiều kiến trúc deep learning, từ mạng tích chập

đến máy biến áp.

Chuẩn hóa hàng loạt hoạt động theo bốn bước chính.

Đầu tiên, đối với mỗi lô dữ liệu huấn luyện nhỏ, nó sẽ tính toán giá trị trung bình và phương sai của các lần kích hoạt cho

từng tính năng.

Thứ hai, nó bình thường hóa các kích hoạt này bằng cách trừ giá trị trung bình và chia cho độ lệch chuẩn,

dẫn đến các giá trị có giá trị trung bình bằng 0 và phương sai đơn vị.

Thứ ba, nó giới thiệu hai tham số gamma và beta có thể học được, giúp chia tỷ lệ và dịch chuyển giá trị chuẩn hóa

các giá trị.

Điều này đảm bảo rằng mạng vẫn giữ được toàn bộ sức mạnh đại diện thay vì bị hạn chế ở

kích hoạt bình thường hóa.

Cuối cùng, các giá trị đã điều chỉnh được chuyển vào hàm kích hoạt.

Bằng cách lặp lại quy trình này ở mỗi lớp, BatchNorm giữ cho các kích hoạt hoạt động tốt, dẫn đến nhiều

đào tạo ổn định và hiệu quả.

Chuẩn hóa hàng loạt mang lại một số lợi ích cho việc học sâu.

Đầu tiên, nó tăng tốc quá trình đào tạo bằng cách ổn định việc phân phối kích hoạt, cho phép học tập cao hơn

tỷ giá được sử dụng mà không có sự bất ổn.

Thứ hai, nó làm giảm độ nhạy đối với việc khởi tạo trọng số, có nghĩa là các mô hình có thể hội tụ đáng tin cậy hơn

ngay cả với điều kiện khởi đầu dưới mức tối ưu.

Thứ ba, BatchNorm có tác dụng điều chỉnh nhẹ, giảm việc trang bị quá mức bằng cách thêm tiếng ồn trong quá trình mini.

chuẩn hóa hàng loạt.

Điều này làm cho nó hoạt động giống như bỏ học trong thực tế.

Cuối cùng, bằng cách ổn định quá trình học tập, BatchNorm cho phép xây dựng các kiến trúc sâu hơn nhiều

vì nó giảm thiểu các vấn đề như độ dốc biến mất.

Những ưu điểm này giải thích tại sao BatchNorm được áp dụng rộng rãi trên các lĩnh vực như thị giác máy tính, tự nhiên

xử lý ngôn ngữ và học tăng cường.

Mặc dù thành công nhưng việc chuẩn hóa hàng loạt vẫn có những hạn chế.

Nó dựa vào số liệu thống kê được tính toán từ các lô nhỏ, có nghĩa là hiệu suất của nó có thể bị ảnh hưởng khi lô

kích thước rất nhỏ.

Điều này làm cho nó kém hiệu quả hơn trong các cài đặt không khả thi với các lô lớn, chẳng hạn như môi trường bộ nhớ thấp

hoặc các tập dữ liệu nhỏ.

Định mức hàng loạt cũng không lý tưởng cho việc học trực tuyến, nơi dữ liệu đến từng mẫu một hoặc cho

mạng thần kinh tái phát, trong đó sự phụ thuộc thời gian làm phức tạp việc chuẩn hóa dựa trên hàng loạt.

Để giải quyết những hạn chế này, các nhà nghiên cứu đã đề xuất các giải pháp thay thế như chuẩn hóa lớp và

chuẩn hóa nhóm.

Các phương pháp này loại bỏ sự phụ thuộc vào kích thước lô và hoạt động tốt hơn trong các bối cảnh nhất định, chẳng hạn như NLP

hoặc học tăng cường.

Hiểu được sự đánh đổi giúp chúng ta chọn phương pháp chuẩn hóa phù hợp cho vấn đề hiện tại.

Chuẩn hóa lớp hoặc định mức lớp là một giải pháp thay thế cho định mức hàng loạt chuẩn hóa trên các tính năng

trong một ví dụ huấn luyện duy nhất, thay vì trong toàn bộ đợt.

Điều này làm cho nó độc lập với quy mô lô và nhất quán hơn trong các tình huống đào tạo.

Layer Norm đặc biệt hiệu quả trong xử lý ngôn ngữ tự nhiên và các mô hình tuần tự như máy biến áp,

trong đó kích thước lô có thể nhỏ hoặc thay đổi.

Bằng cách chuẩn hóa các tính năng trong mỗi lớp ví dụ, Norm ổn định động lực đào tạo và đảm bảo hoạt động mượt mà hơn

luồng gradient qua mạng.

Nó được sử dụng rộng rãi trong các kiến trúc dựa trên máy biến áp như Bert và GPT, trong đó định mức lô sẽ ít hơn

hiệu quả trong những bối cảnh này.

Layernorm giúp các mô hình học nhanh hơn, hội tụ đáng tin cậy hơn và thực hiện tốt hơn các nhiệm vụ liên quan đến

chuỗi văn bản hoặc dữ liệu chuỗi thời gian.

Quá trình chuẩn hóa lớp hoạt động tương tự như BatchNorm nhưng có điểm khác biệt chính về cách nó chuẩn hóa,

thay vì tính toán số liệu thống kê trên toàn bộ lô.

Layernorm tính toán giá trị trung bình và phương sai của các đặc điểm của từng mẫu đào tạo riêng lẻ.

Mỗi ví dụ được chuẩn hóa để có giá trị trung bình bằng 0 và phương sai đơn vị, sau đó được chia tỷ lệ và dịch chuyển theo giá trị có thể học được.

tham số gamma và beta, giống như trong BatchNorm.

Điều này làm cho layernorm không phụ thuộc vào kích thước lô vì mỗi đầu vào được xử lý riêng lẻ.

Nó đặc biệt phù hợp với các mô hình xử lý dữ liệu có độ dài tuần tự hoặc thay đổi, khi nhất quán

chuẩn hóa là quan trọng bằng cách đảm bảo kích hoạt ổn định cho từng đầu vào, layernorm cải thiện cả khả năng hội tụ

và hiệu suất tổng thể trong các nhiệm vụ như mô hình hóa ngôn ngữ, dịch thuật và củng cố

học tập.

BatchNorm và Layernorm đều nhằm mục đích ổn định và tăng tốc quá trình đào tạo, nhưng chúng thực hiện theo những cách khác nhau.

cách.

BatchNorm chuẩn hóa trên kích thước lô bằng cách sử dụng số liệu thống kê từ tất cả các mẫu trong một lô nhỏ.

Điều này hoạt động tốt trong các tác vụ thị giác máy tính trong đó kích thước lô lớn nhưng gặp khó khăn với các tác vụ nhỏ hoặc thay đổi.

lô.

Mặt khác, Layernorm chuẩn hóa các đặc điểm trong từng mẫu riêng lẻ, làm cho nó

độc lập với kích thước lô.

Điều này làm cho Layernorm phù hợp hơn với việc xử lý ngôn ngữ tự nhiên.

Máy biến áp và mô hình lặp lại trong đó đầu vào là tuần tự và kích cỡ lô có thể khác nhau.

Trong thực tế, việc lựa chọn giữa BatchNorm và Layernorm phụ thuộc vào miền vấn đề.

Các tác vụ tầm nhìn có xu hướng được hưởng lợi nhiều hơn từ BatchNorm, trong khi các tác vụ dựa trên văn bản và trình tự phụ thuộc nhiều vào

trên Layernorm.

Ngoài BatchNorm và Layernorm, còn có các phương pháp chuẩn hóa khác như chuẩn hóa nhóm,

nằm giữa hai cách tiếp cận.

Chuẩn nhóm chia các đặc điểm thành các nhóm và chuẩn hóa trong mỗi nhóm.

Điều này làm cho nó ít phụ thuộc hơn vào kích thước lô trong khi vẫn tận dụng được lợi thế của việc chuẩn hóa dựa trên tính năng.

Định mức nhóm đã cho thấy tính hiệu quả đặc biệt trong các nhiệm vụ thị giác máy tính trong đó quy mô lô nhỏ là phổ biến,

chẳng hạn như đào tạo về bộ nhớ GPU hạn chế.

Mặc dù không được sử dụng rộng rãi như BatchNorm hoặc Layernorm, nhưng nó mở rộng bộ công cụ về các phương pháp chuẩn hóa,

giúp người thực hành linh hoạt hơn trong việc xử lý các tập hợp dữ liệu, kiến trúc và các ràng buộc phần cứng khác nhau.

Việc đề cập đến chuẩn mực nhóm giúp chúng ta hiểu rằng việc chuẩn hóa không phải là một kích thước phù hợp cho tất cả.

Các chiến lược khác nhau hoạt động tốt nhất tùy thuộc vào nhiệm vụ và nguồn lực sẵn có.

Tóm lại, các kỹ thuật chuẩn hóa là rất quan trọng để huấn luyện các nơ-ron sâu một cách ổn định và hiệu quả.

mạng.

Chuẩn hóa hàng loạt chuẩn hóa các kích hoạt trên một loạt dữ liệu, giảm sự dịch chuyển hiệp phương sai nội bộ

và cho phép hội tụ nhanh hơn, đáng tin cậy hơn.

Nó đặc biệt mạnh mẽ trong các tác vụ thị giác máy tính với kích thước lô lớn.

Ngược lại, chuẩn hóa lớp chuẩn hóa các đặc điểm trong mỗi mẫu, làm cho nó trở nên độc lập

có kích thước lô và lý tưởng cho máy biến áp NLP và các mô hình tuần tự.

Chuẩn hóa nhóm cung cấp một cách tiếp cận kết hợp hữu ích cho các nhiệm vụ tầm nhìn hàng loạt nhỏ.

Cùng với nhau, những kỹ thuật này tạo thành một phần quan trọng của bộ công cụ học sâu hiện đại bằng cách ổn định kích hoạt.

Chúng cho phép các mô hình đào tạo nhanh hơn, hội tụ tốt hơn và đạt được hiệu suất hiện đại trên toàn thế giới.

một loạt các ứng dụng.