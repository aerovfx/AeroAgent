# 1 -6.1 Bộ mã hóa tự động (Cơ bản & VAE).en US

---

WEBVTT

Bộ mã hóa tự động là một trong những kiến trúc nền tảng trong mô hình tổng quát.

Không giống như các mô hình được giám sát dự đoán nhãn, bộ mã hóa tự động học cách biểu diễn dữ liệu ở dạng nén

và sau đó được xây dựng lại sao cho giống nhất có thể với bản gốc.

Chúng bao gồm hai phần chính, một bộ mã hóa giúp giảm đầu vào thành biểu diễn tiềm ẩn nhỏ hơn,

và một bộ giải mã tái tạo lại dữ liệu từ mã tiềm ẩn đó.

Mục đích không chỉ là nén mà còn để tìm hiểu cách biểu diễn dữ liệu bên trong có ý nghĩa.

Sau đó, những biểu diễn này có thể được sử dụng cho các nhiệm vụ như khử nhiễu, phát hiện bất thường hoặc làm cơ sở

cho mô hình tổng quát.

Kiến trúc của bộ mã hóa tự động cơ bản là đối xứng.

Lớp đầu vào tương ứng với kích thước dữ liệu gốc, chẳng hạn như pixel trong ảnh.

Bộ mã hóa là một chuỗi các lớp mạng thần kinh làm giảm việc sử dụng chiều, nén

thông tin vào một vectơ nhỏ hơn gọi là mã tiềm ẩn.

Bộ giải mã sau đó lấy biểu diễn tiềm ẩn này và cố gắng tái tạo lại đầu vào ban đầu.

Việc đào tạo được hướng dẫn bởi sự mất mát tái thiết, lỗi bình phương thường có nghĩa là, đo lường mức độ gần gũi

đầu ra được xây dựng lại là đầu vào ban đầu.

Lỗi càng nhỏ thì bộ mã hóa tự động càng học được cách biểu diễn dữ liệu có ý nghĩa càng tốt.

Mặc dù các bộ mã hóa tự động cơ bản đơn giản có những ứng dụng mạnh mẽ nhưng chúng có thể làm giảm tính chiều của

dữ liệu trong khi vẫn bảo toàn các mẫu quan trọng, làm cho chúng trở thành một sự thay thế phi tuyến tính cho các kỹ thuật như hiệu trưởng

phân tích thành phần.

Bộ mã hóa tự động cũng có thể thực hiện khử nhiễu bằng cách huấn luyện dữ liệu sạch và xây dựng lại dữ liệu đó từ đầu vào nhiễu.

Một ứng dụng phổ biến khác là phát hiện sự bất thường, trong đó các lỗi tái tạo có thể chỉ ra sự bất thường.

hoặc các mô hình gian lận trong nghiên cứu học sâu trước đây.

Bộ mã hóa tự động cũng được sử dụng để huấn luyện trước các mạng lưới thần kinh sâu không có giám sát trước khi được giám sát

tinh chỉnh.

Thậm chí ngày nay, khả năng học hỏi các tính năng nén, có ý nghĩa khiến chúng trở nên có giá trị trên nhiều miền

bất chấp sức mạnh của họ.

Bộ mã hóa tự động cơ bản có một số hạn chế.

Đầu tiên, họ có thể dễ dàng khớp quá mức bằng cách ghi nhớ dữ liệu thay vì học các mẫu có thể khái quát hóa.

Thứ hai, mặc dù chúng có thể tái tạo lại đầu vào nhưng chúng không có khả năng tạo ra một cách tự nhiên, nghĩa là chúng không thể

dễ dàng tạo ra các mẫu mới, chưa từng thấy.

Thứ ba, hiệu suất phụ thuộc rất nhiều vào kích thước của không gian tiềm ẩn.

Quá nhỏ thì họ sẽ mất thông tin quá lớn và không nén được một cách có ý nghĩa.

Cuối cùng, bộ mã hóa tự động thiếu khả năng diễn giải xác suất rõ ràng, gây khó khăn cho việc mô hình hóa dữ liệu

phân phối trực tiếp.

Những hạn chế này đã truyền cảm hứng cho sự phát triển của các phiên bản nâng cao hơn như Bộ mã hóa tự động biến đổi

hoặc VAE.

Bộ mã hóa tự động biến đổi hoặc Vaes đã được Kingma và Welling giới thiệu vào năm 2013 để giải quyết những thiếu sót

của bộ mã hóa tự động cơ bản.

Thay vì chỉ nén dữ liệu đầu vào, Vaes xử lý không gian tiềm ẩn dưới dạng phân phối xác suất,

thường là Gaussian.

Điều này cho phép mô hình lấy mẫu từ không gian tiềm ẩn và tạo ra các điểm dữ liệu hoàn toàn mới, không chỉ

tái tạo lại những gì nó đã thấy.

Sự đổi mới này đã làm cho bộ mã hóa tự động trở thành những mô hình tổng hợp thực sự có khả năng tạo ra dữ liệu tổng hợp thực tế.

Vaes đại diện cho một cột mốc quan trọng trong học sâu, kết hợp mạng lưới thần kinh với các ý tưởng từ xác suất

suy luận.

Trong bộ mã hóa tự động biến thiên, bộ mã hóa không chỉ làm nhiều việc hơn là chỉ nén dữ liệu đầu vào.

Thay vào đó, nó đưa ra hai giá trị trung bình và phương sai của phân bố Gauss tiềm ẩn.

Sau đó, một vectơ tiềm ẩn được lấy mẫu từ phân phối này để thể hiện đầu vào.

Vì việc lấy mẫu phá vỡ luồng gradient nên Vaes sử dụng thủ thuật Tham số lại.

Điều này biểu thị độ tiềm ẩn được lấy mẫu dưới dạng phương sai trung bình nhân với một thuật ngữ nhiễu ngẫu nhiên.

Điều này làm cho toàn bộ quá trình có thể khác biệt và có thể huấn luyện được bằng phương pháp lan truyền ngược.

Bộ giải mã sau đó sẽ xây dựng lại dữ liệu từ vectơ tiềm ẩn được lấy mẫu này, cho phép cả việc tái tạo và

tạo ra các mẫu mới bằng cách lấy mẫu từ phân phối tiềm ẩn.

Hàm mất của bộ mã hóa tự động biến thiên có hai phần.

Đầu tiên là tổn thất tái tạo, tương tự như bộ mã hóa tự động cơ bản, đảm bảo rằng đầu ra giống với

đầu vào.

Thứ hai là tổn thất phân kỳ KL, đo lường mức độ phù hợp của phân phối tiềm ẩn đã học

phân phối trước Gaussian bằng cách giảm thiểu cả hai số hạng.

Vaes đạt được sự cân bằng.

Chúng tái tạo lại các đầu vào một cách chính xác đồng thời cấu trúc không gian tiềm ẩn trở nên trơn tru và liên tục.

Không gian tiềm ẩn có cấu trúc này giúp Vaes có khả năng tạo ra các mẫu thực tế mới bằng cách lấy mẫu đơn giản.

từ lần phân phối trước đó.

Vaes.

Không giống như các khả năng tổng hợp vượt xa các bộ mã hóa tự động cơ bản, chúng có thể tạo ra các hình ảnh tổng hợp

chẳng hạn như khuôn mặt người hoặc chữ số viết tay trông rất thực tế.

Chúng cũng hữu ích cho việc tăng cường dữ liệu, tạo ra các biến thể của dữ liệu hiện có để cải thiện việc đào tạo mô hình.

Vì phân bố xác suất theo mô hình Vaes nên chúng rất tuyệt vời để phát hiện sự bất thường khi khả năng xảy ra thấp

điểm số cho thấy đầu vào bất thường.

Một ứng dụng thú vị khác là khám phá không gian tiềm ẩn.

Bằng cách nội suy giữa các điểm trong không gian tiềm ẩn, Vaes có thể chuyển đổi một cách trơn tru đầu vào này sang đầu vào khác,

thể hiện khả năng nắm bắt các biểu diễn liên tục phong phú.

Vaes cung cấp những điểm mạnh quan trọng.

Chúng cung cấp một khung xác suất rõ ràng, các không gian tiềm ẩn có cấu trúc và khả năng tạo ra

những mẫu mới.

Tuy nhiên, chúng không phải là không có hạn chế.

Một trong những lời chỉ trích phổ biến nhất là hình ảnh được tạo ra bởi VAE có xu hướng bị mờ so với các hình ảnh tổng hợp khác.

cách tiếp cận như Gans.

Sự mờ nhạt này là kết quả của sự đánh đổi giữa hàm mất mát của chúng giữa độ chính xác của việc tái tạo và

thực thi phân phối tiềm ẩn trơn tru.

Ngoài ra, mặc dù Vaes ít tốn tài nguyên hơn một số mô hình hiện đại, nhưng việc đào tạo chúng trên quy mô lớn

vẫn có thể đòi hỏi tính toán.

Bất chấp những thách thức này, Vaes vẫn là một công cụ mạnh mẽ trong bộ công cụ tạo mô hình tổng quát.

Tóm lại, bộ mã hóa tự động là các mạng thần kinh không được giám sát, tìm hiểu các cách biểu diễn dữ liệu nhỏ gọn.

Bộ mã hóa tự động cơ bản rất hữu ích cho việc tái tạo, khử nhiễu và phát hiện bất thường, nhưng chúng thiếu

năng lực sáng tạo thực sự.

Bộ mã hóa tự động biến thể mở rộng khung này bằng mô hình xác suất, cho phép tiềm ẩn có cấu trúc

không gian và tạo ra các mẫu hoàn toàn mới.

Chúng đã được áp dụng trong các lĩnh vực từ tạo hình ảnh đến phát hiện sự bất thường và vẫn là một phần quan trọng

nền tảng cho AI sáng tạo.

Tuy nhiên, xu hướng tạo ra các kết quả đầu ra mờ nhạt của chúng đã thúc đẩy sự phát triển của các phương pháp sinh sản khác.

các mô hình.

Trong phần tiếp theo, chúng ta sẽ đi sâu vào một trong những cách tiếp cận nổi tiếng và mạnh mẽ nhất.

Mạng hoặc Gans.