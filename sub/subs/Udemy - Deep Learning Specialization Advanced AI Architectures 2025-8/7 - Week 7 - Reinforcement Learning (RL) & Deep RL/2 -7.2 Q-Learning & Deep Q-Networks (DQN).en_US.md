# 2 -7.2 Q-Learning & Deep Q-Networks (DQN).en Hoa Kỳ

---

WEBVTT

Q-learning là một trong những thuật toán quan trọng nhất trong học tăng cường.

Nó không có mô hình, nghĩa là nó không yêu cầu kiến thức về cách môi trường hoạt động, nó chỉ đơn giản là học

từ kinh nghiệm.

Ý tưởng chính là tác nhân học một hàm giá trị hành động được gọi là Q, hàm này ước tính mức độ tốt của nó.

là thực hiện một hành động nhất định trong một trạng thái nhất định.

Không giống như một số thuật toán khác, Q-learning nằm ngoài chính sách, có nghĩa là nó có thể học từ cả hành động thực tế

các hành động được thực hiện và thăm dò, ngay cả khi chúng không tối ưu.

Theo thời gian, Q-learning cập nhật các ước tính của mình cho đến khi hội tụ được chính sách tối ưu, chính sách tối đa hóa

thưởng tích lũy.

Cách tiếp cận này tạo nền tảng cho nhiều thuật toán RL nâng cao, bao gồm cả mạng Q sâu hoặc Dqns.

Hàm Q là trọng tâm của Q-learning Q a thể hiện phần thưởng tích lũy dự kiến khi thực hiện

hành động A ở trạng thái và sau đó tuân theo chính sách tối ưu.

Ban đầu, giá trị Q chỉ là phỏng đoán, nhưng qua sự tương tác lặp đi lặp lại với môi trường,

thuật toán cập nhật chúng để phản ánh những kỳ vọng chính xác hơn.

Bằng cách chọn các hành động có giá trị Q cao nhất, tác nhân học cách đưa ra quyết định tốt hơn.

Về cơ bản, hàm Q cung cấp cho tác nhân cách so sánh các tùy chọn của nó ở bất kỳ trạng thái nào và chọn

hành động có khả năng mang lại phần thưởng lâu dài lớn nhất.

Quá trình cập nhật trong Q-learning được điều khiển bởi phương trình Bellman.

Công thức đệ quy này thể hiện cách ước tính giá trị của một cặp hành động trạng thái từ

phần thưởng, cộng với những phần thưởng tốt nhất có thể có trong tương lai.

Quy tắc cập nhật là Qsar q a cộng a r cộng max Kusa kusa.

Đây a là tốc độ học tập kiểm soát lượng thông tin mới lấn át kiến ​​thức cũ.

G là hệ số chiết khấu giúp cân bằng tầm quan trọng của phần thưởng trước mắt và phần thưởng trong tương lai.

Thuật ngữ trong ngoặc thể hiện sự khác biệt giữa ước tính hiện tại và phần thưởng mục tiêu,

cộng với phần thưởng dự kiến trong tương lai.

Việc áp dụng bản cập nhật này nhiều lần cho phép tổng đài viên tinh chỉnh các giá trị q của nó và cuối cùng tìm hiểu các giá trị tối ưu

chiến lược.

Để minh họa Q-learning, hãy tưởng tượng một tác nhân đang điều hướng một thế giới lưới.

Mỗi ô là một trạng thái và các hành động có thể thực hiện bao gồm di chuyển lên, xuống, sang trái hoặc sang phải.

Một số trạng thái có các hình phạt tiêu cực, trong khi một trạng thái mục tiêu mang lại phần thưởng tích cực.

Lúc đầu, tác nhân di chuyển ngẫu nhiên, thu thập phản hồi.

Theo thời gian, nó cập nhật bảng q bằng phương trình Bellman, tìm hiểu đường đi nào dẫn đến điểm cao hơn.

phần thưởng.

Cuối cùng, tác nhân khám phá ra con đường tối ưu đến mục tiêu bằng cách tuân theo các giá trị Q cao nhất trong mỗi

trạng thái.

Ví dụ đơn giản nhưng mạnh mẽ này cho thấy Q-learning có thể giải quyết các vấn đề ra quyết định tuần tự như thế nào.

Mặc dù Q-learning có hiệu quả trong môi trường nhỏ nhưng nó nhanh chóng trở nên không thực tế khi các vấn đề ngày càng mở rộng.

Bảng Q lưu trữ các giá trị cho từng cặp hành động trạng thái, tăng theo cấp số nhân với trạng thái lớn hơn

không gian, làm cho nó không hiệu quả.

Trong môi trường liên tục, bảng Q trở nên vô hạn và không thể quản lý được.

Hơn nữa, Q-learning thiếu tính khái quát.

Kiến thức về một trạng thái không được chuyển sang các trạng thái tương tự.

Những hạn chế này đã thúc đẩy các nhà nghiên cứu kết hợp Q-learning với mạng lưới thần kinh, dẫn đến

phát triển mạng Q sâu hoặc Dqns, có thể xấp xỉ giá trị Q q cho các môi trường lớn và phức tạp.

Mạng Deep Q hoặc Dqns mở rộng Q-learning bằng cách sử dụng mạng nơ-ron sâu làm công cụ xấp xỉ hàm cho

hàm Q.

Thay vì lưu trữ các giá trị q trong một bảng, mạng lấy trạng thái là đầu vào và đầu ra được ước tính

giá trị q cho mỗi hành động có thể.

Điều này giúp có thể xử lý các không gian trạng thái lớn hoặc liên tục có thể áp đảo các phương pháp truyền thống.

Q-học tập.

Thời điểm đột phá đến vào năm 2015, khi DeepMind sử dụng Dqns để đào tạo các đặc vụ có thể chơi Atari

trò chơi điện tử trực tiếp từ đầu vào pixel thô.

Thuật toán tương tự đã học để làm chủ nhiều trò chơi với hiệu suất siêu phàm.

Đánh dấu một cột mốc quan trọng trong học tập tăng cường sâu, quá trình đào tạo DQN bắt đầu với các trạng thái như

dưới dạng hình ảnh thô từ trò chơi Atari.

Chúng được đưa vào mạng lưới thần kinh sâu, mạng này đưa ra các giá trị Q ước tính cho tất cả các hành động có thể xảy ra.

Tác nhân chọn các hành động bằng cách sử dụng chiến lược tham lam điện tử để cân bằng giữa việc thăm dò và khai thác.

Sau khi thực hiện một hành động, nó sẽ lưu trữ trạng thái trải nghiệm phần thưởng hành động ở trạng thái tiếp theo trong bộ đệm phát lại.

Trong quá trình đào tạo, các mẫu ngẫu nhiên được lấy từ bộ đệm này để cập nhật mạng.

Kỹ thuật này, được gọi là phát lại trải nghiệm, làm giảm mối tương quan giữa các mẫu liên tiếp và ổn định

học qua nhiều lần lặp lại.

DQN hướng tới một chính sách hiệu quả để đảm bảo đào tạo ổn định.

Dqns kết hợp một số kỹ thuật quan trọng.

Bộ đệm phát lại trải nghiệm cho phép tác nhân lấy mẫu các trải nghiệm trong quá khứ, phá vỡ các mối tương quan một cách ngẫu nhiên

giữa các bước liên tiếp.

Mạng mục tiêu được sử dụng để cung cấp các mục tiêu có giá trị q ổn định.

Nó được cập nhật ít thường xuyên hơn mạng chính, ngăn ngừa dao động.

Chiến lược tham lam điện tử đảm bảo việc khám phá bằng cách thỉnh thoảng chọn các hành động ngẫu nhiên, ngăn chặn

đại lý hội tụ quá sớm.

Chính sách dưới mức tối ưu

Cuối cùng, việc chuẩn hóa các trạng thái đầu vào như chia tỷ lệ các giá trị pixel thô sẽ cải thiện hiệu quả học tập.

Những đổi mới này đã giúp Dqns đủ mạnh để đạt được hiệu suất ngang bằng con người trong các môi trường phức tạp.

Mạng Deep Q đã được áp dụng vượt xa các trò chơi Atari trong lĩnh vực robot.

Chúng được sử dụng cho các nhiệm vụ điều hướng và thao tác, cho phép robot học các chuyển động từ quá trình thử nghiệm.

và sai sót trong quản lý tài nguyên.

Dqns tối ưu hóa việc sử dụng máy chủ hoặc định tuyến lưu lượng mạng trong hệ thống đám mây.

Chúng cũng được sử dụng trong tài chính, nơi các đại lý mô phỏng các chiến lược giao dịch trong điều kiện không chắc chắn.

Khả năng thích ứng của dqns khiến chúng trở thành một công cụ có giá trị ở bất cứ nơi nào cần đưa ra quyết định tuần tự,

đặc biệt là trong môi trường quá phức tạp đối với Q-learning truyền thống.

Tóm lại, Q-learning là một thuật toán RL nền tảng để học các chiến lược tối ưu bằng cách ước tính

Giá trị Q thông qua cập nhật Bellman.

Mặc dù hiệu quả trong môi trường nhỏ nhưng nó lại gặp khó khăn với không gian trạng thái lớn hoặc liên tục.

Mạng Q sâu khắc phục hạn chế này bằng cách sử dụng mạng thần kinh để ước tính các giá trị Q, cho phép

Đặc vụ RL để thành công trong các nhiệm vụ phức tạp như chơi trò chơi Atari.

Các kỹ thuật ổn định như trải nghiệm, phát lại và mạng mục tiêu là những đổi mới quan trọng trong việc tạo ra

Dqns đáng tin cậy.

Trong phần tiếp theo, chúng ta sẽ vượt ra ngoài các phương pháp dựa trên giá trị và khám phá các phương pháp tiếp cận độ dốc chính sách,

trực tiếp tối ưu hóa các chính sách để có tính linh hoạt cao hơn nữa.