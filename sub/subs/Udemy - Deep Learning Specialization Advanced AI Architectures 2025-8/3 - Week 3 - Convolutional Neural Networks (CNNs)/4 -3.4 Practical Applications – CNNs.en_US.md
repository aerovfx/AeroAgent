# 4 -3.4 Ứng dụng thực tế – CNNs.en US

---

WEBVTT

Mạng lưới thần kinh tích chập là xương sống của thị giác máy tính hiện đại.

Khả năng tìm hiểu các đặc điểm phân cấp từ hình ảnh khiến chúng trở nên lý tưởng cho nhiều thế giới thực

ứng dụng.

Ba nhiệm vụ phổ biến nhất mà CNN tỏa sáng là phân loại hình ảnh, phát hiện đối tượng và

sự phân đoạn.

Những ứng dụng này trải rộng trên các ngành công nghiệp từ chăm sóc sức khỏe đến lái xe tự động, nông nghiệp, bán lẻ và

an ninh.

Cho dù đó là nhận diện khuôn mặt trong ảnh selfie, phát hiện người đi bộ trên đường hay phân chia khối u

trong quét y tế, CNN đã trở thành cầu nối giữa nghiên cứu và các giải pháp AI thực tế.

Hiểu được những ứng dụng này không chỉ cho thấy sức mạnh của CNN mà còn cho thấy lý thuyết

chuyển thành công nghệ có tác động mạnh mẽ đến thế giới thực.

Phân loại hình ảnh là nhiệm vụ cơ bản và nền tảng nhất trong thị giác máy tính.

Mục tiêu đơn giản là gán nhãn cho toàn bộ hình ảnh.

Ví dụ: với một bức ảnh, CNN có thể phân loại nó là chó, mèo hoặc ô tô.

CNN đã đạt được điều này bằng cách đầu tiên trích xuất các đặc điểm bằng cách sử dụng các lớp tích chập và gộp, sau đó chuyển qua

các tính năng này thành các lớp được kết nối đầy đủ để thực hiện phân loại cuối cùng.

Phân loại hình ảnh là điểm khởi đầu cho nhiều tác vụ nâng cao, làm cơ sở cho việc nhận dạng

hệ thống.

Mặc dù có vẻ đơn giản nhưng việc phân loại hình ảnh chính xác là nền tảng cho nhận dạng khuôn mặt,

phân tích hình ảnh và vô số ứng dụng khác.

Phân loại hình ảnh có nhiều ứng dụng thực tế trong chăm sóc sức khỏe.

CNN được đào tạo về quét y tế có thể phân loại hình ảnh để phát hiện các bệnh như viêm phổi ở chụp X-quang ngực

hoặc bệnh võng mạc tiểu đường khi quét mắt.

Trong các mô hình phân loại bán lẻ tự động phân loại sản phẩm, tinh giản, quản lý hàng tồn kho,

và mua sắm trực tuyến.

Trong nông nghiệp, nông dân sử dụng bộ phân loại dựa trên CNN để xác định sớm bệnh cây trồng, cải thiện năng suất cây trồng

lợi nhuận và giảm tổn thất.

Trong lĩnh vực bảo mật, hệ thống nhận dạng khuôn mặt sẽ phân loại khuôn mặt để nhận dạng cá nhân.

Cung cấp năng lượng cho các ứng dụng như xác thực và giám sát điện thoại thông minh.

Những ví dụ này cho thấy một khái niệm đơn giản gán nhãn cho hình ảnh có thể biến đổi các ngành như thế nào khi

được cung cấp bởi CNN.

Phát hiện đối tượng vượt xa sự phân loại.

Thay vì dán nhãn toàn bộ hình ảnh bằng một danh mục duy nhất, tính năng phát hiện đối tượng sẽ xác định và định vị

nhiều đối tượng trong ảnh.

Đầu ra bao gồm các hộp giới hạn xung quanh từng đối tượng được phát hiện cùng với nhãn của chúng.

Ví dụ: trong cảnh đường phố, tính năng phát hiện đối tượng có thể xác định ô tô, người đi bộ và đèn giao thông

đồng thời.

Điều này đòi hỏi cả phân loại và bản địa hóa, khiến nó trở thành một nhiệm vụ phức tạp hơn so với phân loại đơn giản.

Các mô hình phát hiện phổ biến bao gồm họ Rcnn, tiên phong trong việc phát hiện dựa trên khu vực.

YOLO bạn chỉ nhìn một lần được biết đến về hiệu suất thời gian thực và trình phát hiện bắn một lần SSD, cân bằng

tốc độ và độ chính xác.

Phát hiện đối tượng là nền tảng cốt lõi cho các ứng dụng như xe tự lái và giám sát video.

Phát hiện đối tượng có một số ứng dụng thực tế rõ ràng nhất trong xe tự hành.

Mô hình phát hiện xác định người đi bộ, xe ô tô khác, vạch kẻ làn đường và biển báo giao thông để đảm bảo an toàn

điều hướng trong các cửa hàng bán lẻ, thanh toán miễn phí như Amazon Go.

Sử dụng tính năng phát hiện đối tượng để theo dõi sản phẩm khách hàng nhận, cho phép thanh toán tự động trong giám sát an ninh

hệ thống, phát hiện và theo dõi các hoạt động đáng ngờ, giúp ngăn chặn các mối đe dọa trong thời gian thực.

Phát hiện vật thể cũng được sử dụng trong nghiên cứu động vật hoang dã, trong đó CNN tự động phát hiện và đếm động vật

trong các cảnh quay bẫy camera, hỗ trợ các nỗ lực bảo tồn.

Những ứng dụng này nêu bật cách phát hiện đối tượng đang định hình các ngành công nghiệp từ vận tải

đến thương mại và khoa học môi trường.

Phân đoạn hình ảnh là hình thức hiểu hình ảnh chi tiết nhất.

Thay vì gán nhãn cho toàn bộ hình ảnh hoặc vẽ các hộp giới hạn, phân đoạn sẽ phân loại

từng pixel trong ảnh.

Điều này tạo ra các kết quả đầu ra chi tiết, chi tiết giúp mô tả chính xác các đối tượng.

Có hai loại chính.

Phân đoạn ngữ nghĩa, trong đó tất cả các pixel của một lớp được nhóm lại với nhau, ví dụ: tất cả các xe được dán nhãn là

phân đoạn tức thời ô tô và cá thể để phân tách các đối tượng riêng lẻ của cùng một lớp, ví dụ: nhiều

xe được dán nhãn riêng.

Các mẫu phổ biến bao gồm unet, được sử dụng rộng rãi trong hình ảnh y tế và mặt nạ R-cnn, kết hợp khả năng phát hiện

và phân đoạn.

Phân đoạn hình ảnh là điều cần thiết cho các tác vụ trong đó chi tiết mức pixel là rất quan trọng.

Phân đoạn hình ảnh có các ứng dụng mang tính biến đổi trong các lĩnh vực mà mức độ chi tiết của pixel là quan trọng.

Trong chăm sóc sức khỏe, mô hình phân đoạn xác định các khối u, cơ quan hoặc các điểm bất thường trong quét MRI và CT,

giúp các bác sĩ chẩn đoán và lập kế hoạch phẫu thuật trong việc lập bản đồ phân đoạn lái xe tự động,

làn đường, vỉa hè và người đi bộ, cung cấp cho các phương tiện hiểu biết chi tiết về môi trường của họ.

Trong nông nghiệp, việc phân đoạn giúp lập bản đồ ranh giới cánh đồng trồng trọt.

Theo dõi sự phát triển và phát hiện cỏ dại trong AR và VR.

Phân đoạn cho phép loại bỏ nền và tách đối tượng, làm cho lớp phủ ảo trở nên thực tế hơn.

Các ứng dụng này cho thấy cách phân đoạn đưa thị giác máy tính vượt ra ngoài khả năng nhận dạng thành ngữ cảnh chính xác

nhận thức được sự hiểu biết về hình ảnh.

Phân loại hình ảnh, phát hiện đối tượng và phân đoạn hình ảnh thể hiện mức độ phức tạp ngày càng tăng

trong thị giác máy tính.

Phân loại gán một nhãn duy nhất cho toàn bộ hình ảnh.

Khả năng phát hiện còn tiến xa hơn bằng cách định vị và dán nhãn cho nhiều đối tượng bằng cách sử dụng các hộp giới hạn.

Phân đoạn cung cấp độ chi tiết cao nhất bằng cách phân loại từng pixel, tạo ra một cảnh hoàn chỉnh.

Hiểu biết.

Mỗi nhiệm vụ đều có điểm mạnh và ứng dụng riêng.

Phân loại nhanh chóng và hiệu quả khi một đối tượng duy nhất thống trị hình ảnh, Việc phát hiện là điều cần thiết

khi nhiều đối tượng phải được định vị và xác định.

Phân đoạn là rất quan trọng khi cần có ranh giới chính xác.

Cùng với nhau, ba nhiệm vụ này tạo thành nền tảng của các ứng dụng thị giác máy tính hiện đại.

Các ứng dụng thực tế của CNN là

Chuyển đổi các ngành công nghiệp trên toàn thế giới.

Trong lĩnh vực chăm sóc sức khỏe, phân loại và phân khúc được hỗ trợ bởi CNN.

Cải thiện độ chính xác chẩn đoán và giảm khối lượng công việc cho bác sĩ.

Trong hệ thống lái xe tự động phát hiện và phân chia năng lượng ô tô giúp đường đi an toàn hơn.

Trong nông nghiệp, việc phân loại và phân khúc sẽ tối ưu hóa việc quản lý cây trồng, giảm chất thải và tăng

năng suất.

Trong bán lẻ, tính năng phát hiện đối tượng cho phép trải nghiệm thanh toán miễn phí nhằm nâng cao sự thuận tiện cho khách hàng.

Trong các hệ thống giám sát an ninh được cung cấp bởi CNN, sẽ giám sát môi trường và phát hiện các mối đe dọa.

Cùng với nhau, những ứng dụng này đại diện cho ngành công nghiệp trị giá 1 tỷ USD được xây dựng trên AI thị giác máy tính.

Thúc đẩy hiệu quả, an toàn và đổi mới trên hầu hết mọi lĩnh vực.

Để tóm tắt ba nhiệm vụ thị giác máy tính cốt lõi của CNN.

Phân loại, phát hiện và phân đoạn.

Phân loại gán một nhãn duy nhất cho một hình ảnh.

Tính năng phát hiện định vị và gắn nhãn cho nhiều đối tượng, đồng thời phân đoạn sẽ phân loại từng pixel để có được độ phân giải chi tiết

sự hiểu biết cảnh.

Những nhiệm vụ này hỗ trợ các ứng dụng trong thế giới thực về chăm sóc sức khỏe, xe tự hành, nông nghiệp, bán lẻ,

và hơn thế nữa.

Từ việc phát hiện bệnh tật trong quá trình quét y tế đến hướng dẫn ô tô tự lái và lập bản đồ cánh đồng cây trồng, CNN

đang là trung tâm của những đột phá về AI.

Hiểu những ứng dụng này không chỉ thể hiện tác động của CNN mà còn chuẩn bị cho chúng ta

áp dụng deep learning vào các vấn đề trong thế giới thực theo những cách có ý nghĩa và mang tính thay đổi.