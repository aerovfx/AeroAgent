# 1 -3.1 CNN Fundamentals.en Hoa Kỳ

---

WEBVTT

Mạng nơ-ron tích chập, hay CNN, là mạng nơ-ron chuyên dụng được thiết kế để xử lý hình ảnh

và dữ liệu không gian.

Không giống như các mạng được kết nối đầy đủ, nơi mọi đầu vào đều kết nối với mọi nơ-ron, CNN dựa vào tích chập

các lớp tập trung vào các vùng nhỏ, cục bộ của đầu vào.

Điều này làm cho chúng cực kỳ hiệu quả trong việc nắm bắt các mẫu cục bộ như các cạnh, kết cấu,

và hình dạng.

Các đặc điểm cục bộ này sau đó được kết hợp giữa các lớp để nhận dạng các mẫu phức tạp hơn như khuôn mặt hoặc

đồ vật.

CNN là xương sống của thị giác máy tính hiện đại, hỗ trợ các ứng dụng như nhận dạng khuôn mặt,

hình ảnh y tế, lái xe tự động, v.v.

Bằng cách hiểu về tích chập, gộp, đệm và bước tiến, chúng ta sẽ khám phá ra các cơ chế tạo ra

CNN rất mạnh mẽ và có thể mở rộng.

Tích chập là hoạt động chính trong CNN.

NS.

Nó liên quan đến việc áp dụng một bộ lọc, còn được gọi là kernel, trượt qua hình ảnh đầu vào để phát hiện

các mẫu.

Mỗi bộ lọc tập trung vào một vùng nhỏ của đầu vào, nhân giá trị của nó với trọng số của bộ lọc và

tổng hợp chúng lại.

Kết quả là một biểu diễn mới được gọi là bản đồ đặc trưng.

Ví dụ: một bộ lọc có thể phát hiện các cạnh dọc, một đường ngang khác và một kết cấu khác.

Không giống như các mạng được kết nối đầy đủ, tích chập không yêu cầu mọi pixel đầu vào phải kết nối với mọi

tế bào thần kinh.

Điều này làm giảm số lượng tham số và cho phép mạng tập trung vào các cấu trúc cục bộ có ý nghĩa.

Tích chập làm cho CNN hiệu quả hơn và có khả năng mở rộng hơn nhiều so với các mạng dày đặc truyền thống, đặc biệt là

khi xử lý dữ liệu nhiều chiều như hình ảnh.

Trong các mạng tích chập, các bộ lọc là các ma trận nhỏ, thường là 3 x 3 hoặc 5 x 5, quét qua

đầu vào.

Mỗi bộ lọc được đào tạo để phát hiện một tính năng cụ thể.

Ví dụ: một bộ lọc có thể làm nổi bật các cạnh dọc trong khi bộ lọc khác xác định các đường ngang.

Khi được áp dụng, mỗi bộ lọc sẽ tạo ra một bản đồ đặc điểm thể hiện sự hiện diện và sức mạnh của đặc điểm đó

trên toàn bộ đầu vào.

Bằng cách xếp chồng nhiều bộ lọc, mạng tạo ra nhiều bản đồ đặc trưng, thu thập nhiều loại

của các mẫu.

Khi chúng tôi đi sâu hơn vào mạng, các bản đồ tính năng này kết hợp với nhau để tạo thành các biểu diễn trừu tượng và phức tạp hơn,

cuối cùng nhận ra toàn bộ đối tượng.

Việc trích xuất tính năng phân cấp này là điều mang lại cho CNN sức mạnh vượt trội trong các nhiệm vụ tầm nhìn.

Các lớp tổng hợp đóng một vai trò quan trọng trong CNN bằng cách giảm kích thước của bản đồ đặc trưng trong khi vẫn giữ lại các lớp thiết yếu.

thông tin.

Ý tưởng là tóm tắt các vùng của bản đồ đặc trưng để mạng trở nên có tính tính toán cao hơn.

hiệu quả và ít nhạy cảm hơn với những thay đổi nhỏ hoặc biến dạng ở đầu vào.

Hình thức phổ biến nhất là gộp tối đa, trong đó chúng tôi lấy giá trị tối đa từ mỗi khu vực một cách hiệu quả.

giữ được đặc điểm nổi trội nhất.

Một cách tiếp cận khác là gộp trung bình, trong đó chúng tôi lấy giá trị trung bình.

Việc gộp nhóm làm giảm tính chiều của dữ liệu trong khi vẫn bảo toàn các tín hiệu quan trọng, cho phép mạng

để tập trung vào các mẫu chính mà không làm nó quá tải với những chi tiết không cần thiết.

Điều này không chỉ tăng tốc độ đào tạo mà còn cải thiện khả năng khái quát hóa.

Việc gộp chung có thể được thực hiện theo nhiều cách khác nhau, trong đó gộp chung tối đa và gộp chung trung bình là phổ biến nhất.

Tổng hợp tối đa lấy giá trị tối đa từ mỗi cửa sổ tổng hợp, giữ kích hoạt mạnh nhất trong đó

khu vực.

Điều này làm cho nó trở nên tuyệt vời trong việc phát hiện các đặc điểm mạnh như cạnh hoặc góc, bất kể các chi tiết nhỏ.

Các biến thể.

Ngược lại, việc gộp trung bình sẽ tính giá trị trung bình của tất cả các giá trị trong cửa sổ, tạo ra kết quả mượt mà hơn.

các đại diện.

Trong khi việc gộp trung bình phổ biến hơn trong các mạng trước đó, việc gộp tối đa đã trở thành lựa chọn chủ yếu

bởi vì nó bảo tồn các tính năng phù hợp nhất trong khi bỏ qua các tín hiệu yếu hơn.

Tuy nhiên, trong một số trường hợp, việc gộp nhóm trung bình vẫn được sử dụng, đặc biệt là ở các lớp gộp chung toàn cầu,

nơi làm mịn tổng thể là có lợi.

Phần đệm đề cập đến việc thêm các đường viền bổ sung, thường là các số 0 xung quanh hình ảnh đầu vào trước khi áp dụng tích chập.

Nếu không có phần đệm, các bộ lọc không thể che phủ hoàn toàn các cạnh của hình ảnh, khiến bản đồ đối tượng bị thu nhỏ

ở mỗi lớp.

Điều này có thể dẫn đến mất thông tin biên quan trọng và giảm kích thước không gian quá nhanh bằng cách thêm

đệm.

Chúng tôi cho phép các bộ lọc trượt qua các pixel viền và giữ nguyên kích thước đầu vào ban đầu.

Điều này thường được gọi là phần đệm giống nhau vì kích thước đầu ra khớp với kích thước đầu vào.

Phần đệm đặc biệt quan trọng khi xếp chồng nhiều lớp chập, vì nó ngăn cản tính năng

bản đồ từ thu nhỏ lại thành không có gì.

Nói tóm lại, phần đệm đảm bảo rằng tất cả các phần của đầu vào, bao gồm cả các cạnh, đều góp phần vào việc học

bước tiến và tích chập đề cập đến số bước bộ lọc di chuyển qua đầu vào với một bước tiến

của một.

Bộ lọc thay đổi từng pixel một, tạo ra các bản đồ đặc trưng chi tiết chỉ nhỏ hơn một chút

hơn đầu vào.

Với bước tiến lớn hơn, chẳng hạn như hai, bộ lọc sẽ bỏ qua các pixel, tạo ra bản đồ đặc trưng nhỏ hơn.

Điều này tăng tốc độ tính toán nhưng có thể làm mất đi các chi tiết chi tiết.

Do đó, các bước tiến là sự đánh đổi giữa việc nắm bắt chi tiết và duy trì hiệu quả.

Trong thực tế, sải bước một phổ biến nhất ở các lớp đầu tiên, nơi mà các chi tiết rất quan trọng.

Những bước tiến lớn hơn thường được sử dụng sau này trong mạng, nơi chúng tôi quan tâm nhiều hơn đến các mẫu cấp cao hơn là

chi tiết nhỏ.

Bây giờ hãy đặt tất cả các khái niệm này lại với nhau.

Các lớp tích chập trích xuất các đặc điểm cục bộ như các cạnh, kết cấu và hình dạng bằng các bộ lọc.

Các lớp gộp làm giảm kích thước không gian của các bản đồ đặc trưng này, làm cho mạng hiệu quả hơn và

ít nhạy cảm hơn với những thay đổi nhỏ.

Phần đệm đảm bảo rằng thông tin cạnh quan trọng không bị mất.

Giữ nguyên kích thước ban đầu khi cần thiết.

Các bước kiểm soát tốc độ thu nhỏ kích thước không gian của bản đồ đối tượng, cân bằng việc chụp chi tiết với

hiệu quả tính toán.

Cùng với nhau, các khối xây dựng này tạo thành nền tảng của mạng lưới thần kinh tích chập.

Bằng cách kết hợp chúng một cách chiến lược, CNN biến những hình ảnh thô thành những hình ảnh đại diện mạnh mẽ cho phép

các tác vụ như phát hiện đối tượng, nhận dạng khuôn mặt và phân loại hình ảnh để xem các nguyên tắc cơ bản của CNN trong

hành động.

Hãy xem xét ví dụ về nhận dạng chữ số viết tay, chẳng hạn như tập dữ liệu mNIST.

Hình ảnh đầu vào của một chữ số như số năm đầu tiên đi qua các lớp chập, nơi các bộ lọc phát hiện

các cạnh và đường cong nhỏ ở các lớp sâu hơn.

Mạng kết hợp các cạnh này thành các hình dạng như vòng hoặc đường đặc trưng của một số

chữ số.

Các lớp gộp làm giảm độ phức tạp bằng cách giữ lại các tính năng quan trọng nhất trong khi loại bỏ các tính năng không liên quan

chi tiết.

Cuối cùng, sau một số giai đoạn tích chập và gộp chung, các tính năng được xử lý sẽ đưa vào một kết nối đầy đủ

lớp phân loại chữ số thành một trong mười loại từ 0 đến 9.

Quá trình trích xuất tính năng từng bước này là điều cho phép CNN vượt trội hơn so với học máy truyền thống

các phương pháp xử lý dữ liệu ảnh

Tóm lại, các nguyên tắc cơ bản của CNN xoay quanh một số hoạt động chính.

Tích chập trích xuất các tính năng bằng cách sử dụng các bộ lọc phát hiện các mẫu như cạnh và kết cấu.

Các bộ lọc này tạo ra các bản đồ đặc trưng thể hiện sức mạnh của các đặc điểm khác nhau trên hình ảnh.

Việc gộp chung làm giảm kích thước của bản đồ đặc trưng, ​​giúp việc đào tạo hiệu quả hơn và ít nhạy cảm hơn với tiếng ồn.

Phần đệm đảm bảo rằng các cạnh không bị bỏ qua và bản đồ đối tượng không bị co lại quá nhanh.

Các bước kiểm soát tốc độ thu nhỏ bản đồ đối tượng, cân bằng độ phân giải và hiệu quả.

Cùng với nhau, những phần tử này tạo thành các khối xây dựng thiết yếu của mạng lưới thần kinh tích chập.

Nắm vững chúng là bước đầu tiên để hiểu các kiến trúc CNN nâng cao hơn như ResNet,

VGG và E Efficiencynet, được xây dựng dựa trên những nguyên tắc cơ bản này để đạt được hiệu suất hiện đại trong

thị giác máy tính.