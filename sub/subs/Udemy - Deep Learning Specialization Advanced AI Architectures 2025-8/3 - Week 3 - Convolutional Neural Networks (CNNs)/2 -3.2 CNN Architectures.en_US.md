# 2 -3.2 CNN Architectures.en Hoa Kỳ

---

WEBVTT

Lịch sử của kiến trúc CNN phản ánh sự phát triển của chính deep learning, bắt đầu từ rất sớm

mô hình như LeNet, đã giới thiệu ý tưởng về các lớp tích chập để nhận dạng chữ số, CNN đã

phát triển sâu hơn và phức tạp hơn với những đột phá về thời gian như AlexNet, VGG và ResNet được thúc đẩy bởi

Thử thách nhận dạng hình ảnh quy mô lớn của ImageNet, thúc đẩy các nhà nghiên cứu tạo ra chính xác hơn

và kiến trúc hiệu quả.

Mỗi mô hình đều đưa ra những cải tiến như lớp sâu hơn, chức năng kích hoạt mới hoặc bỏ qua kết nối

đã giúp giải quyết những thách thức trong việc đào tạo mạng lưới sâu hơn.

Các kiến trúc gần đây hơn như Effientnet tập trung vào việc tối ưu hóa không chỉ chiều sâu mà còn cả chiều rộng và

độ phân giải cao hơn, giúp CNN sử dụng tài nguyên hiệu quả hơn.

Cùng với nhau, những kiến trúc này đã mở đường cho các ứng dụng thị giác máy tính hiện đại.

lái xe đến hình ảnh y tế.

Linette, được Yann LeCun giới thiệu vào năm 1998, là một trong những mạng nơ-ron tích chập sớm nhất và

thường được coi là người tiên phong của kiến trúc CNN.

Nó được thiết kế để nhận dạng các chữ số viết tay trong tập dữ liệu mNIST, điều này đã tạo nên một bước đột phá

để số hóa séc ngân hàng.

Kiến trúc bao gồm các lớp tích chập để trích xuất các tính năng, các lớp tổng hợp để giảm kích thước,

và các lớp được kết nối đầy đủ để phân loại.

Mặc dù nhỏ theo tiêu chuẩn ngày nay chỉ với một vài lớp, Linette đã chứng minh được sức mạnh của phép tích chập

các kiến trúc để nhận dạng hình ảnh.

Nó đặt nền móng cho tất cả các CNN trong tương lai, chứng minh rằng deep learning có thể vượt trội hơn truyền thống

phương pháp học máy đối với các tác vụ hình ảnh.

AlexNet đánh dấu một bước ngoặt trong lịch sử deep learning, được Alex Krizhevsky giới thiệu trong Krizhevsky.

Vào năm 2012, nó đã giành được chiến thắng ngoạn mục trong cuộc thi ImageNet, vượt trội hơn tất cả các phương pháp khác.

một biên độ rộng.

AlexNet có tám lớp, bao gồm năm lớp tích chập và ba lớp được kết nối đầy đủ.

Một trong những cải tiến quan trọng của nó là việc sử dụng chức năng kích hoạt ReLU, giúp giải quyết vấn đề biến mất

vấn đề về độ dốc và tăng tốc độ đào tạo so với các hàm sigmoid hoặc thiếu niên cũ hơn.

Một đóng góp quan trọng khác là tận dụng GPU, giúp đào tạo các mạng lớn trong một

thời gian hợp lý.

Thành công của AlexNet đã chứng minh rằng mạng tích chập sâu có thể mở rộng quy mô một cách hiệu quả, tạo ra một

làn sóng nghiên cứu và mở ra kỷ nguyên học sâu hiện đại.

Mạng VGG, được phát triển bởi Oxford Visual Geometry Group vào năm 2014, cho thấy tầm quan trọng của độ sâu

trong các mạng tích chập.

Không giống như các bộ lọc lớn hơn của Alex, VGG sử dụng các bộ lọc nhỏ ba x ba nhưng xếp chồng nhiều bộ lọc lại với nhau.

Điều này cho phép mạng sâu hơn 16 hoặc thậm chí 19 lớp mà không tăng đáng kể khả năng tính toán

chi phí.

Việc sử dụng các bộ lọc nhỏ hơn cũng giúp ghi lại các chi tiết hình ảnh mịn hơn trong khi vẫn giữ được độ phân giải không gian.

Mặc dù đơn giản nhưng VGG vẫn đạt được độ chính xác vượt trội trên mạng hình ảnh và trở thành một trong những mạng được sử dụng rộng rãi nhất.

sử dụng các trích xuất đặc trưng trong thị giác máy tính.

Thiết kế đơn giản của nó đã khiến nó trở thành một mô hình chuẩn, thường được sử dụng làm nền tảng cho việc học chuyển tiếp

trong nhiều ứng dụng thực tế.

Mặc dù mạng VGG có độ chính xác cao nhưng chúng cũng gặp phải những thách thức đáng kể.

Vấn đề lớn nhất là số lượng tham số khổng lồ, khoảng 138 triệu trong VGG 16.

Điều này khiến việc đào tạo trở nên cực kỳ tốn kém về mặt tính toán và yêu cầu GPU mạnh mẽ ngay cả sau khi đào tạo.

Chạy suy luận trên các mô hình lớn như vậy tốn rất nhiều tài nguyên.

Làm cho chúng ít thực tế hơn đối với các ứng dụng thời gian thực hoặc di động.

Những hạn chế này nêu bật sự cần thiết của các kiến trúc hiệu quả hơn có thể mang lại cùng một

hoặc độ chính xác tốt hơn, với ít tham số hơn.

Điều này đã mở đường cho các mô hình mới hơn như ResNet và Effielnet, không chỉ tập trung vào độ chính xác

mà còn về khả năng mở rộng và hiệu quả.

ResNet, viết tắt của Residual Network, được Microsoft Research giới thiệu vào năm 2015 và đã tạo nên một cuộc cách mạng

học sâu.

Sự đổi mới chính của nó là sự ra đời của các kết nối bỏ qua, còn được gọi là kết nối dư.

Các kết nối này cho phép đầu vào của một lớp bỏ qua một hoặc nhiều lớp và được thêm trực tiếp vào

đầu ra.

Thay đổi đơn giản này đã giải quyết được vấn đề độ dốc biến mất gây khó khăn cho các mạng rất sâu, cho phép

đào tạo các mô hình với 50, 101 hoặc thậm chí 152 lớp.

ResNet đã đạt được hiệu suất phá kỷ lục trên ImageNet và trở thành tiêu chuẩn vàng mới cho CNN.

Kiến trúc của nó đã chứng minh rằng các mạng sâu hơn có thể hoạt động tốt hơn, miễn là chúng có quyền

đổi mới cơ cấu để hỗ trợ sự ổn định đào tạo.

ResNet về cơ bản đã thay đổi cách thiết kế mạng sâu.

Trước khi đào tạo ResNet, các mạng rất sâu thường dẫn đến hiệu suất kém hơn do độ dốc biến mất

với việc bỏ qua các kết nối, độ dốc có thể chảy trực tiếp qua các lớp, giúp đào tạo mạng

với hàng trăm lớp.

Điều này không chỉ cải thiện độ ổn định trong luyện tập mà còn cả độ chính xác.

ResNet đã giảm lỗi trên ImageNet xuống mức không thể đạt được trước đây và trở thành xương sống cho nhiều

các mô hình xuôi dòng trong việc phát hiện, phân đoạn và nhận dạng.

Ngày nay, các khối ResNet vẫn được sử dụng rộng rãi làm khối xây dựng trong các kiến trúc hiện đại, nhấn mạnh

tác động lâu dài của sự đổi mới này.

E Efficiencynet, được Google Brain giới thiệu vào năm 2019, đã áp dụng một cách tiếp cận khác.

Thay vì chỉ xếp chồng nhiều lớp hơn hoặc làm cho bộ lọc lớn hơn, nó tập trung vào việc mở rộng quy mô mạng theo cách

cách cân bằng.

Hiệu quảnet đã giới thiệu Chia tỷ lệ phức hợp, giúp tối ưu hóa đồng thời độ sâu, chiều rộng và độ phân giải đầu vào.

Điều này cho phép nó đạt được độ chính xác hiện đại với ít thông số hơn nhiều so với các mẫu cũ

như VGG hoặc ResNet.

Trên thực tế, Effientnet mang lại hiệu suất tốt hơn đồng thời hiệu quả về mặt tính toán hơn nhiều,

làm cho nó phù hợp không chỉ cho nghiên cứu mà còn cho các ứng dụng trong thế giới thực trên thiết bị di động và thiết bị biên.

Mạng hiệu quả nhấn mạnh rằng thiết kế và mở rộng quy mô thông minh hơn có thể hoạt động tốt hơn độ sâu lực lượng vũ phu.

Mỗi kiến ​​trúc CNN mà chúng tôi nghiên cứu đều đóng một vai trò quan trọng trong việc phát triển lĩnh vực này.

Linette đã đi tiên phong trong việc sử dụng phép tích chập và gộp, chứng tỏ CNN đã giải quyết được các vấn đề thực sự.

AlexNet đánh dấu thời điểm đột phá bằng việc giành được ImageNet với mô hình hỗ trợ GPU lớn sử dụng

Kích hoạt ReLU.

VGG đã chứng minh rằng độ sâu rất quan trọng, giúp mạng lưới sâu hơn đạt được độ chính xác cao.

ResNet đã khắc phục được vấn đề biến mất độ dốc, mở khóa tiềm năng của các kiến ​​trúc rất sâu.

Mạng hiệu quả được xây dựng dựa trên những tiến bộ này, cho thấy rằng việc mở rộng quy mô cân bằng, cẩn thận có thể đạt được kỷ lục

hiệu suất với ít tài nguyên hơn.

Cùng với nhau, những kiến trúc này tạo thành xương sống của thị giác máy tính hiện đại và nhiều công nghệ tiên tiến.

các mô hình ngày nay được xây dựng dựa trên các nguyên tắc được họ giới thiệu lần đầu tiên.

Tóm lại, sự phát triển của kiến ​​trúc CNN là một hành trình đổi mới.

Lanette giới thiệu khái niệm này.

AlexNet đã chứng minh được sức mạnh của mình, VGG cho thấy giá trị của chiều sâu, ResNet cho phép các mạng rất sâu và hiệu quả

hiệu quả tối ưu hóa ròng với quy mô cân bằng.

Mỗi kiến trúc giải quyết được những thách thức riêng, từ độ dốc biến mất đến giới hạn tính toán, mở đường

con đường cho các mô hình tầm nhìn mạnh mẽ và thực tế hơn.

Ngày nay, những kiến trúc này tạo thành nền tảng cho nhiều ứng dụng tiên tiến, bao gồm cả đối tượng

phát hiện, phân đoạn hình ảnh và xe tự lái.

Hiểu các nguyên tắc thiết kế của họ không chỉ cung cấp bối cảnh lịch sử mà còn trang bị cho chúng ta

những hiểu biết cần thiết để xây dựng và áp dụng các mô hình học sâu hiện đại một cách hiệu quả.