# 2. What is the difference Parameters, Architecture, Pretraining size & more

---

In this video we will talk about the differences between all these models.

So why we don't have just one model and one model rules them all.

And to be honest, right now it seems just a little bit that one model can rule them all.

But we have a lot of different models.

And so I want to tell you what the differences are.

So this right here is a website from meta meta meta lm Lama two.

And they have a quick and nice explanation because we have the model size.

And this right here stands for 7 billion parameters, 13 billion parameters 70 billion parameters.

So one thing is the parameters.

And I want to explain you really really quick what parameters are.

Think of parameters.

This way.

If you look for example at a cat and you see that the cat is gray, that is maybe one parameter, but

you also see that the cat stands in the wood.

That's a second parameter.

You may be also see that the teeth of the cat are white.

That's a third parameter.

You see that the hair of this cat are fluffy.

That's a fourth parameter.

So parameters are simply point of views, angles of views.

So if a model has 70 billion parameters, they use 70 billion different point of views to look at the

problems or to look or to search for solutions.

That's the simplest explanation of parameters.

Besides the parameters, we have also other stuff.

So all of these LMS are of course pre-trained.

You know that.

So GPT stands for generative pre-trained transformer.

You know we use the transformer architecture to train this model.

And all of this is pre-trained trained.

And of course this is important.

So the important stuff is of course the architecture.

So how these models are trained every model is trained a little bit different.

I already told you we have this Mistral Mistral.

And this model is trained completely different than most of the other models.

More on that of course.

Later.

And in this architecture we have also of course the pre-training tokens.

So the size of this whole thing on how much data this model is trained.

And so this model is trained, for example, on 2 trillion tokens that are roughly 1.5 trillion words.

So that's a lot.

So we have 2 trillion tokens.

And the best Lama model looks with 70 billion different points of view on this training tokens.

And that's why Lamas are relatively good model.

And we have of course also more because the context length is really, really important.

So how long the model understands about what we talk and also how long the model can give an output

that is relevant for the input.

So after 4096 tokens, Lama two doesn't really get what we talk about anymore.

And you know, all of this is different with every single LM.

So every single LM has a different parameter size.

Every single LM has a different context length, has different pre-trained tokens and has a little bit

of a different model architecture.

I must admit we don't will do a really, really deep dive in the model architecture because I think

this is boring and you don't get anything out of this, and we don't necessarily need to know on how

much training tokens all these models are trained.

We don't even know exactly on how much tokens.

G.p.t four is trained because GPT four is not open source, GPT four is closed source.

So they don't tell us everything on GPT four.

We don't know exactly how much parameters this thing has.

We also don't know exactly on how much tokens GPT four was trained.

Yes, we have information from different sources, but I don't want to include them because we are not

100% sure if this is true.

So we have parameters.

The parameters are important.

And generally speaking, the more parameters the better.

We have the pre-training tokens.

And also here generally speaking the more the better.

We have the context length.

And here not just generally speaking here is more better.

Why?

I told you that the parameters and.

The training tokens are not always that important or are just generally better because of course the

training tokens.

So the quality of the tokens matter.

If you have 2 trillion -- tokens, 2 trillion, two trillions of input to train the model, that

is not really good.

We won't get a really good model.

And the same thing is with the parameters.

If the model has the false parameter.

So don't good parameters has don't good points of use to look at problems.

All of this doesn't really matter.

And then of course we have more.

So not only these things are important because also the fine tuning and the reinforcement learning is

important.

So you can see all of this is important.

So how good.

Of course the fine tuning and the reinforcement learning is after a good model is also important.

And by the way here are some benchmarks of open source LMS.

They do normal benchmarks.

So of course the higher the better.

They do tests with these models and the best model get the highest points.

We have here a lot of different open source models, for example empty Falcon and then of course also

the llama models.

And they are tested on different stuff, for example on human evael, on natural questions and much,

much more.

And in this specific example, llama two is of course the best open source LM, but we haven't included

Mistral mixture right here because I think this thing is even better.

I have made my own list.

What's also important for a really, really good LM, and I want to show you this because we don't have

to see this all black and white.

There are a lot of different things that we need to consider if we want a good LM.

Of course, the parameter.

So the model size and generally speaking, like I told you, the more the better.

So llama had for example 70 billion.

We have also other models that are bigger and other models that are smaller.

And of course we have models that are smaller and still better than llama two because size is not all

that matters.

Then of course, the architecture.

So how exactly is this model trained?

And like I said, I will tell you a little bit more about the architecture.

As soon as we talk about mixture then the pre-training size, you know this the context length.

And here of course the more the better we have right now GPT four with 128,000 tokens of context length.

And we have cloud two from anthropic with 200,000 tokens of context length.

But this right here is also important.

So the quality of the training data and then of course the fine tuning and the reinforcement learning.

So how good was the fine tuning and how good was the reinforcement learning is also huge for a good

model.

And that's also not all because we also need a good model scalability.

So the ability to scale models up.

So more parameters or down fewer parameters depending on the application without losing effectiveness.

This is really, really important because if you can't scale a model, the model is limited and it can't

get any better.

Things that are also important is like bias and fairness.

And at the end of the course we will talk a bit about this.

The same thing is about safety and robustness.

So you don't want to have models that make dumb inside jokes and that stuff all the time.

You want models that are concise, correct, and not that much biased.

Of course the models also need to be efficient.

This is also really important, so we don't want to have a model with like eight gazillion trillion

billion parameters.

And then the model is unbelievably slow and unbelievably expensive to use.

We don't want that, but we want that.

The model continuously learns new stuff.

And something that's also really, really important is the multi-modal capabilities.

As you know, ChatGPT can see, speak and hear and that are the multimodal capabilities.

Besides that, at least right now, we don't have a lot of llms that are multimodal.

Yes, we have Gemini.

Gemini is one of the best LMS right now that are multimodal.

And I am sure as soon as you see the course, we will have a lot of multimodal LMS.

And of course I will update the course every time if we got something cool.

And it would be also cool if we can integrate these models in different applications.

Relatively easy.

As you saw, OpenAI is perfect.

We can simply copy and paste an API key in nearly every application that we want to make.

And also the fine tuning is relatively simple.

And this right here, I think this is really underappreciated because the user experience is everything.

If people don't use your stuff, you are useless.

So you need a model that people love to use.

If you don't have that, you will not make it.

And the last sentence is right here that I wrote down is the goal is our efficient model.

So the right parameters and the right training data.

With good fine tuning, we want to have a model that is efficient.

So we don't need a model that is unbelievably big, that has the complicated architecture of them all

and the biggest pre-training size.

No, that's not what's really needed.

We need a compact model that's fast, that's cheap, that's accurate, and that works good.

And I would argue that smaller models are more valuable because you can run smaller models also locally

you can in small, really small models also on your handy and on your local PC.

If you have a really small model, you can install it locally.

And this is really, really cool because you don't have to wait until the API calls you back.

So most of the time if you use an LM, of course you make API calls, but if you can install something

locally, we don't have the latency of the API calls.

We can run this instantly and get the answers instantly back like I showed you on ChatGPT mobile.

If we talk with ChatGPT, we need to wait a long time until we get an answer and that is not cool.

If we install an LM locally, a really small LM, this LM can be unbelievably fast and then we have

no latency.

The LM will talk instantly back.

So a multimodal LM that small and that can run on.

Nahendeh would be awesome.

And the smallest Gemini model right now is also out there.

And this model can do this.

So yes, we have a model that can run locally on your handy, and this model will be fast and cheap

to use and has nearly no latency.

This is really, really cool, at least in my mind.

So in this video you have learned that not every model is the same.

Every model has different parameters, a slightly different architecture, a different pre-training

size, different context, length.

Of course, also the quality matters.

Different fine tuning, different reinforcement learning, different scalability has different biases

and fairness.

There are models that are more and less safe, more and less efficient.

Some models can and will learn better in the future than others.

Not every model is multimodal.

The integration is different and of course also the user experience.

So that's why we have such a large amount of LMS, because everyone wants to make the best one and they

play with all of these.

They try to make the best solution.

So you have simply learned that there are a lot of different LMS, and every LMS tries to become the

best LM, and they try this via different architecture and via different training data and parameters.

And that's basically it.

And in the next sections we will make a small deep dive into the best LMS, because I think you should

also see the other LMS and not just ChatGPT.

Some of them are open source and completely free to use.

So I think this is cool.

And maybe someday another LM is a lot better than GPT.

You can't really know this.

Yes, the bias right now, at least from me, goes to OpenAI.

I think they are awesome.

But other companies are also great.