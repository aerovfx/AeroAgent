# 6. Embeddings and Vector Databases for RAG A Detailed Explanation

---

In the last two videos you have sounded, we can use In-context learning the so-called Short-Term memory.

In this video, I will tell you that we can also use long term memory.

We use this rug technology in order to do this.

But of course, before we do this, we should understand this a little bit deeper.

I hope that you understand what Rec is.

I already told you this.

Basically we just ask an lemma question and if the LM does not know the question, the LM will search

the answer in its knowledge and the knowledge is stored as embeddings in a vector database.

The LM can simply browse its knowledge and give us our informations.

The cool thing is right here that this is not token restraint, so the token limit will be completely

similar as it was in our LM, because the LM simply browses our external data set.

In this video I will show you how this works.

So I will show you what the vector database is and what embeddings are, so that you understand how

all of this works before we use it in the next video.

This right here, this website is pinecone.

Pinecone is a company that makes vector databases, and they also have an article that describes us

what vector databases are.

And I like pictures.

You know these in a vector database.

You can simply think that the vector database stores embeddings.

And first of all of course what are embeddings?

Embeddings are simply these numbers.

They are just like tokens.

But the special thing is that these beddings, they will get vector embeddings and they will get stored

in a vector database.

A vector database is three dimensional.

You can see it like here.

And we make this now as simple as possible.

Just think of it this way.

Content will go into the embed models.

The embed models will make this tokens will make this vector embeddings.

And then in the vector database they get stored as this vector embeddings.

And they are stored relatively cool.

Let's just say you go to a party.

And on the party of course, there are a lot of people.

Some people like to talk about the AI.

So the nerds, some people are the cool girls that are dancing on the dance floor and then are some

guys that are drinking.

These are three different clusters.

And these vector embeddings, they are similar.

They make clusters of words that are similar.

So if we embed, for example, stuff all about LMS, the vectors will look relatively similar and the

vector embeddings will get stored in one place.

If we have other words about milk and cows and whatsoever, they get stored in a different space of

the vector database.

So all of these tokens, all of these embeddings, they get stored into this vector database.

And then the LLM can come and search and browse this files.

And if we ask something in ChatGPT, for example, about LMS, ChatGPT will simply browse this vector

database and search in this specific cluster.

In this vector embeddings that talk about LMS so it can browse really, really precise, it will not

browse over there where we talk about milk.

Same thing.

If you are the dad of one of these girls, you will search in the club, not where the AI nerds are

talking.

You would go most likely to the dance floor and search your daughter so you are more efficient.

That's the best analogy that I can think of.

So a vector database is simply the store where we can store this embeddings.

This embeddings are just tokens words.

And the vector embeddings, they are simply clusters of words.

We make similar words into this vector database so that the LLM can find them later in an easier manner.

So that's a vector database that are embeddings.

And in the next video we will see how we can use all of this.