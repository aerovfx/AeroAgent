# 12. What is a API

---

In the next video we will use the zipper API.

We do this via webhook and we do this as simple as possible.

But the APIs we will use APIs over this course over and over and over again.

And that's why we need to look first at what's an API.

So in this video I will show you what an API is.

And in the next video we will use this API or API to hook it into ChatGPT.

And later in the course we will lose a lot of different APIs.

So what's an API?

The best explanation of an API, at least in my mind, is on AWS.

So this is from Amazon.

So what is an API?

APIs are simply software components to communicate with each other using a set of definitions and protocols.

For example, the Weather Barrel software system contains daily weather data.

The weather app on your phone talks in this system via APIs and shows you daily weather updates on your

phone.

So think of it this way you can simply connect to different software pieces, and they simply talk to

each other so you can connect, for example, your phone via this weather app, and then your phone

will send a signal to this weather app.

The weather app will send a signal back and all of this works via the API.

This is an API call that your phone sends out to this app.

What does API stands for?

API stands simply for Application Programming interface.

And in the context of APIs, the word application refers to any software with a distinct function.

Inference can be thought of as a contract of service between two applications.

This contracts defines how the two communicate with each other using requests and responses as their

API.

Documentation contains information on how developers are to structure those requests and responses.

So in the API documentation, we can simply see how these apps talk to each other and we will make this

really quick.

So how do APIs work to make it really, really simple?

We have always a client and a server.

So the application that is sending the request is called the client and the application sending the

response is called the server.

So in the example of the weather app barrels weather database is the server.

And the mobile app or your phone is simply the client.

Then we have a lot of different ways to communicate with APIs, but I think we don't need to know all

of this.

The rest of the APIs.

You can read this yourself if you really want, but for me, this is not really relevant for the stuff

that we do.

So in this video, you have simply learned what an API is.

An API is simply a connection point of two things of two softwares.

For example, if you connect your mobile phone with, for example, this weather app, you have the

client and the server and we can simply talk to each other.

So the client your mobile phone asks to the server something and the server communicates back.

So we can simply plug in one software into another software.

And that's basically an API.

And we will use APIs over the next lectures and also over the next sections.

Because right now we start to develop our own small models, and then we also start to program our own

chatbots.

And we will also include such a chatbot into our website.

So stay tuned.