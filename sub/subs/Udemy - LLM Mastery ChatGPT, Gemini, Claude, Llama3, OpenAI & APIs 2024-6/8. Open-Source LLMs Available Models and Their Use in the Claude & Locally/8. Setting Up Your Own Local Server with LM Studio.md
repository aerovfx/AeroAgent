# 8. Setting Up Your Own Local Server with LM Studio

---

In our studio, we have even the chance and the possibility to host our own local server.

This is important if we want to start to develop our own apps.

Of course, we can also use Olama for this, but nonetheless, I want to show you how we can set up

a server in LM studio.

Every single thing that we have to do is, of course to press on local server.

And here you have every single thing that you need.

So you see here we can make our own local inference server, start a local HTTP server that mimic select

OpenAI and OpenAI API endpoints.

And it's really easy.

You just need to use a model, use the model that you like, for example, this dolphin model.

Then of course you can go on configuration.

You can give a server port.

If I just leave it at one, two, three, four right now, then you have this three things right here.

And of course if you don't know what they are doing, just go with the mouse over it.

I leave them at the default settings.

Then we simply press Start Server.

And of course we need to accept these and boom, there we have our server.

Our server is running now and we can call this server either in Curl in Python, AI assistants in Python,

vision in Python, and also the embeddings in Python.

We can do everything.

If you go, for example, on the chat model in Python, you see how everything works.

We use the OpenAI library.

Then of course the server is our local server, so it's client OpenAI.

But then the base URL, it's HTTP double point slash slash localhost one two, three four and so on.

Version one.

And the API key is simply LM studio.

You can of course also make your own API key, but this should totally work with this server.

The model is of course this dolphin 2.2.9 Lama three model.

And then we can simply copy code and you can run this code wherever you want, for example in Google

Colab.

Or if you want to make a wrecked chatbot that runs locally, you can do all of it.

And of course, you can also do the GPU offload more or less, or context length and so on.

This server is really easy to set up and you can run it wherever you want.

You can even try this with vision if you want to stop a server, of course, stop server, you can use

different models.

The Pi three mini for example.

I think this model is also multimodal, so this should also have vision.

Then you simply as soon as this is loaded, you can simply start your server with that if you really

want.

And then of course every single thing is the same once again.

And one last thing that I want to show you down here.

There are my models.

If you click on them, you can simply search through your models and then you can delete them.

If you do not need them.

For example, if you think the Pi three model is no longer useful, just press right here and you can

delete this file, because this files can add up relatively quickly because they are relatively big.

So in this video, you have seen how easy it is to host a local server on your PC.

Just press on start server.

Of course first you need to use a model and then you can develop whatever you like in Python or in curl,

you have every single thing set up in no time whatsoever.

Of course, we will soon start to create our own apps, and then every single thing will make more sense.

Because in order, if you want to start a server, we should develop something.

But in the next video I will show you one last thing.

I want to show you your own fine tuning of open source LMS, because we have a few possibilities and

I think you should know this possibilities.

So see you in the next video where we talk about the fine tuning.

And then then we will start to develop our own stuff.