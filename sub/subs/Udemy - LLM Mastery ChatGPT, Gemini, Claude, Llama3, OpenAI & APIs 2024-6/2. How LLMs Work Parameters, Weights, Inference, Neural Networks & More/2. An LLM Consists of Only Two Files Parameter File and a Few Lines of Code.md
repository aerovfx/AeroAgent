# 2. An LLM Consists of Only Two Files Parameter File and a Few Lines of Code

---

In this video, I want to tell you that an LM is basically just two files.

Until now, I always explain the LM like really, really easy.

It's simply an AI trained on a lot of text.

You ask questions, the AI gives you answers, and it's also correct.

But we want to do a deep dive.

So first of all an LM is just two files.

And I make an example with llama two.

Llama two is the LM from meta.

So llama is basically the LM from meta.

We have llama two.

We have llama three.

Right now we have llama three also.

And llama is open source.

Open source simply means that everybody can see the weights and all the stuff that is inside of this

LM and in this LM there are just two files.

And yes, this is llama two.

So this is the second version of llama.

Right now we also have llama three.

And this is a model with 70 billion parameters.

Don't worry we will get into all of these.

And like I said this is open source.

So we can see what's in there.

And in this models there are just two files.

There is one file.

And we call this file P because this is simply the parameter file.

And there is a second file.

And the second file is a file with some code.

The code is in C so it's written in C.

You can also write this stuff in Python.

But in this example in llama two the 70 billion version, we have a file that is written in C.

And this file, this parameter file is 140GB.

So 140 gigabyte is this parameter file.

Now why is this 140GB.

Because every single parameter needs two bits in order to be saved.

So 70 billion parameters times two bytes is 140GB.

Besides this 140GB of parameters, we need this file.

This is the run file.

And here we have just 500 lines of code.

So I simply write 500 lines of code.

Yes.

Excuse me.

This is like really, really bad, but I hope you get it.

So 500 lines of code in order to run this parameter file.

So you understand we only need these two files in order to run this LM.

And then we can make inference with these files.

Inference simply means that we can run this LM and how we can run it.

We can either download it and run it locally, or we can also run it in the cloud.

Like I said, this is open source.

We don't need to pay.

I will show you a lot of cool stuff how we can use this LMS totally for free.

And of course they are also uncensored and your data is safe if you run this locally.

This is like really, really cool in my mind.

One of the fastest way to run llama is with grok.

Grok is simply an interface that gives us an LPU.

So we make inference with a chip that's a language processing unit.

Don't worry, we will go in all to this later.

So here we have a chip that is especially designed for LMS.

And here we can use some open source LMS for example GEMA from Google, llama from meta or Mistral from

Mistral.

I just want to leave it at llama three, the 8 billion model, just to show you how -- fast this is.

If I press write here, you see, boom!

We have a lot of output in no time whatsoever because this LPU is really, really efficient in inference.

And of course also this model is relatively small.

And like I said, we can also run this locally, but this is one of the simplest ways to run an LM in

the cloud, to run an open source LM in the cloud.

And this is forever totally for free because these models are open source.

So one more time, back on to this nice little picture.

We have only two files.

And we can run these files either in the cloud or we can run it locally.

Most of the people they only know ChatGPT ChatGPT comes from open AI, and open AI is not open source.

Even if the even if the name gives us a hint that it's open source.

But it's not on OpenAI, we cannot download these two files and we can not run it locally.

If we want to use the models from OpenAI, we can either work over their API or in the standard ChatGPT

interface.

So in a web interface, if you work in the web interface, you have either to pay for the newest models,

if you want to do a lot with them, and it doesn't matter if it's cheaper.

For Omni or GPT five or GPT six, whatever model is there, as soon as you see the course, I would

guess that we have to pay if we want to do a lot of stuff in the chat.

GPT interface.

Yes, we can do some stuff for free, but not everything.

And if we want to work over their API, we have also to pay because these are the closed source LMS.

So they don't give us these files.

We cannot download it.

If we use open source LMS, we can absolutely download this LMS and run it locally.

And the coolest part is if we can download this files, we can also fine tune it and do a lot of stuff

with them.

And the second plus for this open source models is of course if we run them locally, no company can

train on our data.

If we insert data into the standard chat GPT interface.

This companies can train on our inputs and our data is not private.

These are just some little side notes.

You should understand this if we talk about LMS.

So in this video you have learned what an LM is.

An LM is simply two files.

It's most of the time a parameter file and a run file, and the run file is written in C or in Python.

And it's really, really simple.

We can do inference so we can run it either locally or in the cloud.

Most of the closed source LMS, they don't give us of course, the files to download it.

So we have to use them in the cloud.

We have to use them in our interface.

And we need most likely to pay for the newest and coolest stuff.

And we can also not customize it.

And our data is eventually not safe.

We can also work over their APIs, and if we work over their APIs, it's nice.

Our data is safe.

But of course we also have to pay for the APIs.

And don't worry if you don't understand a word right now, because we will break every single thing

down over this course.

So long story short, an LM is just two files.

It's two simple files.

We can download them, we can run it locally, or we can run it in the cloud.

But the magic happens in this file.

In the parameter file happens the magic.

And I want to tell you how this magic works in the next video.