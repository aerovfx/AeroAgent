# 7. Other Possibilities of the Transformer Architecture Mixture of Experts Explaied

---

You have learned what the transformer architecture is and how it works.

We throw words in it, the words get divided into tokens and we get our right output back.

And these are all just dreams.

You can simply say that you use text.

Woop!

So we have here some text.

Then we throw it in the transformer architecture and then we get once again text is output back.

This can also be code or numbers or whatever.

This is just how this works.

And I have to tell you this transformer architecture you have sown, we have for example models with

7 billion.

This is for example the small llama model.

We have versions with 70 billion models.

And the newest llama model has 400 billion parameters.

So these things are really, really big.

And this is also a little bit inefficient.

And I have to tell you this is not the only way how we can use the transformer architectures in LMS.

We have also the possibility to use a lot of small experts.

So if we go back one more time, if we use the biggest llama model as example, these are transformer

architecture, this zip file, this has 400 billion parameters.

And this is of course really, really big.

And it turns out that these models get a little bit inefficient and how we can make them more efficient.

We have the mixture of experts.

And here on Hugging Face we have a nice explanation.

The explanation is always easiest to understand if we use some pictures.

So here we have some pictures.

It's not that complicated, but I think I can make it even easier.

Of course.

Once again in paint.

So this is the standard transformer architecture with raw text in.

We get text back out all from one single model.

Now I will show you what a lot of new companies are doing.

Of course we start with text.

After that we go to something that is called a router.

And the router simply makes a decision.

And what decision makes the router?

We call this error.

The router makes the decision what soup expert we need.

We have a model that is called eight times 7 billion.

It's called mixture eight times 7 billion.

And what does this means.

This simply means that we have eight different small experts.

So I just make a handful just to make this a little bit faster and easier.

So the concept is that we have different sub experts.

Every single expert has 7 billion parameters.

So they are small, they are efficient and they are fine tuned.

This expert is for example fine tuned for code.

This expert is for example fine tuned for creative writing.

This expert is for example fine tuned for math for whatever.

And the router decides what expert we need to use.

If I use a query that needs, for example, Python code as output, we get.

Of course, our coding expert and the small efficient coding model will give me the answer at the end.

But if, on the other hand, I use an example where we want to have a creative story about a fox that

plays in the wood, I will have my writing expert and the writing expert give me the output at the end

of the day.

So it simply means we have eight different experts, eight different models.

Every model has 7 billion parameters, and it turns out that this architecture is more efficient than

the standard architecture, where we have one overall big, big, big model, and we have more and more

and more models that work with this transformer architecture.

We have, for example, Mistral.

Mistral has the model mixture eight times 7 billion.

That's the stuff that I have shown you here.

We have for example models from snowflake.

Snowflake has, I think 22 different experts.

Also ChatGPT for Omni has different experts and of course all of these experts, they are relatively

big on GPT four.

We don't really know it because this is not open source.

But from Mistral we know it.

The small experts have 7 billion parameters.

Llama, on the other hand, don't use this mixture of experts.

They just use one big model.

The 400 billion model is their biggest model.

But I think also they will bring something with mixtures of experts.

So in this video you have learned that we can use the transformer architecture in different ways.

We can use one gigantic model, but it turns out that this is a little bit.

Inefficient.

We can also use smaller sub experts inside of one big file and the sub experts.

They are a little bit more efficient in one single task, and a router decides what small little sub

expert we need in order to get our output.

We don't really need to understand what expert to trigger when, because all of this works completely

automatically.

And in the next video I will show you the fine tuning.

Until now we just talked about the pre-training.

And the pre-training is the expensive stuff.

The pre-training only occurs like 1 or 2 times a year.

It depends a little bit, but the fine tuning is much more efficient than we can do this nearly daily.

It's cheap, but it works really, really good.

So until now, the pre-training.

This was the heavy lifting.

Now comes the fine tuning in order to get better outputs.