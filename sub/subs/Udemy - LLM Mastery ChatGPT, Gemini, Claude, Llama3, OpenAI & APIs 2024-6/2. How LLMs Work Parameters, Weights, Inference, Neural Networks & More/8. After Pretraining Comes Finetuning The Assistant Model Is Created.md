# 8. After Pretraining Comes Finetuning The Assistant Model Is Created

---

So you understand how the pre-training works.

We use a lot of text from the internet.

We throw a lot of GPU on it, we get our zip file and we can dream out of our zip file.

And I have to tell you right now, we don't get really good outputs.

We get really, really bad dreams out of this LMS.

But in order to get better dreams, of course we can do the so-called fine tuning.

And it doesn't really matter what transformer architecture we use.

If we use, for example, the mixture of expert or also one big model, all of them get fine tuned,

they get fine tuned on specific data.

And this specific data that is generated by humans.

And also with a little bit of AI assistance.

Now I will show you how this works because out of this normal zip file we call this models the pre-trained

models.

We need to train them.

We need to fine tune them in order to get our assistant models.

The assistant models are helpful, and if you type something in a chatbot, you have your assistant

model that is already fine tuned.

Yes, we can do the fine tuning ourselves.

We can fine tune for a specific use case.

And now I want to show you how the fine tuning works.

Later in the course, we will also see how we can fine tune our self.

So we have our pre-trained model and in order to fine tune it we need to give it examples.

We need to feed examples that human make.

And here we don't use 100 terabyte of text.

We just use roughly a 100,000 examples, examples that are structured like this.

We have a user and we have an assistant.

Our goal is to create the assistant.

And we want to show the assistant how it has to behave.

I could, for example, make a fine tuning on text that is something like this.

I give the question, so can you write a brief introduction about the significance of the term blockchain

in financial technology?

Please use examples that pertain to the application of blockchain in payment systems and inside relevant

research.

So this is the question and I also give the answer.

Blockchain is a decentralized and distributed database technology that records transaction in a way

that ensures transparency, security and immutability in financial technology and so on.

My main point is right here that we need to show the model how it should behave.

We give the model question and we give the model answers.

And of course, we simply structure it so that the model can learn.

The model can see, hey, the user asks questions like this and it gets expected that an output is structured

like this.

The better this fine tuning data is, the better the output gets.

And you see here is quality much, much more important than quantity?

First we had 100TB of text and we have more than enough quantity.

Now it's all about quality.

We need a 100,000 examples that are really, really specific, really really good.

And we can fine tune these models on specific text, on specific code, on specific whatsoever.

And the more narrow our fine tuning is, the more an expert our model gets in one specific use case.

That's also why we have this mixture of experts.

It turns out that if we use a smaller model and fine tune it for coding, it gets more efficient on

coding.

And if we use this model, it is really, really good.

It is more efficient to fine tune these smaller models than to fine tune one gigantic model with like

for example, a million different inputs.

It it gets a little bit less crisp this way.

That's why we have this mixture of experts.

But the concept is always the same.

We give the model instructions how it should behave.

We tell the model, hey, the user gives questions like this, and the assistant you will.

We will be the assistant.

The assistant should answer in a manner that is somehow like this.

And in this process, the model learns how it should behave.

And this only takes up, for example, a few minutes, a few hours, a few days.

It depends on the size of the training data, of course.

But you see, this is really more about quality than quantity.

This is a lot cheaper.

And all of these models get fine tuned all day long.

And we can even do this ourself later.

Of course, I will show you how to find fine tuned models, how to fine tune models yourself.

And I have to tell you that OpenAI does fine tuning all day, every day long.

ChatGPT gets minor minor updates all the time.

I can't tell you when I think it occurs, like once a week, sometimes a little bit bigger, sometimes

a little bit smaller, but they fine tune all day long.

This occurs all the time.

The pre-training is big here.

We need a lot of computing power.

This occurs as soon as you get a new version.

So for example GPT three, then 3.5, then four, then 4.5, then five.

I don't know what models come around the corner, but then we have our pre-training new and the fine

tuning occurs all day long.

You have always smarter models nearly every single day, and you can fine tune yourself.

You just need to structure the data the right way and throw a little bit of computing power on it.

And of course, we can borrow computing power from Huggingface or use local machines, or use Google

Colab.

You will learn all of it.

And I already told you that this was created by humans, but we can also use AI to create these things.

So of course the quality needs to be nailed down.

So of course humans will control this.

This data sets are not that big.

So humans can do this.

So first humans write a lot of stuff and then you can do something like this.

You can go into ChatGPT and make these things for yourself.

I can tell for example something like this.

Make a similar example but with code and I click run.

By the way, this is the playground.

Of course you will learn about this playground later.

And here we have another example.

So of course we start with user.

This right here is now written in Python.

This is not a perfect example.

I just want to show you how this works and then how the assistant should behave.

So this is now also written in Python.

This was a stupid example, but I hope you get what I mean.

We can make really, really similar output with the LMS that has simply help us.

So we create a smaller data set for fine tuning.

We always tell user and assistants.

So user ask the question and the assistants behaves a specific way.

And then we train the model on this.

And the model learns how we should behave.

And here is quality more important than quantity.

And that's why humans are involved a lot in this fine tuning process.

We can like push the LMS in the direction how they should behave.

If we don't do this, they just dream stuff that makes absolutely no sense.

And like I said, OpenAI does this over and over and over again and we can fine tune also really small

data.

You have shown in a previous lecture that these models hallucinate that the model understands who Tom

Cruise's mother is.

But you also saw that the model doesn't understand who the son of Marilyn Pfeiffer is.

And OpenAI simply did a little bit of fine tuning.

So they gave a few examples where they told the model, hey, the mother of Tom cruise is XYZ.

Hey, the son of Mary Lee Pfeiffer is XYZ.

And after this fine tuning one of a sudden this works.

And if we ask right now who is the mother of Tom cruise, then we get the right answer because OpenAI

simply did a little bit of fine tuning.

So in this video you have learned how the fine tuning works.

We use our base model, the base model that was pre-trained.

The base model always hallucinate.

It doesn't know anything.

But then we use the fine tuning and in the fine tuning we are a lot more specific.

We give really specific nailed down examples that people make people in combination with machines,

and we train the model on this example so that the model understands how it should behave.

This is a lot cheaper, a lot faster.

This occurs nearly every single day because it's relatively cheap.

And so the models learn.

We can even bake new knowledge into these models if we simply train it.

You have shown that the model has learned right now who the mother and the son is, and quality beats

quantity in this process.

So we need to understand that this process is more efficient, is faster, but we need quality.

And this is also an iterative process.

If the output is not perfect, we fine tune and tweak again and again and again and again until the

model behaves how we want.

So you saw the pre-training, the fine tuning.

And in the next video I will show you the last step the reinforcement learning.

So see you in the next video.